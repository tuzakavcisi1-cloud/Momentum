# ORTAM.md — Momentum · KANLA YAZILI ORTAM UYARILARI

> 🔴 **BU DOSYA AÇILIŞTA TAM OKUNUR** (`DURUM.md` §2 adım 1). `BORCLAR.md` ve `KAPILAR.md`'den
> **farkı budur:** onlar başvuru malzemesidir, bu liste **operasyoneldir** — okunmadan yazılan komut
> mayına basar. Ölçülmüş gerekçe: aşağıdaki maddelerin her biri bu projede **en az bir kez ısırdı**.
> **Tavan: ≤ 16 KB** (`belge-tavan-kapisi.py` kapsamında). Aşarsa budanmaz — **mekanikleşen madde
> silinir**: bir mayın kapıya dönüştüğü an burada yaşamayı bırakır (K73 doktrini).
> **Kaynak:** `DURUM.md` §7'den **2 Ağu 2026 (oturum 49)** ayrıldı, Onur kilitledi. §7 numarası ve
> başlığı `DURUM.md`'de **yönlendirici olarak DURUYOR** — `DURUM.md §7`'ye adıyla atıf yapan
> **6 canlı satır ölçüldü** (`DURUM.md`:13 · `KAPILAR.md`:30 · `GOREV-A8`:54,208 · `GOREV-A9`:369 ·
> `GOREV-A9b`:318,320); bölümü silmek `kanonik-kopya`nın kardeşi **sarkan atıf** sınıfını doğururdu.

---

- 🔴 **COWORK BULUTTA UTC İLE KOŞUYOR — TARİH ONUR'UN TAKVİMİNDEN 3 SAAT GERİDE [ölçüldü o39, o66'da yeniden doğrulandı: bulut UTC 13:17 ↔ cihaz 16:17 +03].** **00:00–03:00 arası her oturum bir gün geriye tarih yazar.** o39 bu kusuru fiilen üretti (`K83` checkpoint'i bir gün geri yazdı; `oturum-sagligi.py` `D1` zaman ayağı yakaladı ⇒ append-only iki dosyaya **düzeltme kaydı** girildi). 🔴 **Kural: tarih ortam beyanından OKUNMAZ, cihazdan ÖLÇÜLÜR** — `Get-Date -Format 'yyyy-MM-dd'` (ya da `TZ='Europe/Istanbul' date`). Anlatım: hafıza.
- **Claude Code DAİMA `Momentum` kökünden açılır** (üstten açarsan `.mcp.json` görünmez, dart MCP yüklenmez).
- **Cowork→PowerShell köprüsü `$` değişkenlerini SİLİYOR** ve iç içe tırnakları bozuyor ⇒ `$` gönderme, **Python betiği yaz**.
- **Commit mesajına ÇİFT TIRNAK yazma** (PowerShell argümanı böler, commit sessizce düşer); sonra `git log --oneline -1` ile SHA'yı doğrula.
- **git'te `--no-optional-locks` ZORUNLU.** Commit **yalnız Desktop Commander** ile; `device_bash`/mount **YASAK**. **PUSH ONUR'DA.**
- 🔴 **`cmd /c "... & echo %ERRORLEVEL%"` KÖRDÜR — SAHTE `EXIT=0` VERİR [ölçüldü, oturum 33].** `%VAR%` **ayrıştırma anında**, komut koşmadan önce genişler. Bu kusurla üç kapı sahte `0` bildirdi; radar gerçekte **2** dönüyordu. **DOĞRUSU: `cmd /v:on /c "... & echo !ERRORLEVEL!"`.** Kör kapı sınıfının **ölçüm katmanındaki** hâli: yeşil gördüğünü sanan el, hiç koşmamış bir kapıyı geçmiş sayar.
- **Cowork↔masaüstü köprüsü oturum ORTASINDA düşebilir** (`device not connected to the bridge`; oturum 34'te internet kesintisinde oldu, `mcp__remote-devices__*` araçlarının **tamamı** düştü). Uzun hafıza yazımını tek parçada yapma; ölçümleri köprüden **bağımsız** bir yere (Cowork projesi) de yaz. K60 *dosyanın* yarım kalmasını engeller, **oturumun** yarım kalmasını engellemez.
- 🔴 **`os.replace` bu makinede `WinError 5` verebilir** — hedef de kaynak da **kilitli olmadığı hâlde** (ölçüldü: her ikisi de `rename` edilebiliyor, hedef `r+b` açılıyor). Windows'un klasör/fidye koruması *var olan dosyayı değiştir* desenini engelliyor, düz `rename`'e izin veriyor. **Çalışan yol:** `rename(hedef→.yedek)` → `rename(.tmp→hedef)` → **sha doğrula** → `.yedek` sil (adım 2 patlarsa yedek geri alınır). K60'ın atomik takası bu makinede **üç adımlı yedekli takas**tır. 🟢 **KAPSAM DARALTILDI [o66'da ölçüldü]:** bu yalnız **Windows/PowerShell** yolu içindir. **`device_bash`'in Linux VM'inden mount'a yazarken `os.replace` ÇALIŞIYOR** (kontrollü test: yeni yazım ✔ · üzerine `os.replace` ✔ · düz üzerine yazma ✔ · `os.remove` **PermissionError**) ⇒ o eldeyken K60 takası **tek adımlıdır**, `.yedek` artığı **doğmaz**.
- 🔴 **`python` stdout bu makinede cp1254** — `⇒` gibi bir karakter yazdıran betik `UnicodeEncodeError` ile **kabuğu öldürüyor** (ölçüldü: PowerShell süreci komut ortasında düştü). Zorunlu: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`. Kusur **stdout**'ta, dosyada değil.
- **`device_stage_files` BAYAT KOPYA sunabiliyor** (oturum 28; 30'da tekrarlanmadı) ⇒ stage'lenenin **sha'sını karşılaştır**; tutmuyorsa `read_file` kullan.
- 🔴 **YOL SAF ASCII KALMAK ZORUNDA [K56].** Türkçe karakter dört zinciri kırdı (`build_runner`, `flutter analyze`, AGP, `.ps1`). Boşluk suçsuz, junction çözmez. Ayrıntı: `KANIT/slice-3b/ORTAM-YOL-KISITI.txt`.
- 🔴 **`findstr` AYNI DOSYADA bir dizgeyi bulup diğerini KAÇIRABİLİYOR [ölçüldü, o46].** Aynı satırdaki iki desenden biri bulundu, öteki bulunamadı; **bayt-özdeş kopyası** başka adla arandığında ikisi de bulundu (dosya UTF-8/LF/BOM'suz — Python ile ölçüldü). Ayrıca `/s` **çıplak dizin** yolu (wildcard'sız) verilirse **sessizce 0** döner. ⇒ **`findstr` ile YOKLUK ölçen her ayak, AYNI dosyada bir VARLIK pozitif kontrolü koşmak ZORUNDADIR.** Vakalar: hafıza o46.
- **Git Bash/MSYS, `cmd /c`'deki `/c`'yi POSIX yol sanıp `C:/` diye YENİDEN YAZIYOR** ⇒ ham `cmd /c` içeren komutlar **PowerShell'den** koşulur.
- **Başka bir el çalışırken `git add -A` YASAK** — commit'lenmemiş işini kör alır (ölçüldü: `dee6dbc`). Yol belirterek `git add <yol>` yap.
- **`flutter test --platform chrome` bu ortamda SONUÇ ÜRETMİYOR** (iki ölçüm: 7 dk ve 9,8 dk) ⇒ web test ayağı `[DOĞRULANMADI]`.
- **pub.dev HTML sayfaları BAYAT** — kanıt yalnız `/api/` ucudur (spec Z10). · `.ps1`'e Türkçe yol literali yazma. · `kasif` skill'ini **Cowork çağıramaz**; Onur `/kasif` yazar.
- 🔴 **GIT SATIR SONU ÇEVİRİYOR — mekanizma `core.autocrlf` DEĞİL, `.gitattributes` [o64'te ölçüldü].** Repo-yerel `core.autocrlf` **TANIMSIZ**; çeviriyi `* text=auto eol=lf` yapıyor ve `autocrlf`'i **ezer**. Kanonik belgeler diskte **CRLF 0**, buna karşın `git diff` hâlâ *"CRLF will be replaced by LF"* diyen dosyalar sayıyor ⇒ **çeviri gerçek, ATIF yanlıştı.** 🔴 **BEYAN EDİLMİŞ SINIR:** Windows'un **global** `autocrlf` değeri ÖLÇÜLEMEDİ (`device_bash` ayrı bir Linux VM, farklı `HOME`) — sonucu değiştirmez. 🔴 **KORUMA DÜŞMEZ:** çalışma kopyası ↔ HEAD blob **bayt karşılaştırması bu ortamda tek başına KÖRDÜR**; kimlik ölçen her araç **LF'e normalize etmelidir**.
- 🔴 **`io.open(yol,"w")` DOSYAYI ÖNCE BOŞALTIR** — encode hatası gelirse dosya **0 bayt** kalır (oturum 31: 542 KB arşiv gitti, `git restore` kurtardı). **Python'da `"\ud83d\udd3b"` iki `\u` kaçışı olarak yazılırsa BİRLEŞMEZ**, yalnız vekil karakter olur ve `encode` patlar; emoji için `\U0001F53B` yaz. Kural: **K60 atomik yazım**.
- **`flutter test` Desktop Commander kabuğunda ÇÖKÜYOR** — `%PROGRAMFILES(X86)% environment variable not found` (ölçüldü: değişken `os.environ`'da **yok**, dizin diskte **var**). ⇒ alt sürece `PROGRAMFILES(X86)=C:\Program Files (x86)` **enjekte et**; kalkanla test 36/36 geçti. Bu bir **ortam** kusurudur, ürün kusuru değil.
- 🔴 **`uiautomator dump` uygulama henüz çizilmeden çağrılırsa "null root node" verir ve DOSYA OLUŞMAZ [K86, oturum 40].** Sabit bekleme değil, çıktıda `"dumped to"` dizgesi görünene kadar **yoklanır** (tavanlı).
- **`flutter` bu makinede `.bat`'tir [K86]** — Python `subprocess` PATHEXT'i çözmez ⇒ doğrudan çağrı `WinError 2` verir; tam yol `C:\src\flutter\bin\flutter.bat` kullanılır.
- **Commit mesajındaki çift tırnak yasağı yeniden ISIRDI [K86, oturum 40]** — bir commit `pathspec` hatasıyla düştü, ağaç sağlam kaldı; kural aynı kalır (§ üstteki madde).
- 🔴 **`verify.ps1` ÇALIŞAN BİR `Momentum.Api` VARKEN KOŞULAMAZ [ölçüldü, oturum 50].** Backend ayakta iken `verify.ps1` **EXIT 1** ve **36 hata** verdi; hepsi `MSB3026`/`MSB3027`: *"Dosya şunun tarafından kilitlendi: `Momentum.Api (<pid>)`"* — `bin\Debug\net10.0\Momentum.{Application,Domain,Infrastructure}.dll` üzerine yazılamıyor. **Ürün kusuru DEĞİL.** ⇒ **Sıra: cihaz kanıtı (backend ÇALIŞIR) → backend KAPATILIR → `verify.ps1`.** Kapatma **ölçülür** (`netstat -ano | findstr :5298` **boş** dönmeli), varsayılmaz. 🔴 **Kapatmayı Cowork YALNIZ Onur'un açık izniyle yapar; YENİDEN BAŞLATMAZ (K80 ayakta).**
- 🔴 **MUTANT KOŞUMUNDA `git restore` İLE GERİ ALMA YASAK — `core.autocrlf` onu bayt-özdeşlik için KÖR kılar.** Doğru yol (oturum 50'de 17 mutantta koştu, üç dosyanın da sha8'i özdeş döndü): dosyayı `rb` ile **ikili yedeğe** al → yamayı **bayt düzeyinde** uygula (dosya CRLF ise desendeki `\n`'leri `\r\n`'e çevir, yoksa eşleşme sessizce **0** olur) → kapıyı koş → **yedekten `wb` ile geri yaz** → `sha256` ile özdeşliği **ölç**. Referans koşucu: `KANIT/A11/_mutant_kosucu.py`.
- 🔴 **`cmd /v:on` içinde `%DEGISKEN%` YİNE PARSE ANINDA GENİŞLER — `set ADB=...` sonrası `%ADB%` "is not recognized" verir [ölçüldü, oturum 50].** `!ADB!` yaz. Bu, `!ERRORLEVEL!` dersinin (oturum 33) aynısının **değişken tanımlamaya** uzanan hâlidir. Ayrıca aynı komutta `^|` yazılırsa pipe **cmd'de değil `adb shell`'in içinde** çalışır ve cihazda `findstr` **yoktur**; `< nul` da cihaza gider.
- 🔴 **`Select-String`/`ForEach-Object` içinde `$_` KÖPRÜDE SİLİNİYOR** — `.LineNumber` diye ayrıştırılıp `ParserError` veriyor (oturum 50'de iki kez ısırdı). Bu, ORTAM.md'nin *"`$` gönderme, Python betiği yaz"* maddesinin **PowerShell boru hattına** uzanan hâli: filtreleme/tarama işi için `.py` yaz.
- 🔴 **BACKEND'İ BAŞLATMANIN TEK DOĞRU YOLU — REPODA BETİK YOK, `appsettings.json`'da BAĞLANTI DİZESİ DE YOK [ölçüldü, oturum 50].** `Program.cs:24` `GetConnectionString("Momentum")` okur; **verilmezse host DB'siz açılır** (slice-2b1 `D1`, kırmızı çizgi 1) — port **yine dinler**, `netstat` LISTENING gösterir, ama Postgres'e **hiç bağlanmaz**. Oturum 49'un *"127 s'de kuyruk boşalmadı"* vakası tam buydu (`28P01`). Reçete (ayrı pencerede, açık kalır): `cd C:\dev\Momentum\src\backend\Momentum.Api` → `$env:ASPNETCORE_ENVIRONMENT="Development"` (K61; yoksa her istek 401) → `$env:ASPNETCORE_URLS="http://0.0.0.0:5298"` (emülatör `10.0.2.2` ile gelir; `localhost` dinlersen cihaz göremez) → `$env:ConnectionStrings__Momentum="Host=localhost;Port=5432;Database=momentum;Username=momentum;Password=momentum_dev"` (değerler `docker inspect momentum-postgres` ⇒ `POSTGRES_DB/USER/PASSWORD`) → `dotnet run`.
- 🔴 **BACKEND'İN HAZIR OLDUĞU PORTLA ÖLÇÜLMEZ.** Zorunlu üçlü: `/health/live` **200** · `/health/ready` **200** (Npgsql readiness; 503 ⇒ bağlantı dizesi yok/yanlış) · `POST /v1/sync` başlıksız **401** ve `X-Momentum-Dev-User` ile **200** (K61 kalkanı canlı). Hazır betik: `KANIT/A11/_backend_dogrula.py`.
- 🔴 **`/v1/sync` PROBUNDA `clientId` GEÇERLİ BİR GUID OLMAK ZORUNDA [ölçüldü, oturum 50].** `"cowork-probe"` gibi bir dize gönderilince uç **500** döndü ve bu **backend'in değil PROBUN kusuruydu**; GUID'e çevrilince aynı istek `401`/`200` verdi. **Ölçüm aracının kendi kusurunu ürüne yazmak, kör kapının aynadaki hâlidir.**
- 🔴 **MOUNT'TA TÜM-AĞAÇ `git status`/`git diff` `device_bash`'in 45 sn TAVANINI AŞIYOR — EXIT 124 VE ARDINDA `.git/index.lock` BIRAKIYOR [o63'te ölçüldü, o64'te AYNEN TEKRARLANDI — o64 devir notunun açık uyarısına rağmen].** 🔴 **Sandbox kilidi SİLEMEZ**, yalnız `mv` ile `_SILINECEKLER/<oturum>/`'e alabilir; kalıcı silme Onur'da. **ÇALIŞAN DARALTILMIŞ ÖLÇÜM:** `diff --name-only -- <tek dosya>` · `ls-files --others --exclude-standard -- <dizin>` · `log --oneline -1` · `rev-list --left-right --count origin/main...HEAD` — saniyeler sürer (ama alttaki maddeyi oku). **`DURUM.md` §2 adım 7 bunu HÂLÂ BİLMİYOR** (`B-O64-4`).
- 🔴 **DAR KOMUTLAR DA KİLİT BIRAKABİLİYOR — AMA YALNIZ SERİ KOŞUMDA [o66'da ölçüldü].** Üstteki maddenin *"çalışan daraltılmış ölçüm"* listesi **tek çağrı** için doğrudur: `diff --name-only -- <tek dosya>`, `ls-files --others -- <dizin>`, `log --oneline -1` **tek tek** koşulduğunda üçü de **kilit bırakmadı** (kontrollü izolasyon testi). 🔴 **AMA 23 dar çağrının arka arkaya koştuğu turda 0 baytlık `.git/index.lock` OLUŞTU**; hangi çağrının bıraktığı **İZOLE EDİLEMEDİ** — `[TAHMİN]`: `timeout` bir git sürecini index yenileme ortasında öldürdü. ⇒ **Her git ölçüm turunun SONUNDA `.git/index.lock` YOKLANIR**; varsa `_SILINECEKLER/<oturum>/`'e `mv` edilir. Sandbox **silemez**; bırakılırsa Onur'un bir sonraki `git add`/`commit`'i patlar.
- 🔴 **MOUNT'TA `os.remove`/`unlink` YASAK — `hafiza-dizin.py` BU YÜZDEN `EXIT 1` VERİR AMA İŞİNİ YAPMIŞTIR [o65'te ölçüldü].** Araç `_atomik_yaz`'ın **son** adımında (`os.remove(yedek)`) `PermissionError: [Errno 1]` ile düşer; asıl yazım **tamamlanmıştır** (o65: dizin bloğu `K163`–`K165`'i fiilen kazandı). 🔴 **`EXIT 1` burada *"iş yapılmadı"* DEMEK DEĞİLDİR — çıktıyı ölç, çıkış koduna bakıp geri dönme.** Geride `PROJE_HAFIZA.md.yedek` **artığı** kalır: `mv` ile `_SILINECEKLER/<oturum>/`'e alınır — bırakılırsa `tek-kopya-kapisi` ve `git status` onu görür.
- 🔴 **ORTAM-DUYARLI ARACI BULUT KOPYASINDA KOŞMA — KANONİK ÖLÇÜM YERİ CİHAZDIR [o65'te ölçüldü].** `sayi-tazeligi.py` **bulut kopyasında KIRMIZI** (*"`izolasyon-olc.py` için 9/9 diyor, araç FİİLEN 11 vaka taşıyor"*), **cihazda TEMİZ** verdi. Sebep kusur değil **ortam**: `izolasyon-olc.py`'nin `T` ayağı playwright ister; bulutta playwright **var** (11), Onur'un makinesinde **yok** (9) — `DURUM.md` §6'nın *"buradaki sayı cihazda ölçülendir"* beyanı **doğrulandı** (`B-O62-3`). ⇒ **Kapı hükmü, koştuğu ortamın hükmüdür.** Bulut kopyası (`git archive HEAD` + commit'siz dosyalar) **okuma ve ajan denetimi** için doğrudur; **kapı hükmü için cihazda tekrar koşulur.**
- 🔴 **`computer_*` İLE TERMİNAL KONTROLÜ YOK — 'click' KİPİ [o65'te ölçüldü].** `computer_resolve_access` birebir: *"Terminals and IDEs can only be granted in **'click' mode** — you can see and left-click, but **cannot type, press keys, or paste**."* ⇒ Desktop Commander'ın **ikamesi değildir**; PowerShell açılıp komut **yazdırılamaz**. DC yoksa commit ve `docker`/`netstat`/`adb` ölçümü **Onur'da ya da Claude Code'da** kalır.
- 🔴 **`PROJE_RADAR.jsonl`'a (ve LF'li HER deftere) EKLEYEN EL `"ab"` İKİLİ MODDA YAZAR [ölçüldü, oturum 60].** `open(yol,"a",encoding="utf-8")` Windows'ta **metin modudur**: yazdığın `\n` diske `\r\n` iner ve dosyaya **tek bir CRLF** sızar — `dosya-kimlik.py` bunu **CRLF=1 ⇒ HÜKÜM KIRLI** ile yakaladı. Bu, `core.autocrlf` maddesinin **yazma tarafıdır** (orada `git restore` çeviriyordu, burada `open`). **Onarım append-only'yi BOZMADAN yapılır:** ikili oku → yalnız **kendi** son iki baytını `\n` yap → `yeni[:-1] == eski[:-2]` diye **bayt-bayt doğrula** → `dosya-kimlik.py` ile CRLF=0 **ölç**. Düzeltme deftere ayrı bir `asama: olcum-duzeltme` satırıyla yazılır, önceki satır **düzeltilmez**.
