# ============================================================
# Momentum — .gitignore
# ============================================================

# ---- Sırlar / ortam (ASLA commit edilmez) ----
.env
*.env
.env.*
!.env.example
appsettings.*.Local.json
appsettings.Local.json
secrets.json
*.pfx
*.p12
*.key
*.pem
key.properties

# ---- .NET / ASP.NET Core ----
# K107 (oturum 46, Onur kilidi): `[Dd]ebug/` ve `[Rr]elease/` SILINDI.
# Olculdu: bin/Debug ve obj/Release ZATEN yukaridaki bin/ ve obj/ ile yakalaniyor;
# o iki desen bu depoda tek bir .NET ciktisini bile korumuyordu (git status --ignored
# ile sayildi: 0 eslesme) ama Android'in `android/app/src/debug|release/` KAYNAK
# kumelerini SESSIZCE yutuyordu -- A10'un iki dosyasi `git add -f` ile girmek zorunda kaldi.
bin/
obj/
x64/
x86/
*.user
*.suo
*.userosscache
*.dbmdl
project.lock.json
artifacts/
.vs/
TestResults/
*.received.*
coverage*.json
*.trx

# ---- Flutter / Dart ----
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.fvm/
*.iml
doctor.log

# Flutter/Android
**/android/.gradle/
**/android/local.properties
**/android/key.properties
**/android/**/GeneratedPluginRegistrant.*
**/android/captures/

# Flutter/iOS (Mac yok — yalnızca CI)
**/ios/Pods/
**/ios/.symlinks/
**/ios/Flutter/Flutter.framework
**/ios/Flutter/flutter_export_environment.sh
**/ios/**/*.mode1v3

# Flutter/genel üretilen
**/generated_plugin_registrant.dart

# ---- Python (araclar/ betikleri) ----
__pycache__/
*.pyc

# ---- Node (tooling varsa) ----
node_modules/
npm-debug.log*
yarn-error.log

# ---- IDE / editör ----
.idea/
.vscode/
*.swp
*~

# ---- İşletim sistemi ----
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# ---- Log / geçici ----
*.log
*.tmp

# ---- Onur elle silecek (Cowork tasidi, K66/5) ----
_SILINECEKLER/

# --- K108 / oturum 47: KANIT altindaki HAM ARTIKLAR (OLCULEREK eklendi) ---
# OLCUM (K107 yontemi -- bir yok-sayma kuralinin NEYI KORUDUGU da olculur):
#   git ls-files -i -c -X <aday>  =>  0 IZLENEN dosya yutuluyor
#   git ls-files -o -i -X <aday>  =>  69 izlenmeyen artik susuyor
#   git ls-files KANIT/slice-3d/09-MUTANT  =>  40 IZLENEN kanit dosyasi ZATEN VAR,
#   yani susturulan kume gercek kanit degil, ham yakalamadir.
# DIKKAT -- REDDEDILEN ADAY: KANIT/**/_baseline/  IKI IZLENEN dosyayi
#   (KANIT/R9/_baseline/*.orig) yutuyordu. K107 tuzaginin aynisi; uygulanmadan olculdu.
KANIT/**/_tmp_*
KANIT/**/_commit_msg*.txt
KANIT/**/_defter_ekle*.py
KANIT/**/_ozet*.py
KANIT/**/_gitignore_olc.py

.claude/
