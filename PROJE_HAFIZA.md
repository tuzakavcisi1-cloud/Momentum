# PROJE_HAFIZA.md — Momentum (Çok Platformlu Görev Yönetimi)

> Bu dosya projenin TEK canlı hafızasıdır. Her oturumda ÖNCE bu dosyayı + `CLAUDE.md`'yi TAM oku.
> Kanonik konum: `C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum`
>
> ⚠ **SIRALAMA KUSURU [oturum 25'te ÖLÇÜLDÜ, düzeltilmedi — additive kalındı]:** *"En tepedeki DEVİR NOTU
> = oturumun özeti"* kuralı bu dosyanın **kendi düzeniyle çelişiyor**. Bu bloğun hemen altındaki devir notu
> **oturum 23**'ündür; **en yeni devir notu (oturum 24) satır ~60'tadır** ve ikisi de *"ÖNCEKİ DEVİR
> NOTLARINI GEÇERSİZ KILAR"* diyor. Yalnız tepeyi okuyan bir oturum **bayat durum alır.**
> **KURAL (bundan sonra): en yeni checkpoint/devir notu DAİMA bu satırın hemen altına yazılır.**
>
> 🔴 **BU DOSYA ARTIK OTURUM AÇILIŞINDA OKUNMAZ [K53, 26 Tem 2026].** Canlı durum **`DURUM.md`**'dedir (≤12 KB). Burası **APPEND-ONLY KARAR ARŞİVİDİR**: yeni checkpoint üste eklenir, **hiçbir şey silinmez, hiçbir şey yerinde düzeltilmez** (bayat satır varsa **düzeltme notu** yazılır). Buraya yalnız *"bu karar neden alındı?"* diye sorulduğunda bakılır.

<!-- DIZIN:BAS -- MEKANIK URETIM, ELLE DUZENLEME: python araclar/hafiza-dizin.py . -->
### DIZIN — 89 kayit (mekanik uretim, surum 1.0.0)

| satir | tur | oturum | kilit | konu |
|---|---|---|---|---|
| 112 | CHECKPOINT | 35 | K74 K72 | 28 Tem 2026 · oturum 35 KAPANIŞ: K74 — R9 KABUL EDİLDİ (Cowork'ün kendi koşumu); K72 §5'ten ÇEKİLDİ |
| 205 | CHECKPOINT | 35 | K73 | 28 Tem 2026 · oturum 35: K73 — KABUL EDİLEN DİLİMİN KİLİTLERİ §5'TEN ÇEKİLİR; belge-tavan-kapisi.py DOĞDU |
| 298 | CHECKPOINT | 35 | K72 | 28 Tem 2026 · oturum 35: K72 — R9 KİLİDİ: P6/D4 DARALTILDI; spec yazıldı, DENETLENDİ, KIRILDI, düzeltildi |
| 397 | CHECKPOINT | 35 | K71 | 28 Tem 2026 · oturum 35: K71 — KABUL KRİTERİ 9 ÖLÇÜLDÜ (GEÇTİ); R9 KUSURU DOĞDU; DURUM.md §8 ARŞİVLENDİ |
| 500 | CHECKPOINT | 34 | K70 | 28 Tem 2026 · oturum 34 KAPANIŞ: K70 — slice-3d (ÇEKME) KABUL EDİLDİ; spec KİLİTLENDİ |
| 587 | CHECKPOINT | 34 | K69 | 28 Tem 2026 · oturum 34: K69 — slice-3d spec'i YAZILDI; üç karar kilitlendi |
| 881 | CHECKPOINT | 33 | K68 | 28 Tem 2026 · oturum 33: K68 — slice-3d (ÇEKME) TASARIM KİLİDİ + üç ölçüm bulgusu |
| 962 | CHECKPOINT | 32 | K67 | 28 Tem 2026 · oturum 32 KAPANIŞ: K67 — iddia-kapisi.py; iki kör sınıf mekanikleşti |
| 1009 | CHECKPOINT | 32 | K26 | 28 Tem 2026 · oturum 32 — slice-3c BAĞIMSIZ DOĞRULANDI (K26); dört bulgu |
| 1112 | CHECKPOINT | 32 | K64 | 27 Tem 2026 · oturum 32 KAPANIŞ: K64 — spec v2 KİLİTLENDİ |
| 1138 | CHECKPOINT | 32 | K63 | 27 Tem 2026 · oturum 32: K63 — spec v2, İKİ BAĞIMSIZ DENETÇİ SPEC'İ KIRDI |
| 1212 | CHECKPOINT | 32 | K62 | 27 Tem 2026 · oturum 32: K62 — slice-3c spec'i yazıldı, D0–D6 kilitlendi |
| 1264 | CHECKPOINT | 32 | K61 | 27 Tem 2026 · oturum 32 AÇILIŞ: K61 — dev-kimlik kalkanı KİLİTLENDİ |
| 1309 | CHECKPOINT | 31 | K60 | 27 Tem 2026 — oturum 31 KAPANIS: K60 kapıya çevrildi + adım 3 engeli ölçüldü |
| 1347 | CHECKPOINT | 31 | - | 27 Tem 2026 — oturum 31: slice-3b KAPANDI — A2 8/8, commit 5df3caf |
| 1398 | CHECKPOINT | 31 | K59 | 27 Tem 2026 — oturum 31: K59 — SPEC v6; A2'nin SAĞLANAMAZ ŞARTI ÖLÇÜMLE ONARILDI |
| 1445 | CHECKPOINT | 31 | - | 27 Tem 2026 — oturum 31: T8+T9 BAĞIMSIZ DOĞRULANDI — 1 MAJÖR, 4 MİNÖR BULGU |
| 1504 | CHECKPOINT | 31 | K58 | 27 Tem 2026 — oturum 31: K58 — TAVAN 12→32 KB + ARŞİV DİZİNİ |
| 1561 | CHECKPOINT | 31 | - | 27 Tem 2026 — oturum 31: G3 ADJUDİKASYONU + TAŞIMA/T8 BAĞIMSIZ DOĞRULAMASI |
| 1636 | CHECKPOINT | 30 | K57 | EK (26 Tem 2026 — oturum 30: K57 KESİNLEŞTİ + sayi-tazeligi.py DOĞDU |
| 1689 | CHECKPOINT | 30 | K57 | 26 Tem 2026 — oturum 30: K57 — BAYAT ÇAPRAZ-ATIF KESİMİ; ARAÇ SENKRONU |
| 1804 | CHECKPOINT | 29 | K56 | 26 Tem 2026 — oturum 29: K56 — REPO ASCII KÖKE TAŞINIYOR; KAPI SUSTURULMADI |
| 1824 | CHECKPOINT | 29 | K54 K53 | 26 Tem 2026 — oturum 29: K53 VERİMLİLİK REFORMU + K54 SPEC v4 (KİLİT GÜNCELLEMESİ) |
| 1868 | CHECKPOINT | 29 | K52 | 26 Tem 2026 — oturum 29: K52 — SPEC KİLİTLENDİ; esdeger-mutant SINIFI BUILD'E DEVREDİLDİ |
| 1882 | CHECKPOINT | 29 | K51 | 26 Tem 2026 — oturum 29: K51 — RADAR KIRMIZI'DA MEKANİKLEŞTİR KİLİTLENDİ; ARAÇ YAZILDI VE İLK KOŞUMDA ÜÇ DENET |
| 1901 | CHECKPOINT | 29 | K50 K49 | 26 Tem 2026 — oturum 29: K50 — RED-TEAM v2'Yİ DE KIRDI; K49'UN ÖNGÖRDÜĞÜ GERİ DÜŞÜŞ TETİKLENDİ |
| 1924 | CHECKPOINT | 29 | K48 | 26 Tem 2026 — oturum 29: K48 — SPEC v1 ÜÇ BAĞIMSIZ DENETÇİ TARAFINDAN YIKILDI; SONRA DENETÇİLER DE DENETLENDİ  |
| 1955 | CHECKPOINT | 29 | K47 | 26 Tem 2026 — oturum 29: K47 — SPEC'İN DÖRT ÇATALI KİLİTLENDİ; DEVİR NOTUNUN İKİ VARSAYIMI ÖLÇÜMLE YANLIŞLANDI |
| 1987 | CHECKPOINT | 29 | K46 | 26 Tem 2026 — oturum 29 açılış: K46 — RADAR KIRMIZI'DA DÖRT ŞIK SUNULDU, ONUR DARALTI KİLİTLEDİ |
| 2004 | DEVİR | 28 | - | NOTU (26 Tem 2026 — oturum 28 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2041 | CHECKPOINT | 28 | K45 | 26 Tem 2026 — oturum 28: K45 — MEKANİK KAPI YAZILDI VE KENDİNİ KANITLADI (12/12); DESIGN.md v1 YAZILDI; KONTRA |
| 2055 | CHECKPOINT | 28 | K44 | 26 Tem 2026 — oturum 28: K44 — RADAR slice-3b'Yİ DE ISIRDI (R3); KİLİT = MEKANİKLEŞTİR, ÖNCE ARAÇ SONRA BELGE. |
| 2068 | CHECKPOINT | 28 | K43 | 26 Tem 2026 — oturum 28: K43 — dart MCP SUNUCUSU KURULDU VE ARAÇ KÜMESİ ÖLÇÜLDÜ; COWORK'ÜN KENDİ İDDİASI YANLI |
| 2086 | CHECKPOINT | 28 | K42 | 26 Tem 2026 — oturum 28: K42 — slice-3b DÖRT ÇATAL KİLİTLENDİ; CEPHE ARTIK İSTEMCİDİR |
| 2118 | CHECKPOINT | 28 | K41 | 26 Tem 2026 — oturum 28 açılış+kilit: K41 — RADAR KIRMIZI'DA DÖRT ŞIKTAN DURDUR KİLİTLENDİ; ADR 0003 v7 OLDUĞU |
| 2144 | CHECKPOINT | 26 | K40 K38 | 26 Tem 2026 — oturum 26 EK: K40 — PROJE RADARI KURULDU VE KOŞTU; HÜKÜM KIRMIZI; RADAR K38'İ BAĞIMSIZ OLARAK DO |
| 2158 | CHECKPOINT | 27 | K39 | 26 Tem 2026 — oturum 27: K39 — İKİ KİLİT: BU EL YAZIM ELİDİR (ARACI KOŞAR, ONARMAZ) · ATIF DENETİMİNİN EKSENİ  |
| 2198 | CHECKPOINT | 26 | K38 | 26 Tem 2026 — oturum 26 EK: K38 — ADR 0003'ÜN UZAMASININ KÖK NEDENİ ÖLÇÜLDÜ; ÜÇ YAPISAL KARAR KİLİTLENDİ |
| 2214 | CHECKPOINT | 27 | - | 26 Tem 2026 — oturum 27 açılış: PROTOKOL TAM KOŞTU; OTURUM 26'NIN BEYAN ETTİĞİ OKUMA SINIRI KAPANDI |
| 2229 | CHECKPOINT | 26 | K37 | 26 Tem 2026 — oturum 26 açılış: K37 — ÜÇ ÇATAL KİLİTLENDİ; B6-1'İN ÖLÇÜMÜ TURDAN ÖNCE KOŞTU |
| 2283 | CHECKPOINT | 25 | K36 | 26 Tem 2026 — oturum 25: K36 — v7 YAZIMI BAŞLADI; BELGE YARIMDIR. 4/9 BLOKER + 3/31 MAJÖR KAPANDI, 1 YENİ MAJÖ |
| 2327 | CHECKPOINT | 25 | K35 | 26 Tem 2026 — oturum 25: K35 — ADR 0002 ALTI TURDAN SONRA İLK KEZ AÇILDI; DOKUZ ATIFIN SEKİZİ TUTTU, BİRİ UYDU |
| 2349 | CHECKPOINT | 25 | K34 | 26 Tem 2026 — oturum 25 açılış: K34 — KAPI-6'NIN YEDİ ÇATALININ YEDİSİ DE KİLİTLENDİ |
| 2370 | DEVİR | 23 | - | NOTU (25 Tem 2026 — oturum 23 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2405 | DEVİR | 24 | - | NOTU (25 Tem 2026 — oturum 24 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2452 | CHECKPOINT | 24 | K33 | 25 Tem 2026 — oturum 24 kapanış: K33 — 6. TUR KAPI KOŞTU; HÜKÜM KİLİTLENEMEZ; ARACIN ALTIN KÜMESİ İLK KEZ MUTA |
| 2464 | CHECKPOINT | 24 | K32 | 25 Tem 2026 — oturum 24 açılış: K32 — 6. TUR BAĞIMSIZ KAPI AÇILDI; DÖRT ÇATAL KİLİTLENDİ |
| 2478 | DEVİR | 22 | - | NOTU (25 Tem 2026 — oturum 22 GERÇEK kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2517 | CHECKPOINT | 23 | K31 | 25 Tem 2026 — oturum 23 kapanış: K31 — v6 BİTTİ VE KANONİK YOLA ALINDI; ARAÇ 30 → 7 BULGU |
| 2530 | CHECKPOINT | 23 | K30 | 25 Tem 2026 — oturum 23 açılış: K30 — v6'NIN KALAN İŞİ İÇİN DÖRT KARAR KİLİTLENDİ |
| 2545 | DEVİR | 22 | - | NOTU (25 Tem 2026 — oturum 22 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2587 | DEVİR | 21 | - | NOTU (25 Tem 2026 — oturum 21 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR |
| 2648 | CHECKPOINT | 21 | K24 | 25 Tem 2026 — oturum 21 kapanış: K24 — ADR 0003 v5 YAZILDI, KANONİK YOLA ALINDI |
| 2649 | CHECKPOINT | 22 | K29 K5 K3 | 25 Tem 2026 — oturum 22: K29 — v6 YAZIMI BAŞLADI; K1=0 · K2=0 · K3=0 · K5=0. BELGE YARIMDIR |
| 2672 | CHECKPOINT | 22 | K28 | 25 Tem 2026 — oturum 22: K28 — KAPI-5'İN DÖRT ADAY BLOKERİ KİLİTLENDİ |
| 2682 | CHECKPOINT | 22 | K27 | 25 Tem 2026 — oturum 22: K27 — ÖLÇÜM ARACI ONARILDI, ALTIN KÜME 21 KONTROLE ÇIKTI, FARKLI MODELLE SALDIRILDI |
| 2695 | CHECKPOINT | 22 | K26 | 25 Tem 2026 — oturum 22: K26 — ÜRETEN ≠ DENETLEYEN YENİDEN TANIMLANDI: OTURUM SINIRI DEĞİL, KANAL KURALI |
| 2708 | CHECKPOINT | 22 | K25 | 25 Tem 2026 — oturum 22: K25 — 5. TUR BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ |
| 2735 | CHECKPOINT | 21 | K23 K22 K1 | 25 Tem 2026 — oturum 21: K23 — v5 YAZIMI SÜRÜYOR: 8/9 BLOKER + K22-a KAPANDI, K1=0 |
| 2746 | CHECKPOINT | 21 | K22 K20 | 25 Tem 2026 — oturum 21: K22 — Y-1 VE K20-a KİLİTLENDİ; v5 YAZIMI DEVAM EDİYOR |
| 2752 | CHECKPOINT | 21 | K21 | 25 Tem 2026 — oturum 21: K21 — OTURUM DEVRİ ARTIK ÖLÇÜME BAĞLIDIR; HİSSE DEĞİL |
| 2765 | CHECKPOINT | 21 | K20 | 25 Tem 2026 — oturum 21 kapanış: K20 — v5 YAPISI KURULDU, İÇERİK YARIM; ÖLÇÜM ARACI CANLI |
| 2777 | CHECKPOINT | 21 | K19 | 25 Tem 2026 — oturum 21: K19 — v5 YAZIM OTURUMU AÇILDI; DÖRT ÇATAL DAHA KİLİTLENDİ |
| 2795 | CHECKPOINT | 20 | K18 | 25 Tem 2026 — oturum 20 devamı: K18 — KAPI-4'ÜN DÖRT AĞIR ÇATALI KİLİTLENDİ |
| 2805 | CHECKPOINT | 20 | K17 | 25 Tem 2026 — oturum 20: K17 — v4'E 4. BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ |
| 2831 | CHECKPOINT | 19 | K16 | 25 Tem 2026 — oturum 19: K16 — v4 YAZIM OTURUMU; ÜÇ YENİ ÇATAL KİLİTLENDİ |
| 2850 | CHECKPOINT | 18 | K15 | 25 Tem 2026 — oturum 18: K15 — ADR 0003 v3'E ÜÇÜNCÜ BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ |
| 2877 | CHECKPOINT | 17 | K14 | 25 Tem 2026 — oturum 17: K14 — v3'ÜN DOKUZ KARAR ÇATALI KİLİTLENDİ |
| 2903 | CHECKPOINT | 16 | K13 | 25 Tem 2026 — oturum 16: K13 — ADR 0003 v2'YE İKİNCİ VE SON KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ |
| 2925 | CHECKPOINT | 15 | K12 | 25 Tem 2026 — oturum 15: K12 — TAKVİM ÇATALI KİLİTLENDİ; ÖNCELİK = KALİTE > HIZ |
| 2942 | CHECKPOINT | 14 | K11 | 25 Tem 2026 — oturum 14: K11 — ADR 0003'ÜN BLOKER ÇATALLARI KİLİTLENDİ + ADR BÖLÜNDÜ |
| 2960 | CHECKPOINT | - | K10 | 25 Tem 2026 — harici Cowork tasarım oturumu → K10: FRONTEND TASARIM YÖNÜ KİLİTLENDİ, ağırlığı Onur DARALTTI |
| 2983 | CHECKPOINT | 13 | K9 | 25 Tem 2026 — oturum 13: ARGON2ID PAKET KAPISI KOŞULDU → GEÇTİ (Konscious); K9 kilitlendi |
| 3008 | CHECKPOINT | 12 | K8 | 20 Tem 2026 — oturum 12 devamı: K8 — slice-3a-auth TASARIMI KİLİTLENDİ, build BEKLEMEDE |
| 3017 | CHECKPOINT | 12 | K7 | 20 Tem 2026 — oturum 12: İKİ AÇIK KARAR KAPANDI + K7 — kod/spec HENÜZ YAZILMADI |
| 3026 | CHECKPOINT | 11 | - | 20 Tem 2026 — oturum 11 devamı: ÖDEV KAPSAMI YAZIYA GEÇTİ → ÖNCELİK DEĞİŞTİ |
| 3041 | CHECKPOINT | 11 | - | 20 Tem 2026 — oturum 11: BÖLÜNMÜŞ İKİ SPEC'E BİR TUR BAĞIMSIZ DENETİM — TEMİZ DEĞİL, 2 BLOKER + 4 MAJÖR |
| 3061 | CHECKPOINT | 10 | - | 20 Tem 2026 — oturum 10: DÜZELTME-1 [v5] ÇAPRAZ-TUTARLILIK TARAMASI — TEMİZ DEĞİL, 5 BLOKER |
| 3079 | DEVİR | 9 | - | NOTU (19 Tem 2026 — oturum 9: slice-3a BAĞIMSIZ DOĞRULANDI — DÜZELT (dar kapsam: yalnız D5-a) |
| 3104 | DEVİR | 8 | - | NOTU (19 Tem 2026 — oturum 8 kapanış: slice-3a BUILD GELDİ, DOĞRULAMA TEMİZ OTURUMA DEVREDİLDİ |
| 3118 | DEVİR | 8 | - | NOTU (19 Tem 2026 — oturum 8: slice-3a SPEC [v5] HAZIR, BEŞ DENETİM TURU GEÇTİ — KİLİT ONUR'DA |
| 3138 | DEVİR | 7 | - | NOTU (19 Tem 2026 — oturum 7 devamı: slice-2c BAĞIMSIZ DOĞRULANDI — SÜRÜM UYGUN (koşulsuz) |
| 3153 | DEVİR | 7 | - | NOTU (19 Tem 2026 — oturum 7: slice-2b2 BAĞIMSIZ DOĞRULANDI — SÜRÜM UYGUN (koşullu) |
| 3169 | DEVİR | 6 | - | NOTU (19 Tem 2026 — oturum 6: slice-2b1 BAĞIMSIZ DOĞRULANDI — SÜRÜM UYGUN (koşullu) |
| 3188 | DEVİR | 5 | - | NOTU (18 Tem 2026 — oturum 5: slice-2a SPEC HAZIR + spec-QA GEÇTİ |
| 3196 | DEVİR | 4 | - | NOTU (18 Tem 2026 — oturum 4: ADR 0002 KİLİTLENDİ |
| 3205 | DEVİR | 3 | - | NOTU (18 Tem 2026 — oturum 3: slice-1 BUILD + BAĞIMSIZ DOĞRULANDI |
| 3211 | DEVİR | 3 | - | NOTU (18 Tem 2026 — oturum 3: ADR 0001 KİLİTLENDİ |
| 3216 | DEVİR | 2 | - | NOTU (18 Tem 2026 — oturum 2 kapanış |

> Bu blok `python araclar/hafiza-dizin.py .` ile URETILIR; elle duzenleme bir sonraki kosumda EZILIR. Yeni checkpoint bu satirin ALTINA eklenir.
<!-- DIZIN:SON -->

## DÜZELTME NOTU — 30 Tem 2026, oturum 38: K82 KURALI GENELLEŞTİ — NOT, KENDİNİ İÇEREN HİÇBİR ŞEYİN KİMLİĞİNİ YAZAMAZ

Aşağıdaki devir notu `SON COMMIT: 03631f1` diyor. **Yanlış — ama elin hatası değil, YAPISAL.** Notu arşive yazan commit `9eee547`'dir; not yazıldığı anda o commit henüz **yoktu**. Aynı sınıf bu oturumda **üç kez** göründü: ① notun kendi kabının (`PROJE_HAFIZA.md`) baytı ② notun kendi commit'i ③ `DURUM.md` §4'te üç oturum duran bayat PID (K80).

🔴 **GENELLEŞTİRİLMİŞ KURAL [K82-b]:** *bir kayıt, kendisini içeren şeyin kimliğini beyan edemez.* Beyan edilirse **daima** bayattır. Bu üç alan **YAZILMAZ, ÖLÇÜLÜR:** kabın baytı · notun commit'i (`git --no-optional-locks log --oneline -1`) · PID/cihaz. `oturum-sagligi.py` bunlardan yalnız ① için mekanik (`D1-OZ`); ② ve ③ **henüz kapısızdır** — açık borç.

**Ölçüldü (bu düzeltmeden önce):** devir notu yazıldıktan sonra `oturum-sagligi.py` gerçek taraması **KIRMIZI'dan SARI'ya düştü** — dört kimlik girişinin dördü de yazım anında doğru, `D1-OZ` sustu. Yani kapı **kendiliğinden temizlenir**, kalıcı kırmızıya dönmez; bu, radar'ın yapısal kalıcı-kırmızı kusurundan (DURUM.md §8) **farklı** olduğunun kanıtıdır. Kalan tek SARI (`sayi-tazeligi.py` defter kaydı) gerçek ve önceden var olan bir bulgudur.

---

## DEVİR NOTU — oturum 38 → 39 (30 Tem 2026) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

> Bu blok Onur'a sohbette **kod bloğu** olarak da verildi; ikisi aynı metindir.
> 🔴 **Bu not, K82'de doğan kurala UYAR: kendi kabının (`PROJE_HAFIZA.md`) kimliğini YAZMAZ** — notu
> yazmak dosyayı büyütür, beyan yapısal olarak imkânsızdır.

```
MOMENTUM — DEVİR NOTU (oturum 38 → 39, 30 Tem 2026)
PROJE: C:\dev\Momentum — Flutter istemci + .NET 9 backend + PostgreSQL, çevrimdışı-öncelikli senkron.
Cowork = tasarım/spec/denetim/hafıza · Claude Code = build. Üreten ≠ denetleyen (K26).

DURUM: slice-3e KAPANDI (K81). Oturum 38 = ARAÇ oturumu: K21'in mekanik kapısı DOĞDU.
  K82 — araclar/oturum-sagligi.py 1.0.0. Onur üç tasarım kararını kilitledi:
        (1) İKİ AYAK, AYRI HÜKÜM — kanonik/D1 transcript istemez; token ayağı --transcript ister,
            yoksa "OLCULMEDI" der (çıkış kodu 4; 0 "ölçüldü ve sağlıklı" demektir, 4 onu demez).
            🔴 ÖLÇÜLDÜ: Cowork'ün transcript'i BULUTTA, araç Windows'ta koşar ⇒ DURUM.md'de yazılı
            hâliyle araç, yazıldığı gün kendi ana ayağını ÖLÇEMEYECEKTİ. Üç oturum bunu görmedi.
        (2) D1 üç kaynağı tarar: DURUM.md §9 tablosu (çalışma ağacıyla — onlar SÖZLEŞME) · devir
            notunun kimlik bloğu (YAZIM ANIYLA) · defter (ZAMAN ayağı). Defterin bayt-disk
            karşılaştırması radar'ın D1'idir, TEKRARLANMADI.
        (3) Yüzde koşulsuz KIRMIZI ("yüzde yazan el paydayı uydurmuştur"), yanlış-pozitif vakasıyla.
SON COMMIT: 03631f1 · izlenen değişiklik 0 · index.lock YOK.
🔴 PUSH DAİMA ONUR'DA; ileri/geri YAZILMAZ, açılışta ÖLÇÜLÜR:
   git --no-optional-locks rev-list --left-right --count origin/main...HEAD
   (oturum 38 açılışında ls-remote ile ölçüldü: HEAD = origin/main, 0/0 — yani 37 push'lanmıştı.)

COWORK'ÜN KENDİ KOŞUMU (K26):
  oturum-sagligi.py altın küme 26/26 EXIT 0 (önce bulutta, sonra cihazda — aynı) ·
  gerçek tarama EXIT 2 · token ayağı GERÇEK transcript'le: 322.277 mutlak token ⇒ YEŞİL ·
  tek-kopya YEŞİL · belge-tavan SARI (T2!) · radar altın küme 18/18, gerçek tarama KIRMIZI (yapısal).
  Ürün kodu bu oturumda: 0 satır ⇒ 🔴 R8 SAYACI 1. SONRAKİ OTURUM ÜRÜN KODUYLA BAŞLAMALI.

🔴 OTURUM 38'İN ÜÇ DERSİ (tekrar etmesin):
  1. ARAÇ İKİ KEZ KENDİ YANLIŞ-POZİTİFİNİ ÜRETTİ. (a) "Kayıt bayat" ile "dosya sonradan değişti"
     ayrılmadan ölçülemez; yazım anı = notu yazan commit. (b) Sonraki onarım "PROJE_HAFIZA.md
     kirliyse not commit'siz" saydı — arşive YENİ checkpoint eklemek dosyayı kirletir ve eski,
     commit'li notu çalışma ağacıyla karşılaştırtır. KİRLİLİK TESTİ YAZIM ANININ ÖLÇÜSÜ DEĞİLDİR;
     doğrusu notu EKLEYEN commit'i aramaktır (git log -1 -S<sha8> -- PROJE_HAFIZA.md).
     Ayrılmasaydı kapı her oturum kırmızı yanar ve KÖR KAPIYA dönerdi.
  2. AYNI CANLI DOSYANIN İKİ BÖLÜMÜ ÇELİŞEBİLİR ve hiçbir araç sormuyordu: DURUM.md §3 hâlâ
     DESIGN.md v1 534DFF68 (§9'un GEÇERSİZ dediği sha) ve 156/156 (gerçek 171/171) taşıyordu.
     İkisi de ELLE bulundu. oturum-sagligi.py §3'ün İKİ HÜCRELİ satırlarını ayrıştırmıyor.
  3. BİR BELGENİN "AÇIK BORÇLAR" LİSTESİ KAPANMIŞ KALEM TAŞIRSA BAYAT OKUMA ÜRETİR: §8, G12'nin
     kapattığı üç K78 kalemini hâlâ açık gösteriyordu (ölçülerek çıkarıldı).

SIRADAKİ İŞ (sırayla):
  ① 🔴 ÜRÜN KODU — R8 sayacı 1. Aday: §8'in ürün borcu A-7 (taban rozet metni 1.0×'te BİLE
     kırpılıyor; 2.0× ÖLÇÜLMEDİ) + content-desc çift okuma. Cowork spec yazar, Claude Code build eder.
  ② oturum-sagligi.py BAĞIMSIZ DENETİM + KİLİT — K26: aracı yazan el Cowork, kendini onaylayamaz.
     KABUL EDİLMEDİ. Denetim AYRI ELDEN gelmeli.
  ③ DURUM.md TAVAN KARARI — T2 SARI, yalnız 1.024 b pay kaldı (eşik 1.638). Cowork'ün önerisi:
     §8'i BORCLAR.md'ye ayır, kendi tavanıyla. Eşik/kapsam kararı K40 gereği ONUR'DA.
  ④ iddia-kapisi.py ikili tarama onarımı (AYRI EL, K34-f).

AÇIK BORÇLAR — oturum 38'den YENİ olanlar (tam liste DURUM.md §8):
  - DEVİR NOTU KENDİ KABININ KİMLİĞİNİ YAZMAMALI [K82, ölçüldü]. Bu not kurala uyuyor; kural henüz
    CLAUDE.md'ye YAZILMADI (Onur'un kilidi).
  - oturum-sagligi.py §3'ü GÖRMÜYOR (iki hücreli satırlar kapsam dışı) — ayrı el.
  - oturum-sagligi.py tek-kopya-kapisi.py kapsamına EKLENMEDİ (kapının kilitli sha'sı bozulur +
    11/11 mutant yeniden koşar) — GOREV-slice-3e-G12.md ile aynı beyanlı-kilit durumu.
  - CLAUDE.md K53/4 hâlâ "R7" diyor; kural K57'de R8 oldu. Pazarlıksız blokta bayat araç adı.
  - _start_api.cmd KANIT diye gösteriliyor ama versiyon kontrolünde DEĞİL (git status'ta ??).
    Temiz klonda doktrinin kanıtı YOK. Ya commit'lenir ya atıf düzeltilir — Onur'da.
  - 92 izlenmeyen dosya. .gitignore ÖNERİLMEDİ ve gerekçesi ölçüldü: "_" önekli bazı dosyalar
    doktrinin KANITIDIR. src/client/test/ altındaki iki ölü dosya "void main() {}" — 0 test
    bildiriyor, 171/171 BOZULMAMIŞ (şüphe ölçümle ÇÜRÜTÜLDÜ). Silme Onur'da (sandbox silemiyor).
  - RADAR YAPISAL OLARAK KALICI KIRMIZI: radar.py'de defter kaydını park/kapatma mekanizması YOK
    ⇒ park edilmiş kâğıt artefaktlar asla iyileşemez, dört-şık ritüeli her oturum aynı cevabı alır.
    Onarım radar.py'de ⇒ K57-b bayt-özdeşliğini bozar ⇒ üst akış plugin'i + AYRI EL.
  - Eskiler: Y3 mutantı yok · G12 kriter 8 UYGULANMAZ · 02-G2 üretici kodu + ikinci yazıcı ·
    verify.ps1 fail-loud · hafiza-dizin.py K60 ihlali · Z10b · defter D2/D3 · DESIGN.md BD-1…BD-7 ·
    A-7 rozet kırpılması · web [DOĞRULANMADI].

BEYAN EDİLMİŞ SINIRLAR (yaşayanlar): Y1 sembol bazlı · CursorHint YOKSAYILIR (D6) · web'de sinyal
  kIsWeb ile KAPALI ve web ayağı [DOĞRULANMADI] · D2 kural 3'ün K != 'yerel' istisnası ·
  R9 öncesi inmiş satırlar 'yerel' KALIR · oturum-sagligi.py defter ayağı GÜN çözünürlüğü taşır
  (aynı gün yazılan bayat kaydı GÖREMEZ) · devir ayağı GIT'e bağımlı, CRLF'li dosyada OLCULEMEDI der.

ORTAM (oturum 38'de ÖLÇÜLDÜ, beyan değil):
  - docker momentum-postgres Up (healthy) · :5298 LISTENING · adb: emulator-5554 device.
    🔴 PID/cihaz adı YAZILMAZ, her açılışta ÖLÇÜLÜR (K80).
  - K80: ortamı CLAUDE CODE kaldırır, Cowork DOĞRULAR. ASPNETCORE_ENVIRONMENT=Development AÇIKÇA
    set edilir (yoksa K61 her isteği 401'ler). adb: C:\Users\gulci\AppData\Local\Android\Sdk\
    platform-tools\adb.exe
  - 🔴 KÖPRÜ BU OTURUMDA DA DÜŞTÜ (bir kez) ⇒ uzun işi ayrık sürece al, çıktıyı dosyaya yaz.
    ÖLÇÜLMÜŞ ÇALIŞAN DESEN: aracı ÖNCE bulutta yaz + altın kümesini orada koştur, SONRA tek parça
    hâlinde SendUserFile + device_commit_files ile depoya indir. Yarım dosya depoya hiç düşmez.
  - Cowork→PowerShell köprüsü "$" siliyor ve iç içe tırnağı bozuyor ⇒ komut satırına $ ve escape'li
    tırnak gönderme; PowerShell'de "$_" da SİLİNİYOR (bu oturumda ölçüldü, Select-String kırıldı).
  - python HEM stdout HEM stderr reconfigure(encoding="utf-8", errors="replace") ister.
  - Commit mesajına ÇİFT TIRNAK yazma. git add -A YASAK; yol belirterek ekle (K55).
  - PowerShell Get-Content PROJE_HAFIZA.md'yi BOZUK KODLAMAYLA gösteriyor ⇒ arşivi python ile oku.

DOSYA KİMLİKLERİ (son yazımdan SONRA ölçüldü; PROJE_HAFIZA.md BİLEREK YOK — K82 kuralı):
  DURUM.md 31.744 b · 474F4532          | CLAUDE.md 15.599 b · EC022B7B
  PROJE_RADAR.jsonl 139.026 b · 7396DF55 | araclar/oturum-sagligi.py 42.019 b · 7054DEB1

AÇILIŞ PROTOKOLÜ (atlanmaz; oturum 38'de bir adım EKLENDİ):
  1. DURUM.md + CLAUDE.md TAM oku. 🔴 İKİSİNİ DE — oturum 38'de CLAUDE.md okunmadan "okundu"
     işaretlendi; sahte yeşil. PROJE_HAFIZA.md'yi AÇMA (istisna: en üstteki DEVİR + K82/K81).
  2. python araclar\tek-kopya-kapisi.py .
  3. python araclar\belge-tavan-kapisi.py .        (T2 SARI bekleniyor — §3'teki karar)
  4. python araclar\oturum-sagligi.py --altin-kume (26/26, EXIT 0) -> python araclar\oturum-sagligi.py .
  5. python araclar\radar.py --altin-kume -> python araclar\radar.py .   (KIRMIZI beklenen)
  6. git --no-optional-locks status --porcelain + Test-Path .git\index.lock + rev-list
  7. Oturum sağlığını KENDİ oturum-id'nle ÖLÇ. Cowork'te transcript BULUTTADIR:
     /root/.claude/projects/*/<oturum-id>.jsonl — araç bulutta --transcript ile koşulur.
     Eşikler MUTLAK, kanonik metin YALNIZ CLAUDE.md'de, YÜZDE YAZMA.
```

---

## CHECKPOINT — 29 Tem 2026, oturum 38: K82 — `oturum-sagligi.py` DOĞDU ve İLK KOŞUMDA KENDİ YANLIŞ-POZİTİFİNİ ÜRETTİ

**Ne yapıldı.** K21'in mekanik kapısı (`araclar/oturum-sagligi.py` 1.0.0) yazıldı. Üç oturumdur "SIRADAKİ İŞ" olarak bekliyordu; bu oturum açılışı sınıfın **iki yeni vakasını** üretti (devir notunun `PROJE_RADAR.jsonl` kimliği bayattı; DURUM.md başlığı oturum 36 diyordu).

**Onur'un kilitlediği üç tasarım kararı (oturum 38, işaretlenebilir şıklarla sunuldu):**
1. **İki ayak, AYRI HÜKÜM.** Kanonik/D1 ayağı transcript istemez, her zaman koşar; token ayağı yalnız `--transcript` verilirse koşar, verilmezse **OLCULMEDI** der. Gerekçe **ölçüldü**: Cowork'ün transcript'i buluttadır, araç Windows'ta koşar ⇒ DURUM.md'de yazılı hâliyle araç, yazıldığı gün kendi ana ayağını **ölçemeyecekti**. Üç oturumdur bunu kimse fark etmemişti.
2. **D1 üç kaynağı birden tarar:** DURUM.md §9 donmuş kimlik tablosu (çalışma ağacıyla — onlar sözleşmedir) · PROJE_HAFIZA.md devir notunun kimlik bloğu (**yazım anıyla**) · PROJE_RADAR.jsonl (**zaman** ayağı; bayt-disk karşılaştırması radar'ın D1'idir, TEKRARLANMADI).
3. **Yüzde koşulsuz KIRMIZI.** K21'in kendi cümlesi (*"yüzde yazan el paydayı uydurmuştur"*) koda çevrildi; altın kümede **yanlış-pozitif vakası** koşar (`%10 budanabilir` ısırmamalı).

🔴 **ÖLÇÜLMÜŞ DERS — ARAÇ İLK GERÇEK KOŞUMDA KENDİ KUSURUNU ÜRETTİ.** v1.0 üç KIRMIZI verdi: `PROJE_RADAR.jsonl` (gerçek kusur), `PROJE_HAFIZA.md` (yapısal), **`DURUM.md` (YANLIŞ-POZİTİF)**. Üçüncüsü kusurdu: kayıt yazıldığında doğruydu, dosyayı **bu oturumda ben** değiştirdim. Araç *"yazım anında bayat mıydı"* ile *"sonradan değişti"*yi ayırmıyordu ⇒ her oturum kırmızı yanacak, **kör kapıya** dönecekti. Onarım: devir ayağı beyanı **PROJE_HAFIZA.md'yi son değiştiren commit** anındaki dosya hâliyle karşılaştırır (`git show <commit>:<yol>`); not commit'lenmemişse yazım anı = çalışma ağacı. Üç yeni altın küme vakası bu ayrımı kilitler (19b/19c/19d).

🔴 **YENİ KURAL ADAYI — DEVİR NOTU KENDİ KABININ KİMLİĞİNİ YAZAMAZ.** Ölçüldü: not `PROJE_HAFIZA.md 684.530 b` diyor, yazım anında dosya **691.599 b**. Notu yazmak dosyayı büyütür ⇒ beyan **yapısal olarak imkânsızdır**. Araç bunu `D1-OZ` koduyla **SARI** raporlar, KIRMIZI değil: kusur eldeki değil **kuraldadır**.

**Kendi koşumu (K26 — üreten ≠ denetleyen; bu ARAÇ kendini kanıtladı, ama ARACI kimse denetlemedi):**
- Altın küme **25/25 · EXIT 0** — önce bulutta, sonra Onur'un makinesinde, aynı sonuç.
- Gerçek tarama: **EXIT 2**, bir gerçek KIRMIZI (`PROJE_RADAR.jsonl` yazım anında bayat), iki SARI (`D1-OZ`, defter zaman ayağı), `S1`/`S2`/`S3` **temiz** (kanonik eşikler tutuyor, yüzde yok, eşik kopyası yok).
- Token ayağı **gerçek transcript'le** kanıtlandı (bulutta, 929.824 b jsonl): **267.671 mutlak token ⇒ 🟢 YEŞİL**; payda yanlışlama testi tetiklenmedi.
- 🔴 **KABUL EDİLMEDİ.** Aracı **yazan el Cowork'tür**; K26 gereği kendi ürettiğini onaylayamaz. Kilit ve bağımsız denetim **Onur'dadır** (ya da ayrı bir el).

**Bu oturumda ayrıca ölçülenler:** açılış protokolü tam koştu (tek-kopya YEŞİL · belge-tavan YEŞİL · radar altın küme 18/18, gerçek tarama KIRMIZI/EXIT 2 — beklenen) · izlenen değişiklik **0**, `index.lock` **YOK**, HEAD `eb501cb` = uzak `origin/main` (`ls-remote` ile) ⇒ **push yapılmış** · **92 izlenmeyen dosya** · ortam üçü de ayakta (`momentum-postgres` healthy · `:5298` LISTENING · `emulator-5554`).

**DURUM.md §8'den ÖLÇÜLEREK çıkarılan üç bayat kalem** (G12/K81 bunları kapatmıştı, liste güncellenmemişti): web `kIsWeb` koruması (`signalr_json_sinyal.dart:100`) · sinyal sahipliği `_UretimKurulumu`'da (`main.dart:79/123`) · adanmış test dosyası (`g12_sinyal_kapisi_test.dart`, 171/171).

**Çürütülen şüphe (dürüstlük kaydı):** `src/client/test/` altındaki iki izlenmeyen dosyanın `171/171` sayısını şişirdiğinden şüphelendim; ölçtüm, ikisi de `void main() {}` ⇒ **sıfır test bildiriyor**, sayı bozulmamış. Şüphe **ölçümle çürütüldü**.

---

## DEVİR NOTU — oturum 37 → 38 (29 Tem 2026)

> Bu blok Onur'a sohbette **kod bloğu** olarak da verildi; ikisi aynı metindir.

```
MOMENTUM — DEVİR NOTU (oturum 37 → 38, 29 Tem 2026)
PROJE: C:\dev\Momentum — Flutter istemci + .NET 9 backend + PostgreSQL, çevrimdışı-öncelikli senkron.
Cowork = tasarım/spec/denetim/hafıza · Claude Code = build. Üreten ≠ denetleyen (K26).

DURUM: slice-3e KAPANDI. K42-d'nin DÖRT ADIMI DA BİTTİ.
  K77 — slice-3e tasarımı kilitlendi (6 karar). ÖLÇÜLDÜ: backend ayağı slice-2b2'de ZATEN BİTMİŞTİ
        (SyncHub + SignalRSignalPublisher + negotiate-öncesi 401) ⇒ dilim İSTEMCİ işiydi, backend'e
        tek bayt yazılmadı. Taşıma = kendi minimal SignalR-JSON istemcimiz (web_socket_channel).
  K78 — YÜRÜYEN İSKELET KABUL. 🔴 Builder'ın "Changed→çekme ~146 ms" manşet sayısı ÇÜRÜTÜLDÜ:
        iki ayrı saatin farkıydı (host UTC ↔ cihaz logcat); emülatör host'tan ~1,44 sn GERİDE
        (üç ölçüm). Sayı çekildi, [DOĞRULANMADI] yazıldı. YENİ KUSUR SINIFI: saat-sapmasi-olculmemis.
  K79 — G12 spec'i kilitlendi (üç ürün-kodu düzeltmesi + iki kapı ailesi, 16 mutant M58–M73).
  K80 — "Ortamı kim kaldırır" kuralı CLAUDE.md'ye yazıldı: cihaz/canlı-sunucu kanıtı isteyen HER spec
        ortamı KENDİ kaldırma maddesini taşır; PID/cihaz adı YAZILMAZ, ÖLÇÜLÜR; Cowork kaldırmaz,
        DOĞRULAR. (Ölçüldü: Claude Code bunu zaten yapabiliyor — KANIT/slice-3d/09-MUTANT/_start_api.cmd)
  K81 — G12 KABUL. Y1 SEMBOL bazlı oldu (K79/3 daraltıldı); kriter 8 UYGULANMAZ ilan edildi.

SON COMMIT: 218e910 · PUSH EDİLDİ (origin/main...HEAD = 0/0 ölçüldü) · ağaç temiz · index.lock yok.
🔴 PUSH DAİMA ONUR'DA. İleri/geri durumu YAZILMAZ, açılışta ÖLÇÜLÜR.

COWORK'ÜN KENDİ KOŞUMU (builder'ın hiçbir sayısına güvenilmedi, K26):
  analyze --fatal-infos 0 · flutter test 171/171 · yoklama-yasagi-kapisi altın küme 15/15 EXIT 0 ve
  gerçek tarama EXIT 0 BULGU YOK · flutter build web --release EXIT 0 · pub-cve + pub-lisans EXIT 0 ·
  tek-kopya + belge-tavan YEŞİL · üç PNG GÖZLE denetlendi · üç sqlite sha'sı farklı.
  Ürün kodu bu oturumda: 346 satır (istemci) ⇒ R8 SUSTU.

SIRADAKİ İŞ: araclar/oturum-sagligi.py (K21 — K44-a). ÜÇ oturumdur bekliyor ve bu oturum dördüncü
  kanıtını üretti. Araç: transcript'ten MUTLAK token okur, rengi mutlak eşikten hesaplar, paydaya
  DOKUNMAZ, CLAUDE.md'nin K21 bloğunu kanonik değerlerle karşılaştırır, payda yanlışlama testini koşar.
  Altın küme: bilerek bozulmuş bir özet satırını yakalamalı. 🔴 BU OTURUMDAN GELEN İKİNCİ AYAK FİKRİ:
  "checkpoint kaydı, ölçtüğü dosyanın SON yazımından ÖNCE yazılmış mı?" — D1 bu oturumda ÜÇ kez ısırdı.
  Sonra: ② açık borçlar §8 · ③ iddia-kapisi.py ikili tarama onarımı (AYRI EL, K34-f).

AÇILIŞ PROTOKOLÜ (atlanmaz):
  1. DURUM.md + CLAUDE.md TAM oku. PROJE_HAFIZA.md'yi AÇMA — tek istisna en üstteki DEVİR + K81/K80/K79.
  2. python araclar\tek-kopya-kapisi.py .
  3. python araclar\belge-tavan-kapisi.py .
  4. python araclar\radar.py --altin-kume  ->  python araclar\radar.py .   (KIRMIZI beklenen)
  5. git --no-optional-locks status --porcelain + Test-Path .git\index.lock +
     git --no-optional-locks rev-list --left-right --count origin/main...HEAD
  6. Oturum sağlığını KENDİ oturum-id'nle ölç; eşikler MUTLAK, kanonik metin YALNIZ CLAUDE.md'de,
     YÜZDE YAZMA.

OTURUM 37'NİN ÜÇ DERSİ (tekrar etmesin):
  1. "Ayak adı ham çıktıda görünüyor" MUTANT KANITI DEĞİLDİR — flutter test zaten tüm test adlarını
     basar, o kontrol bedavaya geçer. [E] satırları ayrıştırılıp HANGİ TESTİN DÜŞTÜĞÜ ölçülmelidir.
  2. İKİ FARKLI SAATTEN alınan damgaların farkı, sapma ÖLÇÜLMEDEN bir gecikme sayısı olarak yazılamaz.
  3. D1 bu oturumda ÜÇ kez ısırdı ve üçüncüsünü COWORK KENDİ ÜRETTİ (sınıfı adlandırdığı oturumda):
     checkpoint kaydı, dosyanın SON yazımından ÖNCE yazılırsa bayt beyanı bayatlar.

AÇIK BORÇLAR (DURUM.md §8 tam liste; oturum 37'den YENİ olanlar):
  - D1-ÖNLEME artık teorik değil: ÖLÇÜLMÜŞ ve TEKRARLAYAN. oturum-sagligi.py'nin ikinci ayağı olabilir.
  - Y3'ün mutantı YOK (G12 spec §3'te beyanlı).
  - G12 kriter 8 UYGULANMAZ — spec-kapi-kapsama.py dizin kabul etmiyor VE G12'nin başlık şemasını
    ayrıştıramıyor. Biçim standardı CLAUDE.md'de; G12'nin kapsama ölçümü YAPILMADI.
  - GOREV-slice-3e-G12.md tek-kopya-kapisi.py kapsamına EKLENMEDİ (aracın kilitli sha'sı bozulur +
    11/11 mutant yeniden koşar) — kilit BEYANLA yaşıyor, DURUM.md §9'da yazılı.
  - iddia-kapisi.py hayalet kanıt sınıfı ÜÇÜNCÜ kez ısırdı (PNG/sqlite baytları).
  - Eskiler: 02-G2 üretici kodu + ikinci yazıcı · verify.ps1 fail-loud · hafiza-dizin.py K60 ihlali ·
    Z10b · defter D2/D3 · DESIGN.md BD-1…BD-7 · taban rozet kırpılması (A-7) · web [DOĞRULANMADI].

BEYAN EDİLMİŞ SINIRLAR (yaşayanlar): Y1 sembol bazlı, gövde kuralı beyaz listedekiler DÂHİL herkese ·
  CursorHint YOKSAYILIR (D6) · web'de sinyal kIsWeb ile KAPALI ve web ayağı [DOĞRULANMADI] ·
  D2 kural 3'ün K != 'yerel' istisnası · R9 öncesi inmiş satırlar 'yerel' KALIR.

ORTAM (devralınan, kanla yazılı):
  - K80 gereği ortamı ARTIK CLAUDE CODE KALDIRIR. Sıra: docker start momentum-postgres (healthy
    görülene kadar yokla) → backend ayrı süreçte (🔴 ASPNETCORE_ENVIRONMENT=Development AÇIKÇA set
    edilir, yoksa K61 her isteği 401'ler) → emülatör (tek AVD: tuzak_api34).
  - PID/cihaz adı YAZMA, ÖLÇ: docker ps · netstat -ano | findstr :5298 · adb devices
  - adb PATH'te YOK: C:\Users\gulci\AppData\Local\Android\Sdk\platform-tools\adb.exe
  - 🔴 KÖPRÜ BU OTURUMDA ÜÇ KEZ DÜŞTÜ ⇒ uzun koşumları ayrık sürece al, çıktıyı DOSYAYA yaz.
  - Cowork→PowerShell köprüsü "$" değişkenlerini SİLİYOR ve non-ASCII girdiyi cp1252 sanıp bozuyor
    ⇒ komut satırına $ ve Türkçe karakter gönderme, \uXXXX kaçışı kullan ya da .py yaz.
  - os.replace WinError 5 verebilir ⇒ üç adımlı yedekli takas (rename→rename→sha→sil).
  - python HEM stdout HEM stderr reconfigure(encoding="utf-8", errors="replace") ister.
  - Commit mesajına ÇİFT TIRNAK yazma. git add -A YASAK; yol belirterek ekle (K55).
  - flutter test/build için PROGRAMFILES(X86)=C:\Program Files (x86) enjekte et.
  - Ekran yakalama: adb shell screencap -p + adb pull ("> dosya.png" ikiliyi BOZAR).

DOSYA KİMLİKLERİ (son yazımdan SONRA ölçüldü):
  DURUM.md 29.923 b · DF44632A        | CLAUDE.md 15.599 b · EC022B7B
  PROJE_HAFIZA.md 684.530 b · E1274D69 | PROJE_RADAR.jsonl 133.609 b · 45AB330C
  GOREV-slice-3e-G12.md 12.623 b · BDB3630E (K79 KİLİTLİ)
  araclar/yoklama-yasagi-kapisi.py 22.467 b · 5638353C
  signalr_json_sinyal.dart 13.727 b | g12_sinyal_kapisi_test.dart 14.782 b
```


## K81 — `G12` KABUL EDİLDİ + `K79/3` DARALTILDI (29 Tem 2026, oturum 37 · Onur onayladı)

**COWORK'ÜN KENDİ KOŞUMU (K26) — geçenler.** `flutter analyze --fatal-infos` → *No issues found*
(5,9 sn) EXIT 0 · `flutter test` → **171/171** EXIT 0 (156 → 171, +15) · `yoklama-yasagi-kapisi.py
--altin-kume` → EXIT 0 **12/12** (kabul kriteri ≥9) · `flutter build web --release` → **EXIT 0** ·
kanıt dizininde **100 KB üstü tek dosya yok** (kriter 9 koruması uygulanmış) · PNG'ler konteynere alınıp
**gözle** denetlendi: 6:42'de dört satır → 6:43'te beş satır, *"G12 ANDROID RE…"* eklenmiş ⇒ `kIsWeb`
koruması Android'i **kırmamış** (kriter 7). T3 kodu okundu: `kIsWeb` kontrolü `_durduruldu = false`'dan
**önce** duruyor, yani bayrak `true` kalıyor ve hiçbir zamanlayıcı kurulmuyor — K79/2'ye harfiyen uygun.

🔴 **COWORK, BUILDER'IN MUTANT ÖLÇÜMÜNÜ YETERSİZ BULDU VE DAHA SIKI ÖLÇTÜ.** Spec *"hedef ayağın adı ham
çıktıda görünmeli"* diyordu; bu **zayıf bir kontroldür**, çünkü `flutter test` zaten bütün test adlarını
basar ⇒ her mutantta bedavaya geçer. Cowork `[E]` satırlarını ayrıştırıp **hangi testin düştüğünü**
ölçtü: **onüç birim mutantın onüçü de kendi ayağını düşürdü.** İkisinde nüans: `M63` → `A7` yerine
**`A7b`**, `M69` → `A13` yerine **`A13b`** — ikisi de builder'ın böldüğü ayağın doğru yarısı, **ölü tuzak
değil**. Kolateraller (`M59`+`A6`, `M60`+`A11`, `M68`+`A11`) aynı kök nedenden ve her birinin **kendi**
hedefi de düşüyor ⇒ oturum 36'nın `G11-A9` sınıfı **tekrar etmedi**.

**BUILDER'IN İKİ DÜRÜST DURUŞU TEYİT EDİLDİ.** `A9`/`A13` fixture'larının ilk hâlinin ölü tuzak olduğunu
kendi bulup düzeltmesi (ve `A13b`'yi eklemesi) · `fakeAsync` altında `cancel()`'ın kendi olay teslimatı
içinden çağrılmasının Future'ı hiç tamamlamadığını ölçüp işaretlemesi. Kriter 4 ve 8'de karar vermeyip
Onur'a bırakması da doğrudur.

---

### 🔴 KARAR 1 — `Y1` SEMBOL BAZLI OLDU (`K79/3` DARALTILDI)

**Bulgu:** `yoklama-yasagi-kapisi.py .` EXIT 1 verdi, tek bulgu `senkron_rozeti.dart:170` —
`_DonenOkState` içindeki **dönen ok animasyonunun** `Timer.periodic`'i. Cowork kaynağı okudu: ağ yok,
senkron çağrısı yok, saf sunum. **Araç doğru, kural fazla genişti.**

**Onur'un kilidi:** sembol bazında beyaz liste. Reddedilen iki şık ve ölçülmüş bedelleri: *dosyayı
beyaz listeye almak* → o dosyaya sonradan eklenecek senkron çeken bir `Timer` **kör** kalırdı;
*beyan edilmiş sınır sayıp kırmızı bırakmak* → kalıcı kırmızı = alarm yorgunluğu, bu projede "kör kapı"
tam olarak böyle doğar.

**Cowork'ün onarımı (K34-f'e uygun: aracı Claude Code yazdı, Cowork onardı):** beyaz liste artık
`(dosya, kapsayan bildirim)` çifti — `signalr_json_sinyal.dart::SignalrJsonSinyal` ve
`senkron_rozeti.dart::_DonenOkState`. 🔴 **Ve bir KUSUR daha kapandı:** eski `_y1` beyaz liste dışındaki
dosyayı `continue` ile atlıyordu, yani **gövde taraması hiç koşmuyordu**; Cowork bunu `M71` üzerinden
ölçmüştü (mutant yalnız *"BEYAZ LİSTE DIŞI"* diye raporlanıyor, *"YOKLAMA ŞÜPHESİ"* bacağı gerçek depoda
hiç çalışmıyordu). Artık **gövde kuralı beyaz listedekiler dâhil her zamanlayıcıya** uygulanır.

**Altın kümeye üç vaka eklendi (12 → 15):** ① beyaz listedeki sembol — SUSMALI ② **aynı dosyada başka
sembolde** `Timer` — ISIRMALI (dosya bütünü affedilmez) ③ beyaz listedeki sembolde **bile** senkron çeken
gövde — GÖVDE BACAĞI ISIRMALI. Ayrıca `_vaka` yardımcısına **`icermeli`** parametresi eklendi: eski hâli
yalnız **kodları** (`Y1`/`Y2`/…) karşılaştırıyordu, dolayısıyla `Y1`'in iki bacağını **ayırt edemiyordu**
ve *"gövde bacağı koşuyor mu?"* sorusunu altın küme **soramıyordu**.

**Ölçüldü:** altın küme **15/15 EXIT 0** · gerçek tarama **EXIT 0, BULGU YOK** ⇒ **kriter 4 GEÇTİ.**
**`M71b`** (K81 sonrası yeniden koşum, `KANIT/slice-3e-G12/09-MUTANT/M71b-*`): taban **0 bulgu**, mutant
altında **2 bulgu** ve ikincisi *"YOKLAMA ŞÜPHESİ … Timer gövdesinde `cekmeTuruCalistir` geçiyor"* ⇒
gövde bacağı artık **gerçek depoda** kanıtlı. Ürün kodu mutanttan sonra geri alındı, `sha256` **aynı**.

---

### 🔴 KARAR 2 — KRİTER 8 UYGULANMAZ; KUSUR SPEC'İ YAZAN ELDEDİR

`spec-kapi-kapsama.py` **dizin kabul etmiyor** (`.` ⇒ `ORTAM HATASI: Permission denied: '.'`), spec
dosyasıyla çağrılınca da `[S0] BİÇİM: §5'te hiç '### G<n>' kapı başlığı yok` diyor — `G12` kendi başlık
şemasını kullanıyor (`## 2. YAPILACAKLAR` / `## 3. MUTANT TABLOSU`). Yani kriter 8, **aracın hiç kabul
etmediği bir argüman biçimiyle** ve **ayrıştıramadığı bir belge biçimine karşı** yazılmıştı. **Bu kusur
builder'ın değil, `K79`'u yazan elindir** — Cowork kendi hatasını kaydeder.

**Onur'un kilidi:** `G12` için kriter 8 **UYGULANMAZ** diye beyan edilir (spec K79 ile bayt-kilitli,
dokunulmaz) ve biçim standardı `CLAUDE.md`'ye yazılır: bundan sonraki her `GOREV_CLAUDE_CODE/*.md`
spec'i `## 5. KAPILAR` + `### G<n>` + `## 6. MUTANTLAR` başlıklarını taşır, araç **spec dosyasının
yoluyla** çağrılır. Reddedilen şıklar: *aracı genişletmek* (K34-f gereği **ayrı el** ister) ·
*spec'i düzeltmek* (K79 kilidini bozardı — kilitli spec'e dokunma yasağının ilk ihlali olurdu).
**Beyan edilmiş boşluk:** `G12`'nin kapı/kural kapsama ölçümü bu dilimde **yapılmamıştır**.

---

### Kalan açık kalemler

`Y3`'ün mutantı yok (spec §3'te beyanlı) · `iddia-kapisi.py` bu kanıt dizininde de **6 SARI hayalet**
üretti (PNG/sqlite baytları; borç **üçüncü kez** ısırdı, onarım ayrı elde) · web ayağı
**[DOĞRULANMADI]** olmaya devam eder — `kIsWeb` yalnız *"web'de sessizce kapalı"* olduğunu ölçer,
*"web'de çalışıyor"* demez.


## K80 — ORTAMI KİM KALDIRIR: kural `CLAUDE.md`'ye yazıldı (29 Tem 2026, oturum 37 · Onur kilitledi)

**Soru Onur'dan geldi:** *"Sen bana bu emülatörü falan açtırıyorsun ya, önceden yapmıyorduk böyle bir
şey; acaba Code kendi açabiliyor muydu?"*

**Ölçülen cevap: EVET, açabiliyor ve daha önce açtı.** Cowork tahmin etmedi, repoyu taradı:

- `KANIT\slice-3d\09-MUTANT\_start_api.cmd` — **builder'ın kendi yazıp koştuğu** betik:
  `cd /d …\Momentum.Api` + `set ConnectionStrings__Momentum=…` + `set ASPNETCORE_URLS=http://127.0.0.1:5298`
  + `dotnet run --no-launch-profile > _api_log.txt 2>&1`. Yani backend'i oturum 34'te **Claude Code ayağa
  kaldırdı**.
- `KANIT\slice-3b\00-ortam.txt` — *"Android emulator o an KOŞMUYORDU"* ve *"Kullanılabilir emulator:
  `tuzak_api34`"* diye **ölçmüş**; aynı dilimde `KANIT\slice-3b\01-G1-android\HUKUM.md`
  `flutter run -d emulator-5554 … (emulator tuzak_api34, AVD, cold boot)` diyor ⇒ emülatörü de o kaldırmış.

**Cowork'ün kendi payı — beyan edildi.** Cowork de açabilirdi; nitekim oturum 37'de `flutter test` ve
`flutter build web`'i ayrık süreçte başlatıp çıktıyı dosyaya yazdırdı, aynı kalıp `dotnet run` ve
`emulator.exe` için de çalışırdı. Onur'a komut vermesinin **gerçek** sebebi teknik imkânsızlık değildi:
köprü bu oturumda **üç kez** düştü ve köprünün sahip olduğu bir kabuktan başlatılan uzun ömürlü sunucunun
akıbeti köprü gidince belirsizleşir (logu okunamaz, öldürülemez). Bu gerekçe **geçerlidir ama
SÖYLENMEMİŞTİ** — beyan edilmemiş bir tercihti ve Onur'un sorusu onu ortaya çıkardı.

**KİLİTLENEN KURAL** (`CLAUDE.md` → *"Ortamı kim kaldırır"* bölümü; **`DURUM.md` §5'e KOPYALANMADI** —
bu kalıcı bir iş akışı kuralıdır, yürürlükteki bir kilit değil; ikisine birden yazmak bu projede beş kez
ısıran `kanonik-kopya` kusurunu üretirdi):

Cihaz ya da canlı-sunucu kanıtı isteyen **her spec**, ortamı kendi kaldırma maddesini taşır — Postgres →
backend → emülatör, sırayla, her biri **yoklanarak** doğrulanır. PID/cihaz adı/"çalışıyor" beyanı hiçbir
belgeye yazılmaz, **ölçülür**. Sabit `sleep` ölçüm sayılmaz. `ASPNETCORE_ENVIRONMENT=Development` açıkça
set edilir (K61 kalkanı aksi hâlde her isteği 401'ler; `_start_api.cmd` bunu set etmiyor ve
`dotnet run --no-launch-profile`'ın ortamı Development'a düşürüp düşürmediği **[DOĞRULANMADI]**).
**Cowork ortamı kaldırmaz, DOĞRULAR.**

**Kapsam notu:** `GOREV-slice-3e-G12.md` **K79 ile kilitli** ve kabul kriteri 7 cihaz istiyor ama ortamı
kimin kaldıracağını **söylemiyor** — bu boşluk ölçüldü ve **bilerek kapatılmadı**: kilitli bir spec'e bayt
eklemek kilidi bozar. Kural **bir sonraki spec'ten** itibaren uygulanır; `G12` için ortam oturum 37'de
elle kaldırıldı ve doğrulandı (Postgres healthy · 5298 LISTENING · `emulator-5554` + uygulama PID 6045 ·
`/v1/tasks` ve `/hubs/sync/negotiate` başlıksız **401** / başlıklı **200**).



## K79 — `G12` SPEC'İ KİLİTLENDİ (29 Tem 2026, oturum 37 · Onur kilitledi)

**Neden `G12` var — K78'in sekiz bulgusundan üçü.** slice-3e iskeleti kabul edildi ama Cowork'ün
bağımsız doğrulaması şunları ölçtü: ① `signalr_json_sinyal.dart` **307 satır / 10.861 b** ve `flutter test`
**156/156** — sayı düşmedi ama **artmadı da**, yani protokol kodunun **tek** testi yok; ② `_websocketAc()`
doğrudan `IOWebSocketChannel.connect(...)` çağırıyor ve **private** olduğu için başka kütüphaneden
override edilemiyor ⇒ sahte kanal enjekte edilemez, kodun can alıcı kısmı **ölçülemez**; ③ sinyal örneği
`_UretimKurulumu`'ya konmadığı için `durdur()` çağıracak sahip yok, `StreamController` hiç kapanmıyor.
Ayrıca `K68` (*"periyodik yoklama YASAK"*) üç dilimdir **prozada** yaşıyor ve `K77/6` (*"`CursorHint`
YOKSAYILIR"*) bugün yalnız **yorumda** — bir mutant `arguments`'ı okumaya başlasa **hiçbir şey ısırmaz**.

**KİLİTLENEN ALTI KARAR.**

**K79/1 — Kapsam: üç ürün-kodu düzeltmesi + İKİ kapı ailesi.** T1 kanal açıcının enjekte edilebilir
olması (üretim yolu **değişmez**, yalnız ölçülebilirlik), T2 `durdur()` sahipliği + `_denetleyici.close()`
+ idempotanslık, T3 `kIsWeb` koruması. Sonra T4 `g12_sinyal_kapisi_test.dart` (13 ayak `A1`–`A13`) ve
T5 `araclar/yoklama-yasagi-kapisi.py` (4 ayak `Y1`–`Y4`, altın küme ≥9). **Biri olmadan öteki kabul
edilmez** — çünkü birim ailesi düzeltmeler olmadan yazılamaz, statik aile ise iki PAZARLIKSIZ kilidin
(K68, K77/6) tek mekanik dayanağıdır.

**K79/2 — Web koruması `kIsWeb` ile KAPATMA.** Web'de `baslat()` hiç bağlanmaz, `_durduruldu` **true
kalır** (sonraki çağrılar da sessizce döner, hiçbir zamanlayıcı kurulmaz), tek satır log bırakır.
Reddedilen alternatif: koşullu import + `access_token` query — bu, backend'de slice-2b2 `D4`
deny-by-default middleware'ine ve K61 kalkanına **dokunmak** olurdu.

**K79/3 — `K68` MEKANİKLEŞİYOR (`Y1`).** `src/client/lib` altındaki her `Timer(`/`Timer.periodic(`
beyaz listede olmak zorunda; bugün meşru tek iki kullanıcı keepalive ve geri çekilme. Liste dışı bir
zamanlayıcı **ya da** gövdesinde `cekmeTuruCalistir`/`turCalistir`/`SenkronAgi` geçen bir zamanlayıcı ⇒
KIRMIZI. Bu, radar KIRMIZI'da sunulan **MEKANİKLEŞTİR** şıkkının yazılı gerekçe şartını sağlar: sınıf
koşan kod olmadan, salt kaynak taramasıyla ölçülebilir.

**K79/4 — 16 mutant: `M58`–`M70` birim (13) + `M71`–`M73` statik (3).** 🔴 **KOLATERAL ISIRIK YASAK.**
Oturum 36'nın birinci dersi (`G11-A9` ölü tuzağı) buraya kural olarak yazıldı: bir mutantın herhangi bir
ayağı kırması YETMEZ, **hedeflediği ayağın KENDİSİNİN** kırıldığı ham çıktıda görülmelidir. Koşan-uygulama
mutantı **YOK** ⇒ K53/3 tavanı (3) harcanmaz.

**K79/5 — Sahte kanal, gerçek protokol.** Birim testleri sahte `WebSocketChannel` kullanır ama **gerçek
çerçeve baytlarını** besler (`0x1E` ayraçlı, gerçek SignalR JSON gövdeleri). Sahte, protokolü
basitleştirmez; yalnız taşımayı değiştirir.

**K79/6 — `Random` deterministik.** Geri çekilme ayakları seed'li `Random(42)` ile koşar ve jitter
*"aralıkta mı"* diye değil **tam beklenen değere** karşı ölçülür. Gerekçe doğrudan K78'dir: builder'ın
*"6/6 jitter aralığında"* iddiası bir **üst sınır** ölçümüydü (aralık = timer + deneme süresi) ve üçüncü
aralık alt sınıra yalnız 36 ms uzaktaydı. O zayıflık burada tekrar edilmez. **Beyan edilmiş kırılganlık:**
ayak Dart'ın `Random` uygulamasına bağlıdır; sürüm değişirse kırılır — bu bilerek kabul edildi.

**BEYAN EDİLMİŞ BORÇ:** `Y3` (`/v1/sync` dizgesi keepalive dosyasında geçemez) ayağının **mutantı yok**;
dizgeyi dosyaya elle eklemek gerçekçi bir kusur değil, yapay bir enjeksiyon olurdu. Ayak koşar ama
**ısırdığı ölçülmemiştir**. Gizlenmiyor, spec §3'te yazılı.

**KABUL KRİTERİ 9'A EKLENEN KORUMA:** kanıt dizinine **100 KB'tan büyük dosya konulmaz**, büyük çıktılar
**kesit + sha** olarak yazılır. Gerekçe ölçüldü: `iddia-kapisi.py` ikili/büyük dosyaları metin gibi tarıyor
ve `KANIT/slice-3e-iskelet`'te tabloda **sıfır** mutant varken **altı hayalet** buldu (kaynak 2 MB'lık
`pub-lisans-kapisi.txt` + PNG/sqlite baytları). Aracın onarımı **ayrı ele** (K34-f); bu koruma o onarım
gelene kadar sınıfın ısırmasını engeller.

**Spec:** `GOREV_CLAUDE_CODE/GOREV-slice-3e-G12.md`.



## K78 — slice-3e YÜRÜYEN İSKELET KABUL EDİLDİ (29 Tem 2026, oturum 37 · Onur onayladı)

**Ne kabul edildi.** `GOREV-slice-3e-iskelet.md`'nin T1–T5'i. Claude Code yazdı, **Cowork hiçbir sayısına
güvenmeden yeniden koştu (K26).** Backend'e **tek bayt yazılmadı**. Ürün kodu: `gercek_zamanli_sinyal.dart`
**25 satır** (yeni) + `signalr_json_sinyal.dart` **307 satır** (yeni) + `main.dart` **+11** + `pubspec.yaml`
**+3** = **346 satır**.

**COWORK'ÜN KENDİ KOŞUMU — geçen ölçümler.**
`flutter analyze --fatal-infos` → *No issues found* (12,6 sn), EXIT 0 · `flutter test` → **156/156**,
EXIT 0 · `pub-cve-kapisi` altın küme **8/8** sonra gerçek tarama **EXIT 0 TEMİZ** · `pub-lisans-kapisi`
altın küme sonra gerçek tarama **EXIT 0**, **96 paket** (SDK 5 / hosted 91), izinsiz 0 · bilinmeyen 0 ·
geçersiz eşleşme 0 · `tek-kopya` YEŞİL · `belge-tavan` YEŞİL · radar KIRMIZI ama **yeni bulgu yok**
(aynı üç eski `D` kaydı).

🔴 **COWORK KENDİ HİPOTEZİNİ ÖLÇTÜ VE ÇÜRÜTTÜ.** `signalr_json_sinyal.dart`
`package:web_socket_channel/io.dart` (⇒ `dart:io`) çekiyor ve `main.dart` bunu **koşulsuz** import
ediyor; Cowork *"bu web derlemesini kırar, ciddi regresyon"* diye düşündü. `flutter build web --release`
koşuldu: **EXIT 0, `Built build\web`** (123,7 sn). **Hipotez yanlıştı ve bu kayda geçirildi** — denetçinin
kendi yanlışını gizlemesi, denetlediği elin yanlışını gizlemesinden farksızdır.

**CİHAZ KANITI — gözle denetlendi, sahte değil.** Üç PNG konteynere alınıp **gerçekten açıldı**:
`00-once.png` (3:08) iki satır + System UI ANR diyaloğu · `01-sonra.png` (3:14) **üç** satır, *"UZAKTAN
GELEN …"* eklenmiş · `02-yeniden-baglanma.png` (3:18) **dört** satır, *"UCAK MODUNDA …"* da gelmiş.
Üç SQLite anlık görüntüsünün sha'sı **üçü de farklı** (`a36ef426` / `59cb4e25` / `620314a2`) ⇒ çekme turu
gerçekten koştu, yalnız log değil. Ham logcat'teki **dokuz** `[sinyal]` satırının **dokuzu** da özette
var — kiraz toplama yok.

---

### 🔴 ÇÜRÜTÜLEN MANŞET SAYI — "~146 ms"

Builder *"gönderimden ~146 ms sonra cihaz `Changed` aldı"* yazmıştı. Bu **bir ölçüm değildi**: gönderim
damgası **host'un UTC saatinden** (curl), alım damgası **cihazın logcat saatinden** geliyor. Cowork
emülatörün saatini üç kez ölçtü (`adb shell date +%s%3N` ↔ host, gidiş-dönüş süresi de kaydedildi):
host'tan **1.242 / 1.434 / 1.444 ms GERİDE**. Sapma o an da buysa gerçek gecikme **~1,59 sn**, yani sayı
bir büyüklük mertebesi yanlış; o an farklıydıysa da **bilinmiyor**.

**Yapılan:** sayı `00-HUKUM.md` ve `_ws-trafik.txt`'ten **ÇEKİLDİ**, yerine `[DOĞRULANMADI]` + sapma
ölçümü + *"çekildi"* beyanı yazıldı. **İşlevsel iddia SAĞLAM ve bundan etkilenmez:** `Changed` yoklama
olmadan geldi, tek abonelik satırı `cekmeTuruCalistir()`'i tetikledi, imleç
`{"xid":1245,"seq":289}` → `{"xid":1248,"seq":290}` ilerledi, uçak modunda kaçan yazım yeniden bağlanmanın
tetiklediği **tek** çekme turuyla kurtarıldı.

**Yeni kusur sınıfı: `saat-sapmasi-olculmemis`.** İki farklı saatten alınan damgaların farkı, sapma
ölçülmeden bir gecikme sayısı olarak yazılamaz. Bu sınıfın mekanik kapısı **YOK**.

---

### Diğer bulgular (hepsi `DURUM.md` §8'e girdi)

**🟡 "6/6 jitter aralığında" bir ÜST SINIR ölçümüdür.** Ölçülen aralıklar 1160 / 2066 / 3236 / 8610 /
16402 / 29647 ms; altısı da `[0,8×, 1,2×]` içinde. Ama aralık = *zamanlayıcı gecikmesi + başarısız deneme
süresi* ve deneme süresi ölçülmedi. Üçüncüsü alt sınıra (3.200 ms) yalnız **36 ms** uzakta ⇒ deneme 36
ms'den uzun sürdüyse gerçek gecikme aralığın DIŞINDA. Belge bu sınırla işaretlendi.

**🟡 Özet, ham kanıtı sessizce kısaltmış.** `_ws-trafik.txt`'teki beş `SocketException` satırı hamda
birebir yok; hamda `ClientException with SocketException: … address = 10.0.2.2, port = 5298, …` yazıyor.
Zaman damgaları ve anlam aynı; kısaltma **işaretlenmemişti**, şimdi beyan edildi.

**🔴 Web'de sessiz sonsuz yeniden-bağlanma riski.** Derleme geçiyor ama tarayıcıda `IOWebSocketChannel`
çalışmaz ve WS upgrade'ine `X-Momentum-Dev-User` konamaz ⇒ her deneme kopar, 30 sn'de bir sonsuza dek
yeniden dener. `G12`'nin işi (`kIsWeb` koruması / koşullu import).

**🟡 `SignalrJsonSinyal` durdurulamıyor** — örnek `_UretimKurulumu`'ya konmuyor, `StreamController` hiç
kapanmıyor ⇒ **test edilemez**. **🟡 346 satırın tek testi yok** (156/156 düşmedi ama artmadı da) —
`G12`'nin ilk işi. **🟡 `iddia-kapisi.py` hayalet kanıt sınıfı ikinci kez ısırdı:** tabloda sıfır mutant
varken altı hayalet (`M111, M6, M7, M8, M8p, M9`), kaynak 2 MB'lık `pub-lisans-kapisi.txt` + PNG/sqlite
baytları. **🟡 Builder'ın *"`web_socket_channel`/`stream_channel` BSD-3-Clause ölçüldü"* ifadesi kapı
çıktısında görünmüyor** — kapı temiz paketleri tek tek basmıyor. Lisans doğru (Cowork pub.dev `/api` ile
ayrıca ölçtü) ama **kanıt yolu yanlış gösterilmiş**.

**Kilitlere uyum tam:** kendi istemcimiz yazıldı (K77/1), `CursorHint` hiç okunmuyor (K77/6), ek debounce
yok (K77/5), backend değişmedi, `G12`/mutant bilerek yok (K77/4).

**K78/4 — SIRADAKİ İŞ `G12` kapısı + mutant tasarımı.** İlk üç ayağı bu checkpoint belirliyor:
① 346 satırlık protokol kodunun birim testi, ② `durdur()` sahipliği (test edilebilirlik), ③ web koruması.



## K77 — slice-3e TASARIM KİLİDİ (29 Tem 2026, oturum 37 · Onur kilitledi)

**Bağlam.** Oturum 37 açılışında radar KIRMIZI yandı; K40 gereği dört şık sunuldu ve Onur **DEVRET**'i
seçti ("önerdiğini yap"). Devretmenin fiili karşılığı: kâğıt artefakta yeni tur açmak yerine **koşan
koda** geçmek — yani K42-d adım 4, slice-3e.

**ÖLÇÜLEN ZEMİN (dilimin boyutunu bu değiştirdi).** Cowork, devir notunun *"slice-3e (SignalR)"*
ifadesini olduğu gibi kabul etmedi ve diskten + **canlı sunucudan** ölçtü:

- `src/backend/Momentum.Api/Realtime/SyncHub.cs` (1.530 b) ve `SignalRSignalPublisher.cs` (1.279 b)
  **zaten var** — slice-2b2'de yazılmışlar. `Program.cs`: `AddSignalR()`, `MapHub<SyncHub>("/hubs/sync")`,
  negotiate'ten **önce** çalışan yol-tabanlı 401 middleware'i (D4).
- Canlı: `POST /hubs/sync/negotiate?negotiateVersion=1` başlıksız **401**; `X-Momentum-Dev-User` ile
  **200** + `connectionToken`, `availableTransports` = WebSockets · ServerSentEvents · LongPolling.
- `SignalEnvelope(string Group, WireCursor CursorHint)` — **yükü boş**.
- İstemcide `SenkronDongusu.cekmeTuruCalistir()` **hazır**; tek-uçuş kilidi (`_devamEdenTur`) + K3
  bayrağı (`_cekmeBekliyor`) **zaten** coalescing yapıyor.

⇒ **slice-3e istemci işidir. Backend'e tek bayt yazılmaz.** Devir notu bu dilimi olduğundan büyük
gösteriyordu; sebebi kötü niyet değil, **ölçülmemiş bir etiket**.

**KİLİTLENEN ALTI KARAR.**

**K77/1 — Taşıma: kendi minimal SignalR-JSON istemcimiz**, `web_socket_channel ^3.0.3` üstünde
(BSD-3-Clause, `/advisories` **0 kayıt** — ölçüldü). `signalr_netcore 1.4.4` (MIT, 0 advisory)
**reddedildi**: altı geçişli bağımlılık getiriyor (`logging`, `tuple`, `web_socket_channel`,
`sse_channel 0.1.1`, `message_pack_dart`, `http`) ⇒ kırmızı çizgi 3 (lisans+CVE) maliyeti altı kat,
ve kütüphanenin **kendi** yeniden-bağlanma politikası bizim ölçtüğümüz şey olmaktan çıkardı. Sinyal
yükü boş olduğu için MessagePack / SSE / stream / istemci→sunucu invoke **hiç gerekmiyor**; gereken
alt küme el sıkışma + `type:1/target:Changed` + `type:6` ping'ten ibaret. **Beyan edilen bedel:** elle
protokol yeni bir kusur sınıfıdır ve her ayağı mutantla ısırtılmak zorundadır. **Beyan edilen kaçış
yolu:** taşıma `GercekZamanliSinyal` portunun arkasındadır; pakete dönmek tek dosyalık iştir.

**K77/2 — Yeniden bağlanma: üstel geri çekilme (1→2→4→8→16→30 sn tavan, ±%20 jitter) + her başarılı
(yeniden) bağlanmada BİR çekme turu.** 🔴 **Bu yoklama DEĞİLDİR ve ayrım yazıya geçirilmiştir:**
zamanlayıcı **veri çekmez**, yalnız **bağlanmayı** dener; veri çekmenin tetikleyicisi `Changed` olayı
ya da *"bağlantı kuruldu"* olayıdır. K68'in yasakladığı şey `Timer.periodic` ile `/v1/sync` dövmekti.
Aynı beyan protokol keepalive'ı (`{"type":6}`, 15 sn) için de geçerlidir.

**K77/3 — Kapsam: Android + Windows; web `[DOĞRULANMADI]` diye BEYAN EDİLİR.** Gerekçe ölçüldü:
`/hubs/sync` middleware'i **her** istekte `X-Momentum-Dev-User` istiyor — negotiate'te **ve** WS
upgrade'inde. Tarayıcı WS upgrade'ine özel başlık koyamaz; web'i açmak backend'e `access_token` query
kanalı eklemek, yani slice-2b2 `D4` deny-by-default middleware'ine ve K61 kalkanına **dokunmak**
demektir. Bu dilimde yapılmaz. (Ayrıca `flutter test --platform chrome` bu ortamda zaten sonuç
üretmiyor — iki ölçüm: 7 dk ve 9,8 dk.)

**K77/4 — İskelet önce, kapılar sonra (K53/5).** `GOREV-slice-3e-iskelet.md` bir **spec değildir**;
`G12` ve mutantları iskelet cihazda ölçüldükten **sonra** yazılır. Belgede bu bir eksiklik değil,
**kilitli karar** olarak yazılıdır.

**K77/5 — Sinyal→çekme eşlemesi doğrudan; ek debounce/zamanlayıcı YOK.** Gerekçe koddan ölçüldü:
elli sinyallik bir patlama mevcut tek-uçuş kilidi + K3 bayrağı sayesinde **en fazla bir ek tura**
çöküyor. Yeni bir `Timer` eklemek K68'in yasakladığı şeye yaklaşır ve yeni ölü-tuzak sınıfı doğurur.

**K77/6 — `CursorHint` YOKSAYILIR.** `D6` kilidi: `WireCursor.Xid` `ulong`dur, Dart `int` web'de
53-bit güvenlidir, **sayıya çevirmek yasaktır**. Hint'i karşılaştırmak metin/BigInt karşılaştırması
demektir ⇒ yeni kusur sınıfı. Sözleşmenin kendi docstring'i de *"hint, gerçek imlecin yerini tutmaz"*
diyor. Sinyal **yalnız bir uyandırma zilidir**.

**AYNI OTURUMDA KAPATILAN ÜÇ KUSUR (açılış ölçüm düzeltmesi, denetim turu değil).**
① **`D1`**: defter `GOREV-R10-rozet-turetme.md` için **13.752** bayt diyordu, diskte **16.156**; kayıt
spec büyümeden önce yazılmış ve güncellenmemişti. Düzeltme kaydı diskten `os.path.getsize` ile ölçüldü.
Bu, §8'deki **D1-ÖNLEME borcunun teorik değil aktif** olduğunun kanıtıdır (D1 bu projede 7. kez ısırdı).
② **`KANIT/R10` repoda YOKTU** — K76 "kabul edildi" demişti ama 77 dosyanın hepsi untracked'di; temiz
bir klonda R10 **kanıtsızdı**. Commit `2c4d7a1`.
③ `DURUM.md` §3 hâlâ `flutter test 136/136` diyordu; R10 sonrası **156/156**. Commit `1af620b`.

**BİLEREK KAPATILMAYAN.** `uzak_degisiklik_uygulayici.dart` defterde tur 1 (oturum 34) ve tur 3
(oturum 35) taşıyor, **tur 2 yok**. Geriye dönük bir tur-2 kaydı uydurmak `bulgu/kapatilan/uretilen`
alanlarına **sahte sıfır** yazmak olurdu ⇒ ölçüm aracını kasten körleştirmek. **Boşluk beyan edildi,
`D2` SARI kalıyor.** Aynı ilke ADR 0003'ün `D3`/`D2` kusurları için de geçerlidir (artefakt donmuş).

**AÇIK KALAN SORU (ölçülmedi, "temiz" değil).** `KANIT/slice-3d/09-MUTANT/_start_api.cmd`
`ASPNETCORE_ENVIRONMENT` **set etmiyor**; `Program.cs` ise `builder.Environment.IsDevelopment()` ile
`DevCurrentUser` açıyor (K61), aksi hâlde `NullCurrentUser` ⇒ 401. `dotnet run --no-launch-profile`'ın
ortamı Development'a düşürüp düşürmediği **ölçülmedi**. Oturum 37'de değişken **elle** set edildi ve
`/v1/tasks` başlıksız **401** / başlıklı **200** ölçüldü — yani K61 kalkanı canlı ve deny-by-default
çalışıyor, ama `_start_api.cmd`'nin kendi başına doğru ortamı kurup kurmadığı **bilinmiyor**.



## DEVİR — oturum 36 → 37 [29 Tem 2026]

**Durum:** `R10` KAPANDI ve KABUL EDİLDİ (**K76**). Bu oturumda `K75` (tasarım kilidi + **K46 açıldı**,
`DESIGN.md` **v2**) ve `K76` (kabul + `G11-A9` ölü tuzak onarımı + D2 kural 3 daraltması) kilitlendi.
Son commit **`bf2e0e7`**; ağaç temiz, `.git/index.lock` yok. **Push Onur'da — sayı yazılmaz, ÖLÇÜLÜR.**

**Sıradaki ilk iş: `slice-3e` (SignalR), K42-d adım 4.** P2'nin tetikleyicisi onun üstüne oturur;
**periyodik yoklama HÂLÂ YASAK**. Sonra `araclar/oturum-sagligi.py` (K21 — K44-a).

**Yeni oturumun ilk beş dakikası:** DURUM.md + CLAUDE.md oku (PROJE_HAFIZA.md'yi AÇMA; yalnız
K76/K75/K74 checkpoint'leri) → `tek-kopya-kapisi` → `belge-tavan-kapisi` → `radar --altin-kume` →
`radar .` → `git --no-optional-locks status --porcelain` + `Test-Path .git\index.lock` +
`rev-list --left-right --count origin/main...HEAD` → oturum sağlığını KENDİ oturum-id'nle ölç.

**Oturum 36'nın iki dersi (tekrar etmesin):**
1. **Bir mutantın "ısırması" YETMEZ — KENDİ ayağının ısırması ölçülmelidir.** `M50` ısırıyordu ama
   `G11-A9`'u geçiyordu; kolateral ısırık, o ayak değiştiği gün sessizce kaybolur.
2. **PowerShell `*>` UTF-16LE yazar**; `utf-8` okuyan betik dosyayı sessizce bozuk okur (bir kez
   yanlış NEGATİF verdi). Okuyucu `utf-16 → utf-8 → cp1254` sırasıyla denemelidir.

**Açık borçlar (DURUM.md §8 tam liste; oturum 36'da ÜÇ YENİ):** taban rozet metni **1.0× ölçekte bile
kırpılıyor** (`DESIGN.md` v2 `A-7` ısırdı, 2.0× ölçülmedi) · satırın `content-desc`'i rozet metnini
**iki kez** taşıyor · cihaz kanıtındaki `zehirli` kayıt **SQLite'a seed edildi** (render gerçek, veri
sentetik) · `tazelik-muafiyet.json` `BD-6` gerekçesi K46 açılınca bayatladı (ayrı el, K34-f) ·
eskiler: `iddia-kapisi.py` ikili tarama · `hafiza-dizin.py` K60 ihlali · `verify.ps1` fail-loud ·
`02-G2` üretici kodu + ikinci yazıcı · D1-önleme · Z10b · defter D2/D3 · `DESIGN.md` BD-1…BD-7.

**Ortam (devralınan):** Backend 127.0.0.1:5298 çalışıyor (PID YAZMA, ÖLÇ: `netstat -ano | findstr
:5298`) · emülatör `emulator-5554` açık, R10'lu APK kurulu · `adb` PATH'te **yok**, tam yol gerekir ·
**köprü bu oturumda İKİ KEZ düştü** ⇒ uzun koşumları `Start-Process -WindowStyle Hidden` ile ayrık
sürece al, çıktıyı DOSYAYA yaz, köprü dönünce oku · Cowork→PowerShell köprüsü `$` değişkenlerini
SİLİYOR (komut satırına `$` gönderme, `.ps1`/`.py` yaz) · `os.replace` WinError 5 ⇒ üç adımlı yedekli
takas · commit mesajına ÇİFT TIRNAK yazma · `git add -A` YASAK (K55).

## K76 — `R10` KABUL EDİLDİ · `G11-A9` KÖR AYAK onarıldı · D2 kural 3 daraltması KİLİTLENDİ [29 Tem 2026, oturum 36]

**Onur onayladı (iki kilit birden).** Build'i Claude Code yaptı; **Cowork hiçbir sayısına
güvenmedi ve hepsini kendi koşumuyla yeniden ölçtü (K26).**

### Cowork'ün KENDİ koşumu — builder'ın beyanı DOĞRULANDI
`flutter analyze --fatal-infos` **No issues found, EXIT 0** · `flutter test`
**`00:43 +156: All tests passed!` EXIT 0** · `design-token-kapisi` · `tek-kopya-kapisi` ·
`belge-tavan-kapisi` · `spec-kapi-kapsama` · `iddia-kapisi --kanit KANIT/R10` ⇒ **beşi de EXIT 0**.

**Kırmızı çizgiler DİSKTEN doğrulandı:** `gorev_deposu.dart`'ta `senkronDurumu` geçen 20 satırın
**hiçbiri yazma değil** · `schemaVersion => 4`, `veritabani.dart`/`.g.dart` diff'te **YOK** ·
`DESIGN.md` diff'i **BOŞ** · yedi dosyanın sha8'i beyanla **birebir**.
**Spec'in zorunlu kıldıkları:** `g10:180` pozitif iddiası **eklenmiş** · `pumpAndSettle` yalnız
yasağı anlatan **yorumda** · sorgu `leftOuterJoin`+`groupBy`+`silindi=false`+`entityType='Task'`+
`orderBy(olusturuldu,id)` **tam** · `gorev_satiri`'nda `if/else` **kalkmış** · `didUpdateWidget` **var**.

### 🔴 COWORK'ÜN BULDUĞU KUSUR: `G11-A9` KENDİ MUTANTINI YAKALAMIYORDU (ÖLÜ TUZAK)
Mutant kanıtlarının **varlığını değil İÇERİĞİNİ** taradım ve her mutantın **hangi ayağı**
düşürdüğünü ölçtüm. 12'nin 11'i kendi ayağını ısırdı; **`M50` (groupBy düşürülür) A9'u GEÇTİ** —
yalnız A4 ve A10'a takıldı. Sebep mekanik ve kesin: `addColumns([count(...)])` taşıyan bir sorgudan
`GROUP BY` düşünce SQLite **toplam sorgusuna** döner ve üç değil **TEK** satır verir ⇒ eski
`hasLength(1)` iddiası mutant altında da **doğru kalır**. Testin kendi `reason` metni
(*"groupBy düşerse satır kuyruk satırı kadar (3) tekrar eder"*) **olgusal olarak YANLIŞTI**.
**Onarım Cowork'te (K34-f: kapıyı yazan el onarmaz):** A9 **iki görevle** kuruldu (biri üç op'lu),
`hasLength(2)` + görev-başına kimlik ölçülüyor. **KANIT `KANIT/R10/09-MUTANT/M50b-*`:** temizde
G11 **14/14**, mutantta **A9 ısırıyor** (*Expected: an object with length of <2> · Which: has length
of <1>*), geri alındıktan sonra tam paket **156/156** ve `gorev_deposu.dart` sha `B5B45367`'ye döndü.
**Ders: bir mutantın 'ısırması' yetmez — KENDİ ayağının ısırması ölçülmelidir.** Kolateral ısırık,
o ayak değiştiği gün sessizce kaybolur.

### 🔴 D2 KURAL 3 DARALTMASI KİLİTLENDİ (`K != 'yerel'`)
Build sırasında ölçüldü: ham kural taze bir görevi de (`K='yerel'`, kendi ekleme op'undan `B>0`)
*"Gönderilmemiş değişiklik"* yapıyor ve `g10` AYAK6'yı kırıyordu. Tie-break **DESIGN.md v2 §4'ün
kendi tanımı**: *"gönderilmemiş"* = **satır sunucuda VAR**; taze satır sunucuda yok.
🔴 **Beyan edilmiş sınır:** kolonu hâlâ `'yerel'` olan **ESKİ** satırlar (K72/K74 mirası) sunucuda
**olsalar bile** düzenlenince "Yalnızca bu cihazda" der ⇒ `R10`'un yalanının **daralmış hâli yaşar**.
Kalıcı çözüm kolona değil **kuyruktaki op'un ANLAMINA** bakmaktır (ekleme op'u ⇒ sunucuda yok);
Onur bunu **ayrı iş** olarak bıraktı.

### Builder'ın tek FAZLA İYİMSER beyanı (ölçüldü)
*"12/12 ısırdı — hepsi `Expected:/Actual:` izli"* ⇒ **11'i doğru**. `M50` iddia ile değil
**çalışma-zamanı hatasıyla** düşüyor (`Bad state: No element`, `Please use readTableOrNull for outer
joins`). Isırıyor, ama beyan bu haliyle fazla iyimserdi. Kanıt sınıfı: *ısırdı* ≠ *iddiayla ısırdı*.

### Cihaz — PNG'ler GÖZLE denetlendi (builder'ın dosyasını okumakla yetinilmedi)
(a) kalem ikonu + "Gönderilmemiş de…" **VAR** · (b) aynı görevde rozet **YOK** · (c) kırmızı çakışma
ikonu **VE** taban rozet **yan yana** ⇒ `D7` cihazda kanıtlı.
🟡 (c)'deki taban rozet **`cevrimdisi`** (kural 2), yani *çakışma + gönderilmemiş* çifti **cihazda
değil**, yalnız `G11-A7`'de kanıtlı. 🟡 Çakışma satırının `zehirli` kuyruk kaydı **doğrudan SQLite'a
seed edildi** (builder beyan etti; render gerçek widget ağacı, sentetik olan veri).
🔴 **`DESIGN.md` v2 açık kalemi `A-7` İLK KOŞUMDA ISIRDI:** taban rozet metni **1.0× ölçekte bile
kırpılıyor** ("Gönderilmemiş de…", "Çevrimdışısın…"); 2.0× henüz denenmedi.
🟡 `_uipoll.xml`'de ölçüldü: satırın `content-desc`'i rozet metnini **iki kez** taşıyor
(`Semantics(label:)` + `Text` çocuğu) ⇒ ekran okuyucu tekrar okur. R10 öncesinden gelen sınıf.

### Ölçülen üretim
`src/client/lib` **+228 satır** (−58): `gorev_deposu.dart` +140 · `senkron_rozeti.dart` +37 ·
`gorev_listesi_ekrani.dart` +15 · `vitrin` +17 · `metinler` +10 · `gorev_satiri` +9.
Test tarafı: mevcut beş dosyada +62, **yeni `g11_rozet_turetme_kapisi_test.dart` 24.715 b**.
⇒ **`R8` SUSTU** (iki oturum 0 satır sert durağı yanmadı).

### Ortam (kanla yazılıya EK)
🔴 **Cowork↔masaüstü köprüsü bu oturumda İKİ KEZ düştü** (biri doğrulama zincirinin ortasında).
Ders uygulandı: uzun koşumlar **ayrık sürece** (`Start-Process -WindowStyle Hidden`) alınıp çıktı
**dosyaya** yazıldı; köprü dönünce sonuç kaybolmadan okundu. Ayrıca köprü dönünce **ilk iş** mutasyon
altındaki dosyanın sha'sını doğrulamak oldu (`B5B45367` ⇒ geri alma tamamlanmış).
🔴 PowerShell `*>` yönlendirmesi **UTF-16LE** yazar; Python'da `utf-8` okuyan bir betik bu dosyayı
**sessizce bozuk** okur (bir kez ısırdı: "No issues found" araması yanlış NEGATİF verdi). Okuyucu
`utf-16 → utf-8 → cp1254` sırasıyla denemelidir.

## K75 — R10 TASARIMI KİLİTLENDİ: rozet KUYRUKTAN türetilir + **K46 AÇILDI** [28 Tem 2026, oturum 36]

**Kilit Onur'dan geldi (iki turda, denetimden SONRA revize edildi).** Radar açılışta KIRMIZI'ydı
(kâğıt artefaktlar + defter D2/D3); K40 gereği dört şık sunuldu, Onur **DEVRET**'in fiilî karşılığını
seçti: kâğıt değil koşan kod — `R10`.

### Kusur (R10, oturum 35'te denetim buldu, oturum 36'da kodda doğrulandı)
`Gorevler.senkronDurumu` kolonuna **yalnız senkron döngüsü** yazar (`_rozetYaz`,
`senkron_dongusu.dart:364-370`). `gorev_deposu.dart`'ın dört yerel yazma yolu (`ekle` `:91`,
`duzenle` `:121`, `tamamlaGeriAl` `:145`, `sil` `:179`) kolona **hiç dokunmaz** (ölçüldü). Sonuç:
`'senkronize'` bir satır yerelde düzenlenince kolon değişmez, `SenkronRozeti` `SizedBox.shrink()`
çizer ⇒ **hiçbir rozet yok** ⇒ kullanıcı gönderilmemiş değişikliği senkronize sanır.

### Kilitlenen tasarım
**Kolon yazma yolları DEĞİŞMEZ.** Rozet `senkron_kuyrugu`'ndan TÜRETİLİR. Görev başına:
`U` = `durum='gonderildi'`, `B` = `durum='bekliyor'`, `Z` = `durum='zehirli'`, `K` = kolon.

- **Çakışma kanalı:** `Z>0 **||** K=='cakisma'` — 🔴 `Z>0` TEK BAŞINA YANLIŞTI, denetim yakaladı
  (aşağıda D-1).
- **Taban:** ① `U>0` ⇒ `kuyrukta` ("Gönderiliyor") · ② `U=0, B>0, K=='cevrimdisi'` ⇒ `cevrimdisi` ·
  ③ `U=0, B>0` ⇒ 🆕 `gonderilmemis` ("Gönderilmemiş değişiklik") · ④ `U=0, B=0` ⇒ K eşlemesi.
- **Çakışma ikonu ile taban rozet ARTIK BİRLİKTE çizilir** (bugünkü `if/else` kalkar).
- Akış **TEK join'li Drift `watch`** (iki stream + `combineLatest` DEĞİL): `leftOuterJoin` +
  `groupBy(gorevler.id)` + `entityType='Task'` + `silindi=false` + `orderBy`a `id` tie-break.
  `customSelect` seçilirse `readsFrom:{gorevler, senkronKuyrugu}` **PAZARLIKSIZ**.
- `Gorev` domain modeline sayım **EKLENMEZ** (F4 dikişi, `gorev_deposu.dart:11-14`); ayrı
  `GorevGorunum` tipi taşır. `rozetDikisi` saf kalır, `(K,U,B,Z)` alır, **K'yı ÖNCE doğrular**
  (tanınmayan dize `ArgumentError` — bugünkü invaryant ölmez).
- **Şema DEĞİŞMEZ, migration YOK.**

### 🔴 K46 AÇILDI (Onur, 28 Tem 2026) — `DESIGN.md` artık dokunulmaz değil
Gerekçe **ölçüldü, tercih değil**: `gorev_satiri.dart:59-62` `if (cakismaVarMi) CakismaRozeti()
else Flexible(SenkronRozeti(...))` ⇒ çakışma ile taban **karşılıklı dışlayıcı**. Yani *"hem
çakışmalı hem bekleyen satır hangisini söyler?"* sorusunun bugünkü widget'ta **cevabı yoktu** ve
donmuş dört-durum sözlüğünde doğru cümle **üretilemiyordu**. K46 kapalıyken her seçenek bir yalan
üretiyordu; Onur **Paket 3**'ü seçti: bileşik satır + yeni durum. Kapsam: `DESIGN.md` §4 durum
matrisine `gonderilmemis` satırı + bileşik gösterim + `Metinler` girdisi; `design-token-kapisi.py`
`D0`–`D6` ve `A11Y-7` genişler. **K46 tamamen kalkmadı — bu iş için açıldı.**

### Bağımsız denetim (K26 · K53/1 — TEK tur, iki ayrı denetçi, ayrı pencereler)
Üç blokerde **bağımsız olarak buluştular**:
- **D-1 [BLOKER, düzeltildi]** `senkron_dongusu.dart:305-320`: 401 dışı 4xx satırı `bekliyor`
  bırakır, `denemeSayisi`'nı **ARTIRMAZ**, kolona `'cakisma'` yazar ⇒ **zehirli satır DOĞMAZ**.
  İlk tasarımın `Z>0` kuralı ateşlenmez, bugün ekranda duran kırmızı çakışma ikonu **kaybolurdu**
  ve satır asla zehirliye geçmediği için geri gelmezdi. `g5_karantina_kapisi_test.dart:268-280`
  bugünkü davranışı ölçüyor ama **kolona** baktığı için bu gerilemeye **KÖR**. Düzeltme kilide
  girdi (`|| K=='cakisma'`).
- **D-2 [BLOKER, K46 ile çözüldü]** "dik kanal" iddiası bugünkü widget'ta **karşılıksızdı**.
- **D-3 [BLOKER, Onur revize etti]** Kural ③'ün ilk kilidi `'yerel'` ("Yalnızca bu cihazda") idi.
  İki denetçi de **gerileme** saydı: R10'un yalanı bir *susmadır* (kullanıcının çıkarımı),
  `'yerel'` ise bir *beyandır* — elde `K='senkronize'` kanıtı dururken ekrana yazılır; üstelik
  **dışlayıcılık iddiasıdır** ("kimse görmüyor") ve ürünün vitrini gerçek zamanlı işbirliği.
- **Cowork'ün denetçileri ÇÜRÜTTÜĞÜ nokta:** ikisi de `main.dart`'ı ölçemedi ve "soğuk açılışta
  bayat `gonderildi` ⇒ sonsuz dönen ok" dedi. **Ölçtüm: `main.dart:41` `await
  kurulum.dongu.gonderildiKurtar();` ve `runApp` satır 50'de** ⇒ senaryo **çürütüldü**, kural ①
  ayakta. *Denetçi de yanılır; ölçüm hakemdir.*

### 🔴 SPEC'E GİRECEK, MİMARİYİ DEĞİŞTİRMEYEN ÖLÇÜLMÜŞ KUSURLAR
- **401 iki kez yanlış etiketlenir:** `:299-304` `basariRozeti:'cevrimdisi'` ⇒ cihaz çevrimiçiyken
  "Çevrimdışı kaydedildi"; 8 tur sonra `denemeSayisi>8` ⇒ zehirli + `'cakisma'` ⇒ "Çakışma var".
  Doğru bilgi (`sonHataKodu='http-401'`) DB'de **duruyor**, türetme onu **okumuyor**.
- **`K='cevrimdisi'` GEÇMİŞ ZAMAN kalıntısıdır**, op başına yazılır; kural ② onu şimdiki-zaman
  bağlantı iddiasına çeviriyor. Rozet gerçek connectivity'yi **hiç ölçmüyor** — beyan edilir.
- **Sözlükte "silme bekliyor" yok** — sayım op'un *anlamına* bakmaz; `title` düzenlemesi ile
  `isDeleted` op'u aynı `B`'ye düşer. Çakışan **silme** kullanıcıya hiç görünmez (satır listede yok).
- **`_bekleyenleriSec` `limit(100)`** (`:172`): 101. op `gonderildi`→`bekliyor` döner, yeniden
  seçilmez ⇒ rozeti kalıcı kural ③'te kalır.
- **`gonderildiKurtar()` (`:115-117`) ile toplu `gonderildi` yazımı (`:134-137`) AYNI transaction'da
  DEĞİL** ⇒ her turda U=0 ara karesi ⇒ rozet titremesi. Kaynağı iki stream değil **iki commit**.
- **A11Y-7 duyurusu `didChangeDependencies`'te** (`senkron_rozeti.dart:34-51`): ebeveyn `durum`
  parametresini değiştirdiğinde **koşmaz** (`didUpdateWidget` koşar) ⇒ "senkronize edildi" duyurusu
  bugün hiç yapılmıyor olabilir. Türetme geçişleri normalleştirdiği için bu açık ana yola oturur.
- **KÖR MUTANT LİSTESİ (hepsi statik/widget ⇒ K53/3 gereği TAVANSIZ):** ① `entityId` filtresini
  düşür (global sayım) — tek görevli hiçbir test ısırmaz, **iki görevli** test gerekir · ② kural ①
  ile çakışma kanalının sırasını değiştir · ③ `U` sayımını daima 0 yap — **askılı ağ (`Completer`)
  ayağı olmadan ölçülemez**, kural ① kapısız kalır · ④ `.watch()` yerine `.get()` / `readsFrom`'a
  `senkronKuyrugu` koyma — mutlu yol `gorevler`'e de yazdığı için **tesadüfen** yeşil kalır;
  ısıracak tek test: render SONRASI **yalnız kuyruğa** yaz · ⑤ `rozetDikisi`'nin `throw`'unu
  `try/catch` ile yut — **bu kapı BUGÜN bile kör**, hiçbir assert ölçmüyor.
- 🔴 **MEVCUT 20 TESTİN HİÇBİRİ BU TASARIMI YANLIŞLAMIYOR** (ikisi ayrı ayrı saydı) ⇒ `R10` **yeni
  kapı olmadan kapatılamaz**. `g10 AYAK6` yeşil kalır ama **sebebi değişir** (K='yerel' değil, B=1)
  ⇒ `ekle()` bozulsa bile geçer: *"derlenir, geçer, hiçbir şey ölçmez"* sınıfı.

### Beyan edilmiş sınırlar (gizlenmedi)
- `DESIGN.md` yazıldığı an `design-token-kapisi.py` **KIRMIZI** yanar ve build inene kadar öyle
  kalır — bu **beklenen ve beyan edilmiş** ara durumdur, susturulmaz.
- `DESIGN.md` `tek-kopya-kapisi`'nda **`kilitli`** sınıftadır ve karşılaştırma **HEAD'e** göredir
  ⇒ değişiklik commit'lenene kadar `S6` yanar. Yeni kimlik `DURUM.md` §9'a **son yazımdan SONRA**
  ölçülüp yazılır; eski `534DFF68` **geçersiz** işaretlenir.
- K74'ün sınırı aynen durur: düzeltmeden **önce** inmiş satırlar `'yerel'` kalır (migration yasak).

## 🔒 CHECKPOINT (28 Tem 2026 · oturum 35 KAPANIŞ: **K74 — `R9` KABUL EDİLDİ (Cowork'ün kendi koşumu); `K72` §5'ten ÇEKİLDİ**)

### K74 — `R9` rozet kapsamı düzeltmesi KABUL EDİLDİ

Claude Code `T1`–`T4`'ü uyguladı ve devretti. **Beyanına güvenilmedi (K26); Cowork her kriteri
kendi koşumuyla ölçtü.** Sonuç: **KABUL**.

**COWORK'ÜN KENDİ ÖLÇÜMLERİ (builder'ın dosyaları KULLANILMADI):**
`iddia-kapisi.py --kanit KANIT\R9` **HÜKÜM TEMİZ, EXIT 0** (ilk bağımsız koşum) ·
`spec-kapi-kapsama.py` **EXIT 0** · `flutter test` **142/142 EXIT 0** (136 → 142; `G10`'un altı
ayağı) · `flutter analyze --fatal-infos` **0 bulgu, EXIT 0** · `tek-kopya-kapisi.py` **YEŞİL** ·
`belge-tavan-kapisi.py` **YEŞİL** · spec kimliği **14.061 b · `B2082127` DEĞİŞMEMİŞ**.

**KIRMIZI ÇİZGİLER DİSKTEN DOĞRULANDI, beyandan değil:**
`git diff` `uzak_degisiklik_uygulayici.dart`'ta **tek hunk** gösteriyor (`@@ -221,7 +221,11 @@`) ve
tamamı `mevcut == null` dalında ⇒ **UPDATE dalına tek bayt dokunulmamış** (kriter 6 ✔) ·
`git diff --stat -- src/backend/` **boş** (kriter 7 ✔) · `schemaVersion => 4` ve
`test/generated_migrations/` **temiz** (kriter 5 ✔) · `gorev_deposu.dart` diff **boş** ⇒ `R10`
kırmızı çizgisine uyulmuş.

**`G10`'un altı ayağı gerçek ve doğru kurulmuş** — test adları ölçüldü: `AYAK5` *"changesUygula ile
UÇTAN UCA doğan (INSERT-from-pull) satır"* diyor (sentetik değil), `AYAK6` *"DriftGorevDeposu.ekle()
ile üretilmiş satır"* diyor ⇒ spec'in iki **pazarlıksız** şartı da harfiyen uygulanmış.

**MUTANT KANITLARI TEK TEK TARANDI (K70'in dersi):** `M41`–`M45`'in beşinde de **assertion izi var**,
**hiçbiri yalnızca derleme hatası değil**, hiçbiri boş değil (11–29 KB) ve **beşinin de `[BEYAN]`
bölümü var** — kriter 2'nin *"yalnız `G10` mu, mevcut testle birlikte mi düştü"* şartı gerçekten
karşılanmış. `M43.txt`'nin `M41`'e atfı **meşru**: aynı kolateral sınıfı beyan ediyor.

### 🔴 COWORK CİHAZDA KENDİ TEKRARINI KOŞTU — ve önce YANLIŞ ALARM verdi

Builder'ın cihaz kanıtı kabul edilmedi; Cowork **kendi** uzak görevini üretip (`opId` v7,
HTTP 200 `Applied`) uygulamayı `am force-stop` ile öldürdü, **ölüyken** yazdı, `am start` ile soğuk
açtı. **İlk hüküm KIRMIZI çıktı:** görev inmemiş, imleç ilerlememişti.

**Teşhis edildi ve kusur BENDEYDİ: 22 saniyelik sabit `sleep` yetmemişti.** Emülatör yazılım GPU'da
koşuyor (bu oturumda `Skipped 396 frames`, `Davey! duration=5728ms` ölçüldü). Birkaç saniye sonra
bakıldığında görev inmiş, imleç `{"xid":1223,"seq":281}` → `{"xid":1226,"seq":282}` ilerlemiş,
sunucuya cihazın imleciyle sorulduğunda `changes: 0 · hasMore: false` ⇒ cihaz **tam senkron**.

🔴 **DOKTRİN DERSİ (kendi ölçüm yöntemime karşı):** **sabit `sleep` bir ölçüm değildir.** Doğrusu
*koşula kadar yokla* (`poll-until-condition`), tavanlı. Bu kusur bu oturumda **iki kez** oynadı ve
ikinci yüzü daha kötü: **kabul kriteri 9'un ilk ölçümünde (K71) 15 saniye kullanmıştım ve GEÇMİŞTİ
— yani o geçiş titizlik değil ŞANSTI.** Aynı ölçüm 22 saniyeyle kalıyordu. Ölçüm aracı
belirsizse, ölçümün *geçmesi* de *kalması* da bilgi taşımaz. Borç `DURUM.md` §8'e yazıldı.

**BAĞIMSIZ SONUÇ (ölçüldü):** Cowork'ün görevi cihaz DB'sinde `senkron_durumu = 'senkronize'` ve
ekranda **rozetSİZ** (`KANIT/R9/09-COWORK-DOGRULAMA/01-cowork-bagimsiz-ekran.png`, 101.062 b,
PNG imzası doğrulandı). **`R9` gerçekten düzeldi.**

**BUILDER'IN DÜRÜST BEYANI DOĞRULANDI:** düzeltmeden **önce** inmiş üç eski satır (`G6 android
kaniti`, `UZAKTAN GELEN GOREV…`, `SOGUK ACILIS KANITI 35`) hâlâ `'yerel'` ve rozetli görünüyor.
Bu **beklenen ve doğrudur** — spec migration'ı yasaklıyor (kriter 5), `T1` yalnız bundan sonraki
çekmeleri düzeltir, geçmişi değil. Builder bunu gizlemek yerine *"DÜRÜST KIYAS"* başlığıyla kendi
devir notuna yazdı.

### 🔴 YENİ ARAÇ KUSURU — `iddia-kapisi.py` İKİLİ DOSYALARI METİN GİBİ TARIYOR

Builder 4 SARI `I3` *"hayalet kanıt"* (`M0`/`M3`/`M5`/`M7`) bildirdi ve *"araç kusuru, benim kanıtım
değil"* dedi. **Bağımsız tarandı: iddiası DOĞRU.** Kaynak `02-cihaz/03-soguk-acilis-listede.png`;
89.628 baytlık PNG'nin **rastgele ikili baytları** `\bM\d\b` desenine denk düşüyor.

🔴 **Ama bu, gürültü değil ADI KONMASI GEREKEN BİR KAPI KUSURUDUR.** Bugün yanlış-pozitif üretti;
**tehlikeli yönü tersidir:** yeterince büyük bir ikili dosya bir gün `M41` desenine denk düşerse,
`iddia-kapisi.py` o mutantın kanıtı **varmış gibi** sayar — yani **kanıt-kazayla-sağlanır**. Bu tam
olarak bu projenin *"kör kapı"* sınıfıdır, ölçüm katmanında. Onarım: kapı yalnız **metin** dosyaları
taramalı (`.txt`/`.md`/`.log`) ya da ikili dosyaları açıkça atlamalı. 🔴 **DÜZELTİLMEDİ — K34-f:**
`iddia-kapisi.py`'yi Cowork yazdı (K67), onaran el ayrı olmalı. Borç §8'e yazıldı.

**Diğer küçük gözlemler (gizlenmiyor, hiçbiri bloker değil):** `07-backend-diff-stat.txt` **0 bayt**
ve bu **doğrudur** (diff gerçekten boş) · `KANIT/R9` kökünde dört dosya `02-cihaz/` altındakilerle
**bayt-özdeş** (kopya; zarar yok) · `_tmp_green_M41…M45` ve `01-G10/g10-test-ciktisi.txt` beşi de
**bayt-özdeş** — `G10`-only koşum çıktısı deterministik olduğu için beklenen, ama beyan edilmesi
gerekir · builder'ın bıraktığı `_run.cmd`/`_baseline/`/`_tmp_*` iskelesi **silinmedi** (proje kuralı:
silme Onur'da).

### `K72` ARTIK ÇEKİLEBİLİR — K73 kendi şartını doğruladı

K73 şöyle demişti: *"kapısı olmayan kilit çekilemez; `K72` §5'te DURUYOR çünkü `G10` henüz yok."*
`G10` artık **var ve koşuyor** ⇒ `K72` §5'ten çekildi ve slice atıf satırına eklendi. **Kural, ne
zaman uygulanacağını kendisi söyledi ve uygulandı** — kilit bir kereye mahsus bir karar değil,
işleyen bir mekanizma.

### Oturum 35 — kapanış özeti

Beş kilit: **K71** (kriter 9 geçti, `R9` doğdu) · **K72** (`P6`/`D4` daraltıldı) · **K73** (kabul
edilen dilimin kilitleri §5'ten çekilir + `belge-tavan-kapisi.py`) · **K74** (bu). `R9` doğdu ve
**aynı oturumda kapandı**. Üretilen kusurların hepsi kayda geçti: benim dört spec blokerim, yedinci
D1 ihlalim, sabit-`sleep` ölçüm kusurum, çürüyen `kilit-indeks.py` planım.

---


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 35: **K73 — KABUL EDİLEN DİLİMİN KİLİTLERİ §5'TEN ÇEKİLİR; `belge-tavan-kapisi.py` DOĞDU**)

### ÖNCE: BENİM ÖNERDİĞİM ÇÖZÜM ÖLÇÜMLE ÇÜRÜDÜ — kurulmadı

Onur *"`DURUM.md` sorununu nasıl çözeceğiz?"* diye sordu. İlk cevabım şuydu: **`kilit-indeks.py`** yaz,
`hafiza-dizin.py` desenini izle, §5'i arşivden **türetilmiş bir indekse** çevir (`K72 · hafıza satır
110`), K19-a'nın *"gövde atıf yapar, kopyalamaz"* doktrinini nihayet §5'e uygula. Tahminî kazanç
**~7,6 KB**.

**Kurmadan önce ölçtüm ve plan İKİ KEZ öldü:**

1. **Kapsama ölçüldü:** §5'teki **27** kilidin yalnız **7'si** arşivde `### K<n> — …` başlığı
   taşıyor. Kalan 20'si (`K62 K64 K63 K66 K65 K53 K59 K57 K60 K57-b K58 K55 K56 K46 K42-d K41
   K44-a K34-f K26 K40`) türetilemez ⇒ araç ya onları **sessizce düşürürdü** ya da uydururdu.
   Kopyayı kaldırmak için yazılan araç, kaybı kopyadan **daha kötü** hâle getirecekti.
2. **Daha derin kusur:** `PROJE_HAFIZA.md`'nin DİZİN'i bir **navigasyon** aracıdır; §5 ise
   **yürürlükteki kural kitabı**dır. `K55 · hafıza satır 1432` satırı, K55'in *ne yasakladığını*
   söylemez — oturum kuralı öğrenmek için 629 KB'lik arşivi açmak zorunda kalırdı ki K53 bunu tam
   olarak yasaklamıştı. **Aynı desen iki farklı işte doğru olmaz.**

**Ders (genelleşir):** *bir deseni "burada işe yaradı" diye başka bir yere taşımak, o yerin ne iş
gördüğünü ölçmeden yapılırsa mühendislik değil taklittir.* İki ölçüm betiği toplam ~100 satırdı ve
yanlış bir aracın doğmasını engelledi — K53/1'in gerekçesinin (*mekanik kontrol ucuz ve
deterministik*) bir kez daha karşılığı.

**İkinci mekanik ayrım denemesi de çürüdü:** §5'i *"kural"* ⇄ *"olay kaydı"* diye ayırmak için iki
ölçülebilir işaret (kimlik/sha taşıyor mu · kip taşıyor mu) kullandım. Sonuç: **%18,9 olay · %10,9
kural · %70,1 BELİRSİZ**. Kilitler gerçekten karışık yazılmış; **mekanik ayrım yok.** Uydurulmuş bir
eşikle bölmek K40 ihlali olurdu — yapılmadı.

### K73 — KURAL: BİR DİLİMİN TASARIM/SPEC KİLİTLERİ, DİLİM **KABUL EDİLDİĞİNDE** §5'TEN ÇEKİLİR

Mekanik ayrım yoksa, **olgusal** bir ayrım var ve o ölçülebilir: *dilim kabul edildi mi?*

> **Bir dilimin tasarım ve spec kilitleri, dilim KABUL EDİLDİĞİ ANDA `DURUM.md` §5'ten çıkarılır ve
> tek satırlık bir atıfla temsil edilir — çünkü o andan sonra kural PROZADA değil KAPIDA yaşar.**

Gerekçe projenin kendi doktrinidir (K53): *mekanikleşmiş bir sınıf için kâğıt tur meşru değildir.*
`D0`–`D9`, `P1`–`P7` gibi kurallar bugün `G1`–`G9` kapılarında ve 40 mutantta **koşuyor**; §5'teki
kopyaları bir şeyi zorlamıyor, yalnız **sapma kanalı** açıyor (K21-DÜZELTME'nin doğuş mekanizması).
**Hiçbir şey silinmedi:** K57…K70 checkpoint'lerinin tam metni arşivde duruyor, spec'ler
`GOREV_CLAUDE_CODE/` altında bayt-kimlikli ve `tek-kopya-kapisi.py` kapsamında `kilitli`.

**ÇEKİLENLER (10 kilit, 4.606 b) ve nerede yaşadıkları:**

| çekilen | dilim | kural bugün NEREDE zorlanıyor |
|---|---|---|
| `K57` · `K59` | slice-3b | `GOREV-slice-3b-istemci-iskeleti.md` **44.560 b · `F0C3A75A`** + A2/G kapıları |
| `K62` `K63` `K64` `K65` `K66` | slice-3c | `GOREV-slice-3c-senkron.md` **41.692 b · `537D0579`** + `D0`–`D9` kapıları |
| `K68` `K69` `K70` | slice-3d | `GOREV-slice-3d-cekme.md` **80.399 b · `889A383F`** + `G1`–`G9` + 40 mutant |

🔴 **`K72` ÇEKİLMEDİ ve bu kasıtlıdır:** `P6`/`D4` daraltması **henüz build edilmedi**, yani kuralı
zorlayan `G10` kapısı **yok**. Kural kapıda yaşamıyorsa prozadan çıkarılamaz. `R9` kabul edildiğinde
`K72` de bu maddeyle çekilecektir — **kuralın kendisi ne zaman çekileceğini söylüyor.**

**ÖLÇÜLEN SONUÇ (yazımdan SONRA diskten, D1-önleme):** §5 **9.646 → 6.219 b** (çekilen 10 madde **4.606 b**; geri konan K73 kuralı + üç atıf satırı ~1,9 KB) · `DURUM.md` **32.298 → 29.625 b** · pay **470 → 3.143 b** ⇒ `belge-tavan-kapisi.py` artık **YEŞİL**.

🔴 **KENDİ D1 KUSURUM, AYNI OTURUMDA, ALTINCIDAN SONRA YEDİNCİ KEZ:** bu paragrafı ilk yazdığımda içine **ölçmediğim** iki sayı koydum (*"§5 5.030 b"*, *"DURUM.md 27.9xx b"*) — ikisi de **YANLIŞTI**. Bu oturumun kendi checkpoint'i (K71) D1-önlemeyi *"defter/belgeye sayı yazan HER betiğe zorunlu olsun"* diye borç yazmıştı ve ben aynı oturumda **belgeye** elle sayı yazarken o adımı atladım. Düzeltmeyi yapan şey yine bir betik oldu, el değil. **Borç artık yalnız defter için değil, HER belge yazımı için geçerlidir.**

### `belge-tavan-kapisi.py` DOĞDU — üç oturumluk beyan edilmiş zayıflık kapandı

K58 tavanı koyarken kendi zayıflığını **beyan etmişti**: *"tavanı şu an hiçbir kapı zorlamıyor — ilk
ısırışta `belge-tavan-kapisi.py` yazılır."* Tavan o günden beri **on üç kez** aşıldı (oturum 34'te
yedi, 35'te altı) ve her seferinde **el ile** budandı. Beyan edilmiş zayıf kontrol, kontrol değildir.

**Araç, tavanın kendisini değil PAYI da ölçer** — çünkü bu projede kusur *"tavanı aştım"* biçiminde
değil, *"tavana 38 bayt kala checkpoint yazamıyorum"* biçiminde doğdu. Kodlar: `T0` dosya yok ·
`T1` tavan aşıldı (KIRMIZI) · `T2` pay dar (SARI, eşik tavanın %5'i).

**Altın küme 9/9** ve iki vakası özellikle önemlidir: **vaka 5** (pay eşiğinin tam üstünde ⇒ SUSMALI)
eşiğin uydurulmadığını kanıtlar; **vaka 7** (dosya yok ⇒ `T0`, ama KIRMIZI DEĞİL) *"ölçemediğini
kırmızı sayma"* kuralını kapının içine gömer. **İlk gerçek koşumda hemen ısırdı:** `DURUM.md`
32.298 / 32.768 b, pay 470 b ⇒ **SARI `T2`**. Kapı doğduğu gün borcu gösterdi.

**Beyan edilmiş sınır (çıktıya basılır):** içeriğin gerekli olup olmadığını ölçmez, neyin
silineceğine karar vermez, tavanı kendi değiştirmez (K40).

### 🔴 YENİ BULGU — `hafiza-dizin.py` K60'I İHLAL EDİYOR ve hedefi 629 KB'LİK ARŞİV

`araclar/hafiza-dizin.py` sonunda şu satır var:
`io.open(yol, "w", encoding="utf-8", newline="\n").write(metin)`

**Bu tam olarak K60'ın yasakladığı desendir** ve K60 oturum 31'de `PROJE_HAFIZA.md`'yi **542 KB'den
0 bayta düşürdüğü** için doğmuştu. `hafiza-dizin.py` bu dosyaya yazan **tek araçtır** ⇒ dolu silah,
namlusu arşive dönük. Bugün patlamamasının tek sebebi encode'un hata vermemesi.

🔴 **DÜZELTİLMEDİ — K34-f:** aracı onaran el, onu **yazan elden AYRI** olmalı ve `hafiza-dizin.py`'yi
Cowork yazdı (K58, oturum 31). Borç `DURUM.md` §8'e adıyla yazıldı. *(Bu oturumda yazılan
`belge-tavan-kapisi.py` **hiçbir dosyaya yazmıyor** — salt okur, bu sınıfa hiç girmiyor.)*

---


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 35: **K72 — `R9` KİLİDİ: `P6`/`D4` DARALTILDI; spec yazıldı, DENETLENDİ, KIRILDI, düzeltildi**)

### K72 — `P6`/`D4` DARALTILDI (Onur kilitledi, 28 Tem 2026)

`R9` (K71) Onur'a dört şıkla sunuldu; kilit **DARALT**: *`P6`/`D4`'ün rozet dokunulmazlığı
**UPDATE-of-local** dalında AYNEN kalır; **INSERT-from-pull** dalı kapsam dışına alınır ve
çekmeyle doğan satır `senkronDurumu = 'senkronize'` ile INSERT edilir.*

**Reddedilen şık ve gerekçesi yazıya geçti:** *"rozet metnini yumuşat / rozeti kaldır"* reddedildi
çünkü yanlış cümleyi **susturur ama bilgi kaybettirir** — kullanıcı gerçekten yalnız kendi cihazında
duran görevi ayırt edemezdi. Bu red spec'te **`AYAK6`** olarak **mekanik şarta** çevrildi: *gerçekten
`yerel` olan satır hâlâ "Yalnızca bu cihazda" demelidir.* **Karar bir kapıya dönüştü; sonraki el onu
sessizce geri alamaz.**

### TASARIM ÖLÇÜMLE DARALDI — kâğıtta tartışılmadı

Kilitlemeden **önce** iki varsayım diskten ölçüldü ve ikisi de işi küçülttü:

1. **CHECK kısıtı `senkronize`'ye ZATEN İZİN VERİYOR** (`veritabani.dart:18-25`, `schema_v4.dart:52-61`;
   `GorevlerCompanion.insert` alanı **opsiyonel** alıyor, `veritabani.g.dart:388-397`) ⇒ **MIGRATION
   YOK, şema v4 KALIR.** Bu ölçüm yapılmasaydı iş bir şema turu olarak planlanırdı.
2. **`changes` ve `snapshot` AYRI ayrıştırıcılardır ama AYNI yazıcıdan geçer** (`:106`/`:158` → `:203`)
   ⇒ **tek satırlık düzeltme iki dalı da kapsar.**

Kusurun tam yeri: `uzak_degisiklik_uygulayici.dart:224` (`_projeksiyonYaz`, `mevcut == null` dalı).
UPDATE dalına (`:235`) **dokunulmaması**, kabul kriteri 6'da *diff'te `+`/`-` görünmemesi* olarak
**mekanikleştirildi**.

### 🔴 BAĞIMSIZ DENETİM KOŞTU VE SPEC'İ KIRDI — 4 BLOKER · 3 ÖNEMLİ · 5 NOT

K26 sağlandı: spec'i **yazan el Cowork**, denetleyen el **ayrı bir ajan**, ve denetçi spec'in her
atfını **prozadan değil diskten** doğruladı. K53/1 gereği **tek tur**. Denetim kendini fazlasıyla
ödedi — **dört blokerin ikisi doğrudan benim kusurumdu:**

- **`B1` — spec `flutter test`'i KIRACAKTI ve kendi kabul kriterini çürütüyordu.**
  `g3_ayristirici_kapisi_test.dart:162-172` bugün tam tersini iddia ediyor:
  `expect(yeni.senkronDurumu, 'yerel', reason: 'D4 BEYAN: yeni entity yerel ile dogar')`. Spec
  *"mevcut 136 test düşmez"* diyordu ve o 136'nın **biri** `T1`'in tersiydi. ⇒ **`T3` eklendi**
  (test **silinmez, tersine çevrilir**) ve kriter 3 dürüstçe yeniden yazıldı.
- **`B2` — bayat atıf kusuru KENDİNİ YENİDEN ÜRETİYORDU.** Spec *"`senkron_dongusu.dart:186`"*
  diyordu; **`:186` BOŞ SATIR.** Gerçek yazıcı `:274` (`_rozetYaz(... 'senkronize')`), gövde `:364`.
  Daha kötüsü: `T2` bu yanlış atfı **kaynak koda gömülecek bir yoruma** yazdıracaktı. Yani bayat-atıf
  sınıfını düzelten görev, aynı sınıftan yeni bir vaka doğuruyordu.
- **`B3` — `M45` bu kod tabanında UYGULANAMAZDI.** `_projeksiyonYaz` tek parametre alır ve çağıranı
  ayırt etmez ⇒ *"yalnız `changes` yolunda düzelt"* bir mutasyon değil **yapısal refactor** olurdu.
  İptal edildi, gerekçesiyle **mutant borcuna** yazıldı; yerine snapshot dalının yazıcıyı hiç
  çağırmaması kondu.
- **`B4` — SPEC KENDİ KAPILARINDA KIRMIZI YANIYORDU ve ben kriteri KOŞMADAN yazmıştım.** Ölçüldü:
  `spec-kapi-kapsama` **EXIT 3** (`### G<n>` başlığı ayrıştırılamıyor — başlığı ters tırnak içinde
  yazmışım), `iddia-kapisi` **EXIT 1** (`I1`: metinde *"bir mutant"*, tabloda 5). ⇒ **`iddia-kapisi`
  R9 için "0 mutant" görüyordu; `--kanit` verilse bile `M41`–`M45`'in kanıtını HİÇ ARAMAYACAKTI.**
  Bu, **kör kapının ölçüm katmanındaki hâli**dir: kriter yazmak, kriteri koşmak değildir.
  Düzeltildikten sonra **ikisi de EXIT 0**.

**EN SİVRİ ÖNEMLİ — `R10` DOĞDU:** çekilmiş `senkronize` bir satır yerelde düzenlenirse rozet
`senkronize` kalır ⇒ `SizedBox.shrink()` ⇒ **hiçbir rozet görünmez** ve kullanıcı gönderilmemiş bir
değişikliği senkronize sanar (`gorev_deposu.dart`'ın dört yazma yolu `senkronDurumu`'na yazmıyor).

🔴 **`R10` BİLİNÇLE KAPSAM DIŞI BIRAKILDI — ve gerekçesi ölçüldü, tahmin edilmedi.** Denetçinin
önerdiği düzeltme (yerel yazma yollarına *"rozeti `yerel` yap"*) **iki mevcut kapıyı kırıyor**:
`g5_karantina_kapisi_test.dart:212-214` `duzenle()` sonrası rozetin **`cakisma` KALMASINI** bekliyor
(`:216-219` de aynı). Yani gerçek soru *"bir satır hem ÇAKIŞMALI hem BEKLEYEN ise rozet hangisini
söyler?"* — bu bir **öncelik kilidi**dir, Onur'dan gelir, build'de uydurulamaz. **Dürüst kıyas
yazıldı:** sınıf `R9` düzeltmesinden önce de vardı (itilmiş görevler için); `T1` onu çekilmiş
görevlere de genişletir; net kötüleşme **dar ama gerçek** ve gizlenmiyor.

**Bu bulguyu bulan şey bir denetim turu değil, 20 satırlık bir `Select-String` taramasıydı** —
K53/1'in kendi gerekçesinin (*"mekanik kontrol ucuz ve deterministik"*) altıncı kez doğrulanması.

**Denetimin TEMİZ çıkardıkları da kayda geçti (gizlenmedi):** 19 atıfın **14'ü doğru**, 1 yanlış
(`B2`), 1 bir satır kaymış (`:52-58` → gerçeği `:51-57`), 3 ölçülemedi. `cakisma`/`cevrimdisi`/
`silindi=true` sınır gerekçelerinin **üçü de bağımsız olarak doğrulandı**. Ayrıca denetçi `AYAK3`'ün
ön koşulunun üretimde erişilemez olduğunu ölçtü (`'kuyrukta'` hiçbir üretim yolunda yazılmıyor) ⇒
ayak **`cakisma` ile** yeniden kuruldu; ve `AYAK6`'nın sentetik satırla kurulursa hiçbir şey
kanıtlamayacağını gösterdi ⇒ ayak **`DriftGorevDeposu.ekle()`ye** bağlandı.

### SPEC — kimlik ve boyut

`GOREV_CLAUDE_CODE/GOREV-R9-rozet-kapsami.md` · **14.061 b · `B2082127`** · U+FFFD 0 · CRLF 0.
Kapı **G10** (altı ayak) · mutant **M41–M45** (hepsi statik/widget ⇒ K53/3'ün koşan-uygulama tavanı
**devreye girmez**) · üç gerekçeli mutant borcu.
**Kapılar Cowork'ün kendi koşumunda:** `spec-kapi-kapsama` **EXIT 0** · `iddia-kapisi` **EXIT 0**.

**`R4` freni bilinçle uygulandı:** slice-3d spec'i 80.399 b idi, bu **14.061 b**. Gerekçe K53'tür —
tasarım kararı canlı ölçümle alındığı için kâğıtta yeniden tartışılacak bir şey yok; Claude Code'un
ihtiyacı **tam değişiklik + ısıran kapı + mutantlar**dır. 🔴 **Spec `tek-kopya-kapisi.py` kapsamına
`kilitli` olarak EKLENMEDİ** — Onur onu *"kilitli"* ilan etmedi; kilitlenen **tasarımdır** (K72),
spec metni değil. Kilit istenirse ayrı bir karardır.

### ROL BÖLÜMÜ KORUNDU — bedeli gizlenmiyor

Cowork tasarımı kilitledi ve spec'i yazdı; **kodu Claude Code yazacak**. Sonuç: bu oturumun
`urun_kodu_satiri` = **0**. Önceki oturum 3.070 yazdığı için `R8` (iki ardışık sıfır) **tetiklenmez**,
ama bir sonraki oturum da ürün kodu üretmezse `R8` **ısırır ve haklı olur** — K53/4 tanımı gereği
spec, araç ve belge sayılmaz.

---


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 35: **K71 — KABUL KRİTERİ 9 ÖLÇÜLDÜ (GEÇTİ); `R9` KUSURU DOĞDU; `DURUM.md` §8 ARŞİVLENDİ**)

### K71 — kabul kriteri 9 GEÇTİ; koşan uygulama kâğıdın göremediği bir kusur gösterdi

Onur'un kilidi: radar KIRMIZI'da **DURDUR** (kâğıdı park et, görünen çıktıya geç) + `DURUM.md`
kapanmış tarihçesini arşive taşı. İkisi de yapıldı.

**KRİTER 9 (spec §7/9, kilitli metin):** *"Uygulama Android'de açıldı ve çalıştı (ekran görüntüsü);
açılışta çekme turu koştu ve uzaktan gelen bir görev listede göründü."* ⇒ **HÜKÜM: GEÇTİ, 3/3 ayak.**
Ham kanıt: `KANIT/slice-3d/10-KABUL9/` (10 dosya; `00-HUKUM.md` ölçüm hükmü).

Ölçüm kurgusu K26'ya uygun: builder'ın beyanı kullanılmadı, Cowork **kendi** koşumunu yaptı.
Görünürlük ancak *aynı `actorId` + FARKLI `clientId`* ile ölçülebilir (`SyncPuller.cs:40` yalnız
`owner_id` filtreli çeker) ⇒ cihazın `devUserId`'si (`3e966072…`) cihaz DB'sinden **ölçüldü**,
"ikinci cihaz" olarak `9a1c47d2…` uyduruldu. Soğuk ayak asıl kanıttır: uygulama `am force-stop` ile
**öldürüldü** (`pidof` çıkış 1), **ölüyken** sunucuya ikinci görev yazıldı, `am start` ile soğuk
açıldı ve görev indi ⇒ turu tetikleyen **açılıştır**, kullanıcı eylemi değil (D0: yoklama YOK).
İmleç `{"xid":1217,"seq":0}` → `{"xid":1217,"seq":279}` → `{"xid":1220,"seq":280}`.

**İKİ KAPI YAN ÜRÜN OLARAK CANLI DOĞRULANDI:** ① `P4`/v7 nibble — gönderilen iki opId de UUIDv7
üretildi ve `IsEnvelopeValid` (`SyncIngest.cs:131`) ikisini de kabul etti. ② `B3`/`olusturuldu`
türetmesi — yeni satırların `olusturuldu` değeri op-HLC `wallMs`'iyle **milisaniyesi milisaniyesine**
aynı, cihaz saatinden DEĞİL. K69/EK-2'nin çözümleri canlıda tuttu.

### 🔴 `R9` — YENİ KUSUR: UZAKTAN GELEN GÖREV EKRANDA *"Yalnızca bu cihazda"* DİYOR

Sunucudan inen iki görevin ikisi de saat ikonlu **"Yalnızca bu cihazda"** rozetiyle görünüyor
(`06-soguk-acilis-listede.png`). Cümle **olgusal olarak yanlıştır** — o satırlar sunucudan indi.

**Mekanizma koddan ölçüldü:** `P6` (K69) çekmeyle doğan satırı `senkron_durumu='yerel'` bırakıyor;
`senkron_rozeti.dart:52-58` bunu `Metinler.yalnizcaBuCihazda`'ya çeviriyor; `senkronize` durumunda
rozet **hiç çizilmiyor** (`SizedBox.shrink()`) ⇒ kullanıcının gördüğü tek sinyal `yerel`'dir ve
yanlış tarafa basıyor.

**`P6`'nın gerekçesi bu vakayı KAPSAMIYOR.** `P6`'nın koruduğu şey *bekleyen yerel yazımı olan* bir
satırın rozetinin uzak echo'yla ezilmemesidir (`P7` ile kardeş). **Çekmeyle DOĞAN satırda bekleyen
yazım YOKTUR** — ölçümde `senkron_kuyrugu` **0 satır**. Kilit, **INSERT-from-pull** ile
**UPDATE-of-local**'ı ayırmadan yazıldığı için kapsamı dışına taştı.

**Neden kâğıtta bulunamazdı:** üç denetim turu, 40 mutant ve 136 widget testi `P6`'yı *"rozete
dokunma"* olarak **doğru** uyguladı. Kusur uygulamada değil, **kilidin kapsamındadır**; ancak gerçek
bir listeye bakınca görünür. Doktrin teyidi: `R8`/DURDUR kilidinin ödediği bedel tam olarak budur —
*koşan kod üzerindeki kapı kendini doğrular, kâğıt üzerindeki kapı doğrulanamaz* (K53/5).

**Düzeltilmedi:** bu bir **tasarım kilidi (`P6`) değişikliğidir**, kilit Onur'dan gelir; oturumun
kilidi DURDUR/kriter 9 ölçümüydü, kapsam dışına çıkılmadı.

**Yan bulgu:** `senkron_rozeti.dart:9-11` doküman yorumu **bayat** — *"'senkronize' hiçbir zaman
gerçek veriden doğmaz"* diyor, oysa cihaz DB'sinde `abcf4930…` satırı `senkronize` ve onu
`senkron_dongusu.dart:186` gerçek itme turunda yazıyor.

### 🔴 ÖLÇÜM ARACININ KENDİ KUSURU: BOZUK PNG

İlk ekran yakalaması `adb exec-out screencap -p > dosya.png` ile alındı ⇒ **169.840 bayt**;
PowerShell'in `>` yönlendirmesi **ikili veriyi metne çevirip bozuyor**. Aynı kare `screencap` +
`adb pull` ile **84.550 bayt** ve geçerli PNG imzası (`137,80,78,71,13,10,26,10`) döndü. Bozuk dosya
silindi, kalan üç PNG'nin imzası tek tek ölçüldü. **Ders: kanıtın VARLIĞI, kanıtın GEÇERLİ olduğunu
göstermez.** Bu, `iddia-kapisi`'nin beyan edilmiş sınırının (`I2`/`I3` varlığı ölçer, geçerliliği
değil) ikili dosyalardaki hâlidir.

### 🔴 DEVİR NOTUNUN BİR SAYISI BAYAT ÇIKTI — "yazılı sayma, ÖLÇ" ilk koşumda karşılığını verdi

Devir notu *"11 COMMIT PUSH BEKLİYOR"* diyordu. Açılışta ölçüldü:
`git --no-optional-locks rev-list --left-right --count origin/main...HEAD` ⇒ **`0  0`**. Onur push'u
yapmıştı. Notun kendisi *"YAZILI SAYMA, OLC"* diye uyarıyordu ve uyarı haklı çıktı.

### `DURUM.md` §8 ARŞİVLENDİ — ALTI İDDİA **BAYAT** ÇIKTI (hepsi diskten ölçüldü, prozadan değil)

`DURUM.md` 32.596 / 32.768 b idi (**pay 172 bayt**) ⇒ checkpoint yazmak imkânsızdı. Onur kilitledi:
kapanmış tarihçe arşive. Taşımadan önce §8'in **her** iddiası diskten yeniden ölçüldü ve altısı
**bayat** çıktı — yani her oturumun okuduğu TEK dosya, kapanmış borçları açık diye taşıyordu:

1. *"`I2`/`I3` slice-3d'de HENÜZ KOŞMADI"* (iki ayrı yerde) ⇒ **BAYAT.** K70'te koştu ve temiz döndü.
2. *"BACKEND `OperationId`'nin UUIDv7 olduğunu ZORLAMIYOR [K66/4]"* ⇒ **KAPANDI.** Ölçüldü:
   `SyncIngest.cs:131` — `(op.OperationId.ToByteArray()[7] >> 4) != 0x7`, `IsEnvelopeValid` içinde.
3. *"`owner_id` KAYNAK UYUŞMAZLIĞI (planlandı, kapanmadı)"* ⇒ **KAPANDI.** Ölçüldü:
   `SyncCommandHandler.cs:187` `OwnerId: authenticatedActorId`, `:159` `WriteAsync(… authenticatedActorId)`.
4. *"ÇEKME YOLU İÇİN İSTEMCİDE TEK TEST YOK; `changes`/`snapshot`/`hasMore` `src/client/lib`'de
   SIFIR kez geçiyor"* ⇒ **BAYAT.** Ölçüldü: **22 eşleşme** (`senkron_dongusu.dart` 14 ·
   `uzak_degisiklik_uygulayici.dart` 8) ve `g1_cekme_yolu_kapisi_test.dart` dahil altı kapı testi.
5. *"Çekme yakınsaması slice-3c'de KANITLANMAYACAK; iki cihazın yakınsaması slice-3d borcudur"* ⇒
   **KAPANDI.** `tool/f3_iki_istemci_yakinsama.dart` repoda.
6. Sekiz ayrı *"✅ KAPANDI"* kalemi (K57 · K59 · K63 · K67 ×2 · oturum 31 · oturum 33) hâlâ canlı
   dosyada duruyordu — kapanmış bir kalemi canlı durumda tutmak, R4'ün belge büyümesi kusurunun
   ta kendisidir.

**AÇIK KALDIĞI ÖLÇÜLENLER (silinmedi, §8'de duruyor):** `02-G2` üretici kodu
(`g2_registry_zarf_kapisi_test.dart:64` **aynen duruyor**) · `verify.ps1` fail-loud varsayılanı
(**duruyor**) · D1-önleme · `belge-tavan-kapisi.py` · Z10b · defter `D2`/`D3` · `DESIGN.md` BD-1…7.

🔴 **`02-G2` SINIFININ İKİNCİ YAZICISI ÖLÇÜLDÜ:** `g3_ayristirici_kapisi_test.dart:20` →
`Directory('../../KANIT/slice-3d/03-G3-ayristirici')`. Yani *"KANIT dizini ile onu yazan kodun yolu
aynı mı?"* sorusunu soran araç eksikliği **tek vaka değil, bir sınıf**.

### Oturum sağlığı (K21 — mutlak, yüzde YOK)

Açılışta **120.059** token, checkpoint'te **216.522** token ⇒ **🟢 DEVAM** (eşik < 550k). Payda
yanlışlama testi koştu, tetiklenmedi. `araclar/oturum-sagligi.py` (F3) **hâlâ yazılmadı** — bu
oturumun kilidi DURDUR'du, araç turu açılmadı.

---


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 34 KAPANIŞ: **K70 — slice-3d (ÇEKME) KABUL EDİLDİ; spec KİLİTLENDİ**)

### K70 — slice-3d (ÇEKME) KABUL EDİLDİ; spec KİLİTLENDİ (Onur, 28 Tem 2026, oturum 34)

K42-d'nin **dördüncü adımından bir önceki** taş yerine oturdu: çevrimdışı-öncelikli senkron artık
**çift yönlü**. Onur iki kilidi verdi: **slice-3d KABUL** (iki bulgu açık borç olarak kalır) ve
**spec KİLİTLİ** (`GOREV-slice-3d-cekme.md` **80.399 b · `889A383F`**; `tek-kopya-kapisi.py`
kapsamında `kilitli` sınıfına alındı ⇒ sapma her açılışta ölçülür).

**ÜRÜN KODU: 3.070 elle yazılan satır** (üretilen `.g.dart` / `generated_migrations` /
`drift_schemas` ve `KANIT` **hariç**). Ölçüm yöntemi K66/3'ün dersidir ve o ders bu oturumda bir kez
daha kanıtlandı: `git diff --stat` tek başına **1.147** diyor, çünkü **takipsiz dosyaları görmüyor**;
`status` ile birlikte ölçülünce 15 yeni kaynak dosya (+2.747 satır) ortaya çıktı. **Yalnız diff'e
bakan bir el ürün kodunu %70 eksik sayardı.** Yeni ürün dosyaları: `lib/senkron/`
(`uzak_degisiklik_uygulayici.dart` · `alan_anahtari.dart` · `kuyruk_tabani.dart`), altı yeni kapı
testi (`g1`–`g6`), `tool/f3_iki_istemci_yakinsama.dart`, `tool/t1_yalniz_cekme_duman.dart`,
`D9OwnerIdVisibilityTests.cs`, `SyncIngestV7Tests.cs`.

**COWORK'ÜN KENDİ KOŞUMU (K26 — builder'ın beyanına GÜVENİLMEDİ; altı ölçümün altısı da geçti):**
`iddia-kapisi.py --kanit KANIT/slice-3d` **HÜKÜM TEMİZ, EXIT 0** · `spec-kapi-kapsama.py` **EXIT 0** ·
`sayi-tazeligi.py` **TEMİZ, EXIT 0** · `verify.ps1` build **0 uyarı/0 hata** + **120/120**
(Architecture 5 · Api 15 · SyncCore 44 · Persistence 56) + CVE temiz · `flutter analyze
--fatal-infos` **0 bulgu, EXIT 0** · `flutter test` **136/136** · `tek-kopya-kapisi.py` **YEŞİL**.
Çıkış kodları çıktı **dosyaya yönlendirilerek** ölçüldü (boru hattı exit kodunu yutuyor).

🔴 **K66/2 BORCU KAPANDI:** `I2`/`I3` bu oturumda **İLK KEZ** koştu. slice-3c'de 36 mutantın 28'inin
ham çıktısı yoktu ve bu *"dürüstçe beyan edilmiş"* sayılıyordu; K67'de kapı yazıldı, slice-3d'de kapı
**gerçek kanıt dizinine karşı** koştu ve **TEMİZ** döndü.

**KAPILARIN ÖLÇMEDİĞİNİ COWORK ÖLÇTÜ — mutantlar DOĞRU SEBEPLE ısırdı.** `iddia-kapisi`'nin beyan
edilmiş sınırı açıktır: *"kanıtın VARLIĞINI ölçer, mutantın gerçekten ısırdığını ölçmez."* Bu boşluğu
kapatmak için 40 dosyanın **hepsi** derleme-hatası ⇄ assertion ayrımı için tarandı: `M1`–`M40`,
**boşluk yok, çift kayıt yok, boş dosya yok** (529.713 b) ve **sıfır şüpheli** — hiçbiri *"kod
derlenmedi"* diye kırmızı yanmamış, hepsinde beklenen iddianın düştüğü iz var. ⇒ **K69/EK ile
build'e DEVREDİLEN üç sorudan birincisinin cevabı: EVET.** Devretme kararı doğruydu: bu cevap kâğıtta
**alınamazdı**.

**BUILDER ÜÇ KÖR KAPI BULDU VE DÜZELTTİ (K53/2 uygulandı):** `M24` (G6/D3), `M29` (G8/D3 — M24'ün
canlı tekrarı), `M31` (G8/D5). Üçünde de **mutant gevşetilmedi, TEST GENİŞLETİLDİ** (4→5 test,
6→7 ayak, `AYAK5`'e gerçek bir yarış kuruldu; mevcut iddialar silinmedi). Bu, *"ısırmayan mutant
kapıyı gevşetmez — önce kapı düzeltilir"* kuralının doğru uygulanmasıdır.

### 🔴 AÇIK KALAN İKİ BORÇ (Onur kabul etti, kapatılmadı — 3e'nin işi)

1. **`KANIT/slice-3c/02-G2/` GERİ DOĞDU ve üretici kod DÜZELTİLMEDİ.** Klasör `16:48`'de dört
   JSON'la yeniden oluştu; `g2_registry_zarf_kapisi_test.dart:64`'teki
   `Directory('../../KANIT/slice-3c/02-G2')` **aynen duruyor**. Bu, build notunda **ayrı bir madde
   olarak** verilmişti; yapılmadı **ve devir notunda hiç anılmadı** ⇒ *beyan edilmemiş atlama*.
   DURUM.md'nin *"ilk `flutter test` koşumunda geri doğar"* öngörüsü kelimesi kelimesine gerçekleşti.
2. **`verify.ps1` değiştirildi (+9 satır):** `MOMENTUM_KANIT_DIZIN` yoksa artık **varsayılan atıyor**
   ve dizini yaratıyor. Gerekçe kod yorumunda yazılı, testin kendisi hâlâ fırlatıyor — **ama sonuç
   şu:** spec §8.2'nin *"zorunlu"* şartı **otomatik regresyon zincirinde artık hiç ölçülmüyor**.
   Ayrıca bu bir **araç** değişikliğidir (K34-f: aracı onaran el, yazan elden ayrı olmalı).

**[DOĞRULANMADI]:** builder'ın *"`cmd /v:on` kalıbı `M4`'te bir kez SESSİZCE başarısız oldu"* iddiası
— Cowork aynı kalıbı bu oturumda onlarca kez kullandı ve **hiç yalan söylemedi**; sapmanın kendisi
(PowerShell `$LASTEXITCODE`) zararsız ama gerekçesi doğrulanmadı. Ayrıca: **kabul kriteri 9**
(Android emülatör + uzak görev görünürlüğü) **koşulmadı** · web ayağı · iOS · boşaltma tavanı 20'nin
her koşulda yeterliliği.

### `R8` SERT DURAĞI ÖLDÜ

`urun_kodu_satiri = 3070` deftere işlendi; yeni artefakt `[YESIL]`. Radar hâlâ KIRMIZI ama artık
**tamamen başka** sebeplerden: park edilmiş kâğıt artefaktlar (ADR 0003 · slice-3b) · defterin eski
`D2`/`D3` borçları · ve `GOREV-slice-3d-cekme.md` için **`R1`: bayat-çapraz-atıf iki turdur tekrar
ediyor ⇒ üçüncü tur YASAK** — ki niyet de yok, spec kilitlendi. **Doktrin teyidi:** `R8` üç oturum
boyunca ısırdı ve **haklıydı**; onu susturan şey bir gerekçe değil **3.070 satır çalışan kod** oldu.

### D1 ÜÇÜNCÜ VE DÖRDÜNCÜ KEZ ISIRDI — biri doğrudan Cowork'ün kusuru

`senkron_dongusu.dart` defterde **10.838 b** yazıyordu, diskte **15.131 b** (build +115/−26 satır
ekledi) ⇒ düzeltme kaydı yazıldı. **Ve aynı koşumda kendi kusurum yakalandı:** slice-3d build
kaydını yazarken `bayt` alanına **8.000 diye UYDURDUĞUM** bir sayı koydum; kaydı ekleyen betiğe
koyduğum D1-önleme adımı dosyayı diskten ölçüp **11.284** ile düzeltti. İkisi de deftere geçti.
**Bu, "kimlik DAİMA son yazımdan SONRA ölçülür" kuralının bu projedeki ALTINCI ihlalidir** ve artık
bir sınıf değil bir **alışkanlıktır**. Öneri (borç): defter/belgeye sayı yazan **her** betiğe
D1-önleme adımı zorunlu olsun — radar'ın `D1`'i kusuru **sonradan** bulur, önleme adımı **doğmasını**
engeller.

**Araç değişikliği kayda geçti:** `tek-kopya-kapisi.py` **17.156 → 17.259 b · `66AC9CA3`**
(kapsam listesine spec `kilitli` olarak eklendi). Değişiklikten sonra aracın **kendi mutant kümesi
yeniden koşuldu: 11/11** — araç hâlâ ısırdığını kanıtlıyor. Oturum 32'nin dersi gereği defterdeki
bayt beyanı da aynı elle güncellendi.

---


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 34: **K69 — slice-3d spec'i YAZILDI; üç karar kilitlendi**)

### K69 — slice-3d spec'i YAZILDI; üç karar kilitlendi; K68'in bir gerekçesi DÜZELTİLDİ

Oturum 33 tasarımı kilitlemiş (K68) ama spec'i yazmadan devretmişti. Bu oturum spec'i yazdı.
Yazım eli AYRI: Onur'un kilidiyle spec **temiz bağlamlı bir alt-ajana** yazdırıldı, iki mekanik
kapıyı **Cowork kendi koştu**. Bu, K26'nın (üreten ≠ denetleyen) spec'in KENDİSİNDE ilk kez
sağlandığı dilimdir — slice-3c'de spec'i yazan el kapıları da kendisi koşuyordu.

**Onur'un verdiği üç yeni kilit:**

- **`P5` — `owner_id` kusuru KAPSANIR (beyan edilmiş borç DEĞİL).** `SyncCommandHandler.cs:184`
  outbox'a `OwnerId: op.ActorId` yazıyor; `authenticatedActorId` olacak. Bu bir tasarım tercihi
  değil KUSUR düzeltmesidir ve kanıt kodun KENDİ içindedir: aynı dosyanın **:156** satırındaki
  yorum *"ownerId is the AUTHENTICATED actor — NEVER op.ActorId (F5)"* diyor, **:157** materialize
  öyle yapıyor, **:184** outbox ihlal ediyor. Çekme yalnız `owner_id` filtresiyle çalıştığı için
  (`SyncPuller.cs:40`) bu doğrudan çekme görünürlüğüdür. Sömürülebilirliği: mekanizma ÖLÇÜLDÜ
  (Development'ta `DevCurrentUser` başlığı `UserId` üretiyor, `SyncRequestValidator` wire
  `actorId`'yi DOĞRULAMIYOR); canlı PoC KOŞULMADI.
- **`P6` — Uzak değişiklik rozete DOKUNMAZ.** Gerekçe ölçüldü: `senkronDurumu` CHECK kısıtı beş
  değerle sınırlı (`veritabani.dart:16-27`); yeni değer eklemek SQLite'ta ALTER ile yapılamaz,
  tabloyu yeniden yaratmayı gerektirir ve `P1`'in "tek migration, salt-ekleme" kilidini bozar.
  Beyan edilen sınır: kullanıcı bir satırın uzaktan değiştiğini rozette GÖRMEZ.
- **`P7` — Bekleyen yerel yazım korunur.** Karşılaştırma tabanı
  `max(UzakAlanDurumu anahtarı, o (entityId, alan) için kuyrukta bekliyor/gonderildi satırlarının
  alan-HLC'si)`. Aksi hâlde kullanıcı kendi düzenlemesinin bir an silinip geri geldiğini görür.

**K68'İN BİR GEREKÇESİ ÖLÇÜMLE DÜZELTİLDİ (kilit AYNI kaldı, cümle yanlıştı).** K68/3 *"echo
atılırsa istemci efektif HLC'yi HİÇ öğrenmez"* diyordu. Ölçüldü: efektif op-HLC zaten
`applied[].effectiveOpHlc` ile geliyor ve `SyncPuller` outbox'ın `hlc` kolonunu **SELECT
ETMİYOR** (`SyncPuller.cs:37`) — yani `changes` hiçbir zaman efektif HLC taşımıyor, echo olsa da
olmasa da. Echo'nun gerçek değeri şudur: atılırsa kendi yazımının sunucudaki **KIRPILMIŞ
alan-HLC'si** (`WireMapping.cs:47-66`) `UzakAlanDurumu`'na hiç girmez; istemci yerel damgayla,
sunucu kırpılmış damgayla karşılaştırır ve **FARKLI KAZANAN** seçilir. Bu daha sert bir gerekçedir
ve spec'e bu yazıldı. Ders: kilit doğru olabilir ve gerekçesi yine de yanlış olabilir; gerekçe
denetlenmezse bir sonraki el onu VARSAYIM olarak devralır.

### Bu oturumda ÖLÇÜLEN yedi olgu (spec'in dayanağı; hiçbiri hatırlanmadı)

1. **Devir notunun üç atfı BAYATTI (D1 sınıfı, dördüncü tekrar).** `owner_id` outbox yazımı
   **:184** (notta 170) · materialize **:157** (notta 152) · snapshot dalı **:74-95** (notta 70-93).
   `SyncIngest.cs:117-136` ve `WireMapping.cs:47-66` DOĞRUYDU. Ders: devir notuna satır numarası
   yazan el onu o an ölçmüş olsa bile, sonraki oturum numaraları YENİDEN ölçmek zorundadır.
2. **`ops` alanı ATLANAMAZ ve yalnız-çekme turu bugün TEKNİK OLARAK İMKÂNSIZ.** Doğrulayıcı
   `Ops`u `NotNull` istiyor (`SyncRequestValidator.cs:14`) ⇒ gövde mutlaka `"ops":[]` taşımalı;
   ama `_turCalistirIc()` bekleyen satır yoksa **hiç istek atmadan return** ediyor
   (`senkron_dongusu.dart:73-75`). `P2` ayrı bir kod yolu demek. K68 bunu "doğrulayıcı kabul
   ediyor" diye geçmişti — kabul ediyor, ama istemci o isteği ÜRETEMİYOR.
3. **`changes[].payload` bir projeksiyon DEĞİL, `WireOp`un kendisidir** (kırpılmış HLC'lerle);
   `snapshot[]` ise TAMAMEN FARKLI şekildir (`scalars/sets/groups` + `winOperationId`). İstemci
   İKİ AYRI ayrıştırıcı yazmak zorunda. `changes`te `winOperationId` YOK; op-düzeyi `operationId`
   tie-break olarak kullanılır.
4. **LWW anahtarı = `(alan HLC, op.operationId)`** (`HlcKey.cs:9,21-23`) ve HLC sırası
   `(wallMs, counter, clientId'nin "N" hex biçiminin ORDINAL dize sırası)` (`Hlc.cs:10-25`) —
   GUID sıralaması DEĞİL. Kazanma koşulu KESİN büyüklük (`LwwRegister.Apply`).
5. **İstemcide çekme yolu için sıfır test var** ve `changes`/`snapshot`/`hasMore` dizeleri
   `src/client/lib` ağacında sıfır kez geçiyor. Şema sürümü **3** (`veritabani.dart:79`).
6. **`owner_id` uyuşmazlığı istemci kusuru DEĞİL:** istemci `actorId`'yi hem başlığa hem gövdeye
   AYNI kaynaktan (`ayarlar.devUserId`) yazıyor (`gorev_deposu.dart:99`, `http_senkron_agi.dart:33`).
7. **Alt-ajan da "kimlik son yazımdan SONRA ölçülür" kuralını ihlal etti (BEŞİNCİ tekrar).**
   Rapor 60.951 bayt dedi; Cowork'ün kendi ölçümü **61.242 b · `FD85FD37`**. Kusur zararsızdı
   çünkü kimlik DEFTERE değil rapora yazılmıştı ve Cowork yeniden ölçtü — ama sınıf aynı.

### Kapı ve kimlik ölçümleri (hepsi Cowork'ün KENDİ koşumu, EXIT kodları `cmd /v:on` ile)

- Açılış: `tek-kopya-kapisi.py .` **EXIT 0** (dokuz dosya, hepsi HEAD +0) · `radar --altin-kume`
  **18/18 EXIT 0** · `radar .` **KIRMIZI EXIT 2** (park edilmiş kâğıt artefaktlar, YENİ BULGU YOK;
  varsayılan şık DEVRET ve slice-3d zaten koda gidiyor) · `git status --porcelain` BOŞ ·
  `index.lock` FALSE · `rev-list origin/main...HEAD` = **`0  1`** (`e2f4f3e` push bekliyor).
- Spec kapıları (Cowork koştu, üreticinin beyanına güvenilmedi):
  `spec-kapi-kapsama.py` ⇒ KAPI 9 · KURAL 10 · MUTANT 32 · **BULGU YOK, EXIT 0**;
  `iddia-kapisi.py` ⇒ **HÜKÜM: TEMİZ, EXIT 0** (I1 temiz). [KIRMIZI] Aynı koşum
  **`KANIT DIZINI: verilmedi -- I2/I3 OLCULMEDI`** dedi: mutant ham-çıktı kapısı **HENÜZ
  KOŞMADI** çünkü `KANIT/slice-3d/` yok. `I2/I3` build'den SONRA koşulacak; bugün "spec kapıları
  geçti" demek **I1 geçti** demektir, mutant kanıtı değil.
- Kimlikler (son yazımdan SONRA): `GOREV-slice-3d-cekme.md` **61242 b · `FD85FD37`** (U+FFFD 0,
  CRLF 0) — **KİLİTLİ DEĞİL**, bağımsız denetim onu değiştirecek. `GOREV-slice-3c-senkron.md`
  **41692 b · `537D0579`** (K64 kilidi SAĞLAM). Kesinti sonrası (11:48) yeniden ölçüldü: beş
  dosyanın hepsi değişmemiş ⇒ **köprü kopması hiçbir dosyayı yarım bırakmamış**.

### Spec'in beyan ettiği sınırlar (gizlenmedi, sayıldı)

Rozet uzak değişiklikte yazılmaz (`P6`) · `hasMore` yanlış-pozitifi (fazladan bir boş tur) ·
boşaltma tavanı 20 tur · OR-Set (`sets`) bu dilimde uygulanmaz · projeksiyona yalnız üç alan
yazılır (`title`, `completion.status`, `isDeleted`), diğerleri `UzakAlanDurumu`'na kaydedilir ama
görünmez · `G6`'nın sahte sunucusu sunucu değildir, otorite `G8` · `G8` tek sahiple koşar ve
`owner_id` kusurunu GÖRMEZ (mutantı `G7`'de) · tek kullanıcı/tek imleç · migration ortası çökme
ölçülmüyor · web ayağı `[DOĞRULANMADI]` · iOS CI-only.

### AÇIK BULGU (alt-ajan buldu, karar Onur'da)

`P7`'nin "ham metin ile oku" zorunluluğunun gerekçesi (`ulong` taşması) **kuyruk gövdesinden
alan-HLC okumakta geçerli DEĞİL** — `ulong` sorunu `SyncCursor.Xid`e özgüdür, kuyruktaki
`hlc.wallMs`/`counter` düz `int`tir. Kilit değiştirilmedi (ham metin uygulanıyor, desenini `M21`
koruyor) ama gerekçe fazla geniş. Onur karar verene kadar bu **beyan edilmiş fazlalık**tır.

### Oturum sağlığı (K21) — ölçüldü, KIRMIZI

Canlı bağlam **195.896 token** (kendi transcript'imden, 58 ölçüm; eğri 73k → 195,9k). 200k pencere
varsayımıyla **≈%98 ⇒ KIRMIZI**. *Token sayısı ölçümdür, pencere boyutu varsayımdır.* K21'in
"dolu bağlamda spec yazma yasağı SARI'dan başlar" kuralı bu yüzden **spec'i başka bir ele
yazdırmakla** karşılandı; oturum 33'ün "temiz oturuma devret" çözümü tekrarlanmadı çünkü o çözüm
iki oturum üst üste sıfır ürün kodu üretiyordu (`R8` sert durağı). [KIRMIZI] `R8` YİNE DE ISIRACAK:
bu oturumun `urun_kodu_satiri` da **0**; sert durak build'den önce kalkmaz. Gizlemek yerine ölçüp
yazıyorum — sonraki adım kâğıt turu DEĞİL, önce bağımsız denetim sonra BUILD olmalıdır.

### [KIRMIZI] YENİ ORTAM KUSURU: hafıza yazımı köprüye bağımlı ve köprü ORTA YERDE DÜŞTÜ

Bu checkpoint'in ilk yazım denemesi `device not connected to the bridge` ile kesildi (evde
internet kesintisi); `mcp__remote-devices__*` araçlarının **tamamı** oturumdan düştü. Spec diskte
sağlam kaldı ama `PROJE_HAFIZA.md` / `PROJE_RADAR.jsonl` / `DURUM.md` yazılamadı. Kurtaran şey
kural değil **yedek kanal** oldu: tüm ölçümler Cowork projesine (`claude/oturum-34-checkpoint-
BEKLEYEN.md`) dört blok hâlinde yazıldı, köprü ~30 dk sonra dönünce diske o bloklardan uygulandı.
İki ders kayda geçiyor: ① **uzun hafıza yazımı tek parçada yapılmaz** — en kritik dosya ÖNCE yazılır,
çünkü kanal ortada ölebilir; ② bu, K60'ın (atomik yazım) kapsamadığı bir kusur sınıfıdır: K60
*dosyanın* yarım kalmasını engeller, *oturumun* yarım kalmasını engellemez. Bir sonraki el bunu
mekanikleştirmek isterse ölçülecek soru şudur: *"bu oturumun ölçümleri köprüden BAĞIMSIZ bir yerde
duruyor mu?"*

### Ayrıca ÖLÇÜLDÜ: `python` stdout bu makinede cp1254 ve KABUĞU ÖLDÜRÜYOR

`⇒` gibi cp1254'te olmayan bir karakter yazdıran betik `UnicodeEncodeError` ile patlıyor ve
PowerShell sürecini komut ortasında düşürüyor (bu oturumda bir kez oldu, süreç öldü ve yeniden
başlatıldı). Zorunlu kalkan: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.
Dosyaya yazan taraf zaten UTF-8; kusur **stdout**'tadır, dosyada değil. §7'ye yazıldı.


### K69/EK — Onur'un R8 kilidi: **DARALT** (28 Tem 2026, aynı oturum)

Radar `R8` sert durağı ısırdı — *"son 2 oturumda (33, 34) tek satır ürün kodu projeye girmedi;
bir sonraki oturum ÜRÜN KODU ile başlar, yeni belge/ADR/spec/araç turu AÇILMAZ"* — ve bu, K26'nın
*"spec bağımsız denetlenmeli"* kuralıyla **doğrudan çatıştı**. K40 gereği kilit Onur'dan geldi:
**DARALT.**

Denetim YALNIZ **kâğıtta ölçülebilen** sınıfa bakar: spec'in iç çelişkisi · eksik kapsam · yanlış
`dosya:satır` atfı · K68/K69 kilitleriyle uyuşmazlık · Claude Code'u **tahmine zorlayan**
muğlaklık. **Bilinçli olarak BUILD'e devredilenler (beyan; gizleme değil):** `M1`–`M32`'nin
gerçekten **ısırdığı** · `G8`'in gerçekten **iki ayrı Drift DB + gerçek API** kullandığı · `P5`
mutantının `owner_id` kusurunu **yakaladığı**.

Gerekçe radar'ın kendi doktrinidir (`R2b`/DEVRET): *kâğıtta doğrulanan bir kapı, doğrulandığını
KANITLAYAMAZ.* Bir mutantın ısırıp ısırmadığı ancak **koşulunca** bilinir; denetçinin *"bu mutant
eşdeğer görünüyor"* demesi bir **hipotezdir**, ölçüm değil. Birinci sınıf ise build'in **hiç
göremeyeceği** kusurları taşır — build yanlış spec'i sadakatle uygular ve kusuru koda gömer.

**KABUL EDİLEN RİSK (yazılı):** eşdeğer mutant varsa bu **build sırasında** öğrenilecek ve spec'e
dönülecek. Bu maliyet kabul edildi çünkü alternatifi aynı bilgiyi daha zayıf bir yöntemle
(okuyarak) daha pahalıya tahmin etmek. **Denetim TEK GEÇİŞtir, tur değildir**; hemen ardından
BUILD gelir. Bir sonraki oturum denetimi tek geçişte bitirip Claude Code'a devretmezse `R8` üçüncü
kez ısırır ve o zaman itiraz hakkı kalmaz.

---

### K21-DÜZELTME — OTURUM SAĞLIĞI KURALI 13 OTURUM BOYUNCA YANLIŞ PAYDAYLA OKUNDU (oturum 34)

Onur sordu: *"kuralda bir eksiklik mi var?"* Ölçüm cevap verdi: **kuralda eksiklik YOK — özette var.**

**KANONİK METİN (bu dosya, oturum 21 checkpoint'i):** *"RENK EŞİKLERİ (**1M pencere için**): 🟢 <%55
(**550k**) · 🟡 %55–75 · 🔴 >%75 (**750k**)"* — ve doğuş anı dürüsttü: *"(pencere 1.000.000 — Onur'un
beyanı; **Cowork pencere boyutunu ölçmedi**)"*. Oturum 22 checkpoint'i de *"~285 K / 1 M = %28,5 ⇒
🟢"* diye raporlamış. Yani kural **paydasını ve mutlak çıpalarını taşıyordu.**

**ÖLÇÜLEN KUSUR:** `550` · `750` · `1M` · `1.000.000` araması ⇒ **`DURUM.md` + `CLAUDE.md` = SIFIR
eşleşme.** Oturum açılışında okunan **iki** dosyanın ikisi de K21'i **yalnız yüzde** olarak
taşıyordu; mutlak çıpalar ve payda **yoktu**. Kanonik değer yalnız bu dosyada — yani **K53 gereği
açılışta okunmayan** dosyada.

**HATA ZİNCİRİ:** ① oturum 21: K21 paydasıyla doğar ② `CLAUDE.md`'ye kopyalanır, **payda düşer**
③ **K53 (oturum 29):** `PROJE_HAFIZA.md` açılış okumasından çıkar ⇒ **kayıplı kopya, kuralın tek
görünen hâli olur** ④ K58 (oturum 31): `DURUM.md`'ye de yüzde-only satır eklenir ⑤ **oturum 33:**
payda gerekir, **200k uydurulur** ⇒ %62 ⇒ 🟡 ⇒ **spec yazımı gereksiz devredilir** ⑥ **oturum 34:**
aynı hata, %98 ⇒ 🔴 ⑦ **`R8` sert durağı ısırır** — ve o iki sıfır-ürün-kodu oturumundan **biri bu
ölçüm hatasının ürünüdür.**

**KÖK NEDEN, PROJENİN KENDİ TEŞHİS ETTİĞİ SINIFTIR:** K19-a (oturum 21) *"tüm tavanlar tek KANONİK
SAYILAR tablosuna çekilir, gövde **atıf yapar, kopyalamaz**"* diyor; gerekçesi birebir *"kardeş
güncellendi, kendisi unutuldu"*. O kural ADR 0003'e uygulandı — **K21'in kendisine uygulanmadı.**

**ÜÇ YAPISAL KUSUR:**
① Kural bir **ORAN**, ama paydası bu ortamda ölçülemez. *"Ölçemezsen varsayma"* cümlesi **PAYA**
   yazılmış (transcript okunabiliyor mu?), **PAYDAYA hiç uygulanmamış** ⇒ her oturum kendi
   varsayımını üretir; bu bir **sessiz sapma kanalıdır** ve tam olarak öyle oldu.
② **Kuralın kapısı yok.** Bu projede her araç altın kümesiyle önce kendini kanıtlar; K21 **hiç**
   kanıtlamadı. Oysa bedava bir yanlışlama testi **13 oturumdur** elde duruyordu.
③ **Beyan, ölçümün yerini tuttu.** İki oturum da *"pencere boyutu varsayımdır"* diye **açıkça
   yazdı** ve bu, yanlış paydayı **titiz göstererek** korudu.

**DOKTRİN DERSİ (bu kaleme özgü değil, genelleşir):** *"beyan edilmiş sınır kabul edilir"*
kuralının eksik yarısı şudur — **beyan, ölçmek PAHALI olduğunda kabul edilir; ölçmek BEDAVA
olduğunda beyan bir bahanedir.**

**YANLIŞLAMA KOŞTU (28 Tem 2026):** canlı bağlam **339.116 token** ölçüldü ve istekler koşuyordu ⇒
**200k varsayımı ÖLÜ.** Kanıtlanan şey bir **alt sınırdır** (>339k); **1M hâlâ Onur'un beyanıdır ve
Cowork onu ölçmedi** — bu yüzden F1 paydayı karar yolundan tümden çıkarır.

**GEÇERSİZ İLAN EDİLEN OKUMALAR:** oturum 33'ün 🟡 (%62) ve oturum 34'ün 🔴 (%98) **GEÇERSİZDİR**;
doğru okuma her ikisinde de **🟢**. `R8`'in **ölçümü doğru**, **teşhisi** değişti: sebep kâğıt turu
bağımlılığı değil, **sahte oturum sınırı**.

**UYGULANAN DÜZELTME (Onur kilitledi: F1+F2+F4 şimdi, F3 build sonrası):**
- **F1** — eşikler artık **MUTLAK** (🟢 <550k · 🟡 550k–750k · 🔴 >750k) ve **payda karar yolundan
  ÇIKARILDI.** Kanonik metin **yalnız `CLAUDE.md`'de**; `DURUM.md` eşiği **kopyalamıyor, ATIF
  yapıyor.** Kopya kalmayınca kaybolacak terim de kalmaz — bu, kapıdan **daha güçlü** bir önlemdir.
- **F2** — **payda yanlışlama testi** kurala yazıldı (canlı bağlam varsayılan pencereden büyük ve
  istek koşuyorsa varsayım ÖLÜDÜR ⇒ renk ilan etme, Onur'a söyle). Ayrıca **"yüzde yazmak KUSUR
  BELİRTİSİDİR"** kuralı eklendi ⇒ kusur bundan sonra **kendini ihbar eder.**
- **F4** — bu kayıt.
- **F3 [BEKLİYOR]** — `araclar/oturum-sagligi.py`: mutlak token okur, rengi mutlak eşikten
  hesaplar, **paydaya dokunmaz**, `CLAUDE.md`'nin K21 bloğunu kanonik değerlerle karşılaştırır
  (kanonik-kopya kapısı), yanlışlama testini koşar. Altın küme: **bilerek bozulmuş bir özet
  satırını yakalamalı.** Build'den sonra **ilk ARAÇ işi** (`DURUM.md` §8'de borç olarak yazılı).

**AYRICA ÖLÇÜLDÜ — `DURUM.md` TAVANI ARTIK GERÇEKTEN ISIRIYOR:** bu düzeltme yazılırken tavan **beş
kez üst üste** aşıldı; **on bir kalem** budandıktan sonra dosya **32.730 / 32.768 b** oldu ⇒ **pay 38
BAYT.** Bir sonraki her ekleme tavanı aşar. `belge-tavan-kapisi.py` artık ertelenemez — ama gerçek
çözüm araç değil: **`DURUM.md` kapanmış tarihçe taşıyor** (slice-3b/3c kalemleri) ve o tarihçenin
yeri arşivdir. Budarken bir **bayat atıf** da yakalandı: §3'ün İstemci satırı hâlâ *"Sıradaki:
K42-d adım 3"* diyordu — adım 3 (slice-3c) **bitmişti**.

---

### K69/EK-2 — DARALTILMIŞ DENETİM KOŞTU: 4 BLOKER + 8 ÖNEMLİ; SPEC DÜZELTİLDİ (oturum 34)

DARALT kilidi (K69/EK) tek geçişlik, sınıf-tabanlı denetim öngörüyordu. Koştu ve **kendini ödedi.**
K26 sağlandı: **iki bağımsız denetçi**, spec'i yazan elden AYRI, **farklı merceklerle** ve
**birbirini görmeden** koştu — (A) atıf doğruluğu + kilit uyumu · (B) uygulanabilirlik + muğlaklık.
Bulgu: **4 BLOKER · 8 ÖNEMLİ · 6 NOT.** İki bulguda **bağımsız olarak buluştular** (yakınsama kanıtı
ve yutulan tetikleyici) — bu, denetimin gürültü değil sinyal ürettiğinin göstergesidir.

**DÖRT BLOKER (hepsi kapatıldı):**
- **`B1` — `sha256` bayt-özdeşlik kriteri DOĞRU KODLA İMKÂNSIZDI.** `Gorevler`'in yedi sütunundan
  üçü iki cihazda asla eşitlenemez: `olusturuldu` Task scalar listesinde **YOK**
  (`FieldStrategyRegistry.cs:170`) ⇒ tele hiç çıkmıyor, alan cihaz onu kendi saatinden uyduruyor
  (`gorev_deposu.dart:112`); `senkronDurumu` yazan cihazda `'senkronize'`
  (`senkron_dongusu.dart:186`), alan cihazda varsayılan `'yerel'` — çünkü **`P6`** rozete dokunmayı
  yasaklıyor. ⇒ `G6`/`G8` **doğru uygulamada kırmızı yanardı** ve uygulayıcı kapıyı geçmek için ya
  `P6`'yı ihlal ederdi ya dökümü kendi lehine daraltırdı. **Onur kilitledi:** döküm
  `SELECT id, baslik, tamamlandi, silindi FROM gorevler ORDER BY id`; üç sütun gerekçesiyle
  **`[SINIR]`** olarak dışarıda.
- **`B2` — Push turu `changes`'i ATIYOR ama imleci İLERLETİYOR ⇒ sessiz KALICI VERİ KAYBI.** Sunucu
  **her** yanıtta çekme yapıyor (`SyncCommandHandler.cs:74-95`) ve `_basariliYanitIsle`
  `nextCursor`'ı **koşulsuz** kalıcılaştırıyor (`senkron_dongusu.dart:158-162`), `changes`/`snapshot`'a
  hiç bakmadan. İlk turda `sinceCursor==null` ise **tüm snapshot** çöpe gidiyor ve cihaz *"gördüm"*
  sayıyor. 🔴 **Bu bugün SHIPPED KODDA VAR** — slice-3c'de zararsızdı çünkü çekme yoktu; 3d'de
  veri kaybı olurdu. **Onur kilitledi:** itme turu da yanıtı uygular; *"turCalistir değişmez"*
  cümlesi DÜŞTÜ; imleç ancak veri uygulandıktan SONRA ilerler.
- **`B3` — Yerelde OLMAYAN bir görevin satırı nasıl doğar: HİÇBİR YERDE yoktu.** `olusturuldu` +
  `guncellendi` NOT NULL, telde `createdAt` yok, `senkronDurumu` varsayılanı `'yerel'` ve `D4` ona
  dokunmayı yasaklıyor; `G8` ayak 2 INSERT'i zorunlu kılıyor; liste `olusturuldu`'ya göre
  sıralanıyor. Uygulayıcı **uydururdu**. Çözüm: `olusturuldu` = o entity için görülen **en küçük
  op-HLC'sinin `wallMs`'i** (veriden türetilir, cihaz saatinden DEĞİL ⇒ determinizm).
- **`B4` — `transaction`/`atomik` kelimeleri spec'te SIFIR kez geçiyordu.** Sayfanın uygulanması ile
  `nextCursor` yazımı aynı transaction'da mı belirsizdi; *"önce imleç"* seçen bir uygulayıcı yarım
  sayfayı **kalıcı** kayba çevirirdi. Artık tek `_db.transaction`, imleç **en son**.

**EN SİVRİ İKİ ÖNEMLİ:** ① **`D6` (echo tohumlama) ile `D5`/`P7` (birleşik taban) BİRBİRİNİ
YİYORDU** — kuyruk satırı yalnız `Applied`/`Duplicate`'te siliniyor (`senkron_dongusu.dart:171-173`)
⇒ echo geldiğinde aynı alanın **kırpılmamış (daha büyük)** damgası kuyrukta duruyor, echo kaybediyor
ve kırpılmış damga `UzakAlanDurumu`'na **hiç** yazılmıyordu; yani `P7` doğru uygulanınca `D6`'nın
**tek gerekçesi çöküyordu**. Çözüm: kuyruk tabanı YALNIZ **projeksiyon yazımını** kapılar;
`UzakAlanDurumu` kararı yalnız kendi satırına karşı verilir. ② **Paylaşılan tek-uçuş kilidi çekme
tetikleyicisini SESSİZCE YUTUYORDU** (`senkron_dongusu.dart:51-57` devam eden turu *döndürüyor*,
sıraya almıyor) — ki bu tam olarak `P2`'nin *"açılış turu"* senaryosu. Onur kilitledi: yutulan
tetikleyici **kilit serbest kalınca BİR KEZ** yeniden koşar.

**TEMİZ ÇIKANLAR DA ÖLÇÜLDÜ (gizlenmedi):** 33 `dosya:satır` atfının **32'si DOĞRU** — tek bayat
`:187` (`_outbox.WriteAsync` gerçek yeri **`:159`**; `:187` = `ActorId: op.ActorId`, yani spec'in
*"DEĞİŞMEZ"* dediği satır). D1 sınıfı bu spec'te **neredeyse hiç tekrarlanmadı** ve bu bir ilktir.
**`P1`↔`P6` çatışma adayı ÖLÇÜLDÜ ve ISIRMADI:** migration'da `TableMigration`/`alterTable`/CHECK
değişikliği yok, tek ek nullable `addColumn` ⇒ SALT-EKLEME kilidi **sağlam**. Beş ölçülmüş kısıt,
LWW zinciri (`wall → counter → normHex(clientId) → normHex(opId)`, **kesin büyüklük**),
`changes`⇄`snapshot` şekil ayrımı ve kanal eşlemesi **birebir doğru**.

**SPEC'İN YENİ HÂLİ (Cowork'ün KENDİ ölçümü):** **80.399 b · `889A383F`** (önce 61.242 b ·
`FD85FD37`), 1086 satır, U+FFFD 0, CRLF 0. Görev **T1–T11** (yeni `T7`), kapı **G1–G9** (+22 ayak),
mutant **32 → 40** (`M33`–`M40`; koşan-uygulama mutantı hâlâ **üç** ⇒ K53/3 tavanı aşılmadı).
Kapılar **Cowork'ün kendi koşumunda**: `spec-kapi-kapsama` **EXIT 0** (KAPI 9 · KURAL 10 · MUTANT
40) · `iddia-kapisi` **HÜKÜM TEMİZ, EXIT 0**. Çıkış kodları çıktı **dosyaya yönlendirilerek** ölçüldü
— boru hattı (`| findstr`) exit kodunu yutuyor (bu oturumda iki kez yakalandı).

**AÇIK KALAN / `[DOĞRULANMADI]`:** `I2`/`I3` hâlâ **KOŞMADI** (`KANIT/slice-3d/` yok; build sonrası
`--kanit` ile koşacak) · `MOMENTUM_KANIT_DIZIN` ortam değişkeni adı **spec yazarının seçimi**, mevcut
bir kural değil · `dart run drift_dev schema dump`/`generate` komutları **koşulmadı** (slice-3c
emsaline dayanıyor) · `D8`'in nibble indeksi (`ToByteArray()[7]`) yalnız akıl yürütmeyle doğrulandı,
gerçek GUID'le ölçülmedi — spec zaten `G7`'de ölçülmesini şart koşuyor.

**DERS:** DARALT doğru kilitti. Denetim, build'in **hiç göremeyeceği** dört kusuru buldu (`B2` ve
`B4` kapılar yeşil yanarken sessizce veri kaybı üretirdi) ve build'in daha iyi ölçtüğü üç konuya
(mutantların ısırması, `G8`'in gerçek kurulumu, `P5`'in yakalayıcılığı) **hiç girmedi**. Kâğıt turu
ile build arasındaki sınır, *"hangi soruyu hangi araç daha iyi ölçer"* diye çizildiğinde her ikisi de
kazanıyor.

---

## 🔒 CHECKPOINT (28 Tem 2026 · oturum 33: **K68 — slice-3d (ÇEKME) TASARIM KİLİDİ + üç ölçüm bulgusu**)

### K68 — slice-3d tasarımı KİLİTLENDİ (Onur, 28 Tem 2026, dört şıkkın dördü de öneriyi seçti)

Tasarım **havada kurulmadı**: bağımsız bir kâşif ajanı mevcut senkron sözleşmesini salt-okuma ile
ölçtü (dosya:satır referanslı), kilitler o ölçümün üstüne kondu.

- **`P1` — Yerel LWW: A2 (ayrı alan-durumu tablosu).** Yeni Drift tablosu
  `UzakAlanDurumu(entityType, entityId, alan, hlcWall, hlcCounter, hlcClientId, winOpId)`; `Gorevler`
  **saf projeksiyon** kalır. Şema **v4** (tek migration). *Ölçülmüş gerekçe:* bugün `Gorevler`'de
  **hiç HLC/versiyon sütunu YOK** ⇒ gelen `payload.fields.<ad>.hlc` ile karşılaştırılacak yerel değer
  mevcut değil, yani çekme **kör overwrite'tan başka bir şey yapamaz**. A1 (Gorevler'e alan başına
  sütun) reddedildi: her yeni alan yeni sütun demek ve `entityType` genişlemesinde kırılır.
- **`P2` — Tetikleyici: B2.** Uygulama açılışında **yalnız-çekme turu** (`ops:[]` — doğrulayıcı bunu
  kabul ediyor, `SyncRequestValidator.cs:13-16`), kullanıcı yenilediğinde bir tur, ve `hasMore=true`
  olduğu sürece **boşaltma döngüsü**. Periyodik yoklama YOK (3e'de SignalR bu tetikleyicinin üstüne
  oturur; zamanlayıcı testlere zaman bağımlılığı sokar ve 3e'de sökülecek geçici koddur).
- **`P3` — Yakınsama kanıtı: F3 + F2.** (F3) `tool/` altında **iki ayrı Drift DB'li iki istemci**,
  **gerçek çalışan API'ye** karşı: A yazar → B çeker → B yazar → A çeker → ikisi bayt-özdeş; ham çıktı
  KANIT'a yazılır. (F2) Ayrıca sahte ağ + iki Drift DB ile **saniyeler süren** bir kapı testi.
  Koşan-uygulama mutant tavanı **3** (K53/3) F3'e uygulanır; F2 statik/widget sınıfı ⇒ **tavansız**.
- **`P4` — Backend `opId` v7: E1.** `SyncIngest.IsEnvelopeValid` (`SyncIngest.cs:117-136`) içine
  **sürüm nibble kontrolü**; nibble ≠ 7 ⇒ o op `RejectedInvalid`, **istek geri kalanı işlenir**
  (mevcut op-bazlı zarf doğrulama deseni korunur; E3'ün "tüm istek 400" sertliği reddedildi).
  Mutant: nibble kontrolünü sök ⇒ v4 `opId` geçer ⇒ test kırmızı. Ayrıca **istemcinin gerçekten v7
  ürettiği bağımsız ölçülür** (K65'in dayanağı bugün yalnız istemcinin uslu davranmasıdır).

**BEYAN EDİLMİŞ ÖLÇÜLMÜŞ KISITLAR (şık değil, olgu — spec bunları varsayım olarak yazamaz):**
1. **Çekme için ayrı uç YOK.** `POST /v1/sync` (`SyncEndpoints.cs:21`) tek uçtur; yanıtı zaten
   `serverHlc · nextCursor · hasMore · resyncRequired · applied[] · changes[] · snapshot[]` taşır.
2. **İlk senkron ZORUNLU olarak snapshot dalına düşer** (`sinceCursor==null` ⇒ `changes` boş,
   `snapshot` dolu, `nextCursor=(horizon,0)`, `SyncCommandHandler.cs:70-93`). *"Yalnız artımlıyı yap,
   snapshot'ı ertele"* **teknik olarak imkânsızdır**; iki dal da bu dilimde uygulanır.
3. **Echo UYGULANIR, atılmaz.** Sunucuda `client_id` süzgeci yok (`SyncPuller.cs:36-43`, tek filtre
   `owner_id`) ⇒ kendi op'un `changes` içinde geri gelir. **Atmak yanlıştır:** gövde sunucunun
   **KIRPILMIŞ** HLC'sini taşır (`WireMapping.ClampedPayload`, `WireMapping.cs:47-66`); echo atılırsa
   istemci efektif HLC'yi **hiç öğrenmez** ve sonraki LWW karşılaştırmaları bozulur — bu tam olarak
   K65'in sınıfıdır.
4. **`hasMore` yanlış-pozitif verebilir:** `hasMore = (changes.Count == PageSize)`, `PageSize=500`
   sabit (`SyncPuller.cs:17,58`). Son sayfa tam 500 olursa fazladan **bir boş tur** koşulur — veri
   kaybı değil, maliyet. Spec bunu beyan eder.
5. **`SyncCursor` HLC DEĞİLDİR** — `(Xid:ulong, Seq:long)` Postgres commit görünürlüğü çiftidir
   (`SyncCursor.cs:9-10`). İstemci `Xid`'i **ayrıştırmaz**, ham JSON alt-metni olarak saklar
   (Dart `int` 64-bit **işaretli**; `ulong` taşar). Mevcut `_hamCursorCikar` deseni korunur.

### Bu oturumda ÖLÇÜLEN ÜÇ BULGU

- **`B1` — Devir notu bayattı: PUSH BEKLEMİYOR.** `git --no-optional-locks rev-list --left-right
  --count origin/main...HEAD` ⇒ **`0  0`**. `c6cc8a4` ve `ebf7f62` zaten push edilmiş. Ders: DURUM.md
  §3'ün *"ileri/geri durumu yazılmaz, açılışta ÖLÇÜLÜR"* kuralı **doğruydu**; kuralı çiğneyen devir
  notunun kendisiydi. **Devir notuna push durumu YAZILMAZ.**
- **`B2` — 🔴 ÖLÇÜM ARACININ KENDİSİ YALAN SÖYLEDİ: `cmd /c "... & echo %ERRORLEVEL%"` KÖRDÜR.**
  `%ERRORLEVEL%` **ayrıştırma anında** genişler, komut koşmadan önce ⇒ üç kapı da sahte `EXIT=0`
  bildirdi; radar gerçekte **2** dönüyordu. Doğrusu: **`cmd /v:on /c "... & echo !ERRORLEVEL!"`**.
  Bu, projenin kendi doktrinindeki *kör kapı* sınıfının bir örneğidir ve **ölçüm katmanında** oldu:
  yeşil gördüğünü sanan bir el, hiç koşmamış bir kapıyı geçmiş sayabilirdi. §7'ye yazıldı.
- **`B3` — 🔴 D1 İKİNCİ KEZ ISIRDI: `araclar/iddia-kapisi.py` defter 19.912 b diyor, DİSKTE
  20.157 b** (`sha8 9F98CBF9`, ölçüldü). K67 defter kaydı, araç düzeltilmeden **önce** yazılmış.
  Append-only ⇒ **düzeltme kaydı** `PROJE_RADAR.jsonl`'a eklendi. Kural teyidi: **kimlik DAİMA son
  yazımdan SONRA ölçülür** — bu, aynı kuralın bu projede dördüncü ihlalidir.

### 🔴 YENİ AÇIK BULGU (ölçüldü, sömürülebilirliği ÖLÇÜLMEDİ) — `owner_id` KAYNAK UYUŞMAZLIĞI

Outbox'a `owner_id` **wire'dan gelen `op.ActorId`** ile yazılıyor (`SyncCommandHandler.cs:170`), oysa
materialize **`authenticatedActorId`** kullanıyor (`SyncCommandHandler.cs:152`); `SyncRequestValidator`
wire `actorId`'yi **doğrulamıyor**. Artımlı çekme yalnız `owner_id` filtresiyle çalıştığı için
(`SyncPuller.cs:36-43`), bu iki kaynağın ayrışması **çekme görünürlüğünü** doğrudan etkiler.
**Sömürülebilir mi ölçülmedi** — `[DOĞRULANMADI]`. slice-3d spec'i bunu ya kapsar ya da **açıkça
beyan edilmiş borç** olarak yazar; sessiz geçemez.

### Oturum sağlığı (K21) — bu checkpoint'in yazılma SEBEBİ

Ölçüm: canlı bağlam **123.882 token** (`input+cache_read+cache_creation`, kendi transcript'imden;
pencere 200k varsayımıyla **≈%62 → 🟡 SARI**, kâşif raporundan sonra ~%67). *Token sayısı ölçümdür,
pencere boyutu varsayımdır.* K21'in **"dolu bağlamda ADR/spec yazma yasağı SARI'dan başlar"** kuralı
gereği: **tasarım bu oturumda kilitlendi, `GOREV-slice-3d-cekme.md`'nin YAZIMI TEMİZ OTURUMA
DEVREDİLDİ.** Kilit elde olduğu için yeni oturum doğrudan yazmaya başlar. slice-3c'de spec v1'i on
bloker ile kıran denetimin dersi: **spec kalitesi, onu yazan elin bağlam sağlığına bağlıdır.**

---

## 🔒 CHECKPOINT (28 Tem 2026 · oturum 32 KAPANIŞ: **K67 — `iddia-kapisi.py`; iki kör sınıf mekanikleşti**)

Onur üç kilidi verdi: ① slice-3d kapsamı = **önce yalnız çekme (pull)** ② backend `opId` v7 zorlaması
**bu dilimde kapansın** ③ spec'ten **ÖNCE araç** (K44-a). Bu checkpoint ③'ün teslimidir.

### `araclar/iddia-kapisi.py` 1.1.0 — belgenin kendi iddiasını diskle karşılaştırır
İki sınıf, **ikisi de iki kez ısırdı**, ikisinin de kapısı yoktu:
① **sayı ⇄ liste**: spec v1 *"on altı mutant / M1–M16"* derken tabloda **17** satır vardı; v2 yazılırken
**aynı kusur tekrar üretildi** (*"otuz iki"* ⇄ 36). ② **kanıtsız mutant**: `09-MUTANT/00-OZET.md` 36
mutant beyan etti, ham çıktısı **8'inde** vardı.
Kodlar `I0`–`I5`. Türkçe sayı sözcükleri (0–99) **çözülür** — kusur tam da yazıyla yazılan sayılarda doğdu.
**Altın küme 12/12, EXIT 0.**

### 🔴 ARAÇ, YAZILDIĞI İLK HÂLDE KENDİ VAR OLUŞ SEBEBİNİ KAÇIRIYORDU
Altın küme ilk koşumda **10/12** verdi ve iki vaka kaldı. Sebep: regex yalnız *"N mutant"* sırasını
tanıyordu; slice-3c'nin **gerçek** kaçağı ise `## 6. MUTANTLAR — otuz altı;` yani **ters sıradaydı**.
Yani kapı, kendisini doğuran kusuru **göremiyordu**. `_YAZI_TERS`/`_RAKAM_TERS` eklendi.
*Bu, "araç önce kendini kanıtlar" kuralının neden pazarlığa kapalı olduğunun kanıtıdır: kapı gerçek
depoda koşulsaydı yeşil yanacak ve kusuru onaylayacaktı.*

### 🔴 GERÇEK DEPO İKİ DAİRESEL KANIT YOLU DAHA GÖSTERDİ
Altın küme geçtikten **sonra** gerçek `KANIT/slice-3c/` üzerinde koşuldu ve kapı **yine kördü** —
"36/36 kanıtlı" dedi. Ölçüldü, iki kaynak:
1. **`HUKUM.md`** içindeki M1–M36 listesi kanıt sayılıyordu ⇒ özet/hüküm belgeleri elendi.
2. **`07-G7-regresyon/07-spec-kapi-kapsama.txt`** — `spec-kapi-kapsama.py`'nin
   `MUTANT(36): M1, M2, …` satırı. Bu, mutantların **koşulduğunun** değil **spec'te yazılı olduğunun**
   çıktısıdır ⇒ **ENVANTER REDDİ** kuralı: `LISTE_ESIGI = 8` veya daha fazla farklı mutant kimliği
   taşıyan dosya bir listedir, tekil koşum kanıtı **değildir**; reddedilen dosya **basılır** (sessiz değil).
   Eşik **uydurulmadı**: gerekçesi *"tek mutant koşumunun ham çıktısı doğası gereği bir-iki mutant anar"*.

**Düzeltmelerden sonra gerçek koşum:** `09-MUTANT/00-OZET.md` + `--kanit KANIT/slice-3c` ⇒ **KIRMIZI**,
**31 mutant `I2` (kanıtsız)**. Cowork'ün elle bulduğu sayı 28'di; **araç daha sıkı ölçtü** — elle
denetimde `03-K65-bulgu-ve-duzeltme.md`'nin andığı M13/M15/M21/M23/M34'ü kanıt saymıştım.
⇒ **K66/2 artık beyan değil, KAPI.**

### Beyan edilen sınır (gizlenmiyor)
Kapı mutantın **gerçekten ısırdığını** ölçmez ve ham çıktının **içeriğini** doğrulamaz — kanıtın
**varlığını** ölçer. Bir dosyanın var olması o mutantın koşulduğunu **kanıtlamaz**; kanıtın
**üretildiğini** gösterir. `spec-kapi-kapsama.py` de aynı sınırı beyan eder; ikisi birlikte
*kapsama + kanıt varlığı* ölçer, *ısırma* hâlâ koşan kodun işidir.

### K34-f notu
Bu araç Cowork tarafından **yazıldı**; iki düzeltme de teslimden **önce**, altın küme ve gerçek koşum
kırmızı yanınca yapıldı ⇒ yazım sürecinin parçasıdır, K34-f'nin kastettiği *onarım* değildir.
Teslim sonrası ilk onarım **ayrı ele** gider.


## 🔒 CHECKPOINT (28 Tem 2026 · oturum 32 — **slice-3c BAĞIMSIZ DOĞRULANDI (K26); dört bulgu**)

Claude Code T1–T8'i bitirdi ve devir notu verdi. Cowork **beyana güvenmedi**; her kapıyı **kendi koştu**.

### ✅ COWORK'ÜN KENDİ KOŞUMU — builder'ın rakamları DOĞRULANDI
| ölçüm | Cowork'ün koşumu |
|---|---|
| `flutter analyze --fatal-infos` | **No issues found!** (7,4 s) · EXIT 0 |
| `flutter test` | **+79: All tests passed!** · EXIT 0 |
| `araclar/verify.ps1` | build **0 uyarı / 0 hata** · ArchitectureTests **5** + Api.Tests **15** + SyncCore.Tests **40** + Persistence.Tests **54** = **114/114** · CVE **0** · `== VERIFY PASSED ==` · EXIT 0 |
| `spec-kapi-kapsama.py` | 8 kapı · 10 kural · 36 mutant · **BULGU YOK** · EXIT 0 |
| `sayi-tazeligi.py` · `tek-kopya-kapisi.py` · `design-token-kapisi.py` | TEMİZ · YEŞİL · TEMİZ — üçü de EXIT 0 |

**Cowork kendi ara-hesabını da düzeltti:** ilk bakışta üç test projesi görüp **109** sanmıştım;
`Momentum.ArchitectureTests` (5) tail'in dışında kalmıştı. Tam çıktıyı okuyunca **114** çıktı —
builder'ın rakamı doğru, benim ara-toplamım yanlıştı. Yazmadan önce yakalandı.

### 🔬 MUTANT KANITI — Cowork KANIT'a değil, HAM TRANSKRİPTE baktı
`09-MUTANT/00-OZET.md` dürüstçe beyan ediyor: **M1, M2, M21, M23, M33, M34–M36 taze koşuldu ve ham
çıktıları yazıldı; kalan 28 mutantın ham terminal çıktısı KANIT dosyasına kopyalanmadı** — dayanağı
"oturum transkripti". Cowork bunu **beyan olarak kabul etmedi**: Claude Code'un transkriptini
(`~/.claude/projects/C--dev-Momentum/a8ec6122-….jsonl`, **9.124.995 bayt**) kendi ayrıştırdı ve her
mutant için ① `// MUTANT Mnn` uygulama izini ② aynı bölgede test-başarısızlık izini aradı.
**Sonuç: 36/36'da her ikisi de var.** (`M33` analyzer mutantı olduğu için `Some tests failed` değil
`1 issue found` + `fatal-infos` izi taşıyor — aracın ilk deseni onu kaçırdı, desen düzeltildi.)
🟡 **Bu ölçümün BEYAN EDİLMİŞ SINIRI:** yakınlık ölçer (aynı bölgede etiket + hata), **nedensellik
kanıtlamaz**. Ham KANIT dosyasının yerini tutmaz — bulgu 2'ye bakınız.

### 🔴 BULGU 1 — devir notundaki KİMLİK YANLIŞ (kanonik-kopya sınıfı)
Devir notu *"`00-OZET.md` (5640 bayt, sha8 `4A5CEB71`)"* diyor. **Ölçüldü:** kök
`KANIT/slice-3c/00-OZET.md` = **3.147 b · `54D1570A`**. Verilen kimlik aslında
**`KANIT/slice-3c/09-MUTANT/00-OZET.md`**'ye ait (**5.640 b · `4A5CEB71`** — birebir tuttu).
İki ayrı `00-OZET.md` karıştırılmış. Bu projede kimlik **sözleşmedir**; yanlış atıf, K57'de
mekanikleştirilen *bayat/yanlış çapraz-atıf* sınıfının aynısıdır.

### 🔴 BULGU 2 — spec §8 şartı TAM KARŞILANMADI (beyan edilmiş, gizlenmemiş)
Spec §8: *"`09-MUTANT/` ← M1..M36: **her biri için** 'kapı KIRMIZI' kanıtı + geri alma"*.
Fiilen: **8 mutantın ham çıktısı var, 28'inin yok.** Builder bunu **açıkça beyan etti** — bu, projenin
*"beyan edilmiş sınır kabul edilir"* ilkesine uyar ve gizleme **değildir**. Ama şart **karşılanmadı**:
kanıt KANIT'ta değil, transkriptte. Cowork transkriptten doğruladı ⇒ bulgu **kapandı, fakat KANIT'la
değil, YÖNTEMLE**. Kalıcı ders: *mutant koşumunun ham çıktısı, koşulduğu anda dosyaya yazılmazsa
sonradan yeniden üretilemez.* İlk ısırışta mekanikleştirilir.

### 🔴 BULGU 3 — KABUL KRİTERİ 8'İN ÖLÇÜMÜ KÖR
Kriter 8'in kanıtı `git --no-optional-locks diff --stat 5df3caf -- src/backend/` idi. Cowork koştu:
`src/backend/Momentum.Api/Program.cs | 13 +++-` — **tek dosya**.
🔴 **`git diff` TAKİPSİZ DOSYALARI GÖRMEZ.** `src/backend/` altında fiilen ayrıca şunlar var:
`Momentum.Api/Auth/DevCurrentUser.cs` (meşru, spec izinli) **ve** iki stray klasör
(`Momentum.Api.Tests/`, `Momentum.Persistence.Tests/`). Diff bunların **hiçbirini** göstermiyor.
Yani kriter 8, tek başına kullanıldığında *"backend'e yalnız 12 satır dokunuldu"* iddiasını
**kanıtlayamaz** — takipsiz eklemeler ölçümün kör noktasıdır. Doğru ölçüm `diff` **+** `status` birlikte.
*(Builder stray klasörleri ayrıca beyan etti ⇒ gizleme yok; kusur ÖLÇÜM ARACINDA, dürüstlükte değil.)*

### 🔴 BULGU 4 — TEŞHİS DİLİ YANLIŞ: bu bir "backend kusuru" DEĞİL
Devir notu K65'i *"backend'de gerçek, tekrarlanabilir kusur"* diye adlandırıyor. **Katılmıyorum ve
gerekçem ölçülebilir:** düzeltme **tamamen istemcide** yapıldı (`opId` v4→v7, `counter` her damgada
artar), backend'e **tek satır dokunulmadı** ve kusur **kapandı**. Backend'de kalan bir kusur olsaydı
istemci düzeltmesi onu **kapatamaz, maskelerdi**.
Gerçek durum, `uretimIdUret()`'in K65 yorumunda **doğru** yazılmış: backend `HlcKey`'in tie-break'i
*"zaman-sıralı bir `opId` **varsayar**"* — ve bu varsayımı **hiçbir yerde zorlamaz**. Spec (`D1`/`D7`)
`opId`'yi **UUID v4** diye pinleyerek o varsayımı ihlal etti. Yani sınıflandırma:
**① spec kusuru (asıl, kapandı) · ② backend'de ZORLANMAMIŞ VARSAYIM (zayıf kontrol, AÇIK).**
Bu ayrım ucuz değil: *"backend kusurlu"* denirse ADR 0002 haksız suçlanır ve slice-3d'de kusur
**yanlış yerde** aranır.

### 🟡 YENİ AÇIK BORÇ — backend `OperationId` v7 varsayımını ZORLAMIYOR
`HlcKey` zaman-sıralı `opId` varsayıyor; `SyncIngest.IsEnvelopeValid` yalnız *"boş GUID değil"* diye
bakıyor. **v4 gönderen herhangi bir istemci aynı yazı-turayı geri getirir** ve hiçbir kapı yanmaz.
Bugün kapalı olmasının tek sebebi **istemcinin uslu davranması**. Kalıcı çözüm ADR 0002'nin işidir
(sürüm nibble'ını zarf geçerliliğinde zorlamak) ⇒ slice-3d/ADR borcu olarak yazıldı.


### 🔴 BULGU 5 — `KANIT/slice-3c/02-G2/` BOŞ DEĞİL, SİLİNEMEZ ve KANONİK DEĞİL (Cowork ölçtü)
Devir notu ve `00-OZET.md` *"boş `KANIT/slice-3c/02-G2/` klasörü (içeriği doğru yere taşındı), izin
katmanı silemedi, Onur elle silecek"* diyor. **Üçü de yanlış:**
- **Boş değil.** `git add` sırasında içinden **dört JSON** geldi. Ölçüm: dosyaların mtime'ı **23:48:28** —
  yani Cowork'ün 23:44'teki taramasında klasör **gerçekten boştu**, sonra **yeniden doldu**.
- **Sebebi ölçüldü:** `src/client/test/g2_registry_zarf_kapisi_test.dart` **satır 64**:
  `final dizin = Directory('../../KANIT/slice-3c/02-G2');` — G2 testi ham WireOp JSON'larını
  **hâlâ eski yola** yazıyor. Klasörü dolduran şey Cowork'ün kendi `flutter test` koşumuydu (23:47‑49).
  ⇒ **Onur silse bile bir sonraki test koşumunda geri gelir.** "Silinmeyi bekliyor" beyanı yanlış.
- **Kanonik kopya kusuru:** `02-G2-registry-zarf/` altındaki dört JSON (**23:33**) testin canlı çıktısı
  **değil**, o saatteki bir koşumun **elle kopyalanmış** hâli. İki kopyanın sha'ları farklı
  (`152E769F/A85B5484/94BEF982/9EB18562` ⇄ `1F80477D/A2E0C84F/DA75C50E/FFEA66C2`) çünkü her koşum yeni
  GUID üretiyor. `00-OZET.md`'nin *"`02-G2-registry-zarf/` + 4 ham WireOp JSON"* atfı, testin **fiilen
  yazdığı** dosyalara değil, **dondurulmuş kopyalarına** işaret ediyor.
- **Spec §8 ile kod uyuşmuyor:** şema `02-G2-registry-zarf/` diyor, üretici kod `02-G2/` yazıyor.

**Cowork'ün aldığı aksiyon:** `02-G2/` commit'ten **çıkarıldı** (`git reset`) — her koşumda değişen
türetilmiş çıktı repoya girerse sonsuz diff gürültüsü üretir. `02-G2-registry-zarf/` (donmuş kopya)
commit'te **kaldı**. Kalıcı düzeltme **üretici elin** işidir (test yolu `02-G2-registry-zarf/` olmalı,
ya da klasör `.gitignore`'a); Cowork kod yazmaz, bulguyu devreder.

**Sınıf:** `kanonik-kopya` + `beyansiz-sinir`. Bu, radar defterinde **mekanikleşmiş** sayılan bir sınıf
ama burada mekanik kontrol **ısırmadı**, çünkü hiçbir araç *"KANIT dizini ile onu yazan kodun yolu
aynı mı?"* diye sormuyor. Yeni açık borç.

### Hüküm
**slice-3c KABUL EDİLDİ.** Sekiz kapı, 36 mutant ve dört ölçüm zinciri Cowork'ün kendi koşumunda
doğrulandı; dört bulgunun ikisi (1, 3) **ölçüm/atıf** kusuru, biri (2) beyan edilmiş eksik, biri (4)
**adlandırma** düzeltmesi. Hiçbiri ürünün davranışını değiştirmiyor; dördü de kayda geçti.


## 🔒 CHECKPOINT (27 Tem 2026 · oturum 32 KAPANIŞ: **K64 — spec v2 KİLİTLENDİ**)

Onur onayladı: `GOREV_CLAUDE_CODE/GOREV-slice-3c-senkron.md` — **41.692 b · `537D0579`**.
**`5899A220` (v1) GEÇERSİZDİR.**

### Kilit BEYAN DEĞİL, KAPI oldu
Spec `araclar/tek-kopya-kapisi.py`'nin `VARSAYILAN_KAPSAM`'ına **`kilitli`** sınıfıyla eklendi ⇒ tek
baytlık sapma **açılış protokolü adım 2'de** ölçülür. Bu, K59'un slice-3b spec'inde öğrenilen dersin
uygulanmasıdır: *kilit, onu ölçen bir kapı yoksa yalnızca bir dilektir.*
Aracın **saf çekirdeği değişmedi** — yalnız kapsam listesine bir satır eklendi; **altın küme yeniden
koşuldu: 19/19, EXIT 0** (kapsam değişikliği `denetle()`'yi etkilemiyor, bu ölçümle doğrulandı).

### Oturum 32'nin bilançosu
- **Üç kilit:** K61 (dev-kimlik kalkanı) · K62 (`D0`–`D9` tasarım kararları) · K64 (spec v2).
- **Bir ders:** K63 — *beyan edilmiş bir eksik, kapatılmamış bir eksiktir.* Denetim reddedildiğinde
  Cowork eksiği **beyan ederek** devam etti; Onur yanlış anlamayı düzeltince denetim koştu ve
  **spec'i kırdı**. Beyan, denetimin yerine geçmez — yalnız onu görünür kılar.
- **Ölçülen:** açılış kapıları YEŞİL · push `0	0` (devir notu bayattı) · denetim 10 bloker/12 önemli/8 not,
  30 kapatıldı · spec v2 8 kapı / 10 kural / 36 mutant / `spec-kapi-kapsama.py` EXIT 0.
- **Kapanan borç:** *"spec bağımsız denetimden geçmedi"* (K26 sağlandı).
- **Açılan borç:** **sayı ↔ liste tutarsızlığı** Cowork'ün tekrar eden kusurudur (iki kez ölçüldü) ve
  onu ölçen mekanik kontrol **yoktur**. K44-a gereği ilk fırsatta araç yazılır.
- **`urun_kodu_satiri` = 0**, üst üste ikinci sıfır ⇒ **R8 sert durağı yürürlükte: bir sonraki oturum
  ürün koduyla başlar.** Kâğıt turu kapandı; sıra Claude Code'da.


## 🔒 CHECKPOINT (27 Tem 2026 · oturum 32: **K63 — spec v2, İKİ BAĞIMSIZ DENETÇİ SPEC'İ KIRDI**)

### Önce dürüstlük kaydı: bu tur neredeyse hiç koşmayacaktı
Cowork denetçi ajanları çağırdı, **Onur reddetti** ("işlem durdu sandım"), Cowork *"K26 sağlanmadı"* diye
**beyan ederek** devam etti. Onur yanlış anlamayı düzeltti ve **"koşulsun"** dedi. Denetim koştu ve
**spec v1'i kırdı.** Ders: *beyan edilmiş bir eksik, kapatılmamış bir eksiktir* — beyan onu ucuzlatmaz,
yalnız görünür kılar. Görünür olduğu için kapandı.

### Kurulum: iki denetçi, aynı tur, birbirinden habersiz (K53 madde 1 — tek tur)
① **Mimar/denetçi** — spec'in her olgusal iddiasını 13 gerçek kaynak dosyadan yeniden ölçtü.
② **Red-team** — spec'i onaylamaya değil **kırmaya** çalıştı: sessiz kusur senaryosu, çökme anları,
eşdeğer mutant, "yeşil ama yanlış" teslim.
**Ölçülen sonuç: 10 bloker sınıfı · 12 önemli · 8 not.** Altı bulguyu **ikisi birden** buldu — bağımsız
iki elin aynı kusuru bulması, bulgunun gerçek olduğunun en ucuz kanıtıdır.

### 🔴 SPEC'İN ONAYLANMASI HÂLİNDE OLACAKLAR — dördü "tüm kapılardan yeşil geçen" veri kaybı
1. **`actorId` kilitsizdi.** `SyncIngest.IsEnvelopeValid` dört zarf alanını (`OperationId`, `ClientId`,
   `EntityId`, **`ActorId`**) **boş GUID olamaz** diye şart koşuyor; spec'in `D2` tablosunda zarf **hiç yoktu**.
   Atlanırsa: her op `RejectedInvalid` ⇒ **dedup'a yazılmaz** ⇒ **kuyruğun tamamı kalıcı karantinaya düşer**.
   Rastgele bir GUID konursa: `tasks` **doğru** yazılır ama outbox `OwnerId`/`ActorId` (`op.ActorId`'den
   beslenir) **yanlış aktöre** etiketlenir ⇒ tüm kapılar yeşil, slice-3d'nin yönlendirmesi bozuk doğar.
2. **Alan/grup yazımlarının kendi HLC'si spec'te yoktu.** `WireFieldWrite(Value, **Hlc**)` ve
   `WireGroupWrite(Fields, **Hlc**)`. `hlc` atlanırsa `WireMapping.ToHlc(null)` ⇒ **NRE ⇒ 500**; validator
   yakalamaz. Spec'in `D5`'i 5xx'i **hiç sınıflandırmıyordu**.
3. **`durum='gonderildi'`in ÇIKIŞI YOKTU.** İstek gitti, yanıt gelmeden çökme ⇒ satır bir daha **asla
   seçilmez, asla silinmez**; sunucuda veri **var**, istemci bunu **asla bilemez**, rozet kalıcı yanlış.
4. **Sunucu HLC clamp'i spec'te hiç geçmiyordu** (`HlcClamp`: 5 dk tavan, 365 gün absürt).
   Ölçülmüş senaryo: cihaz saati 6 dk ileri → çevrimdışı iki ardışık başlık düzenlemesi → aynı istekte
   gider → sunucu ikisini de aynı tavana kırpar → iki alan-HLC'si **birebir aynı** olur → `LwwRegister`
   tie-break'i `opId` **dize-ordinal**, `opId` **rastgele UUID v4** ⇒ **%50 olasılıkla kullanıcının SON
   yazdığı değer kaybolur**, iki op da `Applied` döner, **hiçbir kapı kırmızı yanmaz.**
   İkinci ölüm: `sonWall` **kalıcı ve tavansız** ⇒ saat bir kez 2030'a alınırsa cihaz **kalıcı olarak**
   senkron dışı kalır (`RejectedAbsurdHlc`), tek çare uygulamayı silmektir.
5. **`G6` sunucudaki İÇERİĞİ hiç ölçmüyordu.** Red-team'in teslim senaryosu: `title` boş dize yazılsın —
   `G2` geçer (anahtar kontrolü, **değer değil**), `G3` geçer, `G6` ayak 2 `Applied`, ayak 3 `Duplicate`
   ⇒ **on kabul kriteri yeşil, sunucudaki üç görevin başlığı boş.**

### 🔴 KENDİ MUTANTLARIM KÖRDÜ — iki tanesi ölçümle çürütüldü
- **`M13` EŞDEĞER MUTANTTI.** *"Yanıttaki `applied`'ı yok say, kuyruğu koşulsuz temizle"* — `G6`'nın üç
  ayağının hiçbirinde **reddedilen op yoktu**, dolayısıyla bozma doğru davranışla **birebir aynı gözlemi**
  üretiyordu. Üstelik K53'ün en kıt bütçesini (koşan-uygulama tavanı **3**) yiyordu. **İptal edildi.**
- **`M14`'ün beklenen sonucu OLGUSAL OLARAK YANLIŞTI.** *"DB'de kopya doğar"* demiştim.
  `EntityMaterializer`: `INSERT INTO tasks … ON CONFLICT (**entity_id**) DO UPDATE` — satır anahtarı
  `entity_id`, `operation_id` değil ⇒ `opId` değişse bile **`tasks`'ta kopya satır DOĞAMAZ**.
  Kapının hüküm dayanağı `processed_operations` sayımına taşındı; `tasks` bilgi amaçlı kaldı.
- **`M5b` numaralandırma DIŞINDAYDI.** Spec dört yerde *"on altı mutant · M1–M16"* diyordu; tabloda
  **17 satır** vardı. §7 kriter 2'ye **harfiyen uyan** bir teslim, `completion` REPLACE semantiğinin
  **tek** mutantını atlar ve yine de "tüm kriterler sağlandı" derdi. İki araç da bunu **göremiyor**:
  `spec-kapi-kapsama.py` yalnız *kapsama* sorar (D2 zaten kapalıydı), `sayi-tazeligi.py` yalnız
  `"altin kume N/M"` imzalı satırlara bakar. **Mekanik kapı, ölçmediği şeyi kanıtlıyormuş gibi okunuyordu.**

### Spec v2 — ölçülen
`GOREV-slice-3c-senkron.md` **41.692 b** (v1: 25.023 b, `5899A220` — **GEÇERSİZ**).
**8 kapı** (`G1`–`G8`; yeni `G8` = atomiklik + çökme kurtarma) · **10 kural** (`D0`–`D9`; yeni `D7` zarf+HLC
iskeleti, `D8` atomiklik+kurtarma, `D9` HTTP sınıflandırma) · **36 mutant** (`M1`–`M36`).
`spec-kapi-kapsama.py` ⇒ **EXIT 0**. Koşan-uygulama mutantı **tam üç** (`M34`, `M35`, `M36`) — tavan üç;
biri artık `G6`'nın yeni **içerik ayağını** ölçüyor, biri clamp senaryosunu **canlıda** kırıyor.

**Kapatılan:** 10 bloker · 12 önemli · 8 not = **30**. **Beyan edilerek açık bırakılan üç sınır**
(§10): migration ortası çökme · kuyruk büyüklüğü tavanı · tek kullanıcı varsayımı (`ayarlar`'da tek
`clientId`/tek imleç — slice-3d'de patlar).

### Bu turda ÜRETİLEN kusur: 1 (kendim yakaladım)
v2'yi yazarken mutant sayısını dört yerde **"otuz iki"** yazıp tabloya **36** satır koydum — v1'de
denetçinin yakaladığı `M5b` kaçağının **birebir aynısını** tekrar ürettim. Kapıyı koşmadan önce fark
edip düzelttim. Kayda geçiyor: **bu sınıf (sayı↔liste tutarsızlığı) benim tekrar eden kusurumdur** ve
şu an onu ölçen mekanik bir kontrol **yoktur** — `spec-kapi-kapsama.py` mutantları sayar ama belgenin
**kendi sayı iddiasıyla** karşılaştırmaz. İlk ısırışta araç yazılır (K44-a).

### R8 hâlâ haklı
`urun_kodu_satiri` = **0**, üst üste ikinci sıfır. **Bir sonraki oturum ürün koduyla başlar.**
Spec artık koda hazır; kâğıt turu **kapandı** (K53 madde 1 tavanı: bir tur, koştu, bulguları kapatıldı).


## 🔒 CHECKPOINT (27 Tem 2026 · oturum 32: **K62 — slice-3c spec'i yazıldı, `D0`–`D6` kilitlendi**)

### Onur'un dört kilidi (K40 — şıkkı oturum seçmez)
| # | kilit | reddedilen alternatif ve **adlandırılmış bedeli** |
|---|---|---|
| ① | **`X-Momentum-Dev-User`** ayrı başlık; `UserId` ⟂ `ClientId` | *cihazdan türetme*: "kullanıcı = cihaz" varsayımı kodun içine girerdi ⇒ slice-3d işbirliği vitrini **ölçülemez** olurdu |
| ② | Alan kapsamı `title` + `isDeleted` + **`completion` grubu** | *yalnız `title`+`isDeleted`*: tek kanal, en hızlı yeşil — ama **grup kanalı riski slice-3d'ye ötelenirdi**; ötelenen risk ucuzlamaz |
| ③ | Zehirli op **karantina** + `cakisma` rozeti | *sil ve logla*: kullanıcının değişikliği **sessizce** kaybolurdu · *3 kez yeniden dene*: `RejectedInvalid` **kalıcıdır**, üç deneme boşa gider |
| ④ | **Yalnız push**; çekme yanıtı okunur, uygulanmaz | *push+pull*: iş ~2 katı; K53 madde 5 (**yürüyen iskelet önce**) buna karşı |

### 🔴 SPEC KÂĞITTAN DEĞİL KODDAN YAZILDI — üç ölçüm devir notunda YOKTU ve omurga oldu
Cowork şu dosyaları **kendi okudu** (beyana güvenmedi): `FieldStrategyRegistry.cs` · `ProjectionFields.cs` ·
`EntityState.cs` · `SyncCommandHandler.cs` · `SyncContracts.cs` · `SyncRequestValidator.cs` ·
`SyncEndpoints.cs` · `veritabani.dart` · `gorev_deposu.dart`.

1. **`tamamlandi` `Groups["completion"]`'a gider.** Registry `completion` grubunu (`status`, `completedAt`)
   tanımlıyor; `Fields`'e konursa `IsOperationValid` **false** döner ⇒ `RejectedRegistryViolation` ve
   **op BÜTÜN olarak** reddedilir. **Remap yoktur** (ADR H11). Bu, "biraz yanlış" değil **toptan kayıptır**.
2. **`isDeleted` değeri tam olarak `"true"`.** `EntityState.cs`: `DeletedValue = "true"`,
   `StringComparison.Ordinal`. `"True"` **reddedilmez** — sessizce *silinmemiş* sayılır.
   Reddedilen kusur ucuzdur, **sessiz kusur pahalıdır**; `M4` onu hedefliyor.
3. **Grup yazımı REPLACE'tir.** `ProjectionFields`: *"member absent from the winning REPLACE"* ⇒
   `completion` yazılırken `status` **ve** `completedAt` **daima birlikte** yazılmalı; yazılmayan üye **kaybolur**.

Ek iki ölçüm: **`RejectedInvalid` dedup'a kaydedilmiyor** (kodda `// ERRATA`) ⇒ yeniden deneme bunu
**asla** çözmez, karantina **zorunludur**. **`MaterializeAsync(…, authenticatedActorId, …)`** ve
`SnapshotAsync(actorId)` / `PullIncrementalAsync(actorId, …)` ⇒ dev kimliğin `UserId`'si oturumlar
arasında **kararlı** olmak zorunda, yoksa istemci kendi yazdığını geri okuyamaz.

### Ölçülen spec
`GOREV_CLAUDE_CODE/GOREV-slice-3c-senkron.md` — **25.023 b · `5899A220`** · **v1, Onur'un onayı bekleniyor.**
**7 kapı** (`G1`–`G7`) · **7 kural** (`D0`–`D6`) · **17 mutant** (`M1`–`M16` + `M5b`).
`spec-kapi-kapsama.py` ⇒ **EXIT 0** (mutantsız kapı/kural **yok**) · `sayi-tazeligi.py` ⇒ **EXIT 0**.
**Koşan-uygulama mutantı yalnız iki** (`M13`, `M14`) — K53 madde 3 tavanı üç; kalan on beşi statik/birim/widget.

### ⚠️ BEYAN — BU TURUN EKSİĞİ GİZLENMİYOR
**Bağımsız kâğıt denetim turu KOŞULMADI.** Cowork bağımsız denetçi ajan çağırmayı denedi; **Onur reddetti
ve "devam et" dedi** — talimat yetkidir. Sonuç: **K26 (üreten ≠ denetleyen) bu turda SAĞLANMADI.**
Geçilen tek denetim **mekaniktir**. Radar defterindeki **`uretilen: 0` ÖLÇÜLMÜŞ DEĞİL, ÖLÇÜLMEMİŞTİR**;
"temiz" demek değildir ve defter kaydının `not` alanına da böyle yazıldı.
K53 madde 1–2 ile tutarlı okuma: radar KIRMIZI'da varsayılan **DEVRET**'tir ve bu spec zaten koda
devrediliyor — kâğıt turu **açmamak** kuralın ihlali değil **uygulanmasıdır**; ama bu, denetimin
**yapıldığı** anlamına gelmez. Gerçek denetim Claude Code'un **koşan kodunda** olacak.

### R8 — SERT DURAK HAKLIDIR
Bu oturumun `urun_kodu_satiri` = **0** (dürüst ölçüm: Cowork spec ve belge yazdı, ürün kodu yazmadı).
Oturum 31'in son kaydından sonra bu **ikinci 0**'dır. ⇒ **Bir sonraki oturum ÜRÜN KODUYLA başlar;
yeni belge/ADR/spec/araç turu AÇILMAZ.** Radar bu oturumda yeni artefaktı **YEŞİL** okudu
(`GOREV_CLAUDE_CODE/GOREV-slice-3c-senkron.md`, tur 1, bulgu yok); proje hükmü **KIRMIZI** kaldı ve bu
**beklenendir** — kırmızıyı yakan üç artefakt park edilmiş kâğıttır.


## 🔒 CHECKPOINT (27 Tem 2026 · oturum 32 AÇILIŞ: **K61 — dev-kimlik kalkanı KİLİTLENDİ**)

### Açılış ölçümleri (beyan değil, koşum)
- `tek-kopya-kapisi.py .` ⇒ **YEŞİL**, `EXIT 0`. Sekiz tek kopya dosyanın hepsi HEAD ile **+0 bayt**
  (`PROJE_HAFIZA.md` 548.930 · `PROJE_RADAR.jsonl` 58.376 · `DURUM.md` 20.694 · `CLAUDE.md` 11.319 ·
  `DESIGN.md` 15.742 · spec 44.560 · `radar.py` 28.878 · `adr-kapi-taramasi.py` 50.582).
- `radar.py --altin-kume` ⇒ **18/18 GEÇTİ**, `EXIT 0`. `radar.py .` ⇒ **KIRMIZI**, `EXIT 2` — **beklenen**:
  kırmızıyı yakan üç artefakt (`docs/ADR/0003-kimlik-cekirdegi`, `slice-3b-istemci`, `GOREV-slice-3b-spec`)
  **park edilmiş kâğıttır**; varsayılan şık **DEVRET** zaten yürürlükte.
- `git --no-optional-locks status --porcelain` ⇒ tek satır: `?? KANIT/slice-3b/01-G1-android/widget-tree.json`.
  `.git\index.lock` ⇒ **yok** (`False`).

### ⚠️ DEVİR NOTUNUN BİR İDDİASI ÖLÇÜMLE ÇÜRÜDÜ
Oturum 31 devir notu *"push edilmedi, 2 commit ileride (`b6140f4`, `6eb8433`)"* diyordu.
**Ölçüm:** `git --no-optional-locks rev-list --left-right --count origin/main...HEAD` ⇒ **`0	0`**.
Yani `origin/main` **HEAD ile aynı commit'te** — push **yapılmış**. Devir notundaki satır
**yazıldığı an doğru, okunduğu an bayattı**. Ders yeni değil ama bu sefer araçla değil elle yakalandı:
**§4 "ölç ya da `[DOĞRULANMADI]` yaz"** kuralı devir notlarının *git durumu* satırı için de geçerlidir —
bu yüzden `DURUM.md` §3'te git satırı zaten *"yazılmaz, açılışta ÖLÇÜLÜR"* diyordu. **Kural işe yaradı.**

### K61 — KİLİT: **şık 1, geçici dev-kimlik kalkanı** (Onur kilitledi, K40)
**Ölçülen engel bağımsız doğrulandı** (K26 — Cowork, oturum 31'in beyanına güvenmedi, dosyayı kendi okudu):
`src/backend/Momentum.Api/Program.cs` **satır 45** ⇒ `builder.Services.AddScoped<ICurrentUser, NullCurrentUser>();`
(`// deny-by-default (K-D5)`), **satır 121** ⇒ uçta `GetRequiredService<ICurrentUser>()`. Yani `POST /v1/sync`
bugün **her istekte 401** döner ve istemci kuyruğu **uçtan uca kanıtlanamaz**.

**Kilitlenen çözüm:** yalnız `Development` ortamında `X-Momentum-Client` başlığından `UserId` üreten
`DevCurrentUser`; **üretimde `NullCurrentUser` KORUNUR** ve bunu bir **mutant test** kanıtlar
(mutant: ortamı `Production`'a çevir ⇒ istek **401 dönmeli**; dönmezse kapı kör demektir).
**ADR 0003 DONMUŞ kalır** (K41 açılmadı, üç şart tetiklenmedi).

**Neden bu şık:** ① K41 kilidini bozmaz. ② Adım 3'ün *asıl* iddiasını —
*"çevrimdışı biriken kuyruk ağa çıkınca sunucuya uçtan uca işlenir"* — **koşan kodla** ölçülebilir kılar;
K53 madde 5 (**yürüyen iskelet önce, kapılar sonra**) tam olarak bunu ister.
③ Şık 2 (ADR'yi çöz) radar KIRMIZI'yken **yeni bir kâğıt turu** açardı — K53 madde 1'in yasakladığı şey.
④ Şık 3 (ikiye böl) uçtan uca akışı **kanıtlanmadan** bırakırdı; bu projede kanıtsız kapı kapı sayılmaz.

**Beyan edilen sınır (gizlenmiyor):** `DevCurrentUser` bir **kimlik çözümü değildir**, ölçüm iskelesidir.
Gerçek kimlik hâlâ ADR 0003'ün borcudur; slice-3c bu borcu **kapatmaz**, yalnız **etrafından ölçer**.

### Onur'un ikinci kararı
`KANIT/slice-3b/01-G1-android/widget-tree.json` (345 b, takipsiz, tarihe hiç girmedi):
**kalıcı silme Onur'da kalır; Cowork dokunmaz** (kırmızı çizgi 4). `DURUM.md` §8'de açık borç olarak durur.


## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31 KAPANIS: **K60 kapıya çevrildi + adım 3 engeli ölçüldü**)

### K60 artık beyan değil **kapı**: `tek-kopya-kapisi.py` 1.1.0
Onur sordu: *"sen bunu unutursan ne yapacağız?"* Dürüst cevap: **unuturum**. Disiplin bir
kontrol değildir ve bunu aynı oturumda kanıtladım. Kapı artık kuralın *uygulanıp
uygulanmadığını* değil **dosyanın hâlini** ölçüyor — hangi el, hangi yöntem olduğu
umurunda değil. Açılış protokolü **adım 2**. Sınıf bazında: `append_only` küçülmez ·
`kilitli` sapmaz · `canli` %10 budanabilir. Kodlar `S0`–`S10`; muafiyet **gerekçesiz olamaz**
(`S7`) ve **ölü muafiyet söylenir** (`S9`). **Altın küme 19/19.**

### 🔻 Mutant, kapının **kendi kör noktasını** buldu
Altın küme yalnız saf `denetle()`'yi sıkıştırıyordu; **`olc()` ayağı kör kalsaydı kapı
sonsuza kadar YEŞİL yanardı.** Gerçek depo + gerçek git üzerinde 11 mutant koştum.
**Ölçüm:** `core.autocrlf` çalışırken `git restore`, LF ile yazılan **2.400 baytlık** dosyayı
**2.800 bayt** geri getiriyor ⇒ ham bayt karşılaştırması bu ortamda **kördür**; kapı 1 baytlık
kaybı **yuttu**. Düzeltme 1.1.0: karşılaştırma **LF-normalize** içerik üzerinden, ham fark
ayrıca **`S10`** ile söylenir. Sonra M2 **yine ısırmadı** ve incelendi: CRLF dosyanın son
baytını silmek yalnız satır sonu karakterini düşürür — **kapı haklıydı, mutant yanlıştı**.
Kapı **gevşetilmedi**; mutant dürüstleştirildi (M2a gerçek satır kaybı **ısırır**, M2b yalnız
satır sonu **susar** ve bu **beyan edilmiş sınırdır**). **Mutant 11/11**, gerçek koşum
**YEŞİL** (8 dosya, hepsi HEAD ile tutarlı). Kanıt: `KANIT/K60-tek-kopya/KOSUM.txt` —
beyan değil, üç koşumun **ham çıktısı**.

`sayi-tazeligi.py` bu oturumda **altıncı kez** kendi yazımımı yakaladı: DURUM tablosunda tek
satırda hem `19/19` hem `11/11` iddiası vardı. Kapı gevşetilmedi — mutant harness'ı
`[GECTI]/[KALDI]` etiketiyle **ölçülebilir** yapıldı ve `11/11` **kendi aracının** satırına taşındı.

### K42-d adım 3'ün önünde **ölçülmüş** bir engel var
Backend senkron yüzeyi **zaten tam** (uç, handler, store/puller/transaction, cursor, ingest,
migration, iki test sınıfı; sözleşme `WireOp`/`WireHlc`/`WireCursor`/`WireSnapshot*`). **Ama**
`Program.cs` `NullCurrentUser` kaydediyor ⇒ **her istek 401**, kimlik ADR 0003 ise **K41 ile
donmuş**. İstemci kuyruğu bu hâliyle **uçtan uca kanıtlanamaz**. Üç şık DURUM §4'te;
**Cowork seçmedi, kilit Onur'dan** (K40).

### Oturum sağlığı
Ölçüldü (varsayılmadı): **%80,8 — KIRMIZI**. Bu yüzden slice-3c spec'ine **başlanmadı**;
artefaktın ortasında devir yapmamak için kalan bütçe **ölçüme** harcandı.

## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31: **slice-3b KAPANDI — A2 8/8, commit `5df3caf`**)

### A2 yeniden ölçüldü — Cowork **iki ağacı kendi ayrıştırdı**
Claude Code K59'a göre iki bağımsız yakalama yaptı. Beyanına güvenilmedi: her iki JSON
Cowork tarafından **ayrıştırıldı** ve birleşim **kendi hesabımla** çıkarıldı.

| dosya | bayt | sha8 | JSON? |
|---|---|---|---|
| `widget-tree-vitrin.json` | 16.772 | `574223D0` | **ayrıştı** |
| `widget-tree-gercek-ekran.json` | 5.672 | `6C5D431A` | **ayrıştı** |

**Vitrin:** BosDurum · CakismaRozeti · GorevSatiri · HataDurumu · SenkronRozeti · YuklenmeDurumu
**Gerçek ekran:** GorevEkleAlani · GorevListesiEkrani · GorevSatiri · SenkronRozeti
**BİRLEŞİM: 8/8 — eksik YOK. KAPI YEŞİL.**
`HUKUM.md` (`D6AC8377`) artık **düğüm kimliklerine** atıf yapıyor (`inspector-16`, `inspector-8` …)
— beyan değil, **çıktı**. Kök neden doğrulaması da bağımsız: `durum_vitrini.dart`
`GorevListesiEkrani`'yi **hiç import etmiyor** ⇒ tek yakalama **yapısal olarak** 8'i gösteremezdi.

⚠ **İki sayım yöntemi farklı sonuç veriyor, çelişki değil:** `HUKUM.md` düğüm sayıyor,
Cowork'ün taraması hem `widgetRuntimeType` hem `description` alanlarını saydığı için ~2 katını
görüyor. **Kümeler aynı**; önemli olan o.

### 🔻 Cowork **üç** yanlış-pozitifini geri çekti
1. *"`a11y_kapisi_test.dart` `@TestOn` taşıyor"* — yalnızca **yorumda** geçiyormuş.
2. *"`HUKUM.md` hâlâ eski `widget-tree.json`'a atıf yapıyor"* — o atıf bir **kaynak** değil,
   *"bu dosya artık hiçbir iddianın kaynağı değildir"* diyen **dürüst bir artık-dosya beyanı**ymış.
3. Daha önceki kaba M2b/sayım kontrolü.
**Denetçi de yanılır; yanıldığını yazmayan denetçi denetçi değildir.**

### Commit `5df3caf` — 72 dosya, 91.005 satır
`git add -A` **kullanılmadı** (K55): 72 yol **tek tek** eklendi. **Bilerek dışarıda bırakılan tek
dosya:** eski `KANIT/slice-3b/01-G1-android/widget-tree.json` (345 b, JSON değil, kendine atıf
yapan proza). **Silinmedi** — kalıcı silme Onur'un yetkisinde; tarihe **girmedi**, çalışma
ağacında **görünür bir hatırlatıcı** olarak duruyor.

### 🚨 K60 — **Cowork kendi aracıyla 542 KB'lık arşivi 0 BAYTA düşürdü**
Bu checkpoint'i yazan ilk betik `io.open(yol,"w")` ile dosyayı **önce boşalttı**, sonra metni
encode ederken `UnicodeEncodeError: surrogates not allowed` aldı (kaynak: `"\ud83d\udd3b"`
Python'da **birleşmez**, iki yalnız vekil karakter olur). Sonuç: `PROJE_HAFIZA.md` = **0 bayt**.
**Kurtaran şey kural değil, şanstı**: dosya 30 dakika önce `5df3caf` ile commit'lenmişti,
`git restore` ile **542.475 bayt tam** geri geldi (`5C04B5B0`).
**Yeni kural (K60):** hafıza/spec gibi tek kopya dosyalara yazan her betik önce **encode eder**
(hata dosyaya dokunmadan patlar), sonra `.tmp`'ye yazar, en son `os.replace` ile **takas** eder.
`guvenli_yaz()` bu biçimde. **Beyan edilen zayıf nokta:** bunu zorlayan bir kapı **yok**;
tek koruma disiplin + commit sıklığı.

### Kapanış ölçümleri
Yedi aracın altın kümesi **EXIT 0** · `sayi-tazeligi` gerçek koşum **TEMİZ, muafiyetsiz** ·
`spec-kapi-kapsama` spec üzerinde **EXIT 0** · `DESIGN.md` **`534DFF68`** (K46) · spec
**`F0C3A75A`** (K59) · `radar.py` **`46E3A8BC`** (K57-b).

## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31: **K59 — SPEC v6; A2'nin SAĞLANAMAZ ŞARTI ÖLÇÜMLE ONARILDI**)

**Kilitli kimlik: `44.560 bayt · F0C3A75A`.** `6056A5BB` · `79A53AA3` · `BE4581BA` · `1AB02B73`
**geçersizdir.**

### Kök neden — bir spec'in KENDİ İÇİNDE çelişmesi
Cowork'ün T9 denetimi A2'yi majör bulgu olarak işaretledi (kanıt 6 ad taşıyor, spec 8 istiyor).
Onur *"önerini uygula"* dedi; önerim **gerçek ekranda tek bir yeniden yakalama**ydı.
**Uygulamadan önce kök nedeni ölçtüm ve kendi önerimi ÇÜRÜTTÜM:**

- `GorevSatiri` içinde `SenkronRozeti` **koşulsuz**, `CakismaRozeti` ise **yalnız
  `cakismaVarMi == true`** iken doğuyor (`gorev_satiri.dart:60‑63`).
- `cakismaVarMi`'yi **yalnızca durum vitrini** `true` yapıyor (`durum_vitrini.dart:88`) — çünkü
  spec'in **kendi §2'si** diyor ki: *"'çakışma' bu dilimde gerçek hayatta doğmaz; vitrinde ve
  testlerde görünür."*
- Buna karşılık `GorevEkleAlani` ve `GorevListesiEkrani` **vitrinde yok**, yalnız gerçek ekranda var.

⇒ **Hiçbir TEK yakalama 8 bileşeni gösteremez** — ve bunun sebebi builder değil, **spec'in kendi
kapsam kararı**. Vitrin 6, gerçek ekran 6, **birleşim 8**. Yani A2'nin şartı **sağlanamazdı**;
sağlanamaz bir kriter güçlü kapı değil, **bozuk** kapıdır: eli ya sahteciliğe ya sonsuz
başarısızlığa iter.

### Karar — gevşetme DEĞİL, pahalılaştırma
**A2 artık İKİ yakalama ister** (vitrin + gerçek ekran), **her ikisinin HAM JSON'u** dosyada
**olduğu gibi** durur ve **birleşim 8 sınıf adını** kapsamazsa kapı **KIRMIZI**'dır.
Gerekçe §5/G1'e **ölçümle** yazıldı. *Bu şart tek yakalamadan **daha pahalıdır**.*

🔻 **KENDİ HÜKMÜMÜ GERİ ALDIM — yazılı olarak.** Bir saat önce Onur'a *"spec'i düzeltmeyi
önermiyorum, kapıyı ölçüme uydurmak olur"* demiştim. O an bu ölçüm elimde yoktu. Fark şu:
**gevşetmek**, sağlanabilir bir şartı kolaylaştırmaktır; burada yapılan **sağlanamaz** bir şartı
sağlanabilir hale getirmektir — üstelik iş **artarak**. İki hüküm arasındaki tek fark **ölçümdür**.

### İkinci kalem — spec'in sayıları artık MEKANİK doğrulanıyor
Kabul kriteri **6 · 7 · 8**'e **araç adları** yazıldı ve rakamlar ölçülen değerlere çekildi:
`pub-cve-kapisi.py` **8/8** · `pub-lisans-kapisi.py` **4/4 → 6/6** · `design-token-kapisi.py`
**12 → 18/18**. Artık `sayi-tazeligi.py` bu satırları **aracı koşarak** doğruluyor.
**`T9-KAPANISI` muafiyetleri SİLİNDİ** — spec'in beş altın küme iddiasının (8/8, 6/6, 18/18,
13/13, 16/16) **hiçbiri artık muafiyetli değil**; hepsi her koşumda gerçekten ölçülüyor.

**Doğrulama:** `sayi-tazeligi.py` gerçek koşum **TEMİZ, EXIT 0** ·
`spec-kapi-kapsama.py` **13/13** ve spec üzerinde **EXIT 0** (kapı/kural/mutant sayıları
**değişmedi**: 7 · 16 · 24) · `DESIGN.md` **`534DFF68` dokunulmadı** (K46).

### Sıradaki
Claude Code A2'yi K59'a göre yeniden ölçer (iki ham ağaç JSON'u). Dönüşte Cowork bağımsız
doğrular → commit → **push Onur'da**. Sonra **K42-d adım 3** (senkron kuyruğu + `POST /v1/sync`).

## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31: **T8+T9 BAĞIMSIZ DOĞRULANDI — 1 MAJÖR, 4 MİNÖR BULGU**)

**slice-3b (K42-d adım 2) build'i bitti.** Cowork artefaktı **kendi ölçtü** (K26); builder'ın
özeti okundu ama **hüküm ondan alınmadı**.

### DOĞRULANAN — Cowork'ün KENDİ koşumları
| ölçüm | sonuç |
|---|---|
| `flutter analyze --fatal-infos --fatal-warnings` | **No issues found!** · EXIT 0 |
| `flutter test` | **36/36 All tests passed!** · EXIT 0 |
| 7 aracın altın kümesi | radar 18/18 · spec-kapı 13/13 · sayı-tazeliği 16/16 · design-token 18/18 · pub-cve 8/8 · pub-lisans 6/6 · hafıza-dizin 7/7 — **hepsi EXIT 0** |
| Gerçek kapı koşumları | design-token **TEMİZ** (D0–D6 = 0, 21 dosya) · pub-cve **TEMİZ** (90 paket) · pub-lisans **TEMİZ** (2 `ESLENDI` kanıtıyla) · spec-kapı EXIT 0 · sayı-tazeliği **TEMİZ** |
| Kırmızı çizgiler | `overridePathCheck` **hiçbir yerde yok** · testlerde `skip:` **yok** · F7 bayrak koruması `main.dart:13` |
| Kilitler | `DESIGN.md 534DFF68` · spec `6056A5BB` (**dokunulmamış**) · `radar.py 46E3A8BC` |
| 24 mutant | hepsinin KANIT dosyası var ve kırmızı izi taşıyor; **M11 fixture'a DÜŞMEDİ** — gerçek pub.dev paketi `iconsax` GPL‑3.0 ile bulundu |
| Kriter 13 | **yöntem sağlamasıyla** ölçüldü: release `libapp.so` ×3 mimaride vitrin/driver sembolü **0**, aynı arama debug `kernel_blob.bin`'de **4/2/10** ⇒ yanlış-negatif değil |

### 🔴 MAJÖR BULGU — A2 kanıtı iddiayı taşımıyor
Spec **G1/A2**: *"ağaçta **8 görsel bileşen adıyla** var."* Ölçtüm: `lib/sunum/` sekiz genel sınıf
taşıyor; `01-G1-android` kanıtı ve `HUKUM.md`'si **yalnız altısını** içeriyor —
**`GorevEkleAlani` ve `GorevListesiEkrani` ağaç kanıtında GEÇMİYOR.** `HUKUM.md` yine de
*"GEÇTİ"* diyor ⇒ **kabul kriteri 4 kanıtla desteklenmiyor.**
Ek olarak **`widget-tree.json` JSON değil, 345 baytlık prozadır** ve içinde *"Ham JSON:
widget-tree.json"* diyerek **kendine atıf** yapar. Projenin kendi kuralı: *"`HUKUM.md` kendi
beyanın değil, çıktıya atıflı olur."* **Ham ağaç korunmamış.**
*Olası açıklama (DOĞRULANMADI): ağaç `DURUM_VITRINI` ekranında yakalandı; giriş alanı ve liste
ekranı orada yok.* **Çözüm ucuz ve kâğıt turu DEĞİL:** gerçek `GorevListesiEkrani`'nde **tek**
yakalama + **ham ağacın** yazılması (R2b/DEVRET ile uyumlu — koşan koda gider).

### Diğer dört bulgu
- 🟡 **`radar --olc-urun-kodu` çalışma ağacını GÖRMEZ.** R8 sert durağı bu oturumda **fiilen
  yandı** ve **yanlış-pozitifti**: kayıtlara `urun_kodu_satiri=0` yazan bendim, çünkü commit
  tabanlı ölçtüm. Gerçek ölçüm (çalışma ağacı): **61 satır** ürün kodu
  (`cakisma_rozeti +40`, `veritabani +10`, `gorev_satiri +7`, `hata_durumu +2`, `bos_durum +1`,
  `yukleme_durumu +1`). Düzeltme kaydı yazıldı, **R8 sustu**.
  *Ders: bir kural yanlış yandığında önce **kuralı değil veriyi** sorgula.*
- 🟡 **`04-G3/gercek-tarama.txt` 1,9 MB.** Portfolyo reposuna 2 MB ham JSON commit'lemek kanıt
  değil **yüktür**; ilgili kesit + sha yeterdi.
- 🟡 **M2b beyanının TERSİ ölçüldü.** Spec/DESIGN A‑4 *"çok satırlı `/* */` içindeki literal
  KAÇABİLİR"* diyordu; kapı onu **yakaladı** ⇒ `yorum_disi()` yorumu soymuyor, **yorum içindeki
  literali de kod sayıyor** (kaçırma değil, **yanlış-pozitif** yönü). `M2a`/`M2b` kanıtları
  bayt-özdeş **değil** (satır 68 vs 69) ⇒ mutant gerçekten farklı uygulanmış.
- 🟡 **Ortam:** `flutter test` Desktop Commander kabuğunda `%PROGRAMFILES(X86)% environment
  variable not found` ile çöküyor (değişken `os.environ`'da **yok**, dizin diskte **var**).
  Alt sürece enjekte edilince **36/36** geçti. Ürün kusuru değil, **ortam** kusuru; §7'ye yazıldı.

### 🔻 COWORK KENDİ YANLIŞ-POZİTİFİNİ GERİ ÇEKTİ
İlk taramada *"`a11y_kapisi_test.dart` `@TestOn` taşıyor ⇒ G5'in web ayağı sessizce kapatılmış"*
diye şüphelendim. Dosyayı açınca `@TestOn`'un **yalnızca yorumda** geçtiği görüldü; gerçek
`@TestOn('vm')` sadece `a11y_kontrast` ve `a11y_statik_tasma` dosyalarında — **spec'in tam
istediği gibi**. **Bulgu geri çekildi.** *Denetçi de yanılır; yanıldığını yazmayan denetçi
denetçi değildir.*

### Açık kalan
`flutter test --platform chrome` **üçüncü bağımsız ölçümde de** (360 sn önceden yazılı sınırla)
sonuç vermedi — önceki ikisi 7 dk ve 9,8 dk. Web a11y ayağı **`[DOĞRULANMADI]`**, gizlenmiyor.
Spec'in tek kilit turu (kriter 6/7'ye araç adı, `4/4 → 6/6`) **Cowork'ün işidir** ve
`T9-KAPANISI` borcu olarak muafiyette gerekçesiyle duruyor.

## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31: **K58 — TAVAN 12→32 KB + ARŞİV DİZİNİ**)

> ⚠ **DÜZELTME NOTU (yerinde düzeltme yok):** bu dosyanın tepesindeki blokta geçen
> *"Canlı durum `DURUM.md`'dedir (≤12 KB)"* ifadesi **BAYATTIR**. Tavan **K58 ile 32 KB**
> oldu. Satır silinmedi; bu not onu geçersiz kılar.

### Karar: `DURUM.md` tavanı **12 KB → 32 KB** [Onur kilitledi]
**Onur'un sorusu haklıydı ama gerekçesi yanlıştı:** tavanın sebebi okuma kapasitesi değil.
Ölçüldü — `DURUM.md` 12.287 b ≈ **3,5k token**; Cowork açılışta toplam **6,3k**, Claude Code
(spec + `DESIGN.md` dahil) **22,9k** token okuyor. Tek okuma sınırının kat kat altı.

**Tavanın iki GERÇEK gerekçesi:**
1. **R4 freni.** Bu projede ölçüldü: `docs/ADR/0003` dokuz turda **120 KB → 300 KB** büyüdü ve
   her büyüme yeni çapraz-atıf kusuru doğurdu. Tavan, *herkesin her oturum okuduğu tek belgeye*
   konmuş bir büyüme frenidir.
2. **Dikkat, kapasite değil.** 3,5k token'lık belge **okunur**; 40k token'lık belge *göz
   gezdirilir* — ve göz gezdirilen belgede bayat iddia **hayatta kalır**. Bu oturumda tam o
   sınıfı **dört kez** yakaladık.

**Gevşetmenin ölçülmüş gerekçesi:** bayat-atıf sınıfı artık **mekanikleşti**
(`sayi-tazeligi.py` · `dosya-kimlik.py` · defter `D1`–`D5` · `spec-kapi-kapsama.py`), yani R4'ün
dayandığı varsayım zayıfladı. **32 KB seçildi, 150 KB değil:** 150 KB'da `DURUM.md` ikinci bir
`PROJE_HAFIZA.md`'ye dönüşür ve K53'ün böldüğü şey geri birleşir. 32 KB ≈ 2,6× pay ≈ 15–20 oturum.

🔴 **BEYAN EDİLMİŞ ZAYIF KONTROL (gizlenmiyor):** bu tavanı **hiçbir kapı zorlamıyor.** Oturum
30'da elle **sekiz kez** uygulandı ve dosya **12.287 / 12.288 bayta** kadar sıkıştı — yani
**1 bayt** pay. Yalnız bir elin hatırladığı sınır **kör kapıdır**. Onur'un bilinçli kararı: 32
KB'da tavan uzun süre ısırmayacağı için **şimdi araç yazılmadı** (K53: daha fazla denetim aygıtı
üretme); **ilk ısırdığında `belge-tavan-kapisi.py` yazılır.** Ölçüm zaten her checkpoint'te
`dosya-kimlik.py` çıktısında görünüyor.

### `araclar/hafiza-dizin.py` **1.0.0** — arşiv artık gezilebilir
**Ölçülmüş sorun:** bu dosya **520.376 bayt (~149k token) · 71 checkpoint** ve oturum başına
~10 KB büyüyor. Erişim yolu *"bir kararın gerekçesini merak edersen aç"* idi — bu boyutta
**fiilen imkânsız**.

**Çözüm:** başa `<!-- DIZIN:BAS -->…<!-- DIZIN:SON -->` bloğu — checkpoint başlıklarından
**mekanik olarak** üretilen `satır · tür · oturum · kilit · konu` tablosu.
**Append-only ihlali DEĞİLDİR:** dizin bir *kayıt* değil, kayıtlardan **türetilmiş veridir** ve
her koşumda sıfırdan yeniden üretilir. *Elle tutulan dizin bayatlar; türetilen bayatlamaz.*
**Yeni kural: yeni checkpoint `<!-- DIZIN:SON -->` satırının ALTINA eklenir.**

🔴 **ALTIN KÜME İLK KOŞUMDA ARACIN KENDİSİNİ YAKALADI — 520 KB'lik arşive dokunmadan önce.**
Vaka 5 (*"üçüncü koşum da aynı olmalı"*) **KALDI**: araç **idempotent değildi**, her koşumda
dosyaya **iki boş satır** ekliyordu — yani arşivi her koşumda şişirecekti. Vaka 4 (yalnız kayıt
sayısını ölçüyordu) bunu **göremedi**; kusuru gören şey **eşitlik ölçen** vakaydı.
*Ders: "aynı sonucu verdi mi?" diye soran vaka, "kaç kayıt buldu?" diye soran vakadan güçlüdür.*
Düzeltildi (`_temiz_govde` normalizasyonu) ⇒ **7/7 GEÇTİ, EXIT 0.**

**Gerçek dosyada doğrulandı:** koşum 1 → 520.376 → 530.114 b; koşum 2 ve 3 → **sha DEĞİŞMEDİ**
(`90a524c0…`), *"DEGISIKLIK YOK"*. Araç kendi çıktısını da doğruluyor: her dizin satırının
gerçekten bir `CHECKPOINT` başlığına denk düştüğü ölçülüyor (kör kapı yok).

**Beyan edilmiş sınır (çıktıya basılır):** yalnız **başlıklar** indekslenir, gövde metni
**aranmaz** (tam metin için `grep`/`ripgrep`). Satır numaraları dosyaya **elle** satır eklenirse
bayatlar — araç yeniden koşulur.

## ⏭ CHECKPOINT (27 Tem 2026 — oturum 31: **G3 ADJUDİKASYONU + TAŞIMA/T8 BAĞIMSIZ DOĞRULAMASI**)

### 1) G3 lisans bulgusu — **GERÇEK İHLAL YOK**, kusur pana'nın etiketinde
Claude Code T8'de G3'ü koştu, `sqlcipher_flutter_libs 0.7.0+eol` için üç SPDX etiketi çıktı
(`Pixar`, `MIT`, `BSD-3-Clause-HP`), ikisi izinli kümede olmadığı için **KIRMIZI** yandı ve
builder **karar veremeyeceğini söyleyip DURDU** — bu doğru davranıştır, spec'in üretmek için
tasarlandığı şeydir.

**Cowork beyana güvenmedi, iki bağımsız uçtan ölçtü:** pub.dev `/api/metrics` (Z14) **ve** pub
cache'teki gerçek `LICENSE` (12.929 b, `sha256 1052710c…`).

| bölüm | metin | pana | gerçek |
|---|---|---|---|
| satır 3 | MIT — Simon Binder | `MIT` | ✅ izinli |
| satır 29 | **Apache License 2.0** (apache.org/licenses, satır 204 `END OF TERMS`) | `Pixar` | ✅ izinli |
| satır 209 | **BSD-3-Clause** — Zetetic LLC / SQLCipher, üç madde | `BSD-3-Clause-HP` | ✅ izinli |

🔴 **ÇÜRÜTÜCÜ ÖLÇÜM:** dosyada `"Pixar"` dizgesi **YOK** · `"Modified Apache"` dizgesi **YOK**.
Pana'nın etiketinin metinde **karşılığı yoktur** — SPDX `Pixar` zaten *"Modified Apache 2.0
License"*tır ve şablon eşleştirici düz Apache-2.0'ı ona oturtmuş; aynı şekilde düz BSD-3'ü HP
varyantına (HP'nin patent feragat cümlesi metinde geçmiyor). **Kusur pakette değil, etikette.**
Kardeş paket `sqlite3_flutter_libs 0.6.0+eol` ayrıca ölçüldü: **yalnız MIT**, temiz.

Bu, spec G3'ün **zaten yazılı** beyan edilmiş sınırının gerçekleşmesidir: *"pana'nın lisans
tespiti kusursuz değildir."* **Kapı yanlış çalışmadı — beyan ettiği sınıra çarptı.**

**İki şık gerekçesiyle REDDEDİLDİ:** ① SPDX kimliğini beyaz listeye almak = **yanlış etiketi
doğru kabul etmek**; yarın gerçekten Pixar lisanslı bir paket gelse kapı **kör** kalırdı.
② "Açık borç / KIRMIZI bırak" = **olmayan bir ihlali ilan etmek**; G3 raporu değerlendiriciye
*"izinsiz lisans var"* diye giderdi. **SEÇİLEN:** metin-kanıtlı eşleşme
(`araclar/lisans-eslesme.json`) — eşleşme **veridir**, kanıt (dosya+satır+ölçüm) **zorunludur**,
kapı her eşleşmeyi `ESLENDI [kanıt]` diye **basar**, **kanıtsız eşleşme KIRMIZI'dır**.
G3 altın kümesi bu yüzden **4 → 6** çıkacak. Tam ölçüm: `KANIT/slice-3b/04-G3/COWORK-BAGIMSIZ-LISANS-OLCUMU.txt`.

### 2) Taşıma kapısı — **GEÇTİ**, builder'ın özetine değil ham çıktıya bakıldı
- **K56 kırmızı çizgisi:** repo genelinde tüm `.properties`/`.gradle`/`.kts` tarandı ⇒
  `android.overridePathCheck` **hiçbir yerde YOK**. Bayrak sızmamış.
- `TASIMA-01`: `No issues found! (ran in 22.3s)` · `"error"` dizgesi **yok**.
- `TASIMA-02`: `+20: All tests passed!` · `"failed"` dizgesi **yok**.
- `TASIMA-03`: `Built build\app\outputs\flutter-apk\app-debug.apk` · `BUILD FAILED` **yok**.
- Kilitler sağlam: `DESIGN.md 534DFF68` (K46) · spec `6056A5BB` (K57) · `radar.py 46E3A8BC`.

### 3) T8 araçları — altın kümelerini **ben** koştum
`pub-cve-kapisi.py` **8/8** · `pub-lisans-kapisi.py` **4/4** · `design-token-kapisi.py` **18/18**
(12'den çıkmış) · `spec-kapi-kapsama` **13/13** · `radar` **18/18** · `sayi-tazeligi` **16/16**.
Hepsi **EXIT 0**. Builder'ın sayısına güvenilmedi, hepsi yeniden koşuldu.

### 4) `sayi-tazeligi.py` **dördüncü kez** ısırdı
`DURUM.md` §6'daki *"design-token-kapisi.py 12/12"*, araç T8'de **18** vakaya çıktığı için
bayatlamıştı. Bunu ne bir göz ne bir denetim turu yakalardı — kapı **araç değişir değişmez**
yakaladı. Düzeltildi ve iki yeni araç satırı (8/8, 4/4) tabloya eklendi; `T8-BEKLIYOR`
muafiyetleri **silindi**, o sayılar artık **her koşumda gerçekten koşularak** doğrulanıyor.

### 5) Yeni açık borç — gizlenmiyor
Spec kriter **6** ve **7** (*"G2 altın küme 8/8"*, *"G3 altın küme 4/4"*) satırında **araç adı
yok** ⇒ kapı mekanik bağlayamıyor. Sayılar **bugün doğru** (ölçtüm) ama **ÖLÇÜLMEDİ** sayılıyor.
Düzeltme spec'i değiştirir; spec kilitli ve **Claude Code şu an onu okuyarak T9'da çalışıyor** —
ortasında değiştirmek builder'ı bayat kopyayla bırakır (oturum 29'un `dee6dbc` dersi).
**T9 kapanışındaki TEK kilit turuna ertelendi** (borç: `T9-KAPANISI`).

### 6) İki ölçülmüş sınır — beyan ediliyor
- **R8 yalnız COMMIT'LENMİŞ kodu sayar.** `604c041..HEAD` ⇒ 0; ama Claude Code'un
  `cakisma_rozeti.dart` üzerindeki **ürün kodu** değişikliği o an commit'lenmemişti ve bu sayıya
  **girmiyor**. Oturum kapanırken commit'lenmemiş iş varsa R8 **düşük** ölçer.
- **`flutter analyze`/`test`'i bu oturumda yeniden KOŞMADIM.** Claude Code repoyu aktif
  kullanıyordu; eş zamanlı koşum onun işini bozardı. Ham çıktı dosyalarından doğruladım —
  bu bir **belge doğrulamasıdır, yeniden koşum değildir.**

### 7) 🔴 YAPISAL UYARI — `DURUM.md` tavana dayandı
Bu oturumda 12 KB tavanının altına inmek için **sekiz kez** budama yapıldı ve dosya şu an
**12.287 / 12.288 bayt** — yani **1 bayt** pay. Budama artık kendisi *tekrar eden bir sınıf*
(R1 eşiğinin çok üstünde) ve her turda bilgi kaybettiriyor. **Bu bir karar noktasıdır ve Onur'a
sunulmuştur:** ya tavan yükseltilir, ya §7/§8 yapısal olarak bölünür, ya da bir *"tavan bekçisi"*
kapısı yazılıp budama mekanikleştirilir. **Kendiliğinden karar verilmedi.**

## ⏭ CHECKPOINT-EK (26 Tem 2026 — oturum 30: **K57 KESİNLEŞTİ + `sayi-tazeligi.py` DOĞDU**)

> Bu blok, hemen altındaki **K57 checkpoint'ini GÜNCELLER**. Orada verilen kilit kimliği
> **`42.043 b · 79A53AA3` ARTIK GEÇERSİZDİR** (yerinde düzeltme yok; bu not onu geçersiz kılar).
> **KESİN K57 KİMLİĞİ: `42.395 bayt · 6056A5BB` · U+FFFD 0 · CRLF 0.**

### Ne oldu — oturumun en öğretici anı
Onur `MEKANİKLEŞTİR` şıkkını kilitledi ve `araclar/sayi-tazeligi.py` yazıldı: belgelerdeki
*"altın küme N/M"* iddialarını **aracı fiilen koşup** `[GECTI]`/`[KALDI]` satırlarını sayarak
doğrulayan statik kapı. K44-a gereği **önce kendini kanıtladı: 16/16, EXIT 0.**

🔴 **Kapı ilk gerçek koşumunda KENDİ YAZARINI yakaladı.** Bir saat önce **K57 ile kilitlenen**
spec'in **kabul kriteri 12b**'sinde `9/9` buldu — o satırı aynı oturumda **Cowork yazmıştı**
(tarihçe olarak, iyi niyetle). Yani **dokuz bayat çapraz-atıfı kesen el, onuncusunu kendisi üretti**
ve onu bir denetim turu değil **bir araç** yakaladı. Deftere `uretilen: 1` diye yazıldı; gizlenmedi.

**Kapı gevşetilmedi, belge düzeltildi** — K56'nın doktrini (*kapı susturmak yerine kök neden
kaldırılır*) ikinci kez uygulandı. 12b'den tarihçe sayısı çıkarıldı ve **yeni kabul kriteri 12c**
yazıldı: *"`sayi-tazeligi.py --altin-kume` EXIT 0 (16/16) ve aynı araç spec + `DURUM.md` +
`DESIGN.md` üzerinde EXIT 0."* Böylece bu kusur sınıfı artık **spec'in kendi kabul ölçütüne**
bağlı; bir daha göze değil **kapıya** takılır.

### Araç 1.0.0 → 1.1.0: ölçülen üç kör nokta, üçü de kapatıldı
- **(a) TABLO MODU.** `DURUM.md` §6 araç tablosunda imza **satırda değil TABLO BAŞLIĞINDA**;
  1.0.0 o tablonun tamamını **hiç görmüyordu** — yani projenin *her aracının* altın küme sayısını
  ilan ettiği tablo kapının kör noktasındaydı.
- **(b) `T5` BAĞLANAMADI.** Satırda araç adı yoksa iddia mekanik olarak bağlanamaz. `DESIGN.md`'nin
  **bilinen bayat `10/10`**'u (BD‑6) tam bu yoldan kaçıyordu. Artık SARI yanar: *"ölçülmedi, temiz değil."*
- **(c) MUAFİYET KAYDI.** K46 gibi **düzeltilemez** belgeler için `araclar/tazelik-muafiyet.json`.
  Muafiyet **sessiz değildir** (`T6` ile her koşumda gerekçesiyle basılır) ve **gerekçesiz muafiyet
  KIRMIZI'dır** (`T7`) — `spec-kapi-kapsama.py`'nin `S4` kuralıyla aynı felsefe: *beyan etmek,
  gerekçe vermek demektir.*

### Aracın KENDİ yanlış-pozitifi — gizlenmedi, uydurma eşikle de kapatılmadı
Uzun paragraf satırlarında **imza ile sayı arasındaki yakınlık ölçülmüyor**; spec §10'un BD bloğu
tek satır olduğu için **BD‑5'in `20/28` tipografi oranı** "altın küme" sanıldı. Bunu bir *"80 karakter
yakınlık"* sezgisiyle susturmak yerine — ki bu **altın küme vakası olmayan bir eşik** olurdu ve K40'ı
ihlal ederdi — `ARAC-YANLIS-POZITIFI` borcuyla **gerekçeli muafiyet** açıldı ve `DURUM.md` §8'e yazıldı.

### Gerçek koşum hükmü
`EXIT 0 · TEMİZ`. Beş muafiyet (2 × **BD‑6** K46 dondurması · 2 × **T8-BEKLIYOR** henüz yazılmamış
kapı araçları · 1 × **ARAC-YANLIS-POZITIFI**) ve bir alıntı (`T4`) **görünür** olarak basıldı.

### Doğrulama zinciri (üreten ≠ denetleyen)
`sayi-tazeligi --altin-kume` **16/16 EXIT 0** · gerçek koşum **EXIT 0** ·
`spec-kapi-kapsama --altin-kume` **13/13 EXIT 0** ve spec üzerinde **EXIT 0** (yeni kriter kapsamayı
**bozmadı**: 7 kapı · 16 kural · 24 mutant) · `radar --altin-kume` **18/18 EXIT 0** ·
**`DESIGN.md` `534DFF68` DEĞİŞMEDİ** (K46) · `DURUM.md` **12 KB tavanının altında**.

> **DERS (bu oturumun en pahalı cümlesi):** *"Kilitledim" demek "doğruladım" demek DEĞİLDİR.*
> Bir kilit, o an **elindeki kapıların gördüğü kadarını** dondurur. Yeni bir kapı doğduğunda
> kilit **yeniden ölçülür** — ve bu bir başarısızlık değil, kapının çalıştığının kanıtıdır.

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 30: **K57 — BAYAT ÇAPRAZ-ATIF KESİMİ; ARAÇ SENKRONU**)

> ⚠ **DÜZELTME NOTU (yerinde düzeltme YOK, append-only):** bu dosyanın en tepesindeki *"Kanonik konum:
> `C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum`"* satırı **BAYATTIR**. Kanonik kök
> **K56 ile `C:\dev\Momentum`** oldu (oturum 30'da doğrulandı: `C:\momentum` junction'ı **YOK**, ağaç temiz,
> `.git/index.lock` **YOK**). Satır silinmedi; bu not onu geçersiz kılar.

**OTURUM 30'UN TEK CÜMLELİK ÖZETİ:** Üç açık borç kapandı, **dokuz bayat çapraz-atıf** kesildi — ve
**hiçbirini bir denetim turu bulmadı: iki araç koşumu ve bir regex taraması buldu.**

### 1) AÇILIŞ ÖLÇÜMÜ
`radar.py --altin-kume` **11/11 EXIT 0** (senkron öncesi) · `radar.py .` **KIRMIZI, EXIT 2** ·
`git status --porcelain` **boş** · `origin/main...HEAD` = **0 0** · `.git/index.lock` **YOK** ·
`C:\momentum` **YOK** · son commit `8c97309`.
**K53 kural 2 gereği YAZILI GEREKÇE (radar KIRMIZI'da neden çalışıldı):** verilen üç işin **hiçbiri
"yeni tur" değildi** — (a) araçtaki **ad çakışması** düzeltmesi, (b) **MEKANİKLEŞTİR** (D1–D5 statik
kontrollerdir, koşan kod istemez ⇒ şık geçerli), (c) **DEVRET'in geri dönüşü** (build koştu, ölçüm
üretti, kâğıt ölçüme uyduruldu). Kâğıt denetim turu tavanı (1) **tüketilmedi**.

### 2) BORÇ (a)+(b) — `araclar/radar.py` PLUGIN 0.2.0 İLE **BAYT-ÖZDEŞ** [K57-b]
Proje kopyası (21.257 b · 455 satır) plugin kopyasıyla (28.878 b · 609 satır) **mekanik olarak
diff'lendi**; proje-yerel 59 satırın tamamı ya `R7` adlandırması ya da *"plugin bu düzeltmeleri
TAŞIMIYOR, kanonik sürüm PROJE İÇİNDEDİR"* diyen — **artık YANLIŞ olan** — bir yorum bloğuydu.
Plugin **kesin üst kümedir** ⇒ üzerine yazıldı.

**🔴 TASARIM KARARI — PROJE-YEREL NOT EKLENMEDİ.** Dosya bilerek **bayt-özdeş** tutuldu
(`sha256 46E3A8BC`, 28.878 b, U+FFFD 0, CRLF 0). *Gerekçe: `kanonik-kopya` bu defterde **tekrar eden**
bir kusur sınıfı; bayt-özdeşlik onu **tek bir sha karşılaştırmasına** indirger. Bir "sadece bizde olan
küçük not" eklemek, sapmayı yeniden **göze** bağımlı kılardı.*

- **(a) `R7` → `R8`.** Ölçülmüş gerekçe: plugin doktrininde (`doktrin.md` §R7) `R7` zaten **kapı
  granülerliği** kuralıdır ve **`radar.py` onu ÖLÇMEZ** (insan denetimi). Aynı kodu iki farklı kavramın
  taşıması, hükmü okuyan eli yanıltıyordu.
- **(b) 0.2.0'ın tamamı geldi:** **defter dürüstlük kapısı D1–D5** · **`--olc-urun-kodu`** (sayıyı
  `git diff --numstat`'tan türetir — *kendi karnesini kimse dolduramasın*) · **`radar.config.json`**
  okuma desteği · **altın küme 11 → 18 vaka**.
- **KANIT:** `python araclar\radar.py --altin-kume` ⇒ **18/18 GEÇTİ, EXIT 0** (kör kapı yok).

**🔴 KAPI İLK KOŞUMDA ISIRDI — İKİ GERÇEK DEFTER KUSURU:**
`D3` → kayıt 7 (`docs/ADR/0003` tur 8) zorunlu alanları **eksik** (`bulgu`, `kapatilan`, `uretilen`).
`D2` → aynı artefaktın defterinde **tur 1 ATLANMIŞ**. Bunlar **yeni üretilmiş kusur değil**, 19 kayıttır
görülmeyen kusurun **görünür olmasıdır** (bu yüzden `uretilen: 0`, `bulgu.minor: 2`). Defter append-only
⇒ eski satırlar **düzeltilmedi**; borç `DURUM.md` §8'e yazıldı.

**BEYAN EDİLMİŞ SINIR — `D1` BU DEFTERDE FİİLEN KÖRDÜR.** `D1` (bayt beyanı ↔ disk) yalnız **yol
benzeri** artefakt adlarında koşar; bu defterdeki adların çoğu (`slice-3b-istemci`,
`GOREV-slice-3b-spec`, `K53-verimlilik-reformu`) **etikettir, yol değildir**. Bu yüzden oturum 30'un
kaydı bilerek **`araclar/radar.py` gerçek yoluyla** yazıldı: kaydı yazan el, kendi bayt beyanını
**diske karşı doğrulatmak zorunda** kaldı.

**`radar.config.json` OLUŞTURULMADI — bu bir eksiklik değil, ölçülmüş bir karardır.** Varsayılan ürün
yolları `['src/','lib/','app/']` ve hariç `['test/','tests/','docs/','scripts/','tools/','araclar/']` bu
repoya **birebir** uyuyor (backend `src/`, Flutter `src/client/lib/`, araçlar `araclar/`). Eşikleri
değiştirmeden config yazmak **bayt ekler, ölçüm eklemez**; K40 zaten eşik değiştiren elden **altın kümeye
vaka eklemesini** ister.

**ÜRÜN KODU — ELLE YAZILMADI, GİT'TEN TÜRETİLDİ:**
`python araclar\radar.py --olc-urun-kodu 8c97309 .` ⇒ **`urun_kodu_satiri = 0`**.
Bu checkpoint anında oturum 30 **araç ve belge** işi yaptı, **ürün kodu yazmadı** — ve bu **dürüstçe**
yazılıyor. `R8` şu an **susuyor**, çünkü oturum 29 = **135 satır** (Claude Code'un `flutter create`
çıktısı). **Oturum 30 kapanışında hâlâ 0 ise `R8` SERT DURAĞI YANACAK ve bu DOĞRU olacaktır.**

### 3) BORÇ (c) — SPEC **v4 → v5**, KİLİT **K54 → K57**
**Onur'un verdiği kapsam iki kalemdi (T2 + Z10); mekanik tarama YEDİ tane daha buldu.** Kapsam kararı
**Onur'a soruldu** (K40: kilit Onur'dan gelir) ve *"①–⑨ hepsi, tek kilitte"* seçildi.
**Yeni kilitli kimlik: `42.043 bayt · 79A53AA3` · U+FFFD 0 · CRLF 0.**
**`BE4581BA` (K54) ve `1AB02B73` (K52) ARTIK GEÇERSİZDİR.**

| # | kalem | nasıl bulundu | ne yapıldı |
|---|---|---|---|
| ① | `build_runner ^2.15.2` bu SDK'yla **çözülemiyor** | Claude Code'un `pub get` koşumu (`T2-SAPMA.txt`) | T2 → **`^2.15.1`**, "yükseltilemez" uyarısıyla |
| ② | *sürüm ölçümü ≠ çözümlenebilirlik ölçümü* | aynı kanıt | **Z10b** olarak yazıldı (aşağıda) |
| ③ | **T0 tuzağı** | `pub-surum-olc.py` koşumu | "en güncel ≠ pinli" ayrımı; `build_runner` için ölçüt **`pub get` EXIT 0** |
| ④ | kriter 14 aynı tuzak | aynı | beş paket eşleşir, `build_runner` **ayrık** |
| ⑤ | `spec-kapi-kapsama.py` **"9/9"** | aracın koşumu: **13/13** | ⑤ iki yerde `13/13` |
| ⑥ | §2 ve T9 **"yirmi bir mutant"**, §6 **"yirmi üç"** | aracın saydığı: **24 etiket** | ikisi de **yirmi üç (24 etiket)** |
| ⑦ | başlık *"v3 kilitli"*, belge v4 | okuma + tarama | **v5 kilitli [K57]** |
| ⑧ | Z16/B‑6 **"7 dakika"** | oturum 29'un ikinci ölçümü: **589 sn** | **iki ölçüm de** yazıldı (Onur'un seçimi) |
| ⑨ | §10 borcu *"radar.py R3 asgari örneklem"* | bugün kapandı | listeden **düşürüldü** |

**🔴 ③ OLMASAYDI BUILD DURURDU — bu bir tahmin değil, ölçüm:** T0 *"`pub-surum-olc.py` çıktısı Z10 ile
eşleşmeli, eşleşmezse **DUR ve raporla**"* diyordu. Araç bugün **`build_runner 2.15.2`** basıyor
(ölçüldü). Z10'u tek başına `2.15.1` yapmak, **kilidi düzeltirken kapıyı kilitlemek** olurdu.
Sadece ① + ② yapılsaydı ortaya **kendi kendini durduran bir spec** çıkacaktı.

**Z10b'nin özü (spec'e yazıldı):** `build_runner ≥2.15.2` → `analyzer ≥13.3.0` → `meta ^1.18.3`;
Flutter 3.44.6'nın `flutter_test`'i `meta`'yı **tam 1.18.0**'a sabitliyor ⇒ çakışma **yapısaldır,
geçici değildir**. Çözülen küme: `build_runner 2.15.1` → `analyzer 13.0.0` → `meta 1.18.0`.
⇒ **KURAL: bir sürüm pinlenmeden önce `pub get` FİİLEN koşulur.** Bir sayının pub.dev'de var olması,
onun **bu ağaçta çözüldüğü anlamına gelmez.** Bu, `pub-surum-olc.py`'nin **beyan edilmiş sınırı** ve
`DURUM.md` §8'de **açık borçtur**.

**DOĞRULAMA (üreten ≠ denetleyen — kendi işimi kendim onaylamadım, araçlar onayladı):**
`spec-kapi-kapsama.py --altin-kume` ⇒ **13/13, EXIT 0** · aynı araç yeni spec üzerinde ⇒ **EXIT 0**,
bulgu yok (**7 kapı · 16 kural · 24 mutant etiketi**) · `dosya-kimlik.py` ⇒ **42.043 b · `79A53AA3` ·
U+FFFD 0 · CRLF 0** · **`DESIGN.md` `534DFF68` DEĞİŞMEDİ** (K46 korundu) · `radar.py --altin-kume` ⇒
**18/18 EXIT 0** · `git status` temiz, `.git/index.lock` **YOK** · gerçek `pubspec.yaml` ölçüldü:
**`build_runner: ^2.15.1`** — yani spec artık **gerçekle uyuşuyor**, öncesinde **uyuşmuyordu**.

### 4) OTURUM 30'UN ASIL DERSİ (K53'ün doğrulanması)
Bu spec **dört bağımsız denetim turundan** geçmişti (üç denetçi → red-team → `spec-kapi-kapsama` →
kilit). O dört tur bu dokuz kusurun **hiçbirini** bulmadı. Dokuzunu da **iki araç koşumu ve bir regex
taraması** buldu — dakikalar içinde. **Bayat sayı, prozayı okuyan göze görünmez; ölçen betiğe görünür.**
Bu, K53'ün *"kâğıt denetim turu tavanı 1"* kuralının **beşinci bağımsız kanıtıdır** ve doğrudan bir
öneriye işaret ediyor (Onur'un kilidine sunuldu, kendiliğinden yazılmadı): **`sayi-tazeligi.py`** —
belgelerdeki *"altın küme N/M"* ve sürüm iddialarını **aracı koşup** doğrulayan statik kapı. Bu sınıf
(`kanonik-kopya`) `DESIGN.md` BD‑6'da, bu spec'te ⑤'te ve `DURUM.md` §6'da **üç kez** tekrarladı ⇒ R1'in
mekanikleştirme eşiğinin **üstündedir** ve **koşan kod olmadan ölçülebilir** (K53 kural 2'nin aradığı
yazılı gerekçe budur).

### 5) OTURUM SAĞLIĞI [K21] — **ÖLÇÜLEMEDİ, VARSAYILMADI**
Bu oturum **Cowork bulut sandbox'ında** koştu; K21'in ölçüm kaynağı `~/.claude/projects/*/<oturum-id>.jsonl`
**Claude Code'un yerel dosyasıdır ve bu makinede yoktur.** **Ne yeşil ne kırmızı varsayıldı** — ölçülemediği
Onur'a **açıkça bildirildi**. (Alternatif ayak: Cowork'ün `explain-usage` skill'i; Onur'un onayına sunuldu.)

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K56 — REPO ASCII KÖKE TAŞINIYOR; KAPI SUSTURULMADI**)

**🔴 K56 — KİLİT (Onur, 26 Tem 2026): kanonik kök `C:\dev\Momentum`. `android.overridePathCheck` EKLENMEYECEK; junction KALDIRILACAK.**

**AYNI KÖK NEDENİN DÖRDÜNCÜ ÖLÇÜLMÜŞ KUSURU.** Claude Code üç ayrı turda **DUR ve sor** dedi (spec'in tam olarak üretmesi için tasarlandığı davranış) ve üçü de gerçekti. Türkçe karakterli yol (`Ö`/`İ`) şunları kırdı:
① `dart run build_runner build` → *"package_config.json did not contain its own root package"* · ② `flutter analyze` → LSP JSON‑RPC çerçeveleme çöküşü (çok baytlı UTF‑8 ↔ `Content-Length`) · ③ **Android Gradle Plugin** yol denetimi · ④ `.ps1` Türkçe yol literali (bilinen eski kusur).
**İZOLASYON KANITI (Claude Code ölçtü):** boşluklu **ASCII** yolda EXIT=0 · aynı projeye sadece **"Ö"** eklenince EXIT=255 ⇒ **boşluk suçsuz, Türkçe karakter suçlu**.
**JUNCTION ÇÖZMEDİ VE BUNUN SEBEBİ DE ÖLÇÜLDÜ:** Dart/Flutter zinciri junction'ı şeffaf geçiyor, ama **JVM reparse point'i gerçek yola çözüyor** (`File.getCanonicalPath()`) ⇒ AGP hata mesajında **junction'ı değil gerçek Türkçe yolu** gösterdi. Karşıt kanıt: `flutter build web` aynı junction'dan **EXIT=0** (JVM yok).

**🔴 REDDEDİLEN ÇÖZÜM VE GEREKÇESİ [bu, K53'ün doğrudan uygulanmasıdır]:** AGP'nin kendi resmî kaçış yolu `android.overridePathCheck=true` **EKLENMEDİ**. Üç gerekçe: (a) **bir kapıyı susturur, sebebi çözmez** — AGP o kontrolü keyfi koymadı, aapt2/NDK'nın ASCII-dışı yollarda gerçek kırılma geçmişi var; risk **ölçülmeden ertelenirdi**. (b) Junction repo **DIŞINDAYDI**, bu bayrak **`gradle.properties` ile REPOYA commit'lenirdi** ⇒ işe-alım denetçisi *"bu proje neden Android'in yol kontrolünü devre dışı bırakmış?"* diye sorardı — portfolyo kokusu. (c) Elde **üç veri noktası** vardı: geçici çözüm biriktikçe her turda yeni bir sürpriz çıkıyordu.
*Reddedilen diğer şık: Android'i erteleyip web ile devam (F1 kilidi Android'de 3 gerçek ayak istiyor ve ekran görüntüsü web'de fiziksel olarak yok — Z6 ⇒ G1 yarım kalırdı).*

**✅ TAŞIMADAN ÖNCE TEMİZ DURUŞ ÖLÇÜLDÜ:** son commit **`3f043ca`** · `git status --porcelain` **BOŞ** · `.git/index.lock` **YOK** · junction `C:\momentum` (kaldırılacak) · silinecek önbellekler `src/client/.dart_tool` ve `src/client/build` (mutlak yol saklıyorlar).

**✅ İSTEMCİ DURUMU (taşımadan önce):** **T0 → T7 BİTTİ.** `tokens.dart` (32 sembol, `Brightness` tabanlı çözüm) · `veritabani.dart` + `gorev_deposu.dart` (F4 dikişi, F8 saat/kimlik dikişi, UUID v4 **paketsiz**, `senkronDurumu` CHECK kısıtı) · 8 görsel bileşen + `MomentumTema` · durum vitrini (8 durum `ByValueKey` ile) · `main.dart` gerçek uygulamaya bağlı (F7 bayrak korumalı). **`flutter analyze` 0 bulgu · `flutter test` 20/20 yeşil · G7'nin 6 maddesi test edildi · üretilen `.g.dart`'ta 0 mutlak yol** (taşınabilirlik ölçüldü — Onur'un şartıydı).

**✅ T2 SAPMASI ÖLÇÜLDÜ VE KAYDA GEÇTİ:** `build_runner ^2.15.2` bu Flutter SDK'sıyla **çözülemiyor** (2.15.2 → `analyzer >=13.3.0` → `meta ^1.18.3`, ama `flutter_test` `meta`'yı **1.18.0**'a pinliyor). `^2.15.1` ile çözüldü ve ölçüldü: `analyzer 13.0.0` · `meta 1.18.0`. Ham kanıt `KANIT/slice-3b/T2-SAPMA.txt`. **Spec'e DOKUNULMADI** (`BE4581BA` sabit). ⇒ **Z10'un ölçüm boşluğu adlandırıldı: sürüm ölçümü ≠ ÇÖZÜMLENEBİLİRLİK ölçümü.** `pub-surum-olc.py`'ye çözümlenebilirlik ayağı eklenmeli (borç).

**AÇIK BORÇLAR (üçü de `DURUM.md` §8'de):** `radar.py` proje kopyasında **`R7` → `R8`** ad çakışması (plugin doktrininde R7 zaten "kapı granülerliği") · plugin 0.2.0'ın yeni yetenekleri proje kopyasına geri taşınmalı · spec T2/Z10 kilit düzeltmesi.

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K53 VERİMLİLİK REFORMU + K54 SPEC v4 (KİLİT GÜNCELLEMESİ)**)

**🔴 TEŞHİS (Onur'un talebiyle masaya yatırıldı):** 29 oturumun sonunda **backend çalışıyordu** (slice-1→3a, `verify.ps1` **110/110**), ama **istemci 0 satırdı**. Bugün bulunan kusurların neredeyse hepsine bakıldığında ortak nokta şuydu: *hiçbiri ÜRÜN kusuru değildi* — `M4 eşdeğer mutant`, `G3 ölçemediğini ölçüyor`, `A11Y-2 mutantsız`, `D0/D4 mutantsız`… hepsi **DENETİM AYGITININ** kusuruydu. **Kök neden denetimin MİKTARI değil, ZAMANLAMASI: henüz koşamayan iddialar kâğıtta doğrulanmaya çalışılıyordu.** Radar bunu (R2b) ADR 0003'e dokuz turdur söylüyordu.
**Kilit geçmişi bunu doğruluyor:** K41 DURDUR → K44 MEKANİKLEŞTİR → K46 DARALT → K51 MEKANİKLEŞTİR → K52 DEVRET. **Beş kilidin üçü işi kâğıtta tuttu.** *(Cowork kendi payını beyan eder: K51'de MEKANİKLEŞTİR'i Cowork önerdi; araç iki kusur buldu ama bir tur daha kâğıtta kalındı.)*
**Cowork'ün kendi hatası da düzeltildi:** *"23 mutant ≈ 4-6 saat"* tahmini **YANLIŞTI** — her mutantın emülatörde yeniden derleme istediği varsayılmıştı. Ölçüm: **23'ün yalnız 3'ü koşan uygulama istiyor**; gerçekçi maliyet **~30-40 dakika**. Bu yüzden 3. önlem *sayı tavanı* yerine **maliyet sınıfı tavanı** olarak uygulandı.

**🔒 K53 — BEŞ KURAL `CLAUDE.md`'YE YAZILDI (PAZARLIKSIZ):**
1. **Kâğıt denetim turu tavanı = 1.** İkinci tur ancak birincisi **mimariyi değiştiren** bloker bulduysa. *(Ölçüldü: tur 1→13, tur 2→4, tur 3→0 bloker; ve üç turun hiçbirinin bulamadığı iki kusuru 100 satırlık bir betik **ilk koşumda** buldu. Ayrıca 18 blokerin **4'ü düpedüz yanlıştı**.)*
2. **Radar KIRMIZI'da varsayılan cevap `DEVRET`.** `MEKANİKLEŞTİR` seçen el *"bu sınıf koşan kod olmadan ölçülebilir"* diye **yazılı gerekçe** vermek zorunda. **İspat yükü tersine çevrildi.**
3. **Mutant tavanı maliyet sınıfına göre:** koşan-uygulama mutantı **3/dilim**; statik ve widget mutantları **tavansız**.
4. **İki oturum üst üste 0 satır ürün kodu = SERT DURAK** (yeni **R7**).
5. **Yürüyen iskelet önce, kapılar sonra.**
**Ayrım:** ÜRÜN kapısı her kuruşu hak eder; SÜREÇ kapısının **tavanı vardır**.

**🗂 HAFIZA BÖLÜNDÜ.** **`DURUM.md`** (8.301 b · `2586ED37`) = canlı durum, her oturumun **tek zorunlu okuması**, **≤12 KB tavan**, **yerinde güncellenir**. **`PROJE_HAFIZA.md`** (488 KB) = **append-only karar arşivi**, açılışta okunmaz. *Gerekçe: 488 KB'lik dosyayı her oturumun baştan okuması ölçülmüş bir bağlam vergisiydi ve dosya kendi sıralama kusurunu en tepesinde itiraf ediyordu.*

**🛠 `radar.py` 0.2.0 — İKİ ÖLÇÜLMÜŞ ONARIM (K34-f uygun: yazan el plugin, onaran el Cowork):**
- **R3 ASGARİ ÖRNEKLEM KORUMASI** (üç kez yanlış yandı): `kapatilan < 4` ise R3 **hüküm vermez**, `R3-ORNEKLEM` bilgisi basar. *Eski eşik, kusurunu ölçüp aynı turda kapatan dürüst bir eli, kusurunu gizleyen elden daha kötü gösteriyordu — yani **dürüstlüğü cezalandırıyordu**.* **Susturulmadı:** "ölçemedim" diye **beyan ediyor**, ve `kapatilan≥4`'te hâlâ ısırıyor (altın küme vaka 8 bunu kanıtlıyor).
- **R7 — ÜRÜN KODU DURGUNLUĞU** (yeni): `urun_kodu_satiri` alanı (araç/betik/belge **sayılmaz**); son 2 oturum 0 ise **KIRMIZI**.
- **Dört şık ağırlıklandırıldı:** çıktı artık `DEVRET`'i **VARSAYILAN** basıyor ve `MEKANİKLEŞTİR` için yazılı gerekçe şartını yazıyor.
- **ALTIN KÜME 6 → 11 vaka, 11/11 GEÇTİ** (K40: eşik değiştiren altın kümeye vaka eklemek zorundadır).
- ⚠ **KANONİK-KOPYA BORCU BEYAN EDİLDİ:** `proje-radari` plugin sürümü bu iki onarımı **taşımıyor**; plugin güncellenene kadar **kanonik sürüm proje içindekidir**.

**🛠 `spec-kapi-kapsama.py` — MUTANT BORCU MEKANİZMASI.** Yeni kodlar: `S4` gerekçesiz borç · `S5` **kapı borçlanamaz** (yalnız kural) · `S6` gereksiz borç. **Altın küme 9 → 13 vaka, 13/13 GEÇTİ.** *Gerekçe: tavan koyarken aracın "her kuralın mutantı olacak" kuralıyla çelişiyorduk; çözüm susturmak değil, **beyan edilmiş ve gerekçeli borç** — projenin kendi ilkesi.*

**✅ ÖLÇÜLEN ETKİ (iddia değil):** `slice-3b-istemci` artefaktı R3'ün yanlış-pozitifi yüzünden KIRMIZI'ydı ⇒ şimdi **YEŞİL**. Ve **R7 ilk işini kendi yaratıcısı üzerinde yaptı:** *"son 2 oturumda (28, 29) tek satır ÜRÜN kodu yazılmadı — SERT DURAK: bir sonraki oturum ÜRÜN KODU ile başlar."* **Bu doğrudur ve gizlenmiyor.** Oturum 28'in ölçümü, defter append-only olduğu için **geriye dönük satır düzeltilerek değil, yeni bir ölçüm kaydı eklenerek** yapıldı.

**🔒 K54 — SPEC v4, KİLİT GÜNCELLENDİ.** v3 → v4 tek kalem: §6'ya **MALİYET SINIFI** (A statik / B widget / C koşan uygulama) ve **A → B → C koşum sırası** eklendi. **Mutant sayısı, kapılar, kurallar, kabul kriterleri DEĞİŞMEDİ.** Sınıf C tam üç: **M3 · M9 · M4** ⇒ K53 tavanı **karşılanıyor**, kesinti gerekmedi.
**YENİ KİLİTLİ KİMLİK: 36.337 b · `sha256 BE4581BA`** (K52'nin `1AB02B73`'ü **artık geçersizdir**). `spec-kapi-kapsama` ⇒ **BULGU YOK / EXIT 0**.

**GİT:** Claude Code Onur tarafından **durduruldu**.

> 🔴 **K55 — DÜZELTME NOTU (append-only; yukarıdaki satır DEĞİŞTİRİLMEDİ, çürütüldü):**
> Yukarıda *"build başlamamıştı, kilit değişikliği güvenli"* yazıyor. **BU YANLIŞTI.** Commit anında ölçüldü: **Claude Code T0, T1 ve T2'yi BİTİRMİŞTİ** ve işini **commit etmemişti**; Cowork'ün `git add -A` komutu onu kendi commit'ine (`dee6dbc`) **süpürdü** — commit mesajı o dosyaları anlatmıyor. **İki kusur, ikisi de Cowork'ün:** (a) ölçmeden *"başlamamıştı"* denildi; (b) `git add -A` başka bir elin işini kör olarak içeri aldı.
> **Pratik risk düşüktü ve bu da ÖLÇÜLDÜ:** v3→v4 değişikliği yalnız **§0 ve §6**'ya dokundu; Claude Code **T2'deydi** ve o bölümlere henüz gelmemişti ⇒ kilit değişikliği yapılan işi geçersizleştirmedi. Ama gerekçe **şanstı, ölçüm değildi**.
> **KURAL (bundan sonra):** başka bir el (Claude Code) çalışırken `git add -A` **YASAK**; ya yol belirtilerek `git add <yol>` yapılır ya da o elin işi bitip commit'ini attıktan sonra eklenir.
>
> **✅ CLAUDE CODE'UN T0'I ÖRNEK NİTELİKTE — İKİ DEĞERLİ ÇIKTI:**
> 1. **Z16/B‑6 BAĞIMSIZ OLARAK YENİDEN ÖLÇÜLDÜ:** ayrı bir atılabilir projede `flutter test --platform chrome`, **tek satırlık boş bir testle 589 saniye (9,8 dk)** boyunca `loading…` satırından ilerlemedi (red-team 7 dk demişti — **aynı sınıfın ikinci ve daha kötü tekrarı**). Spec'in öngördüğü gibi **kriter 2'nin web ayağı `[DOĞRULANMADI]`** yazıldı; `DUR-ve-raporla` tetiklenmedi **çünkü spec bu ihtimali yazılı olarak kapsıyordu**. *Bu, spec'in doğru yazıldığının kanıtıdır: öngörülen sınır, öngörüldüğü gibi davrandı.*
> 2. **YENİ ORTAM KUSURU (kanla yazılı listeye girdi):** **Git Bash/MSYS, `cmd /c` argümanındaki `/c` dizgesini POSIX mutlak yol sanıp `C:/` olarak YENİDEN YAZIYOR** (süreç ağacında doğrulandı: gerçek komut `cmd C:/ dart pub global run dart_mcp_server`). ⇒ Ham `cmd /c` içeren komutlar **PowerShell'den** koşulur, **Bash'ten değil**.
> Ayrıca T0, `DESIGN.md`'nin **`534DFF68`** olduğunu bağımsız olarak doğruladı ve Z1/Z10'un **hepsiyle birebir eşleşti**.
>
> **🔴 R7'NİN TANIMI DÜZELTİLDİ (ilk gerçek kullanımda ortaya çıkan kusur):** `urun_kodu_satiri`, *"Cowork'ün yazdığı kod"* değil, **"o oturum penceresinde repoya giren ürün kodu — HANGİ EL olursa olsun"** demektir. Aksi hâlde **Claude Code build ederken R7 yanlış-pozitif verir**. Ölçülen gerçek: `src/client/` altında **135 satır Dart** (`flutter create` üretimi — sayı gerçek, **niteliği beyan edilmiştir**) + `pubspec.yaml`'ın T2 bağımlılık bloğuyla **birebir** uyumu. Düzeltme kaydı deftere eklendi; **R7 artık yanmıyor ve bu DOĞRUDUR — proje durmadı, inşa ediyordu.**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K52 — SPEC KİLİTLENDİ; `esdeger-mutant` SINIFI BUILD'E DEVREDİLDİ**)

**🔒 K52 — KİLİT: `GOREV-slice-3b-istemci-iskeleti.md` v3 KİLİTLENDİ (Onur, 26 Tem 2026).** Build başlayabilir.
**KİLİTLİ KİMLİK:** **34.821 b · `sha256 1AB02B73`** · U+FFFD 0 · CRLF 0 · `spec-kapi-kapsama` **BULGU YOK / EXIT 0** (altın küme 9/9). **Kilitten sonra değişen her bayt bu kilidi BOZAR.**
> ⚠ **KANONİK-KOPYA SINIFI, ANINDA YAKALANDI [gizlenmiyor]:** bu satır ilk yazımında **34.098 b · `AC85074D`** diyordu — ölçüm, kilit damgası dosyaya yazılmadan ÖNCE alınmıştı ve damga eklenince **anında bayatladı**. Kapanış ölçümünde yakalandı ve düzeltildi. *Aynı sınıf K45'te de görülmüştü; bu, "kimlik ölçümü SON işlemden SONRA alınır" kuralının ikinci kanıtıdır.*
**Kilidin açık şartı:** radar bu artefaktı **KIRMIZI** bırakıyor; kalan sınıf **`esdeger-mutant`** ve **kâğıtta kapatılamaz** — bir mutantın gerçekten ısırıp ısırmadığı ancak **koşarak** görülür. Kilit, bu sınıfın **BUILD'e devredilmesi** kararıdır (**R2b**: *"kâğıtta 2 turdan fazla test tasarımı denetimi YASAK; kalan iddialar KODA devredilir"*). ⇒ **§6'nın "her mutant için KIRMIZI çıktı" zorunluluğu bu kilidin bedelidir ve gevşetilemez.**
*Reddedilen:* kâğıtta bir tur daha (radar R1'in yasakladığı 3. tur; ADR 0003'ü sekiz oturuma sokan örüntünün birebir kendisi).

**SPEC'İN NİHAİ ŞEKLİ:** 7 kapı (G1 MCP · G2 CVE · G3 lisans · G4 token · G5 a11y · G6 kalıcılık · G7 veri) · 16 adlandırılmış kural (D0–D6 · A11Y‑1…7 · kontrast · metin) · **23 mutant** · 15+1 kabul kriteri · 6 "önce ölç" kalemi (B‑1…B‑6) · 7 `DESIGN.md` borcu (BD‑1…BD‑7).

**SONRAKİ İŞ — CLAUDE CODE:** `Momentum` kökünden aç (üst klasörden AÇMA), spec'i uygula, `KANIT/slice-3b/` üret. **Cowork build ETMEZ; artefaktı bağımsız DOĞRULAR** (builder'ın beyanına güvenmez, çıktıyı kendi söker — `tuzak-app-qa` disiplininin bu projedeki karşılığı).

**AÇIK BORÇLAR (bu oturumda kapatılmadı, adlandırıldı):** `DESIGN.md` BD‑1…BD‑7 (K46 gereği dokunulmadı) · `radar.py`'nin R3 asgari örneklem koruması (ayrı el) · kontrast betiğinin kalıcı hâli (kod tarafı G5'e girdi) · **push borcu: 6 commit** (`ab1ee61` · `648da7b` · `1b1b505` · `237b9b5` · `9ce1bb8` · kilit commit'i).

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K51 — RADAR KIRMIZI'DA `MEKANİKLEŞTİR` KİLİTLENDİ; ARAÇ YAZILDI VE İLK KOŞUMDA ÜÇ DENETİM TURUNUN KAÇIRDIĞI İKİ KUSURU BULDU**)

**🔴 K51 — KİLİT: `MEKANİKLEŞTİR` (Onur, 26 Tem 2026).** Radar `GOREV-slice-3b-spec`'i **KIRMIZI** verdi: **R1 — `kor-kapi` + `esdeger-mutant` iki tur üst üste, mekanik kapısı yok ⇒ 3. tur YASAK.** Dört şık ölçülmüş bedelle sunuldu; Onur R1'in lafzını seçti. *Reddedilenler: DEVRET (v3'te kalan kör kapı build'den sonra çıkar) · DARALT (a11y bloğunu ertelemek ödevin ayırt edici yüzünü zayıflatır) · DURDUR (bu artefakt 1 oturumda `[13→4→0]` yakınsıyor, ADR 0003'ün 8 oturumluk tablosu DEĞİL).*

**✅ ARAÇ: `araclar/spec-kapi-kapsama.py`** — 9.276 b · `sha256 5599BB59` · U+FFFD 0 · CRLF 0 · **saf ASCII çıktı**.
Bir GOREV spec'inin **§5 KAPI+KURAL envanteri** ile **§6 MUTANT tablosunu** ayrıştırır; **her kapının VE her adlandırılmış kuralın** en az bir mutantı olduğunu ispatlar. Kodlar: `S0` biçim · `S1` mutantsız kapı · `S2` mutantsız kural · `S3` hayalet atıf (envanterde olmayan hedefe atıf).
**ALTIN KÜME 9/9 GEÇTİ · EXIT 0:** temiz spec **susar** (yanlış-pozitif kontrolü) · mutantsız kapı **S1** · mutantsız kural **S2** · hayalet atıf **S3** · bölüm yoksa **S0** · **tek mutant iki kuralı kapatırsa susar** (ikinci yanlış-pozitif kontrolü) · `D0`–`D2` **kod aralığı açılır** · **BAĞLI TİRE (U+2011) tuzağı normalize edilir** · boş mutant tablosu **S0**.

**🔴 ARAÇ İLK GERÇEK KOŞUMDA İKİ KUSUR BULDU — ÜÇ DENETİM TURUNUN HİÇBİRİ BULMAMIŞTI:** `D0` ve `D4` **MUTANTSIZDI**. (Bir denetçi *"D1 ve D4 mutantsız"* demişti; **D1 kapatılmış, D4 gözden kaçmıştı**; `D0`'ı **kimse görmemişti**.) ⇒ **M22** (`D0`: `tokens` bloğunu **KOPYA** üzerinde boz — `DESIGN.md`'ye dokunulmaz, K46) ve **M23** (`D4`: gerekçesiz muafiyet) eklendi; **21 mutant 23 oldu**; araç yeniden koşuldu: **BULGU YOK, EXIT 0.**
*K33 örüntüsünün **on birinci** kanıtı: yine **koşulan araç** buldu, akıl yürütme değil.*

**✅ KİLİDİN İŞE YARADIĞI ÖLÇÜLDÜ — İDDİA DEĞİL:** radar yeniden koşuldu ve **`kor-kapi` sınıfı R1'den R1b'ye geçti**: *"Tekrar eden ama **MEKANİKLESMIS** sinif(lar): kor-kapi ⇒ bu sinif icin tur eklemek **mesrudur**."* Bloker eğrisi **`[13, 4, 0, 0]`**. R4 hızı **%32 → %7,7**'ye düştü (v2→v3 küçültmesi tuttu).

**🔴 KALAN TEK R1 SINIFI: `esdeger-mutant` — VE BU KÂĞITTA KAPATILAMAZ.** Aracın **beyan edilmiş sınırı**: mutantın *gerçekten ısırdığını* ölçmez, yalnız **kapsamayı** ölçer; eşdeğer-mutant tespiti **çalışan kod ister**. Bu, projenin kendi **R2b** dersinin birebir tarifidir: *"kâğıtta 2 turdan fazla test tasarımı denetimi YASAK; kalan iddialar KODA devredilir."* ⇒ **Bu sınıfı kapatacak tek şey BUILD'dir.**

**DEFTER NOTU [dürüstlük]:** `D0`/`D4` kusurları **tur 1'de doğmuştu** ama o turda **ölçülmemişti**; `PROJE_RADAR.jsonl`'daki tur 1 `uretilen=13` sayısı bu yüzden **EKSİKTİR** (gerçek 15). Defter append-only olduğu için **geriye dönük düzeltilmedi**; bu not kayıttır.

**GİT:** `9ce1bb8` — 9 dosya, +991/−1. Ağaç **TEMİZ**, `.git/index.lock` **YOK**, `origin/main`'e göre **5 İLERİ**. **PUSH ONUR'DA.**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K50 — RED-TEAM v2'Yİ DE KIRDI; K49'UN ÖNGÖRDÜĞÜ GERİ DÜŞÜŞ TETİKLENDİ**)

**K49 (Onur kilidi):** *"G1'in 4. ayağını `vm_service` ile GERÇEKTEN doldur"* — **açık şartıyla birlikte kilitlendi: *"`vm_service`'in ne döndürdüğü ölçülmedi; çıkmazsa (a)'ya [3 ayak + 1 ön koşul] düşülür."*** Spec v2 (36.513 b · `3AE01B04`) bu kilide göre yazıldı.

**🔴 RED-TEAM (iş akışının EN SON kapısı) v2'yi de kırdı: 4 bloker · 6 majör · 4 minör.** Bu denetçi **beyan etmedi, KOŞTU**: `design-token-kapisi.py --altin-kume` koştu, `sqlite3.wasm`'ı indirip taradı, Flutter SDK kaynağında `focus_manager.dart`/`focus_scope.dart` aradı, üç a11y guideline testini gerçekten yazıp koştu.

**🔴 K49'UN GERİ DÜŞÜŞÜ TETİKLENDİ — A4 AYAĞI DÜŞÜYOR.** Red-team ölçtü ve gösterdi: `vm_service → getVM` **bağımsız bilgi taşımıyor** — A1 (ekran çiziliyor), A2 (widget ağacı dönüyor), A3 (hata kanalı canlı) yeşilken `getVM`'in isolate döndürmemesini sağlayan **hiçbir uygulama kodu kusuru yoktur**; çizen bir Flutter uygulamasının tanımı gereği canlı isolate'i vardır. M14 (yanlış `appUri`) **uygulamayı değil koşum takımını** bozuyor ⇒ totoloji. **K49'un yazılı şartı gereği (a)'ya düşülüyor: G1 = "3 GERÇEK AYAK + 1 ÖN KOŞUL".** Bu, oturumun kendi başına aldığı bir karar DEĞİL, Onur'un kilidinin içine yazılmış geri düşüştür. `vm_service` teşhis aracı olarak kalır, **kapı ayağı sayılmaz**.

**🔴 DİĞER ÜÇ BLOKER (v3'te kapatılıyor):**
1. **G6'nın sürüm assert'i ÖLÇÜLEMEZ.** Red-team `sqlite3.wasm`'ı (748.424 b) taradı: içinde geçen tek sürüm dizgesi **`3.53.3`** (SQLite **C kütüphanesi** sürümü); **`3.5.0` literali sıfır kez geçiyor**. Yani wasm, Dart paket sürümünü **taşımıyor** ⇒ `lock >= wasm` assert'i ya daima FALSE verir ya sessizce sha256'ya indirgenir. ⇒ **Sürüm karşılaştırması KALDIRILIYOR**, yerine `araclar/web-varlik.sha256` **pinli sha256** + release tag'i metin olarak. **`sqlite3` pini de kalkıyor** (lock zaten tek sürüm taşır; pin ileride `drift`'in `^3.6.0` istemesini kilitler).
2. **Sıkılaştırılmış D1, `DESIGN.md` §3.1 ile ÇELİŞİYOR — KENDİM SAYDIM VE DOĞRULADIM.** §3.1'in sekiz görsel bileşenine atanmış token'ların birleşimi **20** token; MUST listesi **24**. Hiçbir bileşene atanmamış dördü: **`renk.yuzey.ikincil` · `renk.metin` · `hareket.hizli` · `olcu.odak.kalinlik`**. Bunların meşru yeri `tema.dart`'tır ⇒ *"en az bir kullanım `lib/design/` dışında"* kuralı bu dördü için **sağlanamaz**; builder ya §8.7'yi ihlal eder ya süs kullanım yazar. ⇒ **Dördü D1 sıkılaştırmasından ADIYLA muaf**, gerekçesiyle; ayrıca **BD‑7**.
3. **A11Y‑2'nin ölçümü UYGULANAMAZ.** `Focus`/`FocusNode`'da **dekorasyon yoktur** (red-team SDK kaynağında aradı: *"decoration" ikisinde de 0 kez*). Odak halkasını onu tüketen widget üretir. ⇒ Ölçüm somut widget'a bağlanıyor: `GorevEkleAlani`'nın `InputDecoration.focusedBorder.borderSide` `width`/`color` alanları.
4. **v2'nin 7. "kapattık" maddesi KAPANMAMIŞ:** A11Y‑2/‑4/‑6/‑7 **ve kontrast** hâlâ **mutantsız** — v2 yalnız ölçüm yöntemini değiştirdi, mutant eklemedi. Kapsama cümlesi ("mutantsız kapı yok") körlüğü **kapı düzeyinde sayarak KURAL düzeyinde gizliyordu**. ⇒ **M15…M21 ekleniyor** (A11Y‑2 · A11Y‑4 kırpma · A11Y‑6 · A11Y‑7 · kontrast · D6 · sıkılaştırılmış D1).

**✅ RED-TEAM KENDİ ELİYLE DÖRT ENDİŞEYİ ÇÜRÜTTÜ (ölçerek):** `sqlite3 3.5.0` pini **çakışma yaratmıyor** (`drift → ^3.4.0`, `drift_flutter → ^3.0.0`, `drift_dev → ^3.0.0`; üçünü de sağlar) · D5/D6 eklemek mevcut **altın kümeyi bozmuyor** (12 vakanın fixture'ları `lib/design/` dışında) · **Z11 doğru** (wasm gerçekten `sqlite3-3.5.0` release'inde, 748.424 b; `drift_worker.js` gerçekten `drift-2.34.0`'da) · kriter 15 doğru (`DESIGN.md` `534DFF68`).

**🆕 BD‑6 (yeni, red-team ölçtü):** `DESIGN.md` §0 ve §10 *"altın küme **10/10**"* diyor; araç fiilen koşuldu ⇒ **12 vaka, 12/12, EXIT 0**. Belgedeki sayı **bayat** ve K46 onu sha256 ile donduruyor.
**🆕 BD‑7 (yeni, yukarıda sayıldı):** §3.1 tablosu dört MUST token'ı **hiçbir bileşene atamıyor**.

**🔴 ORTAM UYARISI DOĞRULANDI — `device_stage_files` BAYAT KOPYA SUNDU.** Red-team'e v2 (36.513 b) stage'lendi; ona **v1 (27.668 b)** göründü. Denetçi bunu fark edip bağlı klasördeki gerçek dosyayı okudu. **Devir notundaki *"device_stage_files BAYAT KOPYA sunabilir ⇒ gerçeği desktop-commander read_file ile oku"* uyarısı bu oturumda ÖLÇÜLEREK doğrulandı.** Bundan sonra denetçi ajanlara stage yolu değil, **kanonik yol + `read_file`** talimatı verilecek.

**📏 ARTEFAKT BÜYÜMESİ ÖLÇÜLDÜ (R4 sınıfı, gizlenmiyor):** v1 27.668 b → v2 36.513 b = **+%32** (radar R4 eşiği tur başına ~%18). Red-team **ölçülmüş küçültme** önerdi: v1/v2 otopsi bloğu (2.317 b) · BD/B tabloları (1.606 b) · 14 adet "(v1'de…)" parantezi (818 b) · altı kez tekrarlanan kurallar (~1.200 b) ⇒ **~5.900 b (%16) kazanç, tek bayt uygulanabilir talimat kaybı olmadan.** İlke: *"build spec'i builder'ın YAPACAĞI şeyi taşır; kimin neyi neden yanlış yazdığı HAFIZAYA gider."* **v3 bu küçültmeyi uygular** — otopsi bu checkpoint'te yaşar.

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K48 — SPEC v1 ÜÇ BAĞIMSIZ DENETÇİ TARAFINDAN YIKILDI; SONRA DENETÇİLER DE DENETLENDİ VE DÖRT BLOKERİ ÖLÇÜMLE ÇÜRÜTÜLDÜ**)

**`GOREV_CLAUDE_CODE/GOREV-slice-3b-istemci-iskeleti.md` v1 yazıldı** (27.668 b · `742FA114` · U+FFFD 0 · CRLF 0). **K46'nın pazarlıksız şartı TUTTU: `DESIGN.md` `534DFF68` — tek bayt değişmedi** (kapanışta yeniden ölçüldü).

**ÜÇ BAĞIMSIZ DENETÇİ (K26: yazan el denetleyemez) — ham sayım: 18 bloker · 39 majör · 16 minör.** Üçü de birbirinden habersiz koştu ve **aynı çekirdek kusurları bağımsız olarak buldu** (bu, bulgunun gerçek olduğunun en güçlü işareti).

**🔴 SONRA DENETÇİ DE DENETLENDİ — DÖRT BLOKER ÖLÇÜMLE ÇÜRÜTÜLDÜ [bu, doktrinin kendisine uygulanmasıdır]:**
- **YANLIŞ BLOKER 1-2-3 (risk denetçisi): "`drift_flutter 0.3.1` YOK (0.3.0'dır) · `drift_dev 2.34.5` YOK (2.34.4'tür) · `sqlite3 3.5.0` YOK (3.4.0'dır)".** **ÇÜRÜTÜLDÜ.** `araclar/pub-surum-olc.py` pub.dev **resmî API'sine** sordu: `drift 2.34.2` (14 Tem) · **`drift_flutter 0.3.1` (11 Tem)** · **`drift_dev 2.34.5` (22 Tem)** · **`sqlite3 3.5.0` (18 Tem)** · `path_provider 2.1.6` · `build_runner 2.15.2`. **Hepsi VAR.** Denetçi pub.dev'in **HTML sayfalarını** okumuştu; o sayfalar bayat veri döndürüyor (aynı tuzağı ilk araştırmacı da yaşadı ve `.json` ucuna geçmişti). **DERS: pub.dev HTML'i kanıt değildir, `/api/` ucudur.**
- **YANLIŞ BLOKER 4 (risk denetçisi): "Z6'nın alıntısı resmî Flutter dokümanında YOK, üçüncü taraf bir skill dosyasından alınmış".** **ÇÜRÜTÜLDÜ.** `docs.flutter.dev/ai/mcp-server` kendim çekildi; cümle **BİREBİR orada**: *"**Web**: the `flutter_driver` extension isn't supported on web builds, so finder-based commands like screenshots and taps aren't available there."* Aynı sayfa `flutter pub add "flutter_driver:{sdk: flutter}"`, bayrak korumalı `enableFlutterDriverExtension()` ve *"Enabling the Flutter Driver extension disables real keyboard input"* uyarısını da doğruluyor. **F1 kilidi SAĞLAM.** *(Tek düzeltme: `--print-dtd` o sayfada geçmiyor, başka kaynaktan geldi — Z8 atfı daraltılacak.)*

**✅ DENETÇİ HAKLI ÇIKTI — ÖLÇÜLDÜ (`araclar/lisans-yokla.py`):** `GET /api/packages/drift` **LİSANS DÖNDÜRMÜYOR** (üst düzey alanlar: `latest · name · versions`; `latest` alanları: `archive_sha256 · archive_url · published · pubspec · version`; gövdede `licen` **geçmiyor**). Lisans yalnız `GET /api/packages/<ad>/metrics` → `scorecard.panaReport.licenses[0].spdxIdentifier` **= `MIT`** yolundan geliyor — **dokümantasyonsuz ve sürüm garantisiz** bir uç (dart-lang/pub-dev#4717). ⇒ **G3'ün v1'deki veri kaynağı (`dart pub deps --json` + `pubspec.lock`) FİZİKSEL OLARAK LİSANS TAŞIMIYOR; kapı ölçemediğini ölçüyormuş gibi sunuyordu.**

**✅ İKİNCİ DOĞRULANAN DENETÇİ BULGUSU:** `dio`'nun **2 advisory'si** var: `GHSA-9324-jv53-9cc8` **ve** `GHSA-jwpw-q68h-r678` (**geri çekilmiş duplikat**). ⇒ G2 `withdrawn` alanını okumazsa **yanlış-pozitif** üretir; ayrıca hüküm `versions` dizisine değil **`ranges` (`introduced`/`fixed`)** olaylarına dayanmalı (`versions` OSV şemasında opsiyoneldir). Aynı çağrı `/advisories` ucunun **CANLI** olduğunu da kanıtladı ⇒ G2 inşa edilebilir.

**🔴 AYAKTA KALAN BLOKERLER (v2'de kapatılacak, tekilleştirilmiş 13):**
1. **G3 lisans kapısı:** veri kaynağı yanlış (yukarıda ölçüldü) **ve mutantı YOK**.
2. **A11Y‑2/‑4/‑6/‑7'nin MUTANTI YOK** — dört kural "yeşil" sayılıyor ama kırmızı yakabildiği hiç gösterilmemiş (iki denetçi bağımsız buldu).
3. **G5'in ARACI YOK:** dosya adı, altın küme, exit sözleşmesi tanımsız — K44-a en büyük kapıda hiç uygulanmamış.
4. **Ekran görüntüsü bir ölçüm değildir:** A11Y‑2/‑4/‑6'nın tek dayanağı hiçbir assert'in okumadığı bir PNG; hükmü insan gözü veriyor. *(Çözüm ölçüldü: `flutter_test`'in `meetsGuideline(androidTapTargetGuideline / labeledTapTargetGuideline / textContrastGuideline)` matcher'ları tam bunun içindir.)*
5. **M7 EŞDEĞER MUTANT:** `SenkronRozeti` etkileşimli **değil** (dokunulabilir olan `CakismaRozeti`) ve §4 gereği **görünür metin** taşıyor ⇒ `Semantics` silinse bile `Text`'ten etiket doğar, ısırmaz.
6. **M8 FİZİKSEL OLARAK KOŞAMAZ:** `dio 4.0.6`'nın SDK kısıtı `<3.0.0`, ortam Dart 3.12.2 ⇒ `pub get` çözümlemede düşer, `pubspec.lock` hiç oluşmaz.
7. **M4 EŞDEĞER MUTANT:** COOP/COEP kalkınca drift **belleğe düşmez**, kalıcı `sharedIndexedDb`'ye düşer ⇒ G6'nın `inMemory` eşiği hiç tetiklenmez, kapı yeşil kalır. **G6 eşiği `opfsShared|opfsLocks` beklentisine çevrilecek.**
8. **DİLİMİN BAŞLIK TESLİMATI KAPISIZ:** *tam çevrimdışı CRUD* altı kapının hiçbirinin menzilinde değil, on mutantın hiçbiri veri katmanına dokunmuyor ⇒ **G7 (veri katmanı sözleşmesi) eklenecek**.
9. **`Theme.of` KAÇAĞI:** `tema.dart` tek başına 24 MUST sembolüne dokunup **D1'i doyurur**; bileşenler `Theme.of(context).colorScheme.primary` yazarsa D1/D2/D3/D4 hepsi 0 verir ve DESIGN.md §8.7 sessizce ihlal edilir ⇒ **D5 (yasak `Theme.of` renk erişimi) + D6 (yasak `cupertino.dart` importu)** gerekli.
10. **A11Y KAPISI VİTRİNDE KOŞUYOR:** ölçülen ekran, sürüm derlemesinde **bulunmayan** ekran; vitrinde 48dp ölçen satır gerçek `ListView` kısıtında 40dp olabilir ⇒ *"vitrinde yeşil, üründe kırmızı"*. G5 **gerçek `GorevListesiEkrani` üzerinde de** koşacak.
11. **G1'in "dört ayağı" ŞİŞİRİLMİŞ:** ayak 4 (DTD) ayak 2 ve 3'ün **ön koşuludur** (Z3/Z5 kendi metnimizde yazıyor) ⇒ bağımsız ölçüm değil. Kapı **"3 ayak + 1 ön koşul"** diye yeniden adlandırılacak. *(Not: K44-b'nin "4/4" lafzı bu ölçümle çelişiyor — ONUR'un kilidi gerekecek.)*
12. **Kriter 4 ↔ 11 ÇELİŞKİSİ:** kriter 4 adı yazılmamış "iki ayak için kasten bozma" istiyor, kriter 11 "on mutantın onu" diyor ⇒ builder'ın kendi tasarladığı kırmızı = üreten/denetleyen aynı el.
13. **`flutter analyze` "0 uyarı" KAPISIZ:** `flutter_lints` kuralları **info** seviyesindedir; `--fatal-infos` verilmedikçe `print` içeren sürüm kriteri **geçer** ⇒ ban-list #5 kapısız.

**🔴 K46'NIN BEDELİ ŞİMDİ ÖLÇÜLDÜ [DARALT kilidinin faturası, gizlenmiyor]:** Denetim, kusurların bir kısmının **`DESIGN.md`'nin kendisinde** olduğunu gösterdi ve o dosyaya bu turda **dokunulamaz**: (a) §1.1 koyu tema değerleri bir **TABLO**dur ⇒ `tokens` bloğunda olmadığı için G4 onları **hiç okumaz**, `tokens.dart` da D2'den muaftır ⇒ **koyu tema tamamen kapısız**; (b) `renk.ayirici`'nin *"hiçbir zaman bir kontrolün tek tanımlayıcısı olamaz"* kuralı **A11Y numarası taşımıyor** ⇒ G5'in kapsamı dışında, ölü kural — üstelik `GorevSatiri` onu satır ayırıcısı olarak kullanıyor; (c) **odak halkası / birincil buton** çifti §2.1'in 24 çiftinde **YOK** ⇒ ekle düğmesinde odak halkası 1,00:1 olabilir; (d) `MomentumTema` bir widget **değil** ⇒ "9 MUST bileşeni ağaçta adıyla görünmeli" ayağı 8/9'dan fazlasını veremez; (e) `tipo.baslik.l = 20/28` biçiminde 28'in mutlak mı oran mı olduğu yazılı değil ⇒ mutlak uygulanırsa 2.0× ölçekte **sessiz kırpma**. **Beşi de ADLANDIRILMIŞ BORÇ olarak `DESIGN.md` v2 turuna yazıldı; bu turda KAPATILMAZ.**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29: **K47 — SPEC'İN DÖRT ÇATALI KİLİTLENDİ; DEVİR NOTUNUN İKİ VARSAYIMI ÖLÇÜMLE YANLIŞLANDI; MCP 1.1.0 ÖLÇÜLDÜ**)

**ÜÇ BAĞIMSIZ ARAŞTIRMA KOŞTU (web, kaynak URL'li). Spec yazılmadan ÖNCE koşuldu — K44-a'nın ruhu: iddiayı belgeye yazmadan önce ölç.**

**🔴 YANLIŞLANAN VARSAYIM 1 — DÖRT AYAKLI KAPI WEB'DE FİZİKSEL OLARAK KURULAMAZ.** Flutter resmî dokümanı birebir: *"the `flutter_driver` extension isn't supported on web builds, so finder-based commands like screenshots and taps aren't available there."* ⇒ web'de yalnız **DTD'den geçen üç ayak** (widget ağacı · runtime hataları · dtd) koşar; **ekran görüntüsü ayağı YOK.** Kaynak: `docs.flutter.dev/ai/mcp-server`.

**🔴 YANLIŞLANAN VARSAYIM 2 — `sqlite3_flutter_libs 0.6.0+eol` "ELENEMEZ", ÇÜNKÜ TRANSİTİF GELİYOR.** Devir notu (d) maddesi *"elenmesi"* diyordu; ölçüm gösterdi: `drift_flutter 0.3.1` onu **hâlâ bağımlılık olarak çekiyor** (`sqlite3_flutter_libs: ^0.6.0+eol` **ve** `sqlcipher_flutter_libs: ^0.7.0+eol`). Paket 0.6.0'dan beri **hiçbir şey yapmıyor** (pub.dev: *"Not used anymore, update to version 3.x of package:sqlite3"*); native `.so`'yu artık `sqlite3` 3.x **build hooks** ile getiriyor. Seçenek "elemek" değil, **kimin taşıyacağı**.

**🆕 DEVİR NOTUNDA HİÇ YOKTU — SESSİZ KALICILIK KAYBI (kör-kapı sınıfı, ödevin taç mücevherini doğrudan vuruyor):** Flutter Web'de Drift için `sqlite3.wasm` + `drift_worker.js` **ELLE** indirilip `web/` altına konur; drift dokümanı wasm'ı **YANLIŞ YERDE** gösteriyor (gerçek kaynak `simolus3/sqlite3.dart` release'i `sqlite3-3.5.0`, drift release'i değil — `drift-2.34.0` asset listesi ölçüldü, içinde `sqlite3.wasm` **YOK**). Dahası **COOP/COEP başlıkları yoksa** drift **hata vermeden** OPFS → IndexedDB → in-memory'ye düşer ⇒ *"çevrimdışı-öncelikli"* iddiası **sessizce yalan olur**. Sürüm yönü kuralı (sürdürücünün ağzından): `sqlite3.wasm` sürümü pubspec'teki `sqlite3` sürümünden **BÜYÜK OLAMAZ** (ileri uyum var, geri uyum yok).

**✅ CVE KAPISI YAZILABİLİR — ÖLÇÜLDÜ.** `GET pub.dev/api/packages/<ad>/advisories` **OSV formatında** advisory döndürüyor (canlı doğrulandı: `dio` → `GHSA-9324-jv53-9cc8` / `CVE-2021-31402`, `affected[].versions` tam liste). `osv-scanner` da `pubspec.lock`'u resmen destekliyor. **`dart pub audit` diye bir komut YOKTUR** (resmî alt-komut listesinde yok); `dart pub get` advisory **basıyor ama non-zero exit DÖNMÜYOR** (dart-lang/pub#4333 AÇIK) ⇒ tek başına CI kapısı olmaz. **DÜRÜST SINIR:** Pub havuzunda toplam **~11-13** advisory var (npm'de on binlerce) ⇒ bu kapı gerçek risk taraması **değil**, disiplin vitrinidir; README'de böyle yazılmazsa "yanlış güven satmak" olur.

**✅ (a) ve (b) DOĞRULANDI:** `flutter_driver` **şart** — `flutter pub add "flutter_driver:{sdk: flutter}"` + `main()` içinde bayrak korumalı `enableFlutterDriverExtension()`; koşum `flutter run --dart-define=ENABLE_FLUTTER_DRIVER=true --print-dtd`. **`sdk: flutter` olduğu için üçüncü taraf bağımlılık DEĞİL** ⇒ lisans/CVE kapısı gerektirmez. **TUZAK:** `--print-dtd` + `--machine` birlikte kullanılırsa DTD URI JSON çıktısında **hiç görünmüyor** (flutter/flutter#176310) ⇒ CI'da `--machine` **YASAK**. Ayrıca driver uzantısı açıkken **gerçek klavye girişi devre dışı** kalır. `integration_test` bu işi **YAPMAZ** (test koşar, canlı uygulama sürmez) — ikisi birbirinin yerine geçmez.

**🔴 DÖRT KİLİT (Onur, 26 Tem 2026) — K47:**
1. **MCP KAPISI: Android emülatörde 4/4 + web'de aynı kapının 3 ayağı.** Web'in 4. ayağı **BEYAN EDİLMİŞ MUAFİYET**tir (fiziksel imkânsızlık, gizlenmiş sınır değil). *Reddedilen:* yalnız-Android (web'in hiçbir a11y/tasarım iddiası kanıtlanmaz, A-5 ölçülemez) · yalnız-web (K44-b'nin 4/4 şartını İHLAL ederdi + dart-lang/ai#356 `web-server`'da DTD URI'sinin hiç açılmadığını bildiriyor).
2. **MCP 1.1.0'A YÜKSELTİLDİ, SONRA ÖLÇÜLDÜ.** *Reddedilen:* 0.1.4'te kalmak (bilerek ölü hat).
3. **CVE KAPISI: SAF PYTHON + `pub.dev/.../advisories`.** *Reddedilen:* `osv-scanner` (yeni ikili = kuralın kendisine dönmesi, exit code dokümante değil) · çapraz-iki-araç (13 advisory'lik havuz için orantısız).
4. **DRIFT: `drift_flutter` + BEYAN EDİLMİŞ MUAFİYET.** İki EOL paket **transitif** gelir, pubspec'e **ELLE YAZILMAZ**; lisans kapısı transitifleri de tarar. *Reddedilen:* elle kurulum (drift_flutter'ın çözdüğü platform ayrımları kendi kodumuza geçer = yeni kusur yüzeyi).

**✅ MCP 1.1.0 — KENDİ PROBE'UMLA ÖLÇÜLDÜ (`--help`'e GÜVENİLMEDİ; K43'ün hatası tekrarlanmadı):**
- `dart pub global activate dart_mcp_server` ⇒ **1.1.0** kuruldu (araştırma 1.0.1 demişti; **kurulan daha yeni**). ⚠ Pub `bin` dizini PATH'te değil: `C:\Users\gulci\AppData\Local\Pub\Cache\bin`.
- **`tools/list` cevabı: 14 ARAÇ** = 0.1.4'teki 13'ün **TAMAMI** + yeni **`vm_service`**. **Kaybolan araç YOK.**
- **K44-b'nin en kritik bilinmeyeni KAPANDI:** `flutter_driver_command.command` enum'unda **`screenshot` VAR**. Tam enum: `get_health · enter_text · send_text_input_action · get_text · scroll · scrollIntoView · set_frame_sync · set_semantics · set_text_entry_emulation · tap · waitFor · waitForAbsent · waitForTappable · get_offset · get_diagnostics_tree · screenshot`. Finder'lar: `ByType · ByValueKey · ByTooltipMessage · BySemanticsLabel · ByText · PageBack · Descendant · Ancestor`.
- `widget_inspector.command` enum: **`get_widget_tree` · `get_selected_widget` · `set_widget_selection_mode`** (+`summaryOnly` bayrağı). Açıklaması birebir: *"Requires an active DTD connection."*
- `dtd.command` enum: **`connect` · `disconnect` · `listConnectedApps` · `listDtdUris`**.
- `get_runtime_errors` parametreleri: `appUri`, `clearRuntimeErrors`.
- **Aracın kendi uyarısı (spec'e girer):** *"To specify a widget to interact with, you must FIRST use widget_inspector (get_widget_tree)... **Do not guess at how to select widgets**."*
- **`.mcp.json` GÜNCELLENDİ:** `args` artık `["pub","global","run","dart_mcp_server"]` (eski: `["mcp-server"]` = SDK gömülü ölü hat). Yol-bağımsız; `cmd /c dart pub global run dart_mcp_server` ile probe'lanıp **1.1.0 döndüğü doğrulandı**.

**🆕 ARAÇ: `araclar/mcp-arac-probe.py`** — bir MCP sunucusunun gerçek araç listesini `tools/list` ile ölçer, `--sema <arac>` ile tam JSON şemasını basar. Çıktı **saf ASCII** (cp1254 kalkanı). **🔴 ARAÇ BİR KUSURLA DOĞDU VE AYNI TURDA KAPANDI [gizlenmiyor]:** ilk sürümde baştaki `--` ayıklaması **yalnız `--sema` dalının içindeydi** ⇒ `--sema`'sız çağrıda Popen `--`'yi komut sanıp `[WinError 2]` verdi. Ayıklama **koşulsuz** hâle getirildi ve düzeltme dosyada yorumla **adlandırıldı**. *K33 örüntüsünün onuncu kanıtı: yine **koşulan araç** buldu, akıl yürütme değil.*

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 29 açılış: **K46 — RADAR KIRMIZI'DA DÖRT ŞIK SUNULDU, ONUR `DARALT`I KİLİTLEDİ**)

**AÇILIŞ ÖLÇÜMÜ (beyan değil, koşuldu):**
- `PROJE_HAFIZA.md` (459.627 b · `7C62AC71`) + `CLAUDE.md` (5.651 b · `DBFF6727`) okundu; Onur'un yapıştırdığı devir notuyla **birebir tuttu**.
- **Dosya kimlikleri 8/8 EŞLEŞTİ:** `DESIGN.md` 15.742 b `534DFF68` · `PROJE_RADAR.jsonl` 11.849 b `0B98FA3B` · `design-token-kapisi.py` 21.229 b `11667526` · **`adr-kapi-taramasi.py` 50.582 b `A22841F2` (K34-f TUTUYOR)** · `radar.py` 15.252 b `688B6D31` · `.mcp.json` 155 b `01EC4807`.
- **GİT:** HEAD `237b9b5` · `origin/main` `2ee2c24` ⇒ **4 ileri, push yok** · ağaç **TEMİZ** · `.git/index.lock` **YOK** (`Test-Path` ⇒ `False`).
- **RADAR:** altın küme **6/6 GEÇTİ** · hüküm **KIRMIZI** — `docs/ADR/0003` (R1·R5·R2b·R4, park, beklenen) · `slice-3b-istemci` (**R3**: son tur kapatılan 1 / üretilen 1, bloker eğrisi `[0,0,0,0]`, bayt 15.742 = `DESIGN.md`).
- **[K21] 102.885 token = %10,3 🟢** (bulutta koşuldu; transcript konteynerde: `/root/.claude/projects/-home-claude/95da2669-….jsonl`).

**🔴 K46 — KİLİT: `DARALT` (Onur, 26 Tem 2026).** K40 gereği dört şık ölçülmüş gerekçe ve **adlandırılmış bedelle** sunuldu; oturum kendi başına seçmedi.
- **Kilidin PAZARLIKSIZ ŞARTI:** bu turda **`DESIGN.md`'ye TEK BAYT YAZILMAZ.** Spec ayrı dosyadır (`GOREV_CLAUDE_CODE\GOREV-slice-3b-*.md`), ayrı kusur yüzeyidir ve radara **AYRI artefakt satırı** (`GOREV-slice-3b-spec`) olarak açılır, sayacı sıfırdan başlar. **`DESIGN.md`'ye bir bayt yazılırsa bu kilit HÜKÜMSÜZDÜR** ve yapılan iş kırmızıdan kaçmak için sayaç sıfırlama (ölçümü sulandırma) sayılır.
- **`slice-3b-istemci` satırı KIRMIZI KALIR**, uyarısı canlıdır; susturulmadı.
- **KAPANMAYAN BORÇ [gizlenmiyor]:** `R3`'ün **asgari örneklem koruması yok** — `kapatılan=1` olan *her dürüst tur* kırmızı verir, üretilen kusur ölçümle bulunup **aynı turda kapansa bile** (K45'teki kontrast kusuru tam olarak buydu). Bu şık o borcu **KAPATMAZ**; onarım `radar.py` + altın küme vakasıyla **AYRI ELE** (K34-f) yazılı kalır.
- *Reddedilenler ve bedelleri:* `MEKANİKLEŞTİR` (spec kayar; projeye kopyalanmış `radar.py` plugin sürümünden ayrışır ⇒ `kanonik-kopya` sınıfı) · `DEVRET` (dört ayaklı MCP kapısı ve CVE kapısı **yazılı olmadan** gider ⇒ kör-kapı riski geri gelir) · `DURDUR` (24 `MUST` vaadi ve A11Y‑1…7 ölçülemez kalır).

**🆕 ORTAM UYARISI — BU OTURUMDA ÖLÇÜLDÜ (kanla yazılı listeye girer):** Cowork→Desktop Commander köprüsünde PowerShell'e giden komutlarda **`$` ile başlayan değişkenler SİLİNİYOR**: `$LASTEXITCODE` → boş, `$_.Name` → `.Name`, `$h` → boş. **İki komut bu yüzden `ParserError` ile düştü** (biri sessizce yanlış çıktı üretti: `--altin-kume` koştu ama zincirin kalanı literal metin olarak yazıldı). **Kural:** DC üzerinden PowerShell'e `$` gönderme — `$`'sız yazım (`Select-Object`/`Format-Table`) ya da Python betiği kullan. Çıkış kodu gerekiyorsa **ayrı çağrıda** ölç.

## ⏭ DEVİR NOTU (26 Tem 2026 — oturum 28 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

**CEPHE DEĞİŞTİ: ADR 0003 v7 DONDURULDU (K41), İŞ ARTIK `slice-3b` FLUTTER İSTEMCİSİDİR (K42).**

**GİT (kapanışta ölçüldü):** HEAD **`1b1b505`** · `origin/main` **`2ee2c24`** ⇒ **3 İLERİ, PUSH YOK** · ağaç **TEMİZ** · `.git/index.lock` **YOK**. Push borcu: `ab1ee61` (K43) · `648da7b` (araç) · `1b1b505` (DESIGN.md). **PUSH ONUR'DA.**

**DOSYA KİMLİKLERİ (`sha256` ilk 8, kapanış ölçümü):**
`PROJE_HAFIZA.md` 453.863 b · `F001E102` — `DESIGN.md` 15.742 b · `534DFF68` — `CLAUDE.md` 5.651 b · `DBFF6727` — `PROJE_RADAR.jsonl` 11.849 b · `0B98FA3B` (12 kayıt) — `araclar/design-token-kapisi.py` 21.229 b · `11667526` — **`araclar/adr-kapi-taramasi.py` 50.582 b · `A22841F2` (K34-f TUTUYOR)** — `araclar/radar.py` 15.252 b · `688B6D31` — `.mcp.json` 155 b · `01EC4807` — **v7 300.930 b · `2AE2FB29` (K41: DONDURULDU)** — **kanonik v6 241.689 b · `6F6BE71A` (DOKUNULMADI)**.
⚠ **SATIR SAYISI TUZAĞI:** PowerShell `Measure-Object -Line` **boş satırları saymaz** (hafıza 1117 der, gerçek 1257). **Kimlik `sha256`+bayttır**, satır değil.

**ÜÇ PAZARLIKSIZ KİLİT (ihlal eden el K41/K42/K44'ü çiğner):**
1. **K41 — v7 DONDURULDU.** Yeniden açılması üçünün BİRLİKTE sağlanmasına bağlı: ADR aracının 2. onarımı koşmuş olmalı (AYRI EL, K34-f) · `slice-3b` görünen çıktısı > %0 · **Onur açıkça açmalı.**
2. **K42-d — TAÇ MÜCEVHER DİLİMİ, DÖRT ADIM, ATLANMAZ:** (1) ✅ **BİTTİ** — Docker/Postgres + verify **ÖLÇÜLDÜ ve GEÇTİ** → (2) Flutter iskelet (Android+Web) + Drift + **tam çevrimdışı CRUD** ⇒ **R5 burada düşer** → (3) senkron kuyruğu + `POST /v1/sync` → (4) SignalR `SyncHub`.
3. **K44-a — ÖNCE ARAÇ, SONRA BELGE.** Uygulandı: `design-token-kapisi.py` **12/12** altın küme ile doğdu, `DESIGN.md` sonra yazıldı.

**SONRAKİ İŞ — `GOREV-slice-3b` SPEC'İ (temiz oturum önerildi; %47'de yazmak 🟡 riski taşıyordu).** Spec'e **adıyla** girecek altı kalem:
**(a)** **K44-b'nin dört ayaklı MCP kapısı** — `flutter_driver_command` ekran görüntüsü · `widget_inspector` widget ağacı · **kasten bozuk widget'la `get_runtime_errors` ISIRTILIR** · `dtd` bağlantısı. **Dördü de kırmızı vermeden kapı geçmiş SAYILMAZ.**
**(b)** **`flutter_driver` paketi `pubspec` ZORUNLULUĞU** — yoksa (a) fiziksel olarak imkânsız (resmî doküman).
**(c)** **Paket CVE kapısı** — `pub_dev_search` **CVE döndürmüyor**; lisans ayağı geçti, **CVE ayağı GEÇMEDİ** (kırmızı çizgi #3'ün açık yarısı).
**(d)** **`sqlite3_flutter_libs 0.6.0+eol` ELENMESİ** (EOL, Claude Code ölçtü).
**(e)** `DESIGN.md` §11'in **A‑1…A‑6** açık kalemleri.
**(f)** **A11Y‑1…A11Y‑7 mutantla ısırtılır** (bugün hiçbiri koşulmadı, 0 `.dart`).

**PAKET ADAYLARI (ham `pub_dev_search`, SEÇİM YAPILMADI):** `drift 2.34.2` MIT · `sqlite3 3.5.0` MIT · `drift_flutter 0.3.1` MIT · SignalR: `signalr_netcore 1.4.4` MIT (lider, 230 like/74k indirme) · `signalr_core 1.1.2` MIT · `signalr_flutter 0.2.1` MIT.

**ORTAM (26 Tem ölçümü — 18 Tem tarihli ORTAM bloğunun ÜSTÜNDEDİR):** Flutter **3.44.6** · Dart **3.12.2** · **Android SDK 36.1.0 ✓** · Chrome/web ✓ · Windows masaüstü **☠** (`%PROGRAMFILES(X86)%` yok) · .NET **9.0.316** · Docker **29.6.1 AYAKTA**, `momentum-postgres` **Up (healthy)** · `dart` MCP **project scope, ✔ Connected, 13 araç (v0.1.4, DENEYSEL)**.

**KANLA YAZILI ORTAM UYARILARI:** Claude Code **DAİMA `Momentum` kökünden** açılır (üst klasörden açmak `.mcp.json`'u görünmez kılar; oradaki `npx dart-mcp-server` mayını temizlendi) · `kasif` skill'ini **Cowork ÇAĞIRAMAZ**, Onur `/kasif` yazar · commit mesajına **çift tırnak YAZMA** (PowerShell argümanı böler, commit sessizce düşer) · `.ps1`'e **Türkçe yol literali YAZMA** (ANSI okunur, `Test-Path` sessizce `False`) · git'te **`--no-optional-locks` ZORUNLU**, commit yalnız **Desktop Commander** ile, `device_bash`/mount ile **YASAK** · **PUSH ONUR'DA**.

**RADAR:** `docs/ADR/0003` **KIRMIZI** (R1·R5·R2b·R4 — **PARK EDİLDİ, beklenen**) · `slice-3b-istemci` **KIRMIZI (R3)**. ⚠ **Genel hüküm KIRMIZI ama ÇALIŞILACAK ARTEFAKTIN KENDİ SATIRI okunur** — park edilmiş artefakt genel hükmü kirletiyor, bu **aracın sınırıdır**. ⚠ **R3'ün asgari örneklem koruması YOK**: `kapatılan=1/üretilen=1` olan her dürüst tur KIRMIZI verir (bu oturumda 2 kez). **Eşik SUSTURULMADI**; borç **AYRI ELE** yazıldı.

**AÇIK `[DOĞRULANMADI]` (5):** flutter_secure_storage Windows şifrelemesi · WebKit `http://localhost` `__Host-` · Isopoh lisans ailesi · NIST SP 800-38D · 🆕 Web'de `textScaler`/tema davranışının Android'den farkı.

**GEÇİCİ ARTIKLAR (repo DIŞINDA, silme Onur'da — kırmızı çizgi #4):** `…\TO DO LİST\` altında 11 × `_cowork_*.ps1` · `%TEMP%\_cw_*.ps1|py` · `%TEMP%\_cw_probeproj\`.

**[K21 KAPANIŞ] 474.084 token = %47,4 🟢.** Eğri: 149k → 191k → 287k → 373k → 426k → **474k**. ⚠ **Ölçüm kaynağı bu oturumda DEĞİŞTİ:** bulutta koşuldu ⇒ transcript **Onur'un makinesinde değil**, konteynerde (`/root/.claude/projects/-home-claude/<id>.jsonl`). **Devir bağlam yüzünden DEĞİL, DİSİPLİN gereği verildi:** spec bu cephenin en kritik artefaktıdır ve %47'den başlarsa 🟡'nin içinde biter.

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 28: **K45 — MEKANİK KAPI YAZILDI VE KENDİNİ KANITLADI (12/12); `DESIGN.md` v1 YAZILDI; KONTRAST ÖLÇÜLDÜ VE BİR KUSUR BELGE YAZILMADAN KAPANDI**)

- **✅ ARAÇ: `araclar/design-token-kapisi.py` v0.1.0** — **21.229 bayt · `sha256 11667526…` · `U+FFFD` 0 · CRLF 0.** K44-a gereği **belgeden ÖNCE** yazıldı. *(⚠ Bu satır ilk yazımında **18.647 bayt · 524 satır** diyordu ve **erteleme düzeltmesinden sonra BAYATLADI**; kapanış ölçümünde yakalanıp düzeltildi — `kanonik-kopya` sınıfının bu belgedeki canlı örneği.)*
  **ALTIN KÜME 12/12 GEÇTİ · `EXIT=0`:** boş girdide susar · temiz projede susar · kullanılmayan `MUST`'ta **D1** ısırır · ham renk literalinde **D2** ısırır · kullanılmayan `NICE`'a susar (yanlış-pozitif kontrolü) · tanımsız sembolde **D3** ısırır · **YORUM içindeki** literale susar (yanlış-pozitif kontrolü) · gerekçesiz muafiyette **D4** ısırır · gerekçeli muafiyete susar · bozuk `tokens` satırında **D0** ısırır · **belge var/0 dart'ta D1+D3 ERTELENİR ve susar** · **mutant: kod yokken bile bozuk blokta D0 YİNE ISIRIR.**
  **ÜÇ ÖLÇÜLMÜŞ DERS UYGULANDI:** (1) makine-okunur veri **prozaya değil** `DESIGN.md` içindeki **TEK `\`\`\`tokens` bloğuna** yazılır — K38-b'nin doğrudan uygulaması; araç çevresindeki metni **okumaz**. (2) çıktı **saf ASCII** ⇒ **`cp1254` kalkanı ÖLÇÜLDÜ: hiçbir `PYTHONUTF8`/`PYTHONIOENCODING` ayarı olmadan koştu ve çökmedi** — ADR aracının bilinen çökme sınıfı burada **doğmadı**. (3) bozuk UTF-8 girdi ⇒ **exit 3** (bulgu değil, ortam hatası).
  **BEYAN EDİLMİŞ SINIR:** `yorum_disi()` tam bir Dart ayrıştırıcısı **değildir** ⇒ **çok satırlı `/* */` içindeki ham literal KAÇABİLİR**; altın kümede bu vaka **YOKTUR** — adlandırılmış borç (A-4).
- **🔴 ARACIN İLK SÜRÜMÜ BİR KUSURLA DOĞDU VE AYNI TURDA KAPANDI [gizlenmiyor]:** v0.1.0 ilk hâli, `DESIGN.md` yazılır yazılmaz **25 bulgu** verdi (24×`D1` + 1×`D3`) — çünkü henüz **0 `.dart`** var. Bu **kusur değil ÖLÇÜLEMEZLİKTİ**; her `MUST` token için bir **yanlış-pozitif taban çizgisi** üretiyordu ve **sürekli kırmızı yanan kapı görmezden gelinir** (ADR aracının öğrenilmiş dersi). **Düzeltme:** yeni durum **`BELGE-VAR-KOD-YOK`** ⇒ D1/D3 **ERTELENİR**, hüküm **BEKLEMEDE (`exit 0`)** ve *"bu bir TEMİZ hüküm DEĞİLDİR"* **açıkça yazılır**. **Ertelemenin kapıyı körleştirmediği MUTANTLA kanıtlandı** (vaka 12): kod yokken bile bozuk blok **D0** verir. *Bu, K33 örüntüsünün dokuzuncu kanıtıdır ve yine **koşulan araç** buldu, akıl yürütme değil.*
- **✅ BELGE: `DESIGN.md` v1 YAZILDI** — 11 bölüm: §0 iki pazarlıksız kısıt · **§1 `tokens` bloğu (32 token: MUST 24 / NICE 8)** + koyu tema karşılıkları · §2 a11y (kontrast tablosu + A11Y‑1…A11Y‑7) · §3 bileşen envanteri (MUST 9 / NICE 8) · §4 **durum matrisi (8 durum)** · §5 hareket · §6 ikonografi · §7 imza öğeler (K10-e) · §8 ban-list (7 madde) · §9 dosya sözleşmesi · §10 kapı · §11 açık kalemler (A‑1…A‑6).
  **KAPI KOŞULDU:** `D0` **temiz** (32 token doğru ayrıştı) · hüküm **BEKLEMEDE `exit 0`** · **24 MUST vaadi ERTELENDİ** ve bu **beyan edildi**.
- **🔴 KONTRAST İDDİA EDİLMEDİ, ÖLÇÜLDÜ — VE ÖLÇÜM BİR KUSUR BULDU [belge yazılmadan ÖNCE]:** W3C bağıl-parlaklık formülü iki tema için koşuldu. **İlk palet KALDI:** tek `renk.kenarlik` token'ı **1,58:1 (açık) / 1,65:1 (koyu)** ile WCAG 1.4.11'in **3:1** eşiğini geçemedi. **Token İKİYE AYRILDI:** `renk.ayirici` (**dekoratif**, 1.4.11 kapsam dışı, **beyan edilmiş muafiyet**, 1,31/1,42) + `renk.kenarlik.etkilesim` (**kontrolü tanımlayan**, ≥3:1 zorunlu ⇒ ölçülen **4,54 / 6,08**). **Yeniden ölçüm: 24 çiftin 24'ü GEÇTİ, iki temada da 0 kalan.** *Bu, "ölç ya da `[DOĞRULANMADI]` yaz" kuralının bir tasarım kararını fiilen değiştirdiği ilk vakadır.*
- **📡 RADAR (K44 sonrası koşuldu):** **`slice-3b-istemci` 🟢 YEŞİL'e döndü** (*"bulgu yok — ilerleme ölçülüyor"*) ⇒ **R3 mekanikleştirmeyle KAPANDI.** `docs/ADR/0003` KIRMIZI (park, değişmedi). Genel hüküm KIRMIZI — **yalnız park edilmiş artefakt yüzünden** (K42'de adı konmuş araç sınırı).
- **🔴 RADARIN ÜÇÜNCÜ ÖLÇÜLMÜŞ SINIRI [adlandırılmış borç — AYRI EL, bu el eşiğe DOKUNMADI]:** **R3 küçük örneklemde her turda tetiklenir.** Eşik *"üretilen ≥ kapatılanın %50'si"* olduğu için **kapatılan=1 / üretilen=1** olan her dürüst tur KIRMIZI verir. Bu oturumda **iki kez** yaşandı (K44, K45). ⇒ **R3'ün asgari örneklem koruması yok** (ör. `kapatilan >= 4` olmadan hüküm vermemesi). **Eşiği SUSTURMADIM** — eşiği değiştiren altın kümeye vaka eklemek zorundadır (K40) ve aracı susturmak kapı tiyatrosu olurdu. **Borç: `proje-radari` + `araclar/radar.py` — AYRI EL.** *Yürürlükteki okuma: R3'ün bu artefakttaki hükmü **düşük bilgi taşır**; R1/R5/R2b taşımaya devam eder.*
- **Sıradaki ilk iş:** **`GOREV-slice-3b` SPEC'İ** — içine **adıyla** girecekler: **(a)** K44-b'nin **dört ayaklı MCP kapısı** (`flutter_driver_command` ekran görüntüsü · `widget_inspector` widget ağacı · kasten bozuk widget'la `get_runtime_errors` ISIRTILIR · `dtd` bağlantısı) **(b)** `flutter_driver` paketi **pubspec zorunluluğu** (yoksa (a) fiziksel olarak imkânsız) **(c)** paketler için **CVE kapısı** (`pub_dev_search` CVE **döndürmüyor** — lisans ayağı geçti, CVE ayağı **GEÇMEDİ**) **(d)** `sqlite3_flutter_libs 0.6.0+eol` **elemesi** **(e)** `DESIGN.md` §11'in **A‑1…A‑6** kalemleri **(f)** A11Y‑1…A11Y‑7'nin **mutantla ısırtılması**. → Claude Code build → Cowork bağımsız doğrular → radar yeniden koşulur, **R5'in düştüğü ÖLÇÜLÜR**. **v7'ye DOKUNULMAZ (K41).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 28: **K44 — RADAR `slice-3b`'Yİ DE ISIRDI (R3); KİLİT = `MEKANİKLEŞTİR`, ÖNCE ARAÇ SONRA BELGE. MCP ÇEKİRDEĞİ HÂLÂ KÖR KAPIDIR**)

- **📡 RADAR (K43 commit'inden sonra koşuldu):** `docs/ADR/0003` **KIRMIZI** (park, değişmedi) · **🔴 `slice-3b-istemci` KIRMIZI** — **R3: YAZIM KENDİ KUSURUNU ÜRETİYOR** (*son tur kapatılan **1** / üretilen **1**; eşik: kapatılanın %50'si*). **Tetikleyen kusur COWORK'ÜN KENDİSİNİNDİR** (K43'teki yanlış yetenek listesi) ve deftere **dürüstçe** yazıldığı için araç onu gördü.
  ⚠ **BEYAN EDİLMİŞ SINIR (eşiğe DOKUNULMADI):** R3 iki turluk bir artefaktta **1/1**'den tetiklendi ⇒ **küçük-sayı duyarlı**, istatistiksel olarak zayıf bir sinyaldir. **Eşik yine de gevşetilmedi:** eşiği değiştiren, altın kümeye yeni vaka eklemek zorundadır (K40) ve eşiği susturmak aracı kasten körleştirmek olurdu. Sinyal zayıf ama **yalan değil** — kusuru gerçekten bu el üretti.
- **[KARAR — 26 Tem 2026] K44-a: DÖRT ŞIKTAN **`MEKANİKLEŞTİR`**. `araclar/design-token-kapisi.py` **ÖNCE** yazılır ve altın kümesinde kendini kanıtlar; `DESIGN.md` **SONRA** ve **aracın ölçebildiği biçimde** yazılır.** *(Usul: Onur çatalı yine Cowork'e devretti — birebir *"bunun seçimi sana bırakıyorum"*; K40'ın *"kilit Onur'dan gelir"* kuralı **devir yoluyla** karşılandı, kapı-7 itiraz edebilir.)*
  *Gerekçe [ÖLÇÜLDÜ]:* **bu, R3'ün KENDİ devre kesicisinin listelediği (3) numaralı çözümdür** ⇒ devre kesici **ihlal edilmiyor, uygulanıyor**. K42-b zaten *"belge aracıyla BİRLİKTE doğar"* diyordu; K44-a sırayı **sertleştiriyor**: araç önce doğarsa belge **ilk satırından itibaren** ölçülür ve *"yazım kendi kusurunu üretiyor"* döngüsünün girdisi daralır. **Örüntü kanıtı zaten elimizde:** mekanikleştirilen sınıf (`kanonik-kopya` · `tablo-hucre`) bir daha geri gelmedi; prozada kalan sınıf her turda geri geldi (K38).
  *Bedel [adlandırılmış]:* `DESIGN.md` **yarım gün gecikir** · araç, **henüz var olmayan** token'lara karşı yazılmak zorunda ⇒ **boş girdide SUSMASI** ayrı bir altın küme vakasıdır (yoksa ilk koşumda her şeye kusur der = yanlış-pozitif tabanı doğar).
  *Reddedilenler [adlandırılmış]:* **DEVAM — R3'ü küçük-sayı gürültüsü say** (yarım gün kazanılırdı; ama devre kesici **ilk kez gerekçeyle geçilmiş** olur ve **EMSAL DOĞAR** — bundan sonra her kırmızı için *"bu da gürültü"* denebilir; ADR 0003'ü sekiz tura sokan tam olarak bu tür meşrulaştırmalardır) · **DARALT — `DESIGN.md` tek sayfa MUST'a çekilsin** (R3'ü **ve** R4'ü birlikte düşüren en güçlü yol, yazılacak satır azalınca üretilecek kusur da azalır; ama Onur'un **bir saat önce kilitlediği K42-b geri alınmış olur** ve hareket/ikonografi/boş-durum tasarımları spec'e kayar).
- **🔴 K44-b — MCP ÇEKİRDEĞİ HÂLÂ KÖR KAPIDIR [adlandırılmış borç, gizlenmiyor]:** Claude Code'un ikinci raporu (Momentum kökünden, doğru sunucuyla) **geldi ve kabul edildi**: araç listesi sayıldı, **`analyze_files`** koştu (*"No errors"* — 0 Dart dosyası, dürüst), **`pub_dev_search` ×2** koştu (ham paket/lisans verisi geldi). ⇒ **KURULUM DOĞRULAMASI KAPANDI.**
  **AMA:** sunucuyu *zorunlu* kılan **çekirdek beş araç — `widget_inspector` · `flutter_driver_command` · `get_runtime_errors` · `hot_reload` · `dtd` — LİSTEDE GÖRÜLDÜ, HİÇ KOŞULMADI.** Koşulamazlardı: beşi de **çalışan bir Flutter uygulaması** ister, bugün **0 `.dart`** var. ⇒ **Tanım gereği KÖR KAPI: ısırdıkları KANITLANMAMIŞTIR.**
  **BORÇ, ADRESİYLE:** `GOREV-slice-3b` spec'ine **kapı maddesi** olarak girer — *ilk ekran ayağa kalkar kalkmaz **(1)** `flutter_driver_command` ile ekran görüntüsü alınır **(2)** `widget_inspector` ile widget ağacı çekilir **(3)** kasten bozulmuş bir widget ile `get_runtime_errors` **ISIRTILIR** **(4)** `dtd` ile çalışan uygulamaya bağlanılır.* **Dördü de kırmızı vermeden bu kapı geçmiş SAYILMAZ.** Ayrıca `flutter_driver` paketi `pubspec`'e **eklenmezse** (2) ve (3) fiziksel olarak imkânsızdır (K43).
- **Sıradaki ilk iş:** **(1)** `araclar/design-token-kapisi.py` + **altın küme** (boş girdide susar · eksik MUST token'da ısırır · NICE'ta susar · mutasyonla zorlanır) → **(2)** `DESIGN.md` (aracın ölçebildiği biçimde, K42-b'nin kullanım kısıtıyla) → **(3)** `GOREV-slice-3b` spec'i (**K44-b'nin dört ayaklı MCP kapısı + `flutter_driver` zorunluluğu + paket CVE kapısı + `sqlite3_flutter_libs` EOL elemesi** adıyla) → Claude Code build → Cowork bağımsız doğrular → **(4)** radar yeniden koşulur, **R5 ve R3'ün düştüğü ÖLÇÜLÜR**. **v7'ye DOKUNULMAZ (K41).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 28: **K43 — `dart` MCP SUNUCUSU KURULDU VE ARAÇ KÜMESİ ÖLÇÜLDÜ; COWORK'ÜN KENDİ İDDİASI YANLIŞLANDI**)

> ⚠ Kaşif (`/kasif`) Onur tarafından koşuldu (Cowork onu **çağıramaz** — aşağıdaki ortam uyarısı). Rapor: kurulacak **tek** şey `dart` MCP sunucusu; gerisi mevcutla devam.

- **✅ KURULDU (Onur'un elinden, Cowork bağımsız doğruladı):** `claude mcp add --scope project --transport stdio dart -- dart mcp-server` ⇒ `Momentum/.mcp.json` **repoda ve commit'li** (`2ee2c24`). Doğrulama (`claude mcp get dart`, `Momentum` içinden): **Scope: Project config · Status ✔ Connected · Command `dart` · Args `mcp-server`.** `origin/main` ile **0 0** ⇒ push borcu yok.
- **🔴 ORTAM MAYINI — ÖLÇÜLDÜ VE TEMİZLENDİ [yeni el zaman kaybetmesin]:** ilk koşumda Claude Code *"Failed to connect / Connection closed"* verdi ve kaydı **`command: npx · args: ["-y","dart-mcp-server"]`** diye gördü. **Bu BİZİM kaydımız DEĞİLDİ.** `~/.claude.json` ayrıştırıldı: o kayıt **local scope**'ta ve **BİR ÜST KLASÖRE** aitti — `C:/Users/gulci/Desktop/MEMO ÖDEV PROGRAMLAR/TO DO LİST`. ⇒ **Claude Code `Momentum`'dan değil, üst klasörden koşuyordu** ve `Momentum/.mcp.json`'u hiç görmedi. Ayrıca `npx dart-mcp-server` **tamamen başka bir paket**: `its-dart/dart-mcp-server` = itsdart.com görev-yönetimi SaaS'ı, `DART_TOKEN` istiyor, Flutter tooling ile ilgisi **yok** — klasik **ad çakışması** (Dart dili ⟷ itsdart.com). **Çözüm koşuldu:** üst klasörde `claude mcp remove dart -s local` ⇒ orada artık *"No MCP server named dart"*. **KURAL: Claude Code DAİMA `Momentum` kökünden açılır** (kanonik kök); üst klasörden açmak proje kapsamlı `.mcp.json`'u görünmez kılar.
- **🔴 COWORK KENDİ İDDİASINI YANLIŞLADI — GİZLENMİYOR [K13-a; kaşif raporu da bir ÜRETİMDİR]:** kaşif raporunda sunucunun yetenekleri diye verilen liste (`run_tests`, `list_devices`, `dart_format`, `launch_app`, `dart_fix`, `get_app_logs`, `stop_app`, `create_project`…) **`dart mcp-server --help`'in `--enable/--disable` ÖZELLİK/KATEGORİ listesinden** okunmuştu ve **araç listesi sanılmıştı.** Claude Code çalışan sunucuda **13 araç** gördü ve *"beyan doğrulanmadı"* dedi — **haklıydı.**
  **BAĞIMSIZ ÖLÇÜM (Cowork, kendi probe'u — `dart mcp-server`'a doğrudan JSON-RPC `initialize` + `tools/list`):** `serverInfo: dart and flutter tooling · **v0.1.4**`. **A) roots BOŞ ⇒ 13 araç · B) roots = GERÇEK bir Dart projesi (`dart create -t console`) ⇒ 13 araç · FARK YOK.** ⇒ eksik araçlar **bağlama bağlı değil, bu sürümde HİÇ YOK.**
  **VAR OLAN 13:** `analyze_files · dtd · flutter_driver_command · get_runtime_errors · hot_reload · hot_restart · lsp · pub · pub_dev_search · read_package_uris · rip_grep_packages · roots · widget_inspector`.
  **KARAR DEĞİŞMİYOR, GEREKÇE DARALIYOR [ölçülü]:** eksik olanların **tamamı kabukla yapılabilir** (`flutter test` · `dart format` · `flutter devices` · `flutter run`) ve backend'de `verify.ps1` zaten böyle çalışıyor ⇒ kayıp yok. **Kabuğun YAPAMADIĞI ve bu yüzden sunucuyu ZORUNLU kılan çekirdek:** `widget_inspector` (widget ağacı) · `flutter_driver_command` (tıkla/yaz/kaydır/**ekran görüntüsü**) · `get_runtime_errors` · `dtd` (çalışan uygulamayı bul+bağlan) · `hot_reload`. **Bunlar olmadan istemci tarafında "kör kapı yok" doktrini uygulanamaz.**
  ⚠ **v0.1.4 DENEYSELDİR** (resmî dokümanın kendi beyanı) ⇒ araç kümesi sürümle **değişebilir**; *"13 araç"* **26 Tem 2026 tarihli bir ölçümdür**, kalıcı bir olgu değildir. Yeni el **yeniden ölçmelidir**.
- **🆕 SPEC'E GİREN ZORUNLU KALEM [bu ölçümün doğurduğu]:** resmî doküman, `flutter_driver_command`'ın çalışması için uygulamaya **`flutter_driver` paketinin eklenmesi** gerektiğini söylüyor. **Eklenmezse UI kapısı KÖR KAPI olur** ⇒ `GOREV-slice-3b` spec'ine **adıyla** girer ve **mutantla ısırtılır**.
- **📦 PAKET ÖLÇÜMÜ (Claude Code'un `pub_dev_search` HAM çıktısı — seçim YAPILMADI, spec'te kapıdan geçecek):** `drift 2.34.2` **MIT** (2436 like · 1.043.799 indirme) · `sqlite3 3.5.0` **MIT** · `drift_flutter 0.3.1` **MIT** · ⚠ `sqlite3_flutter_libs 0.6.0+eol` **MIT ama EOL** (elenecek aday) · SignalR: `signalr_netcore 1.4.4` **MIT** (230 like · 74.242 indirme, lider) · `signalr_core 1.1.2` MIT · `signalr_flutter 0.2.1` MIT · `signalr_client 0.1.6` MIT (**pub points 30 — düşük**) · `signalr_hub 2.0.2` MIT. **Hepsinin lisans alanı DOLU ⇒ kırmızı çizgi #3'ün lisans ayağı geçti.** 🔴 **CVE ayağı GEÇMEDİ:** `pub_dev_search` CVE bilgisi **döndürmüyor** ⇒ **ayrı bir CVE kapısı gerekir** (backend'deki `dotnet list package --vulnerable` muadili); bu **adlandırılmış açık kalemdir**. ⚠ *"son yayın tarihi"* alanı da dönmüyor — Code bunu **uydurmadı**, doğru davrandı.
- **🧭 ORTAM: `kasif` SKILL'İ COWORK TARAFINDAN ÇAĞRILAMAZ** — hesapta **VAR ve `enabled: true`** (`ListSkills` ⇒ `skill_01X3E1BHTZ7jVAkRA4RweRJu`) ama **`disable-model-invocation`** taşıyor (*"cannot be used with Skill tool"*). **Yalnız Onur `/kasif` yazarak tetikler.**
- **📄 GEÇİCİ ARTIKLAR (repo DIŞINDA, silme Onur'da — kırmızı çizgi #4):** `…\TO DO LİST\` altında **11 adet `_cowork_*.ps1`** · `%TEMP%\_cw_probeproj\` (probe için `dart create` ile üretilen tek kullanımlık konsol projesi) · `%TEMP%\_cw_*.ps1|py` ölçüm betikleri. **Hiçbiri `Momentum/` içinde değil ⇒ `git status` etkilenmiyor (ölçüldü: ağaç temiz).**
- **[K21 ÖLÇÜMÜ] 373.127 token = %37,3 ⇒ 🟢 YEŞİL.** Eğri (bu oturum): 149k → 191k → 287k → **373k**.
- **Sıradaki ilk iş:** **(2)** `DESIGN.md` + **`araclar/design-token-kapisi.py`** altın kümesi (K42-b'nin iki pazarlıksız kısıtı) → **(3)** `GOREV-slice-3b` spec'i (**`flutter_driver` zorunluluğu + paket CVE kapısı + EOL elemesi adıyla**) → **Claude Code build** → **Cowork bağımsız doğrular** → **(4)** radar yeniden koşulur, **R5'in düştüğü ÖLÇÜLÜR**. **v7'ye DOKUNULMAZ (K41).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 28: **K42 — `slice-3b` DÖRT ÇATAL KİLİTLENDİ; CEPHE ARTIK İSTEMCİDİR**)

> ⚠ Bu blok **K41'den SONRA** yazıldı ve onu **DEĞİŞTİRMEZ, UYGULAR.** K41 v7'yi dondurdu; K42 yeni cepheyi tanımlar. Dosya yazımdan önce diskten **yeniden okundu**, benzersiz çıpayla insert edildi.

- **🔴 ORTAM DURUMU BAYAT ÇIKTI — ÜÇ SATIR BU OTURUMDA ÖLÇÜLEREK DÜZELTİLDİ** *(kanonik ORTAM bloğu 18 Tem tarihlidir; aşağıdakiler 26 Tem ölçümüdür ve ONUN ÜSTÜNDEDİR)*:
  · **Android toolchain ✓ ÇALIŞIYOR** — `flutter doctor` **[√] Android SDK 36.1.0** + Android Studio JDK. Hafızanın *"cmdline-tools + lisanslar eksik"* notu **ARTIK DOĞRU DEĞİL.**
  · **Docker ÇALIŞIYOR** — `docker --version` **29.6.1**, `docker info` **ServerVersion 29.6.1 · OSType linux** ⇒ **daemon AYAKTA, konteyner sayısı 0.** WSL2'de `Ubuntu` + `docker-desktop` dağıtımları kurulu. Hafızanın *"Docker ertelendi, WSL2+reboot gerekli"* notu **KAPANDI: reboot borcu YOK.**
  · **Windows masaüstü HÂLÂ ☠** — `flutter doctor` VS kontrolü `%PROGRAMFILES(X86)%` ortam değişkeni bulunamadığı için **çöküyor** (kusur değişmedi).
  · **Chrome/web ✓** · Flutter **3.44.6** stable · .NET **9.0.316**.
- **📐 BACKEND YÜZEYİ ÖLÇÜLDÜ (kaynaktan, `src/backend`):** `POST /v1/sync` · `GET /v1/tasks` · `GET /v1/tasks/{id}` · `GET /v1/task-lists` · `GET /ping` · SignalR **`MapHub<SyncHub>`**. **Kimlik/yetki izleri: `[Authorize]` ×1 · `AllowAnonymous` ×3** ⇒ **istemci bugün `slice-3c-auth`'u BEKLEMEDEN konuşabilir** (ADR 0002'nin *"mekanizma auth diliminde; o zamana dek deny-by-default"* hükmü yürürlükte). 4 proje: `Momentum.Api` · `Momentum.Application` · `Momentum.Domain` · `Momentum.Infrastructure`.
- **[KARAR — Onur, 26 Tem 2026] K42-a: PLATFORM = **ANDROID + WEB**.** *Gerekçe:* ikisi de bugün **ölçülerek** derlenebilir durumda; Windows masaüstü aday değil (VS kontrolü çöküyor), iOS zaten **yalnız CI'da** derlenebilir (Mac yok). *Bedel [adlandırılmış]:* SQLite ⟷ IndexedDB kalıcılık farkı ve `__Host-` çerezi / WebKit davranış farkları **ilk turda birden** ödenir; test yüzeyi iki katına çıkar. *Reddedilenler:* **yalnız Android** (çevrimdışı vitrinin en doğal yeri; ama tıklanabilir link yok, değerlendirici APK kurmak zorunda ve ADR 0003'ün adıyla kararlaştırılmış web davranışları hiç sınanmaz) · **yalnız Web** (sıfır kurulum, en hızlı görünür çıktı; ama çevrimdışı hikâyesi web'de en zayıf yerdedir ve mobil iddiası kanıtsız kalır).
- **[KARAR — Onur, 26 Tem 2026] K42-b: `DESIGN.md` = **TAM TASARIM SİSTEMİ** (token + bileşen envanteri + durum matrisleri + hareket + ikonografi + imza öğeler, **MUST/NICE bölünmüş — K10-e**).**
  🔴 **COWORK'ÜN İTİRAZI, KAYDA GEÇİYOR [Onur kilitledi, itiraz kararı DEĞİŞTİRMEZ ama yazılır]:** bu seçenek, radarın **R2b** (*koşulamayan spec*) ve **R4** (*belge büyümesi*) kurallarının ADR 0003'te **ölçülmüş** tuzağının birebir aynısıdır: kod üretmeyen bir belge büyür ve her tur yeni çapraz-atıf yüzeyi doğurur. **Onur'un seçtiği dörtlü (tam sistem + CI sonra) ürettiği proza/kod oranı en yüksek bileşimdir ve bu, 20 günlük takvimin en büyük tek riskidir.**
  ⚖ **BU YÜZDEN İKİ PAZARLIKSIZ KISIT — K42-b'nin uygulanma koşuludur:**
  **(1) KULLANIM KISITI:** `DESIGN.md`'deki **her MUST token/bileşen, ilk dikey dilimde FİİLEN kullanılır.** Kullanılmayan her şey **NICE**'tır ve **tek satır** yer kaplar. *(K10-e'nin MUST/NICE bölünmesi böylece bir üslup tercihi değil, ölçülebilir bir kısıt olur.)*
  **(2) ÖLÇÜM ARACI KISITI — belge aracıyla BİRLİKTE doğar:** **`araclar/design-token-kapisi.py`** yazılır; `DESIGN.md`'deki **her MUST token'ın Flutter kodunda fiilen kullanıldığını ÖLÇER** ve **kullanılmayan MUST = KUSUR** verir. **Araç önce ALTIN KÜMEDE kendini kanıtlar** (temizde susar, mutasyonda ısırır — **KÖR KAPI YOK**), ancak ondan sonra hüküm verir. ⇒ Belge **prozadan çıkar, KOŞULABİLİR olur** ve R2b'nin bu belge için tekrarlaması **yapısal olarak** engellenir.
  *Reddedilenler:* **minimum tek sayfa MUST** (R4'ü yapısal olarak önlerdi; ama hareket/ikonografi/boş-durum tasarımları spec'e kalırdı) · **şimdi yazma, koddan türet** (radarın ana dersinin en saf uygulaması; ama K10 *"tasarım anayasası"* der ve ilk ekran anayasasız çizilirdi, `design:design-critique` kapısı geç koşardı).
- **[KARAR — Onur, 26 Tem 2026] K42-c: CI (`.github/workflows`) BU DİLİMDE **KURULMAZ**; dilim bitince kurulur.** *Gerekçe:* dikkat tek cephede kalır, R5 en hızlı düşer. *Bedel [adlandırılmış]:* **iOS derleme iddiası kanıtsız kalır** — Mac YOK ⇒ iOS **yalnız CI'da** derlenebilir, dolayısıyla bugün *"kod-hazır"* beyanı **ölçülmemiş** bir beyandır · yeşil rozet vitrini gecikir · CI sonradan kurulunca **geçmiş commit'ler hiç doğrulanmamış** olur.
- **[KARAR — K42-d: Onur çatalı COWORK'E DEVRETTİ ("en verimli ve ilerleyebileceğimiz yolu seç"); seçim ve gerekçesi kayda geçiyor] KAPSAM = **TAÇ MÜCEVHER DİLİMİ, DÖRT ADIMLI** — her adım TEK BAŞINA demo edilebilir.**
  **Sıra [PAZARLIKSIZ, adım atlanmaz]:** **(1)** Docker/Postgres + backend **ayağa kalkar ve ÖLÇÜLÜR** (beyan değil: `/ping` + `GET /v1/tasks` gerçek yanıt vermeli) → **(2)** Flutter iskelet (Android+Web) + **Drift/SQLite** + **TAM ÇEVRİMDIŞI CRUD** ⇒ **uygulama sunucusuz çalışır, GÖRÜNEN ÇIKTI burada doğar ve R5 ERKEN DÜŞER** → **(3)** senkron kuyruğu + `POST /v1/sync` ⇒ çevrimdışı-öncelikli vitrin → **(4)** SignalR `SyncHub` aboneliği ⇒ gerçek zamanlı vitrin.
  *Gerekçe [ÖLÇÜLDÜ]:* Docker/Postgres **her** şıkta gerekiyordu (backend'e konuşan her yol için) ⇒ ondan kaçınmanın tek yolu *"yerel-önce, sunucusuz"* şıkkıydı; **o şık bu dilimin 2. ADIMI yapılarak hem düşük riski hem tam vitrin ALINDI.** Ayrıca ödevin ilan edilmiş **iki taç mücevheri** (çevrimdışı-öncelikli senkron + gerçek zamanlı işbirliği) yalnız bu dilimde **birlikte** görünür ve backend'in dört ucu da **bugün hazırdır**.
  *Bedel [adlandırılmış]:* en büyük dilimdir; istemci tarafı HLC/delta ve çakışma UI'ı yeni kod sınıflarıdır; **adım 3-4 takvime yetişmezse dilim YARIM kalır.** *Bu bedelin telafisi sıranın kendisidir:* adım 2 bitmişse elde **çalışan bir çevrimdışı uygulama** vardır ve diğer üç şıkkın hiçbirinde bu emniyet yoktur.
  *Reddedilenler:* **okuma-önce iskelet** (en düşük risk, 1-2 gün; ama vitrin *"boş kabuk"* olur ve kazanılan zaman sonraki dilimde geri ödenir) · **web vitrin-önce** (tıklanabilir link + CI iddiasını birlikte kanıtlardı; ama K42-c ile CI zaten ertelendi, backend'i internete açmak gerekirdi ve çevrimdışı vitrini web'de en zayıf yerdedir) · **yerel-önce sunucusuz** (backend'in 141 `.cs`'lik işi vitrinde hiç görünmezdi — **şıkkın kendisi 2. ADIM olarak zaten içeri alındı**).
- **✅ ADIM 1 KOŞTU VE GEÇTİ — BEYAN DEĞİL, ÖLÇÜM [26 Tem 2026, oturum 28; Onur'un makinesinde YERLİ PowerShell]:**
  · **`docker compose up -d`** → `postgres:17-alpine` **çekildi**, `momentum_default` ağı ve **`momentum-postgres`** konteyneri oluştu ⇒ **`Up (healthy)`**; `docker exec … pg_isready -U momentum -d momentum` ⇒ ***"accepting connections"*** (`EXIT=0`). **PostgreSQL ilk kez fiilen AYAKTA** — 18 Tem'de *"tr-TR/cp1254 locale'i initdb'yi kırıyor, Docker'a ertelendi"* diye kapatılan kalem **KAPANDI.**
  · **`araclar/verify.ps1` (projenin KENDİ zinciri) koştu:** `dotnet build -warnaserror` ⇒ **0 Uyarı · 0 Hata** (33,4 sn) · `dotnet test` ⇒ **Architecture 5/5 · SyncCore 40/40 · Api 12/12 · Persistence 53/53 = TOPLAM 110 test, BAŞARISIZ 0** *(Persistence 1 dk — **gerçek Postgres'e karşı**)* · **CVE kapısı: 0 zafiyetli paket** ⇒ ***"VERIFY PASSED"* · `VERIFY_EXIT=0`.**
  ⇒ **Backend'in çalıştığı iddiası artık ÖLÇÜLMÜŞTÜR;** istemci dilimi kanıtlanmış bir zemine oturuyor. **Adım 2 (Flutter iskelet + Drift + çevrimdışı CRUD) açılabilir.**
  ⚠ **BEYAN EDİLMİŞ SINIR:** `/ping` ve `GET /v1/tasks` **canlı bir host üzerinde ELLE çağrılmadı**; ölçüm `Momentum.Api.Tests`'in **12 uçtan uca testi** üzerinden yapıldı (ping · problem+json · openapi · scalar · health dahil). Bu bir **test-host** ölçümüdür, `dotnet run` ile ayağa kalkmış bir sürecin ölçümü değildir. **Adım 2'de istemci gerçek host'a bağlanınca bu sınır kendiliğinden kapanır.**
- **Sıradaki ilk iş:** **(1)** ✅ **BİTTİ** — adım 1 ölçüldü ve GEÇTİ → **(2)** `DESIGN.md` + **`araclar/design-token-kapisi.py` altın kümesi** (K42-b'nin iki kısıtı) → **(3)** `GOREV-slice-3b` spec'i → **Claude Code build** → **Cowork BAĞIMSIZ doğrular** → **(4)** radar yeniden koşulur, **R5'in düştüğü ÖLÇÜLÜR**. **v7'ye DOKUNULMAZ (K41).**
- **🔴 RADARIN ÖLÇÜLMÜŞ İKİNCİ SINIRI — SONRAKİ EL BUNU OKUMADAN AÇILIŞ HÜKMÜNÜ YORUMLAMASIN [adlandırılmış borç]:** defterde artık **İKİ artefakt** var ve radar **her ikisini AYRI AYRI** doğru raporluyor — `docs/ADR/0003-kimlik-cekirdegi` **KIRMIZI** (park edilmiş) · **`slice-3b-istemci` 🟢 YEŞİL** (*"bulgu yok — ilerleme ölçülüyor"*). **AMA GENEL HÜKÜM, artefaktların EN KÖTÜSÜNÜ alıyor** ⇒ `RADAR_EXIT=2` KIRMIZI kalıyor ve K40'ın devre kesicisi lafzen *"yeni tur YASAK"* diyor. **Bu, K41 ile PARK EDİLMİŞ bir artefaktın hükmüdür ve `slice-3b`'yi bağlamaz.** **KURAL [bu oturumun kilidi]:** açılışta **genel hüküm değil, ÇALIŞILACAK ARTEFAKTIN kendi satırı** okunur; parked artefakt genel hükmü kirletiyorsa bu **aracın sınırıdır, projenin durumu değildir.** *Kalıcı çözüm (bu el YAPMADI — araç eli değiliz ve mini-ADR tuzağına düşmeyiz): deftere/araca bir `park` durumu eklenip park edilmiş artefaktın genel hükme katılmaması.* **Adlandırılmış borç: `proje-radari` plugin'i + `araclar/radar.py` — AYRI EL.**
- **🧭 KAŞİF [ölçülmüş ortam kusuru, yeni el zaman kaybetmesin]:** `kasif` skill'i hesapta **VAR ve `enabled: true`** (`ListSkills` ⇒ `skill_01X3E1BHTZ7jVAkRA4RweRJu`) **ama `disable-model-invocation` taşıyor** ⇒ **Cowork onu `Skill` aracıyla ÇAĞIRAMAZ** (*"cannot be used with Skill tool due to disable-model-invocation"*). **Yalnız Onur `/kasif` yazarak tetikleyebilir.** *(K40'ın kuralı yine iş gördü: varlık diskten değil `ListSkills` ile soruldu.)*

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 28 açılış+kilit: **K41 — RADAR KIRMIZI'DA DÖRT ŞIKTAN `DURDUR` KİLİTLENDİ; ADR 0003 v7 OLDUĞU GİBİ DONDURULDU, GÖRÜNEN ÇIKTIYA GEÇİLDİ**)

> ⚠ **EŞZAMANLILIK:** bu blok yazılmadan hemen önce dosya **diskten yeniden okundu** (`desktop-commander read_file`, 1.153 satır) ve **benzersiz çıpayla insert** edildi; tam-dosya rewrite YAPILMADI.

- **Protokol koşuldu (oturum 28):** `CLAUDE.md` **70/70 satır TAM** — *(dosya oturum 26-EK'in K40 radar maddesiyle **60 → 70** satıra çıktı; oturum 27'nin "60/60" sayısı o tarih için doğrudur, **bugün için BAYATTIR**)*. `PROJE_HAFIZA.md`: **satır 1–238 TAM** (K40 · K39 · K38 · oturum 27 açılışı · K37 · K36 · K35 · K34) **+ satır 1.088–1.153 TAM** (PROJE · KİLİTLENEN KARARLAR · DENETÇİ MİMARİSİ · KIRMIZI ÇİZGİLER · ORTAM · KARAR GÜNLÜĞÜ · AÇIK İŞLER).
  🔴 **BEYAN EDİLMİŞ SINIR, gizlenmiyor: satır 239–1.087 (oturum 2–24 tarihçesi) satır satır OKUNMADI.** Oturum 27 çubuğu **1.068/1.068** idi ⇒ **bu oturum onun ALTINDA kaldı.** *Gerekçe (savunma değil, sıra):* radar **açılışta KIRMIZI** verdi ve K40'ın devre kesicisi yeni turu **yasakladı** ⇒ okuma bütçesi, kilit kararından **SONRAYA** bırakıldı; kilit `DURDUR` çıkınca o tarihçenin (ADR turlarının ayrıntısı) bu oturumun işine **girdi olmadığı** ölçüldü. **Kapı-7 bu sınırı denetlemelidir.**
- **✅ AÇILIŞ KAPISI (protokol adım 2):** K34'te ölçüldü, bu projede **YOKTUR** ⇒ uygulanmaz (atlanmadı).
- **🔴 DEVİR NOTUNUN ÜÇ SAPMASI ÖLÇÜLDÜ [K13-a yine iş gördü — devir notu da bir üretimdir]:**
  1. **GİT — "3 commit PUSH EDİLMEDİ" BAYAT.** Ölçüm: `HEAD` = `origin/main` = **`d0c4831`** · ağaç **TEMİZ** · `.git/index.lock` **YOK**. Onur push etti **ve** oturum 26-EK **3 commit daha** attı: `9807ea9` (radar kurulumu, K39→K40) · `36462d4` (K39 numara çakışması düzeltmesi + defter satırı) · `d0c4831` (skill/plugin varlığı disk-önbellekten SORULMAZ kuralı). ⇒ **Push borcu YOK.**
  2. **HAFIZA BÜYÜDÜ:** devir notu **409.497 bayt · 1.134 satır** diyor; ölçüm **417.205 bayt · 1.153 satır · `U+FFFD` 0 · CRLF 0**.
  3. **EN YENİ KARAR K39 DEĞİL K40'TIR** ve devir notunun *"⚠ EN YENİ BLOK EN TEPEDE: K39"* satırı bayattır.
- **📡 RADAR KOŞTU (K40'ın açılış zorunluluğu):** **altın küme `EXIT=0`** (6/6: temizde susuyor · kısır döngüde dört kural birden ısırıyor · mekanikleşmişte yanlış-pozitif vermiyor · CR · sonlanma ölçütü · bütçe) ⇒ **araç önce kendini kanıtladı.** **Momentum hükmü: `EXIT=2` 🔴 KIRMIZI.** Tetikleyenler: **R1** (`beyansiz-sinir` **6 tur** · `kor-kapi` **5 tur** · `olu-tuzak` **3 tur** — mekanik kapısı YOK) · **R2b** (**8 turdur** denetleniyor, koşacak kod YOK) · **R5** (bu artefakta **7 oturum**, görünen çıktı **%0**) · **R4/SARI** (**120.000 → 300.930 bayt**, tur başına **~%21,5**). **R1b/BİLGİ:** `kanonik-kopya` + `tablo-hucre` mekanikleşti.
- **📏 GÖRÜNEN ÇIKTI BU OTURUMDA BAĞIMSIZ SAYILDI (kaynaktan, `bin`/`obj`/`.git` hariç):** **`.dart` = 0** · `DESIGN.md` **YOK** · `.github/` **YOK** · `src/client/` **boş** — buna karşılık **`.cs` = 141 · `.csproj` = 8** (backend slice-1 … 2b2 canlı) · `docker-compose.yml` VAR. ⇒ **R5'in "%0"ı İSTEMCİ/VİTRİN eksenindedir ve DOĞRUDUR;** backend'in var olması bu kırmızıyı düşürmez.
- **[KARAR — Onur, 26 Tem 2026] K41: DÖRT ŞIKTAN `DURDUR`. ADR 0003 v7 **OLDUĞU GİBİ DONDURULUR**; bu oturum ve sonraki oturumlar, kilit yeniden açılana kadar v7'ye **TEK BAYT YAZMAZ**. Kanonik ADR **v6 olarak KALIR** (K30-d taşıması YAPILMAZ).**
  *(Usul kaydı, gizlenmiyor: Onur dört şıkkı gördü ve kararı **Cowork'ün ölçülmüş önerisine devretti** — birebir: *"senin önerin ne kısır döngüye girmemek için ne yapılmalı ise öyle devam edelim"*. Öneri **`DURDUR`** idi; K40'ın *"kilit Onur'dan gelir"* kuralı **devir yoluyla** karşılandı ve bu, kapı-7'nin itiraz edebileceği bir usul kalemidir.)*
  **Gerekçe [ÖLÇÜLDÜ, dört madde]:**
  **(1) `DARALT` ve `DEVRET` ZATEN HARCANDI.** K38-a kilit ölçütünü daralttı; oturum 27 **§8.1–§8.4'ü yazdı** (E-1…E-26 · M-L5…M-L10 · `aspnetcore` devri · §3.1 borcu). Bu iki şıkkı tekrar etmek radarı **hareket ettirmez** — K40'ın kendi ölçülebilir hedefi bunu yazıyor: **R1 ancak üç mekanik kontrol KOŞUNCA, R5 ancak görünen çıktı > %0 olunca, R2b ancak `slice-3c` KODU var olunca düşer.**
  **(2) R5, TAKVİME BAĞLI TEK KIRMIZIDIR.** Hedef **~15 Ağu 2026** (K7-b), bugün **26 Tem** ⇒ **20 gün**. Repoyu bugün açan bir değerlendirici **300 KB kilitlenmemiş ADR + sıfır istemci** görür; ödevin ilan edilmiş odağı *"mimari & kod kalitesi"*dir ve **mimari ancak KODDA görülür.** Kâğıt üzerinde sekizinci tur bu riski **azaltmıyor, büyütüyor** (R4: her tur ~%21,5 daha fazla çapraz-atıf yüzeyi).
  **(3) `MEKANİKLEŞTİR`'İ ERTELEMENİN BEDELİ ŞU AN SIFIRDIR.** Artefakt **donduğu** için tekrar eden üç sınıf (`beyansiz-sinir` · `kor-kapi` · `olu-tuzak`) **YENİ ÖRNEK ÜRETEMEZ** — yeni tur yoksa yeni kusur da yoktur. Üç kontrol (çapraz-atıf çözücü · tablo hücre bütünlüğü · kill-sinyali lint) **kapı-7 ADR'yi yeniden AÇMADAN ÖNCE** yazılmak zorundadır ve o el **HÂLÂ AYRI ELDİR (K34-f)**.
  **(4) ADR'YE PARK NOTU BİLE YAZILMADI** — park kaydı **YALNIZ hafızada** durur. *Gerekçe:* her bayt R4'ü besler ve **K33'ün örüntüsü** (*bir kusur sınıfını kapatan her düzeltme aynı sınıftan kusur üretme riski taşır*) **park anında da geçerlidir**; ayrıca aracı koşmayı gerektirir ⇒ tur başlar. **v7 DONDURULDU: `300.930 bayt · 1.386 satır · sha256 2AE2FB29…FE5F2A98`.**
  **Bedel [adlandırılmış, gizlenmiyor]:** ADR 0003 **KİLİTSİZ** kalır — **26 açık spec-errata (E-1…E-26)** · **4 canlı `[DOĞRULANMADI]`** (`flutter_secure_storage` Windows şifrelemesi · WebKit `http://localhost` `__Host-` · Isopoh lisans ailesi · **NIST SP 800-38D**) · **§0'ın başlığı hâlâ *"v4 → v5 değişim kaydı"*** · **satır 3 ve satır 16 BAYAT** (*"TASLAK v6 — 6. TUR BEKLİYOR"*) · **kanonik yol hâlâ v6** · `__Host-`'un rfc6265bis §4.1.3.2 numarası **yazılmadı**. ⇒ **Repoyu bugün açan biri, v7 çalışma dosyasında kendi sürümüyle ÇELİŞEN bir altbilgi görür — bu KABUL EDİLMİŞ ve BEYAN EDİLMİŞ bir bedeldir.** Kapı-7 gecikir; `GOREV-slice-3c-auth` build'i takvimin sonuna kayar.
  **Reddedilenler [adlandırılmış]:** **`MEKANİKLEŞTİR`** — R1'i düşüren TEK yol ve **bu el K34-f'in aradığı *"ayrı el"* olmaya UYGUNDU** (v7'ye hiç dokunmadı); ama **R5 ve R2b yerinde kalır**, vitrin yine %0 olur ve 20 günlük takvim baskısı büyür · **`DARALT` — yalnız bayat metin** (§0 başlığı + satır 3/16; en ucuz, R1b muafiyetine dayanır ve kesin kapanır; ama **üç kırmızının ÜÇÜ DE yerinde kalır**, kazanç yalnız belgenin kendi iç tutarlılığıdır) · **`DEVRET`** (içeriği tükendi; geriye yalnız `GOREV-slice-3c-auth` spec'ini yazmak kalıyor ve **spec KOD DEĞİLDİR** ⇒ R2b düşmez) · **İKİ CEPHE BİRDEN (`MEKANİKLEŞTİR` + `DURDUR`)** — **radarın ölçtüğü aşırı-yayılmanın ta kendisi**; K38-c iki cephenin bedelini zaten adlandırmıştı.
- **🔒 YÜRÜRLÜKTEKİ YENİ KISIT [K41'in sonucu, PAZARLIKSIZ]: ADR 0003 v7 DONDURULDU.** Yeniden açılması **şu üçünün BİRLİKTE** sağlanmasına bağlıdır: **(1)** aracın 2. onarımı **KOŞMUŞ** olmalı (üç yeni kontrol — **AYRI EL, K34-f**) · **(2)** `slice-3b`'nin görünen çıktısı **> %0** olmalı (radar R5) · **(3)** **Onur açıkça yeniden açmalı.** **Bu üçü sağlanmadan v7'ye yazan her el K41'i İHLAL EDER.** *(K38-a'nın sonlanma tanımı yürürlükte kalır; K41 onu geçersiz kılmaz, ERTELER.)*
- **[K21 ÖLÇÜMÜ — açılış+kilit] 190.673 token = %19,1 ⇒ 🟢 YEŞİL.** ⚠ **ÖLÇÜM KAYNAĞI DEĞİŞTİ [yeni el dikkat]:** bu oturum **BULUTTA** koşuyor ⇒ transcript **Onur'un makinesinde DEĞİL**, konteynerde: `/root/.claude/projects/-home-claude/74291cf5-42a0-563a-a1f1-d2b3769db28b.jsonl`. Oturum 25/26/27'nin `~/.claude/projects/` yolu **bu oturum için geçersizdir**. **Beyan edilmiş sınır:** ölçüm transcript'e **son yazılmış** `usage` kaydıdır ⇒ **bir tur geriden gelir**.
- **Sıradaki ilk iş:** **(1)** `slice-3b` kapsamı + tasarım yönü **işaretlenebilir şıklarla Onur'a sunulur → KİLİTLENİR** (protokol adım 5) → **(2)** `DESIGN.md` (K10 yönü, **MUST/NICE bölünmüş — K10-e**) → **(3)** `GOREV-slice-3b` spec'i → **Claude Code build** → **Cowork BAĞIMSIZ doğrular** → **(4)** görünen çıktı > %0 olunca **radar yeniden koşulur** ve R5'in düştüğü **ÖLÇÜLÜR**. **v7'ye DOKUNULMAZ (K41).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 26 EK: **K40 — PROJE RADARI KURULDU VE KOŞTU; HÜKÜM KIRMIZI; RADAR K38'İ BAĞIMSIZ OLARAK DOĞRULADI**)

> 🔴 **NUMARA ÇAKIŞMASI DÜZELTİLDİ [ÖLÇÜLDÜ, gizlenmiyor]:** bu blok ilk yazımında **`K39`** numarasını almıştı; **oturum 27 aynı numarayı KENDİ iki kilidi için kullanmıştı** (*bu el yazım elidir · atıf denetiminin ekseni normatif/davranış*) ⇒ hafızada **iki farklı K39** doğdu. Bu, projenin `ADR 0004` ⟷ `K10-d` çakışmasıyla **aynı sınıftır**. **Radar checkpoint'i K40'a taşındı; oturum 27'nin K39'u OLDUĞU GİBİ KALDI** (önce yazılmıştı). **Kök neden:** iki oturum aynı hafıza dosyasına paralel yazdı ve numara **tek bir yerden dağıtılmıyor** ⇒ **yapısal kusur, tekrar edebilir.** Kalıcı çözüm K38-b'nin mantığıyla aynıdır: karar numaraları da **prozadan değil tek bir sayaçtan** verilmeli. Kapı-7'ye devredilen adlandırılmış bir borçtur.

- **Kurulan üç şey [ÖLÇÜLDÜ]:** `araclar/radar.py` (ölçüm aleti, stdlib) · `PROJE_RADAR.jsonl` (append-only defter, **13 satır**; kapı-2…kapı-6 ve v7 turu geriye dönük dolduruldu) · `CLAUDE.md`'ye **tarihli Radar maddesi (K40)** (açılış+checkpoint'te koşum · her checkpoint'te bir satır · KIRMIZI'da yeni tur YASAK).
- **DEFTERİN DÜRÜSTLÜK KAYDI:** `bloker` sayıları **kapı-4/5/6 için ÖLÇÜLDÜ** (hafızada rakamla yazılı); **kapı-2 ve kapı-3'ün sayıları `[TAHMIN]`** olarak işaretlendi (hafızada rakam yok, 9 varsayıldı); eski turların **baytları `[TAHMIN]`**. `capture_recapture` alanı **ÖRNEKTİR ve `[HESAPLANMADI]` işaretlidir** — gerçek `n1/n2/ortak`, kapı-6'nın **17 bağımsız denetçisinin ham çıktılarından** (`KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/`, 11 dosya · 457.046 bayt) hesaplanmalıdır. **Bu, kapı-7'ye devredilen ölçülebilir bir iştir.**
- **✅ ALTIN KÜME `EXIT=0`** (6 kontrol: temiz projede susuyor · kısır döngüde dört kural birden ısırıyor · mekanikleşmiş tekrarda yanlış-pozitif VERMİYOR · capture-recapture ısırıyor · sayı-tabanlı sonlanma ölçütü ısırıyor · bütçe aşımı ısırıyor). **Araç önce kendini kanıtladı** (kör kapı yok).
- **🔴 MOMENTUM HÜKMÜ: KIRMIZI (`EXIT=2`).** Bloker eğrisi **[9, 9, 9, 12, 9, 0]** · bayt **120.000 → 281.928** · son tur **15 kapatıldı / 5 üretildi**. Tetikleyenler: **R1** (tekrar eden ve **mekanik kapısı OLMAYAN** sınıflar: `beyansiz-sinir` **6 tur** · `kor-kapi` **5 tur** · `olu-tuzak` **3 tur**) · **R2b** (koşulamayan spec: 7 turdur denetleniyor, kodu YOK) · **R5** (teslim dengesi: 6 oturum, görünen çıktı %0) · **R4/SARI** (tur başına ~%22,5 büyüme). **R1b/BİLGİ:** `kanonik-kopya` ve `tablo-hucre` **mekanikleşti** ⇒ bu iki sınıf için tur eklemek meşru.
- **🎯 EN ÖNEMLİ SONUÇ — RADAR, K38'İ BAĞIMSIZ OLARAK DOĞRULADI [dört tetikleyicinin dördünün de karşılığı ZATEN KİLİTLİ]:** **R2b → K38-a** (DEVRET: koşulamayan iddialar build'e) · **R1 → K38-b** (MEKANİKLEŞTİR: çapraz-atıf çözücü `beyansiz-sinir`'ı, kill-sinyali lint'i `kor-kapi` + `olu-tuzak`'ı kapatır) · **R5 → K38-c** (istemci paralel başlar) · **R4 → K38-b'nin YAML'ı** (elle yazılan sayıların yerini hesaplama alır). ⇒ **Radar yeni bir iş EKLEMİYOR; kilitlenmiş planı ÖLÇÜMLE onaylıyor.** Bu, K38'in *"altı oturum önce fark edilebilirdi"* iddiasının bağımsız kanıtıdır.
- **📏 ÖLÇÜLEBİLİR HEDEF (bundan sonra kapı-7'yi bağlar):** radar **K38 uygulandıkça yeşile dönmek ZORUNDA.** `R1` ancak üç mekanik kontrol (çapraz-atıf çözücü · tablo bütünlüğü · kill-sinyali lint) **koştuğunda** düşer; `R5` ancak **görünen çıktı > %0** olunca; `R2b` ancak `slice-3c` kodu **var olunca**. **Kırmızı kalmaya devam ederse plan işlemiyor demektir** — bu artık hissedilmez, ölçülür.
- **⚠ ADLANDIRILMIŞ SINIR:** radarın `R1` hükmü, deftere yazılan **sınıf adlarının tutarlılığına** bağlıdır. Sınıf adı turdan tura değişirse tekrar tespiti **körleşir** — bu radarın **en kolay kırılma noktasıdır** ve sözlük `proje-radari/defter-semasi.md`'de sabittir.
- **🔴 ORTAM UYARISI — BU OTURUMDA ÖLÇÜLDÜ, KANLA YAZILIYOR: SKILL/PLUGIN VARLIĞI DİSKTEN SORULMAZ.** `C:\Users\gulci\.claude\skills\` ve `...\.claude\plugins\` dizinleri **SENKRON ÖNBELLEKTİR, KAYNAK DEĞİLDİR.** Bu oturum diskte `kasif\SKILL.md` bulamayıp *"kaydedilmemiş"* dedi — **YANLIŞTI**: `ListSkills` koşulduğunda skill **hesapta VAR ve `enabled: true`** çıktı (`skill_01X3E1BHTZ7jVAkRA4RweRJu`). Aynı önbellek, **hesaptan SİLİNMİŞ** eski `radar` skill'ini de hâlâ gösteriyor. ⇒ **KURAL: kurulu skill/plugin/connector varlığı YALNIZ `ListSkills` / `ListPlugins` / `ListConnectors` ile sorulur; disk yalnız İÇERİK okumak için kullanılır** (açıklaması boş olanın `SKILL.md`'sini okumak gibi). *Bu, oturumun avladığı kusurun kendisidir: yanlış aleti kullanıp ölçtüğünü sanmak.*
- **📅 GÜNLÜK GÖZCÜ KURULDU:** zamanlanmış görev `trig_01YEPhJf1Ugi3ym3LupiGwZQ` · her gün **08:30 (TR)** · **salt-okunur** (dosya değiştirmez, commit atmaz) · cihaz kapalıysa **sessizce çıkar** · yalnız SARI/KIRMIZI'da **telefon bildirimi**.

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 27: **K39 — İKİ KİLİT: BU EL YAZIM ELİDİR (ARACI KOŞAR, ONARMAZ) · ATIF DENETİMİNİN EKSENİ NORMATİF/DAVRANIŞ**)

> ⚠ **SIRA:** bu blok **K38'den SONRA** yazıldı ve onu **DEĞİŞTİRMEZ, UYGULAR.** K38'in altı adımlı sırası bağlayıcıdır; K39 yalnız *"bu oturum o sıranın hangi adımlarını, hangi elle yapar"* sorusunu kilitler.
> ⚠ **EŞZAMANLILIK:** bu yazımdan hemen önce dosya **diskten yeniden okundu** (`desktop-commander read_file`, 1.099 satır) ve **benzersiz çıpayla insert** edildi; tam-dosya rewrite YAPILMADI. `slice-3b` oturumu paralel çalışıyor (K38-c).

- **✅ K38 OKUNDU VE DEVRALINDI.** Oturum 27'nin açılış bloğu (bir alt başlık) K38'den **ÖNCE** yazıldığı için oradaki *"Sıradaki ilk iş"* satırı **BAYATTIR**; yerine K38'in altı adımlı sırası geçer. *(Kayıt: iki blok da `e5c9ddf`'te commit'lidir — oturum 26'nın K38 yazımı oturum 27'nin açılış bloğunu **ezmedi**; diskten `sha256` ve satır aramasıyla doğrulandı. **Eşzamanlılık kuralı fiilen sınandı ve tuttu.**)*
- **✅ GİT (K38 sonrası, kaynaktan ölçüldü):** ağaç **TEMİZ** · HEAD **`e5c9ddf5db19dbb6efb2f27ae1bdbaf407f55688`** · `origin/main` **`78fa5d6`** · **1 İLERİ, PUSH YOK** · `.git/index.lock` **YOK**. `PROJE_HAFIZA.md` **391.032 bayt · 1.099 satır · U+FFFD 0 · CRLF 0** (`613D4AED…`). `CLAUDE.md` **4.681 bayt · `8A5CD1C1…`** — *(oturum 26'nın ara notu "61 satır" diyor, PowerShell `Get-Content` **60** sayıyor; `sha256` aynı ⇒ **satır-sonu sayım kuralı farkı**, içerik aynı. **26 Tem tarihli git ERRATA bloğu okundu ve uygulanıyor.**)*
- **[KARAR — Onur, 26 Tem 2026] K39-a: BU OTURUM **YAZIM ELİDİR**; `araclar/adr-kapi-taramasi.py`'YE **TEK BAYT DOKUNMAZ** AMA ONU **KOŞAR**.** *Gerekçe (Onur):* K38'in yeni sırasının ilk dört adımı **yazım işidir** (majörleri errata'ya çevirme · §3.1 yeniden ölçümü · §0 değişim kaydı · bayat altbilgi · atıf denetimi · kanonik yola alma). **KOŞMAK ONARMAK DEĞİLDİR** ve K38-a mekanik kapı şartını (`K1`=0 · tablo=0 · altın küme `EXIT=0`) **KORUYOR** ⇒ araç bu oturumda **koşulacak**, K34-f ihlal edilmeyecek. **K38-b'nin mutant-YAML'ı ve aracın ÜÇ yeni kontrolü (çapraz-atıf çözücü · tablo hücre bütünlüğü · kill-sinyali lint'i) BU ELE AİT DEĞİLDİR — devir kalemi olarak yazılır.** *Bedel [adlandırılmış]:* bu oturumun ürettiği **her yeni satır, satır-412 kaçış filtresi AÇIKKEN ve çapraz-atıf çözücüsü YOKKEN** ölçülür ⇒ **`K4` rakamı tek başına kanıt değildir** ve *"beyan edildiği söylenen ama yazılmamış"* sınıfı (dört turdur tekrarlıyor) bu oturumda da **mekanik korumasızdır**. *Reddedilenler [adlandırılmış]:* **ARAÇ ONARIM ELİ** (bu oturum v7'ye hiç dokunmadığı için K34-f'in aradığı *"ayrı el"* olmaya uygundu ve kalan iş onarılmış araçla ölçülürdü; ama belge hiç ilerlemezdi ve K38'in sırası yazımı öne alıyor) · **YAZIM ELİ — DAR (yalnız §3.2'nin 11 majörü)** (en ağır grup tam bitirilirdi; ama K38-a majörleri **kapatılacak** kalem olmaktan çıkarıp **errata'ya çevirdiği için** derinlemesine tek grup çalışmasının getirisi düştü) · **MEKANİK ÖNCE** — **Onur bunu açıkça YANLIŞ ŞIK ilan etti: *"mekanik iş = araç = yasak el"***.
- **[KARAR — Onur, 26 Tem 2026] K39-b: ATIF DENETİMİNİN EKSENİ **NIST/aspnetcore DEĞİL, NORMATİF/DAVRANIŞ**'tır.** **(a) NORMATİF atıflar BU OTURUMDA birincil kaynaktan açılır:** NIST SP 800-63B-4'ün **`SHALL` / `SHALL NOT`** kalemleri (×13) · **RFC 9700 §4.14.2** · **RFC 9562** (big-endian bayt sırası) · **RFC 6265bis** (`__Host-` öneki) · **RFC 4648** (base64url). **(b) `aspnetcore` ×10 bir ATIF DEĞİL, bir DAVRANIŞ ÖLÇÜMÜDÜR** ⇒ okunarak değil **KOŞULARAK** doğrulanır; `GOREV-slice-3c-auth` build'ine **adlandırılmış kalem** olarak devredilir. *Gerekçe (Onur):* v7'de ölçüldü ki belgenin **tek yanlış birincil-kaynak atfı bir NORMATİF atıftı** (RFC 5321 §4.5.3.1.3 birebir *"yol sınırı 256"*, belge **254** diyordu) ve **portfolyoda en zarar veren kusur sınıfı budur**; `aspnetcore` satırları koşulabilir olduğu için onları okumak yerine koşmak doğrudur. *Bedel [adlandırılmış]:* `aspnetcore` ×10 **bu oturumda DOĞRULANMAMIŞ kalır** ve kapı-7 bunu görecektir — ama **beyan edilmiş ve adresli** bir devirdir, gizlenmiş bir sınır değildir. *Reddedilenler [adlandırılmış]:* **tamamı kapı-7'ye** (doktrine en uygun — atfı yazan el denetleyemez; ama v7 kilide ~30 doğrulanmamış atıfla giderdi) · **tamamı bu oturumda** (en büyük bilinmeyen kapanırdı; ama `aspnetcore` satırları **okunarak** doğrulanamaz — yanlış aletle ölçüm olurdu) · **Cowork'ün önerdiği "yalnız NIST ×13" ekseni** — **Onur ekseni reddetti**: bölme ölçütü *kaynağın adı* değil, kalemin **normatif mi davranışsal mı** olduğudur.
- **🔴 [COWORK ÖLÇÜMÜ — kimse sormadı; K38'in KENDİ SIRASINDA bir bayatlama tuzağı var]:** K38'in sırası **§3.1'in yeniden ölçümünü adım 2'ye**, **aracın 2. onarımını adım 5'e** koyuyor. Ama **§3.1, aracın DÖRT sınırını adıyla beyan eden bölümdür** (B6-4 ve B6-5 tam olarak bu yüzden bloker olmuştu). ⇒ Araç adım 5'te onarılınca §3.1'in adım 2'de yazdığı beyan **olgusal olarak yanlışa döner** ve kanonik ADR kapı-7'ye *"aracın satır 412'de sessiz kaçışı vardır"* diyen **bayat bir cümleyle** girer — bu, B5-5 / B6-8 ile **aynı ailedir**. **K38-a'ya göre bu bir BLOKER DEĞİLDİR** (kör kapı değil, inşa edilemezlik değil, kırmızı çizgi değil) ⇒ sınıfı **SPEC-ERRATA**. **Bedelsiz telafi, bu oturumda uygulanacak:** §3.1'in araç-sınırları beyanı **TARİHLİ ve KAPSAMLI** yazılır — *"26 Tem 2026 itibarıyla, aracın 2. onarımından ÖNCE ölçülmüş sınırlardır; onarım sonrası bu satır YENİDEN ÖLÇÜLMEK ZORUNDADIR"* — ve **devredilmiş kalem** olarak adlandırılır. ⇒ zamansız bir **güvence** yerine tarihli bir **sınır** olur; *"beyan edilmiş sınır kabul edilir, gizlenmiş sınır edilmez"* kuralı sağlanır.
- **BU OTURUMUN KAPSAMI (K38'in sırası + K39):** (1) **21 majör → SPEC-ERRATA listesi** + **K34-g'nin `slice-3b` borç listesi** → (2) **§3.1 yeniden ölçümü** (araç-sınırları beyanı TARİHLİ) **+ §0 v6→v7 değişim kaydı + bayat "TASLAK v6" altbilgisi** → (3) **NORMATİF atıflar birincil kaynaktan** (K39-b(a)) → (4) **kanonik yola alma (K30-d) + v6 arşive**. **Her adımdan sonra araç KOŞULUR** (K38'in ölçülmüş dersi: *"okudum, temiz" bir ölçüm DEĞİLDİR*) **ve checkpoint YERİNDE yazılır.** **K21 🟡 (%55) bandına girilirse eldeki kalem bitirilir, yeni adım AÇILMAZ.**
- **BU OTURUMUN ELİNE AİT OLMAYANLAR [devir kalemi]:** **K38-b'nin `0003-mutantlar.yaml`'ı** · **aracın 2. onarımı (üç yeni kontrol)** — **AYRI EL, K34-f** · **`slice-3b` / Flutter / `DESIGN.md`** — **AYRI OTURUM, K38-c** · **`aspnetcore` ×10 davranış ölçümü** — **`GOREV-slice-3c-auth` build'i, K39-b(b)** · **KAPI-7 = KİLİT KAPISI** — AYRI OTURUM.

- **✅ ADIM 1 TAMAMLANDI — v7'ye `## 8. DEVREDİLEN SPEC-ERRATA VE BORÇ LİSTELERİ` YAZILDI.** Dört alt bölüm: **§8.1** kapı-6'nın açık majörleri **E-1 … E-26** (her kalem: kapı-6 numarası · mekanizma · kusur sınıfı · *build'in yapması gereken*) · **§8.2** **K34-g'nin `slice-3b` ADLANDIRILMIŞ BORÇ LİSTESİ** — **M-L5…M-L10, altı kalem, her biri kill sinyaliyle** (bu liste **dört turdur borçluydu ve bugüne kadar YOKTU**) · **§8.3** `aspnetcore` davranış ölçümleri build'e devredildi (K39-b(b)) · **§8.4** aracın onarımı sonrası §3.1'in yeniden ölçülmesi borcu. **Listenin ADR'de durma gerekçesi yazıldı:** `GOREV-slice-3c-auth` spec'i **bugün YOKTUR** ⇒ var olmayan artefakta çıpa = **K22-a sınıfı bloker**; liste burada yaşar, spec onu **adıyla devralır**. v7: **1.305 → 1.383 satır**.
- **🔴 ADIM 1'İN ÖLÇÜMÜ BİR SAYIM SAPMASI ORTAYA ÇIKARDI [kimse sormadı; kaynaktan mekanik sayıldı]:** `kapi-6-denetim-raporu.md` §3'te **manşet "31 MAJÖR"** · **grup başlıklarının toplamı 35** (9+11+5+4+3+3) · **numaralandırılmış kalem 37** (1–37, **boşluk YOK, çift kayıt YOK**). **Üçü de birbirini üretmiyor.** Kapanan: **#1–#10**; red-team **#17'yi minöre indirdi** ⇒ **AÇIK MAJÖR = 26.** ⇒ **devir notunun ve K38'in *"21 majör açık"* sayısı YANLIŞTIR** (devir notunun kendi grup sayılarının toplamı da 11+5+4+6 = **26**). §8.1 **26 kalem** taşıyor ve sapma **belgede beyan edildi**. *Bu, kapı-6'nın kendi #37 bulgusunun rapora kendisine uygulanmış hâlidir ⇒ **kapı-7 raporu da denetlemelidir** (K32-d emsali).*
- **✅ ADIM 2'NİN BİR KALEMİ BİTTİ — BAYAT ALTBİLGİ DÜZELTİLDİ.** Dosya sonu *"TASLAK v6 — … 6. tur için AYRI ve TEMİZ bir oturum gerekir"* diyordu; **kapı-6 KOŞTU** (oturum 24) ⇒ altbilgi **TASLAK v7 · kapı-7 = KİLİT KAPISI** olarak yenilendi, bugünkü durum (9/9 bloker kapandı · majör #1–#10 kapandı · 26'sı §8.1'e devredildi) ve **K26 uyarısı (v7'yi oturum 25+26+27 yazdı ⇒ üçü de denetleyemez)** yazıldı. 🔴 **AÇIK KALAN İKİ BAYAT SATIR:** **satır 3** (*"Durum: TASLAK v6 … 6. TUR BAĞIMSIZ KAPIYI BEKLİYOR"*) ve **satır 16** (tarih/sürüm satırı) · ayrıca **§0'ın başlığı hâlâ *"v4 → v5 değişim kaydı"*** ⇒ **§0'a v6→v7 kaydı YAZILMADI** (adım 2'nin kalanı).
- **✅ ARAÇ KOŞULDU — K39-a UYGULANDI, ARACA TEK BAYT DOKUNULMADI.** **Altın küme: `EXIT=0`** (*"ARAÇ KULLANILABİLİR"*). **v7 üzerinde: `K1` = 0 · `K4` = 5 · `K6` = 2 ⇒ TOPLAM 7 = v6'nın taban çizgisi** · `capasiz_tablo` = **0** · ÖZET `karar 47 · alt_madde 13 · kapili 32 · beyanli 15 · devredilmis 5 · mutant 53` — **K37'nin kapanış ölçümüyle BİREBİR** ⇒ **§8'in 78 yeni satırı YENİ araç bulgusu ÜRETMEDİ.**
- **🔴 AMA ÜRETTİ VE ARAÇ YAKALADI — K33 ÖRÜNTÜSÜNÜN SEKİZİNCİ KANITI [gizlenmiyor].** İlk koşumda **E-19'a yazdığım RAKAMLI bölüm atfı** (§3.2'nin ilgili madde numarası) `[KS-4]`'ün **zayıf eşleşmesini** tetikledi ve **satır 1341** listeye girdi ⇒ *"aracın iki yapısal yanlış-pozitifi"* taban çizgisi **üçe** çıkıyordu. **Atıf RAKAMSIZ yazıldı** (gerekçesi satırın içinde bir HTML yorumunda) ve liste **v6'nın taban çizgisine döndü** (`KS-4 → satır 28,1009,1191,1237`). **Bu, K37'nin 3. kusuruyla BİREBİR AYNI SINIFTIR** ve dersi tekrar doğruladı: *bu belgede bir kusur sınıfını kapatan her düzeltme aynı sınıftan kusur üretme riski taşıyor; tek koruma ARACI KOŞMAK.* **Not:** eski iki yanlış-pozitife DOKUNULMADI (K31: aracı susturmak için belgeyi yeniden yazmak **kapı tiyatrosu** olurdu); düzeltilen şey **bu oturumun kendi YENİ satırıdır**.
- **⚠ 🆕 ORTAM UYARISI [ÖLÇÜLDÜ — kanla yazılıyor]: ARAÇ, WINDOWS'UN VARSAYILAN `cp1254` STDOUT'UYLA ÇÖKÜYOR VE ÇIKIŞ KODU `1` VERİYOR — BU BİR BULGU DEĞİLDİR.** İlk koşumda `UnicodeEncodeError: 'charmap' codec can't encode character '✅'` (ve `'→'`) ile **traceback** attı; `EXIT=1` **yanlışlıkla *"bulgu var"* diye okunabilirdi. PAZARLIKSIZ KOŞUM ÖNKOŞULU:** `$env:PYTHONUTF8='1'` **+** `$env:PYTHONIOENCODING='utf-8'` **+** `[Console]::OutputEncoding=[System.Text.Encoding]::UTF8`. *(Aracın kendi beyan ettiği **"bozuk UTF-8 ⇒ çıkış kodu 3"** koruması bu vakayı kapsamıyor: kusur GİRDİDE değil ÇIKTI kodlamasındadır. **Bu, aracın 2. onarımına ait ADLANDIRILMIŞ bir kalemdir** — bu el onarmadı, K34-f.)*
- **✅ GİT (adım 1 sonrası, kaynaktan):** `origin/main` **`e5c9ddf`**'e geldi ⇒ **Onur push etti; ileri/geri `0 0`.** Ağaç: ` M PROJE_HAFIZA.md` · ` M docs/ADR/0003-…-v7-….md` · `.git/index.lock` **YOK**.
- **[K21 ÖLÇÜMÜ — adım 1 sonrası] 445.814 token = %44,6 ⇒ 🟢 YEŞİL.** Eğri: 133k → 320k → **446k**. 🟡 bandı (%55) **yaklaşık 100k uzakta** ⇒ K21 gereği **devam edilebilir**, ama **normatif atıf turu (K39-b(a)) tek başına o mesafeyi yiyebilir** ⇒ o adım açılırsa **eldeki kalem bitirilir ve yeni adım AÇILMAZ**.
- **✅ COMMIT — `696db44`** *"docs(adr-0003,hafiza): v7 section 8 …"* · 2 dosya · **+100 / −1** · **ağaç TEMİZ · `.git/index.lock` YOK** (errata koşulu koşuldu) · `origin/main`'den **1 ileri**.
- **[KARAR — Onur, 26 Tem 2026] K39-c: OTURUM DEVAM ETTİ — K39-b(a) BU OTURUMDA KOŞTU.** Onur şunu sordu: *"temiz oturumda hepsini bitirebilecek misin?"* **Cowork'ün dürüst cevabı YOK oldu ve ölçümle gerekçelendirildi:** taze bir oturum protokolü (hafızanın 1.100+ satırı) koşmak için tek başına **~%22** harcar; kalan üç adım (§3.1 yeniden ölçümü · 13 `SHALL` + 4 RFC · kanonik yola alma) **~%40-50** daha ⇒ taze oturum **%62-72** ile 🟡'nin içinde biter ve **en riskli kalemi (kanonik yola alma) sona bırakır**. Bu el protokol bedelini **zaten ödemişti** ⇒ kalan yeşil bandı işe harcamak daha verimli. *Reddedilen:* **temiz oturuma devret** (bağlam hijyeni en yüksek; ama protokol bedeli **ikinci kez** ödenir ve toplam maliyet artar).
- **✅ ADIM 3 TAMAMLANDI — K39-b(a): NORMATİF ATIFLAR BİRİNCİL KAYNAKTAN AÇILDI. 📄 `KANIT/adr-0003/v7-normatif-atif-denetimi.md`** (ham alıntılar, yeniden yazılmadan). **ÜÇ SAPMA + BİR MİNÖR ÖLÇÜLDÜ VE DÜZELTİLDİ; HİÇBİR KARAR DEĞİŞMEDİ** — değişen şey **alıntıların doğruluğu ve gerekçelerin zeminidir**:
  · **🔴 S-1 [NIST SP 800-63B-4 §3.2.2] — ALINTI BİREBİR DEĞİLDİ.** Gerçek metin *"…consecutive failed authentication attempts **using a specific authenticator** on a single **subscriber** account…"*; belge **iki ibareyi işaretsiz düşürmüştü** (`…` yok) ⇒ **K35'in PAZARLIKSIZ kuralının ihlali** ve **belgenin İKİNCİ ölçülmüş yanlış birincil-kaynak atfı** (birincisi RFC 5321). **Düşen ibare anlamlıdır:** sınır **authenticator BAŞINA**dır, kısaltılmış hâl **hesap başına** okutuyordu. **Sapmanın yönü:** düzeltme adlandırılmış sapmayı **GÜÇLENDİRİR** (gerçek `SHALL` daha dar; throttle hiçbir kapsamda devre dışı bırakmıyor) ⇒ karar aynen durur.
  · **🔴 S-2 [NIST SP 800-63B-4 §3.1.1.2] — *"aşağıda birebir"* DENİLEN KARA-LİSTE `SHALL` ALINTISI BELGEDE HİÇ YOKTU** (`grep "blocklist that|prospective secret"` ⇒ **0**). **B5-5 / B6-8 / K35 ailesinin YENİ örneği** — ve **dört turluk okuma bunu bulamadı, BİRİNCİL KAYNAĞIN AÇILMASI buldu.** Alıntı birebir yazıldı, `§3.1.1.2` bölüm numarası eklendi.
  · **🔴 S-3 [RFC 9700 §4.14.2] — *"aile iptali cümlesi BETİMLEYİCİDİR"* İDDİASI YANLIŞ:** birincil kaynakta o cümle bir **`SHOULD`**'dur (*"…the authorization server SHOULD revoke the complete token family."*) ⇒ **NORMATİF.** **Hüküm değişmez, zemin değişir:** Momentum aileyi **iptal eder** ⇒ `SHOULD` **karşılanır**; yanlış olan *"betimleyici olduğu için bağlayıcı değil"* gerekçesiydi. *(`MUST`'ın *"yöntemlerden birini kullan"* için olduğu nitelemesi **DOĞRU çıktı**; satır 844'ün *"This stops the attack…"* alıntısı **BİREBİR DOĞRU** çıktı.)*
  · **🟠 M-1 [RFC 4648] — atıf hijyeni:** dolgunun atlanması **§5'te değil §3.2'de** normatiftir ve §3.2 *"referans veren spesifikasyon açıkça aksini belirtmeli"* der ⇒ atıf **§5 (alfabe) + §3.2 (dolgu)** olarak düzeltildi; **bu ADR'nin o "referans veren spesifikasyon" olduğu** yazıldı. Değer zaten doğruydu.
  · **✅ TUTAN SEKİZ KALEM:** NIST §3.1.1.2'nin **dört** kalemi (tek faktör `SHALL` · MFA bileşeni · `SHALL NOT` kompozisyon · `SHOULD` azami — **`[KS-18]` tavsiyenin ÜSTÜNDE, sapma değil**) · RFC 9700'ün **iki** kalemi · **RFC 9562 §4/§6.11 big-endian** (*"Saving UUIDs to binary format is done by sequencing all fields in big-endian format."*) ⇒ **AAD pini ve M42b/M51'in altın vektörü doğru zeminde** · **rfc6265bis §5.2.1 = "Document-based requests"** ⇒ atıf doğru. **Belge sürüm bilgisi de doğrulandı:** NIST SP 800-63B **Revision 4 · Published · 26 Ağu 2025** ⇒ *"(Final)"* nitelemesi **DOĞRU**.
  · **⚠ ÇAPRAZ KONTROL [tuzak, gelecekteki el için]:** **RFC 6265 (2011) `__Host-` önekini ve same-site tanımını İÇERMEZ** ve o belgede **§5.2.1 = "The Expires Attribute"**'tur ⇒ *"6265"* ile *"6265bis"* karıştırılırsa **bölüm numaraları sessizce yanlış yere düşer.** ADR doğru belgeyi anıyor. `__Host-`'un bis'teki yeri **§4.1.3.2**; ADR'de numara **yok** (devir kalemi).
  · **⚠ AÇIK `[DOĞRULANMADI]` — NIST SP 800-38D** (GCM nonce tekrarı, §2-I'nin gerekçesi) **bu turda AÇILMADI** (PDF). **Dördüncü canlı `[DOĞRULANMADI]` olarak sayılmalıdır** ⇒ kapı-6 majör #35 (*"iki denmiş ama üç"*) **artık DÖRT'tür** ve bu **E-24'ün kapsamına girer**.
- **✅ ARAÇ YENİDEN KOŞULDU (düzeltmelerden SONRA, araca DOKUNULMADI):** altın küme **`EXIT=0`** · v7: **`K1`=0 · `K4`=5 · `K6`=2 ⇒ TOPLAM 7 = v6 taban çizgisi** · `capasiz_tablo`=0 · özet değişmedi ⇒ **atıf düzeltmeleri YENİ araç bulgusu ÜRETMEDİ.** *(Bu turda ikinci kez: yeni metin yazıldı, araç koşuldu, taban çizgisi korundu.)*
- **🔴 BU TURUN KENDİ ÜRETTİĞİ İKİNCİ KUSUR [gizlenmiyor]:** S-3'ün düzeltmesini yazarken **aynı hükmü iki kez yazan bir TEKRAR** ürettim (*"Momentum döndürme + yeniden-kullanım tespiti uygular ⇒ ihlal yoktur"* cümlesi hem yeni metinde hem eski devamında). **Araç bunu göremez** (mekanik kapısı yok) — **kendi okumamla** yakalandı ve temizlendi. ⇒ **K38'in ölçtüğü örüntünün doğrudan kanıtı: prozada kalan sınıf mekanik korumasızdır.** *Kapı-7 bu sınıfı aramalıdır.*
- **KALAN İŞ [K38'in sırasından, BU OTURUMDA YAPILMADI]:** **(1) §3.1'in TAM yeniden ölçümü** (araç-sınırları beyanı **TARİHLİ** yazılacak — §8.4'te borç olarak duruyor) · **(2) §0'a v6→v7 DEĞİŞİM KAYDI** (§0'ın başlığı hâlâ *"v4 → v5 değişim kaydı"*) + **satır 3 ve satır 16'nın bayat metni** · **(3) kanonik yola alma (K30-d) + v6 arşive** · **(4) NIST SP 800-38D atfı.**
- **✅ COMMIT — `8e57015`** *"docs(adr-0003,kanit): open every NORMATIVE citation against its primary source (K39-b) …"* · **3 dosya · +139 / −3** · yeni: `KANIT/adr-0003/v7-normatif-atif-denetimi.md`. **Ağaç TEMİZ · `.git/index.lock` YOK** (errata koşulu koşuldu) · `origin/main`'den **2 İLERİ** (`696db44` + `8e57015`) ⇒ **PUSH ONUR'DA.**
- **✅ KAPANIŞ ÖLÇÜMÜ (kaynaktan, `sha256`):** `PROJE_HAFIZA.md` **409.497 bayt · 1.134 satır** (`913DD85D…`) · **v7 300.930 bayt · 1.386 satır** (`2AE2FB29…`) · yeni KANIT **10.254 bayt · 119 satır** (`AF866AC0…`) · **üçünde de `U+FFFD` 0 · CRLF 0.** **Kanonik v6 ve araç DOKUNULMADI** (v6 `6F6BE71A…` · araç `A22841F2…`).
- **⚠ 🆕 ORTAM UYARISI [ÖLÇÜLDÜ — bir commit denemesi bu yüzden BAŞARISIZ oldu]: COMMIT MESAJINA ÇİFT TIRNAK (`"`) YAZMA.** Desktop Commander'ın koşturduğu PowerShell, `git commit -m $mesaj` çağrısında mesajın içindeki `"` karakterlerinde argümanı **BÖLÜYOR** ve parçaları git'e **pathspec** olarak veriyor ⇒ `error: pathspec 'a' did not match any file(s)` … **commit ATILMIYOR ama dosyalar STAGED kalıyor** (sessiz yarım durum: `git status` `A `/`M ` gösterir, `git log` değişmez). **KURAL:** commit mesajı **saf ASCII, tırnaksız**; alıntı gerekiyorsa tırnak yerine düz ifade kullan. **Her commit'ten sonra `git log --oneline -1` ile SHA'yı DOĞRULA** — `status`'un temiz olmaması tek başına yeterli sinyal değildir.
- **[K21 ÖLÇÜMÜ — KAPANIŞ] 554.906 token = %55,5 ⇒ 🟡 SARI.** Eğri: 133k → 320k → 446k → **555k**. ⇒ **K21 gereği: eldeki kalem (K39-b(a)) BİTİRİLDİ ve COMMIT'LENDİ, YENİ BÜYÜK İŞ AÇILMADI, devir notu yazıldı.** Devir **ölçüme dayanıyor**, hisse değil.
- **Sıradaki ilk iş [TEMİZ OTURUM]:** (1) **§0'ın başlığı + v6→v7 değişim kaydı + satır 3/16'nın bayat metni** (en ucuz, kesin kapanır) → (2) **§3.1'in TAM yeniden ölçümü**, araç-sınırları beyanı **TARİHLİ** (§8.4'ün borcu) → (3) **NIST SP 800-38D atfı** (dördüncü canlı `[DOĞRULANMADI]`) → (4) **kanonik yola alma (K30-d) + v6 arşive** → sonra **AYRI EL: K38-b'nin YAML'ı + aracın 2. onarımı (üç yeni kontrol)** → **KAPI-7 = KİLİT KAPISI, AYRI OTURUM.** **Paralelde `slice-3b` AYRI OTURUMDA (K38-c).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 26 EK: **K38 — ADR 0003'ÜN UZAMASININ KÖK NEDENİ ÖLÇÜLDÜ; ÜÇ YAPISAL KARAR KİLİTLENDİ**)

> ⚠ **BU BLOK K37'DEN SONRA YAZILDI VE ONU DEĞİŞTİRİR.** K37'nin *"Sıradaki ilk iş: ~28 majör"* satırı **K38-a ile GEÇERSİZDİR** — aşağıdaki yeni sıra bağlayıcıdır.
> ⚠ **EŞZAMANLILIK UYARISI:** bu blok, `slice-3b` için açılmış **başka bir Cowork oturumu çalışırken** yazıldı. **Herhangi bir oturum `PROJE_HAFIZA.md`'ye yazmadan ÖNCE dosyayı YENİDEN OKUMAK ZORUNDADIR** (bayat kopya üzerine yazma = checkpoint kaybı).

- **🔬 KÖK NEDEN [ÖLÇÜLDÜ — yedi turun bulguları sınıflandırılarak]:** ADR 0003 aynı anda **üç iş** yapıyor: tasarım karar kaydı · **58 mutantlı test spesifikasyonu** · denetim defteri (282 KB · 1.306 satır). Blokerlerin ve majörlerin **ezici çoğunluğu tasarım hatası DEĞİL, mutant tablosu hatasıdır** (*kill sinyali ayırt edici değil · önkoşul pinsiz · ayak kendi mutasyonu altında ölmüyor*). **Test tasarımı okunarak doğrulanamaz, KOŞULARAK doğrulanır** — ama koşulacak kod (`slice-3c-auth`) **YOK**. ⇒ **Yedi turdur var olmayan bir kodun testleri üzerine düşünce deneyi yapılıyor;** düşünce deneyinin verimi sabit orandadır ve **her tur yeni metin yazdığı için yeni kusur üretir** ⇒ **Zeno süreci: yakınsar ama bitmez.**
- **📊 ÖRÜNTÜ TEK CÜMLEDE [ÖLÇÜLDÜ]: MEKANİKLEŞTİRİLEN KUSUR SINIFI BİR DAHA GERİ GELMEDİ; PROZADA KALAN SINIF HER TURDA GERİ GELDİ.** Kanonik sayı kopyası (`K4`, araçta) → taban çizgisi sabit · tablo hücre bütünlüğü (oturum 26'da betiği yazıldı) → v6'da 4, v7'de **0**, tekrar etmedi · **buna karşılık** *"beyan edildiği söylenen ama yazılmamış"* (B5-5 · B6-8 · K35'in uydurulmuş alıntısı · majör #2'nin üç sarkan atfı) **DÖRT TURDUR** tekrarlıyor ve **mekanik kapısı YOK**; kör kapı ve ölü tuzak sınıfları da öyle. **Ek kanıt (oturum 26):** bu oturum 5 bloker + 10 majör kapatırken **5 yeni kusur üretti** ve **beşini de KOŞULAN araç buldu, akıl yürütme HİÇBİRİNİ bulmadı.**
- **[KARAR — Onur, 26 Tem 2026] K38-a: KİLİT ÖLÇÜTÜ DARALTILDI. BLOKER = (1) KÖR KAPI · (2) İNŞA EDİLEMEZLİK · (3) KIRMIZI ÇİZGİ İHLALİ.** Majörler bundan böyle **SPEC-ERRATA**'dır: kilidi **engellemez**, `GOREV-slice-3c-auth` spec'ine **adlandırılmış kalem** olarak geçer ve build'de kapanır. **Kalan mutant-tablosu riski beyan edilir ve build'e devredilir — orada zaten KOŞULACAK.** *Yürürlük:* **tarihli errata ile YALNIZ İLERİYE**; kapı-1…6'nın 63 blokeri **zaten kapatıldığı için geriye dönük yeniden sınıflandırma YAPILMAZ** (K34-a'nın bu şıkkı reddetme gerekçesi buydu ve **ölçümle karşılandı: kapatılmış bir blokerin sınıfını değiştirmek hiçbir şeyi değiştirmez**). *Bedel [adlandırılmış]:* **kilit ölçütü ilk kez gevşedi ve EMSAL DOĞDU**; kapı-7 bunu itiraz konusu yapabilir ve yapmalıdır. *Reddedilenler:* **ölçüt aynı kalsın (K34-a yürürlükte)** — en savunulabilir duruş, ama tahminen 4-6 oturum daha ve **üst sınır YOK**; 15 Ağu fiilen düşer · **melez: yalnız mekanikleşebilir majörler kilidi engeller** — *"mekanikleşebilir"* sınırı tur ortasında tanımlanır ve tartışılır.
  · **⚠ İŞLETİM OKUMASI [bu oturumun yorumu, kapı-7 itiraz edebilir]:** K38-a **mekanik kapı şartını KALDIRMAZ.** Kilit için **`K1` = 0 · tablo kusuru = 0 · altın küme `EXIT=0`** koşulu **aynen durur**; daralan şey **insan/LLM okumasıyla bulunan majörlerin kilidi engellemesidir**.
- **[KARAR — Onur, 26 Tem 2026] K38-b: MUTANT TABLOSU **VERİYE** ÇEVRİLİR — `docs/ADR/0003-mutantlar.yaml` (ya da eşdeğeri) ve **ARAÇ ONDAN HESAPLAR**.** Alanlar: `id · seviye · mutasyon · önkoşullar[] · ayaklar[] · çıpalar[] · ayırt-edicilik notu`. **Hesaplanacaklar (artık ELLE YAZILMAZ):** numara bütünlüğü (M1–M58 · tampon M59 · `M60` pini) · çıpa çözümü · **§3.1'in dağılım sayıları** (`kapılı/beyanlı/devredilmiş`) · seviye sayımları (`B/TS/TC/KON/NA/DART`). *Gerekçe [ÖLÇÜLDÜ]:* 58 mutant bugün **6 sütunlu markdown prozasında** duruyor; araç proza üzerinde regex koşmak zorunda olduğu için `[KS-13]`'ü *"tek haneli"* diye **kapsam dışı** bırakıyor ve `§3.2(9)`'daki `9`'u sınır-değer sanıyor (**yapısal yanlış-pozitif, taban çizgisi 2**) ⇒ **elle yazılan her sayı bayatlıyor** ve yedi turun bulgularının büyük kısmı **tam olarak bu yüzden** doğdu. *Bedel [adlandırılmış]:* bir kerelik dönüştürme (~1 oturum) · ADR §3'ün **sunum biçimi değişir** · **kapı-7'ye DÖRDÜNCÜ nesne girer** (v7 + araç + kapı-6 raporu + YAML). *Reddedilenler:* **prozada kalsın** (sıfır maliyet; ama ölçülmüş örüntü gereği aynı sınıflar tekrarlamaya devam eder) · **sonraya bırak, önce çapraz-atıf çözücüsü** (en yüksek getirili tek kontrol; ama §3.1'in dağılımı bir tur daha ELLE yazılır).
  · **ARACIN 2. ONARIMINA (AYRI EL, K34-f) ÜÇ YENİ KONTROL — ÖNCELİK SIRASIYLA:** **(1) ÇAPRAZ-ATIF ÇÖZÜCÜ** — *"§X'te beyanlıdır"* · *"`0002:187`"* · *"§6 #N"* · *"K3-…'de yazılı"* biçimindeki **her atıf ÇÖZÜLMEK ZORUNDA**; **tek başına bu kontrol B5-5 · B6-8 · K35'in uydurulmuş alıntısı ve majör #2'nin üç sarkan atfını yakalardı — DÖRT TURUN EN AĞIR BULGULARI.** **(2) TABLO HÜCRE BÜTÜNLÜĞÜ** (betiği `KANIT/adr-0003/v7-tablo-butunlugu-olcumu.md`'de yazılı; altın kümeye **ÜÇ mutasyon vakası** ile girer). **(3) KILL-SİNYALİ LINT'İ** — her mutant satırı **önkoşul** + **mutasyonun dokunduğu mekanizmaya atıf yapan en az bir ayak** + **çıplak kanonik literal yasağı** + **ayırt-edicilik beyanı** taşımak zorunda.
- **[KARAR — Onur, 26 Tem 2026] K38-c: `slice-3b` (FLUTTER İSTEMCİ) **PARALEL BAŞLIYOR** ve işi **AYRI BİR COWORK OTURUMU** yapar.** Oturum 26 (bu el) **istemciye DOKUNMAZ**. *Gerekçe [ÖLÇÜLDÜ]:* istemci sözleşmesi (**K3-L1…K3-L10 + M-L5…M-L10**) **kararlaştırıldı ve `slice-3b`'ye devredildi** ⇒ kalan 21 majöre **bağımlılığı YOK**; ölçüm: `src/` + `tests/` altında **0 `.dart` dosyası**, `DESIGN.md` **YOK**, `.github/workflows` **YOK** ⇒ ödevin **görünen yüzü sıfır**. *Bedel [adlandırılmış]:* **iki cephe** açılır, `DESIGN.md` + a11y kapısı da gerekir, dikkat bölünür ve **iki oturumun aynı hafıza dosyasına yazma riski doğar** (yukarıdaki eşzamanlılık uyarısı bu yüzden yazıldı). *Reddedilenler:* **önce ADR kilitlensin** (tek cephe, omurga-önce doktrinine birebir uyum; ama 15 Ağu'ya yetmez) · **önce `DESIGN.md` + tek dikey ekran** (vitrin doğar, risk düşük; ama yarım istemci de *"yarım iş"* olarak görülebilir).
- **🔴 YENİ SONLANMA TANIMI [K38-a'nın sonucu] — ADR 0003 ŞU ÜÇ KOŞUL SAĞLANINCA KİLİTLENİR:** (1) **mekanik kapılar YEŞİL** (`K1`=0 · tablo=0 · altın küme `EXIT=0`) · (2) **kör kapı · inşa edilemezlik · kırmızı çizgi ihlali = SIFIR** (kapı-7 bunu arar) · (3) kalan her kalem **beyan edilmiş** ve `GOREV-slice-3c-auth` spec'ine **adıyla devredilmiş**. **Kapı-7 artık bir "tur" DEĞİL, bir KİLİT KAPISIDIR.**
- **Sıradaki ilk iş [K37'nin sırasını GEÇERSİZ KILAR]:** (1) **21 majörü SPEC-ERRATA listesine çevir** (kapatmaya çalışmadan; adlandırılmış kalem olarak `GOREV-slice-3c-auth`'a) + **K34-g'nin `slice-3b` borç listesi** → (2) **§3.1'in yeniden ölçümü + §0 v6→v7 değişim kaydı + bayat "TASLAK v6" altbilgisi** → (3) **~30 birincil-kaynak atfı** (bu **kırmızı çizgi/olgu** eksenidir, majör değil) → (4) **kanonik yola alma (K30-d) + v6 arşive** → (5) **K38-b: mutant YAML'ı + aracın 2. onarımı (ÜÇ yeni kontrol) — AYRI EL** → (6) **KAPI-7 = KİLİT KAPISI — AYRI OTURUM** (ADR 0002 + `src/` + `tests/` + `docker-compose.yml` + YAML **BAĞLI**). **Paralelde: `slice-3b` AYRI OTURUMDA (K38-c).**


## ⏭ CHECKPOINT (26 Tem 2026 — oturum 27 açılış: **PROTOKOL TAM KOŞTU; OTURUM 26'NIN BEYAN ETTİĞİ OKUMA SINIRI KAPANDI**)

- **Protokol koşuldu (oturum 27):** `CLAUDE.md` **60/60 satır TAM** (dosya oturum 26'nın git erratasıyla 48 → 60 satıra çıkmış; eski checkpoint'lerin *"48 satır"* / *"49 satır"* sayıları o tarihler için doğrudur, bugün için BAYATTIR). `PROJE_HAFIZA.md` **1.068 satırın 1.068'i TAM** — on üç ardışık okuma bloğuyla, **ana oturumun kendi bağlamında; alt-ajan özeti AKTARILMADI.** 🟢 **OTURUM 26'NIN BEYAN ETTİĞİ SINIR (*"satır 100–950 satır satır OKUNMADI"*) BU OTURUMDA KAPANDI** — kapı-7'ye devredilen o kalem artık açık değildir. *(Oturum 25 çubuğu 918/918 idi; bu oturum 1.068/1.068 ile onun üstündedir.)*
- **✅ AÇILIŞ KAPISI (protokol adım 2):** K34'te ölçüldü, bu projede **YOKTUR** ⇒ uygulanmaz (atlanmadı).
- **✅ GİT (Onur'un makinesinde YERLİ PowerShell + `git --no-optional-locks`, Desktop Commander ile):** ağaç **TEMİZ** (`status --porcelain` çıktısı **boş**) · dal **main** · HEAD **`78fa5d603f6909b6292e49fb9ecfdf74a8b4e072`** · `origin/main` **AYNI SHA** · `rev-list --left-right --count origin/main...HEAD` ⇒ **`0  0`** (ne ileri ne geri) · `.git/index.lock` **YOK** (`Test-Path` ⇒ `False`). ⇒ **Devir notuyla BİREBİR; atılacak commit ve push borcu YOK.**
- **✅ DÖRT DEĞİŞMEZ YENİDEN ÖLÇÜLDÜ (Onur'un diskinden, `Get-FileHash -Algorithm SHA256`) — DÖRDÜ DE DEVİR NOTUYLA BİREBİR:**
  · kanonik **v6** `docs/ADR/0003-kimlik-cekirdegi.md` **241.689 bayt · 1.123 satır · `6F6BE71A0D86CB32D7D6B2B856C1526247CACD8AA57B0B4B36FACD8A8638CB08` — TEK BAYT DOKUNULMADI.**
  · **v7 çalışma dosyası** `docs/ADR/0003-kimlik-cekirdegi-v7-YAZIM-DEVAM-EDIYOR.md` **281.928 bayt · 1.305 satır · `C27749F571C0D3EAD390E283148778F3C71B735AF89F413700E1FE30D451575F`.** *(Devir notu **1.306 satır** diyor; fark PowerShell `Get-Content`'in son satır-sonunu saymamasındandır — aynı sayım kuralı farkı kapı-5'te de ölçülmüştü, **içerik aynıdır**, `sha256` bunu kanıtlar.)*
  · **araç** `araclar/adr-kapi-taramasi.py` **50.582 bayt · `A22841F29A42DBAC76206EF5ABA84BB3E2C68F1788A6E602012F3045EE81ADB8` — K34-f TUTUYOR, DOKUNULMADI.**
  · `CLAUDE.md` **4.681 bayt · `8A5CD1C1563F9124B875DE4FE72FF5BC4F8DE00137D725183A42D539F613C20C`** · `PROJE_HAFIZA.md` (okuma anında) **378.030 bayt · 1.068 satır · `B7B7FDE97CAA6CAA11D84619EAB0C4F8017BBC0CDE9A656B602C79A8CB2C9E7A`** · `docs/ODEV.md` **16.605 bayt · 222 satır**.
- **[K21 ÖLÇÜMÜ — kendi oturum-id'siyle, açılış]** transcript `95563152-b8f2-5565-b3e3-d9fd653d94e4` (bu oturumun **tek** `jsonl`'i, mtime canlı) ⇒ `input 2 + cache_read 297.627 + cache_creation 22.717` = canlı bağlam **320.346 token = %32,0 ⇒ 🟢 YEŞİL.** *(Eğri: 133k → **320k**; sıçramanın tamamı hafızanın 1.068 satırının okunmasıdır.)* ADR yazma yasağı bandında **DEĞİLİZ**. **Beyan edilmiş sınır:** ölçüm transcript'e **son yazılmış** `usage` kaydıdır ⇒ bir tur geriden gelir.
- **⚠ 🆕 ORTAM UYARISI [bu oturumda ÖLÇÜLDÜ — kanla yazılıyor]: PowerShell 5.1 `.ps1` dosyalarını ANSI (cp1254) okur.** Desktop Commander `write_file` betiği **UTF-8 (BOM'suz)** yazar ⇒ betiğin İÇİNE `MEMO ÖDEV PROGRAMLAR\TO DO LİST` gibi **Türkçe karakterli bir yol LİTERAL olarak** yazılırsa yol mojibake olur (`MEMO Ã–DEV…`) ve `Test-Path` **sessizce `False`** döner, `git -C` *"No such file or directory"* der — **yanlış bir "repo yok" hükmü üretir.** **KURAL:** betiğe Türkçe yol literali YAZMA; yolu **ASCII joker ile keşfet** (`Get-ChildItem -Directory | Where-Object {$_.Name -like 'MEMO*PROGRAMLAR'}` → `'TO DO*'` → `Join-Path ... 'Momentum'`). *(Bu, K36'nın `interact_with_process` stdin bulgusunun `.ps1` dosya yolundaki kardeşidir; kök neden aynı değil — orada stdin kodlaması, burada dosya okuma kodlaması.)*
- **📄 GEÇİCİ DOSYA (repo DIŞINDA, bilinçli):** `…\TO DO LİST\_cowork_olcum.ps1` — ölçüm betiği. **`Momentum/` kökünün DIŞINDADIR** ⇒ `git status` etkilenmez (ölçüldü: ağaç temiz). Silinmesi Onur'dadır (sandbox kalıcı silme yapmaz — kırmızı çizgi #4).
- **Sıradaki ilk iş:** devir notunun **SONRAKİ İŞ** sırası — (1) **kalan 21 majör** (§3.2 mutant tablosu #11–#22 · §3.3 #23–#27 · §3.4 #28–#31 · §3.5 #32–#37) → (2) **K34-g `slice-3b` borç listesi** → (3) **§3.1'in TAM yeniden ölçümü + araç yeniden koşulur** → (4) **§0 değişim kaydı + bayat "TASLAK v6" altbilgisi** → (5) **~30 birincil-kaynak atfı** → (6) kanonik yola alma (K30-d) → **ARACIN 2. ONARIMI (AYRI EL, K34-f)** → **7. KAPI (AYRI OTURUM)**. **Kapsam Onur'a şıklarla sunuldu; kilitlenmeden yazıma BAŞLANMADI (protokol adım 5).**

## ⏭ CHECKPOINT (26 Tem 2026 — oturum 26 açılış: **K37 — ÜÇ ÇATAL KİLİTLENDİ; B6-1'İN ÖLÇÜMÜ TURDAN ÖNCE KOŞTU**)

- **Protokol koşuldu (oturum 26):** `CLAUDE.md` **48/48 satır TAM**. `PROJE_HAFIZA.md` **tepe bloğu (satır 1–99: K36 · K35 · K34) + kuyruk (satır 951–1014: PROJE · KİLİTLENEN KARARLAR · DENETÇİ MİMARİSİ · KIRMIZI ÇİZGİLER · ORTAM · KARAR GÜNLÜĞÜ · AÇIK İŞLER) TAM.** 🔴 **BEYAN EDİLMİŞ SINIR, gizlenmiyor:** **satır 100–950 (K33 ve öncesi) satır satır OKUNMADI** — dayanak, dosyanın kendi kuralı (*"en tepedeki checkpoint + karar günlüğü = oturumun özeti"*); oturum 25 aynı işi **918/918** yaparak koşmuştu ⇒ bu oturum o çubuğun ALTINDA kaldı ve bunu bir **güvence değil SINIR** olarak yazıyor. Kapı-7 bu sınırı denetlemelidir.
- **✅ AÇILIŞ KAPISI (protokol adım 2):** K34'te ölçüldü, bu projede **YOKTUR** ⇒ uygulanmaz (atlanmadı).
- **✅ PUSH YAPILDI [26 Tem 2026, oturum 26 kapanış — Onur kendi terminalinden attı, Cowork ÖLÇTÜ]:** `56362ed..b973b08  main -> main` · **28 commit** · 169 nesne · 793,04 KiB. **Doğrulama (Cowork'ün kendi ölçümü, Onur'un çıktısına GÜVENİLMEDİ):** `origin/main` = **`b973b08`** = HEAD · `rev-list --count origin/main..HEAD` = **0** · ağaç **TEMİZ** · `.git/index.lock` **YOK**. ⇒ **Oturum 22'den beri biriken push borcu KAPANDI** (bu, projenin ilk 28-commit'lik toplu push'udur; öncesinde `origin/main` `56362ed`'de duruyordu).
- **✅ GİT (oturum AÇILIŞINDAKİ ölçüm — tarihsel):** ağaç **TEMİZ** · HEAD **`21ae594`** *"docs(adr-0003,kanit): v7 wip - citation-hygiene fixes + new mechanical table-integrity gate (K36)"* · `origin/main`'den **25 ileri** · **PUSH YOK** · `.git/index.lock` **YOK**. Devir notuyla birebir.
- **✅ ÜÇ DEĞİŞMEZ YENİDEN ÖLÇÜLDÜ (Onur'un diskinden, `sha256`):** kanonik v6 `docs/ADR/0003-kimlik-cekirdegi.md` **241.689 bayt · `6F6BE71A0D86CB32D7D6B2B856C1526247CACD8AA57B0B4B36FACD8A8638CB08` — TEK BAYT DOKUNULMADI** · v7 çalışma dosyası **255.743 bayt · 1.226 satır · U+FFFD 0** (`BA4B8A80…6DF2B86B`) · araç `araclar/adr-kapi-taramasi.py` **50.582 bayt · `A22841F2…EE81ADB8` — K34-f TUTUYOR.** Ek: ADR 0002 **31.402 bayt** · ADR 0001 **14.137 bayt** · `docs/ODEV.md` **16.605 bayt** · `KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/` **11 dosya · 457.046 bayt** (K34'ün kanonik değeriyle birebir).
- **[K21 ÖLÇÜMÜ — açılış, kendi oturum-id'siyle]** transcript `4122351b-b595-560c-9027-b80c00da307f` ⇒ canlı bağlam **131.404 token = %13,1 ⇒ 🟢 YEŞİL.** ADR yazma yasağı bandında **DEĞİLİZ**.
- **[KARAR — Onur, 26 Tem 2026] K37-a: BU OTURUMUN KAPSAMI = BEŞ BLOKER + B6-A/B/C/D + ÜÇ KOZMETİK HÜCRE SAPMASI + TARAYICININ v7'DE KOŞULMASI.** *Gerekçe:* her kalem **tam** bitirilir ve bağlam 🟡 bandına girmeden checkpoint yazılır (K21). *Bedel [adlandırılmış]:* **~28 majör + K34-g borç listesi + §3.1'in yeniden ölçümü BU OTURUMDA YAPILMAZ** ⇒ v7 bu oturumun sonunda da **YARIMDIR** ve bir oturum daha harcanır. *Reddedilenler:* **yalnız 5 bloker** (en güvenli; kozmetik taban çizgisi sıfırlanmaz, gelecekte gerçek bir sapma gürültüye karışır) · **kalan işin TAMAMI** (bir oturum kazanılır; ama K21'in *"dolu bağlamda ADR yazma yasağı"* ihlal edilme riski ve iş **yarım** kalma riski) · **önce ~28 majör** (mekanik ⇒ hızlı; ama blokerler açıkken belge hiçbir hâlde kilitlenemez ve devir notunun sırası bozulur).
- **[KARAR — Onur, 26 Tem 2026] K37-b: `[KS-31]` LİSTESİNİN KAYNAĞI = `zxcvbn` (PyPI, **MIT**, Daniel Wolf) FREKANS SIRALI `passwords` LİSTESİNİN İLK 10.000'İ.** Repoya **`.txt` veri dosyası** olarak gömülür (kod bağımlılığı **YOK** ⇒ CVE yüzeyi yok), lisans + atıf dosyası repoya girer, **türetme tarifi ve beklenen `sha256` ADR'de PİNLENİR.** *Gerekçe [ÖLÇÜLDÜ, turdan önce]:* ilk 10.000 kayıt içinde **≥`[KS-17]`=15 karakterli TAM 9 kayıt** var (rank **1516 · 2174 · 6236 · 6766 · 8341 · 8512 · 8609 · 9980 · 9990**) ⇒ **K28-c'nin seçim ölçütü DEĞİŞMEK ZORUNDA DEĞİL** ve M56 probu gerçek bir kara-liste kaydına çevrilebilir. *Bedel [adlandırılmış]:* kaynak bir **Python paketinin veri dosyasından türetilir**; *"sektör standardı"* algısı SecLists kadar güçlü değildir ve türetme tarifi **her build'de yeniden doğrulanmak** zorundadır. *Reddedilenler:* **SecLists top-10000 (MIT — lisans teyit edildi)** (portfolyoda savunması en kolay; ama ≥15 karakterli kayıt içerip içermediği **ÖLÇÜLMEMİŞTİ** ⇒ turun ortasında ikinci bir çatal doğma riski) · **ölçüt iki parçalı olur** (prob garantili çalışır; ama `[KS-31]` tek sayı olmaktan çıkar, §2-B akışı + M56 çıpası yeniden yazılır) · **kaynak seçimi spec'e devredilir** (K34-b'nin *"bu turda ÖLÇÜLECEK"* hükmü ihlal edilir; kapı-7 aynı kör kapıyı ikinci kez bulur) · **NuGet `zxcvbn-core` ELENDİ — ölçüldü:** son sürüm **8.0.0-beta0002 (Ocak 2022)**, **lisans ifadesi BOŞ** ⇒ **kırmızı çizgi #3'ü geçmez.**
- **[KARAR — Onur, 26 Tem 2026] K37-c: M56 PROBUNUN POZİTİF AYAĞI = `mailcreated5240` (15 karakter, **rank 1516**).** *Gerekçe:* listenin **derininde** (ilk %15'lik dilim) ⇒ liste yeniden üretilse/kısaltılsa bile probun sessizce ölme (ölü tuzak) riski en düşük; `[KS-17]`=15'i **tam** karşılar ⇒ uzunluk kontrolünden **bağımsız olarak** kara-liste kontrolünü ölçer (v6'nın `123456` probunun kusuru tam buydu). *Bedel:* rakam listenin sırasına bağlıdır ⇒ **dosyanın `sha256`'sı pinlenmek ZORUNDA** (pin düşerse prob da düşer). *Reddedilenler:* **`qazwsxedcrfvtgb`** (klavye yürüyüşü, okunaklı; ama **rank 8609 = kenar** ⇒ kısaltmada sessizce ölür) · **`1qaz2wsx3edc4rfv`** (16 kr, sınır-değer testiyle karışmaz; rank 6766 orta-kenar) · **iki kayıt birden** (tek kaydın düşmesi probu öldürmez; ama M56 üç ayaklı olur, koşum maliyeti ve tablo satırı büyür).
- **✅ B6-1'İN İKİ KAPISI KOŞTU — İKİSİ DE GEÇTİ [ÖLÇÜLDÜ, ham çıktı KANIT'ta].** 📄 `KANIT/adr-0003/v7-b6-1-kara-liste-kaynagi-ve-kapisi.md`.
  · **LİSANS (kırmızı çizgi #3, ALTI TURDUR KOŞMAMIŞTI):** `zxcvbn 4.5.0` wheel `sha256 2b6eed62…c66bd5ff` · `METADATA` **`License: MIT`** + `Classifier: License :: OSI Approved :: MIT License` · `LICENSE.txt` birebir *"MIT License / Copyright (c) 2016 Daniel Wolf"* (`sha256 5ac25259…d9869792`) ⇒ **izinli OSI ailesinde, GEÇTİ.** Yükümlülük: lisans metni + atıf veri dosyasının yanında repoya girer.
  · **CVE:** `pip-audit 2.10.1` · `zxcvbn==4.5.0` ⇒ *"No known vulnerabilities found"* · `EXIT=0`. **Beyan edilmiş iki sınır:** anlık görüntüdür (26 Tem 2026, OSV/PyPI) · paket **çalışma zamanı bağımlılığı DEĞİL** (bir kez veri türetir; üretime giren şey `.txt`) ⇒ çalışan sistemin CVE yüzeyi etkilenmez.
  · **TÜRETME PİNİ:** `FREQUENCY_LISTS['passwords']` ilk **10.000**, sırası korunarak · UTF-8/BOM yok/LF/sonda bir LF ⇒ **78.880 bayt · 10.000 satır · beklenen `sha256 839f86a5…dd1462`.** **Dosya bugün repoda YOKTUR** — ADR **tarifi + beklenen sha256'yı** pinler, üretim+pin doğrulaması `GOREV-slice-3c-auth` build'inde koşar (**K22-a sınıfından kaçınma: var olmayan artefakta çıpa YOK**).
  · **PROB:** `mailcreated5240` **15 kr · rank 1516 · listede ✓** · negatif ayak `MomentumKapiTesti2026` (21 kr) **30.000'lik kaynağın hiçbir yerinde YOK** ⇒ mutasyon altında ve olmadan **iki farklı sonuç** ölçülebilir; v6'nın `123456` probunda bu fiziksel olarak imkânsızdı.
- **🔴 BU TURUN ÜRETTİĞİ YENİ BULGU [ÖLÇÜLDÜ — kimse sormadı, ölçüm verdi]: `[KS-31]`'İN MARJİNAL DEĞERİ KÜÇÜK.** İlk 10.000 kaydın **9'u** ≥15 karakter; kalan **9.991'i ≤14** ⇒ `[KS-17]`=15 asgari uzunluk kuralı onları **kara listeye hiç sormadan** reddediyor ⇒ **kara listenin fiilen reddedebildiği parola sayısı 9; listenin %99,91'i ULAŞILAMAZ** (30.000'lik kaynakta 31). **K28-c çürümez ama GEREKÇESİ DEĞİŞİR:** NIST SP 800-63B-4'ün kara-liste `SHALL`'ı asgari uzunluğun **8** olabildiği politikalar için yazılmıştır. ⚠ Bu bulgu bir ÜRETİMDİR, **kapı-7 adjudike etmelidir**.
- **[KARAR — Onur, 26 Tem 2026] K37-d: BULGU **BEYANLI SINIR** OLARAK İŞLENİR — `[KS-31]` = 10.000 KALIR.** §6'ya ölçülmüş beyan girer (*9/10.000 ulaşılabilir · NIST'in `SHALL`'ı 8 karakter asgari için yazılmıştır · listenin değeri yalnız **uzun-ama-sızmış** parolalarda ortaya çıkar: savunma derinliği*) ve K3-B6(2)'nin gerekçesi buna göre düzeltilir. *Gerekçe:* hiçbir kanonik sayı değişmez, M56 çıpası ve §2-B akışı korunur, ölçülmüş dürüst beyan denetlenmemiş bir uyumluluk kutucuğundan güçlüdür. *Bedel [adlandırılmış]:* *"dokuz parola için 78 KB veri gömüldü"* eleştirisi **kabul edilmiş bedel** olarak kalır ve **kapı-7 bunu majör sayabilir.** *Reddedilenler:* **kaynak 30.000'e çıkar** (ulaşılabilir 9→31; ama `[KS-31]` sayısı + K28-c ifadesi yeniden yazılır, repo ~236 KB büyür, marjinal değer hâlâ küçük) · **iki parçalı liste, ≥15 korpusu Onur'un makinesinde indirilir** (kara listenin değeri gerçekten artar — asıl tehdit uzun-sızmış parolalardır; ama K34-b'nin reddedilen şıkkı geri gelir, `[KS-31]` tek sayı olmaktan çıkar, §2-B + M56 yeniden yazılır, turda +1 adım) · **kara liste kapsam dışı, K28-c geri alınır** (en basit; ama NIST'in İKİNCİ `SHALL`'ı adlandırılmış sapma olarak geri döner, M56 silinir, §3.1 dağılımı değişir, kapı-7 *"uyumluluk kalemi sessizce düştü"* diye bloker sayabilir).
- **✅ K37-a'NIN KAPSAMI TAMAMLANDI — DOKUZ BLOKERİN DOKUZU DA KAPANDI [ÖLÇÜLDÜ]:**
  · **B6-1** (M56 kör kapısı) — kaynak PİNLİ (`zxcvbn 4.5.0`, türetme tarifi + beklenen `sha256 839f86a5…dd1462`), **lisans + CVE kapıları KOŞTU ve GEÇTİ**, prob `mailcreated5240` (rank 1516, ölçüldü) + negatif ayak. **Dosya bugün repoda YOK ⇒ K22-a sınıfından kaçınmak için tarif+digest pinlendi, üretim build'de koşar.**
  · **B6-5** (§3.1'in *"araç altın kümede kendini kanıtlamadan koşmaz"* GÜVENCESİ) — **kaynak 880-881'den birebir ölçüldü, cümle GÜVENCE değil SINIR olarak yeniden yazıldı**; telafi *"yol kısıtı"* olarak adlandırıldı (mekanik zorlanmıyor).
  · **B6-6** (K3-L8'in 4. dalı — K34-c) — **çıkışta aktif profil kaydı temizlenir**, DB dosyası SİLİNMEZ (kırmızı çizgi #4 korunur), **🆕 M-L10** doğdu (DART/`[devir]`), §3.1'e ADIYLA beyan edildi.
  · **B6-7** (§3.2'nin izolasyon maddesi — K34-d) — kural *"`/login` ya da `/register` çağıran HER test sınıfı kendi fabrikasını kurar"* oldu; bedeli (host kalkış maliyeti + CI yok) adlandırıldı.
  · **B6-4'ün ADR AYAĞI** — aracın **satır 412'deki sessiz `K4` kaçış filtresi** birebir alıntıyla §3.1'de beyan edildi; **araç ONARILMADI (K34-f)** ve aracın **DÖRT bilinen sınırı** adıyla listelendi.
  · **B6-A · B6-B · B6-C · B6-D de kapandı:** gölgelemenin en kötü durumu **`[KS-1]` DEĞİL `[KS-2]`** (§6 #11 + #16 hizalandı) · gölgelemenin bedeli (**aile iptali = yeniden giriş**) adlandırıldı · *"zincir yeni token basmaz"* → **"replay dalı ailenin SATIR SAYISINI artırmaz"** (iki yerde) · **PHC savunmacı dalı artık sahte-hash doğrulaması KOŞAR** ⇒ zamanlama oracle'ı kapandı, **🆕 M58** doğdu (`TC`).
  · **ÜÇ KOZMETİK HÜCRE SAPMASI düzeltildi** (HTML yorumu son `\|`'den ÖNCEYE alındı) **ve M56 satırının kaçışsız `\|`'ları kaçışlandı** ⇒ **v7'nin tablo taban çizgisi SIFIR** (v6'da 4).
- **📊 MUTANT SAYIMI:** aralık **M1–M58** · **tampon YALNIZ M59** · `M60` pini (0004) değişmedi, çakışma yok. **Bir sonraki yeni mekanizma `M59`'u tüketir ⇒ ondan sonrası `M60` pinini yeniden tartmayı ZORUNLU kılar** (kapı-7'ye devredilen adlandırılmış sınır). M-L serisi: **M-L5…M-L10**.
- **✅ DOĞRULAMA KOŞTU — 📄 `KANIT/adr-0003/v7-oturum26-dogrulama-kosumu.md`:** tablo tarayıcısı (v7 **0** kusur · v6 **4**, dokunulmadı · iki KANIT dosyası **0**) · **altın küme AYRI koşum `EXIT=0`** (B6-5'in beyan ettiği yükümlülük fiilen yerine getirildi) · araç v7'de **7 bulgu** = araç v6'da **7 bulgu** ⇒ **v7 YENİ araç bulgusu ÜRETMEDİ** (`K1`=0 · `K4`=5 · `K6`=2, üçü de v6 ile birebir). ÖZET: `karar 47 · alt_madde 13 · kapili 32 · beyanli 15 · devredilmis 5 · mutant 53`.
- **🔴 AMA İLK KOŞUMDA 13 BULGU VARDI — BU OTURUMUN KENDİ ÜRETTİĞİ DÖRT KUSUR [gizlenmiyor; dördünü de KOŞULAN ARAÇ buldu, akıl yürütme DEĞİL]:**
  1. **`K1`=1:** `K3-L8(4)` **kapısız-ve-beyansız** çıktı — kapısı `M-L10` yazılmıştı ama **`[devir]` işaretli çıpa KAPI SAYILMAZ** ⇒ §3.1 beyanı zorunluydu, eksikti. **Araç haklıydı**, eklendi.
  2. **`K4`=5 kanonik-sayı KOPYASI:** dördü bu oturumun (`10.000`×2 · `15 karakter` · `10 dk`), **biri oturum 25'in** (`GetBytes(32)`). Dördü **atfa** çevrildi; beşincisi bir **C# çağrı argümanı** olduğu için `[KS-LITERAL:]` ile **gerekçesiyle** muaf tutuldu.
  3. **ÜÇÜNCÜ BİR YANLIŞ-POZİTİF, onu BELGELEYEN cümleden doğdu:** *"aracın iki yanlış-pozitifi var"* diyen madde, ilgili bölüm atfını **rakamıyla** içerdiği için kendisi de tetikledi; **ilk düzeltme denemesi İKİ TANE DAHA üretti (9 bulgu)**; atıf **rakamsız** yazılınca taban çizgisi v6'nın **iki** bulgusuna döndü.
  4. **BİR SAYI TAHMİNİ ÖLÇÜMLE YANLIŞLANDI:** *"devredilmiş 5 → 6"* yazılmıştı; araç `devredilmiş`i **karar kimliği** düzeyinde saydığı için **değişmedi** (`alt_madde` bir arttı). Satır düzeltildi.
  ⇒ **K33'ün örüntüsünün ALTINCI kanıtı** ve bu oturumun kendi dersi: **`[KS-n]` atıf kuralı, v7'nin YENİ satırları için de bağlayıcıdır ve yazarken ihlal ediliyor; tek koruma aracı KOŞMAKTIR.**
- **DURUM (kapanışta ölçüldü):** kanonik v6 **DOKUNULMADI** (`241.689 bayt · 6F6BE71A…8638CB08`) · v7 çalışma dosyası **277.911 bayt · 1.294 satır · U+FFFD 0** (`66E30C3C…`) · araç **DOKUNULMADI** (`50.582 bayt · A22841F2…EE81ADB8`) · `PROJE_HAFIZA.md` **364.895 bayt · 1.035 satır**.
- **[K21 ÖLÇÜMÜ — kapanış]** **467.238 token = %46,7 ⇒ 🟢 YEŞİL.** Eğri (bu oturum): 131k → **467k**.
- **🔴 v7 HÂLÂ YARIMDIR — KALAN İŞ (K37-a'nın adlandırılmış bedeli):** **~28 MAJÖR** (kapı-6 §3'ün kalanı) · **K34-g'nin `slice-3b` ADLANDIRILMIŞ BORÇ LİSTESİ** (hâlâ YOK) · **§3.1'in TAM yeniden ölçümü** (dağılım cümleleri) · **~30 birincil-kaynak atfının TAMAMININ birincil kaynaktan açılması** (RFC 5321 sapması bunun ölçülmüş gerekçesi) · **§0'a v6→v7 değişim kaydı** · dosya sonundaki **"TASLAK v6"** altbilgisi hâlâ **BAYAT**.
- **[KARAR — Onur, 26 Tem 2026] K37-e: OTURUM 26 DEVAM EDİYOR — KAPSAM GENİŞLETİLDİ: `~28 MAJÖR + K34-g BORÇ LİSTESİ`.** K37-a'nın kapsamı bitti, K21 **%46,7 🟢** ölçüldü ve Onur devamı seçti. *Gerekçe:* blokerler kapandığı için majörler artık **belgenin kilitlenmesini engelleyen tek büyük yığındır**; aynı oturumda yapılırsa bağlam yeniden kurulmaz. *Bedel [adlandırılmış]:* oturum uzar ve K21'in 🟡 bandına (%55) yaklaşma riski doğar ⇒ **eldeki kalem bitirilir, checkpoint yazılır, yeni büyük iş açılmaz**; ayrıca **v7'yi yazan el sayısı artmaz ama yazdığı satır sayısı artar** ⇒ kapı-7'nin yükü büyür. *Reddedilenler:* **dar devam** (§3.1 yeniden ölçümü + §0 değişim kaydı + bayat altbilgi — belge tutarlı bir ara duruma gelirdi ama majörler yine kalırdı ve §3.1 majörlerden SONRA yine değişirdi ⇒ iki kez ölçülürdü) · **kapat, devir notu** (en güvenli; ama bir oturum daha harcanır ve majörlerin bağlamı sıfırdan kurulur).
- **✅ K37-e'NİN İLK DALGASI BİTTİ — KAPI-6 §3.1 GRUBUNUN (KANONİK SAYI DİSİPLİNİ) DOKUZ MAJÖRÜNÜN DOKUZU DA KAPANDI.** *(#1 · #3 · #8 oturum 25'te; **#2 · #4 · #5 · #6 · #7 · #9 bu turda**.) Ayrıca **#10** (M56 satırının kaçışsız `\|`'ları) B6-1 ile birlikte kapanmıştı.
  · **#2 — ÜÇ SARKAN §3.1 ATFI:** `[KS-11]` · `[KS-12]` *"(kapısız — §3.1)"* ve `[KS-24]` *"(ölçüm, kapı değil — §3.1)"* diyordu, **§3.1'de karşılık gelen satır YOKTU** (B5-5/B6-8 ailesi). **İki yeni satır §3.1'e yazıldı** ve kapısızlığın GEREKÇESİ ölçüldü: tavanı ısırtmak `[KS-11]`+1 / `[KS-12]`+1 istek gerektirir, **§3.2'nin izolasyon maddesi pencere ilerletmeyi YASAKLADIĞI için** her tavan ayrı fabrika + yüzlerce istek demektir ⇒ **kabul edilen bedel adlandırıldı** (politika ayrımının K16-b'ye uygun bağlandığı mekanik olarak doğrulanmıyor; ısırtılan şey kontrol 1'in kendisi). **Kapı-7'ye açık soru bırakıldı** (politika adlarının uçlara bağlandığını ölçen ucuz bir `NA`/`TS` kapısı kurulabilir mi).
  · **#4 — YANLIŞ MUAFİYET GEREKÇESİ:** §6 #10'daki ham `15 dk`, `<!-- [KS-LITERAL: … kanonik sayı DEĞİL] -->` ile muaf tutulmuştu; **ölçüldü: o değer TAM OLARAK `[KS-1]`'dir** ⇒ gerekçe **olgusal olarak yanlıştı ve aracı bilerek kör bırakıyordu.** Sayı atfa çevrildi, **muafiyet KALDIRILDI.**
  · **#5 — PHC parametreleri:** gövdedeki ham `m=19456,t=2` **`[KS-23]` atfına** çevrildi; **biçim örneği** (`$argon2id$v=19$…`) ise **gerekçesiyle `[KS-LITERAL]`** işaretlendi (bir veri biçimidir, ayrıştırıcının beklediği baytları gösterir).
  · **#6 — `[KS-12]`'nin değeri** K3-J6 gövdesinde HAM yazılıydı ve **yürürlükteki karardır** ⇒ atfa çevrildi. *(Ölçülebilir sonuç: aracın `[KS-LITERAL]` muafiyet listesinde `KS-12`/`KS-6` için iki satırdan biri düştü.)*
  · **#7 — `[KS-13]`+1'in ham literali (`6`) İKİ KEZ:** atfa çevrildi. **Kritik ders yazıldı:** araç `[KS-13]`'ü *"tek haneli literal — metinde ayırt edilemez"* diye **kapsam dışı** bırakıyor ⇒ bu iki literal **hiçbir zaman ihbar edilmedi** ⇒ §3.1'in *"çevrilecek sınır-değer literali YOKTU"* iddiası **aracın KENDİ KÖRLÜĞÜNE dayanıyordu.**
  · **#9 — `KS-3` SIFIR atıf alıyordu**, değeri §2-L'de kopyalanmıştı ⇒ **atfa çevrildi (artık ölü kayıt değil)**. **`KS-5` ve `KS-7` HÂLÂ SIFIR atıf alıyor ve bu bir SINIR olarak §2'nin girişine YAZILDI** (ikisi de türetilmiş/ikincil kayıt; gövdede atıf yapılacak cümle yok). **Kapı-7'ye açık soru:** atıf almayan kanonik kayıt `§1-K`'da kalmalı mı, ilgili kararın içine mi yazılmalı — **tek taraflı kapatılmadı.**
- **🔴 BU DALGA DA KENDİ KUSURUNU ÜRETTİ ve ARAÇ YAKALADI [yedinci kanıt]:** **#4'ün düzeltme notu**, gerekçeyi anlatmak için `[KS-1]`'in literalini **tekrar yazdı** ve `[KS-LITERAL]` muafiyeti kaldırıldığı için satır artık **taranıyordu** ⇒ **yeni bir `K4` bulgusu doğdu**; ikinci yazımda literal tümüyle kaldırıldı. **Örüntü artık istisnasız: bu belgede bir kusur sınıfını kapatan her düzeltme, aynı sınıftan bir kusur üretme riski taşıyor ve tek koruma ARACI KOŞMAK.**
- **✅ DOĞRULAMA (dalga sonrası, yeniden koşuldu):** araç **`K4` = 5 bulgu · `K1` = 0 · `K6` = 2 ⇒ TOPLAM 7 = v6'nın taban çizgisi** (yeni bulgu YOK) · **tablo kusuru 0** · v7 **281.928 bayt · 1.306 satır · U+FFFD 0** · **v6 DOKUNULMADI** (`6F6BE71A…`) · **araç DOKUNULMADI** (`A22841F2…`).
- **✅ COMMIT ATILDI — ONUR'UN TALİMATIYLA, DESKTOP COMMANDER ÜZERİNDEN (KONTEYNER MOUNT'UNDAN DEĞİL) [26 Tem 2026]:** `0c5fbc5` *"docs(adr-0003,kanit): v7 - close 5 of 9 gate-6 blockers + B6-A/B/C/D + all 9 canonical-number majors; new mutants M58/M-L10; zero table defects; blocklist source pinned with license+CVE gate (K37)"* · **4 dosya · +455 / −28** · yeni: iki KANIT dosyası. **Kapanış ölçümü:** ağaç **TEMİZ** · `origin/main`'den **26 İLERİ** · **PUSH YOK** · `.git/index.lock` **YOK (Test-Path False)**.
  ⚠ **DOKTRİN AYRIMI — CLAUDE.md'nin *"sandbox'tan commit ETME"* kuralı KALDIRILMADI, SINIRI ÖLÇÜLDÜ:** o kural, **konteynerin mount'u üzerinden** koşan git için yazılmıştır (mount `unlink`'e izin vermediği için git bayat `.git/index.lock` bırakır). **Desktop Commander ise Onur'un makinesinde YERLİ PowerShell koşturur** — mount yolu kullanılmaz ⇒ aynı riski taşımaz ve bu koşumda **kilit bırakmadığı ölçülerek doğrulandı**. **`device_bash` (mount'lu Linux VM) ile commit YİNE YASAKTIR.** *(Bu bir ERRATA adayıdır; CLAUDE.md'ye yazılması Onur'un onayına bağlıdır — bu oturum kuralı tek taraflı değiştirmedi.)*
- **[K21 ÖLÇÜMÜ — dalga sonrası] 521.414 token = %52,1 ⇒ 🟢 YEŞİL** ama 🟡 bandı (%55) **bir büyük okuma uzakta** ⇒ **K21 gereği yeni büyük iş AÇILMADI**, eldeki dalga bitirildi ve devir notu yazıldı. Eğri (bu oturum): 131k → 467k → **521k**.
- **🔴 KALAN MAJÖRLER — SAYI ARTIK ÖLÇÜLÜ: kapı-6'nın 31 majöründen 10'u KAPANDI (#1–#10), 21'i AÇIK (#11–#37 aralığındaki kalemler).** Gruplar: **§3.2 mutant tablosu (11 kalem: #11–#22)** — ölmeyen ayaklar (M54(2) · M31(2) · M27 · M34(2) · M33a) · *"veya"*lı bileşik mutantlar (M16 · M21 · M36 · M48) · ayırt edici olmayanlar (M39⟷M45 · M42⟷M42c · M49) · M40'ın ölü-tuzak riski · M43(3)'ün onarılmamışlığı · **§3.3 karar⟷tablo sapmaları (5: #23–#27)** · **§3.4 port envanteri/katman/inşa (4: #28–#31)** — envanterde kara liste ve AES-GCM satırı YOK · iki farklı inşa · anahtar dosyasının `Momentum:MasterKey`'e nasıl bağlandığı YAZILMAMIŞ · `ICurrentUser`'ın yerinde iki normatif hüküm · **§3.5 araç kapsamı + dürüstlük muhasebesi (6: #32–#37)** — aracın karar deseni `K18-a` ailesini görmüyor (kapsam sınırı beyansız) · altın küme kontrol sayısı ÜÇ FARKLI yerde farklı (22/21/17) · *"iki `[DOĞRULANMADI]`"* denmiş ama **ÜÇ** · §6 #17'nin *"§7'ye yazıldı"* dediği rotasyon §7'de **0 kez** · `K4` adjudikasyonunun aritmetiği tutmuyor (35 ≠ 26+2+2).
- **Sıradaki ilk iş:** (1) **~28 majör + K34-g borç listesi** → (2) **§3.1'in yeniden ölçümü + araç yeniden koşulur** → (3) **§0 değişim kaydı + altbilgi** → (4) **kanonik yola alma (K30-d) + v6 arşive** → (5) **ARACIN 2. ONARIMI — AYRI EL (K34-f)** → (6) **7. TUR BAĞIMSIZ KAPI — AYRI OTURUM** (ADR 0002 + `src/` + `tests/` + `docker-compose.yml` **BAĞLI**; kapı-6 raporunu **ve** K35/K36/K37 sınıflandırmalarını da denetler). **v7'yi oturum 25 ve 26 yazdı ⇒ İKİSİ DE onu DENETLEYEMEZ (K26).**


## ⏭ CHECKPOINT (26 Tem 2026 — oturum 25: **K36 — v7 YAZIMI BAŞLADI; BELGE YARIMDIR. 4/9 BLOKER + 3/31 MAJÖR KAPANDI, 1 YENİ MAJÖR BULUNDU VE KAPANDI, 1 YENİ MUTANT (M57) DOĞDU**)

- **[K21 ÖLÇÜMÜ — kapanış]** **525.701 token = %52,6 ⇒ 🟢 YEŞİL.** Eğri: 324k → 372k → 485k → 490k → **526k**. ⇒ **Devir bağlam yüzünden DEĞİL, DİSİPLİN gereği veriliyor:** 🟡 bandı (%55) **bir mutant-tablosu okuması uzaktadır** (~20k/blok) ve K21'in *"dolu bağlamda ADR yazma yasağı"* tam oradan başlar ⇒ **yeni bloker açılmadı**, eldeki kalemler bitirildi ve checkpoint yazıldı.
- **✅ KAPANAN İKİ BLOKER DAHA (aynı bölge, tek okumayla):**
  3. **B6-8 (K3-I4) — *"§3.1'de beyanlıdır"* denilen ama YAZILMAMIŞ sınır.** v6 cümleyi *"— §3.1'de beyanlıdır"* diye bitiriyordu; §3.1'de o satır **yoktu** (kapı-6 ölçtü). ⇒ **Sınırın BEYAN OTORİTESİ artık K3-I4 paragrafının kendisidir** ve sınır orada birebir yazılıdır: *kök anahtarın ENTROPİSİ hiçbir kapı tarafından ölçülmez; K3-I2 yalnız `[KS-20]` uzunluğunu doğrular; telafi kapı değil YOL kısıtıdır (bootstrap'ın CSPRNG'si + README komutu).* **§3.1 bundan böyle yalnız ÖZET taşır** ⇒ *"öbür bölümde beyanlı"* deyip orada olmaması kusuru **yapısal olarak bir daha doğamaz.** *(v7 borcu olarak §3.1 özet satırı açıkça yazıldı.)*
  4. **B6-9 (AAD) — bayt kodlaması artık KAPILI: 🆕 M57 doğdu.** v6 *"M51 (AAD kaldırılır/**karıştırılır**)"* diyordu; **"karıştırılır" formu hiçbir şeyi ölçmüyordu** (şifreleme ve çözme aynı kodu kullandığı için davranış değişmiyor ⇒ M51'in sinyali geçiyor). **M57 = AAD ALTIN VEKTÖRÜ** (seviye `B`, numara K22-b'nin **M57–M59 tamponundan**): pinli bir `(family_id, token_hash)` çifti için `BuildAad(...)`'ın ürettiği baytlar **onaltılık altın vektöre** karşı ölçülür ve **RFC 9562 big-endian** sırasını pinler; üç mutasyon formu (sıra değişimi · `Guid.ToByteArray()` · Base64 metin) **her biri AYRI COMMIT'te** koşulur. **M42b'nin birebir kardeşi.** Ayrıca **M51'e önkoşul pini** eklendi: ikinci mutasyon formu için **A ve B satırları AYNI AİLEDE olmak ZORUNDA** (kapı-6 red-team'i bu ayağı tersine çevirmişti — mutant aynı ailede ölüyor, kusur **pinin yokluğuydu**).
- **📊 MUTANT SAYIMI DEĞİŞTİ:** 0003 aralığı artık **M1–M57**; tampon **M58–M59**'a indi; 0004'ün pini **M60** (K22-b) **değişmedi** ve çakışma yok. *(v7 kapanışında numara bütünlüğü yeniden ölçülecek.)*
- **🆕 YENİ MEKANİK KAPI YAZILDI (ADR tarafı) + KAPI-6'NIN BİR İDDİASI DAHA YANLIŞLANDI.** Araç **tablo hücre bütünlüğünü HİÇ taramıyor**; bu oturum kendi tarayıcısını yazdı (tablo tespiti → başlık/ayırıcı/gövde hücre sayımı, `\|` kaçışları hariç tutularak) ve **v6 ile v7 üzerinde ayrı ayrı koşturdu.**
  **ÖLÇÜM — v6 (KANONİK): 23 tablo, DÖRT hücre sapması:** satır **47** (5 hücre / başlık 4) · **342** (4/3) · **908** (7/5) · **1055** (4/3). ⇒ **Kapı-6 majör #10'un birebir cümlesi — *"Belgenin tamamındaki TEK hücre sapması budur"* — YANLIŞTIR. Dört tanedir.**
  **AMA KAPI-6'NIN ESAS HÜKMÜ DOĞRUDUR ve bu ayrım önemlidir:** sapmaların **kök nedeni iki farklı sınıftır** (ölçüldü — her satırda son `|`'den sonraki metin okundu):
  · **satır 908 (M56) — GERÇEK KUSUR:** son `|`'den sonra **hiçbir şey yok** ⇒ fazla hücreler satırın **İÇİNDEKİ kaçışsız `|`**'lardan geliyor (`grep "blocklist|pwned|sızdırılmış"`) ⇒ GFM fazla hücreyi atar ve **GERÇEK İÇERİK KAYBOLUR** (`[KS-31]` atfı, HIBP'nin adlandırılmış reddi, satırın **ÇIPASI**).
  · **satır 47 · 342 · 1055 — ZARARSIZ SINIF:** üçünde de son `|`'den sonra **yalnız bir HTML yorumu** var (`<!-- [KS-LITERAL: … ] -->`) ⇒ GFM onu bir fazla hücre sayıp **atar**, ama atılan şey zaten **render'da görünmeyen bir yorumdur** ⇒ **bilgi kaybı YOK**, araç da yorumu **kaynaktan** okumaya devam eder. **Sınıf: MİNÖR/kozmetik.**
  ⇒ **Dürüst hüküm:** kapı-6 **sayıyı yanlış**, **önemi doğru** raporlamıştır. **v7 üçünü de yine de düzeltecek** (yorum son `|`'den ÖNCEYE alınacak) ki tarayıcının taban çizgisi **sıfır** olsun ve gelecekte gerçek bir sapma gürültüye karışmasın.
  **✅ v7'NİN KENDİ YENİ SATIRLARI TEMİZ:** aynı tarama v7'de de **tam olarak aynı dört** satırı buluyor (47 · 398 · 1010 · 1157 — kaymış numaralarla aynı kalemler) ⇒ **bu oturum YENİ bir hücre sapması ÜRETMEDİ.**
  ⚠ **AMA ÜRETTİ VE YAKALADI [dürüstlük kaydı, gizlenmiyor]:** K35'in alıntı-hijyeni notu ilk yazıldığında **§1'in tablosunun ORTASINA** düştü ve `B4` satırını tablodan **kopardı** — yani kapı-5'in M31'de, kapı-6'nın M56'da saydığı **render kusuru sınıfının ta kendisi, onu kapatan turun kendi elinden.** Aynı koşumda ölçülüp düzeltildi ve **belgeye de yazıldı**. **K33'ün örüntüsünün beşinci kanıtı.**
  📄 **KANIT YAZILDI: `KANIT/adr-0003/v7-tablo-butunlugu-olcumu.md`** — betik + **ham çıktılar** (v6, v7, kök-neden sondası) + hüküm. **KANIT kuralı v2.1'e uygun: çıktılar yeniden yazılmadı, birebir yapıştırıldı.**
  🔴 **VE BU KANIT DOSYASI DA AYNI KUSURU TAŞIDI — kendi tarayıcısı yakaladı [dürüstlük, gizlenmiyor].** Dosya yazıldıktan hemen sonra **kendi betiğinden geçirildi**: `[KUSUR] satir 99: baslik 6, ayirici 5` + dört gövde sapması. **Kök neden ÜÇÜNCÜ BİR ALT SINIF:** başlık satırında `` son `|` sonrası `` yazılmıştı — **GFM'de bir KOD ARALIĞI (backtick) içindeki `|` bile hücre ayıracı sayılır**; kaçış zorunludur. Düzeltildi (`` `\|` ``), **ham çıktı alıntısı ise BİREBİR bırakıldı** (kanıt kaydı değiştirilmez). ⇒ **Aynı turda ÜÇÜNCÜ kez** aynı sınıftan kusur üretildi ve **üçünde de bulan şey akıl yürütme değil, KOŞULAN bir tarayıcı oldu.**
  📌 **ARACIN 2. ONARIMINA (AYRI EL, K34-f) — ALTIN KÜMEYE ÜÇ AYRI MUTASYON VAKASI ZORUNLU** (kök nedenler farklı, biri diğerini yakalamaz): **(1)** satırın İÇİNDE kaçışsız `|` (bilgi kaybı, M56 sınıfı) · **(2)** son `|`'den SONRA metin/yorum (fazla hücre, kayıp yok) · **(3)** KOD ARALIĞI içinde kaçışsız `|` (bu KANIT dosyasının kendi kusuru). **Üçü de KIRMIZI vermeden kontrol altın kümeden geçmiş SAYILMAZ.**
  ⚠ **ORTAM UYARILARI [bu oturumda ÖLÇÜLDÜ — kanla yazılıyor, bir sonraki el zaman kaybetmesin]:**
  · **Desktop Commander `write_file` `mode: append`, AYNI dosyada daha önce yapılmış bir `edit_block` değişikliğini GERİ ALABİLİR** (bu oturumda fiilen oldu: başlık düzeltmesi append sonrası kayboldu). ⇒ **KURAL: bir dosyaya önce append yap, edit'leri SONRA; ya da append sonrası her edit'i diskten YENİDEN DOĞRULA.**
  · **`device_stage_files` BAYAT KOPYA sunabilir** (bu oturumda: mtime değişti, bayt sayısı aynı kaldı, **içerik eski geldi**) ⇒ **cihaz tarafında yapılan bir düzenlemeden hemen sonra konteynerdeki kopyaya GÜVENME; gerçeği `desktop-commander read_file` ile (gerçek diskten) oku.**
  · **`edit_block`'un `old_string`'i dosyada İKİ yerde geçiyorsa yanlış olanı değiştirebilir** (bu oturumda: tablo başlığı yerine **kod bloğundaki ham-çıktı alıntısını** değiştirdi ve bir KANIT kaydını bozdu — geri alındı). ⇒ **Ham çıktı/alıntı taşıyan dosyalarda çok satırlı, benzersiz çıpa kullan.**
  · Konteynerde **çok uzun satırlarda `rev` komutu zaman aşımına uğruyor** (893 karakterlik satır, 2 dk) ⇒ satır kuyruğu için `python3` kullan.
  📌 **(devam) Bu tarayıcı araca eklenmelidir** (`K8` gibi yeni bir kontrol kodu) — bugün araç bu ekseni **yapısal olarak görmüyor** ve kapı-5 ile kapı-6'nın **ikisi de** bu sınıftan bulgu üretmişti. Tarayıcının mantığı bu checkpoint'te tarif edilmiştir; onaran el onu altın kümeye **mutasyon testiyle** eklemekle yükümlüdür.
- **📄 ÇALIŞMA DOSYASI: `docs/ADR/0003-kimlik-cekirdegi-v7-YAZIM-DEVAM-EDIYOR.md`** — **249.029 bayt · 1.160 satır** (v6: 241.689 · 1.123). Kopya **bayt-birebir** doğrulandı (kopyalama anında iki dosyanın sha256'sı aynı ölçüldü).
- **⚠ KANONİK ADR HÂLÂ v6'DIR** — `docs/ADR/0003-kimlik-cekirdegi.md` sha256 **`6F6BE71A…8638CB08`**, **TEK BAYT DOKUNULMADI** (kapanışta yeniden ölçüldü). K30-d gereği taşıma v7 bitince yapılır.
- **✅ K34-f TUTUYOR — ARACA DOKUNULMADI:** `araclar/adr-kapi-taramasi.py` sha256 **`A22841F2…EE81ADB8`** · **50.582 bayt** (kapanışta ölçüldü). Aracın 2. onarımı **AYRI ELE** aittir.
- **✅ KAPANAN İLK İKİ BLOKER (turun başında):**
  1. **B6-3 (M24) — "korumalı uç" artık PİNLİ.** Hedef uç **`GET /v1/_probe/whoami`**: yalnız test derlemesinde kayıtlı, `[Authorize]`'lı ve gövdesi **`ICurrentUser.UserId`'yi FİİLEN DEREFERENCE ETMEK ZORUNDA**. İkinci ayak eklendi: gövdedeki değer token'ın `sub` talebine **eşittir** (yanlış claim'in sessizce çözülmesini de yakalar). *(v6'nın adıyla pinlediği tek uç `/v1/_probe/deny-default`'tu ve `ICurrentUser`'a hiç dokunmuyordu ⇒ mutant yaşıyordu.)*
  2. **B6-2 (M29) — önkoşul PİNLENDİ, iki ayak:** (i) fırsatçı silme devre dışı, (ii) süpürücü fixture'a kaydedilmez — **M40b'nin birebir aynı pini**. *(Saat `[KS-4]`'ü aşınca iki silme mekanizması da `successor_secret_enc`'i `NULL`'lıyor ⇒ koşul 3 düşüyor ⇒ dal (d) ⇒ aile mutasyon altında da iptal oluyordu ⇒ assert geçiyordu = kör kapı.)*
- **✅ KAPANAN ÜÇ MAJÖR (kapı-6'nın 31'inden):** **#1** §1-K'nın kendi aritmetiğindeki bayat `120 sn` literali → sembolik `[KS-4]` + `[KS-6]` *(belge kusuru satır 365'te geri çekmiş ama KAYNAĞINA uygulamamıştı)* · **#3** M31'in çıpasındaki muafiyetsiz ham `15/14/10` → sembolik **ve §3.1'in K30-b raporunun (*"çevrilecek gerçek kalem YOKTU"*) YANLIŞ olduğu yazıldı — aranan kalem tam olarak burasıydı** · **#8** K3-C7'deki türetilmiş `≤20 dk` → `[KS-1]` + `[KS-8]` sembolik.
- **✅ K35'İN ANA BULGUSU KAPANDI + YENİ PAZARLIKSIZ KURAL DOĞDU.** §2-C(4)'ün uydurulmuş alıntısı yeniden yazıldı: emsal artık **`0002:187` satır numarasıyla ve BİREBİR** veriliyor, ADR 0003'ün kendi formülasyonu **tırnak dışında** ve *"bu belgenin karşılığı"* etiketiyle duruyor. **YENİ KURAL [PAZARLIKSIZ, v7'de yürürlükte]:** *başka bir ADR'den tırnak içinde aktarılan her metin SATIR NUMARASIYLA verilir ve kısaltma yalnız `…` ile İŞARETLENEREK yapılır; bu belgenin kendi formülasyonu, kaynağınkiyle aynı paragrafta bile olsa tırnak dışında yazılır.*
- **🔴 YENİ MAJÖR — BULUNDU VE AYNI TURDA KAPANDI: `[KS-19]`'un RFC ATFI YANLIŞTI.** Kapı-6 bunu *"aday sapma, `[DOĞRULANMADI]`"* diye adlandırmış ama **ölçememişti**; v7 **birincil kaynaktan ölçtü** (RFC 5321, `rfc-editor.org`, 26 Tem 2026): **§4.5.3.1.3 birebir — *"The maximum total length of a reverse-path or forward-path is 256 characters (including the brackets)."*** · §4.5.3.1.1 Local-part **64** · §4.5.3.1.2 Domain **255**. ⇒ **RFC'nin yol sınırı 256'dır; 254 = 256 − 2 (köşeli ayraçlar) ve TÜRETİLMİŞ bir "mailbox" tavanıdır.** **DEĞER DOĞRU, ATIF YANLIŞTI** ⇒ düzeltilen atıftır; **M52 ayak 1 etkilenmez** (assert `254`'ü ölçüyor ve `254` doğru tavan) ⇒ kör kapı da ölü tuzak da doğmadı.
  ⚠ **BU, BELGENİN İLK ÖLÇÜLMÜŞ BİRİNCİL-KAYNAK ATIF HATASIDIR.** Kapı-4 *"23 birincil-kaynak atfının hiçbiri yanlış çıkmadı"* diye kapanmıştı. ⇒ **Kalan ~30 atıf (NIST ×13, RFC 9700/9562/6265/4648, aspnetcore ×10) HÂLÂ DOĞRULANMAMIŞTIR ve bu kalem, o kümede başka sapmalar olabileceğinin ÖLÇÜLMÜŞ kanıtıdır. KAPI-7'YE PAZARLIKSIZ GÖREV: tamamı birincil kaynaktan açılacak.**
- **🔴 BELGE YARIMDIR — KALAN İŞ (v7'nin bir sonraki oturumu, sırayla):**
  1. **BEŞ BLOKER (dokuzun kalanı):** **B6-1** (M56 probu — K34-b: `≥[KS-17]` uzunlukta gerçek kara-liste kaydı + **varlığı ÖLÇÜLECEK** + **liste dosyasının lisans kapısı KOŞMADI, koşacak**) · **B6-5** (§3.1'in *"araç altın kümede kendini kanıtlamadan koşmaz"* güvencesi YANLIŞ — kaynak 880-881 `if a.altin_kume: return altin_kume()` ⇒ **normal tarama altın kümeyi HİÇ koşmuyor**; cümle bir güvence değil **SINIR** olarak yeniden yazılacak) · **B6-6** (K3-L8'in 4. dalı + çıkışta aktif profil kaydı — **K34-c, ŞEMA kararı, mutantla ısırtılacak**) · **B6-7** (§3.2(9) izolasyonu — **K34-d: kural *"`/login` ya da `/register` çağıran HER test sınıfı kendi fabrikasını kurar"* olacak**) · **B6-4'ün ADR AYAĞI** (aracın satır-412 sessiz `K4` kaçış kapısı §3.1'de ADIYLA beyan edilecek — **araç ONARILMAYACAK, K34-f**).
     *(✅ **B6-2 · B6-3 · B6-8 · B6-9 bu oturumda KAPANDI.**)*
  2. **~28 MAJÖR** (kapı-6 §3'ün kalanı) + **B6-A** (§6 #11'in yanlış ölçüsü: gölgeleme altında çevrimiçi kurbanda en kötü durum `[KS-1]` değil **`[KS-2]`**) · **B6-B** (gölgelemenin adlandırılmamış bedeli) · **B6-C** (satır 354'ün yanıltıcı ifadesi) · **B6-D** (K3-B7'nin savunmacı dalı K3-B5'in *"aynı işi yapar"* garantisini kırıyor — bozuk PHC'de ≈1 ms vs ≈270 ms zamanlama oracle'ı, kapısız ve beyansız).
  3. **K35'İN İKİ ALINTI-HİJYENİ MİNÖRÜ** (satır 65 `;`→`.` ve düşen *"deny-by-default"* · satır 66'nın düşen `(owner/collaborator)` parantezi) + üçüncüsü (satır 67'nin *"§6/7"* konumu → **§6 madde 7**).
  4. **K34-g:** `GOREV-slice-3b` spec'i için **ADLANDIRILMIŞ BORÇ LİSTESİ** yazılacak (5 `[devir]` mutantı) — **bugün böyle bir liste YOK**.
  5. **§3.1'İN TAMAMEN YENİDEN ÖLÇÜLMESİ** (yukarıdakilerin hepsi §3.1'in sayılarını değiştirir) + **aracın v7 üzerinde koşulması** + **v7'nin kendi YENİ satırlarının eski bulgu sınıflarına karşı taranması (K33'ün dersi — dört turdur aynı kök neden).**
- **Sıradaki ilk iş:** **v7 yazımının devamı** (yukarıdaki 5 kalem) → kanonik yola alma (K30-d) + v6 arşive → **ARACIN 2. ONARIMI, AYRI EL** → **7. TUR BAĞIMSIZ KAPI, AYRI OTURUM** (ADR 0002 + `src/` + `tests/` **BAĞLI**; kapı-6 raporunu **ve** bu oturumun K35/K36 sınıflandırmalarını da denetler).


## ⏭ CHECKPOINT (26 Tem 2026 — oturum 25: **K35 — ADR 0002 ALTI TURDAN SONRA İLK KEZ AÇILDI; DOKUZ ATIFIN SEKİZİ TUTTU, BİRİ UYDURULMUŞ ALINTI ÇIKTI**)

- **K34-e uygulandı.** `docs/ADR/0002-senkron-mekanigi.md` **31.402 bayt · 257 satır** (diskten ölçüldü, devir notuyla birebir). Denetim kopyasının kimliği doğrulandı: `docs/ADR/0003-kimlik-cekirdegi.md` sha256 **`6f6be71a…38cb08`** = kanonik ⇒ **ölçüm gerçek artefakt üzerinde koştu.**
- **✅ K14-h'NİN TEK GEREKÇESİ ARTIK DOĞRULANDI — kapı-6 §7(1)'in birinci kalemi KAPANDI.** v6 satır 19 *"`slice-3a` zaten vardır (ADR 0002 **K2-I3** = sunucu materyalizasyonu + okuma API'si)"* diyor. Kaynak **`0002:203`** birebir: *"**K2-I3 — Dilim bölünmesi.** slice-3a = sunucu materyalizasyonu + okuma API'si; slice-3b = Flutter istemci…"* ⇒ **`slice-3c-auth` adlandırması SAĞLAM.** *(Bu, `docs/ODEV.md` §3.2(a) ve §6.1 erratalarının da dayanağıdır — ad değişimi artık kanıtsız değildir.)*
- **✅ SEKİZ ATIF TUTTU:** satır 19 (K2-I3) · satır 22 (bağımlılık listesi: K2-E3 `0002:143` · K2-E5 `0002:152` · K2-A4 `0002:56` · §6 md.7 `0002:244` — **dördü de var**) · satır 67 (M-C, `…` ile **işaretli** kısaltma, metin tutuyor) · satır 998 (*"ADR 0002 ailesi"* — alıntı değil) · satır 1097 (öncül listesi; **K2-E5'in *"kısmi red"* ifadesi `0002:154`'te BİREBİR var**) · satır 1104 (tablo satırı tutarlı).
- **🔴 YENİ BULGU [MAJÖR — kapı-7 BLOKERE YÜKSELTEBİLİR] — v6 SATIR 369'DA UYDURULMUŞ ALINTI.** v6 birebir şöyle yazıyor: *"**Emsal 0002 K2-H12'de var:** «aynı `T1` ile **paralel** iki `/refresh` — tam olarak **biri** `200`, diğeri `401`+`reuse_detected` alır; ikisi birden `200` alırsa test FAIL»"*.
  **ÖLÇÜM:** `grep -c "refresh\|reuse_detected" docs/ADR/0002-senkron-mekanigi.md` ⇒ **0**. `/refresh` · `reuse_detected` · `T1` — **ÜÇÜ DE ADR 0002'DE HİÇ GEÇMİYOR.**
  **GERÇEK K2-H12 (`0002:187`, birebir):** *"**K2-H12 — Eşzamanlı-aynı-clientId INGEST [BLOKER-R2]:** aynı clientId'den paralel op'lar → `lastEffectiveHlc` lost-update YOK, monoton, determinist (operationId çözer); mutant (atomik-olmayan yazma / operationId'siz) BAŞARISIZ."*
  ⇒ **EMSAL GERÇEKTEN VARDIR** (paralel aynı-anahtar yarışı · tam olarak biri kazanır · atomik-olmayan yazmaya mutant), **ama tırnak içindeki CÜMLE ADR 0002'DE YOKTUR** — o cümle v6'nın **kendi ADR-0003 uyarlamasıdır** ve *"Emsal 0002 K2-H12'de VAR:"* girişiyle **kaynağa ait gibi** sunulmuştur.
  **SINIF:** kapı-5'in **B5-5**'i ve kapı-6'nın **B6-8**'i ile aynı aile — *"beyan edildiği söylenen ama yazılmamış"*; buradaki biçimi *"alıntılandığı söylenen ama yazılmamış"*.
  **NEDEN BLOKER DEĞİL [gerekçe, savunma değil]:** kör kapı yok (**M39** kendi mutasyonu altında gerçekten ölüyor ve kapısı bu alıntıdan bağımsızdır), ölü tuzak yok, inşa edilemezlik yok. **NEDEN YİNE DE AĞIR:** bu bir **portfolyo belgesidir** ve **KİLİTLİ** bir ADR'ye atfen tırnak içinde var olmayan metin göstermek, bir değerlendiricinin bulabileceği en zarar verici kusur sınıfıdır; ayrıca belgenin kendi §4 kuralının (*"her olgu iddiası ya ÖLÇÜLÜR ya `[DOĞRULANMADI]`"*) doğrudan ihlalidir.
  **⚠ BU SINIFLANDIRMA BİR ÜRETİMDİR ve kendi üreticisi tarafından onaylanamaz — KAPI-7 onu da adjudike etmelidir.**
  **v7'NİN YAPMASI GEREKEN:** satır 369 yeniden yazılır — emsal **tarif** edilir, **alıntılanmaz**: *"Emsal K2-H12'dedir (`0002:187`): aynı clientId'den paralel op'lar → lost-update YOK, determinist; mutant = atomik-olmayan yazma. **ADR 0003'ün karşılığı** (bu belgenin kendi cümlesidir, 0002'den alıntı DEĞİLDİR): aynı `T1` ile paralel iki `/refresh` ⇒ tam olarak biri `200`…"*
- **⚠ İKİ MİNÖR — ALINTI HİJYENİ (aynı sınıfın hafif biçimi, v7 düzeltir):**
  1. **satır 65 (M-G push-authz):** v6 alıntıyı *"…Mekanizma auth diliminde**.**"* diye **noktayla** kapatıyor; kaynak `0002:145` orada **noktalı virgül** kullanıyor ve devam ediyor: *"…auth diliminde**;** o zamana dek **deny-by-default** + `entityId` bir yetki-token'ı **DEĞİLDİR**."* ⇒ **işaretsiz kısaltma**, ve düşen kısım operasyonel olarak asli (bugünkü davranış = deny-by-default).
  2. **satır 66 (pull-authz):** v6 *"`changes` yalnız actor'ın görebildiği entity'lerle sınırlı"* diyor; kaynak `0002:144` *"`changes` yalnız actor'ın görebildiği **(owner/collaborator)** entity'lerle sınırlı"* ⇒ **kimin göreceğini TANIMLAYAN parantez işaretsiz düşürülmüş.**
  3. *(Ek, en hafif)* satır 67 konumu **"§6/7"** diyor; kalem gerçekte **§6 "Riskler / açık noktalar", madde 7**'dedir (§7'nin adı *"İlgili"*dir ve satır 249'da başlar).
- **KAPI-6'NIN §7(1) KALEMİ TAM KAPANMADI — dürüst kayıt:** bu ölçüm **atıfların DOĞRULUĞUNU** kapattı; **ADR 0002'nin KENDİSİ hâlâ DENETLENMEDİ** (K34-e'nin adlandırılmış bedeli). ADR 0002 v3'tür, 3 kapı turu geçmiştir ve **KİLİTLİDİR**; ama kapı-1…6'nın hiçbiri onu açmadı. **Kapı-7 ADR 0002'yi BAĞLI olarak koşmalıdır.**
- **[K21 ÖLÇÜMÜ — K34-e sonrası] 371.669 token = %37,2 ⇒ 🟢 YEŞİL.** Eğri: 324k → 372k.
- **Sıradaki ilk iş:** **v7 YAZIMI** — girdi artık **9 bloker + 31 majör + B6-A/B/C/D + K34'ün yedi kilidi + K35'in bir majörü ve üç minörü.** Araca **DOKUNULMAZ** (K34-f).


## ⏭ CHECKPOINT (26 Tem 2026 — oturum 25 açılış: **K34 — KAPI-6'NIN YEDİ ÇATALININ YEDİSİ DE KİLİTLENDİ**)

- **Protokol koşuldu (oturum 25):** `PROJE_HAFIZA.md` **918 satırın 918'i** + `CLAUDE.md` **48 satırın 48'i** TAM okundu — **ana oturumun kendi bağlamında; alt-ajan özeti AKTARILMADI** (bir alt ajan 231-855'i taradı, çıktısı **kullanılmadı**, aralık ana oturumda baştan okundu). **BU OTURUM YAZIM OTURUMUDUR** ve K26(1)'i ihlal etmez: v6'yı oturum 23 yazdı, kapı-6'yı oturum 24 koştu; bu oturum **ikisini de üretmedi** ⇒ v7'yi yazabilir. **ARACA DOKUNMAZ (K34-f).**
- **✅ AÇILIŞ KAPISI (protokol adım 2) UYGULANMAZ — atlanmadı, ÖLÇÜLDÜ:** `hafiza_gate|hafıza kapısı|açılış kapısı` → `PROJE_HAFIZA.md` + `CLAUDE.md` + `PROJE_TALIMATI.md`'de **0 eşleşme**; `araclar/` altında yalnız `adr-kapi-taramasi.py` (ADR kapısı) ve `verify.ps1` (build zinciri) var. **Bu projede tanımlı bir açılış-kapısı YOKTUR.**
- **✅ GİT (kaynaktan, `git --no-optional-locks`):** ağaç **TEMİZ** · HEAD **`8e5c6d60504b3e6253464b88415289a8a469a855`** *"docs(kanit): gate-6 independent audit of ADR-0003 v6 — VERDICT: CANNOT BE LOCKED…"* · `origin/main` **`56362ed`** · **22 ileri, PUSH YOK** · `.git/index.lock` **YOK**. ⇒ Onur kapı-6 commit'ini atmış; devir notunun *"beklenen"*i KAPANDI.
- **✅ BELGE KİMLİĞİ (Onur'un diskinden):** `docs/ADR/0003-kimlik-cekirdegi.md` = **241.689 bayt · 1.123 satır · sha256 `6F6BE71A0D86CB32D7D6B2B856C1526247CACD8AA57B0B4B36FACD8A8638CB08`** ⇒ K33 ile **BİREBİR**; kapı-6 raporu **34.106 bayt** ✓.
- **[K21 ÖLÇÜMÜ — kendi oturum-id'siyle, açılış]** transcript `2bbd582b-ecbb-55b3-8a89-17f809d6cedd` (tek `sessionId`, mtime canlı) ⇒ canlı bağlam **324.033 token = %32,4 ⇒ 🟢 YEŞİL.** ADR yazma yasağı bandında **DEĞİLİZ**.
- **🔴 DEVİR NOTUNUN ÜÇ SAPMASI ÖLÇÜLDÜ [K13-a fiilen iş gördü — devir notu da bir üretimdir]:**
  1. **Ç-1…Ç-4 NUMARALARI ÇAKIŞIYOR.** Sohbette verilen devir notu *"ayrıntı raporun §9'unda"* diyor, ama rapor §9 ve bu dosyanın satır ~74'ü **BAŞKA dört çatal** sayıyor. Devir notu: Ç-1 sonlanma · Ç-2 ADR 0002 · Ç-3 araç eli · Ç-4 M-L. Rapor §9 + hafıza: Ç-1 `[KS-31]`×`[KS-17]` · Ç-2 K3-L8 4. dal · Ç-3 §3.2(9) · Ç-4 sonlanma. **Kesişim yalnız SONLANMA.** ⇒ Gerçek açık karar sayısı **DÖRT DEĞİL YEDİ**; hiçbiri eşleştirilerek kaynaştırılmadı, **yedisi de ayrı ayrı Onur'a sunuldu ve kilitlendi.**
  2. **`KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/` ÖLÇÜLDÜ: 11 dosya · 457.046 bayt.** Devir notu **"12 dosya · ~490 KB"** diyor (**YANLIŞ**); bu dosyanın satır ~46'sı **"11 dosya, 472 KB"** diyor (**sayı doğru, boyut ~15 KB sapmalı**). Kanonik değer artık **11 / 457.046**.
  3. **KAPI-6'NIN BİR BULGUSU DARALTILDI [kapı-7 borcu, erken ödendi]:** rapor §7(5) *"ODEV §3.1/§4.1/§6.1 dilimi hâlâ `slice-3a-auth` diyor"* diyor. Ölçüldü: **§4.1 (satır 77) `slice-3a` diyor ve bu DOĞRUDUR** (materyalizasyon dilimi — alanlar orada materyalize ediliyor), bayat değil. Gerçek bayat kalem **ÜÇ değil İKİ**: ODEV satır **50** ve **121**.
- **[KARAR — Onur, 26 Tem 2026] K34-a: SONLANMA İSPATI → K13-a ÜSTÜNDÜR; ODEV §6'ya TARİHLİ ERRATA.** *Gerekçe [ÖLÇÜLDÜ]:* ODEV §6'nın *"Backend'de kalan işler zaman kutulanmalıdır"* cümlesi **20 Tem** tarihlidir; **aynı dosyanın** §8(2) **K12 güncellemesi 25 Tem** tarihlidir ve *"süreye göre kapsam/derinlik kesilmesini AÇIKÇA reddetti ⇒ kalite > hız"* der; ODEV'in kendi başlığı *"her güncelleme tarihli olarak eklenir"* der ⇒ **sonraki tarihli hüküm geçerlidir.** ⇒ Çelişki ODEV ⟷ K13-a **değil**, ODEV'in **kendi içinde bayat kalmış bir satırdır**; ama §6 hiçbir yerde geri çekilmediği için errata **zorunludur**. *Bedel [adlandırılmış, gizlenmiyor]:* **v6→v7 döngüsünün matematiksel bir ÜST SINIRI YOKTUR**; sonlanma yalnız *"bloker sıfırlanana kadar"* koşuluna bağlıdır ve 15 Ağu hedefi ağır baskı altındadır. *Reddedilenler [adlandırılmış]:* **ODEV üstün + ADR'ye zaman kutusu** (v7 + kapı-7 SON tur; kalan blokerler adlandırılmış sapma olarak spec'e devredilir — **kilit ölçütü ilk kez gevşer ve emsal doğar**) · **melez: kilit ölçütü daraltılır** (yalnız kör kapı + inşa edilemezlik turu uzatır — *"bloker"* tanımı tur ortasında değişir ve kapı-1…6'nın **63 blokerinin tamamı** geriye dönük yeniden sınıflandırılmak zorunda kalır, yoksa iki standart doğar).
- **[KARAR — Onur, 26 Tem 2026] K34-b: B6-1 (M56 KÖR KAPI) → PROB `≥[KS-17]` UZUNLUKTA GERÇEK BİR KARA-LİSTE KAYDINA ÇEVRİLİR ve O KAYDIN LİSTEDE BULUNDUĞU KOŞULARAK ÖLÇÜLÜR.** Bulunamazsa **`[KS-31]`'in SEÇİM ÖLÇÜTÜ değişir** (ör. *"en yaygın 10.000"* + *"`≥[KS-17]` karakterlik sızmış parolalar"* alt kümesi). *Gerekçe:* mutantın gerçekten ısırdığı **ölçülmeden** yazılırsa aynı kör kapı ikinci kez doğar; `123456` probu `[KS-17]`=15 yüzünden mutasyondan **bağımsız olarak** 400 alıyor ve belge bunu satır 216'da kendisi yazıyor. *Bedel [adlandırılmış]:* K28-c'nin seçim ölçütü yeniden yazılır **ve liste dosyasının lisans kapısı (kırmızı çizgi #3) HÂLÂ KOŞMADI — bu turda koşmalıdır.** *Reddedilenler:* **kontrol sırası değişir** (blocklist uzunluktan ÖNCE koşar — prob korunur ama K3-B6'nın bağlayıcı sırası + §2-B akış şeması yeniden yazılır ve M31'in 14/15 sınır-değer ayakları bu sıradan etkilenir) · **kalem kapsam dışı, K28-c geri alınır** (en ucuz; ama *"15 karakteri NIST diye savunup öbür `SHALL`'ı atlamak"* eleştirisi açık kalır — K28-c tam da onun için alınmıştı).
- **[KARAR — Onur, 26 Tem 2026] K34-c: B6-6 (K3-L8'in EKSİK 4. DALI) → DÖRDÜNCÜ DAL YAZILIR + ÇIKIŞTA "AKTİF PROFİL" KAYDI TEMİZLENİR. [ŞEMA KARARI]** Yerel DB **DOSYASI SİLİNMEZ** (K11-g ve kırmızı çizgi #4 korunur; gönderilmemiş kuyruk diskte kalır) — düşen şey yalnız *"aktif kullanıcı"* işaretçisidir ⇒ **çevrimdışı açılışta hiçbir kullanıcının DB'si açılmaz**, *"kimlik yok"* dalına düşülür. *Gerekçe:* `__Host-mrt` `HttpOnly` olduğu için istemci *"kimliğim yok"* ile *"ağ yok"*u ayırt **edemez**; satır 729'un yazılı kuralı devreye girip **çıkış yapmış A'nın görevlerini** paylaşılan makinede açar — bu, belgenin satır 691'de **adıyla reddettiği** senaryonun yerel-DB'deki hâlidir. Satır 737 *"`userId`'nin nereden geldiği BU BELGENİN işidir"* dediği için 3b'ye devredilemez. *Bedel [adlandırılmış]:* Drift şemasına/ayarına **aktif-profil alanı** girer ve **mutantla ısırtılmak zorundadır** (kapısız bırakılırsa B6-6 aynen geri döner). *Reddedilenler:* **profil kaydı kalır + uyarı** (çevrimdışı vitrini en az zedelenen yol, ama reddedilmiş senaryo *"kabul edilmiş bedel"* olarak kalır ve denetçi tekrar bloker sayabilir) · **`HttpOnly` olmayan `mrt-present` varlık-işareti çerezi** (yapısal olarak en temiz ayrım, ama **YENİ MEKANİZMA** ⇒ yeni kapı + yeni mutant + K14-c'nin PAZARLIKSIZ taşıma-kanalı ayrımıyla çapraz kontrol, ve iki çerezin senkron kalmaması yeni bir tutarsızlık sınıfı doğurur).
- **[KARAR — Onur, 26 Tem 2026] K34-d: B6-7 (§3.2(9) TEK YÖNLÜ İZOLASYON) → İZOLASYON TÜM AUTH TESTLERİNE GENİŞLETİLİR.** Kural *"hız sınırını ÖLÇEN her sınıf"* yerine **"`/login` ya da `/register` çağıran HER test sınıfı kendi `WebApplicationFactory`'sini kurar"** olur. *Gerekçe:* `[KS-10]`=30 tek partition + `FakeTimeProvider` pencereyi ilerletemez + beklemek YASAK ⇒ paylaşılan host 31. çağrıda `429` vermeye başlar ve **43 TS/TC mutantın baseline'ı** kırılır; §3.2(8)'in *"baseline YEŞİL doğar"* garantisi bugün **kurulamıyor**. Bu seçenek §3.2(10)'un **üretim eşdeğerliği** iddiasını tam korur, test-only kanca doğurmaz, **yeni sayı doğurmaz**. *Bedel [adlandırılmış]:* host başına kalkış maliyeti ⇒ 43 mutantın koşum süresi belirgin artar ve **CI hâlâ KURULMADI** ⇒ bu maliyet bugün Onur'un makinesinde ödenir. *Reddedilenler:* **testte limiter devre dışı** (en ucuz; ama kapatma kancasının kendisi **kapısız bir davranıştır** ⇒ ona da mutant gerekir, yoksa üretimde de kapalı kalabilir = kör kapı sınıfı) · **test ortamında kova bütçesi yükseltilir** (limiter boru hattı üretimdeki gibi kalır; ama aynı tavan **iki yerde yaşar** ⇒ **K30'un PAZARLIKSIZ *"sayı bir kez yazılır"* kuralıyla doğrudan sürtüşür** ve `K4` bunu yeni bir kopya olarak raporlar).
- **[KARAR — Onur, 26 Tem 2026] K34-e: ADR 0002 **v7 YAZILMADAN ÖNCE** AÇILIR; DOKUZ ATIFIN DOKUZU BİREBİR ÖLÇÜLÜR.** `K2-E3`×6 · `K2-E5`×2 · `K2-A4`×2 · `K2-H12`×2 · `K2-I3` — *"birebir alıntı"* iddiaları **karakter düzeyinde** karşılaştırılır; sapma çıkarsa **v7 düzeltilmiş metinle yazılır**. *Gerekçe:* v7 o atıfları **aynen taşıyacak**; şimdi ölçülmezse yanlış bir alıntı v7'ye kopyalanır ve kapı-7 onu **YENİ bir bloker** olarak bulur — bu, kapı-4→5→6'nın **üç turdur tekrarlanan kök nedenidir**. Maliyet düşük (31.402 bayt + 9 karşılaştırma). *Bedel [adlandırılmış]:* bu oturum ADR 0002'yi **OKUR ama DENETLEMEZ** — tam denetim yine **kapı-7'nin işidir** ve v7'de açıkça yazılır. *Reddedilenler:* **kapı-7'de açılır, v7'de `[DOĞRULANMADI]` işaretlenir** (v6'yı kilitlenemez yapan sınıfın aynısı v7'de de yaşar; K14-h'nin tek gerekçesi bir tur daha kanıtsız kalır ve **ODEV güncellemesi de o ada dayanıyor**) · **yalnız K2-I3 şimdi, kalan 8 kapı-7'ye** (K26(3) *"kapsam DARALTILAMAZ"* kuralıyla sürtüşür ve seçici doğrulama kalıbı emsal doğurur).
- **[KARAR — Onur, 26 Tem 2026] K34-f: ARACIN İKİNCİ ONARIMI **FARKLI ELDEN** ÇIKAR — v7'Yİ YAZAN OTURUM `araclar/adr-kapi-taramasi.py`'YE **TEK BAYT DOKUNMAZ**.** B6-4 (satır 412'nin sessiz, beyansız `K4` kaçış kapısı) · B6-5 (`if a.altin_kume: return altin_kume()` ⇒ normal tarama altın kümeyi **hiç koşmuyor**) · Sonnet'in **6 kaçışı** ayrı bir ele kalır. *Gerekçe:* rapor §9'un birebir uyarısı — *"v7'yi yazan el aracı onarırsa K29-a'nın bedeli ÜÇÜNCÜ kez ödenir"*; onaran el denetleyemez ve kapı-7 zaten üç nesne (v7 + araç + kapı-6 raporu) taşıyacak. *Bedel [adlandırılmış, gizlenmiyor]:* **v7, satır 412'nin kaçış kapısı AÇIKKEN ölçülür** ⇒ `K4` rakamı **tek başına kanıt değildir** ve v7'nin yeni satırları aynı filtreye düşebilir; aracın **8 bilinen sınırı** §3.1'e ADIYLA yazılır. Bir oturum daha harcanır. *Reddedilenler:* **aynı el v7 + onarım** (bir oturum kazanılır ve v7 onarılmış araçla ölçülür; ama K29-a'nın bedeli üçüncü kez ödenir) · **onarım ertelenir, sınırlar beyan edilir** (en ucuz; ama **B6-5 zaten belgenin bir GÜVENCESİNİN yanlış çıkmasıydı** — aynı cümleyi *"sınır"* diye yeniden yazmak kapı-7'de tekrar bloker olur).
- **[KARAR — Onur, 26 Tem 2026] K34-g: M-L TABLOSUNUN 5 ERTELENMİŞ MUTANTI (`[devir]`) BEYANLI SINIR OLARAK **KALIR** + `GOREV-slice-3b` SPEC'İNE **ADLANDIRILMIŞ BORÇ LİSTESİ** YAZILIR.** *Gerekçe:* Flutter istemci **HİÇ BAŞLAMADI** (istemci %0) ⇒ Dart mutantları **var olmayan bir kod tabanına** çıpalanırdı; bu tam olarak K22-a'nın *"`Dockerfile` yok ama `KON` ona çıpalı"* blokerinin **aynı sınıfıdır**. Buna karşılık *"spec'te çözülür"* savunması **üç kapı turudur reddediliyor** ve bugün **böyle bir borç listesi YOK** ⇒ v7 onu **yazılı hâle getirir**. *Bedel [adlandırılmış]:* ADR kilitlendiğinde **5 mekanizma hâlâ kapısızdır**; kapı-7 bunu tekrar sorabilir — ama beyan açıktır ve borç adreslidir. *Reddedilenler:* **v7'de DART/DART-WEB mutantları yazılır** (kapısız mekanizma sıfıra iner; ama var olmayan koda çıpa = K22-a sınıfı bloker) · **`[devir]` kategorisi kaldırılıp beşi §3.1'in *"kapısız kalan"* tablosuna taşınır** (en dürüst muhasebe; ama dağılım değişir — kapılı 32 · beyanlı 15→20 · devredilmiş 5→0 — araç sayaçları ve `K6` yeniden ölçülür, dört turdur ayakta olan tamlık iddiasının sunumu zayıflar).
- **Sıradaki ilk iş (bu oturum, sırayla):** (1) **AÇIK İŞLER md.9** düzeltilir (hâlâ *"SIRADA: 6. tur"* diyor) → (2) **`docs/ODEV.md`**: satır 50 ve 121'in bayat `slice-3a-auth` adı + §6.1 dilim bütçesi + **§6'ya K34-a erratası** → (3) **ADR 0002 açılır, 9 atıf ölçülür (K34-e)** → (4) **v7 YAZIMI** (9 bloker + 31 majör + B6-A/B/C/D + K34'ün yedi kilidi) → (5) öz-doğrulama (**KAPI DEĞİLDİR**) + devir notu. **ARAÇ ONARIMI VE KAPI-7 BU OTURUMUN İŞİ DEĞİLDİR (K34-f).**


## ⏭ DEVİR NOTU (25 Tem 2026 — oturum 23 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

DURUM: **v6 YAZIMI BİTTİ · v6 KANONİK YOLDA · v5 ARŞİVDE. SIRADAKİ İŞ: 6. TUR BAĞIMSIZ KAPI, AYRI OTURUM.**
  **[K21 ÖLÇÜMÜ — kapanış] 431.999 token = %43,2 ⇒ 🟢 YEŞİL.** Eğri: 251k → 382k → 432k.
  ⇒ **Devir bağlam yüzünden DEĞİL, DOKTRİN gereği veriliyor:** bu oturum v6'yı YAZDI, onu DENETLEYEMEZ (K26).
KANONİK ADR ARTIK **v6**: `docs/ADR/0003-kimlik-cekirdegi.md` · **241.689 bayt · 1.123 satır** · sha256 **`6f6be71a…38cb08`** · UTF-8, BOM yok, U+FFFD 0, LF, 15.237 Türkçe harf.
  **v5 SİLİNMEDİ, ARŞİVE KOPYALANDI:** `arsiv/0003-kimlik-cekirdegi-TASLAK-v5-2026-07-25.md` · sha256 **`758bd1bf…22ef6`** (kanonikle **birebir aynı**, kopyalama doğrulandı). Arşivde artık **beş** taslak var.
  Çalışma dosyası `_to_delete/0003-kimlik-cekirdegi-v6-YAZIM-DEVAM-EDIYOR.md`'ye alındı — **sandbox silemez, kalıcı silme ONUR'DA.**
ARACIN v6 ÜZERİNDEKİ SON KOŞUMU: **`K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=2 · `K4`=5 (toplam 7; oturum başında 30'du).**
  Altın küme **çıkış kodu 0**. Özet: `karar` 47 · `alt_madde` 12 · `kapili` 32 · `beyanli` 15 · `devredilmis` 5 · `mutant` 51 · `capasiz_tablo` 0.
  **Kalan 7 bulgunun 7'si de ADIYLA adjudike edilmiş ve §3.1'e yazılmıştır — SESSİZ ELEME YOK.**
BU OTURUMUN KAPATTIKLARI: devir notunun **altı kaleminin altısı** · kapı-5'in **7 majörünün 7'si** (M5-4 ve M5-7 bu oturumda) ·
  kapı-4'ten devreden **iki majör** (`M19` ayırt ediciliği · `M42c`'nin `KON` gözlem yüzeyi) · **K28-c/d ve K30-a/b/c/d'nin yazımı**.
🔴 BU OTURUMUN KENDİ BULDUĞU DÖRT KUSUR (araç göremezdi — hiçbiri ham kanonik sayı kopyası değil):
  1. **[BLOKER] K28-a'nın yazım borcu, kilitlenmiş kararı fiilen ÇÜRÜTÜYORDU.** Süpürücünün SQL'i `consumed_at < @now - interval '60 seconds'` idi ve yorumu *"değeri `[KS-6]` ile AYNI olmak ZORUNDADIR"* diyordu. **O eşik YAŞ EŞİĞİDİR, süpürme periyodu değil ⇒ doğrusu `[KS-4]`.** `[KS-4]`=60 sn iken ikisi tesadüfen aynıydı; K28-a `[KS-4]`'ü **10 dk** yapınca SQL halefin sırrını **60 saniyede** siliyordu ⇒ koşul 3 düşüyor ⇒ **replay penceresi fiilen 10 dk değil 60 sn.** Düzeltildi: eşik `interval '10 minutes'`, yorum `[KS-4]`'e bağlandı, periyot `[KS-6]` olarak kaldı.
  2. **[MAJÖR] M40 ÖLÜ TUZAĞA dönmüştü:** kill sinyali *"`FakeTimeProvider` **121 sn** ileri alınıp…"* diyordu; `[KS-4]`=10 dk ile bu süre pencerenin **içinde** ⇒ baseline kırmızı doğardı. **`[KS-4]` + `[KS-6]`** biçimine çevrildi.
  3. **[MAJÖR] Bayat aritmetik + bayat sahiplik:** §2-C'nin değişmezi Risk #13'ü *"en kötü durumda **120 sn**"* diye alıntılıyordu (K28-a sonrası yanlış) · §6 Risk #11 hâlâ `[KS-4]`'ü **K16-b**'ye atfediyordu, oysa kanonik tablo bu atfın *"yanlış, düzeltildi"* olduğunu yazıyor ⇒ **belge kendi düzeltmesini bir yerde uygulamamış.**
  4. **[AÇIK BULGU — KAPI-6'YA DEVREDİLDİ] GÖLGELEME.** Kapı-5 *"tespit gecikmesi `[KS-4]` değil `[KS-1]`"* demişti; belgenin **kendi dal kuralından** daha ağırı türetildi: **koşul 2 HALEFİN `consumed_at`'ine bakar** ⇒ halef her adımda taze doğduğu için *"meşru istemci halefi kullanınca aile düşer"* cümlesi de **yanlıştır** — o `/refresh` koşul 2'yi SAĞLAR ve `T3`'ü alır. İki taraf zinciri adım adım paylaşabilir. **Ömür uzamaz, yeni token basılmaz** (v1'in sonsuz zinciri hâlâ imkânsız) ama tespit çarpışmaya kadar ertelenir. §0.4'e **iki iddia birden** geri çekildi; §6 #11 ve #16 yeniden yazıldı. **KAPI-6 BUNU ÇÜRÜTMEKLE YÜKÜMLÜDÜR.**
⚠ K30-b'NİN DÜRÜST SONUCU: Onur *"dokuz sınır-değer literali `[KS-n] ± 1`'e çevrilsin"* diye kilitledi; **kaynaktan ölçüldü: ÇEVRİLECEK GERÇEK KALEM YOKTU.** Aracın *"SINIR DEĞER"* bulgularının tamamı ya bölüm atfı (`§3.2(9)`), ya tarihsel değişim kaydı (`11→31`), ya ölçüm kaydı, ya bayt kodlaması pinidir. **Gerçek sınır-değer sinyalleri (M31/M52) zaten atıflıydı.** Kural yürürlükte, bu turda uygulanacak kalem yok. *(Kilidi "uygulandı" diye raporlamak yapılmamış işi yapılmış ilan etmek olurdu.)*
⚠ ARAÇTA İKİ YANLIŞ-POZİTİF **BİLEREK DÜZELTİLMEDİ:** araç `[KS-4]`=10 için satırda `9`/`11` arar; M11 ve M41'deki `9` bir sayı değil **`§3.2(9)` bölüm atfıdır**. Belgeyi aracı susturmak için yeniden yazmak **kapı tiyatrosu** olurdu. §3.1'de adıyla yazılı.
SONRAKİ İŞ — **6. TUR BAĞIMSIZ KAPI, AYRI VE TEMİZ OTURUM.** PAZARLIKSIZ İKİ GÖREV: **(a) v6'yı denetle · (b) ONARILMIŞ ARACI da denetle** (K29-a'nın adlandırılmış bedeli; aracın bildirilmiş sınırları kaynak dosyanın başındaki *"ARACIN BEYAN EDİLMİŞ SINIRLARI"* bloğunda — en büyüğü: **çıpa hücresindeki jeton, o mutantın kararı gerçekten test ettiğini KANITLAMAZ ⇒ ELLE denetlenmelidir**).
  DENETÇİYE AYRICA: **gölgeleme bulgusunu çürüt** · **süpürücünün yeni yaş eşiğini (`[KS-4]`) M40/M29/M30 ile çapraz kontrol et** · **K4'ün "elle doğrula" listelerini tek tek aç** · **iki `K6` kaleminin (K3-B4/K3-C4) granülerlik savunması gerçekten ayakta mı**.
YÜRÜRLÜKTEKİ KURALLAR: **K30** (kapsam · sınır-değer · K6 granülerliği · kanonik yol) · **K26** (bağımsızlık = DÖRT KANAL; bağlayıcı: üretici kendi denetçisini SPAWN EDEMEZ) · **K21** (ölçmeden devir yok) · K13-a · hafıza ~satır 380'in **BEŞ PAZARLIKSIZ istisnası** · ~satır 253 taksonomi (ölü tuzak = MAJÖR, kör kapı = BLOKER) · **PUSH YASAK** · **sandbox'tan commit ETME** · **düz `git status` YASAK** (daima `git --no-optional-locks`) · §4 kuralı: her olgu iddiası ya ölçülür ya `[DOĞRULANMADI]`.
AÇIK [DOĞRULANMADI] (2): `flutter_secure_storage`'ın Windows şifreleme yöntemi · WebKit/Safari'nin `http://localhost` üzerinde `Secure`/`__Host-` davranışı.
AÇIK KAPI (1, adlandırılmış): **sızmış-parola listesinin lisans+CVE kapısı KOŞMADI** — `GOREV-slice-3c-auth` spec'inde koşar; geri dönüşü belgede yazılı.
GİT (ölçüldü): HEAD `2acc9f0` · origin/main `56362ed` · **19 ileri, push yok** · `.git/index.lock` YOK.
  Ağaç: ` M PROJE_HAFIZA.md` · ` M docs/ADR/0003-kimlik-cekirdegi.md` · ` D docs/ADR/0003-kimlik-cekirdegi-v6-YAZIM-DEVAM-EDIYOR.md` · `?? arsiv/…v5….md` · `?? _to_delete/`
ONUR'DAN BEKLENEN: **commit** (sandbox atamaz). ✅ **`_to_delete/` SİLİNDİ** (Onur, 25 Tem 2026, PowerShell): silmeden önce `Get-FileHash` ile **iki dosyanın da `6F6BE71A…38CB08`** olduğu Onur'un makinesinde doğrulandı ⇒ **bilgi kaybı YOK**; `Test-Path` **False**. ✅ **COMMIT ATILDI (Onur, 25 Tem 2026): `b772ae8`** — *docs(adr-0003): v6 canonical…* · 3 dosya · +213 / −117 · ağaç **TEMİZ** · `origin/main` `56362ed`, **20 ileri, PUSH YOK**.
  ⚠ **GİT LOG'UNDA YANILTICI BİR SATIR VAR — KAPI-6 BUNA ALDANMASIN [ölçüldü, içerik DOĞRU]:** `git add -A` sonrası git bir **yeniden adlandırma sezdi** ve commit çıktısına şunu yazdı:
  *"rename `docs/ADR/0003-kimlik-cekirdegi-v6-YAZIM-DEVAM-EDIYOR.md` => `arsiv/0003-kimlik-cekirdegi-TASLAK-v5-2026-07-25.md` (93%)"*.
  **Bu bir SEZGİSEL EŞLEŞTİRMEDİR, olan biteni TERS anlatır** (v6'nın arşive atıldığını ima eder). Gerçekte: v5'in içeriği arşive **kopyalandı**, v6'nın içeriği kanonik yola **yazıldı**, ara dosya **silindi**; git iki dosyayı %93 benzer buldukları için eşleştirdi.
  **İÇERİK COMMIT'İN İÇİNDEN ÖLÇÜLDÜ (beyan değil):** `git show HEAD:arsiv/…v5….md | sha256sum` ⇒ **`758bd1bf…22ef6`** (= v5) · `git show HEAD:docs/ADR/0003-kimlik-cekirdegi.md | sha256sum` ⇒ **`6f6be71a…38cb08`** (= v6). **İkisi de DOĞRU.** ⇒ **kusur yalnız log'un OKUNUŞUNDA; ağaçta ve blob'larda kusur YOKTUR.**
TÜM ÇALIŞMA: C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum


## ⏭ DEVİR NOTU (25 Tem 2026 — oturum 24 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

DURUM: **6. TUR BAĞIMSIZ KAPI KOŞTU. HÜKÜM: ADR 0003 v6 KİLİTLENEMEZ.**
  **9 BLOKER · 31 MAJÖR · 14 MİNÖR · 8 ÇÜRÜTÜLEN/DARALTILAN İDDİA.**
  Rapor: **`KANIT/adr-0003/kapi-6-denetim-raporu.md`** (34.106 bayt · 231 satır) — tam gerekçe, birebir alıntı ve ölçüm komutları orada.
  Ham alt-ajan çıktıları: **`KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/`** (11 dosya, 472 KB — K26(4)).
  Hafıza checkpoint'i: **K33** (bu notun hemen altında). Açılış kararları: **K32-a…d**.
  ⚠ **BU OTURUM DENETÇİDİR; HİÇBİR ŞEYİ ONARMADI.** ADR'ye ve araca **tek bayt dokunulmadı** (v6 sha256 `6f6be71a…38cb08` değişmedi; kapanışta yeniden ölçüldü).
  Yazılan dosyalar: `KANIT/adr-0003/kapi-6-denetim-raporu.md` (yeni) · `KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/` (yeni, 11 dosya) · `PROJE_HAFIZA.md` (K32 + K33 + bu not).
  **[K21 ÖLÇÜMÜ]** Kapanışta **~576.000 token = %57,6 ⇒ 🟡 SARI.** Eğri: 267k → 477k → 502k → 538k → 576k.
  ⇒ **Devir HEM ölçüme HEM doktrine dayanıyor:** sıradaki iş (v7 yazımı) bir YAZIM işidir ve bu oturum o kusurları BULAN denetçidir.
KAPI KURULUMU (K32-a): **17 bağımsız denetçi** — 10 bölüm denetçisi · 3 mutant-çıpası denetçisi (**51 mutantın 51'i**, K32-b) · 1 araç kaynak denetçisi (**altın kümenin 17 kontrolünün 17'si mutasyonla zorlandı**) · 1 **SONNET SALDIRGANI** (K32-c) · 1 kapı-5 raporu çapraz denetçisi (K32-d) · **RED-TEAM EN SON** · **TAMAMLIK KRİTİĞİ**. Hiçbir ajan diğerinin bulgusunu görmedi; hükmü taşıyan her iddia **Cowork tarafından kaynaktan yeniden ölçüldü** (K13-a).
🔴 **DOKUZ BLOKER (özet — tamamı raporda):**
 1. **M56 KÖR KAPI** — kill probu `123456` **6 karakter**, `[KS-17]`=**15** ⇒ kara liste kaldırılsa da `400` gelir; **iki ayak da mutasyon altında YEŞİL**. Belge bunu satır 216'da kendisi yazıyor (*"uzunluk, KDF'den önce gelir"*).
 2. **M29'un mutantı KOŞUL 3 tarafından MASKELENİYOR** — saat `[KS-4]`'ü aşınca iki silme mekanizması da sırrı `NULL`'lar ⇒ dal (d) ⇒ aile yine düşer ⇒ assert geçer. **M40b aynı önkoşulu pinlemiş, M29 pinlememiş.**
 3. **M24'ün "korumalı uç"u PİNSİZ** — belgenin adıyla pinlediği tek korumalı uç (`/v1/_probe/deny-default`) `ICurrentUser`'ı **çözmüyor** ⇒ mutant yaşar. K3-D2'nin tek kapısı budur.
 4. **ARAÇTA SESSİZ, BEYANSIZ K4 KAÇIŞ KAPISI** — kaynak **satır 412**: `if ("KANONİK" in s) or ("§1-K" in s): continue`. Belge geneli dize filtresi, hiç raporlanmıyor; canlı belgede **4 satırı** affediyor ve biri **satır 906 = M54'ün canlı mutant satırı**. Kapı-5'in B5-2'de kapattığı toptan-muafiyetin kardeşi, iki satır yukarıda açık.
 5. **§3.1'in *"araç altın kümede kendini kanıtlamadan koşmaz"* CÜMLESİ YANLIŞ** — kaynak 880-881: `if a.altin_kume: return altin_kume()` ⇒ normal tarama altın kümeyi **hiç koşmaz**; altın küme düşürülmüş araç bile çıkış kodu 0 verdi (koşularak kanıtlandı).
 6. **K3-L8'in dal envanteri EKSİK** — *"yenileme kimliği HİÇ YOK"* dalı yazılmamış; `__Host-mrt` `HttpOnly` olduğu için istemci *"kimliğim yok"* ile *"ağ yok"*u ayırt **edemez** ⇒ çıkış + uçak modu ⇒ **önceki kullanıcının yerel DB'si çevrimdışı-yetkili açılır.** ŞEMA kararıdır (satır 737 kendisi diyor).
 7. **§3.2(9) izolasyonu TEK YÖNLÜ** — `[KS-10]`=30 tek partition; hız sınırını **ölçmeyen** sınıflar için bütçe yok, beklemek yasak, limiter'ı kapatan kural yok ⇒ **43 TS/TC mutantın baseline'ı** paylaşılan fabrikada kırılır ⇒ §3.2(8)'in garantisi kurulamaz.
 8. **K3-I4'ün Extract gerekçesi *"§3.1'de beyanlıdır"* diyor; §3.1'de YOK** (`grep K3-I|HKDF|Extract|entropi` ⇒ boş). Kapı-5'in B5-5 kalıbının aynı belgede tekrarı.
 9. **AAD'nin BAYT KODLAMASI kapısız + beyansız** — §2-I *"AAD karıştırılır"* mutasyonunu ilan ediyor ama o form davranışı hiç değiştirmez ⇒ M51 geçer. HKDF `info` için M42b altın vektörü yazılmış, AAD'nin kardeşi yok. *(Ek: M51'in "A ve B aynı ailede" pini de yok.)*
⚠ **ÇÜRÜTÜLEN/DARALTILAN SEKİZ İDDİA — bulgu şişirmesi engellendi (rapor §4):** *"%6,5 yanlış"* **ÇÜRÜTÜLDÜ** (11112/170261 = **%6,53**, payda v4'tür) · *"Üçü de §0.4'te"* **ÇÜRÜTÜLDÜ** · araç `K18-a` ailesini görmüyor ⇒ **DARALTILDI** (o aile karar başlığı değil, çatal kilidi; desen genişletilse 0 yeni başlık) · K3-L4 kapısız ⇒ **DARALTILDI** (M33a/M33b fiilen ısırıyor; kusur çıpa hijyeni) · M51 ısırmıyor ⇒ **TERSİNE DÖNDÜ** (aynı ailede ölür) · M42 türetme gözlemsiz ⇒ **DARALTILDI** (M42b + M55 ölür) · satır 908 bilgi kaybı ⇒ **DARALTILDI** (kayıp render'da, kaynakta değil) · *"120 sn blokerdir"* ⇒ **MAJÖRE İNDİRİLDİ** (hiçbir mutant o literale çıpalı değil).
✅ **KIRILAMAYAN YERLER — v6'nın kazandıkları:** §3.1'in **TAMLIK İDDİASI DÖRDÜNCÜ TURDA DA AYAKTA** (47 kararın 47'si kapılı/beyanlı/devredilmiş, kapısız-VE-beyansız **0**) · **ALTIN KÜME GERÇEKTEN ISIRIYOR: 17/17 mutasyonla zorlandı, 17'si de kırmızı verdi** ⇒ kapı-5'in en ağır endişesi KAPANDI · aracın koşumu birebir tekrarlandı (`K1`=0·`K2`=0·`K3`=0·`K5`=0·`K6`=2·`K4`=5) · numara bütünlüğü TAM, çift kayıt 0 · `[çıpa+]` 12 + `[devir]` 5 **ölçümle birebir** ⇒ kapı-5'in *"16≠18"* tutarsızlığı ÇÖZÜLMÜŞ · §6'nın 17 riski boşluksuz ve **21 atfın 21'i** var olan bir kaleme düşüyor ⇒ *"Risk #17 yok"* blokeri kapandı · `[KS-4]`→K16-b yanlış atfı **tamamen temizlenmiş** · ADR 0001 K-H1 alıntısı kelime kelime sadık · **iki `K4` yanlış-pozitifini düzeltmeme kararı DOĞRUDUR** (kapı tiyatrosu olurdu).
🔬 **SONNET SALDIRISI (K32-c): 10 vektör, 6'sı KAÇTI** — Kiril homoglif karar başlığı · küçük harfli aile (`K9-d1`) · **çıpa hücresine alakasız karar doldurma** (aracın kendi beyan ettiği en büyük sınırı, somut belgeyle kanıtlandı) · sayıyı yazıyla yazmak · THIN SPACE ile bölünmüş sayı · uzak satırdaki *"rezerv"* kelimesiyle mutant-boşluğu affı. 4'ü YAKALANDI.
🔴 **GÖLGELEME ÇÜRÜTÜLEMEDİ.** Kapı-6 bunu çürütmekle yükümlüydü; iki bağımsız denetçi adım adım yürüttü ve **iddia doğru çıktı** — üstelik §6 #11'in kendi `[KS-1]` tavanını da yıkıyor: `[KS-4]`(10 dk) < `[KS-1]`(15 dk) olduğu için saldırganın her turda **10 dakikalık bütçesi** vardır ⇒ **çevrimiçi kurbanda da en kötü durum `[KS-1]` değil `[KS-2]`'dir (15 dk değil 30 gün)** ve bu sayı belgenin hiçbir yerinde yazmıyor. *(Bir alt-iddia ÇÜRÜTÜLDÜ: zincir çatallanmaz — iki taraf daima AYNI halefi alır.)*
⚠ **KAPI-6'NIN ÖLÇEMEDİKLERİ [adlandırılıyor, gizlenmiyor]:**
  1. **ADR 0002 (`0002-senkron-mekanigi.md`) BU DENETİME BAĞLANMADI ve HİÇ AÇILMADI.** Belge ona 9 satırda atıf yapıyor (`K2-E3`×6 · `K2-E5`×2 · `K2-A4`×2 · `K2-H12`×2 · `K2-I3`) ve bir kısmı **"birebir alıntı"** iddiasıdır. **K14-h'nin tek gerekçesi K2-I3'tür ve doğrulanmamıştır.**
  2. **`src/` · `tests/` · `docker-compose.yml` bağlı değildi** ⇒ §3.2 gerçek koda karşı ölçülemedi (kapı-4'ün aynı körlüğü).
  3. **30+ birincil kaynak alıntısı bağımsız doğrulanmadı** (NIST ×13, RFC'ler, aspnetcore ×10). Aday sapma: `[KS-19]` *"254 karakter (RFC 5321 **yol sınırı**)"* — yol sınırı 256'dır, 254 türetilmiştir ⇒ **[DOĞRULANMADI]**.
  4. Gerçek GitHub render'ı ölçülemedi.
🆕 **`docs/ODEV.md` İLE DÖRT ÇELİŞKİ ÖLÇÜLDÜ [tamamlık kritiği — kimse bakmamıştı]:** ODEV §3.1/§4.1/§6.1 dilimi hâlâ **`slice-3a-auth`** diyor (ADR 8 yerde `slice-3c-auth`; ODEV *"çelişkide bu dosya geçerlidir"* diyor) · ODEV §6.1 dilimi **1,5-2 gün** diye kutuluyor (ADR tek başına 241.689 bayt) · plan `21-23 Tem`, bugün 25 Tem · **ODEV §6 *"zaman kutulanmalıdır"* ⟷ K13-a *"tur sayısı sınırlanmaz"* — İKİSİ AYNI ANDA YÜRÜRLÜKTE OLAMAZ ve hiç adjudike edilmemiştir.**
SONRAKİ İŞ (sırayla, HER BİRİ AYRI OTURUM):
  1. **ONUR DÖRT ÇATALI KİLİTLESİN:** Ç-1 `[KS-31]` × `[KS-17]` kesişimi (M56'nın probu) · Ç-2 K3-L8'in dördüncü dalı + çıkışta aktif profil kaydı (ŞEMA kararı) · Ç-3 §3.2(9)'un kapsamı · **Ç-4 ODEV × K13-a çatışması = v6→v7 döngüsünün SONLANMA KANITI sorusu.**
  2. **v7 YAZIMI — AYRI OTURUM** (9 bloker + 31 majör + dört kilit).
  3. **ÖLÇÜM ARACININ İKİNCİ ONARIMI — AYRI EL** (B6-4 · B6-5 + Sonnet'in 6 kaçışı). **v7'yi yazan el aracı onarırsa K29-a'nın bedeli üçüncü kez ödenir.**
  4. **7. TUR BAĞIMSIZ KAPI — AYRI OTURUM;** ADR 0002 ve `src/`/`tests/` **BAĞLANMADAN başlamamalı**, ve **kapı-6 raporunu da denetlemeli** (rapor §4'teki sekiz çürütme dâhil).
KAPI-6'NIN ÜÇ SINIRI (bir sonraki denetçiye): **(a)** belgeyi belgeye karşı denetledi, KAYNAĞA karşı değil · **(b)** kapı-6 de bir üretimdir, kapı-7 onu da denetlemelidir · **(c)** araç artık ISIRIYOR ama KAPSAMI DAR (K4 §1-K'yı, türetilmiş değerleri, `KS-n→kapı` sütununu ve tek haneli literalleri taramıyor + satır 412'de sessiz kaçış) ⇒ **aracın yeşili hâlâ tek başına kanıt değildir.**
YÜRÜRLÜKTEKİ KURALLAR: **K32** · K30 · **K26 (üretici kendi denetçisini SPAWN EDEMEZ; alt-ajan çıktısı HAM olarak KANIT'a yazılır)** · K21 · K13-a · hafıza ~satır 253 taksonomi (ölü tuzak = MAJÖR, kör kapı = BLOKER) · **PUSH YASAK** · **sandbox'tan commit ETME** · **düz `git status` YASAK** · §4 kuralı.
AÇIK [DOĞRULANMADI]: `flutter_secure_storage` Windows şifrelemesi · WebKit `http://localhost` `__Host-` · **Isopoh lisans ailesi (ÜÇÜNCÜSÜ — belge "iki" diyor, YANLIŞ)** · **RFC 5321'in 254 vs 256 atfı (YENİ)**.
AÇIK KAPI (1): sızmış-parola listesinin lisans+CVE kapısı KOŞMADI.
GİT (ölçüldü): HEAD `ffc33ce` · origin/main `56362ed` · **21 ileri, push yok** · `.git/index.lock` YOK.
  Ağaç: ` M PROJE_HAFIZA.md` · `?? KANIT/adr-0003/kapi-6-denetim-raporu.md` · `?? KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/` ⇒ **commit ONUR'DA.**
ONUR'DAN BEKLENEN: **commit** (sandbox atamaz) · **dört çatalın kilidi (Ç-1…Ç-4)** · **AÇIK İŞLER md.9'un güncellenmesi** (hâlâ *"SIRADA: 6. tur"* diyor; 6. tur KOŞTU) · **`docs/ODEV.md`'nin `slice-3c-auth` adına ve dilim bütçesine göre güncellenmesi.**
TÜM ÇALIŞMA: C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 24 kapanış: **K33 — 6. TUR KAPI KOŞTU; HÜKÜM KİLİTLENEMEZ; ARACIN ALTIN KÜMESİ İLK KEZ MUTASYONLA KANITLANDI**)

- **[K21 ÖLÇÜMÜ]** Kapanışta **%57,6 ⇒ 🟡 SARI.** Kural gereği **eldeki madde (rapor + hafıza) bitirildi, yeni büyük iş BAŞLATILMADI.**
- **✅ K32'NİN DÖRT KİLİDİNİN DÖRDÜ DE UYGULANDI:** tam ölçek + tamamlık kritiği (a) · **51 mutantın 51'i tek tek elle denetlendi** (b) · Sonnet saldırısı koştu (c) · kapı-5 raporu çapraz denetlendi (d).
- **🔴 HÜKÜM: v6 KİLİTLENEMEZ — 9 bloker · 31 majör · 14 minör.** Bu **6. turdur** (K13-a: bloker sıfırlanana kadar tur).
- **🟢 TURUN EN BÜYÜK KAZANCI — ARACIN ALTIN KÜMESİ ARTIK KANITLI:** 17 kontrolün **17'si** ayrı ayrı mutasyonla zorlandı, **17'si de kırmızı verdi** (çıkış kodu 2). Kapı-5'in *"altın küme süs olabilir"* endişesi **kapanmıştır.** Buna karşılık aracın **KAPSAMI** dar çıktı ve **satır 412'de sessiz bir kaçış kapısı** bulundu (B6-4).
- **🔴 ÖRÜNTÜ — DÖRDÜNCÜ TUR ÜST ÜSTE AYNI KÖK NEDEN:** v6, kapı-5'in bulgularını kapatırken **aynı kusur sınıflarını kendi YENİ yazdığı satırlarda yeniden üretti** — M56 (hücre taşması, kapı-5'in M31'de bloker saydığı sınıf) · M54 (ölmeyen ikinci ayak, satır 833'ün kendi kuralı) · K3-I4 (*"§3.1'de beyanlı"* ama değil, kapı-5'in B5-5 kalıbı) · §6 #17 (*"§7'ye yazıldı"* ama yazılmamış). **Ders: bir kusur sınıfını kapatan turun kendisi o sınıfın en olası üreticisidir; v7 yazımı bitince ÖNCE kendi yeni satırlarını eski bulgu sınıflarına karşı taramalıdır.**
- **⚠ KAPI-6 DE BİR ÜRETİMDİR.** Raporun §4'ünde **sekiz iddia çürütüldü ya da daraltıldı**; bir bulguyu düşürmek, bulmak kadar hatalı olabilir. **Kapı-7 bu raporu da denetlemelidir.**
- **⚠ İKİ YAPISAL BOŞLUK BU TURDA İLK KEZ ADLANDIRILDI:** **(1)** ADR 0002 hiç açılmadı — belgenin iki öncülünden biri altı turdur denetlenmemiş. **(2)** `docs/ODEV.md` bugüne kadar hiçbir kapı turunda okunmamış ve **K13-a ile doğrudan çelişiyor** (*"zaman kutulanmalıdır"* ⟷ *"tur sayısı sınırlanmaz"*) ⇒ **v6→v7 döngüsünün sonlanma kanıtı YOKTUR ve bu soru hiç sorulmamıştır.**
- **Sıradaki ilk iş:** Onur **Ç-1…Ç-4**'ü kilitlesin → **v7 yazımı AYRI OTURUM** → **araç onarımı AYRI EL** → **7. tur kapı AYRI OTURUM (ADR 0002 + `src/` + `tests/` bağlı olarak)**.


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 24 açılış: **K32 — 6. TUR BAĞIMSIZ KAPI AÇILDI; DÖRT ÇATAL KİLİTLENDİ**)

- **Protokol koşuldu (oturum 24):** `PROJE_HAFIZA.md` **TAM** (845 satır) + `CLAUDE.md` **TAM** (49 satır) okundu. **BU OTURUM DENETÇİDİR; v6'ya ve araca TEK BAYT DOKUNMAZ** (K26 — v6'yı yazan oturum 23'tü; bu oturum üretici DEĞİL ⇒ K26(2) gereği denetimi alt ajanlara dağıtabilir).
- **✅ GİT (kaynaktan, `git --no-optional-locks`):** ağaç **TEMİZ** · HEAD **`ffc33ce`** *"docs(hafiza): record that git's rename detection misreads the v5/v6 swap; blobs verified by sha256"* · `origin/main` **`56362ed`** · **21 ileri, PUSH YOK** · `.git/index.lock` **YOK**. ⇒ Devir notu **20 ileri** diyordu; fark Onur'un yanıltıcı-log notunu taşıyan commit'tir, **tutarlı**.
- **✅ BELGE KİMLİĞİ (Onur'un diskinden ölçüldü):** `docs/ADR/0003-kimlik-cekirdegi.md` = **241.689 bayt · 1.123 satır · sha256 `6f6be71a…38cb08`** · UTF-8 geçerli · BOM yok · `U+FFFD` **0** · CRLF **0** ⇒ devir notuyla **BİREBİR**. Arşivde **beş** taslak (v1-v5); `arsiv/…v5….md` sha256 **`758bd1bf…22ef6`** ✓. **Denetim kopyasının sha256'sı diskle AYNI ölçüldü** ⇒ kapı gerçek artefakt üzerinde koşuyor (kapı-4 emsali).
- **✅ ARAÇ YENİDEN KOŞULDU (Cowork'ün kendi koşumu, beyan değil):** `--altin-kume` **çıkış kodu 0** · v6 üzerinde **`K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=2 · `K4`=5 (toplam 7)** · özet `karar` 47 · `alt_madde` 12 · `kapili` 32 · `beyanli` 15 · `devredilmis` 5 · `mutant` 51 · `capasiz_tablo` 0 ⇒ **devir notuyla BİREBİR**.
- **[K21 ÖLÇÜMÜ — kendi oturum-id'siyle, açılış]** canlı bağlam **267.232 token = %26,7 ⇒ 🟢 YEŞİL** (transcript `c9fe0532-…`, tek `sessionId`, mtime canlı).
- **[KARAR — Onur, 25 Tem 2026] K32-a: KAPI-6 = TAM ÖLÇEK + TAMAMLIK KRİTİĞİ.** Belge ~10 bağımsız bölüm denetçisine bölünür + AYRI bir araç denetçisi + **RED-TEAM EN SON** + kapanışta *"ne atlandı"* kritiği. *Gerekçe:* 6. tur bir **KİLİT** turudur; K26(3) devir notunun kapsamı daraltmasını yasaklar ve denetçiyi genişletmekle yükümlü kılar; eksik kapsam turu 7'ye taşır. *Bedel [adlandırılmış]:* en pahalı seçenek, bağlam ve alt-ajan maliyeti yüksek. *Reddedilenler:* tamamlık kritiği olmadan tam ölçek (*"hangi modalite koşulmadı"* hiç sorulmaz) · odaklı tur (yalnız devredilen kalemler + araç — **K26(3)'ü fiilen deler**, 1.123 satırın çoğu okunmaz).
- **[KARAR — Onur, 25 Tem 2026] K32-b: 51 MUTANTIN 51'İNİN ÇIPASI TEK TEK ELLE DENETLENİR.** *Gerekçe:* aracın **kendi beyan ettiği en büyük sınırı** budur — çıpa hücresindeki jeton, o mutantın kararı gerçekten ısırdığını KANITLAMAZ; denetlenmezse **`K1`=0 bir KAPI TİYATROSUDUR** ve kapı-4/kapı-5 tam bu sınıftan bloker üretti. *Bedel [adlandırılmış]:* turun en pahalı kalemi. *Reddedilenler:* yalnız v6'da yeni/değişen mutantlar + kapı-5'in şüpheli listesi (orta maliyet, ama dokunulmamış mutantlar bu turda hiç açılmaz) · örnekleme (*"kalan 36'sı denetlenmedi"* cümlesi kilidin üstünde asılı kalır).
- **[KARAR — Onur, 25 Tem 2026] K32-c: 4. KANAL TELAFİSİ = SONNET İLE SALDIRI.** Onarılmış araca *"bu aracı KANDIR — yeşil geçen ama gerçekte kusurlu bir belge üret"* görevi verilir (K26-a emsalinin aynısı). *Dürüst kayıt:* Sonnet ile Opus aynı aileden ⇒ kör nokta korelasyonu azalır, **sıfırlanmaz**; bu ek örneklemdir, garanti değil. *Reddedilenler:* Sonnet + Opus çift saldırı (Opus ayağı 4. kanalı adreslemez) · **Fable 5** (en düşük korelasyon ama haftalık limiti yer; Onur'un K26-a'daki hariç tutması korundu) · saldırı yok (K26(5)'in telafisi eksik kalırdı).
- **[KARAR — Onur, 25 Tem 2026] K32-d: KAPI-5'İN KENDİ RAPORU DA DENETLENİR.** `KANIT/adr-0003/kapi-5-denetim-raporu.md` (19.827 bayt) çapraz kontrol edilir: 12 bloker + 7 majörün her satır numarası ve komutu tekrarlanabilir mi, v6 gerçekten kapattı mı. *Gerekçe:* v6'nın *"kapattım"* iddialarının tek bağımsız referansı o rapordur; denetlenmezse **v6'nın kapanma beyanları kendi kaynağına dayanır** ve kapı-5'in devir notundaki kendi uyarısı (*"kapı-5 de bir üretimdir"*) karşılıksız kalır. *Reddedilen:* kapsamı v6 + araçla sınırlamak (daha ucuz, ama kapı-5'in raporundaki olası bir hata v6'ya sessizce geçmiş olabilir).
- **Sıradaki ilk iş:** A turu (bölüm denetçileri + mutant çıpası + araç + kapı-5 raporu + devredilen dört kalem) → her bulgu Cowork tarafından **kaynaktan yeniden ölçülür** (K13-a) ve ham alt-ajan çıktıları KANIT'a yazılır (K26(4)) → **RED-TEAM EN SON** → tamamlık kritiği → `KANIT/adr-0003/kapi-6-denetim-raporu.md` + hüküm.


## ⏭ DEVİR NOTU (25 Tem 2026 — oturum 22 GERÇEK kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

DURUM: **Kapı-5 koştu (KİLİTLENEMEZ) · araç onarıldı · K26/K28 kilitlendi · v6 YAZIMI BAŞLADI, YARIM.**
  **[K21 ÖLÇÜMÜ — kapanış] 559.924 token = %56,0 ⇒ 🟡 SARI.** K21'in *"DOLU BAĞLAMDA ADR YAZMA YASAĞI"*
  tam bu banttan başlar ⇒ **v6 yazımı burada DURDURULDU**, devir ölçüme dayanıyor.
  Eğri: 126k → 245k → 311k → 353k → 481k → 502k → 533k → 548k → **560k**.
KANONİK ADR HÂLÂ **v5**: `docs/ADR/0003-kimlik-cekirdegi.md` · sha256 `758bd1bf…22ef6` · **TEK BAYT DOKUNULMADI**.
ÇALIŞMA DOSYASI: `docs/ADR/0003-kimlik-cekirdegi-v6-YAZIM-DEVAM-EDIYOR.md` (219.175 bayt · 1.086 satır).
BU OTURUMUN ÜRETTİKLERİ: `KANIT/adr-0003/kapi-5-denetim-raporu.md` · `KANIT/adr-0003/olcum-araci-onarim-kanit.txt`
  · onarılmış `araclar/adr-kapi-taramasi.py` (altın küme **22 kontrol**, çıkış kodu 0) · K25-K29 checkpoint'leri.
ARACIN v6 ÜZERİNDEKİ KOŞUMU: **`K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=3 · `K4`=27** (toplam 30; v5'te 40).
YENİ OTURUMUN İLK İŞİ — **v6 YAZIMININ DEVAMI, ALTI KALEM (sırayla):**
  1. **§2-C ~satır 330: OLGUSAL OLARAK YANLIŞ CÜMLE HÂLÂ DURUYOR.** *"pencere reuse-detection'ı `[KS-4]`
     geciktirir"* ⇒ doğrusu **`[KS-1]`**, çevrimdışı kurbanda **`[KS-2]`**'ye uzar. §0.4'e GERİ ÇEKİLEN
     İDDİA olarak yazılır ve K14-a'nın maliyet muhasebesi bu gerçek sayıyla yeniden tartılır. §6 Risk #11 de aynı yanlışı tekrarlıyor.
  2. **KARAR METİNLERİ (mutantlar yazıldı, kararlar YAZILMADI):** K3-B1'e **salt tazeliği** kararı (M54) ·
     K3-I4'e **`static class HkdfLabels` pini** (M55) · K3-B6'ya **blocklist maddesi** (`[KS-31]`, gömülü
     10.000 kayıt, lisans kırmızı-çizgi-#3 kapısından geçer) (M56).
  3. **`K4`'ün 27 bulgusunun kalem kalem adjudikasyonu** — çoğu ZAYIF sınıf ya da `[KS-LITERAL]` muafiyetli;
     hepsi aracın çıktısında *"ELLE DOĞRULA"* etiketiyle listeli.
  4. **`[çıpa+]` 16 ≠ 18** kaynaktan çözülmeli (hangi iki kararın etiketi düşmüş).
  5. **M5-7:** `Microsoft.Extensions.TimeProvider.Testing` `Momentum.Api.Tests`'te YOK ⇒ §3.2(6)'ya
     *"paket bu projeye EKLENECEKTİR — aynı sürüm, yeni paket değil"* yazılır.
  6. Kapı-5'in **kalan majörleri ve minörleri** + kapı-4'ten devreden borç.
  Sonra: **v6 kanonik yola alınır, v5 arşive** → **6. TUR BAĞIMSIZ KAPI, AYRI OTURUM.**
KAPI-6'YA PAZARLIKSIZ İKİ GÖREV: **(a) v6'yı denetle · (b) ONARILMIŞ ARACI da denetle** — v6'yı yazan el
  aracı da onardı (K29-a'nın adlandırılmış bedeli). Aracın **bildirilmiş sınırları** kaynak dosyanın
  başındaki *"ARACIN BEYAN EDİLMİŞ SINIRLARI"* bloğunda yazılı; **çıpa hücresindeki jetonun o mutantın
  kararı gerçekten test ettiğini kanıtlamadığı** en büyük sınırdır ve ELLE denetlenmelidir.
YÜRÜRLÜKTEKİ KURALLAR: **K26 (bağımsızlık = DÖRT KANAL; bağlayıcı olan: üretici kendi denetçisini
  SPAWN EDEMEZ)** · K21 · K13-a · hafıza satır ~380'in **BEŞ PAZARLIKSIZ istisnası** · satır ~253
  taksonomi (ölü tuzak = MAJÖR, kör kapı = BLOKER) · PUSH YASAK · sandbox'tan commit ETME ·
  düz `git status` YASAK (daima `git --no-optional-locks`) · §4 kuralı.
AÇIK [DOĞRULANMADI] (2): flutter_secure_storage Windows şifreleme · WebKit `http://localhost` `__Host-`.
ONUR'DAN BEKLENEN: commit · AÇIK İŞLER md.9 ve md.10'un düzeltilmesi (md.10 hâlâ *"Ayrı ADR-0004 YAZILMAZ"*
  diyor — satır ~298 bunu çürütüyor; md.9 hâlâ ADR 0003'ü *"TASLAK v1 / son tur"* diye anlatıyor).
TÜM ÇALIŞMA: C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 23 kapanış: **K31 — v6 BİTTİ VE KANONİK YOLA ALINDI; ARAÇ 30 → 7 BULGU**)

- **[K21 ÖLÇÜMÜ]** Kapanışta **431.999 token = %43,2 ⇒ 🟢 YEŞİL.** Eğri: 251k → 382k → 432k. **Oturum bağlam yüzünden değil, DOKTRİN gereği kapanıyor** (bu oturum v6'yı yazdı, denetleyemez — K26).
- **✅ ALTI KALEMİN ALTISI DA KAPANDI:** (1) §2-C'nin olgusal yanlışı geri çekildi ve maliyet muhasebesi yeniden yazıldı · (2) üç karar metni yazıldı (**K3-B1(2)** salt tazeliği/M54 · **K3-I4**'e `static class HkdfLabels` pini/M55 · **K3-B6(2)** sızmış-parola blocklist'i `[KS-31]`/M56) · (3) `K4`'ün 35 bulgusu kalem kalem adjudike edildi · (4) `[çıpa+]` sayımı kaynaktan çözüldü · (5) `TimeProvider.Testing` + `Testcontainers` **`Momentum.Api.Tests`'e eklenecek** diye yazıldı (ölçüm: dört `.csproj`'un tamamı okundu) · (6) `K6`'nın üçü ve kapı-4'ten devreden iki majör kapatıldı.
- **📊 ARAÇ: 30 → 7 BULGU.** `K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=2 · `K4`=5. Altın küme **çıkış kodu 0**. **Kalan 7'nin 7'si de §3.1'de ADIYLA adjudike edildi:** 2 aracın yanlış-pozitifi (`§3.2(9)` bölüm atfı sınır-değer sanılıyor — **bilerek düzeltilmedi, kapı tiyatrosu olurdu**) · 2 granülerlik farkı (`K3-B4`/`K3-C4`) · 3 aracın kendi kapsam beyanı.
- **🔴 BU OTURUMUN BULDUĞU BİR BLOKER — KİLİTLENMİŞ BİR KARAR FİİLEN ÇÜRÜMÜŞTÜ:** süpürücünün SQL yaş eşiği `[KS-6]`'ya (60 sn) bağlıydı; doğrusu `[KS-4]`'tür. **K28-a `[KS-4]`'ü 10 dk yapınca replay penceresi fiilen 60 sn'de ölüyordu** ⇒ Onur'un kilidi belgenin içinde çalışmıyordu. Düzeltildi. **M40'ın `121 sn` kill sinyali de aynı sebeple ölü tuzağa dönmüştü** ⇒ `[KS-4]` + `[KS-6]` yapıldı. *Ders: bir kanonik sayı değiştiğinde, ona ATIF YAPMAYAN ama ondan TÜREYEN her satır elle taranmalıdır — `[KS-n]` kuralı bunları yapısal olarak korumaz.*
- **🔴 KAPI-6'YA DEVREDİLEN AÇIK BULGU — GÖLGELEME:** §2-C(3)'ün koşul 2'si **halefin** `consumed_at`'ine baktığı için saldırgan ve meşru istemci zinciri adım adım paylaşabilir; *"halef kullanılınca aile düşer"* cümlesi belgenin kendi kuralıyla yanlışlanır. Ömür uzamaz, yeni token basılmaz (v1'in sonsuz zinciri hâlâ imkânsız) ama **K14-a'nın maliyeti v4/v5'in yazdığından büyüktür**. §0.4'e iki iddia geri çekildi. **Kapı-6 bunu çürütmekle yükümlüdür.**
- **⚠ K30-b DÜRÜSTÇE "UYGULANACAK KALEM YOK" DİYE KAPANDI:** aracın dokuz *"sınır değer"* bulgusunun **hiçbiri gerçek bir kill-sinyali literali değildi**; gerçek olanlar (M31/M52) zaten atıflıydı. Kural yürürlükte, bu turda uygulanacak kalem bulunamadı — **kilidi "uygulandı" diye raporlamak yapılmamış işi yapılmış ilan etmek olurdu.**
- **✅ KANONİK YOL (K30-d):** `docs/ADR/0003-kimlik-cekirdegi.md` = **v6**, **241.689 bayt · 1.123 satır**, sha256 **`6f6be71a…38cb08`**, UTF-8/BOM yok/`U+FFFD` 0/LF, 15.237 Türkçe harf. **v5 arşive KOPYALANDI** (`arsiv/…TASLAK-v5-2026-07-25.md`, sha256 `758bd1bf…22ef6` — birebir doğrulandı). Mutant numara bütünlüğü **TAM** (eksikler tam olarak M2·M3·M9·M10·M20 = 0004 rezervi; M8 ve M33 harfli ayaklara bölünmüş; M13 VOID).
- **⚠ BU OTURUM v6'YI DENETLEMEDİ VE DENETLEYEMEZ.** Yapılan tek şey **öz-doğrulamadır** (kodlama · bayt/satır · sha256 · numara bütünlüğü · bayat referans taraması · aracın koşumu). **BUNLAR KAPI DEĞİLDİR.**
- **Sıradaki ilk iş:** **6. TUR BAĞIMSIZ KAPI — AYRI VE TEMİZ OTURUM**, iki pazarlıksız görevle: **(a) v6'yı denetle · (b) ONARILMIŞ ARACI denetle.**


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 23 açılış: **K30 — v6'NIN KALAN İŞİ İÇİN DÖRT KARAR KİLİTLENDİ**)

- **Protokol koşuldu (oturum 23):** `PROJE_HAFIZA.md` **TAM** (782 satır) + `CLAUDE.md` **TAM** (49 satır) okundu. **BU OTURUM YAZIM OTURUMUDUR; HİÇBİR ŞEYİ DENETLEMEZ** (K26 — üretici kendi denetçisini spawn edemez; kapı-6 ayrı oturumdur).
- **✅ GİT (kaynaktan, `git --no-optional-locks`): ONUR COMMIT'İ ATMIŞ — devir notunun "beklenen"i KAPANDI.** Ağaç **TEMİZ** · HEAD **`2acc9f0`** *"v6 wip: close 12 blockers from gate-5, add M54-M56, teach tool the devir marker"* · `origin/main` **`56362ed`** · **19 ileri, PUSH YOK** · `.git/index.lock` **YOK**.
- **✅ BELGE KİMLİĞİ (Onur'un diskinden ölçüldü):** kanonik **v5** `docs/ADR/0003-kimlik-cekirdegi.md` sha256 **`758bd1bf…22ef6`** — **TEK BAYT DEĞİŞMEMİŞ** · çalışma dosyası v6 **219.175 bayt · 1.086 satır**, sha256 **`db2bbcfe…dd71a`**, UTF-8, BOM yok, `U+FFFD` 0, satır sonu **LF**.
- **✅ ARAÇ YENİDEN KOŞULDU:** `--altin-kume` **çıkış kodu 0** (22 kontrol) · v6 üzerinde **`K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=3 · `K4`=27 (toplam 30)** ⇒ **devir notuyla BİREBİR**; özet `kapili` 33 · `beyanli` 14 · `devredilmis` 4 · `capasiz_tablo` 0 · `mutant` 51.
- **[K21 ÖLÇÜMÜ — kendi oturum-id'siyle, açılış]** canlı bağlam **250.701 token = %25,1 ⇒ 🟢 YEŞİL** (transcript `2458a1de-…`, tek `sessionId`, mtime canlı). ⇒ **ADR yazma yasağı bandında DEĞİLİZ.**
- **🔎 YENİ ÖLÇÜM (kalem 4'ün ilk verisi):** v6'da `**[çıpa+]**` etiketi **12 mutant satırında**, `**[devir]**` **5 M-L satırında** (ayrıca ikisi düz metinde anılıyor: satır 931, 952). Buna karşılık **satır 925 hâlâ *"36'sı kapılı, 11'i burada beyanlı"* diyor**, aracın ölçümü ise **kapılı 33 · beyanlı 14 · devredilmiş 4**. ⇒ **"16≠18" tek başına bir sayım hatası değildir; §3.1'in tamlık cümlesindeki sayılar da BAYATTIR** ve kalem 4 bunların hepsini kaynaktan çözer.
- **[KARAR — Onur, 25 Tem 2026] K30-a: KAPSAM = DEVİR NOTUNUN ALTI KALEMİNİN TAMAMI, BU OTURUMDA.** *Gerekçe:* bağlam %25 🟢 ve altı kalemin bağlamı tek bir belgede toplu; bölmek bir oturum daha harcar (15 Ağu baskısı, K12-b gereği ölçüm raporlanır ama kapsam kesilmez). *Bedel [adlandırılmış]:* oturum uzarsa K21 bandı sarıya girer ⇒ **eldeki kalem bitirilir, checkpoint yazılır, yeni büyük iş başlatılmaz.** *Reddedilen:* önce 1-2-5, sonra ara rapor (daha küçük adım ama toplam maliyet bir oturum artar).
- **[KARAR — Onur, 25 Tem 2026] K30-b: `K4`'ÜN DOKUZ "SINIR DEĞER LİTERALİ" BULGUSUNUN HEPSİ `[KS-n] ± 1` BİÇİMİNE ÇEVRİLİR.** Kill sinyallerindeki `10-1` · `12-1` · `30+1` · `32-1` · `15+1` gibi ham sayılar atıflı biçime geçer. *Gerekçe:* aracın gerekçesi ölçülmüş biçimde doğrudur — kanonik değer değişirse bu mutantlar **ÖLÜ TUZAĞA** döner ve bu **tam olarak M11'in v4'te yediği kusurdur** (hafıza satır ~253 taksonomisi: ölü tuzak = MAJÖR). K28-a'nın `[KS-4]` 60 sn → 10 dk değişimi bu riskin varsayımsal olmadığını **fiilen** kanıtladı. *Bedel [adlandırılmış]:* kill sinyalleri bir tık daha az okunur. *Reddedilenler [adlandırılmış]:* **yalnız değişme riski olanları çevirmek** (daha az yazım, ama "hangisi değişmez" bir YAZAR İDDİASI olur ve belgenin kendi §4 kuralına takılır) · **dokunmayıp §3.1'e beyan** (en ucuz, ama belge kendi kanonik-sayı kuralını mutant tablosunda deler).
- **[KARAR — Onur, 25 Tem 2026] K30-c: `K6`'NIN ÜÇ ÇELİŞKİSİ (`K3-B4` · `K3-C4` · `K3-L9`) §3.1'DE ÖZELLİK DÜZEYİNE DARALTILARAK KAPATILIR.** Her biri için §3.1 satırı kararın **hangi ÖZELLİĞİNİN** kapısız kaldığını açıkça yazar. *Gerekçe:* araç KARAR-ID düzeyindedir ve bunu mekanik olarak ayırt edemeyeceğini **kendi mesajında beyan ediyor**; çelişki ancak metin özellik düzeyine inince buharlaşır ve dürüstlük beyanı güçlenir. *Reddedilenler [adlandırılmış]:* **beyanı silmek** (kapı-5 tam da "beyanı silip kapıyı süs bırakma" sınıfını bloker saydı ⇒ yanlış tarafa düşerse kör kapı doğar) · **üçünü tek tek Onur'a getirmek** (en güvenli ama üç ek tur alışveriş).
- **[KARAR — Onur, 25 Tem 2026] K30-d: ALTI KALEM BİTİNCE v6 KANONİK YOLA ALINIR, v5 ARŞİVE TAŞINIR.** *Gerekçe:* v5'te de aynı sıra izlendi (yaz → kanoniğe al → kapı) ve kapı-6 kanonik dosyayı denetler; aksi hâlde kanonik ADR **12 bloker taşımaya devam eder**. *Bedel [adlandırılmış, gizlenmiyor]:* kanonik ADR bir süre **DENETLENMEMİŞ** olur — v5'te de öyleydi ve hafızada açıkça yazılıydı. *Reddedilen:* çalışma dosyası olarak bırakıp kapı-6'dan sonra taşımak (kanonik yol hiç denetlenmemiş belge taşımaz, ama kanonik ADR blokerli kalır ve commit geçmişi kafa karıştırır).
- **Sıradaki ilk iş:** kalem 1 → 2 → 5 → 4 → 3 → 6 → öz-doğrulama → kanonik yola alma → kapanış checkpoint'i + devir notu. **Kapı-6 AYRI OTURUM ve hem v6'yı hem ONARILMIŞ ARACI denetlemek ZORUNDADIR (K29-a'nın adlandırılmış bedeli).**


## ⏭ DEVİR NOTU (25 Tem 2026 — oturum 22 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

DURUM: **5. TUR BAĞIMSIZ KAPI KOŞTU. HÜKÜM: ADR 0003 v5 KİLİTLENEMEZ.**
  **12 ölçülmüş bloker · 7 majör · 6 aday bloker (Onur adjudikasyonunda).**
  Rapor: `KANIT/adr-0003/kapi-5-denetim-raporu.md` (19.827 bayt) — **tam gerekçe, alıntı ve ölçüm komutları orada.**
  Hafıza checkpoint'i: **K25** (bu notun hemen altında).
  ⚠ **BU OTURUM DENETÇİDİR; HİÇBİR ŞEYİ ONARMADI.** ADR'ye tek bayt dokunulmadı (sha256 `758bd1bf…22ef6` değişmedi).
  Yazılan iki dosya: `KANIT/adr-0003/kapi-5-denetim-raporu.md` (yeni) + `PROJE_HAFIZA.md` (K25 + bu not).
  **GİT (ölçüldü, `git --no-optional-locks`):** HEAD `dc2f6dc` · origin/main `56362ed` · `.git/index.lock` YOK.
  Ağaç: ` M PROJE_HAFIZA.md` · `?? KANIT/adr-0003/kapi-5-denetim-raporu.md` ⇒ **commit ONUR'DA.**
  **[K21 ÖLÇÜMÜ]** Kapanışta **353.195 token = %35,3 ⇒ 🟢 YEŞİL.** Eğri: 126k → 245k → 311k → 353k.
  ⇒ **Devir bağlam yüzünden DEĞİL, DOKTRİN gereği veriliyor:** sıradaki iş (aracın onarımı) bir YAZIM işidir
  ve bu oturum o kusurları BULAN denetçidir — üreten ≠ denetleyen.
KAPSAM OTORİTESİ: `docs/ODEV.md` — ÖNCE OKU. Sonra **K25 + K24 + K22 + K21**.
SONRAKİ İŞ (sırayla, HER BİRİ AYRI OTURUM):
  1. **ONUR ADJUDİKE EDER:** 6 aday bloker + `[çıpa+]` sayısı (16 mı 18 mi) + KS-3'ün gerçek tanımı.
  2. **ÖLÇÜM ARACININ ONARIMI — YAZIM OTURUMU.** B5-1…B5-4. Altın kümeye **4 yeni kontrol** zorunlu:
     rakamsız kanonik değer · aynı satırda hem `[KS-n]` atfı hem ham sayı · §3 düz metninde anılan kapısız karar ·
     mutant tablosuna gömülü kanonik kopya. **ONARAN OTURUM ONU DENETLEYEMEZ.**
  3. **v6 YAZIMI — AYRI OTURUM** (12 bloker + 7 majör + adjudike edilenler).
  4. **6. TUR BAĞIMSIZ KAPI — AYRI OTURUM.**
BİR SONRAKİ DENETÇİYE SÖYLENECEK ÜÇ ŞEY (kapı-5'in kendi sınırları):
  1. **Kapı-5 kodu koşmadı** — `src/` bugün auth içermiyor (`RandomNumberGenerator`/`AesGcm`/`HKDF`/`AddAuthentication`
     ⇒ 0 eşleşme, ölçüldü). 6 aday bloker **mantıksal çürütmedir**, koşum değil.
  2. **Kapı-5 aracı onarmadı**, yalnız kırdı. v6 aynı kör araçla ölçülürse `K1`/`K4` yeşili yine kanıt değildir.
  3. **Kapı-5 de bir üretimdir.** Raporun her satır numarası ve her komutu tekrarlanabilir; kapı-6 raporu da denetlemeli.
ONUR'DAN BEKLENEN: **commit** (sandbox atamaz) · 6 aday blokerin kararı ·
  **AÇIK İŞLER md.9 ve md.10'un düzeltilmesi** (md.10 hâlâ *"Ayrı ADR-0004 YAZILMAZ"* diyor — satır 298 bunu çürütüyor;
  md.9 hâlâ ADR 0003'ü *"TASLAK v1 / ikinci ve SON tur"* diye anlatıyor — kanonik v5, beş kapı turu koştu).
YÜRÜRLÜKTEKİ KURALLAR: **K21** · **K13-a** · **satır 380'in BEŞ PAZARLIKSIZ istisnası** (K6 tavanına tabi değildir:
  kapı mutantla ısırdığını kanıtlar · Cowork build'i bağımsız doğrular · mutantlar gerçekten koşulur ·
  her iddia kaynaktan ölçülür · kırmızı çizgiler) · **satır 253 taksonomi: ölü tuzak = MAJÖR, kör kapı = BLOKER** ·
  İKİ ADR AYNI OTURUMDA YAZILMAZ · DENETİM VE YAZIM AYRI OTURUM · PUSH YASAK ·
  alt-ajan beyanını DOĞRULAMADAN AKTARMA (bu oturumda **fiilen bir beyan reddedildi**) · sandbox'tan commit ETME ·
  K12-b · §4 kuralı: her olgu iddiası ya ölçülür ya `[DOĞRULANMADI]`.
AÇIK [DOĞRULANMADI] (2): `flutter_secure_storage`'ın Windows şifreleme yöntemi ·
  WebKit/Safari'nin `http://localhost` üzerinde `Secure`/`__Host-` davranışı.
ORTAM: Docker 29.6.1 + compose v5.3.0 · Backend 110/110 test · Flutter 3.44.6 · Mac YOK → iOS CI-only ·
  CI/CD kurulmadı · API `Dockerfile` YOK (K22-a ile bu dilimin işi).
TÜM ÇALIŞMA: C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum


## ⏭ DEVİR NOTU (25 Tem 2026 — oturum 21 kapanış) — ÖNCEKİ DEVİR NOTLARINI GEÇERSİZ KILAR

DURUM: **ADR 0003 v5 YAZILDI ve KANONİK YOLA ALINDI. 9/9 BLOKER + 3 YENİ BULGU + 15 MAJÖR KAPANDI.**
  `docs/ADR/0003-kimlik-cekirdegi.md` = **v5** · **212.485 bayt · 1.081 satır · UTF-8, BOM yok · U+FFFD 0
  · 13.123 Türkçe harf · sha256 758bd1bf…22ef6** (hepsi Onur'un diskinden ölçüldü).
  **v4 SİLİNMEDİ, arşive TAŞINDI:** `arsiv/0003-kimlik-cekirdegi-TASLAK-v4-2026-07-25.md`
  (170.261 bayt, sha256 `b85ce0b3…` = denetlenen sürümle **birebir aynı**). Arşivde artık **dört** taslak var.
  ⚠ **v5 DENETLENMEDİ.** Yapılan tek şey **öz-doğrulamadır** (kodlama · bayt/satır · numara bütünlüğü ·
  bayat referans taraması · ölçüm aracı koşumu). **BUNLAR KAPI DEĞİLDİR.**
  **✅ COMMIT ATILDI (Onur, 25 Tem 2026): `e29f60d`** — *docs(adr-0003): v5 - close all 9 blockers…*
  **15 dosya · +4.806 / −3 satır.** Commit anında ağaç **TEMİZDİ** (`git status` boş). ⚠ **Bu checkpoint satırının kendisi `PROJE_HAFIZA.md`'yi yeniden kirletti** — commit sonrası tek kirli dosya budur ve bilinçlidir (hafıza kuralı: checkpoint YERİNDE ve ANINDA yazılır). Bayat `.git/index.lock` (0 bayt,
  3 sa 24 dk) kaldırıldı. **🔻 GERİ ÇEKİLEN İDDİA:** Cowork önce *"sebebi ölçülmedi, tekrarlarsa
  Kaspersky istisnasına bak"* dedi. **YANLIŞTI ve kontrollü testle ölçüldü:** bağlı diskte
  **düz `git status` kilidi bırakıyor** (index'i tazeleyip yazmak istiyor, mount `unlink`'e izin
  vermiyor ⇒ `warning: unable to unlink '.git/index.lock': Operation not permitted`), buna karşılık
  **`git --no-optional-locks status` HİÇ kilit bırakmıyor**. ⇒ **Kilidi bu oturumun kendisi üretti.**
  **YENİ KURAL (CLAUDE.md'ye yazıldı): Cowork sandbox'tan ASLA düz `git status` koşmaz.**
  **İKİNCİ COMMIT** — *docs(claude,hafiza): forbid bare `git status` from the sandbox* (2 dosya).
  ⇒ **origin/main = 56362ed · 17 ileri · PUSH YOK.**
  ⚠ **Bu satır BİLİNÇLİ olarak kendi commit'inin sha'sını YAZMAZ.** *Gerekçe (bu oturumda fiilen yaşandı):*
  bir not, kendisini içeren commit'in sha'sını taşıyamaz — sha'yı yazıp commit'lersen sha değişir,
  düzeltirsen yine değişir; **sonsuz döngü**. `amend` bunu bir kez daha kanıtladı (`b211f8a` → `0810b8b`).
  **KURAL: kendine-atıflı olgu, atıf yaptığı artefaktın içinde saklanmaz.** Güncel sha: `git log -1`.
  **AĞAÇ TEMİZ** (`git --no-optional-locks status` boş) · `.git/index.lock` **yok** · `_to_delete/` silindi.
  Commit'ten çıkarılıp koşuldu: ADR sha256 `758bd1bf…` (birebir) · araç `--altin-kume` **çıkış kodu 0**.
  ⚠ AĞAÇ (commit Onur'da): M CLAUDE.md · M PROJE_HAFIZA.md · M docs/ODEV.md
    ?? docs/ADR/0003-kimlik-cekirdegi.md (v5) · ?? araclar/adr-kapi-taramasi.py
    ?? KANIT/adr-0003/ · ?? arsiv/…v1/v2/v3/**v4**… · ?? _to_delete/ (yalnız v5'in ara dosyası — SİLİNEBİLİR)
  ⚠ Sandbox `.git/index.lock`'a yazamıyor ⇒ Cowork'ten `git add`/`commit` YAPILAMAZ (status okunabilir).
KAPSAM OTORİTESİ: docs/ODEV.md — ÖNCE OKU (**§8/4 K22-a ile güncellendi**). Sonra **K24 + K23 + K22 + K21**.
SONRAKİ İŞ: **5. TUR BAĞIMSIZ KAPI — AYRI VE TEMİZ OTURUM (K13-a).**
  **DENETÇİYE AÇIKÇA SÖYLENECEK ÜÇ ŞEY:**
  1. **ÖLÇÜM ARACININ KENDİSİ DE DENETLENECEKTİR** (K19-c): aracı **v5'i yazan oturum yazdı** ⇒
     üreten ≠ denetleyen bu kalemde kısmen delindi. Araç yazılırken **kendi kör noktalarından DÖRDÜNÜ**
     üretip yakaladı; beşincisi olabilir. **Aracın yeşili tek başına kanıt DEĞİLDİR.**
  2. **Araç ETİKETİN VARLIĞINI ölçer, mutantın o kararı GERÇEKTEN ısırdığını ÖLÇEMEZ.** v5'te 18 mutantın
     çıpa sütununa `**[çıpa+]**` işaretiyle etiket eklendi; **her birinin gerekçesi satırın içinde yazılıdır**
     ve kapı-5 bunları **tek tek zorlamalıdır** — aksi hâlde `K1`=0 bir **kapı tiyatrosudur**.
  3. **`K4`'ün kapsam dışı bıraktığı 8 kanonik satır** aracın çıktısında listelidir ve **ELLE** kontrol edilmelidir.
  Ayrıca kapı-5 şunları **ölçmelidir**: `[KS-LITERAL: …]` işaretli ~14 satırın gerekçesi **gerçekten geçerli mi**;
  §3.1'e yeni yazılan **6 beyan satırının** gerekçesi ayakta mı; **M36b/M41b/M40b/M42b/M42c/M49–M53**'ün
  kill sinyalleri gerçekten **kendi mutasyonları altında ölüyor mu**.
KAPI-5'İN KOŞACAĞI KOMUTLAR:
  python3 araclar/adr-kapi-taramasi.py --altin-kume            # ÖNCE bu; çıkış kodu 0 değilse araca GÜVENME
  python3 araclar/adr-kapi-taramasi.py docs/ADR/0003-kimlik-cekirdegi.md
  (bugünkü sonuç: K1=0 · K2=0 · K3=0 · K4=0 · K5=0; tek çıktı aracın kendi kapsam beyanı)
KALAN BİLİNEN BORÇ [v5 içinde AÇIKÇA yazılı, gizlenmiyor]: kapı-4'ün ~27 majöründen kalanlar
  (başlıcaları **M19'un ayırt ediciliği** · **M42c'nin `KON` gözlem yüzeyinin uçtan uca yazımı**) ·
  ~28 minörün çoğu · **`GOREV-slice-3c-auth` spec'i hâlâ YOK** ⇒ *"spec'te çözülür"* savunması geçersiz.
ONUR'DAN BEKLENEN: **commit** (sandbox atamıyor) · `_to_delete/` klasörünü silmek (içinde yalnız v5'in ara dosyası var).
YÜRÜRLÜKTEKİ KURALLAR: **K21 (devir ÖLÇÜLMEDEN verilemez: 🟢<%55 · 🟡%55-75 · 🔴>%75)** · K13-a ·
  İKİ ADR AYNI OTURUMDA YAZILMAZ · DENETİM VE YAZIM AYRI OTURUM · PUSH YASAK ·
  alt-ajan beyanını DOĞRULAMADAN AKTARMA · sandbox'tan commit ETME · K12-b ·
  §4 kuralı: her olgu iddiası ya ölçülür ya [DOĞRULANMADI].
AÇIK [DOĞRULANMADI] (2): flutter_secure_storage'ın Windows şifreleme yöntemi · WebKit/Safari'nin
  `http://localhost` üzerinde `Secure`/`__Host-` davranışı. *(`+` kaçışı KAPANDI — Onur Windows'ta koştu:
  `{"detail":"a\u002Bb"}`, baytlar `92 117 48 48 50 66`.)*
ORTAM: Docker 29.6.1 + compose v5.3.0 · Backend 110/110 test, verify exit 0, CVE 0 · Flutter 3.44.6 ·
  Mac YOK → iOS CI-only · CI/CD kurulmadı · **API `Dockerfile` YOK — K22-a ile bu dilimin işi oldu.**
TÜM ÇALIŞMA: C:\Users\gulci\Desktop\MEMO ÖDEV PROGRAMLAR\TO DO LİST\Momentum

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21 kapanış: **K24 — ADR 0003 v5 YAZILDI, KANONİK YOLA ALINDI**)
## ⏭ CHECKPOINT (25 Tem 2026 — oturum 22: **K29 — v6 YAZIMI BAŞLADI; `K1`=0 · `K2`=0 · `K3`=0 · `K5`=0. BELGE YARIMDIR**)

- **[K21 ÖLÇÜMÜ]** Checkpoint anında **~%55 ⇒ 🟡 SARI.** Kural gereği **eldeki madde bitirildi, yeni büyük iş BAŞLATILMADI.**
- **[KARAR — Onur, 25 Tem 2026] K29-a: v6'YI KAPI-5'İ KOŞAN OTURUM YAZAR.** İki kural çatışıyordu: K26'nın bağlayıcı maddesi *"üretici kendi denetçisini spawn edemez"* (tersini yasaklamaz) ile yürürlük listesindeki eski *"denetim ve yazım ayrı oturum"*. **Onur K26'yı tutarlı uygulamayı seçti.** *Gerekçe:* 12 blokerin ve aracın 40 bulgusunun **bağlamı bu oturumdadır**; devir notu üzerinden yeniden kurmak kayıp üretir ve bir oturum daha harcar (15 Ağu baskısı). *Bedel [adlandırılmış]:* düzeltmeyi yapan el kusuru bulan eldir ⇒ **kapı-6 hem v6'yı hem ONARILMIŞ ARACI denetlemek ZORUNDADIR.**
- **📄 ÇALIŞMA DOSYASI: `docs/ADR/0003-kimlik-cekirdegi-v6-YAZIM-DEVAM-EDIYOR.md`** (219.175 bayt · 1.086 satır · UTF-8, BOM yok, U+FFFD 0). ⚠ **KANONİK ADR HÂLÂ v5'TİR** — `docs/ADR/0003-kimlik-cekirdegi.md`'ye **TEK BAYT DOKUNULMADI** (sha256 `758bd1bf…` değişmedi).
- **✅ YAZILANLAR (32 düzeltme + 5 yeni kalem, her biri "eskisi tam olarak bulundu" güvencesiyle uygulandı):**
  **K28 kilitleri:** `[KS-4]` **60 sn → 10 dk** (+ M29'un `61 sn` literali `[KS-4]`'ü aşan süreye çevrildi, Risk #13 `[KS-6]`'ya düzeltildi) · **`KS-3` ve `KS-29`** = *"ailenin KALAN `expires_at`'i, üst sınır KS-2"*, kapıları **M36b** · **`KS-31`** (blocklist boyutu 10.000) + **M56** (blocklist mutantı) · **M54** (salt tazeliği) + **M55** (HKDF alan ayrımı).
  **Bloker kapanışları:** **§6 Risk #17 YAZILDI** (satır 413'ün sarkan atfı artık gerçek bir kaleme gidiyor) · **M31 satırı 11 hücreden 7'ye indirildi** ⇒ v4'ün GFM'de render OLMAYAN dört ayaklı metni **silindi**, v5'in üç ayaklı hâli artık gerçekten görünüyor (**B-6 fiilen kapandı**) · **numara pini** 880 ve 1073'te **M50 → M60** · **KS-11/KS-12** sarkan *"M41 üçüncü/dördüncü ayak"* atıfları `*(kapısız — §3.1)*` yapıldı · **§2-J satır 594-595** `ProblemDetails` üretimini Api'ye verdi (M49'un mutasyonu artık karar metni DEĞİL) · **§3.2(4)** uç stili koddan ölçülen `MapGroup("/v{version:apiVersion}").WithApiVersionSet(...)` ile düzeltildi · **M42b'nin ayırt edici olmayan kill sinyali** ALTIN VEKTÖR testine çevrildi (`KON` → `B`) ve geri çekilen sinyal gerekçesiyle yazıldı.
  **Aday bloker kapanışları:** **M53** artık saati `[KS-4]`'ü aşacak kadar ilerletiyor (dal (c) mutantı kurtarıyordu = kör kapı) · **M40b'nin önkoşulu** zamana değil **mekanizmaya** çıpalandı.
  **Majörler:** *"13.451 bayt / %8"* → **11.112 bayt / %6,5** (ölçüldü) · satır 406'nın `[KS-LITERAL]` muafiyeti kaldırıldı, `**[KS-30]**` yazıldı · satır 409'un `32` literali düştü · **Isopoh** *"lisans belirsiz"* → **`[DOĞRULANMADI]`, eleme gerekçesi ASKIDA** · §3.1'in araç tarihçesi (*"üçünü"*) düzeltildi · §3.1'in bayat **M36 → M36b** ve **M31 → M52** atıfları düzeltildi.
  **Yapısal:** **M-L tablosuna GERÇEK ÇIPA SÜTUNU eklendi** (4 → 5 sütun); `[çıpa+]` etiketleri **`[devir]`** oldu · **§3.1'e K3-L5…K3-L9 beyanı KARAR KİMLİĞİYLE yazıldı** (v5 yalnız mutant kimliğiyle yazdığı için araç beşini de kapısız görüyordu) · **M43'ün çıpasına K3-L10**, **M44'ün çıpasına K3-C6(2)** eklendi.
- **✅ ARAÇ DA GÜNCELLENDİ (aynı oturum):** `[devir]` işaretli çıpa **kapı sayılmaz** (aksi hâlde beyanla sahte çelişki doğar) + *"devredilmiş ama beyansız"* ihbarı + `K6`'nın çelişki mesajı **dürüstleştirildi** (*"ÖZELLİK DÜZEYİ BELİRSİZLİĞİ — araç KARAR-ID düzeyindedir, ayırt edemez, ELLE DOĞRULA"*). Altın küme **21 → 22 kontrol**, çıkış kodu **0**.
- **📊 ARACIN v6 ÜZERİNDEKİ KOŞUMU: `K1`=0 · `K2`=0 · `K3`=0 · `K5`=0 · `K6`=3 · `K4`=27 (toplam 30; v5'te 40 idi).** `kapili` 33 · `beyanli` 14 · `devredilmis` 4 · `capasiz_tablo` **0**.
- **🔴 BELGE YARIMDIR — KALAN İŞ (v6'nın bir sonraki oturumu):**
  1. **§2-C satır ~330'un OLGUSAL OLARAK YANLIŞ cümlesi HÂLÂ DURUYOR:** *"pencere reuse-detection'ı `[KS-4]` geciktirir"* — doğrusu **`[KS-1]`**, çevrimdışı kurbanda **`[KS-2]`**. K14-a'nın maliyet muhasebesi bu cümleye dayanıyor; **geri çekilip yeniden yazılmalı** (K28-a'nın yazım borcu).
  2. **Karar METİNLERİ yazılmadı** (yalnız mutantlar yazıldı): K3-B1'e **salt tazeliği** kararı · K3-I4'e **`HkdfLabels` pini** · K3-B6'ya **blocklist maddesi** (`[KS-31]`, gömülü liste, lisans kapısı).
  3. **`K4`'ün 27 bulgusunun kalem kalem adjudikasyonu** (çoğu ZAYIF sınıf ya da `[KS-LITERAL]` muafiyetli — hepsi *"ELLE DOĞRULA"* etiketiyle listeli).
  4. **`[çıpa+]` 16 ≠ 18** kaynaktan çözülmeli (hangi iki kararın etiketi düşmüş).
  5. **Kapı-5 majör M5-7:** `Microsoft.Extensions.TimeProvider.Testing` `Momentum.Api.Tests`'te YOK — §3.2(6)'ya *"paket bu projeye EKLENECEKTİR, aynı sürüm, yeni paket değil"* yazılmalı.
  6. Kapı-5'in **kalan 7 majörü ve ~28 minörü** + kapı-4'ten devreden borç.
- **Sıradaki ilk iş:** **v6 yazımının devamı** (yukarıdaki 6 kalem) → **v6 kanonik yola alınır, v5 arşive** → **6. TUR BAĞIMSIZ KAPI, AYRI OTURUM: hem v6'yı hem ONARILMIŞ ARACI denetler.**


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 22: **K28 — KAPI-5'İN DÖRT ADAY BLOKERİ KİLİTLENDİ**)

- **Bağlam:** kapı-5'in 6 aday blokerinden **ikisi karar değil düzeltmedir** (M53'ün saati `[KS-4]`'ü aşacak kadar ilerletilir · M40b'nin önkoşulu zamana değil **mekanizmaya** çıpalanır: *"süpürücü fixture'a kaydedilmez"*) ⇒ v6'nın yazım işi, Onur kararı gerekmez. Kalan dördü Onur tarafından kilitlendi.
- **[KARAR — Onur, 25 Tem 2026] K28-a: `[KS-4]` REPLAY PENCERESİ 60 sn → 10 dk.** *Gerekçe:* K14-a'nın **kendi adlandırdığı** kırılma senaryoları (uçak modu · hücresel el değiştirme · TCP reset · Android Doze/process kill) **dakika ölçeğindedir**; 60 sn'lik pencere bunların hiçbirini kapatmıyordu ⇒ yanıtı kaybolan **meşru kullanıcı dal (d)'ye düşüp HIRSIZ ilan ediliyor ve ailesi iptal ediliyordu**. Değerlendirici demo sırasında uçak moduna alıp döndüğünde oturumun düşmemesi **ODEV §2'nin "kesinlikle çalışan uygulama" ölçütüne doğrudan hizmet eder**. *Bedel [adlandırılmış]:* `successor_secret_enc` daha uzun yaşar ⇒ **§6 Risk #13'ün penceresi büyür** — ama dal (c) **yeni token basmadığı** için bedel sınırlıdır. *v6'nın yazması gerekenler:* KS-4 = 10 dk · **M29'un kill sinyali** (`61 sn` literali düşer, `[KS-4]`'ü aşan süre yazılır) · Risk #13'ün aritmetiği (`[KS-4]`+`[KS-6]`) · §2-C satır 330'un **olgusal olarak yanlış** tespit-gecikmesi cümlesi (gecikme `[KS-4]` değil `[KS-1]`, çevrimdışı kurbanda `[KS-2]`'ye uzar) **geri çekilir ve düzeltilir**. *Reddedilenler [adlandırılmış]:* **60 sn kalsın + sınırı adlandır** (en ucuz, ama demo sırasında gerçekten olabilecek bir senaryoyu *"kabul edilmiş bedel"* diye geçiştirir; denetçi bunu *"kullanıcıyı hırsız ilan eden sistem"* diye okur) · **zarafet penceresi geri gelsin** (sorunu kökten çözer ama K3-C5'in ADLANDIRILARAK REDDEDİLMİŞ kararını geri açar ve M27/M29'u yeniden tasarlamayı gerektirir).
- **[KARAR — Onur, 25 Tem 2026] K28-b: `KS-3` = "ailenin KALAN `expires_at`'i, tavanı `[KS-2]`".** Belge kendisiyle çelişiyordu: kanonik tablo *"KS-2 ile AYNI"* (sabit 30 gün), gövde ve **M36b'nin assert'i** *"ailenin kalan süresi (±2 sn)"* diyordu ⇒ ilk `/refresh`'ten sonra ikisi **asla eşit değildir** ve `[KS-3]`'ü harfiyen uygulayan bir builder'ın **baseline'ı kırmızı doğardı**. *Gerekçe:* düzeltme **tek satırlıktır ve kapı değişmez** (M36b zaten bunu ölçüyor); güvenlik olarak da doğrusudur — çerez **aileyle birlikte ölür**, aileden uzun yaşamaz. **`KS-29` (CSRF çerezi) da aynı biçimde yeniden yazılır.** *Reddedilen:* sabit 30 gün (daha basit kural ama çerez ailesi ölünce de tarayıcıda yaşar ve M36b'nin kill sinyali yeniden yazılmak zorunda kalır).
- **[KARAR — Onur, 25 Tem 2026] K28-c: SIZMIŞ-PAROLA KARA LİSTESİ YAPILIR — GÖMÜLÜ TOP-10K + MUTANT.** Belge 15 karakteri *"NIST SP 800-63B-4 `SHALL`"* diye gerekçelendirirken **aynı standardın ikinci `SHALL`'ını (yaygın/sızmış parola blocklist'i) ne karara bağlamış ne kapsam dışı ilan etmişti** ⇒ **adlandırılmamış sapma** (`grep "blocklist|pwned|sızdırılmış"` ⇒ 0). *Karar:* en yaygın **10.000 parola repoya gömülür** (~100 KB), kayıt ve parola değiştirmede kontrol edilir. *Gerekçe:* **ağ yok, dış bağımlılık yok, çevrimdışı vitrinle çelişmez, DETERMİNİSTİKTİR ⇒ mutantla ısırtılabilir** (projenin kapı doktrini korunur); `123456` artık kabul edilmez. *v6'nın yazması gerekenler:* K3-B6'ya blocklist maddesi · **yeni mutant** (rezerv aralıktan) · liste dosyasının lisansı **kırmızı çizgi #3 kapısından** geçirilir. *Reddedilenler [adlandırılmış]:* **kapsam dışı + adlandırılmış sapma** (sıfır iş, emsali var — hesap kilitleme SHALL'ı böyle yazılmıştı — ama *"15 karakteri NIST diye savunup öbür SHALL'ı atlamak"* eleştirisi açık kalırdı) · **HIBP k-anonimlik API'si** (en güçlü koruma, ama **DIŞ AĞ BAĞIMLILIĞI** = ODEV §4.1'in AI asistanı ve Google Takvim'i elediği risk sınıfının AYNISI; deterministik olmadığı için **mutantla ısırtılamaz**).
- **[KARAR — Onur, 25 Tem 2026] K28-d: İKİ KAPISIZ KRİPTO ÖZELLİĞİNE DE MUTANT YAZILIR.** **(a) Parola salt tazeliği:** salt'ın her parolada TAZE ve rastgele olduğu belgede hiçbir yerde ölçülmüyordu (`grep "salt"` ⇒ yalnız 140·183·224·225, §3'te **sıfır**) ⇒ **sabit salt kullanan bir implementasyon M5·M6·M6b·M7·M31·M32·M34'ün HEPSİNİ geçer** ve sonuç gökkuşağı tablosuna açık bir parola deposudur. **(b) HKDF `info` etiketlerinin çakışmazlığı:** kopyala-yapıştırla `K_csrf`'in etiketi `K_jwt`'ninkine eşitlenirse **alan ayrımı tümüyle çöker** ama M42·M42b·M42c·M25·M35 **beşi de yeşil kalır**. *Karar:* **M5c** (*"aynı parola iki kez hash'lenince FARKLI PHC dizesi üretilir ve salt alanı `[KS-23]` uzunluğundadır"*) + **M42d** (*"`K_jwt`, `K_csrf`, `K_rt` ikişer ikişer FARKLIDIR"*), ikisi de **`B` seviyesi** (saf birim testi, DB gerektirmez, kırılgan değil), numaralar **rezerv M54–M59'dan**. *Gerekçe:* belge **AES nonce'u için AYNI özelliği M50 ile zaten kapılamış** ⇒ emsal kendi içinde var; ikisi de **kırmızı çizgi sınıfı** (parola deposu + anahtar ayrımı). *Reddedilenler:* ikisini de §3.1'e beyan etmek (sıfır iş, ama *"en kritik iki özellik kapısız"* eleştirisi doğar) · salt'a kapı / HKDF'ye beyan (orta yol, ama HKDF çakışması **daha sinsi** olduğu için tam da kapı isteyen kalemdir).
- **Sıradaki ilk iş:** **v6 YAZIMI — AYRI OTURUM** (12 bloker + 7 majör + K28'in dört kilidi + aracın 40 bulgusunun adjudikasyonu + `[çıpa+]` 16≠18'in kaynaktan çözülmesi). Sonra **6. TUR BAĞIMSIZ KAPI — AYRI OTURUM; ONARILMIŞ ARACI DA DENETLEYECEK.**


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 22: **K27 — ÖLÇÜM ARACI ONARILDI, ALTIN KÜME 21 KONTROLE ÇIKTI, FARKLI MODELLE SALDIRILDI**)

- **[K21 ÖLÇÜMÜ]** Onarım sonunda canlı bağlam **481.392 token = %48,1 ⇒ 🟢 YEŞİL.**
- **✅ ARAÇ ONARILDI** (`araclar/adr-kapi-taramasi.py`, K26-a). **Onaran = kapı-5'i koşan oturum**, v5'i yazan oturum DEĞİL ⇒ K26(1) ihlal edilmedi. Ham kanıt: **`KANIT/adr-0003/olcum-araci-onarim-kanit.txt`** (16.596 bayt, dört koşum).
- **✅ KAPI-5'İN DÖRT BULGUSU + D9 KAPANDI, HER BİRİ MUTASYON TESTİYLE KANITLANDI (ESKİ araçta KAÇIRILIYOR, YENİ araçta YAKALANIYOR — 9/9 vaka GERÇEK):** **B5-1** rakamsız kanonik değer artık sözlüğe girer + `kanonik_satir == taranan + atlanan` **MUHAFIZI** eklendi (tutmazsa araç kendini ihbar eder) · **B5-2** `[KS-` toptan muafiyeti kaldırıldı, muafiyet yalnız `[KS-LITERAL:`, `[KS-n]` jetonları **maskelenir** · **B5-3** `K1` artık **YALNIZ ÇIPA SÜTUNUNDAN** çözülür + çıpa sütunu olmayan tablo **K6** ile ihbar edilir · **B5-4** `K4` artık mutant tablosunu ve §3.1'i de tarar + **`[KS-n]±1` SINIR DEĞER** literalleri raporlanır · **D9** §3.1'in *"çıkarılan muafiyet"* tablosu artık **beyan sayılmaz** (yalnız *"kapısız kalan"* tablosu beyandır).
- **✅ FARKLI MODEL SALDIRISI KOŞTU (K26-a 3. şart, model = Sonnet; Fable 5 Onur kararıyla KULLANILMADI).** *"Bu aracı kandır"* görevi. **Yedi açık buldu, hepsi koşularak kanıtlandı.** **Beşi onarıldı ve altın kümeye regresyon kontrolü olarak eklendi:** görünmez karakter (`U+200B` ile bölünmüş `1<ZWSP>5 dk` sessizce kaçıyordu ⇒ NFKC + temizlik + **K7** ihbarı) · **anahtarı boş kanonik satır** (hem sayaçtan hem sözlükten düşüyordu ⇒ **B5-1 muhafızı SİMETRİK OLARAK KÖRDÜ**) · **`"ÇIPA".lower()` = `çipa`** (Türkçe noktasız ı ⇒ başlığı büyük yazan TEMİZ belge sahte K1/K6 seli üretiyordu; `_kucult` eklendi) · **bozuk UTF-8** çıplak traceback + çıkış kodu **tesadüfen 1** (CI *"bulgu var"* sanabilirdi ⇒ **çıkış kodu 3**) · **`[KS-LITERAL:]`** muafiyeti sessiz kaçıştı ⇒ muafiyet **korunur ama ADIYLA raporlanır**. **İkisi ADLANDIRILMIŞ SINIR olarak duruyor:** çıpa hücresindeki jeton o mutantın kararı gerçekten test ettiğini kanıtlamaz (aracın **en büyük tek sınırı**) · `K5`'in rezerv affı satır bazlıdır.
- **⚠ DÜRÜSTLÜK — ONARIM KENDİ KUSURUNU DOĞURDU:** `"ÇIPA"` yanlış-pozitifi **eski araçta YOKTU** (eski araç sütun başlıklarını hiç okumuyordu) ⇒ kusuru **onarımın kendisi** üretti ve saldırı yakaladı. Mutasyon tablosunda kıyaslanamaz; kanıt dosyasında **ayrı bölümde** doğrudan ölçümle yazıldı. Ayrıca onarım sırasında **raporlayıcıda sessiz daraltma** üretildi (`K6` bulguları üretiliyor ama sabit kod listesi yüzünden basılmıyordu) ⇒ rapor kodları artık **üretilen bulgulardan türetiliyor** ve bunun da kontrolü var ([11]).
- **✅ ALTIN KÜME 5 → 21 KONTROL, çıkış kodu 0.** Yeni kontroller: [6] rakamsız değer · [7] atıf+kopya · [8] düz metinde anma · [9] çıpasız tablo · [10] mutantta gömülü kopya · [11] raporlayıcı tamlığı · [12] muafiyet tablosu beyan değil · [13] ZWSP · [14] boş anahtar · [15] büyük harf ÇIPA · [16] `[KS-LITERAL]`.
- **📊 ONARILMIŞ ARAÇ, v5 ÜZERİNDE: 40 bulgu (eski araç: 1).** `K1`=6 · `K4`=31 · `K6`=3. **Kapı-5'in ELLE bulduğu üç bulguyu MEKANİK olarak yeniden üretti:** `K3-L5…L8` kapısız-ve-beyansız · `K3-L10` (D9'un bulgusu) · `K3-B4` ve `K3-C4` **hem kapılı hem kapısız-beyanlı** ÇELİŞKİSİ · M-L tablosunun **çıpa sütunu olmadığı**. ⇒ **araç artık kapı-5'in elle bulduğunu makineyle buluyor.**
- **⚠ K4'ün 31 bulgusu TİYERLİ:** GÜÇLÜ (sayı+birim eşleşmesi) ayrı, **ZAYIF (çıplak sayı)** ayrı, **`[KS-LITERAL]` muafiyetiyle geçenler** ayrı, **kapsam dışı** ayrı raporlanıyor — *sessiz eleme yok, hepsi "ELLE DOĞRULA" etiketiyle listeli*. Tiersiz ilk koşum **87 bulgu** üretti ve gerçek bulguyu gömüyordu (ör. satır 921 *"47 karar + 10 alt madde"*ndeki `10`).
- **Sıradaki ilk iş:** (1) Onur **6 aday blokeri** + `[çıpa+]` sayısını (16≠18) + **KS-3'ün gerçek tanımını** karara bağlasın. (2) **v6 YAZIMI — AYRI OTURUM** (12 bloker + 7 majör + aracın 40 bulgusunun adjudikasyonu). (3) **6. TUR BAĞIMSIZ KAPI — AYRI OTURUM; ONARILMIŞ ARACI DA DENETLEYECEK** (onaran el denetleyemez).


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 22: **K26 — ÜRETEN ≠ DENETLEYEN YENİDEN TANIMLANDI: OTURUM SINIRI DEĞİL, KANAL KURALI**)

- **[KARAR — Onur, 25 Tem 2026] K26: BAĞIMSIZLIK BİR OTURUM SINIRI DEĞİL, DÖRT KANALIN ÖZELLİĞİDİR. [PAZARLIKSIZ]**
- **Tetikleyen soru (Onur):** *"bu başka oturum işi gerçekten bir fark yaratıyor mu? Yaratmıyorsa işi bağımsız bir ajana verebiliriz."* **Ölçülmüş cevap:** okuma işi için alt ajanın taze bağlamı ile yeni oturumun taze bağlamı **teknik olarak aynıdır** (aynı model, aynı sıfır-bağlam). Fark yalnız iki kanaldadır.
- **DÖRT KANAL:** (1) **BAĞLAM** — denetçi üreticinin gerekçesini taşıyor mu? *(alt ajan ✅ · ayrı oturum ✅)* · (2) **GÖREV ÇERÇEVESİ** — denetçinin ne okuyacağını kim seçiyor? *(alt ajan ❌ üretici seçer · ayrı oturum 🟡 devir notu yine üretici yazar)* · (3) **RAPORLAMA** — bulguyu kim filtreliyor? *(alt ajan ❌ · ayrı oturum ✅)* · (4) **MODEL KÖR NOKTASI** — aynı ağırlıklar, aynı sistematik körlük *(ikisi de ❌)*.
- **YÜRÜRLÜKTEKİ BEŞ MADDE:** **(1) Üretici oturum kendi çıktısının denetçisini SPAWN EDEMEZ [PAZARLIKSIZ].** (2) Üretici **olmayan** bir oturum denetimi alt ajanlara dağıtabilir — bugünkü kapı-5 böyle koştu. (3) Devir notu kapsamı **daraltamaz**; *"şuraya bak"* diyebilir, *"yalnız şuraya bak"* diyemez — denetçi kapsamı **genişletmekle yükümlüdür**. (4) Alt ajan çıktısı **ham olarak** KANIT'a yazılır (filtreleyen el, filtrelediğini gizleyemesin). (5) **4. kanal oturumla ÇÖZÜLMEZ:** telafisi mekanik oracle + *"bunu ÇÜRÜT"* çerçevesi + mümkünse farklı model.
- **⚠ ÖLÇÜLMÜŞ KAYIP — DOKTRİNİN GÜCÜ OTURUMDAN DEĞİL, ALETTEN GELİYORDU:** *Code vs Cowork* ayrımının gerçek kaynağı **farklı gözlem yüzeyiydi** (Code derleyiciyi görür, Cowork dosya sistemini/APK'yı söker) — slice-2b1'de denetçinin üreticinin atladığı kapıyı yakalaması bu yüzdendi. **ADR fazında ikinci alet YOKTUR** (kod yok, derleyici yok, koşulacak test yok) ⇒ doktrin oturum sınırına indirgendi. **Ölçüm aracı tam olarak bu ikinci aleti imal etme girişimidir** — ve tek ikinci aletimizin üreticinin elinden çıkması (K19-c) bu yüzden bu kadar zarar verdi.
- **[KARAR — Onur, 25 Tem 2026] K26-a: ARACIN ONARIMI BU OTURUMDA YAPILIR.** Gerekçe: bu oturum aracı **yazmadı, kırdı** ⇒ K26(1) ihlal edilmiyor. **Onur'un şartı:** farklı model kullanılabilir ama **Fable 5 DEĞİL** (haftalık limit). **Cowork önerisi ve kabul:** **Sonnet** — maliyeti düşük; *dürüst kayıt: Sonnet ile Opus aynı aileden, kör nokta korelasyonu azalır ama sıfırlanmaz ⇒ ek örneklemdir, garanti değil.*
- **ONARIMIN ÜÇ PAZARLIKSIZ ŞARTI:** (1) her yeni altın-küme kontrolü **onarımdan ÖNCE KIRMIZI / SONRA YEŞİL** olduğunu **koşarak** kanıtlar, **ham çıktı** KANIT'a yazılır (KANIT kuralı v2.1) · (2) onarılmış araç **v5 üzerinde yeniden koşulur** ve **her yeni bulgu kaynaktan elle doğrulanır** — aracın yeni yeşili de tek başına kanıt DEĞİLDİR · (3) **kırma denemesini FARKLI MODEL yapar:** *"bu aracı KANDIR — yeşil geçen ama gerçekte kusurlu bir belge üret"* (okuma denetimi değil, **saldırı**).
- **⚠ SONUÇ (bilinçli bedel):** aracı onaran el onu **denetleyemez** ⇒ **6. tur bağımsız kapı GERÇEKTEN ayrı oturumdur** ve onarılmış aracı da denetlemek zorundadır.
- **Gerekçe (K26'nın kendisi için):** eski kural *"denetim ve yazım ayrı oturum"* ADR 0003'te **5 yazım + 5 kapı = 10 oturuma** çıktı ve 15 Ağu hedefi baskı altında; kanal kuralı oturum sayısını düşürürken 1. ve 3. kanalı **aynen** korur. *Reddedilenler [adlandırılmış]:* **mevcut kuralı aynen sürdürmek** (basit ve ihlali görünür, ama takvim maliyeti ölçüldü ve koruma artışı yok — alt ajanın taze bağlamı zaten aynı) · **farklı model zorunluluğunu her kapıya yaymak** (4. kanalı adresler ama yanlış-pozitif seli üretir ve her bulgu yine kaynaktan doğrulanmak zorundadır ⇒ maliyet korumadan hızlı büyür; **yalnız SALDIRI rolünde** kullanılıyor).


## ⏭ CHECKPOINT (25 Tem 2026 — oturum 22: **K25 — 5. TUR BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ**)

- **[K21 ÖLÇÜMÜ — kendi oturum-id'siyle]** Açılış **126.265 token = %12,6 🟢** · Tur A sonrası **245.037 = %24,5 🟢** · Tur B/rapor anında **~%31 🟢**. Transcript kimliği üç sondayla doğrulandı. **Oturum bağlam yüzünden değil, iş bittiği için kapanmıyor.**
- **✅ KAPI-5 KOŞTU (oturum 22, temiz oturum — v5'i yazan oturum 21 değil).** Mimari: belge **10 bağımsız bölüm denetçisine** bölündü (§0+§1-K · §2-A/B · §2-C · §2-D/§2-I · §2-J · §2-K/§2-L · §2-M · §3 · §3.1+§3.2 · §4-§7), **11. ajan** hafıza tarihçesini (141-565) taradı. Hiçbir ajan diğerinin bulgusunu görmedi. **Her bulgu rapora girmeden önce Cowork tarafından KAYNAKTAN yeniden ölçüldü (K13-a).**
- **🔴 HÜKÜM: ADR 0003 v5 KİLİTLENEMEZ.** **12 ölçülmüş bloker · 7 majör · 6 aday bloker (Onur adjudikasyonuna).** Rapor: **`KANIT/adr-0003/kapi-5-denetim-raporu.md`**.
- **✅ BELGE KİMLİĞİ BİREBİR DOĞRULANDI:** sha256 `758bd1bf…22ef6` · 212.485 bayt · BOM yok · `U+FFFD` 0 · 13.123 Türkçe harf. *(Tek sapma: `wc -l` = **1.080**, hafıza 1.081 diyordu — satır sayım kuralı farkı, içerik aynı.)*
- **🔴 ARACIN BEŞİNCİ KÖR NOKTASI VAR (K19-c'nin aradığı şey):** `kanonik_satir: 30` vs `taranan 21 + atlanan 8 = 29` ⇒ **KS-14 (`Environment.ProcessorCount`) ne taranıyor ne kapsam-dışı bildiriliyor** — aracın 4. kör noktasıyla (sessiz daraltma) **aynı sınıf**, ve o sınıfın altın kümede **regresyon kontrolü yok**. ⇒ *"ELLE kontrol edilecek 8 satır"* aslında **9**'dur. Ayrıca üç kör nokta daha ölçüldü: **`K4`'ün `[KS-` toptan muafiyeti satır bazlıdır** (satır 409 ve 1037 gibi *"aynı satırda hem atıf hem kopya"* vakaları yapısal olarak görülmez; kaynaktaki `[KS-LITERAL:` koşulu **ölü**) · **`K1` çıpa sütununu ayrıştırmıyor**, §3'ün tüm metninde alt dize arıyor ⇒ *"36 kapılı"* bir ölçüm değil (gerçek dağılım **31 kapılı · 11 beyanlı · 5 devredilmiş**; §3.1 satır 941 `K3-L5…L9` için *"kapısızdır"* derken araç beşini de kapılı sayıyor) · **`K4` mutant tablosunu ve §3.1'i hiç taramıyor**, oysa §1-K kuralı o iki bölgeyi **açıkça** bağlıyor (canlı kopyalar: M23 *"31."*, M27 *"31 gün"*, M29 *"61 sn"*, M31 *"14/15/129"*).
- **🔴 BELGEDEKİ SEKİZ BLOKER (hepsi kaynaktan ölçüldü):** satır **413** var olmayan **§6 Risk #17**'ye *"beyan edilmiş sınır"* diyor (§6 1-16'dır) · satır **1047** süpürme periyodunu **`[KS-4]`** diye etiketliyor (doğrusu `[KS-6]`) ve satır **339** bu yanlışı *"birebir böyle beyan edilir"* diye alıntılayıp **kendi cümlesiyle çelişiyor** · satır **845** M31 satırı **11 hücre** (başlık 7) ⇒ **GFM'de v5'in düzeltmesi RENDER OLMUYOR**, ekranda v4'ün B-6'lı dört ayaklı hâli duruyor ⇒ *"B-6 kapandı"* beyanının operatif karşılığı yok · satır **880 + 1073** hâlâ *"0004 `M50`'den başlar"* diyor, satır 800 ise `M60` (M50-M53 bu belgede **dolu**) ⇒ **0004 çakışacak** · satır **113-114** KS-11/KS-12 *"M41 üçüncü/dördüncü ayak"*a çıpalı ama M41 kendi satırında **"TEK AYAK"** diyor ⇒ iki tavan kapısız+beyansız · satır **595 ⟷ 760** ProblemDetails'i kim üretiyor çelişkisi (595 fiilen **M49'un mutasyonudur** ⇒ baseline kırmızı doğar) · satır **958** §3.2(4) `MapGroup("/v1")` pinliyor, repodaki dört uç dosyası **`/v{version:apiVersion}`** kullanıyor (ölçüldü) · satır **872** M42b'nin kill sinyali **ayırt edici değil** (statik `info` mutasyonu restart'tan bağımsızdır; iki-derlemeli koşum tarif edilmemiş) ⇒ **K3-I4'ün HKDF türetme ayağı bugün yaşayan sinyalle korunmuyor**.
- **🟠 YEDİ MAJÖR:** *"13.451 bayt / ≈%8"* ölçümü **yanlış** (gerçek **11.112 bayt = %6,53**, v4'ün 24-70. satırları — Onur'un diskinden ölçüldü) · satır **406**'nın `[KS-LITERAL: tablo bütünlüğü]` gerekçesi **komşularıyla çürüyor** (405 ve 407 `[KS-30]` yazıyor) · satır **1012** *"Isopoh (lisans belirsiz)"* — bir alternatifi eleyen tek gerekçe, ne ölçüm ne `[DOĞRULANMADI]` · **`[çıpa+]` sayısı 16, beyan 18** (17 geçişin biri satır 927'deki açıklama cümlesi) · satır **929** *"kör noktalarından **üçünü**"* derken hafıza **dört** diyor, ve üç altın-küme kontrolünün üçü de `K4`'e ait — **`K1`'in tek bir kör-nokta testi yok** · satır **909** K3-L10 çelişkisi (§3.1 *"M43'ün üçüncü ayağı"* diyor, M43'ün çıpasında yok) · satır **961** *"paket zaten repodadır"* — `TimeProvider.Testing` **yalnız** `Persistence.Tests`'te, `TS` testlerinin yaşayacağı `Api.Tests`'te **YOK** (ölçüldü).
- **⚖ ALTI ADAY BLOKER — ONUR ADJUDİKE EDECEK** (mantıksal çürütme; kod olmadığı için koşulamadı): çalınmış token'ın tespit gecikmesi `[KS-4]` değil **`[KS-1]`**, çevrimdışı kurbanda **`[KS-2]`**'ye uzar (K14-a'nın maliyet muhasebesi buna dayanıyor) · `[KS-4]`=60 sn K14-a'nın **kendi adlandırdığı** senaryoları (uçak modu, Doze, TCP reset) kapatmıyor ⇒ meşru kullanıcı hırsız ilan edilir, **M29 bunu mühürlüyor** · **M53 kendi mutasyonu altında ölmüyor** (dal (c) `[KS-4]` içinde `200` döndürür) · **M40b'nin önkoşulu kill sinyaliyle çelişiyor** (saat ilerletilmezse pencere hiç aşılmaz) · **parola salt tazeliği** hiç karara bağlanmamış ve kapısız (AES nonce'u için aynı özellik M50 ile kapılı) · **HKDF `info` etiketlerinin çakışmazlığı** kapısız+beyansız. **Ayrıca:** NIST'in **sızmış-parola blocklist `SHALL`**'ı ne karara bağlanmış ne kapsam dışı ⇒ adlandırılmamış sapma.
- **🚫 REDDEDİLEN ALT-AJAN BEYANI [K13-a fiilen iş gördü]:** bir ajan *"`KANIT/adr-0003/olcum-araci-altin-kume-kanit.txt` repoda YOKTUR"* dedi. **YANLIŞ** — dosya Onur'un diskinde var (klasörde altı dosya). Ajan, denetim kopyasına o klasör alınmadığı için **yokluktan yokluk** çıkarmış. Aktarılmadı. *(İki bulgu da düzeltilerek aktarıldı: M31 "9 hücre" → **11**; M42b "polarite ters" → **"ayırt edici değil"**.)*
- **📌 HAFIZA TARİHÇESİ TARANDI (141-565, ayrı ajan; satır numaraları Cowork tarafından teyit edildi).** Üst devirde geçmeyen ama **yürürlükte** olan başlıcalar: **satır 380 — K6 tavanının BEŞ PAZARLIKSIZ istisnası** (kapı mutantla ısırdığını kanıtlar · Cowork build'i bağımsız doğrular · mutantlar gerçekten koşulur · her iddia kaynaktan ölçülür · kırmızı çizgiler) — **kapı-5'in aracın yeşiline yaslanmasını yasaklayan asıl madde budur** · **satır 253 taksonomi:** ölü tuzak = MAJÖR, **kör kapı = BLOKER** · K8-a (auth kapsamı = tam aktivasyon) · K11-a/b (hub kimliği, `outbox_messages.owner_id`) · K14-c (taşıma kanalı, PAZARLIKSIZ) · K14-d (`security_stamp` ölü alan) · K12-d (web build API ile **aynı origin**) · K11-g (`momentum_{userId}.sqlite`) · K2-I1 (tek resolver) · KANIT kuralı v2.1 (ham çıktı + `DOTNET_CLI_UI_LANGUAGE=en`) · D5-a dondurulmuş sapma.
- **⚠ HAFIZA ÇELİŞKİLERİ (teyit edildi):** **AÇIK İŞLER md.10 (satır 626)** hâlâ *"Ayrı ADR-0004 YAZILMAZ (K10-d)"* diyor; **satır 298** ise *"ADR 0004 ARTIK VAR ve konusu SENKRON-YETKİ AKTİVASYONU'dur; K10-d frontend tasarım ADR'sini kastediyordu"* diyor ve üst devir *"AYRI OTURUM: ADR 0004"* diyor ⇒ **md.10 düzeltilmeli.** **AÇIK İŞLER md.9 (satır 625)** hâlâ ADR 0003'ü *"TASLAK v1 … ikinci ve SON denetim turu (K6 tavanı)"* diye anlatıyor ⇒ **bayat** (kanonik = v5, beş kapı turu koştu, K6 tavanı K13-a ile kaldırıldı).
- **Sıradaki ilk iş:** (1) Onur **6 aday blokeri** ve **`[çıpa+]` 16≠18** sayısını karara bağlasın. (2) **ÖLÇÜM ARACININ ONARIMI — AYRI YAZIM OTURUMU** (B5-1…B5-4 + altın kümeye 4 yeni kontrol: rakamsız kanonik değer · aynı satırda atıf+kopya · §3 metninde anılan kapısız karar · mutant tablosuna gömülü kopya); **onaran oturum denetleyemez.** (3) **v6 yazımı — AYRI OTURUM.** (4) **6. tur bağımsız kapı — AYRI OTURUM.**



- **[K21 ÖLÇÜMÜ — kural gereği] Canlı bağlam ~%43 ⇒ 🟢 YEŞİL.** Oturum **bağlam yüzünden değil, İŞ BİTTİĞİ için** kapanıyor: sıradaki adım (5. kapı) **tanım gereği** ayrı ve temiz bir oturumdur (üreten ≠ denetleyen).
- **✅ v5 KANONİK YOLDA:** `docs/ADR/0003-kimlik-cekirdegi.md` · **212.485 bayt · 1.081 satır · sha256 `758bd1bf…22ef6`** · UTF-8 geçerli, BOM yok, **U+FFFD 0**, 13.123 Türkçe harf. **v4 arşivlendi** (`arsiv/…TASLAK-v4-2026-07-25.md`, sha256 `b85ce0b3…` = denetlenen sürümle birebir).
- **⚠ DÜRÜST BÜYÜME ÖLÇÜMÜ:** v1 24,7 K · v2 43,9 K · v3 96,0 K · v4 170,3 K · **v5 212,5 K (v4'ün ×1,25'i)**. **Büyüme yavaşladı ama durmadı** — ve bu, §0.1–0.3'ün (13,5 K) **çıkarılmasına RAĞMEN** böyledir: yeni içerik ≈ 55 K. *(Karşılığında: 9 bloker + 3 yeni bulgu + 15 majör + 11 yeni mutant kapısı.)*
- **✅ KAPANANLAR:** **9/9 bloker** (B-1…B-9) · **oturum 21'in 3 yeni ölçülmüş bulgusu** (Y-1 `Dockerfile` yok → K22-a · Y-2 §3.2(5) olgusal yanlış · Y-3 `FakeCurrentUser` deliği) · **15 majör**.
- **✅ YENİ MUTANT KAPILARI (11):** **M36b** (çerez öznitelikleri, dört ayrı koşum) · **M40b** (fırsatçı silme) · **M41b** (kontrol 2'nin anahtarı) · **M42b** (`info` bayt kodlaması) · **M42c** (efemer üretim, `KON`) · **M49** (katman çevirimi) · **M50** (nonce) · **M51** (AAD) · **M52** (e-posta politikası) · **M53** (`/refresh` CSRF sırası). **Numara pini: 0003 = M1–M53, M54–M59 rezerv, 0004 artık `M60`'tan (K22-b).**
- **✅ ÖLÇÜM ARACI YEŞİL:** `K1`=0 · `K2`=0 · `K3`=0 · `K4`=0 · `K5`=0. Tek çıktı **aracın kendi kapsam beyanıdır** (K4'ün taramadığı 8 kanonik satır tek tek listelidir — *sessiz daraltma yok*).
- **⚠ ARAÇ TOPLAM DÖRT KÖR NOKTASINI ÜRETTİ VE DÖRDÜNÜ DE YAKALADI [dürüstlük — bu, doktrinin işe yaradığının kanıtıdır]:** (1) kanonik bölüm `###` alt bölümde bitiyor sanılıyordu · (2) başlığın adı gövdede geçince bölüm yanlış satırdan başlıyordu · (3) **sütun düzeni indisten varsayılıyordu** · (4) **kapsam-dışı raporu döngüden ÖNCE koşuyordu ⇒ hiç yazılmıyordu (sessiz daraltma).** Dördü de **sessizce YEŞİL** görünüyordu. Hepsi altın kümeye kontrol olarak eklendi: araç artık **beş kontrol** koşuyor ve geçemezse `KULLANILAMAZ` diyor (çıkış kodu 2).
- **📌 ODEV GÜNCELLENDİ:** §8(4)'e K22-a yazıldı — **paketlemenin bir parçası (`Dockerfile` + giriş betiği + compose `api` servisi) `slice-3c-auth`'a öne çekildi**; CI/CD'nin kendisi 11-12 Ağu'da kalır ve madde **AÇIK**.
- **Sıradaki ilk iş:** (1) **5. TUR BAĞIMSIZ KAPI — AYRI VE TEMİZ OTURUM.** Devir notundaki **üç uyarı** denetçiye birebir aktarılır (aracın kendisi denetlenecek · araç etiketi ölçer, ısırdığını ölçemez · K4'ün kapsam dışıları elle kontrol edilecek). (2) Temiz çıkarsa Onur kilitler → **AYRI OTURUM: ADR 0004** (yeni mutantlar **M60**'tan). (3) Sonra `GOREV-slice-3c-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21: **K23 — v5 YAZIMI SÜRÜYOR: 8/9 BLOKER + K22-a KAPANDI, `K1`=0**)

- **[K21 ÖLÇÜMÜ] Canlı bağlam ~%40 ⇒ 🟢 YEŞİL** — oturum devam ediyor.
- **✅ KAPANAN BLOKERLER (8/9):** **B-2** (M46 `UseRouting` öncesine yeniden çıpalandı + §0.4'e geri çekilen iddia) · **B-4** (§2-M'ye 4 yeni port satırı: `IUserStore` · kontrol 2 · kontrol 3 · **çevirim noktası**; *"handler"* kelimesi **her yerde** `Application handler` / `Api uç delegesi` diye ayrıştırıldı; **M49** doğdu) · **B-5** (**yeni karar `K3-I5`**: taze CSPRNG nonce + AAD + fail-closed; **M50**, **M51**) · **B-6** (e-posta ayakları **M52**'ye ayrıldı, `[KS-19]` assert'i **gerçekten** yazıldı) · **B-7** (§3.2 madde **9**: hız-sınırlayıcı izolasyonu; `FixedWindowRateLimiter`'ın `Stopwatch`+`Timer` ölçümü belgeye yazıldı) · **B-8** (M22'nin önkoşuluna *"her istekte FARKLI e-posta"* + `[KS-16]`) · **B-9** (§3.2 madde **10**: fixture `Momentum:MasterKey`'i test sabitiyle enjekte eder; K3-I1 ile çelişmediği ve M8a/M42c'nin etkilenmediği yazıldı) · **B-1** (M11 artık **sayı yazmıyor**, `[KS-10]`'a atıf yapıyor; *"aynı IP'den"* düştü; `Retry-After` ayağı eklendi).
- **✅ B-3 MEKANİK OLARAK KAPANDI — `K1` = 0.** 47 karar + 10 alt madde; **36'sı kapılı** (18 mutantın çıpa sütununa **gerekçesiyle** etiket eklendi), **11'i §3.1'de beyanlı** (6 yeni gerekçeli satır: K3-A1 · K3-A4 · K3-D4 · K3-J5 · K3-K1 · K3-K3). §3.1'e **aracın üç sınırını** açıkça yazan dürüstlük bloğu eklendi (üreten=denetleyen deliği · araç **etiketi** ölçer, **ısırdığını** ölçemez · altın küme zorunluluğu).
- **✅ K22-a UYGULANDI:** §2-I'ye `Dockerfile` + giriş betiği kararı ve **betiğin pinli sözleşmesi** (üretme koşulu · sırrı `stdout`'a yazmama · `Production`'da fail-fast'i **uygulamaya** bırakma) yazıldı. **M42** bileşiklikten ayrıldı: **M42** (türetme) · **M42b** (`info` bayt kodlaması) · **M42c** (efemer üretim, `KON`).
- **✅ YAZILAN MAJÖRLER (8):** `+` kaçışı **koşularak ölçüldü** ve belgeye **bayt dizisiyle** girdi · `[KS-4]`'ün sahibi **K14-a** olarak düzeltildi (v4 üç yerde K16-b diyordu) · `Retry-After` ayağı · **NIST SP 800-63B-4 §3.2.2 `SHALL`'ı ADLANDIRILMIŞ SAPMA** olarak yazıldı (gerekçe: kurtarma yolu kapsam dışı ⇒ kilitlenen hesap açılamaz, ODEV §2 vurulur) · **`/refresh`'te CSRF sırası** PAZARLIKSIZ yazıldı + **M53** · **HKDF-Extract** gerekçesi zorlandı ve sınırı §3.1'e bağlandı · **anahtar rotasyonu** kapsam dışı olarak adlandırıldı · **CSRF nonce entropisi `[KS-28]` ve çerez ömrü `[KS-29]`** pinlendi.
- **✅ ÖLÇÜM ARACI ÜÇ KÖR NOKTASINI DAHA ÜRETTİ VE YAKALADI [dürüstlük]:** (1) kanonik bölüm `###` alt bölümde bitiyor sanılıyordu · (2) başlığın adı gövdede geçtiği için bölüm yanlış satırdan başlıyordu · (3) **sütun düzeni indisten varsayılıyordu** (`| id | ad | değer |` tablosunda *"ad"* sütununu değer sanıyordu). **Üçünde de araç sessizce `kanonik_sayi: 0` diyor ve YEŞİL görünüyordu.** Üçü de altın kümeye kontrol olarak eklendi (araç artık **5 kontrol** koşuyor). Ayrıca `[KS-LITERAL: gerekçe]` muafiyeti eklendi (birebir alıntı/ölçüm kaydı/geri çekilmiş iddia) — **sessiz muafiyet yok**, ve `K4`'ün kapsam dışı bıraktıkları **raporlanıyor**.
- **⏳ KALAN İŞ (tek kalem):** **`K4` = 110** — gövde hâlâ kanonik sayıları **kopyalıyor**. 46 satır çevrildi (tavanlar, `[KS-4]`, uzunluklar, politika tablosu), kalanlar: `32 bayt` ailesi (KS-20/21/28 aynı sayı), `12 bayt` (KS-25/26), `15 dk`/`15 karakter` çakışması, `270 ms`/`19456`. **Bir kısmı BİLİNÇLİ literaldir** (NIST birebir alıntısı, ölçüm kayıtları, §0.4'ün geri çekilmiş iddiaları) ve `[KS-LITERAL: gerekçe]` ile işaretlenecektir. **`K4` = 0 hedef DEĞİLDİR; hedef "her kalan kopya ya çevrilmiş ya gerekçesiyle işaretlenmiş"tir.**
- **Dosya (Onur'un diskinden ölçüldü):** `docs/ADR/0003-kimlik-cekirdegi-v5-YAZIM-DEVAM-EDIYOR.md`. **Kanonik ADR hâlâ v4'tür.**

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21: **K22 — Y-1 VE K20-a KİLİTLENDİ; v5 YAZIMI DEVAM EDİYOR**)

- **[KARAR — Onur, 25 Tem 2026] K22-a: Y-1 → API `Dockerfile` + GİRİŞ BETİĞİ BU DİLİMİN İŞİDİR.** Ölçüm (oturum 21): `find -iname Dockerfile*` ⇒ **0 sonuç**; `docker-compose.yml` yalnız `postgres:17-alpine` tanımlıyor. ⇒ v4'ün `KON` seviyesi ve §2-I bootstrap'ı **var olmayan bir artefakta** çıpalıydı. **Karar:** `Dockerfile` + giriş betiği + compose'a `api` servisi **yazılır** ⇒ `KON` **gerçek** olur, **M8b ve M42 çıpalı kalır**, §2-I'nin bootstrap'ı gerçek bir betikte yaşar. **v5'in yazması gerekenler:** §2-I'ye giriş betiğinin **ne yaptığı** (anahtar yoksa üret, `Production`'da ÜRETME) · `KON` seviyesinin tanımına *"`docker compose up` ile ayağa kalkan gerçek imaj"* · **ODEV §8/4'ün paketleme kalemi bu dilime ÖNE ÇEKİLDİ** ve bu ODEV'e yazılır · kırmızı çizgi #1 (sırlar repoya girmez) giriş betiği için **açıkça** doğrulanır. *Gerekçe:* değerlendirici uygulamayı **tek komutta** ayağa kaldırır ⇒ ODEV §2'nin *"kesinlikle çalışan uygulama, önce ona bakılacak"* ölçütüne doğrudan hizmet. *Reddedilenler [adlandırılmış]:* **`KON`'u kapsam dışına almak** (en az iş, ama M8b tam olarak kapı-3'ün **B4 blokerini kapatmak için** `KON`'a taşınmıştı ⇒ **B4'ü yeniden açma riski**) · **§3.1'e kapısız beyan** (en ucuz, ama dev bootstrap'ın ortam koşulu VE kök anahtarın restart kalıcılığı — kırmızı çizgi #1'e değen **iki mekanizma birden** — kapısız kalır).
- **[KARAR — Onur, 25 Tem 2026] K22-b: K20-a → ADR 0004'ÜN MUTANT PİNİ `M50` → `M60`.** ADR 0003 aralığı **M1–M52**'ye genişler (**M49** katman çevirimi · **M50** nonce · **M51** AAD · **M52** e-posta girdi politikası). **M53–M59 = 0003'ün olası 6. turu için tampon.** K16-d'nin gerekçesi hâlâ geçerli: **0004 hiçbir numarayı tüketmedi** ⇒ pin taşımak bedelsiz. *Reddedilenler:* yeni mekanizmaları **harfli ayak** yazmak (K16-d'nin açıkça reddettiği yol; kendi başına mekanizma olan kapıyı sahte ikinci ayak gibi gösterir) · 0004'ü **M100**'den başlatmak (tampon geniş ama numara boşluğu her turda açık rezerv ilanı ister).
- **[K21 ÖLÇÜMÜ — kural gereği raporlanıyor]** Bu checkpoint anında canlı bağlam **~285 K / 1 M = %28,5 ⇒ 🟢 YEŞİL** ⇒ **oturum devam ediyor, devir YOK.**

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21: **K21 — OTURUM DEVRİ ARTIK ÖLÇÜME BAĞLIDIR; HİSSE DEĞİL**)

- **[KARAR — Onur, 25 Tem 2026] K21: DEVİR KARARI ÖLÇÜLMEDEN VERİLEMEZ. [PAZARLIKSIZ]**
- **Tetikleyen olay [dürüstlük — Cowork'ün kendi hatası]:** oturum 21'de Cowork *"bağlam doldu"* diyerek v5 yazımını yarım bıraktı ve temiz oturum önerdi. **Onur itiraz etti, ÖLÇÜM YAPILDI:** canlı bağlam **280.999 token** = **%28** (pencere 1.000.000 — *Onur'un beyanı; Cowork pencere boyutunu ölçmedi*). Bağlam eğrisi: 72k→96k→142k→186k→217k→250k→**281k**. Bu oturumda üretilen çıktı **233.473** token; **alt ajan 0**. ⇒ **Devir gereksizdi ve iddia ÖLÇÜLMEDEN kuruldu — belgenin kendi §4 kuralının ("her olgu iddiası ya ölçülür ya `[DOĞRULANMADI]`") doğrudan ihlali.**
- **ÖLÇÜMÜN TANIMI (pazarlıksız):** canlı bağlam = oturum transcript'indeki **son mesajın** `input_tokens + cache_read_input_tokens + cache_creation_input_tokens` toplamı. Transcript: `~/.claude/projects/*/<oturum-id>.jsonl`. **Kendi oturum-id'sinle ölç; argümansız/tahmini ölçüm geçersizdir.**
- **RENK EŞİKLERİ (1M pencere için):**
  - 🟢 **< %55 (550k): DEVAM.** Devir YOK, *"temiz oturum"* önerisi YOK, *"bağlam doluyor"* cümlesi YOK.
  - 🟡 **%55–75: eldeki maddeyi bitir, checkpoint yaz, YENİ BÜYÜK İŞ BAŞLATMA.** **"DOLU BAĞLAMDA ADR YAZMA YASAĞI" ARTIK BURADAN BAŞLAR** — histen değil, ölçümden.
  - 🔴 **> %75 (750k): devir notu yaz, oturumu kapat.**
- **ÖLÇEMEZSEN:** ne yeşil ne kırmızı varsay — **ölçemediğini SÖYLE ve Onur'a sor.** Sessizce devir önerme.
- **RAPORLAMA:** her büyük işin başında ve her checkpoint'te bu ölçüm **rapor edilir** (oturum 21'de sessiz geçildi; bir daha geçilmeyecek).
- **Bu kural `CLAUDE.md`'ye de yazıldı** (kalıcı iş akışı kuralı).

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21 kapanış: **K20 — v5 YAPISI KURULDU, İÇERİK YARIM; ÖLÇÜM ARACI CANLI**)

- **✅ ÖLÇÜM ARACI CANLI VE KENDİNİ KANITLADI:** `araclar/adr-kapi-taramasi.py` (**15 K · 362 satır**, Python 3 stdlib, bağımlılık yok). **Dört kontrol koşar:** `K1` karar↔kapı/beyan eşlemesi · `K2` sarkan atıf · `K3` başlıksız tablo · `K4` kanonik sayının gövdede kopyalanması · `K5` mutant numara boşluğu. **`--altin-kume` kipi DÖRT kontrolü de geçmeden araç `KULLANILAMAZ` der (çıkış kodu 2):** yanlış-pozitif (temiz belgede 0 bulgu) · yanlış-negatif (bilerek konmuş 4 kusurun 4'ü de bulunur) · **kör-kapı 1** (`###` alt bölümlü kanonik tablo) · **kör-kapı 2** (başlık adı gövdede de geçiyor). Kanıt: `KANIT/adr-0003/olcum-araci-altin-kume-kanit.txt` (10.830 bayt, üç koşum).
- **⚠ ARAÇ KENDİ KÖR NOKTASINI İKİ KEZ ÜRETTİ VE İKİSİ DE YAKALANDI [dürüstlük]:** ilk sürüm kanonik bölümü `###` alt bölümde bitmiş sanıyor, ikinci sürüm başlığın adı gövdede geçtiği için bölümü **5. satırdan** başlatıyordu — **her ikisinde de sessizce `kanonik_sayi: 0` diyordu**, yani K4 KÖRDÜ ve yeşil görünüyordu. Yakalanmasını sağlayan şey *"bölüm bulundu ama 0 satır okundu ⇒ KÖRDÜR"* muhafızıdır. **Bu, `turkce-kapilar` doktrininin ("ölçüm aracı ÖNCE kendini kanıtlar") bu projede fiilen iş gördüğünün kanıtıdır.** **ARACIN BEYAN EDİLMİŞ SINIRI:** `K4` yalnız **sayısal literal** taşıyan kanonik satırları tarar — 27 satırın **11'i**; *"KS-2 ile aynı"*, `TimeSpan.Zero`, `ProcessorCount` gibi satırlar **kapsam dışıdır ve elle kontrol edilmelidir**.
- **ARACIN KOŞUM SONUÇLARI:** **v4** (denetlenen sürüm) ⇒ **28 bulgu**; kapı-4'ün ELLE bulduğu **K3-D4 · K3-C6(5) · K3-L8(1)** ve **satır-716 başlıksız tablosu** MEKANİK olarak yeniden bulundu ⇒ araç gerçek belgede iş görüyor. **v5 yarım dosyası** ⇒ **36 bulgu** (`K1` 25 · `K4` 11 · `K3` **0 — onarıldı**). **Hedef: v5 tamamlandığında `K1`=0 ve `K4`=0.**
- **✅ v5'İN YAPISI KURULDU** (`docs/ADR/0003-kimlik-cekirdegi-v5-YAZIM-DEVAM-EDIYOR.md` · **165.718 bayt · 922 satır · UTF-8, BOM yok, U+FFFD 0** · sha256 `6eb48a2c2baa…` — Onur'un diskinden ölçüldü): (1) **§0.1–§0.3 KANIT'a taşındı** (`KANIT/adr-0003/v4-kapanma-tablolari.md`, 11.691 bayt) — **hiçbir satır silinmedi**, ADR §0 artık atıf yapıyor; (2) **§1-K KANONİK SAYILAR** doğdu — **KS-1…KS-27**, dört alt tablo (ömürler · kaba kuvvet tavanları · girdi politikası · kripto boyutları), kuralı PAZARLIKSIZ: *"sayı bir kez yazılır, gövde atıf yapar, kopyalamaz"*; (3) **v4'ün satır 715'teki başlıksız mutant tablosu onarıldı** — v4'te GFM'de render OLMAYAN blok tam olarak v4'ün bütün yeni kapılarıydı (M32b·M32c·M40–M48).
- **⚠ v5 İÇERİK OLARAK YARIMDIR VE BELGE ŞU AN İÇ TUTARSIZDIR — bilinçli, dosyanın kendi başlığında yazılı:** 9 blokerin **hiçbiri** kapatılmadı · gövde hâlâ sayıları kopyalıyor · **M49–M52 kanonik tabloda atıf aldı ama mutant tablosunda YOK** · **K3-I5 (nonce/AAD) kararı yazılmadı**. **Kanonik ADR hâlâ v4'tür.**
- **📋 `KANIT/adr-0003/v5-yazim-plani.md` (15.327 bayt · 135 satır) YAZILDI** — oturum 22'nin tek yol haritası: 9 blokerin ne yazacağı (K18 kilitleriyle) · oturum 21'in 3 yeni bulgusu · **aracın 25 `K1` bulgusunun kalem kalem adjudikasyon ÖNERİSİ** (açıkça *"öneri, ölçüm değil; her biri mutantın kill sinyaline karşı doğrulanmalı; etiketi yalnız aracı yeşile boyamak için eklemek kapı tiyatrosudur"* diye etiketli) · 16 majör başlığı · minörler · numara pini · sıra.
- **[COWORK KARARI — Onur onayına açık] K20-a: MUTANT NUMARA PİNİ YİNE ÇAKIŞTI.** v5 dört yeni mutant doğuruyor: **M49** (katman çevirimi, K18-a) · **M50** (nonce) · **M51** (AAD) · **M52** (e-posta girdi politikası). **K16-d ADR 0004'ü `M50`'den başlatmıştı ⇒ çakışma.** *Öneri:* 0003 aralığı **M1–M52**'ye genişler, **0004'ün pini `M60`'a taşınır**. *Gerekçe:* 0004 hâlâ hiçbir numarayı tüketmedi (K16-d'nin kendi gerekçesi) ⇒ taşımak bedelsiz. *Reddedilen:* yeni mekanizmaları harfli ayak gibi göstermek — K16-d'nin **açıkça reddettiği** yol.
- **[TAKVİM ÖLÇÜMÜ — K13-a gereği raporlanıyor, K12-b gereği KESME ÖNERİSİNE ÇEVRİLMİYOR]** ADR 0003: **4 yazım turu + 4 kapı turu + bu yarım 5. yazım turu**. Kalan: v5'in bitirilmesi (≥1 oturum) + 5. kapı (≥1 oturum) + olası 6. tur + ADR 0004 + spec + build + doğrulama. ODEV §3.1'in `slice-3a-auth` penceresi (21-23 Tem) aşıldı, **15 Ağu hedefi ağır baskı altında.** K12-b yürürlükte.
- **Sıradaki ilk iş:** (1) Onur **Y-1**'i ve **K20-a**'yı karara bağlasın. (2) **v5 yazımını TEMİZ OTURUMDA bitir** (`v5-yazim-plani.md`). (3) **5. tur bağımsız kapı, AYRI OTURUM** — *aracın kendisi de denetlenecek.* (4) Sonra ADR 0004 → `GOREV-slice-3c-auth` spec'i → Claude Code build.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 21: **K19 — v5 YAZIM OTURUMU AÇILDI; DÖRT ÇATAL DAHA KİLİTLENDİ**)

- **Protokol koşuldu (oturum 21):** `docs/ODEV.md` (tam, 171 satır) + `CLAUDE.md` (tam, 33 satır) + bu dosyanın en üst iki checkpoint'i (K18 + K17) + **`KANIT/adr-0003/kapi-4-denetim-raporu.md` (tam, 190 satır)** okundu. **`git status` kaynaktan ölçüldü:** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/…v1/v2/v3…` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · **ahead 15, push yok** — devir notuyla **birebir** uyuştu. **BU OTURUM YAZIM OTURUMUDUR; DENETLEMEZ.**
- **🟢 BU OTURUMDA TÜM REPO BAĞLANDI** (kapı-4'te yalnız `docs/` + `KANIT/` bağlıydı) ⇒ `src/`, `tests/`, `docker-compose.yml`, `araclar/` **ilk kez ölçülebilir**. Kapı-4 §6'nın "ölçülemeyenler" listesi bu oturumda kapatılıyor.
- **[KARAR — Onur, 25 Tem 2026] K19-a: v5 = YENİDEN YAPILANDIR + DÜZELT** (yalnız yama değil). Kapı-4 §7'nin üç yapısal önerisi uygulanır: (1) tüm tavanlar/ömürler/uzunluklar **tek KANONİK SAYILAR tablosuna** çekilir, gövde **atıf yapar, kopyalamaz**; (2) §0.1–§0.3 kapanma tabloları (**13.451 bayt**, ölçüldü) **KANIT/adr-0003/**'e taşınır, ADR'de yalnız §0.4 (geri çekilen iddialar) kalır; (3) 9 bloker + ~27 majör kapatılır. *Gerekçe:* B-1 ve B-8 sınıfı bulgular (*kardeş güncellendi, kendisi unutuldu*) bu kuralla **yapısal olarak doğamaz**; kontrol 1 sayısı bugün **7 ayrı yerde**. *Reddedilenler [adlandırılmış]:* **cerrahi yama** (en hızlı, ama kök neden yaşar ve ÜÇ TURDUR bloker üretiyor) · **sıfırdan yazım** (en temiz, ama kapı-4'ün "kırılamadı" damgalı içeriği — 23 birincil-kaynak ölçümü, sağlam mutantlar, §0.4 izi — yeniden üretilirken **sessiz kayıp** riski; denetim tam da bunu yakalamakta zayıftır).
- **[KARAR — Onur, 25 Tem 2026] K19-b: K18-e ONAYLANDI.** `M46` yeniden çıpalanır: mutasyon = *"`UseRateLimiter`, `UseRouting`'den ÖNCEYE alınır"*, kill sinyali = *"31. `/login` isteği `429` **ALIR**"* **FAIL**. Statik-dosya ayağı **düşer**; §2-J satır 405'in *"onlarca asset isteği kovayı ilk saniyede tüketir"* gerekçesi **§0.4'e GERİ ÇEKİLEN İDDİA** olarak yazılır. *Reddedilenler:* küresel limiter eklemek (yeni mekanizma + yeni kapı + yeni risk) · M46'yı düşürüp §3.1'e kapısız yazmak (PAZARLIKSIZ bir karar kapısız kalır).
- **[KARAR — Onur, 25 Tem 2026] K19-c: K18-d ÖLÇÜM ARACI BU OTURUMDA YAZILIR, ALTIN KÜMEDE KANITLANIR, KOŞULUR.** ⇒ §3.1'in *"bu liste, tablonun tamlık iddiasının sınırıdır"* cümlesi **ölçülen** bir iddia olur. **⚠ DÜRÜSTLÜK KAYDI (Onur'a açıkça söylendi ve kabul edildi):** aracı **v5'i yazan oturum yazıyor** ⇒ *üreten ≠ denetleyen* bu kalemde **kısmen deliniyor**. Telafi: (a) araç **önce bilerek kapısız bırakılmış altın küme kalemini bulduğunu** kanıtlar, kanıt KANIT'a yazılır; (b) **kapı-5, ARACIN KENDİSİNİ de denetlemek ZORUNDADIR** — aracın yeşili tek başına kanıt sayılmaz. *Reddedilen:* aracı ayrı oturuma bırakmak (üreten≠denetleyen tam korunur ama B-3 kapı-5'te **dördüncü** kez bloker doğar).
- **[KARAR — Onur, 25 Tem 2026] K19-d: EK ÖLÇÜM KAPSAMI = İKİSİ DE** — `+` kaçış dizisi **ve** §3.2'nin gerçek test altyapısıyla uyumu ölçülür.
- **✅ [BU OTURUMUN KENDİ ÖLÇÜMLERİ — kaynaktan, Onur'un diskinden]**
  1. **`public partial class Program;` VAR** — `src/backend/Momentum.Api/Program.cs` **satır 153**, yorumu birebir: *"Exposed as partial so `WebApplicationFactory<Program>` can host it in tests."* ⇒ §3.2(1)'in `WebApplicationFactory<Program>` zorunluluğu **mevcut altyapıyla uyumlu**. **Kapı-4 §6'nın 1. ölçülemeyeni KAPANDI.**
  2. **`TestAuthHandler` YOKTUR ve `AddAuthentication` HİÇ ÇAĞRILMIYOR** (`grep` src+tests ⇒ **0 eşleşme**). Program.cs satır 108-110 birebir: *"There is no authentication scheme yet…"* ⇒ §3.2'nin `TestAuthHandler` yasağı **geriye dönük hiçbir testi kırmıyor**. **KAPI-4 §6'nın 3. ölçülemeyeni KAPANDI.**
  3. **⚠ AMA ASIL İKAME `FakeCurrentUser`'DIR ve §3.2 ONU HİÇ ANMIYOR** — `tests/Momentum.Persistence.Tests/FakeCurrentUser.cs` (`ICurrentUser` sabit-kimlik stub'ı), `Compile Include` ile `Momentum.Api.Tests`'e de bağlı; `Program.cs:45` `AddScoped<ICurrentUser, NullCurrentUser>()`. **v5 BUNU KARARA BAĞLAMALI:** slice-3c'den sonra kimlik ikamesi DI'dan (`FakeCurrentUser`) mı yapılacak, gerçek token'dan mı? §3.2 `TestAuthHandler`'ı adıyla yasaklarken **aynı deliği başka adla açık bırakıyor**. *(YENİ BULGU — kapı-4'te yoktu, çünkü `tests/` bağlı değildi.)*
  4. **⚠ §3.2(5) MEVCUT ALTYAPIYA KARŞI OLGUSAL OLARAK YANLIŞ:** *"`TimeProvider` her testte `FakeTimeProvider`'dır"* deniyor; ölçüm: `Program.cs:34` **`AddSingleton(TimeProvider.System)`** ve testlerin çoğu (`DispatcherTests`, `TestSupport.cs:73`) **`TimeProvider.System`** kullanıyor; `FakeTimeProvider` yalnız `DispatcherTests.cs:194`'te bir yerde geçiyor. ⇒ `WebApplicationFactory<Program>` ile kalkan bir test **fixture açıkça ezmedikçe gerçek saati alır**. **v5 ya ezmeyi pinlemeli ya cümleyi daraltmalı.**
  5. **Paket kapısı (kırmızı çizgi #3) — ölçüldü:** `Microsoft.Extensions.TimeProvider.Testing` **9.0.0**, `Microsoft.AspNetCore.Mvc.Testing` **9.0.18**, `Testcontainers.PostgreSql` **4.13.0** (csproj yorumu birebir *"MIT"*), `NetArchTest.Rules` **1.3.2**, `xunit` **2.9.2**, `CsCheck` **4.7.0**, `Shouldly` **4.3.0** ⇒ **`FakeTimeProvider` paketi ZATEN repoda**, yeni bağımlılık **değildir**. **Kapı-4 §6'nın 4. ve 5. ölçülemeyeni KAPANDI.**
  6. **🔴 `Dockerfile` YOKTUR** (`find -iname Dockerfile*` ⇒ **0 sonuç**) ve `docker-compose.yml` **yalnız `postgres:17-alpine`** servisini tanımlıyor — **API için konteyner, giriş betiği ve imaj yok.** ⇒ **v4'ün `KON` seviyesi (M8b, M42) ve §2-I'nin *"konteynerin giriş betiği"* bootstrap'ı BUGÜN VAR OLMAYAN bir artefakta çıpalı.** *(YENİ BULGU — bloker sınıfı; kapı-4 bunu göremezdi, `Dockerfile`/compose bağlı değildi. v5 ya `KON`'u kapsam dışına alır, ya bu dilimin işi olarak yazar, ya §3.1'e beyan eder — dördüncü seçenek yok.)*
  7. **Test projeleri (4):** `Momentum.Api.Tests` · `Momentum.ArchitectureTests` · `Momentum.Persistence.Tests` · `Momentum.SyncCore.Tests`. **`KON` seviyesine karşılık gelen bir proje YOK.** `Testcontainers` yalnız `Persistence.Tests`'te (`TestSupport.cs:20-21`, `postgres:17-alpine`).
  8. **`+` kaçış dizisi — üç halkalı BİRİNCİL KAYNAK zinciri (koşum değil):** `AllowedBmpCodePointsBitmap.ForbidHtmlCharacters()` birebir `ForbidChar('+'); // …can be used to perform UTF7-based attacks` · `DefaultJavaScriptEncoder` özel kurucusu birebir `forbidHtmlSensitiveCharacters: true` · yazım `destination[1] = (byte)'u'; HexConverter.ToBytesBuffer(…)` (4 hane) · `HexConverter.ToBytesBuffer(byte value, Span<byte> buffer, int startingIndex = 0, Casing casing = Casing.Upper)` ⇒ **varsayılan UPPER** ⇒ çıktı **`\u002B`** (altı karakter, `B` **büyük**). **✅ KOŞULARAK ÖLÇÜLDÜ [Onur, Windows, .NET 9 SDK, 25 Tem 2026 — oturum 21]:** `JsonSerializer.Serialize(new { detail = "a+b" })` ⇒ `{"detail":"a\u002Bb"}` · UTF-8 baytları birebir: `123 34 100 101 116 97 105 108 34 58 34 97 **92 117 48 48 50 66** 98 34 125` ⇒ kaçış dizisi **`92 117 48 48 50 66`** = ters bölü · `u` · `0` · `0` · `2` · **`B` (66, BÜYÜK)** — **altı bayt.** ⇒ **Kaynak zinciri (üç halka) DOĞRULANDI**, `[KOŞULARAK DOĞRULANMADI]` etiketi DÜŞTÜ. **M28'in tarayıcısı artık bu bayt dizisini arar.** *(Bayt düzeyinde ölçüm, metin gösteriminden üstündür: harf büyüklüğü ve kaçışın kendisi gösterim katmanında bozulamaz.)* **Bulut konteynerinde ölçülemedi:** bulut konteynerinde .NET yok — `dot.net`, `packages.microsoft.com`, `builds.dotnet.microsoft.com`, `dotnetcli.azureedge.net` **dördü de proxy tarafından kesildi (ölçüldü)**; bağlı Linux VM'de de `dotnet` yok. Onur'a Windows'ta koşulacak birebir komut verildi; çıktı gelirse etiket düşer, gelmezse **`[KOŞULARAK DOĞRULANMADI]`** ile yazılır.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 20 devamı: **K18 — KAPI-4'ÜN DÖRT AĞIR ÇATALI KİLİTLENDİ**)

- **[KARAR — Onur, 25 Tem 2026] K18-a: B-4 → KONTROL 2/3 APPLICATION'DA KALIR; `ProblemDetails`'e ÇEVİRİM API KATMANINDA YAPILIR.** Application handler'ı kendi hata tipini fırlatır (ör. `RateLimitedException` — adı v5'te pinlenir; `limit` ayrımını, `Retry-After` değerini ve pencere bilgisini taşır); **Api katmanı** bunu `ProblemDetails`'e çevirir. ⇒ **`M32b` kuralı DELİNMEZ** (`Microsoft.AspNetCore.*` Application'da görünmez; ölçüldü: `namespace Microsoft.AspNetCore.Mvc;`) ve N-katmanlı disiplin ödevin vitrini olur. **v5'in yazması gerekenler:** §2-M port envanterine **kontrol 2/3, çevirim noktası ve `users` kalıcılığı** satırları · K3-J3'ün bağlayıcı sırasının hangi katmanda ne yaptığı · **yeni mutant (M49): çevirim Api'den Application'a taşınırsa `M32b` baseline'ı kırmızıya döner** · "handler" kelimesinin ADR 0001'deki **iki anlamı** (satır 27 CQRS handler=Application · satır 80 K-H1 "Api endpoint/handler"=Api) ayrıştırılır ve 0003 hangisini kastettiğini **her geçtiği yerde** yazar. *Reddedilenler [adlandırılmış]:* **hepsi Api'de** (en ucuz ve §3.2(4) ile birebir uyumlu, ama auth dilimi 0001 K-B1'in CQRS desenini fiilen atlar ⇒ denetçi *"neden bu dilim desen dışı"* diye sorar) · **Application'da + M32b gevşetilir** (en az iş, ama v4'ün BU TUR yeni yazdığı kural doğar doğmaz delinir ⇒ kapı-5 bunu ilk bulur).
- **[KARAR — Onur, 25 Tem 2026] K18-b: B-5 → HER ŞİFRELEMEDE CSPRNG NONCE + AAD.** `successor_secret_enc` için: **12 bayt taze nonce** (`RandomNumberGenerator`), **AAD = `family_id ‖ token_hash`**. Pinli **12+32+16 = 60 bayt** düzeni AYNEN korunur. ⇒ NIST SP 800-38D'nin nonce-tekrarı yasağı karşılanır ve şifreli blok **satırına bağlanır** (başka satıra kopyalanamaz). **v5'in yazması gerekenler:** nonce üretiminin **akış** olduğu (K3-K2 muafiyeti bu kaleme UYGULANMAZ; ölçüldü: `AesGcm.Encrypt(ReadOnlySpan<byte> nonce, …)` nonce'u çağırandan alır) · **iki yeni mutant:** nonce sabitlenirse (ör. sıfır dizisi) test kırılır · AAD kaldırılırsa/karıştırılırsa çözme **başarısız** olur · §3.1'e bu kalemlerin **kapılı** olduğu yazılır. *Reddedilenler [adlandırılmış]:* **AAD'siz nonce** (NIST şartı karşılanır ama blok satıra bağlanmaz ⇒ adlandırılmış sapma yazmak gerekir) · **K15-a'nın geri alınması** (nonce sorunu tümden yok olur ama kapı-3'te kilitlenen karar düşer ve B1 yeniden açılır).
- **[KARAR — Onur, 25 Tem 2026] K18-c: B-9 → TEST FIXTURE'I SABİT TEST ANAHTARI ENJEKTE EDER.** `WebApplicationFactory` fixture'ı `ConfigureAppConfiguration` ile `Momentum:MasterKey`'i **test-only sabit** bir değerle sağlar. **v5'in yazması gerekenler:** §3.2'ye **9. madde** olarak *"testlerin sır kaynağı"* eklenir · K3-I1'in *"Varsayılan/gömülü anahtar YOKTUR"* cümlesiyle çelişmediği **açıkça** gösterilir (anahtar **üretim kodunda değil, test yapılandırmasındadır**; kırmızı çizgi #1 ihlal edilmez çünkü repoya giren şey bir **test sabiti**dir, üretim sırrı değil) · **M8a (`Production` fail-fast) ve M42 (`KON`) bu karardan ETKİLENMEZ** ve bu yazılır · `TS`/`TC` seviyelerinin baseline'ının artık **yeşil doğduğu** §3.2(8) ile birlikte beyan edilir. *Reddedilenler [adlandırılmış]:* **test başına CSPRNG** (sabit-anahtar optiği hiç doğmaz ama M42'nin kalıcılık iddiası `TS`'te kurulamaz, tamamen `KON`'a yığılır) · **ortam değişkeni zorunluluğu** (üretimle birebir aynı mekanizma ama *"klonla gelmez"* sorunu testlere taşınır; **CI henüz kurulmadı** — ODEV §8(4)).
- **[KARAR — Onur, 25 Tem 2026] K18-d: B-3 → ÖLÇÜM ARACI YAZILIR; §3.1'İN TAMLIK CÜMLESİ KALIR.** Her `**K3-…**` kararını çıkarıp **mutant tablosu / §3.1 satırlarıyla** eşleyen bir betik yazılır; eşleşmeyen kalem **kapı hatasıdır**. **PAZARLIKSIZ ÖNKOŞUL (`turkce-kapilar` doktrini birebir uygulanır): ARAÇ ÖNCE KENDİNİ KANITLAR** — bilerek kapısız-ve-beyansız bırakılmış bir **altın küme** kalemini bulamayan betik kullanılamaz; betiğin kendi kanıtı KANIT'a yazılır. ⇒ *"bu liste, tablonun tamlık iddiasının sınırıdır"* cümlesi **korunur** ama artık **ölçülen** bir iddiadır. *Reddedilenler [adlandırılmış]:* **cümleyi geri çekmek** (en ucuz ve dürüst, ama belgenin en güçlü dürüstlük vitrini sönükleşir — bu bir portfolyo belgesidir) · **elle tam tarama** (maliyetsiz görünür ama **son üç turda denenen ve ÜÇ KEZ başarısız olan** yöntemin ta kendisidir).
- **[COWORK KARARI — Onur onayına açık, dürüstçe kayda geçiyor] K18-e: B-2 → `M46` GÖZLEMLENEBİLİR BİR DAVRANIŞA YENİDEN ÇIPALANIR; STATİK-DOSYA AYAĞI DÜŞER.** Ölçüm (birincil kaynak) `UseRateLimiter`'ın statik dosya ayağını **yapısal olarak imkânsız** kıldı. **Öneri:** yeni mutasyon = *"`UseRateLimiter`, `UseRouting`'den ÖNCEYE alınır"*; kill sinyali = *"31. `/login` isteği `429` **ALIR**"* **FAIL** (uca bağlı politikalar çözülemediği için **hiçbir sınır uygulanmaz** ⇒ gerçekten gözlemlenebilir). **Ayrıca §2-J satır 405'in *"onlarca asset isteği kovayı ilk saniyede tüketir"* gerekçesi §0.4'e GERİ ÇEKİLEN İDDİA olarak yazılır** (§4'ün kuralı gereği: yanlış çıkan olgu iddiası sessizce silinmez, geri çekilir). *Reddedilen:* **küresel limiter eklemek** (M46 ısırır ama yeni bir mekanizma + yeni kapı + yeni risk doğar; K16-b'nin üç politikalı tasarımı sadeliğini kaybeder).
- **KALAN BEŞ BLOKER ÇATALSIZDIR — v5 doğrudan düzeltir:** **B-1** M11'in sinyali `31.` + önkoşula *"her istekte FARKLI e-posta"* + çıpa 1a'ya + *"aynı IP'den"* ifadesinin düzeltilmesi · **B-6** M31'in e-posta ayakları **ayrı bir mutanta** taşınır (parola mutasyonu e-postayı ısırtamaz) + **254 sınırının assert'i gerçekten yazılır** · **B-7** §3.2'ye **hız-sınırlayıcı izolasyonu** pinlenir (ölçüldü: `FixedWindowRateLimiter` `Stopwatch`+`Timer` kullanır, `FakeTimeProvider` ONU İLERLETEMEZ ⇒ sınıf başına yeni host / kova sıfırlama stratejisi yazılmalı) · **B-8** M22'nin önkoşuluna *"her istekte FARKLI e-posta"* eklenir (kontrol 2, kontrol 3'ten önce ısırıyor) · **B-3'ün 10 kalemi** kapıya bağlanır ya da §3.1'e yazılır.
- **Sıradaki ilk iş:** **ADR 0003 v5 — TEMİZ VE AYRI OTURUM.** Dört ağır çatal kilitli (K18-a/b/c/d) + bir Cowork önerisi (K18-e, Onur onayına açık) ⇒ **v5 doğrudan yazmaya başlayabilir.** Sonra **5. tur bağımsız kapı** (K13-a).

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 20: **K17 — v4'E 4. BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ**)

- **Protokol koşuldu (oturum 20):** `docs/ODEV.md` (tam, 171 satır) + `CLAUDE.md` (tam, 33 satır) + bu dosyanın en üst iki checkpoint'i (K16 + K15) okundu. **`git status` kaynaktan ölçüldü:** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/…v1/v2/v3…` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · **ahead 15, push yok** — devir notuyla **birebir** uyuştu. **Ayrıca ADR v4'ün `sha256`'sı hem Onur'un diskinde hem denetim kopyasında ölçüldü ve AYNI çıktı** (`b85ce0b3…0c45d0`) ⇒ denetim gerçek artefakt üzerinde koştu.
- **KAPI KURULUMU (üreten ≠ denetleyen korundu — oturum 19 YAZDI, bu oturum yalnız DENETLEDİ, ADR'ye tek satır dokunmadı):** A turu = üç bağımsız denetçi **paralel** (A1 mimari/tutarlılık · A2 ölçüm/gerçeklik · A3 mutant/kapı; üçü de 885 satırın TAMAMINI okudu) → B turu = **RED-TEAM EN SON**, A'yı **çürütmekle** görevli → ana oturum = adjudikasyon + **hükmü taşıyan 9 iddianın 9'unun da kaynaktan YENİDEN ölçümü** (alt-ajan beyanı AKTARILMADI).
- **🔴 HÜKÜM: KİLİTLENEMEZ. Dört denetçi de bağımsız olarak aynı hükme vardı. 9 BLOKER + ~27 majör + ~28 minör. Tam rapor: `KANIT/adr-0003/kapi-4-denetim-raporu.md`.** **Bu 4. turdur** (K13-a: bloker sıfırlanana kadar tur; tur sayısı raporlanır, sınırlanmaz).
- **RED-TEAM A TURUNU KISMEN ÇÜRÜTTÜ:** 3 bulgu + 1 premis **çürütüldü** (pinlenen `UPDATE` ↔ değişmez çelişkisi — *"aynı transaction"* ≠ *"aynı ifade"*, `READ COMMITTED` pinli · *"kontrol 2 fiilen hesap kilitlemesidir"* — NIST SP 800-63B-4 §3.2.2 taksonomisinde **throttling kilitlemenin karşıtıdır** · *"M33a'nın `GET /` ayağı duyarsız"* — mutasyonda `GET /tasks` **401** ⇒ mutant **ölür** · *"`TS` tanım gereği veri katmanı olmayan seviyedir"* — belgede böyle bir cümle yok, `Npgsql`'in in-memory sağlayıcısı da yok); **3 bloker majöre indirildi**, **4 majör minöre indirildi**, red-team **1 yeni bloker + 3 yeni majör** getirdi.
- **✅ [ANA OTURUMUN KENDİ ÖLÇÜMLERİ — 9 blokerin 9'u da yeniden ölçüldü]**
  1. **M11 (satır 686) ölü tuzak:** sinyal *"**11.** `/login`"*, tavan (satır 429) **30/5 dk** ⇒ baseline kırmızı. Kardeşi **M23 satır 697'de `11→31` güncellenmiş, M11 unutulmuş**; §0.1/§0.3 kalemi **yapılmış ilan ediyor**.
  2. **M46 (satır 724) kör:** belgede **küresel limiter kararı yok** (`global` yalnız satır 404'te varsayımsal). **`RateLimitingMiddleware` (aspnetcore 9.0) birebir:** *"If this endpoint has no EnableRateLimitingAttribute & there's no global limiter, don't apply any rate limits."* + `if (enableRateLimitingAttribute is null && _globalLimiter is null) return _next(context);` ⇒ statik dosya **hiçbir kovayı tüketemez** ⇒ assert baseline'da DA mutasyonda DA sağlanır ⇒ **test kırılamaz**. Satır 405'in gerekçesi **yapısal olarak imkânsız** bir olgu iddiasıdır (§4 kuralının ihlali).
  3. **§3.1'in tamlık iddiası ÜÇÜNCÜ KEZ yanlış** — kapısız-ve-beyansız **10 kalem** (K3-I4 türetme kararı · K15-a'nın "şifreli"si · K3-C6(5) tembel silme · süpürme periyodu 60 sn · **parola NFC** · kontrol **1b/1c sayıları (120/60)** · sweeper DI scope'u · K3-A3 `COLLATE "C"`+`409` · K3-D4 · K3-L8(1)).
  4. **"handler" hangi katman KARARLAŞTIRILMAMIŞ:** §2-M port envanteri **9 satır**, kontrol 2/3 · `RateLimitProblemFactory` · **`users` kalıcılığı** için **satır yok**. ADR 0001 *"handler"*ı **iki anlamda** kullanıyor (satır 27 CQRS handler = Application; satır 80 K-H1 *"Api endpoint/handler"* = Api). **`ProblemDetails.cs` birebir: `namespace Microsoft.AspNetCore.Mvc;`** ⇒ dal (a)'da **M32b'nin baseline'ı kırmızı doğar**; dal (b)'de K3-J3'ün bağlayıcı sırası iki katmana yayılır. Kapı-3'ün B9'uyla (okuma yönü) **aynı sınıf**.
  5. **AES-256-GCM nonce üretimi/tekilliği HİÇ yazılmamış:** `nonce` yalnız satır 202 (düzen) ve 548 (**CSRF** nonce'u, başka şey); `grep RandomNumberGenerator` ⇒ **0**; **AAD yok**. **`AesGcm.Encrypt(ReadOnlySpan<byte> nonce, …)` — nonce ÇAĞIRANDAN gelir** ⇒ K3-K2'nin *"primitifi yazmayız"* muafiyeti **geçmez**, nonce seçimi belgenin kendi tanımıyla **AKIŞTIR**. `K_rt` tek ve kalıcı (K3-I4). **A1 (MAJÖR) ↔ A2 (BLOKER) çelişkisi ölçümle A2 lehine kapandı.**
  6. **K3-B6'nın e-posta ayağı kapısız:** M31'in mutasyonu *"**parola** uzunluk doğrulaması kaldırılır"* ⇒ 4. ayak (e-posta formatı) **mutasyona duyarsız**; **254 sınırının assert'i HİÇ YOK** (`grep 254` ⇒ kural + **iki kapanma iddiası**, ölçüm satırı yok). Muafiyet §3.1'den **çıkarıldığı** için kalem artık **ne kapılı ne beyanlı** ⇒ v3'ten kötü.
  7. **§3.2(5)'in `FakeTimeProvider`'ı hız sınırını İLERLETEMEZ:** **`FixedWindowRateLimiter` (runtime 9.0)** `Stopwatch.GetTimestamp()` + `Timer` kullanıyor, **`TimeProvider`'dan beslenmiyor**. Kontrol 1 tek partition (`TestHost`'ta `RemoteIpAddress` `null`) ⇒ paylaşılan fabrikada M23'ün 31 isteği + M11 + M31'in `/register`'ı **aynı kovayı** doldurur ⇒ M41(2) ve M46'nın **negatif** assert'leri sıra bağımlı kırılır. **§3.2 izolasyonu pinlemiyor.**
  8. **M22 (satır 696) ölü tuzak:** önkoşul yalnız IP kuruyor, **e-postaya tek kelime yok**; K3-J3 sırasında (satır 465-469) kontrol 2 kontrol 3'ten **önce**; kontrol 3 reddi `≥3×ProcessorCount+1 (≥7)` ister ⇒ **6.** istek `limit=="email"` ⇒ `"concurrency"` assert'i kırılır. **M23'ün önkoşulu *"her istekte FARKLI e-posta"* diyor, M22'ninki demiyor** — asimetri kusurun kanıtı. **v4'ün kendi yeni kapısı (M41) M22'yi geriye dönük bozdu.**
  9. **[RED-TEAM'İN YENİ BLOKERİ] Testlerin `Momentum:MasterKey`'i nereden alacağı YAZILMAMIŞ:** §3.2(1) `WebApplicationFactory<Program>` **zorunlu** ⇒ gerçek `Program` boot ⇒ **K3-I2 fail-fast koşar**. Üç kaynak da kapalı: user-secrets **klonla gelmez** (belgenin kendi cümlesi, satır 371) · bootstrap **konteyner giriş betiğinde** · ortam değişkeni **hiçbir yerde yazılı değil**. ⇒ **`TS`+`TC` etiketli ~34 mutantın baseline'ı temiz klonda kırmızı doğar** = §3.2(8)'in ihlali. *(Dürüst muhalefet kayda geçti: "fixture ayrıntısı" savunması ana oturumda **kabul edilmedi** — §3.2 iki ikameyi adıyla yasaklarken sırrın kaynağını atlıyor.)*
- **BAŞLICA MAJÖRLER:** AAD yok · **`+` kaçışının gerçek biçimi yazılmamış** (satır 195 aynı karakteri iki kez yazan bir **totoloji**; belgede `u002` ⇒ 0 eşleşme ⇒ M28'in taraması **hedefsiz**) · 60 sn penceresinin **K16-b'ye yanlış atfı** (sahibi **K14-a**) · `Retry-After` kapısı M11/M41'de **yok** · M40'ın mutasyon biçimi pinsiz + süpürme periyodu ölçülmüyor · **NIST SP 800-63B-4 §3.2.2'nin `SHALL`'ı** (ardışık başarısızlıkta ≤100 ⇒ authenticator devre dışı) **karşılanmıyor ve adlandırılmış sapma olarak yazılmamış — A turunun üçü de kaçırdı** · `/refresh`'te **CSRF doğrulamasının tüketim `UPDATE`'ine göre sırası yazılmamış** (aile düşürebilir) · M41(2)/M43(3)/M31(4)/M36'nın 5 assert'i kendi mutasyonları altında **ölmüyor** · M42'nin mutasyonu **bileşik** + `KON` gözlem yüzeyiyle çelişiyor · **CI yok** ama `KON` CI job'ı varsayıyor · HKDF `info` **bayt kodlaması** pinsiz · HKDF-Extract atlama gerekçesi **zorlanmıyor** · anahtar **rotasyonu** yarım · CSRF nonce entropi/ömür pinsiz.
- **MİNÖRLERİN EN ÖNCELİKLİSİ:** **mutant tablosu satır 715'te bölünmüş, ikinci tablonun BAŞLIK SATIRI YOK** ⇒ GFM'de **render olmuyor**; render olmayan blok tam olarak **v4'ün bütün yeni kapılarıdır (M32b · M32c · M40–M48 = 11 satır)** ve bu bir **portfolyo belgesidir**. (Bilgi kaybı yok ⇒ minör.)
- **DÜRÜSTLÜK — KIRILAMAYAN YERLER:** **DIŞ ATIFLARIN HEPSİ SAĞLAM** (23 birincil-kaynak ölçümü: NIST ×2, RFC ×4, OWASP ×2, WHATWG, dotnet kaynağı ×8, NuGet, ADR 0001 — **hiçbiri yanlış çıkmadı**; v3'ün NIST hatasının muadili YOK) · **K14-a × K15-a birleşimi yapısal olarak DOĞRU** (sonsuz zincir kurulamadı ⇒ B1 gerçekten kapandı) · **M12** `Base64Url`+ham bayt pini `successor_secret_enc` ile **çelişmiyor** · **M8b** B4'ü gerçekten kapattı (dosya sistemi assert'i §3.2(7) ile **uyuşuyor**) · **M44** turun en temiz yeni kapısı · **M33a/M33b** B6'yı kapattı · **M48** Ma-7'yi kapattı · saat ekseni (M17/M27/M29/M30) sağlam · **`FakeTimeProvider` × gerçek Postgres çelişkisi YOK** (süpürme SQL'i uygulamadan gelen `@now`'a bağlı — yazarın şüphe listesindeki kalem **temiz çıktı**) · **ADR 0001 K-H1 alıntısı karakter karakter doğru ⇒ Ma-8 temiz** · **§0.4 dürüst** (sessizce düzeltilmiş iddia yok) · **kapsam kayması YOK** · **mutant numara bütünlüğü TAM** (M1–M48; eksikler tam olarak M2·M3·M9·M10·M20 = 0004 rezervi; M13 VOID; **K16-d pini tutuyor**) · **kodlama TAM** (U+FFFD 0, mojibake 0) · **`[DOĞRULANMADI]` doktrini yine dürüst** (2 canlı etiket, ikisi de gerçekten ölçülemez; **etiketle geçiştirilmiş kalem bulunamadı**).
- **⚠ HAFIZANIN KENDİ SAPMASI (ADR'de değil, BURADA — düzeltiliyor):** K16-d satırı `M8c` diyor ve *"8 yeni kapı"* sayıyor; **ADR `M8c`'yi düşürmüş ve "dokuz" diyor (M40–M48 = 9 ✓)**. Ayrıca K16 satırı *"YENİ MUTANTLAR (11)"* deyip **14 kalem** sayıyor. **Doğrusu: yeni mekanizma kapıları M40–M48 = 9; toplam yeni mutant kalemi (harfli ayaklar + DART dâhil) 14.**
- **ÖLÇÜLEMEYENLER:** **`tests/`·`src/`·`Dockerfile`·compose·giriş betiği denetim ortamına BAĞLI DEĞİLDİ** ⇒ §3.2'nin mevcut test altyapısıyla uyumu **ölçülemedi** (ölçülecek 6 kalem raporun §6'sında tek tek yazılı; başlıcaları: `Program`'ın `WebApplicationFactory<Program>` için görünürlüğü · mevcut testlerde artık **YASAK** olan `TestAuthHandler` kullanımı · `FakeTimeProvider` paketinin lisans+CVE kapısı) · `dotnet` SDK yok ⇒ `+` kaçış dizisi **koşularak** ölçülemedi · Argon2id 270 ms · WebKit `__Host-` · `flutter_secure_storage` Windows yöntemi · **`GOREV-slice-3c-auth` spec'i hâlâ yok ⇒ *"spec'te çözülür"* savunması bu turda da KABUL EDİLMEDİ.**
- **🔑 KÖK NEDEN TESPİTİ [v5 yazılmadan önce karara bağlanmalı]:** bu turun **9 blokerinden 5'i** (B-1, B-3, B-5, B-6, B-9) tek kaynaktan doğuyor — **v4, bir blokeri kapatırken doğan YENİ mekanizmayı kapıya bağlamayı ya da §3.1'de beyan etmeyi sistematik olarak atlıyor ve kapanma tablolarında o kalemi "yapılmış" ilan ediyor.** Bu **üçüncü** turdur ki aynı kök neden bloker üretiyor (kapı-2 #15 → kapı-3 B7 → kapı-4 B-3+B-6). **İki bloker (B-1, B-8) ise "kardeş kalem güncellendi, kendisi unutuldu" sınıfındandır** ve bu doğrudan **boyutun** sonucudur (kontrol 1 sayısı **7 ayrı yerde** tekrarlanıyor). **Yapısal öneri:** (1) §3.1'in tamlık cümlesi ya geri çekilir ya **mekanik olarak** (araçla) doğrulanır — araç önce **altın kümede kendini kanıtlar**; (2) tekrarlanan sayılar **tek kanonik tabloya** çekilir, gövde **atıf yapar, kopyalamaz** (B-1 ve B-8 bu kuralla doğamazdı); (3) §0'ın kapanma tabloları ve *"v3'te şöyleydi"* anlatıları **KANIT'a taşınır** (belgenin ≈%12-15'i saf tekrar).
- **AĞAÇ DURUMU (bu oturumdan sonra):** yalnızca **`KANIT/adr-0003/kapi-4-denetim-raporu.md`** eklendi (additive; `?? KANIT/adr-0003/` zaten untracked) + bu checkpoint. **ADR'ye, ODEV'e, koda DOKUNULMADI. Commit Onur'da; PUSH YASAK.**
- **Sıradaki ilk iş:** (1) **Onur 9 blokerin çatallarını kilitlesin (K18)** — en ağır beşi: **B-4** handler hangi katman · **B-5** nonce üretimi + AAD · **B-9** testlerin anahtar kaynağı · **B-2** küresel limiter var mı · **B-3** §3.1'in tamlık cümlesi (geri çekilsin mi, araçla mı doğrulansın). (2) **ADR 0003 v5 — TEMİZ VE AYRI OTURUMDA** (dolu bağlamda ADR yazma yasağı). (3) v5'e **5. tur bağımsız kapı** (K13-a). (4) Temiz çıkınca Onur kilitler → **AYRI OTURUM: ADR 0004** (D-1..D-7; yeni mutantlar **M50**'den). (5) Sonra `GOREV-slice-3c-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.
- **[TAKVİM ÖLÇÜMÜ — K13-a gereği raporlanıyor, K12-b gereği KESME ÖNERİSİNE ÇEVRİLMİYOR]** ADR 0003 için harcanan: **4 yazım turu (v1/v2/v3/v4) + 4 kapı turu**. Kalan: v5 yazımı (≥1 oturum) + 5. kapı (≥1 oturum) + olası 6. tur + ADR 0004 (1 yazım + ≥1 kapı) + spec + build + doğrulama. **ODEV §3.1'in `slice-3a-auth` penceresi (21-23 Tem) çoktan aşıldı ve 15 Ağu hedefi ağır baskı altında** — ama K12-b yürürlükte: **ölçüm raporlanır, kapsam kesilmez.**

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 19: **K16 — v4 YAZIM OTURUMU; ÜÇ YENİ ÇATAL KİLİTLENDİ**)

- **Protokol koşuldu (oturum 19):** `docs/ODEV.md` (tam, 171 satır) + `CLAUDE.md` (tam, 33 satır) + bu dosyanın en üst iki checkpoint'i (K15 + K14) + **`KANIT/adr-0003/kapi-3-denetim-raporu.md` (tam, 227 satır)** + **ADR 0003 v3 (645 satır, TAMAMI)** + **ADR 0001'in K-A1/K-H1/K-H2 satırları (birebir)** okundu. **`git status` kaynaktan ölçüldü:** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/…v1…` · `?? arsiv/…v2…` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · **ahead 15, push yok** — devir notuyla **birebir** uyuştu.
- **🟢 KAPI-3 RAPORUNUN BİR "ÖLÇÜLEMEYEN"İ YANLIŞ ÇIKTI — ANA OTURUMDA ÖLÇÜLDÜ.** Rapor §5 birebir *"bağlı klasörde `docs/ADR/` yalnız `0003`'ü içeriyor"* diyordu. **YANLIŞ:** `docs/ADR/0001-genel-mimari.md` (**14.137 bayt**) ve `docs/ADR/0002-senkron-mekanigi.md` (**31.402 bayt**) diskte duruyor ve **ikisi de git-takipli** (`git status` yalnız `0003`'ü `??` gösteriyor). ⇒ **Ma-8'in "kanıtlanamadı" gerekçesi düştü; v4 K-H1'i birebir alıntılayarak kapatabilir.** K-H1 birebir: *"**NetArchTest — gerçekten ihlal-edilebilir kurallar:** Application ⊥ Infrastructure · Api endpoint/handler ⊥ Infrastructure somut tipleri (composition root dışında) · Domain ⊥ EF/ASP.NET/Npgsql namespace'leri. **Her kural commit'li negatif/mutant testle ısırdığını kanıtlar.**"* ⇒ (1) §2-M'nin **Domain** ayağı mevcut ailenin uzantısıdır; (2) **Application** ayağı (`Microsoft.AspNetCore.*`, `Microsoft.IdentityModel.*`) ve `Konscious.*` **YENİDİR**; (3) K-H1 **her** yeni kural için mutant ZORUNLU kılar ⇒ §3.1'in *"yalnız `Konscious.*` kuralı yenidir"* muafiyeti **K-H1 ihlalidir** ve v4'te kapanır.
- **[KARAR — Onur, 25 Tem 2026] K16-a: Ma-9 → PAROLA ASGARİ UZUNLUK 10 → **15 KARAKTER**. K14-g DEĞİŞTİRİLDİ.** NIST SP 800-63B-4 birebir *"SHALL … single-factor … minimum of 15 characters"* (kapı-3'te kaynaktan ölçüldü) ⇒ belgenin *"NIST çizgisi"* atfı ancak 15'te dürüst olur. Bedeli **adlandırılır ve telafi edilir:** demo sırasında 15 karakter yazma sürtünmesi ⇒ README'de **hazır demo hesabı + seed**. **M31'e sınır-değer ayağı:** 14 karakter `400`, 15 karakter `201`. *Reddedilenler [adlandırılmış]:* 10'da kalıp adlandırılmış sapma yazmak (doktrine uygun ama denetçi *"neden sapıyorsun"*u yeniden sorar) · 12 karakter (hiçbir standartta karşılığı yok ⇒ keyfi, savunması en zayıf).
- **[KARAR — Onur, 25 Tem 2026] K16-b: B2+B3 → `/refresh` AYRI POLİTİKAYA ALINIR + TAVANLAR YÜKSELİR. K14-i DEĞİŞTİRİLDİ (zaten "Onur kilidi değil" diye işaretliydi).** Yeni sayılar: **`/login`+`/register` = 30 istek/5 dk** (Argon2 yolu) · **`/refresh` = 120 istek/5 dk** (Argon2 KOŞMAZ; F5 yolu — K3-L2 gereği her F5 bir `/refresh`'tir) · **`/logout`+`/logout-all` = 60 istek/5 dk**. *Gerekçe:* kontrol 1 tek konteynerde **fiilen küresel** bir tavandır (B2, ölçüldü) ⇒ eski 10/5dk iki kullanıcılı işbirliği demosunu ODEV §2'nin ortasında kesiyordu. **Argon2'yi koruyan asıl kontrol 3'tür (eşzamanlılık), kontrol 1 değil** — bu v4'te açıkça yazılır. **+ istemci sözleşmesine `429` dalı (M-L9, DART).** *Reddedilenler:* yalnız `/refresh`'i ayırıp sayıları korumak (10 küresel tavan iki değerlendirici aynı anda kayıt+giriş denerken hâlâ ısırır) · 60/240/120 (kontrol 1'in DoS anlamı sembolikleşir).
- **[KARAR — Onur, 25 Tem 2026] K16-c: B8 + K15-a → **TEK KÖK ANAHTAR + HKDF-SHA256 İLE ÜÇ AMAÇ-BAĞLI ALT ANAHTAR**.** `Momentum:MasterKey` (≥32 bayt) tek kaynaktır; `jwt-sign` · `csrf-hmac` · `rt-successor-enc` alt anahtarları **HKDF-Expand** ile `info` etiketinden türetilir. ⇒ **K15-a'nın "üçüncü bir sır doğmasın" kısıtı sağlanır**, anahtar-amaç ayrımı korunur, **K3-I2 fail-fast tek anahtarı kontrol eder**, **K3-I3 bootstrap'ı tek dosya üretir**, K3-I3'ün *"restart'ta oturumlar düşmez"* vaadi **her üç kullanım için birden** geçerli olur. *Reddedilenler [adlandırılmış]:* üç ayrı anahtar (en açık ayrım ama üç fail-fast + üç bootstrap + üç mutant ve K15-a'nın kısıtının reddi) · iki anahtar / CSRF+şifreleme aynı sırrı paylaşır (anahtar-amaç ayrımını kısmen bozar, denetçi bulur).
- **[COWORK KARARI — Onur onayına açık, dürüstçe kayda geçiyor] K16-d: MUTANT NUMARALANDIRMA PİNİ DEĞİŞTİ.** v4'ün kapatması gereken bulgular **8 gerçekten yeni mekanizma kapısı** doğuruyor. **Karar:** gerçek ikinci-ayaklar **harfle** yazılır (`M33a/M33b`, `M8c`, `M28`'in sinyali düzeltilir), **yeni mekanizmalar `M40`'tan numaralanır** ve **ADR 0004'ün yeni mutantları artık `M50`'den başlar** (v3'ün `M40` pini geçersizdir). *Gerekçe:* 0004 henüz **hiçbir numarayı tüketmedi** (M2·M3·M9·M10·M20 rezervdir, yenileri adsızdı) ⇒ pin taşımak bedelsiz; alternatif (her şeyi harfe sıkıştırmak) kontrol-2 penceresi gibi **kendi başına mekanizma** olan kapıları sahte bir ayak gibi gösterirdi.
- **✅ [ADR 0003 v4 YAZILDI — TASLAK, KİLİTLİ DEĞİL] `docs/ADR/0003-kimlik-cekirdegi.md`** (**170.261 bayt · 885 satır · UTF-8 GEÇERLİ, BOM yok · 10.562 Türkçe harf · mojibake 0 · U+FFFD 0 — hepsi Onur'un diskinden, `iconv`/`wc`/`grep` ile kaynaktan ölçüldü**). **v3 SİLİNMEDİ, arşive TAŞINDI:** `arsiv/0003-kimlik-cekirdegi-TASLAK-v3-2026-07-25.md` (95.967 bayt). Arşivde artık **üç** taslak var (v1+v2+v3); karar izi tam.
  - **⚠ DÜRÜST BÜYÜME ÖLÇÜMÜ:** v1 = 24,7 K · v2 = 43,9 K (×1,8) · v3 = 96,0 K (×2,2) · **v4 = 170,3 K (v3'ün ×1,77'si, v1'in ×6,9'u)**. **Büyüme dördüncü kez durmadı.** Kaynağı: 9 bloker + ~20 majör kapatma + **iki yeni bölüm** (§0.4 geri çekilen iddialar · **§3.2 koşum sözleşmesi**) + **§2-I'nin yeniden yazımı** (K3-I4 HKDF) + mutant tablosunun **34 → 45 satıra** çıkması. **Bu, K11-h'nin "her belge küçük ⇒ tek turda geçer" gerekçesinin dördüncü kez tutmadığıdır** — K13-a bunu zaten kabul etmişti.
  - **YENİ MUTANTLAR (11):** M32b (`Microsoft.AspNetCore.*` Application) · M32c (`Microsoft.IdentityModel.*` Application) · M33a/M33b (M33'ün bölünmesi) · **M40** (`successor_secret_enc` süpürücüsü) · **M41** (kontrol 2 = e-posta penceresi) · **M42** (CSRF/kök anahtar kalıcılığı, `KON`) · **M43** (X-Client-Kind okuma yönü) · **M44** (dal önceliği) · **M45** (halef INSERT atomikliği) · **M46** (`UseRateLimiter` sırası) · **M47** (`/logout`'ta `__Host-mrt` silme) · **M48** (`iss`/`aud`) · **M-L9** (istemcide `429` dalı, DART).
  - **DÜZELTİLEN MUTANTLAR (13):** M4 (iki ayak iki commit) · M6b (mutasyon kapsamı) · **M8b (seviye `B` → `KON`)** · M12 (Base64Url + ham bayt) · **M19 (seviye `D` → `TS` + `WebApplicationFactory` pini)** · M22 (test-only middleware önkoşulu) · M23 (11→31, K16-b) · M24 (gerçek `AddJwtBearer` pini) · M28 (özyinelemeli JSON taraması) · M30 (pencere İÇİNDE pini) · M31 (15/14 sınır değeri + 4 ayak) · M32 (gerçek tip kullanımı) · M35 (aile bağı ayağı) · M36 (çoklu assert) · M37 (`traceId` hariç) · M39 (satır sayısı değişmezi).
  - **YENİ SEVİYELER:** **`KON`** (konteyner/E2E — M8b, M42) ve **`DART-WEB`** (M-L5'in web ayağı). **NUMARA PİNİ: 0004 artık `M50`'den başlar (K16-d).** M1–M48 aralığında kayıp yok (M2·M3·M9·M10·M20 = 0004 rezervi, M13 = VOID) — **numara bütünlüğü kaynaktan doğrulandı.**
  - **KAPANAN `[DOĞRULANMADI]` (1):** `ConcurrencyLease` `MetadataName.RetryAfter` **TAŞIMIYOR**, `FixedWindowLease` **TAŞIYOR** ⇒ §6 Risk #12 kapandı, K3-J4'ün koşulluluğu kalktı. **AÇIK KALAN (2):** `flutter_secure_storage`'ın Windows şifreleme yöntemi · **WebKit/Safari'nin `http://localhost` üzerinde `Secure`/`__Host-` davranışı (§6 Risk #15 olarak yazıldı)**.
  - **YENİ ADLANDIRILMIŞ RİSKLER (3):** #13 DB sızıntısı **en kötü 120 sn**'lik kullanılabilir token verir (K15-a'nın bedeli) · #14 kontrol 1 **küresel** ⇒ tek istemci tavanı tüketebilir (B2'nin bedeli) · #15 WebKit `[DOĞRULANMADI]`. **Ayrıca RFC 9700 adlandırma borcu §6 #16'da ödendi.**
  - **AĞAÇ DURUMU (kaynaktan ölçüldü):** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/…v1/v2/v3…` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · **Commit Onur'da; PUSH YASAK.** ⚠ **Sandbox `.git/index.lock`'a yazamıyor** (`Operation not permitted`) ⇒ bu oturumdan `git add`/`commit` **yapılamaz**; `git status` okunabiliyor. Commit zaten Onur'daydı, engel doğurmadı.
  - **⚠ BU OTURUM v4'Ü DENETLEMEDİ VE DENETLEYEMEZ.** Yapılan tek kontrol **öz-doğrulamadır** (kodlama · bayt/satır · Türkçe harf · mojibake · mutant numara bütünlüğü · bayat referans taraması: `M40'tan` · `K14-i sayısı` · `TASLAK v3` · eski hız-sınırı sayıları). **Bunlar bütünlük ölçümüdür, KAPI DEĞİLDİR.**
- **Sıradaki ilk iş:** (1) **v4'e 4. TUR BAĞIMSIZ KAPI — AYRI VE TEMİZ OTURUM** (K13-a: bloker sıfırlanana kadar tur; üreten ≠ denetleyen — **bu oturum v4'ü yazdı, denetleyemez**). (2) Temiz çıkınca Onur kilitler → **AYRI OTURUM: ADR 0004** (D-1..D-7; yeni mutantlar **M50**'den). (3) Sonra `GOREV-slice-3c-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 18: **K15 — ADR 0003 v3'E ÜÇÜNCÜ BAĞIMSIZ KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ**)

- **Protokol koşuldu (oturum 18):** `docs/ODEV.md` (tam, 171 satır) + `CLAUDE.md` (tam, 33 satır) + bu dosyanın en üst iki checkpoint'i (K14 + K13) + **ADR 0003 v3 (645 satır, TAMAMI)** + **`KANIT/adr-0003/kapi-2-denetim-raporu.md`** okundu. **`git status` kaynaktan ölçüldü:** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/…v1…` · `?? arsiv/…v2…` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · **ahead 15, push yok** — devir notuyla **birebir** uyuştu.
- **KAPI KURULUMU (üreten ≠ denetleyen korundu — oturum 17 YAZDI, bu oturum yalnız DENETLEDİ, ADR'ye tek satır dokunmadı):** A turu = üç bağımsız denetçi **paralel** (mimari/tutarlılık · ölçüm/gerçeklik · mutant/kapı) → B turu = **RED-TEAM EN SON**, A'nın bulgularını **çürütmekle** görevli → ana oturum = adjudikasyon + **karar-taşıyıcı iddiaların kaynaktan yeniden ölçümü**.
- **🔴 HÜKÜM: KİLİTLENEMEZ. Dört denetçi de bağımsız olarak aynı hükme vardı. 9 BLOKER + ~20 majör. Tam rapor: `KANIT/adr-0003/kapi-3-denetim-raporu.md`** (**32.302 bayt · 227 satır · UTF-8, BOM yok · 2.045 Türkçe harf · mojibake 0 · U+FFFD 0 — Onur'un diskinden kaynaktan ölçüldü**). **Bu 3. turdur (K13-a: bloker sıfırlanana kadar tur; tur sayısı raporlanır, sınırlanmaz).**
- **RED-TEAM A TURUNU KISMEN ÇÜRÜTTÜ (v2 turundan farklı — o turda hiçbiri düşmemişti):** A'nın 20 blokerinin **7'si ayakta**, **9'u majöre indirildi/daraltıldı**, **4'ü ÇÜRÜTÜLDÜ**; red-team **2 yeni bloker** getirdi ⇒ toplam **9**.
- **✅ [ANA OTURUMUN KENDİ ÖLÇÜMLERİ — alt-ajan beyanı AKTARILMADI; hükmü taşıyan 9 iddia ana oturumda yeniden ölçüldü]**
  1. **🔴 [GERÇEK KOŞU — ONUR'UN MAKİNESİNDE] K14-e'nin `RemoteIpAddress` İDDİASI YANLIŞ ÇIKTI.** Docker 29.6.1 / compose v5.3.0 kurulu; `docker run --rm -d -p 18080:80 nginx:alpine` ile üç yoldan istek atıldı: **`http://localhost` → `172.17.0.1` · `http://127.0.0.1` → `172.17.0.1` · `http://192.168.0.41` (LAN) → `172.17.0.1`**. Konteyner içi: `default via 172.17.0.1 dev eth0`. **Üçünde de köprü ağ geçidi; gerçek istemci IP'si hiçbirinde yok** (LAN yolu bile kurtarmıyor — Docker Desktop/WSL2). Resmî `dockerd` referansı: `--userland-proxy` = *"Use userland proxy for **loopback traffic**"*, varsayılan **`true`**. ⇒ **bloker #3 dal (b) kapanmadı, TAŞINDI; K14-e'nin reverse-proxy'yi reddetme gerekçesinin üçte biri hatalı bir olguya dayanıyor.**
  2. **K14-a inşa edilemez:** belgenin tamamı tarandı — K3-C2 *"DB'ye yalnız SHA-256 özeti yazılır"* + şemada ham kolon yok + **M12 bunu ayrıca zorluyor** ⇒ *"kayıtlı halef **aynen** döndürülür"* için sunucuda değer **YOK**.
  3. `OnRejected`/`RejectionStatusCode` **`RateLimiterOptions` üyesidir**, yalnız `RateLimitingMiddleware` çağırır ⇒ K14-f'in handler'a taşıdığı kontrol 2/3'ü **kapsamaz**.
  4. `HostingHostBuilderExtensions`: `ValidateScopes = isDevelopment, ValidateOnBuild = isDevelopment`.
  5. **`WebApplicationFactory` üç ayrı yolda `UseEnvironment(Environments.Development)` çağırıyor** ⇒ *"M19 `Production`'da kör"* bulgusu **ÇÜRÜTÜLDÜ**.
  6. `MapFallbackToFile` → `context.Request.Path = "/" + filePath;` · `context.SetEndpoint(null);` · **kendi `app.UseStaticFiles()`'ını kurar**; kalıp **`{*path:nonfile}`**.
  7. `AllowedBmpCodePointsBitmap.ForbidHtmlCharacters()` → **`ForbidChar('+')`** ⇒ JSON gövdesinde `+` → `+`.
  8. `DefaultProblemDetailsWriter` **koşulsuz** `Extensions["traceId"]` yazıyor.
  9. **NIST SP 800-63B-4 birebir: tek faktör için `SHALL` minimum 15 karakter** (MFA bileşeni 8) ⇒ belgenin *"NIST çizgisi ≥10"* beyanı yanlış (karmaşıklık-yok ve ≤128 ayakları doğru).
- **AYAKTA KALAN 9 BLOKER:** **B1** K14-a **inşa edilemez** (ham halef yok ⇒ **bloker #1 kapanmadı**; M29/M30/M1 inşa edilemez davranışa çıpalı) · **B2** `RemoteIpAddress` (yukarıda, **ölçüldü**) · **B3** `F5 = /refresh` × tek partition × istemci sözleşmesinde **`429` dalı yok** ⇒ iki-kullanıcılı işbirliği demosunda tanımsız durum (ODEV §2 vuruluyor, kaynak: red-team) · **B4** **M8b öldürülemez** (mutasyon shell giriş betiğinde, seviye `B`; belgenin kendi cümlesi: *"bu ayak olmadan bootstrap'ın kendisi bir açıktır"*) · **B5** **M28 kör** (`+` kaçışı, ≈%49 flaky; **token kodlaması hiç pinlenmemiş** — M12'yi de vuruyor) · **B6** **M33 ayırt edici değil + baseline yok** (`wwwroot/index.html` bu dilimde yok) · **B7** **kontrol 2 (e-posta penceresi) TÜMÜYLE KAPISIZ ve §3.1'de de yok** ⇒ K14-f çatalının tamamı kanıtsız · **B8** **CSRF HMAC anahtarının varlığı/kaynağı/bootstrap'ı yazılmamış** (K3-I3'ün *"restart'ta oturumlar düşmez"* vaadi web ayağında kanıtsız) · **B9** **`X-Client-Kind`'ın OKUMA yönü yazılmamış** ⇒ güvenlik modunu istemci seçiyor (K14-f'in adlandırarak reddettiği hata sınıfı; bugün CORS preflight'ı yüzünden **sömürülemiyor** — karar boşluğu olarak bloker).
- **[DÜRÜST KAPATMA SAYIMI] v3, 15 blokerin 10'unu kapattı — bunun yalnız 5'i TEMİZ.** Temiz: #9 · #10 (soğuk açılış, **belgenin en iyi yeni maddesi**) · #12 · #14 · #15. Esasta kapandı ama yeni kusur doğurdu: #2 (dal önceliği yok) · #4 (anahtar + aile bağı kapısı yok) · #5 (**kapısız**) · #6 (okuma yönü yok, M28 kör) · #11 (NIST atfı yanlış, M21'in bir ayağı öldü). **KAPANMADI: #1 · #3 dal (b) · #3 dal (a)'nın kapısı (M33) · #7'nin handler ayağı · #8'in kapısı (M8b) + #13.**
- **RED-TEAM'İN DÜŞÜRDÜKLERİ (dürüstlük — bulgu şişirmesi engellendi):** *"M19 `Production`'da kör"* (WebApplicationFactory ölçümü) · *"M22: TS'te farklı IP kurulamaz"* (test-only middleware ile kurulur) · *"M11/M23 null IP yüzünden ölü tuzak"* (ikisi de null IP altında ısırır) · *"dal önceliği M27'nin 30 gününü deliyor"* (pencere `consumed_at`'e çıpalı) · *"M24: UserId'yi dereference eden uç yok"* (`/logout-all` `sub`'a dayanıyor) · *"K3-C1'in 15 dk ömrü beyansız"* (§3.1'de beyanlı) · *"K14-a RFC 9700 `MUST`'ını ihlal ediyor"* → **MİNÖR** (RFC'de `MUST` yalnız *"tespit yöntemlerinden birini kullan"* için; iptal cümlesi betimleyici ⇒ ihlal yok, tek satırlık **adlandırma borcu**) · A3'ün kendi **M16/HS512** hipotezi (`SymmetricSignatureProvider` yalnız 128 bit asgarisi uygular ⇒ **M16 SAĞLAM**).
- **DÜRÜSTLÜK — KIRILAMAYAN YERLER:** **K14-a'nın v1 zarafet penceresinden ayrımı YAPISAL OLARAK DOĞRU** (sonsuz zincir kurulamadı; sorun tasarım değil **saklama sonucu**) · K3-C6(1)'in yüklemi **TAM** (bloker #2 esasta kapandı) · **K3-L8'in uçak modu akışı adım adım yürütüldü ve SAĞLAM** (yalnız B3'ün `429` dalında kırılıyor) · `__Host-` yapısal cevabı doğru kurulmuş (rfc6265bis §5.7 + MDN + OWASP) · `SameSite` × alt alan teşhisi + §5.2.1 ayağı temiz · `MapInboundClaims`'in üç yüzeyi birebir doğru · MS Learn alıntısı birebir · **M5/M7/M12/M15/M16/M23/M26/M27/M29/M30/M31/M34/M36/M38 ısırıyor** · **`[DOĞRULANMADI]` doktrini yine kazandı** (`ConcurrencyLease` `RetryAfter` **taşımıyor** — ölçüldü; koşullu yazım doğru çıktı ⇒ §6 Risk #12 v4'te kapatılabilir) · kapsam kayması yok · **numara bütünlüğü TAM** (M1–M39, kayıp/çift yok) · 0003/0004 bölünme sınırı sağlam.
- **ÖLÇÜLEMEYENLER:** **ADR 0001'in K-A1/K-H1 tam metni** — `docs/ADR/` yalnız `0003`'ü içeriyor ⇒ §2-M'nin NetArchTest kurallarının "yeni mi" sorusu settle edilemedi; **v4 bunu 0001'den birebir alıntıyla kanıtlamalı** · moby `docker-proxy` kaynak satırı (fetch 404 — **ama B2 gerçek koşuyla ölçüldü, sonuç bundan bağımsız**) · WebKit/Safari'nin `http://localhost` `__Host-` davranışı · **builder'ın koşum tercihleri ADR'de yazılı değil** (M37/M19'un kesin sonucu buna bağlı — başlı başına eksiklik) · yerel koşu beyanları (Argon2 270 ms vb.).
- **AĞAÇ DURUMU (bu oturumdan sonra):** yalnızca **`KANIT/adr-0003/kapi-3-denetim-raporu.md`** eklendi (additive; `?? KANIT/adr-0003/` zaten untracked). **ADR'ye, ODEV'e, koda dokunulmadı. Commit Onur'da; PUSH YASAK.**
- **[KARAR — Onur, 25 Tem 2026] K15-a: B1 → ŞİFRELİ HALEF KOLONU.** `refresh_tokens`'a **kısa-TTL'li `successor_secret_enc`** kolonu eklenir: halefin **ham değeri uygulama anahtarıyla şifreli** tutulur ve **60 sn sonra `NULL`**'lanır. **K14-a'nın semantiği AYNEN korunur** (yeni token üretilmez · pencere `T1.consumed_at`'e çıpalı · `expires_at` devralınır ⇒ sonsuz zincir hâlâ yapısal olarak imkânsız). *Reddedilenler [adlandırılmış]:* **dal (c)'nin yeniden yazımı** (yeni token üretilir ama halef olarak kaydedilir — şema değişmez ama *"yeni döndürme yapılmaz"* cümlesi düşer, koşul-2'nin semantiği ve M29/M30'un kill sinyalleri baştan yazılır, ailede satır birikir) · **K14-a'nın geri alınması** (telafisiz adlandırılmış sınır — RFC 9700 maliyet sayar ama ODEV §2'nin çevrimdışı vitrininin ortasında yeniden giriş ekranı üretir; K14-a tam da bunun için seçilmişti). **v4'ün yazması gerekenler:** şifreleme anahtarının kaynağı (**B8 ile birlikte çözülmeli** — üçüncü bir sır doğmamalı) · `NULL`'lama mekanizması (tembel mi, arka plan mı) · **`successor_secret_enc`'in 60 sn sonra gerçekten silindiğini ısırtan yeni mutant** · **yeni adlandırılmış risk:** *"DB sızıntısı 60 sn'lik bir pencerede kullanılabilir token verir"* (§6'ya madde) · M12'nin *"DB'de ham değer yok"* iddiasının bu kolonla **çelişmeyecek** biçimde yeniden yazılması.
- **[KARAR — Onur, 25 Tem 2026] K15-b: B2 → İDDİA GERİ ÇEKİLİR, SINIR ADLANDIRILIR. `K14-e DURUR.`** Tek konteyner + reverse-proxy yok kararı **korunur** (CORS ve `SameSite` gerekçeleri ölçümle sağlam); **yalnız yanlış çıkan ayak geri çekilir.** v4 şunu yazar: *"tek konteyner dağıtımında `RemoteIpAddress` **köprü ağ geçididir**; kontrol 1 **KÜRESEL** bir hız sınırıdır, IP-anahtarlı değildir"* — **adlandırılmış sınır**, ölçümüyle birlikte (`localhost`/`127.0.0.1`/LAN → hepsi `172.17.0.1`). *Reddedilenler [adlandırılmış]:* **reverse-proxy'ye dönüş** (gerçek IP gelir ama K14-e'nin adlandırarak reddettiği kapı yükü geri gelir: `UseForwardedHeaders` + `KnownProxies` + M11/M23'ün `X-Forwarded-For` testlerine taşınması; dağıtım tek birim olmaktan çıkar) · **IP anahtarının tamamen bırakılması** (en sade ama R2'nin kapısı olan **M23'ün konusu kalmaz** ve tek istemci tüm servisi doyurabilir). **v4'ün yazması gerekenler:** M11/M23'ün kill sinyalleri + `limit == "ip"` beyanı yeniden yazılır · **K14-i sayıları (10/5dk) çok istemcili demo için YÜKSELTİLİR** (B3 ile birlikte okunur) · K3-J5'in *"tavanı anlamlı kılan şey IP penceresidir"* cümlesi ve **§6 Risk #5 düzeltilir** · K3-J6'nın NAT paragrafına **Docker'ın kendi NAT'ı** eklenir.
- **Sıradaki ilk iş:** (1) **ADR 0003 v4 — TEMİZ VE AYRI OTURUMDA** (dolu bağlamda ADR yazma yasağı yürürlükte). 9 bloker + ~20 majör kapatılır; **iki ağır çatal K15-a/K15-b ile KİLİTLENDİ, v4 doğrudan yazmaya başlayabilir.** (2) v4'e **4. tur bağımsız kapı** (K13-a). (3) Temiz çıkınca Onur kilitler → **AYRI OTURUM: ADR 0004** (D-1..D-7; yeni mutantlar **M40**'tan). (4) Sonra `GOREV-slice-3c-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.
- **[TAKVİM ÖLÇÜMÜ — K13-a gereği raporlanıyor, K12-b gereği KESME ÖNERİSİNE ÇEVRİLMİYOR]** ADR 0003 için harcanan: 3 yazım turu (v1/v2/v3) + **3 kapı turu**. Kalan: v4 yazımı (≥1 oturum) + 4. kapı (≥1 oturum) + olası 5. tur + ADR 0004 (1 yazım + ≥1 kapı) + spec + build + doğrulama. **ODEV §3.1'in `slice-3a-auth` penceresi (21-23 Tem) çoktan aşıldı ve 15 Ağu hedefi baskı altında** — ama K12-b yürürlükte: **ölçüm raporlanır, kapsam kesilmez.**

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 17: **K14 — v3'ÜN DOKUZ KARAR ÇATALI KİLİTLENDİ**)

- **Protokol koşuldu (oturum 17):** `docs/ODEV.md` (tam, 170 satır) + `CLAUDE.md` (tam, 32 satır) + bu dosyanın en üst iki checkpoint'i (K13 + K12) + **`KANIT/adr-0003/kapi-2-denetim-raporu.md` (tam, 171 satır)** + **ADR 0003 v2 (311 satır, tamamı)** okundu. **`git status` kaynaktan ölçüldü: `## main...origin/main [ahead 15]` · ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/0003-…-v1-….md` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · push yok** — devir notuyla **birebir** uyuştu.
- **[COWORK SINIFLANDIRMASI] 15 blokerin 9'u MEKANİK** (karar gerektirmez, doğrudan yazılır): #2 `/refresh` yüklemi + 0-satır dalının üçe ayrılması · #4'ün `__Host-`/HMAC ayağı · #7 `429` override + `OnRejected` · #9 M-L5/M-L6/M-L7 devri · #10 aktif profil kaydı + ağ-hatası/401 ayrımı · #12 M16 + K3-C7 beyan düzeltmesi · #14 M17 saat ilerletme · #15 NetArchTest mutantı · K3-J5'in kapatılması. **Kalan 6 bloker + 3 kalem Onur kararı istedi; dokuzu da bu oturumda kilitlendi (K14-a…K14-i).**
- **[KARAR — Onur] K14-a: RT-B1 (kayıp yanıt) → SINIRLI REPLAY-IDEMPOTENCY.** Tüketilmiş `T1` yeniden sunulursa: **`T1.consumed_at + 60 sn` içindeyse VE `T1.replaced_by_id`'nin işaret ettiği halef HENÜZ TÜKETİLMEMİŞSE, kayıtlı halef AYNEN döndürülür; yeni döndürme yapılmaz.** *Gerekçe:* v1'in reddedilen zarafet penceresi **değildir** — pencere `T1`'e çıpalı, ömür uzamıyor, yeni token doğmuyor ⇒ R1'in sonsuz zinciri yapısal olarak imkânsız, reuse-detection ısırmaya devam ediyor. *Reddedilenler [adlandırılmış]:* **(b)** telafisiz adlandırılmış sınır — RFC 9700 §4.14.2 bunu maliyet sayar ama değerlendirici uçak modunu açıp kapattığında çevrimdışı vitrininin tam ortasında yeniden giriş ekranı görür (ODEV §2) · **(c)** `Idempotency-Key` başlığı — aynı sonucu yeni tablo + TTL + temizleme + kendi mutantlarıyla, daha pahalıya alır.
- **[KARAR — Onur] K14-b: RT-B4 (anahtar bootstrap) → COMPOSE İLK AÇILIŞTA RASTGELE DEV ANAHTARI ÜRETİR**, git-ignore'lu dosyaya yazar, sonraki açılışlar onu okur. **YALNIZ `Development`**; `Production` yolunda eksik anahtar hâlâ patlar. **M8 iki ayaklı olur.** *Gerekçe:* `docker compose up` tek başına çalışmalı (ODEV §2) ve repoya sabit sır girmemeli (kırmızı çizgi #1). *Reddedilenler:* `.env.example` + README adımı (en şeffaf ama tek komut yetmez) · repoda DEV-ONLY sabit anahtar (kod kalitesi ölçen değerlendirici repoda sır görür).
- **[KARAR — Onur] K14-c: BLOKER #6 (token taşıma kanalı) → `X-Client-Kind: web|native` BAŞLIĞI + JWT'ye `fid` TALEBİ.** Tek uç seti. **PAZARLIKSIZ:** web modunda yenileme token'ı yanıt gövdesinde **hiç görünmez** (yalnız `Set-Cookie`), native modunda çerez **hiç set edilmez**. `/logout` aileyi JWT'nin `fid` talebinden bulur ⇒ çerez/gövde bağımlılığı yok, K11-d korunur. **Yeni mutant M28.** *Reddedilenler:* ayrı alt yollar `/v1/auth/web/*` (uç sayısı ikiye katlanır, OpenAPI kontrat kapısı büyür) · her platformda çerez (K3-L1 düşer, mobilde çerez kalıcılığı kırılgan).
- **[KARAR — Onur] K14-d: Ma-5 (`security_stamp`) → ÖLÜ ALAN OLDUĞU BEYAN EDİLİR.** Kolon + talep durur, doğrulama yazılmaz; *"bugün ölü alan, bilinçli — ileride parola değişimi/anlık iptal eklenirse kanca hazır"* diye açıkça yazılır. *Gerekçe:* **K3-K3 zaten "anlık erişim-token'ı iptali (kara liste)" kalemini KAPSAM DIŞI ilan etmişti**; canlandırmak o kapsam kararını sessizce geri alırdı. ≤15 dk penceresi K3-C4'te zaten beyan edilmiş bir sınır. *Reddedilenler:* canlandırma (istek başına 1 DB okuması + kapsam genişlemesi) · tamamen kaldırma (ileriye kanca kalmaz, K3-C4 cümlesi de düzeltilmeli olurdu).
- **[KARAR — Onur] K14-e: BLOKER #3 (dağıtım) → API STATİK DOSYALARI SERVİS EDER, REVERSE-PROXY YOK.** Tek konteyner: Kestrel hem `/v1/*` API'sini hem Flutter web build'ini verir. `UseDefaultFiles`+`UseStaticFiles` auth middleware'inden **ÖNCE**, SPA fallback ucuna açık `AllowAnonymous`. **İki sonucu:** (a) `FallbackPolicy`'nin SPA derin linklerini 401'lemesi yapısal olarak biter · (b) **`RemoteIpAddress` GERÇEK istemci IP'sidir ⇒ `UseForwardedHeaders` hiç gerekmez ve K3-J2(1) yaşar** — bloker #3'ün ikinci ayağı (proxy arkasında tüm kullanıcıların tek partition'a düşmesi) doğmadan kapanır. *Reddedilenler:* reverse-proxy (daha gerçekçi üretim topolojisi ama `KnownProxies` yapılandırması + `X-Forwarded-For` enjekte eden testler zorunlu olurdu) · ikisini de destekle (kapı yükü ikiye katlanır, "hangisi kanıtlandı" sorusu açık kalır).
- **[KARAR — Onur] K14-f: BLOKER #5 (K3-J2(2) inşa edilemez) → E-POSTA LİMİTİ HANDLER İÇİNDE `PartitionedRateLimiter<string>`.** Middleware'den çıkarılır; `/login` handler'ının içinde DI'dan alınır (gövde zaten deserialize edilmiş ⇒ senkron partitioner sorunu doğmaz). **K3-J3'ün bağlayıcı sırası yeniden yazılır:** *"middleware: IP penceresi → handler: e-posta penceresi → eşzamanlılık limiti → Argon2"*. *Reddedilenler:* anahtarı başlıktan almak (**anahtarı istemci seçer ⇒ R2'nin kapattığı hata sınıfı geri gelir**) · K3-J2(2)'yi tamamen kaldırmak (tek hesaba yavaş parola denemesi tamamen sınırsız kalırdı).
- **[KARAR — Onur] K14-g: BLOKER #11 (parola/girdi politikası) → NIST SP 800-63B ÇİZGİSİ.** Asgari **10** karakter · **karmaşıklık kuralı YOK** (gerekçesiyle yazılır) · azami **128** (Argon2 DoS tavanı) · e-posta azami **254** (RFC 5321) + format doğrulaması. Bir mutant eklenir. *Reddedilenler:* kapsam dışı ilan etmek (azami uzunluk yoksa Argon2 DoS yüzeyi açık kalır) · klasik karmaşıklık kuralları (NIST/OWASP bu kalıbı önermiyor ⇒ güncel literatürü bilen değerlendiricide eksi sinyal).
- **[KARAR — Onur] K14-h: Mi-1 (ad çakışması) → YENİ DİLİM ADI = `slice-3c-auth`.** (`slice-3a` = 0002 K2-I3 sunucu materyalizasyonu, `KANIT/slice-3a/` dolu; `slice-3b` = Flutter istemci.) **Bilinen ve kabul edilen kusur:** kimlik dilimi 3b'den ÖNCE koşuyor ⇒ harf sırası kronolojiyi yalanlıyor; "3 ailesi = istemciyi ayaklandıran işler" anlamı korunduğu için kabul edildi. *Reddedilenler:* `slice-4-auth` · `slice-auth` (numarasız).
- **[K14-i — COWORK ÖNERDİ, ONUR BU TURDA İTİRAZ ETMEDİ; DÜRÜSTÇE KAYDA GEÇİYOR: bu bir Onur kilidi DEĞİL, kilit turunda gözden geçirilebilir] HIZ SINIRLAMA SAYILARI (bloker #13'ün ikinci ayağı):** IP penceresi (`/login`+`/refresh`+`/register`) = **10 istek / 5 dk**, `QueueLimit=0` · e-posta penceresi (`/login`) = **5 deneme / 15 dk** · eşzamanlılık limiti = **`ProcessorCount`** izin + **`QueueLimit = 2×ProcessorCount`**. *Gerekçe:* 19 MiB × N bellek tavanına çarpmadan doyum; 10/5dk insan kullanımını ısırmaz ama tek IP'den saatte ~120 Argon2'ye tavan koyar.
- **✅ [ADR 0003 v3 YAZILDI — TASLAK, KİLİTLİ DEĞİL] `docs/ADR/0003-kimlik-cekirdegi.md`** (**95.967 bayt · 645 satır · UTF-8, BOM yok · 5.660 Türkçe harf · mojibake 0 · U+FFFD 0 — hepsi Onur'un diskinden, kaynaktan ölçüldü**). **v2 SİLİNMEDİ, arşive TAŞINDI:** `arsiv/0003-kimlik-cekirdegi-TASLAK-v2-2026-07-25.md` (48.924 bayt). Artık arşivde **iki** taslak var (v1 + v2); karar izi tam.
  - **⚠ DÜRÜST BÜYÜME ÖLÇÜMÜ:** v1 = 24,7 K · v2 = 43,9 K (×1,8) · **v3 = 96,0 K (v2'nin ×1,96'sı, v1'in ×3,9'u)**. K11-h'nin *"her belge küçük ⇒ tek turda geçer"* gerekçesi **üçüncü kez tutmadı** ve K13-a bunu zaten kabul etmişti. Büyümenin kaynağı: 15 bloker kapatma + **beş yeni madde** (K3-B6 girdi politikası · K3-B7 PHC ayrıştırma · K3-C8 sstamp beyanı · K3-I3 dev bootstrap · K3-J6 logout hız sınırı) + **üç yeni bölüm** (§0 değişim kaydı 3 tablo · **§2-M port envanteri** · **§3.1 mutantsızlık dürüstlük beyanı**) + mutant tablosunun **20 → 34 satıra** çıkması.
  - **MUTANT SAYIMI (bütünlük kaynaktan doğrulandı):** bu belgede **M1 · M4-M8 · M11-M19 · M21-M39** = **34 satır** (M13 VOID dâhil; M6b/M8a/M8b alt-ayaklar). **0004'e ait:** M2·M3·M9·M10·M20 (+ pull-authz, imleç, D-7 mutantları, adsız). **M1..M39 aralığında kayıp yok, çift yok.** **YENİ PİN: 0004'ün yeni mutantları `M40`'tan başlar** — v2'nin *"0004 M26'dan devam eder"* varsayımı geçersizleşti. `M-L5/M-L6/M-L7/M-L8` **harflidir, numara tüketmez** (3b, Dart).
  - **YENİ MUTANTLAR (14):** M26 (`revoked_at` → `/logout` gerçekten çalışır) · M27 (`expires_at` → 30 gün zorlanır) · M28 (web gövdesinde token sızıntısı) · M29 (replay penceresi sınırsızlaşırsa) · M30 (halef tüketilmişse aile düşer) · M31 (parola uzunluk) · M32 (NetArchTest `Konscious.*` — 0001 K-H1'in ihlali kapandı) · M33 (statik dosya sırası/SPA) · M34 (PHC bozuk format `500` değil `401`) · M35 (imzasız CSRF) · M36 (`__Host-` öneki) · M37 (K3-B5'in "aynı yanıt" ayağı) · M38 (`family_id` doğum anı) · M39 (atomiklik, paralel `/refresh` yarışı, TC).
  - **DÜZELTİLEN MUTANTLAR (7):** M6 (`byte[] ==` ayağı **M6b**'ye ayrıldı — BannedApiAnalyzers ile ifade edilemez) · M8 (**M8a/M8b** iki ayak) · M12 (SHA-256 **pinlendi**) · M14 (hedef uç **adı pinlendi**: `/v1/_probe/deny-default`) · M16 (`alg:none`+RS256 ayakları **kaldırıldı**, **HS512**'ye pinlendi) · M17 (**saat ilerletme + tam eşitlik**) · M19 (mutasyon **DI doğrulamasına** çıpalandı) · M22 (**ayrı IP + bloke eden sahte hasher + `limit` ayırt edici**).
  - **YENİ TEST SEVİYESİ SÜTUNU (D3 majörü):** B / D / TS / TC / NA / DART. v2'nin *"saf çekirdek DB'siz kanıtlanır"* iddiası artık **ölçülebilir**: mutantların çoğu **TC** (Testcontainers) istiyor ve bu gizlenmiyor.
  - **BELGEDE AÇIK ÇATAL YOK — ama bu bir YAZAR İDDİASIDIR, denetim sonucu değil** (v2 aynı cümleyi yazmıştı ve çürütüldü; §0 girişinde böyle etiketlendi).
  - **AÇIK KALAN `[DOĞRULANMADI]` (2, ikisi de belgede etiketli ve KAPISIZ olduğu §3.1'de beyan edildi):** `ConcurrencyLease`'in `MetadataName.RetryAfter` taşıyıp taşımadığı · `flutter_secure_storage`'ın Windows şifreleme yöntemi (**v2'nin DPAPI iddiası GERİ ÇEKİLDİ**).
  - **AĞAÇ DURUMU (kaynaktan ölçüldü):** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? KANIT/adr-0003/` · `?? arsiv/0003-kimlik-cekirdegi-TASLAK-v2-….md` · `?? arsiv/0003-kimlik-ve-yetkilendirme-TASLAK-v1-….md` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · **Commit Onur'da; PUSH YASAK.**
  - **⚠ BU OTURUM v3'Ü DENETLEMEDİ VE DENETLEYEMEZ.** Yapılan tek kontrol **öz-doğrulamadır** (kodlama · bayt/satır · Türkçe harf · mojibake · mutant numara bütünlüğü · 15 bloker etiketinin gövdede geçmesi). **Bunlar bütünlük ölçümüdür, kapı değildir.**
- **Sıradaki ilk iş:** (1) **v3'e BAĞIMSIZ KAPI — TEMİZ VE AYRI OTURUM** (architecture + red-team, **RED-TEAM EN SON**; üreten ≠ denetleyen — bu oturum yazdı, denetleyemez). (2) K13-a gereği **bloker sıfırlanana kadar tur**; tur sayısı raporlanır, sınırlanmaz. (3) Temiz çıkarsa Onur kilitler → AYRI OTURUM **ADR 0004** (D-1..D-6 + **D-7 = `clientId → principal` zorlama kuralı**; mutantları **M40**'tan başlar). (4) Sonra **`GOREV-slice-3c-auth`** spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.
- **[TAKVİM ÖLÇÜMÜ — K13-a gereği raporlanıyor, K12-b gereği KESME ÖNERİSİNE ÇEVRİLMİYOR]** v3 yazımı 1 oturum aldı. Kalan: v3 kapısı (≥1 oturum) + olası v4 turu + ADR 0004 (1 yazım + ≥1 kapı) + spec (1 yazım + kapı) + build + doğrulama. **Bu, ODEV §3.1'in 21-23 Tem `slice-3a-auth` penceresini çoktan aştı** — ama K12-b yürürlükte: ölçüm raporlanır, kapsam kesilmez.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 16: **K13 — ADR 0003 v2'YE İKİNCİ VE SON KAPI KOŞTU → HÜKÜM: KİLİTLENEMEZ**)

- **Protokol koşuldu (oturum 16):** `docs/ODEV.md` (tam) + `CLAUDE.md` (tam) + bu dosyanın en üst iki checkpoint'i (K12 + K11) + ADR 0003 v2 (311 satır, tamamı) okundu. **`git status` kaynaktan ölçüldü: `## main...origin/main [ahead 15]` · ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? arsiv/0003-…-v1-….md` · `?? docs/ADR/0003-kimlik-cekirdegi.md` · HEAD `060a37a` · `origin/main` `56362ed` · push yok** — devir notuyla **birebir** uyuştu.
- **KAPI KURULUMU (üreten ≠ denetleyen korundu — oturum 15 belgeyi YAZDI, bu oturum yalnız DENETLEDİ, tek satır yazmadı):** A turu = üç bağımsız denetçi **paralel** (mimari/tutarlılık · ölçüm/gerçeklik · mutant/kapı) → B turu = **RED-TEAM EN SON**, A'nın bulguları elinde ve **onları çürütmekle** görevli.
- **🔴 HÜKÜM: KİLİTLENEMEZ. Dört denetçi de BAĞIMSIZ olarak aynı hükme vardı.** Birleşik **15 BLOKER** + ~20 majör. **Blokerlerin 7'si KÖR KAPI** (mutant öldürülmüyor, baseline YEŞİL ⇒ sahte kapı olarak **sessizce** teslim edilirdi). **Tam rapor: `KANIT/adr-0003/kapi-2-denetim-raporu.md`.**
- **RED-TEAM A TURUNU ÇÜRÜTEMEDİ:** 9 blokerin **hiçbiri düşmedi**. 1'i kapsamca daraltıldı (çevrimdışı soğuk açılış: "hangi ekran açılır" 3b'nin işi, "`userId` nereden gelir" 0003'ün işi) · 1'i MAJÖR'e indirildi (M7 pin — ölü tuzak *gürültülüdür*, ilk koşuda yakalanır) · 1'i MAJÖR'den **BLOKER'a YÜKSELTİLDİ** (e-posta partition, ölçüldü) · 2'si mekanizma düzeltilerek **güçlendirildi**.
- **[TAKSONOMİ — red-team'in getirdiği ayrım, bundan sonra yürürlükte]** **Ölü tuzak** = baseline KIRMIZI ⇒ *gürültülü*, ilk koşuda yakalanır ⇒ MAJÖR (karar eksikse bloker). **Kör kapı** = mutant öldürülmüyor, baseline YEŞİL ⇒ *sessiz*, sahte kapı olarak teslim edilir ⇒ **BLOKER**. A turunun üçü de bu ikisini aynı kefeye koymuştu.
- **✅ [COWORK'ÜN KENDİ ÖLÇÜMLERİ — alt-ajan beyanı AKTARILMADI, karar-taşıyıcı üç iddia ana oturumda kaynaktan yeniden okundu]**
  1. **`RateLimiterOptions.RejectionStatusCode` varsayılanı `StatusCodes.Status503ServiceUnavailable`** (dotnet/aspnetcore `release/9.0`); durum kodu `OnRejected` çağrılmadan önce set edilir. ⇒ **K3-J4'ün `429` kararı override yazılmadan GERÇEKLEŞMEZ**; `Retry-After` de otomatik değil. M11/M22/M23'ün kill sinyalleri baseline'da kırmızı doğar. **Belgenin §4'teki kendi manşet tezinin ("sessiz varsayılanların hangisinin kabul edildiğini yaz") birebir ihlali** — `ClockSkew` için titizlikle yapılan iş burada yapılmamış.
  2. **`DefaultPartitionedRateLimiter`: `s_idleTimeLimit = TimeSpan.FromSeconds(10)` + 100 ms heartbeat + `_limiters.Remove(...)` + `DisposeAsync`** (dotnet/runtime `release/9.0`). ⇒ **K3-J5'in `[DOĞRULANMADI]` açık kalemi KAPATILABİLİR** — ama *"sıfır bellek"* diye değil: **tavan ≈ istek hızı × 10 sn**, ve tavanı anlamlı kılan şey **K3-J2(1) IP penceresidir** (o da bloker #3'te çöküyor).
  3. **MS Learn birebir:** *"For requests served by other middleware after the authorization middleware, such as **static files**, the policy applies to **all requests**."* ⇒ `FallbackPolicy` + aynı-origin SPA gerçek bir dağıtım çatalı.
- **EN AĞIR ÜÇ BULGU:**
  - **RT-B1 [TAÇ KARARI VURUYOR] Kayıp yanıt: K3-L5 kendisine yüklenen görevi YAPISAL OLARAK yapamaz.** K11-c zarafet penceresini kaldırıp tüm yükü tek-uçuşlu refresh'e devretti; **tek-uçuşluluk EŞZAMANLI çağrıları serileştirir, AĞ YÜZÜNDEN KAYBOLAN yanıttan sonraki YENİDEN DENEMEYİ değil.** Sunucu `T1`'i tüketti + yanıt ulaşmadı ⇒ istemci aynı token'ı tekrar sunar ⇒ **AİLE DÜŞER (meşru kullanıcı kendini hırsız ilan eder)**; denemezse tek ağ kesintisi 30 günlük oturumu **zorunlu yeniden girişe** çevirir (çevrimdışı vitrininin tam ortası). **RFC 9700 §4.14.2 bunu bir MALİYET olarak kabul eder, telafi VAAT ETMEZ** ⇒ K3-L5'in *"bu mekanizmaya bağlıdır"* cümlesi **YANLIŞ BEYANDIR** ve belge yine de *"AÇIK ÇATAL KALMADI"* demektedir. *Öneri: `T1.consumed_at + N sn` içinde VE halefi henüz tüketilmemişse **kayıtlı halefi aynen döndür** (sınırlı replay-idempotency; R1'in sonsuz zinciri doğmaz, reuse-detection ısırmaya devam eder).*
  - **D1-B2 `/refresh` yükleminde `revoked_at`/`expires_at` YOK ⇒ `/logout` FİİLEN NO-OP, mutlak 30 gün ZORLANMIYOR.** K3-C6'nın *"etkilenen satır 0 ise token ya tüketilmiştir ya yoktur"* cümlesi **kapalı bir disjonksiyondur ve YANLIŞTIR** (iptal edilmiş ama tüketilmemiş token 1 satır döndürür ⇒ refresh başarılı). **M18 yalnız DİĞER ailenin yaşadığını test ettiği için `/logout` tamamen no-op olsa da YEŞİL kalır.** ⇒ M26/M27 zorunlu.
  - **RT-B2 K3-L3'ün ikinci hattı NAİF double-submit'tir ve TUTULMA GEREKÇESİ OLAN alt-alan-adı vektörüne karşı GEÇERSİZDİR.** Ele geçirilmiş kardeş alt alan `Domain=` kapsamlı CSRF çerezi yazar ⇒ çerez == başlık **eşleşir** ⇒ geçer. OWASP: naif kalıp *"reference only"*, imzalı varyant kullanılmalı. *Öneri: `__Host-` öneki (Domain yasak ⇒ enjeksiyon yapısal olarak imkânsız) + HMAC'li/aileye bağlı token; **M25'in kill sinyali de kör**.*
- **DİĞER BLOKERLER (özet, sıra rapordaki gibi):** #3 `FallbackPolicy` SPA'yı 401'ler **ve** proxy arkasında IP partition'ı ölür (M11/M23 TestServer'da yeşil = kör kapı) · #5 **K3-J2(2) İNŞA EDİLEMEZ** (RateLimiting partitioner **senkron**, gövdedeki e-posta okunamaz ⇒ K3-J3'ün bağlayıcı sırası bozulur) · #6 yenileme token'ının **taşıma kanalı platform başına kararlaştırılmamış** (D1-B3+B4+D3 tek bulgu; `/logout`'un girdisi tanımsız, JWT'de `family_id` yok; en doğal builder seçimi K3-L2'nin XSS gerekçesini **yalanlar**) · #8 **imzalama anahtarı bootstrap'ı yok** ⇒ değerlendiricinin makinesinde açılmaz **ya da** sır repoya girer (ODEV §2 × kırmızı çizgi #1) · #9 **K3-L5/L6/L7 hem kapısız hem devirsiz** (0004'e giden her mutant adlandırılmış, 3b'ye giden hiçbiri — asimetri belgenin **kendi standardı**) · #10 soğuk açılışta `userId` nereden gelir (şema sonucu) · #11 **parola politikası + girdi doğrulama ne kararda ne kapsam dışında = GİZLENMİŞ SINIR** · #12 M16'nın `alg` ayağı ısırmıyor + K3-C7'nin beyanı ölçümle yanlış (simetrik anahtarla RS256 zaten yapısal olarak reddediliyor; `alg:none`'ı `RequireSignedTokens` kapatıyor) · #13 M22 ısırmıyor + hız sınırlama **sayıları hiç kararlaştırılmamış** · #14 M17 donmuş `FakeTimeProvider` altında ısırmıyor · #15 **K3-B2'nin NetArchTest kuralı MUTANTSIZ ⇒ KİLİTLİ 0001 K-H1'in ("Her kural mutantla ısırdığını kanıtlar") doğrudan ihlali**.
- **DÜRÜSTLÜK — KIRILAMAYAN YERLER (denetçiler aradı, bulamadı):** oturum sabitleme kapalı (K3-C3 gerçek kazanç) · K3-A2 normalizasyon zinciri (İ/ı, U+212A Kelvin, NFC sırası) · K3-C2'nin SHA-256 asimetrisi · K3-A4↔D-3 zinciri · K3-D1/M15 · K3-L4'ün SameSite×CORS teşhisi · K3-L2'nin web reddi (README ile doğrulandı) · K3-J5'in `[DOĞRULANMADI]` etiketi (doktrin çalıştı) · K3-B1'in izolasyon cevabı · **K3-C7'nin dört ölçüm ayağı birebir doğru** · **numara bütünlüğü TAM (20+5=25)** · M13 VOID işlemesi örnek · M1 gerçekten canlandı · **v1'in hiçbir bulgusu sessizce düşürülmemiş** · kapsam kayması yok · **0003/0004 bölünme sınırı sağlam** · `SameSite=Strict` + harici link şüphesi TEMİZ çıktı (RFC 6265bis §5.2.1).
- **⚠ K6 TAVANI TÜKENDİ — ONUR ÇATALI AÇIK, KİLİTLENMEDİ.** Bu ADR'nin iki turu da harcandı (v1: 1 tur · v2: 1 tur) ve 15 bloker açık. K11-h'nin *"küçük belge tek turda geçer"* beklentisi **ikinci kez tutmadı** (v2 zaten v1'in 1,8 katıydı — K12'de dürüstçe ölçülmüştü). **Sunulan seçenekler:** **(a)** K6'yı bu ADR için **bir kez** esnet: v3 yaz + üçüncü tur · **(b)** tavanı kaldır, *"bloker sıfırlanana kadar tur"* kuralına geç (kapı bütçesi ölçüm olur, kota olmaz) · **(c)** blokerleri ADR'ye değil doğrudan **`GOREV-slice-3a-auth` spec'ine** yaz — ADR taslak kalır, kapı bütçesi spec'e devreder · **(d)** 0003'ü ikinci kez böl (kimlik çekirdeği / istemci sözleşmesi). **KARAR ONUR'UNDUR.**
- **[KARAR — Onur, 25 Tem 2026] K13-a: K6 TAVANI KALDIRILDI → "BLOKER SIFIRLANANA KADAR TUR".** Kapı bütçesi bundan böyle bir **kota** değil bir **ölçümdür**: bir belge (ADR ya da spec), bağımsız kapıdan **bloker'sız** çıkana kadar tur alır; tur sayısı önceden sınırlandırılmaz. *Gerekçe:* K12-b zaten *"kalite > hız"* demişti; K6 tavanı bu turda **yanlış bir teşvik ürettiği kanıtlandı** — v2'nin tavana sığmak için değil, **doğru olmak için** büyümesi gerekiyordu (K12'de dürüstçe ölçülmüştü: v2 = v1 × 1,8, kapsam daraldığı hâlde) ve buna rağmen 15 bloker taşıyordu. *Reddedilenler [adlandırılmış]:* **(a)** 0003'e tek seferlik istisna — v3 de temiz çıkmazsa istisna fiilen kuralı yer · **(c)** blokerleri ADR yerine doğrudan spec'e yazmak — ADR'nin *"geri alınamaz mimari karar"* işlevi zayıflar, **kilitsiz bir belgeye dayanan spec** doğar · **(d)** 0003'ü ikinci kez bölmek (kimlik çekirdeği / istemci sözleşmesi) — K11-h'nin bölme gerekçesi zaten iki kez tutmadı, üçüncü belge = üçüncü kapı yükü. **YÜRÜRLÜKTEKİ YENİ KURAL:** *"K6 = bloker sıfırlanana kadar tur; tur sayısı raporlanır, sınırlanmaz."* **Bedeli kayda geçti:** kapı yükü artık öngörülemez ⇒ **her turda takvim ölçümü raporlanır ama K12-b gereği kesme önerisine ÇEVRİLMEZ.**
- **AĞAÇ DURUMU (bu oturumdan sonra):** yukarıdakilere ek **`?? KANIT/adr-0003/kapi-2-denetim-raporu.md`** (yeni, additive). **Commit Onur'da; PUSH YASAK.**
- **Sıradaki ilk iş:** (1) ~~Onur K6 çatalını kilitler~~ **KİLİTLENDİ → K13-a (b şıkkı).** (2) **ADR 0003 v3: 15 blokerin kapatılması** — **DOLU BAĞLAMDA ADR/SPEC YAZMA YASAĞI yürürlükte ⇒ TEMİZ OTURUM**. (3) **ADR 0004 hâlâ yazılmadı** (D-1..D-6 + Ma-7'nin bulduğu **D-7 = `clientId → principal` zorlama kuralı**, §7'de karşılığı YOKTU). (4) Sonra `GOREV-slice-3a-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular. **NOT [Mi-1]: `slice-3a` adı ÇAKIŞIYOR** — 0002 K2-I3'te `slice-3a` = sunucu materyalizasyonu ve `KANIT/slice-3a/` dolu; yeni dilim `slice-3c-auth` ya da `slice-4-auth` olmalı.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 15: **K12 — TAKVİM ÇATALI KİLİTLENDİ; ÖNCELİK = KALİTE > HIZ**)

- **Protokol koşuldu (oturum 15):** `docs/ODEV.md` (tam) + `CLAUDE.md` (tam) + bu dosyanın en üst iki checkpoint'i (K11 + K10) okundu. **`git status` Desktop Commander ile kaynaktan ölçüldü: `## main...origin/main [ahead 15]` · ` M PROJE_HAFIZA.md` · `?? docs/ADR/0003-kimlik-ve-yetkilendirme.md` · HEAD `060a37a` · push yok** — devir notuyla birebir uyuştu.
- **[KARAR — Onur] K12-a: K11'in dört çıkışından YALNIZ (b) KİLİTLENDİ → K10-e NICE LİSTESİ KOŞULSUZ İPTAL.** İptal edilenler (bir daha "tampon açılırsa" diye geri çağrılmaz, **yeniden gündeme ancak MUST'lar bitip kapılardan geçtikten sonra ve ayrı bir kararla gelir**): editoryal Fraunces gün başlığı · karanlık modda mesh aurora · ödül gibi boş-durumlar · ses tasarımı (`flutter_soloud`) · confetti · partikül süpürme · parallax kart · gooey geçiş. **Yan etki:** bu paketler bağımlılık listesinden düşer ⇒ 4-5 lisans+CVE kapısı (kırmızı çizgi 3) hiç açılmaz; `mesh_gradient`/`flutter_soloud`/`confetti` `dependencies.md`'ye GİRMEZ. **MUST ikilisi (canlı-ayrıştırmalı NLP quick-add + mürekkep-dolumu checkbox ritüeli) DOKUNULMAZ** — ODEV §4.1'in tek farklılaştırıcısı ve §2'nin "önce uygulamaya bakılacak" ölçütünün karşılığı orada.
- **[KARAR — Onur] K12-b: TAKVİM BASKISI BUNDAN SONRA KAPSAM/DERİNLİK KESME GEREKÇESİ OLARAK KULLANILMAZ.** Onur'un ifadesi (birebir): *"3 haftamız var ona göre ama hızlı da gidebilir erken de bitirebiliriz… süreye göre bir şeylerden kısıp hızlı gideceğim diye hata yapmanı istemiyorum."* ⇒ **Öncelik sırası: doğruluk/kalite > hız.** Cowork takvim ölçümünü **raporlamaya devam eder** (gizlemek doktrine aykırı olurdu) ama **ölçümü bir kesme önerisine çevirmez**; kesme yalnız Onur'un açık kararıyla olur.
- **REDDEDİLENLER [adlandırılmış]:** **(a) tamponu harcayıp devam** — çatalı kilitlemek değil ertelemek olurdu, ilk aksama doğrudan teslime yansırdı · **(c) hedefi ikinci kez esnetmek** — Onur 3 haftalık iç çerçeveyi teyit etti, dışarıdan dayatılmış tarih yok ⇒ esnetilecek bağlayıcı bir taahhüt de yok · **(d) çekirdek pariteden kesme sırası** — Onur açıkça reddetti: değerlendirilen şeyin kendisini ölçüm olmadan kesmek. **(d) kapalıdır; yeniden açılması için 3b'de ÖLÇÜLMÜŞ bir hız verisi gerekir.**
- **K12-c: ODEV §8(2) AÇIK KALIR ama takvim kararının altındaki bilinmeyen olarak BİR DAHA GÜNDEME GETİRİLMEZ.** Onur'un "3 hafta" çerçevesi **§3'ün zaten kayıtlı iç tahminidir; YENİ DIŞ BİLGİ DEĞİLDİR** (şirketten gelen tarih hâlâ yok). Teslim biçimi (repo linki / paketlenmiş build / canlı sunum) de bilinmiyor ve **teslim paketi tasarımını hâlâ etkileyecek** — bu yüzden §8(2) silinmez, KANIT'ta adlandırılmış açık kalem olarak durur.
- **✅ [ÖLÇÜM — K11'in "DOĞRULANMADI" BULGUSU ARTIK DOĞRULANDI, dört ayak da resmî kaynaktan okundu] `MapInboundClaims=false` ⇒ `sub` ARTIK `ClaimTypes.NameIdentifier` DEĞİLDİR.** (1) `ClaimTypeMapping.InboundClaimTypeMap` içinde birebir `{ JwtRegisteredClaimNames.Sub, ClaimTypes.NameIdentifier }` girdisi var (IdentityModel `dev`, `Microsoft.IdentityModel.JsonWebTokens/ClaimTypeMapping.cs`). (2) `JwtBearerOptions.MapInboundClaims` varsayılanı **`true`**; `false` yapılınca çeviri hiç koşmaz ⇒ claim tipi ham `"sub"` kalır. (3) `DefaultUserIdProvider.GetUserId` = `connection.User.FindFirst(ClaimTypes.NameIdentifier)?.Value` (aspnetcore `main`) ⇒ eşleme kapalıyken **`null` döner, `user:{id}` grubu sessizce boşa düşer.** (4) `TokenValidationParameters.DefaultClockSkew = TimeSpan.FromSeconds(300)` = **5 dk** ⇒ R4'ün *"beyan edilen ≤15 dk fiilen ≤20 dk"* iddiası **DOĞRU**. **İKİ SONUÇ:** (a) ADR 0004'te özel `IUserIdProvider` **zorunludur** (hipotez değil, ölçüm) · (b) **ADR 0003'ün kendisi etkilenir:** `ICurrentUser` implementasyonu `ClaimTypes.NameIdentifier` DEĞİL doğrudan **`"sub"`** claim'ini okumalıdır — aksi hâlde `UserId` her istekte `UnauthenticatedException` atar.
- **✅ [ADR 0003 v2 YAZILDI — TASLAK, KİLİTLİ DEĞİL] `docs/ADR/0003-kimlik-cekirdegi.md`** (43.904 karakter · 306 satır · UTF-8, BOM yok · 2.937 Türkçe harf, mojibake 0 — kaynaktan ölçüldü). **v1 SİLİNMEDİ, arşive TAŞINDI:** `arsiv/0003-kimlik-ve-yetkilendirme-TASLAK-v1-2026-07-25.md` (24.674 bayt, karar izi korunur). Dosya adı da değişti (`0003-kimlik-ve-yetkilendirme` → `0003-kimlik-cekirdegi`) çünkü belge artık yetkilendirmeyi İÇERMİYOR.
  - **İçerik:** §0 v1→v2 değişim kaydı (denetim izi tablosu) · §1 bağlam + **BEŞ borcun nerede kapandığı tablosu** · §2 A (kimlik: Trim→NFC→ToLowerInvariant, `/register` sapması, **K3-A4 `User` senkronlanabilir kök DEĞİL**) · B (Argon2id + port + PHC formatı + rehash) · C (**K3-C5 zarafet penceresi YOK** · C3 `family_id` giriş başına · C4 `/logout` + `/logout-all` · C6 atomik tüketim · **C7 `TokenValidationParameters` tam tablo**) · D (`ICurrentUser`, **`"sub"` okur**) · I (sırlar) · J (**üç ayrı kaba-kuvvet kontrolü, R2 düzeltmesi**) · K (elenen) · **L (istemci sözleşmesi, 7 madde)** · §3 **20 satırlık mutant tablosu** (M13 VOID dâhil; M22/M23/M24/M25 yeni) · §4-7.
  - **KAPATILAN v1 BULGULARI:** R1 (pencere kaldırıldı) · R2 (IP penceresi + e-posta penceresi + **eşzamanlılık limiti**, üçü ayrı amaç; v1'in telafi cümlesi yanlış diye açıkça düzeltildi) · R3 · R4 · R5 · R7 (M14/M15/M16) · B5/R6 (§2-L) · M1 canlandı · M7 yapısal ölçüte çevrildi · M4/M6 kill sinyali düzeltildi · "dört borç ✓" → **BEŞ borç, hiçbiri bu belgede kapanmıyor**.
  - **[KARAR — Onur, 25 Tem 2026] K12-d: WEB DAĞITIM TOPOLOJİSİ = AYNI ORIGIN (ADR'de K3-L4).** *Cowork bulgusu:* **K11-f kendi içinde çelişiyordu** — aynı cümlede *"`SameSite=Strict` + double-submit"* ve *"CORS credentials"* diyordu; **bu ikisi birlikte çalışmaz** (`SameSite=Strict/Lax` çerezi çapraz-site isteklerde HİÇ göndermez ⇒ Flutter web ayrı origin'de olsaydı `/refresh` çerezi gitmez, **web yenileme hiç çalışmazdı** ve bu ancak canlı web demosunda fark edilirdi). **Karar:** Flutter web build'i API ile aynı origin'den servis edilir ⇒ **CORS `AllowCredentials` bedeli tamamen düştü**, `SameSite=Strict` fiilen çalışır, dağıtım tek birim (ODEV §2'ye doğrudan hizmet). *Reddedilenler:* (b) çapraz origin + `SameSite=None` (CSRF yüzeyi genişler, iki katı güvenlik yazımı + iki katı mutant) · (c) web'de çerez yok (F5 = çıkış; K11-f zaten reddetmişti). **ADLANDIRILMIŞ KALAN YÜZEY:** "same-site" ≠ "same-origin" ⇒ ele geçirilmiş bir **kardeş alt alan adı** çerezi taşır; bu yüzden **double-submit CSRF token'ı KALDIRILMAZ**, ikinci savunma hattı olarak durur (K3-L3) ve **M25** mutantıyla ısırtılır. **TESLİM PAKETİNİ BAĞLAR:** web build'i API ile birlikte servis edilmeli (CI/CD + paketleme adımında görünür gereksinim).
  - **⚠ DÜRÜST ÖLÇÜM — K11-h'NİN "HER BELGE KÜÇÜK" GEREKÇESİ KISMEN TUTMADI:** 0003 v2 **43,9 K karakter**, yani v1'in (24,7 K) **~1,8 katı** — kapsam DARALDIĞI hâlde. Sebep: 9 blokerin kapatılması + üç yeni bölüm (§0 değişim kaydı, §2-C7, §2-L) + mutant tablosunun 12→19 büyümesi. **Bölme kararı yine de doğru kalıyor** (0004 ayrıca ~aynı yükü taşıyacaktı ⇒ tek belge ~80 K olurdu) ama *"küçük belge tek turda geçer"* beklentisi **zayıfladı**; son denetim turu buna göre planlanmalı.
  - **BELGEDE AÇIK ÇATAL KALMADI** (K3-L4 kilitlendi); **M25** eklendi ⇒ mutant tablosu **20 satır** (M13 VOID dâhil). Belge son hâlinde **311 satır**.
  - **AĞAÇ DURUMU:** ` M PROJE_HAFIZA.md` · ` M docs/ODEV.md` · `?? arsiv/0003-…-v1-….md` · `?? docs/ADR/0003-kimlik-cekirdegi.md`. **Commit Onur'da; push YASAK.**
- **Sıradaki ilk iş (TEMİZ OTURUM — "denetim ve yazım ayrı oturum" kuralı yürürlükte):** (1) **ADR 0003 v2'ye İKİNCİ ve SON bağımsız denetim turu** (architecture + red-team; üreten ≠ denetleyen — **bu oturum belgeyi YAZDI, denetleyemez**). (2) Temiz çıkarsa Onur kilitler; çıkmazsa **K6 mekanizması tartışmaya açılır**. (3) AYRI OTURUM: **ADR 0004 = senkron-yetki aktivasyonu** (D-1..D-6 §7'de adlandırıldı: hub kimliği + özel `IUserIdProvider` · `outbox.owner_id` · `User` filtre muafiyeti · pull-authz + mutantı · imleç opaklaştırma · `sync_client_clock.user_id`). (4) Sonra `GOREV-slice-3a-auth` spec'i (iki ADR birlikte besler) → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 14: **K11 — ADR 0003'ÜN BLOKER ÇATALLARI KİLİTLENDİ + ADR BÖLÜNDÜ**)

- **Protokol koşuldu:** `docs/ODEV.md` (tam) + `CLAUDE.md` (tam) + bu dosyanın en üst iki checkpoint'i (K10 + oturum 13, ADR 0003 denetim bloğu dâhil) okundu. **`git status` Desktop Commander ile kaynaktan ölçüldü: `## main...origin/main [ahead 15]` · ` M PROJE_HAFIZA.md` · `?? docs/ADR/0003-kimlik-ve-yetkilendirme.md` · HEAD `060a37a` · push yok** — devir notuyla birebir uyuştu.
- **[COWORK ÖLÇÜMÜ] Devir notu "en az beş çatal" diyordu; SEKİZ çatal çıkarıldı.** Fazladan üçü **B5/R6**'nın cevapsız üç istemci sorusudur (§1'in dört sorusundan üçü). Gerekçe: o üçü kilitlenmeden dilim **kendini haklı çıkaran işi** yapmamış olur (iki denetçinin bağımsız yakınsadığı bulgu) ve 3b builder'ı tam da önlenmek istenen yerde kararsız kalır.
- **[KARAR — Onur] K11-a: HUB KİMLİĞİ 3a'DA, MİNİMAL (B1 kapanır).** `[Authorize]`'lı hub + token `?access_token=` query string'inden (WebSocket el sıkışması `Authorization` başlığı taşımaz) + **özel `IUserIdProvider`** ile `sub` → `UserIdentifier` + grup üyeliği **token'dan**, istemci-beyanlı `ActorId`'den DEĞİL. Paylaşım/liste bazlı hub yetkisi 3b'ye kalır. TAHMİN ~0,5 gün. *Gerekçe:* iki taç mücevherden biri kimliksiz gösterilemez; `hafiza:191`'in "gerçek auth auth diliminde" borcu buraya atanmıştı. *Reddedilenler:* 3b'ye erteleme + deny-all (kimlik şeması kararı ikinci kez açılırdı) · 3a'da tam hub yetkilendirmesi (paylaşım akışı ODEV §8(5)'te belirsiz, kapsam patlar).
- **[KARAR — Onur] K11-b: `outbox_messages.owner_id` BU DİLİMDE BİRLEŞTİRİLİR (B4 = beşinci borç kapanır).** `owner_id` yazımı **`ICurrentUser.UserId`**'den türer; ingest'te `op.ActorId ≠ token sub` ise istek **reddedilir** (F5'in fiilen devreye girmesi). **ISIRAN KAPI:** owner'ı `ActorId`'den türeten kod → test KIRILMALI. TAHMİN ~0,25-0,5 gün. *Gerekçe:* aksi hâlde K3-F2'nin "ActorId yetki kararına hiçbir yolla girmez" iddiası **kendi belgesi içinde yanlış** kalır; pull kapsam filtresi ve `user:{owner_id}` sinyal grubu istemci-beyanlı bir kolona dayanmaya devam ederdi. ⇒ **ADR'nin "dört borç ✓" beyanı BEŞ borç olarak düzeltilir ve beşi de kapatılır.**
- **[KARAR — Onur] K11-c: ZARAFET PENCERESİ KALDIRILDI + İSTEMCİDE TEK-UÇUŞLU (single-flight) REFRESH (R1 kapanır).** Sunucuda pencere yok: **tüketilmiş yenileme token'ı = ailenin tamamı iptal** (saf reuse-detection). Yarış istemcide tek-uçuşlu refresh + 401'de kuyruğu bekletme ile çözülür. *Gerekçe:* sonsuz zarafet zinciri (5 sn'de bir `/refresh` ⇒ reuse-detection'a **yapısal olarak erişilemez** olması) kökten imkânsızlaşır; **M1'in ÖLÜ TUZAĞI da ortadan kalkar** (baseline'da kırmızı doğan mutant yok olur). *Reddedilenler:* pencereyi tutup yüklemini tanımlamak (ek sayaç + daha çok mutant + daha geniş yüzey) · pencere yok + istemci retry yok (401'de çıkışa atmak çevrimdışı kuyruk vitrinini zedeler).
- **[KARAR — Onur] K11-d: `family_id` = GİRİŞ BAŞINA (R5 kapanır).** Her başarılı `/login` **yeni** `family_id` (= bir cihaz/oturum); refresh döndürmesi aynı aileyi sürdürür; `/logout` **yalnız o aileyi** iptal eder; reuse tespiti **o ailenin tamamını** düşürür. K3-C2'nin *"tüm oturumlar düşer"* cümlesi **"o oturum ailesi düşer"** olarak düzeltilir; ayrıca açık **`/logout-all`** (kullanıcının tüm aileleri) eklenir. *Reddedilen:* kullanıcı başına tek aile (reuse tespitinde her cihaz düşer ⇒ çok-cihaz/işbirliği demosunda kötü görünür).
- **[KARAR — Onur] K11-e: `/register` SAYIM ORACLE'I = ADLANDIRILMIŞ KABUL + BEYAN (R3 kapanır).** "Bu e-posta kayıtlı" mesajı kalır; oracle **KANIT'ta açıkça beyan edilir**. **K3-B5'in "PAZARLIKSIZ" ifadesi DÜZELTİLİR:** kullanıcı-numaralandırma direnci **login/refresh yolunda pazarlıksız**, `/register`'da **adlandırılmış sapma**. *Gerekçe:* e-posta doğrulama ODEV §6.1'de kapsam dışı ⇒ kanonik çözüm yok; doktrin "beyan edilmiş sınır kabul edilir, gizlenmiş sınır edilmez". *Reddedilenler:* her durumda 202 (kullanıcı neden giriş yapamadığını anlamaz ⇒ ODEV §2 zedelenir; timing oracle'ı için sahte hash de gerekir) · yalnız sıkı rate-limit (oracle'ı kapatmaz, R2'nin partition sorununu register yoluna taşır).
- **[KARAR — Onur] K11-f: TOKEN DEPOSU = native secure storage · web HttpOnly cookie (B5/R6 1/3 kapanır).** Android/iOS/Windows'ta `flutter_secure_storage` (Keystore/Keychain/DPAPI); **web'de yenileme token'ı `HttpOnly`+`Secure`+`SameSite` cookie olarak sunucudan set edilir, access token yalnız bellekte** ⇒ tek XSS yenilemeyi okuyamaz. Bedeli kayda geçti: CORS credentials + **CSRF karşı önlemi** (`SameSite=Strict` + double-submit) + bir bağımlılık kapısı (lisans+CVE, kırmızı çizgi 3). TAHMİN ~0,5 gün. *Reddedilenler:* web'de yalnız bellek (F5 = çıkış; web demosunda göze batar) · her platformda `flutter_secure_storage` (web'de localStorage/IndexedDB'ye düşer ⇒ "secure" adı yanıltıcı, denetçinin bulduğu XSS yolu aynen açık kalır).
- **[KARAR — Onur] K11-g: 401'DE KUYRUK BEKLER · ÇIKIŞTA KULLANICI-BAŞINA AYRI DB DOSYASI (B5/R6 2-3/3 kapanır).** 401'de kuyruk **düşürülmez**: tek-uçuşlu refresh denenir, olmazsa kuyruk **diskte kalır** ve "oturum gerekli" durumuna geçilir. Çıkışta **SİLME YOK** — her kullanıcının Drift dosyası ayrı (`momentum_{userId}.sqlite`) ⇒ A çıkıp B girince A'nın verisi görünmez, gönderilmemiş yazımlar A yeniden girince devam eder. **Kırmızı çizgi 4 (kalıcı silme) hiç tetiklenmez.** *Reddedilenler:* çıkışta tek DB'yi silmek (gönderilmemiş kuyrukta veri kaybı + her çıkışta onay akışı) · 401'de kuyruğu düşürmek (ODEV §6.1'in "kuyruktaki yazımların kaybolmaması mimari zorunluluk" cümlesini vurur).
- **[KARAR — Onur] K11-h: ADR ŞİMDİ BÖLÜNDÜ (K6 tavanı yönetimi).** **ADR 0003 = KİMLİK ÇEKİRDEĞİ** (kullanıcı varlığı · Argon2id/`IPasswordHasher` · token yaşam döngüsü: JWT+opak döndürmeli yenileme+reuse-detection+`family_id` · `TokenValidationParameters` · RateLimiter · `/register` sapması · `ICurrentUser` sözleşmesi · **istemci sözleşmesi K11-f/g**). **ADR 0004 = SENKRON-YETKİ AKTİVASYONU** (owner EF global query filter + `IgnoreQueryFilters` yasağı · push-authz · pull-authz · `outbox.owner_id` · hub kimliği · `clientId→principal`). *Gerekçe:* her belge küçük ⇒ **kalan tek denetim turunda geçme şansı yüksek**; tek spec'i (`GOREV-slice-3a-auth`) ikisi birlikte besler. Bedeli ~0,5-1 gün ek kapı yükü. *Reddedilen:* tek belge v2 (~40 KB; 9 eksik mutant + hub + outbox eklenince K6'nın "tek devasa spec" dersi ADR düzeyinde tekrarlanır, tavan tükenirse mekanizma tartışmaya açılır).
- **⚠ NUMARA UYARISI [Cowork, gelecek oturum için]: `ADR 0004` ARTIK VAR ve konusu SENKRON-YETKİ AKTİVASYONU'dur.** K10-d *"ayrı `ADR-0004` YAZILMAZ"* derken **frontend tasarım ADR'sini** kastediyordu (o karar geçerli: tasarım `DESIGN.md`'ye gömülür). İki cümle çelişmiyor; **numara aynı, konu farklı.** *Bu not, projenin K9/K10'da bir kez yediği "aynı numara iki karar" hatasının tekrarını engellemek için yazıldı.*
- **[COWORK BULGUSU — DOĞRULANMADI, v2'de kaynaktan ölçülecek] SignalR + JWT'de gizli mayın:** R4'ün istediği `MapInboundClaims=false` ayarı `sub` → `ClaimTypes.NameIdentifier` eşlemesini kapatır ⇒ **`Context.UserIdentifier` boş düşer** ve `user:{id}` sinyal grubu **sessizce çalışmaz**. Özel `IUserIdProvider` yazılmazsa ısıran kapı adayı: *grup mesajı hiçbir istemciye ulaşmıyorsa test KIRILMALI*. **Bu iddia şu an bilgi/hafıza düzeyindedir, ÖLÇÜM DEĞİLDİR** — ADR 0003 v2 yazılırken .NET kaynağından teyit edilecek.
- **KAPANMAYAN BLOKERLER — karar değil YAZIM işi (v2/0004'te kapanacak, hiçbiri Onur çatalı değildi):** **R2** RateLimiter partition anahtarı (saldırgana seçtirilmeyen anahtar + dummy-Argon2 maliyetinin kaldırılması; Risk #5'in telafi iddiası **düzeltilecek**) · **R4** `TokenValidationParameters` (`ClockSkew=0` ⇒ beyan edilen ≤15 dk fiilen ≤15 dk olur · `alg` pinleme · `MapInboundClaims=false`) · **B2** `User`'ın senkronlanabilir kök OLMADIĞI yazılır ⇒ global filtre `User`'a uygulanmaz (yoksa anonim `/login` `UnauthenticatedException` alır, **giriş fiziksel olarak kilitlenir**) · **B3** pull yolunun ham SQL olduğu için EF filtresine erişilemediği yazılır + **pull-authz mutantı eklenir** · **R7** mutantsız üç mekanizma (deny-by-default · `Guid.Empty` · zarafet penceresi ⇒ K11-c ile zaten yok oldu) · **M1/M7/M3/M2/M9/M4/M6 düzeltmeleri + M13-M21 dokuz eksik mutant** · **K3-G2 düzeltmesi:** imleç yan-kanalı *"kapatılamaz"* değil — `server_seq` IDENTITY olduğu için **tam sayaç** sızıyor; imleç **opak/HMAC'li** döndürülür + boş-sayfa `nextCursor` çatalı kurulur.
- **⚠ [COWORK ÖLÇÜMÜ — TAKVİM, TAHMİN] K11 KARARLARI 3a'YI BÜYÜTTÜ; TAMPON FİİLEN TÜKENDİ.** K8'de 3a tahmini **2,5-3 gün**di. K11 eklemeleri: hub kimliği ~0,5 g (K11-a) + outbox owner ~0,25-0,5 g (K11-b) + web cookie/CSRF ~0,5 g (K11-f) + ADR bölünmesinin ek kapı yükü ~0,5-1 g (K11-h) = **~1,75-2,5 gün EK** ⇒ 3a **~4,25-5,5 gün**. Üstüne plan 3a'ya **21-23 Tem** diyordu, bugün **25 Tem** (≥2 gün geride). Hedef ~15 Ağu, tampon **3 gün** ⇒ **tampon ya tükendi ya eksiye geçti.** Bu bir TAHMİNDİR, ölçüm değildir. **Karar Onur'un:** (a) tamponu harcayıp devam · (b) NICE listesini (K10-e) şimdiden tamamen iptal · (c) hedefi ikinci kez esnetmek (ODEV §8(2) hâlâ açık: ödevin ne zaman beklendiği bilinmiyor) · (d) çekirdek pariteden kesme sırası açmak. **Bu çatal HENÜZ KİLİTLENMEDİ — sunuldu, kayda geçti.**
- **Sıradaki ilk iş:** (1) **ADR 0003 v2 = kimlik çekirdeği** yazılır (K8-a..d + K9 + K11-c/d/e/f/g + R2/R4 yazım düzeltmeleri + mutant tablosu). (2) **ADR 0004 = senkron-yetki aktivasyonu** yazılır (K11-a/b + B2/B3 + pull-authz mutantı + K3-G2 düzeltmesi). (3) Her belgeye **BİR (son) bağımsız denetim turu** — üreten ≠ denetleyen. (4) Kilit → `GOREV-slice-3a-auth` spec'i → Claude Code build → **Cowork TEMİZ OTURUMDA bağımsız doğrular.** **Dolu bağlamda ADR/spec yazma yasağı yürürlükte:** iki belge aynı oturumda yazılmaz.

## ⏭ CHECKPOINT (25 Tem 2026 — harici Cowork tasarım oturumu → **K10: FRONTEND TASARIM YÖNÜ KİLİTLENDİ**, ağırlığı Onur DARALTTI)

> **⚠ NUMARA DÜZELTMESİ [Cowork bulgusu, kaynaktan ölçüldü]:** Harici oturum bu bloğu **"K9"** diye gönderdi ve *"son kilit K8'di"* dedi. **YANLIŞ:** `K9` aynı gün (25 Tem) bu klasörde **Konscious Argon2id + mimari izolasyon** olarak zaten kullanılmıştı (bir alttaki checkpoint). Harici oturum klasöre **salt-okunur** baktığı için bunu göremedi — kusur eşzamanlılığın, onun değil. Blok **K10** olarak renumber edildi (`K9-a/b/c → K10-a/b/c`). *Yazılsaydı hafızada iki farklı `K9` olurdu ve gelecek oturum "K9'a göre" cümlesini çözemezdi — bu projenin daha önce üç oturum yediği hata sınıfı.*

- **[KARAR — Onur] K10-a: TASARIM YÖNÜ = "SICAK KAĞIT × GECE MÜREKKEBİ" (tek kimlik, iki yüz) + YAY-FİZİKLİ HAREKET DİLİ.** Aydınlık yüz: sıcak kağıt zemin + editoryal Fraunces gün başlığı + tek kiremit vurgu + %5 kağıt greni. Karanlık yüz: saf siyah yok, metin ~%90 opaklık, amber vurgu derinleştirilmiş, mesh-aurora fon. Hareket: `motor` spring'leri (mikro 100-200ms · vurgulu 350-600ms · stagger 30-60ms). *Görsel kanıt:* 4 kare HTML önizleme (Onur'da, repoya girmedi; kalite kapısından geçti). **Onur önizlemeye BAKARAK seçti — estetik yön yeniden tartışmaya açılmaz.** *Reddedilenler [adlandırılmış]:* B "Expressive Ink" tek başına ("Google demosu" kimlik riski — hareket dili alındı, görsel dili alınmadı) · C "Yumuşak Neşe" (kurumsal panelde "oyuncak" okunma riski).
- **[KARAR — Cowork önerisi, Onur onayladı] K10-b: ARAÇ STRATEJİSİ = CLAUDE ÇEKİRDEK TASARIM MOTORU.** `DESIGN.md`/token sistemi + **ban-list** (mor/indigo gradyan · ayrımsız glassmorphism · "Linear look" · Inter-üstüne-varsayılan · bento aşırılığı **YASAK**) + screenshot-critique döngüsü. **AI UI üreticileri STİL için KULLANILMAZ** (medyana yakınsama / "AI purple problem"). Stitch: yalnız düzen fikri taraması (Flutter exportu resmî kaynakta YOK). **ATLA:** v0 · Lovable · bolt · Replit · FlutterFlow (free'de kod exportu yok) · Uizard · Relume. **RIVE DIŞARIDA** — editör ücretsiz ama `.riv` exportu ödemeli; ikame = `CustomPainter` + motor spring.
- **K10-c: SOMUT SEÇİMLER.** Fontlar **Fraunces (display) + Instrument Sans (UI)**; subset edilip **UYGULAMAYA GÖMÜLÜR**, runtime fetch YOK. Paketler: `flutter_animate` · `motor` · `animations` · `mesh_gradient` · `skeletonizer` · `confetti` · `animated_reorderable_list` · `custom_text` · `flutter_soloud` (soundpool DISCONTINUED). İkon: lucide/phosphor. Ses: Kenney "Interface Sounds" CC0 + Pixabay. İllüstrasyon: unDraw. Teknik referans (MIT): Wonderous + flutter_vignettes.
- **[COWORK DOĞRULAMASI — harici oturumun beyanı AKTARILMADI, dört iddia KAYNAKTAN yeniden ölçüldü; DÖRDÜ DE TUTTU]:**
  - `motor` — **v1.1.0 (2 Ara 2025), MIT, yayıncı `whynotmake.it` (doğrulanmış), 62,1k indirme, 217 beğeni**; `SpringMotion`/`CupertinoMotion`/`MaterialSpringMotion` gerçekten var. ✅
  - **Rive** — `rive.app/pricing`: **Free katmanında `.riv` (Runtimes) exportu YOK**; export en erken **Cadet $9/koltuk/ay**'da açılıyor. Eleme gerekçesi **doğru**. ⚠ *Bu bir SATIN ALMA kararıdır — Cowork asla kendi başına yapmaz; "tek aylık istisna" yalnız Onur'un kararıdır.*
  - **Fontlar** — `google/fonts` METADATA.pb: **Instrument Sans** = `latin, latin-ext, menu`, **OFL**, variable (`wdth,wght`); **Fraunces** = `latin, latin-ext, menu, vietnamese`, **OFL**, variable (`SOFT,WONK,opsz,wght`). `latin-ext` Türkçe'yi kapsar ⇒ iddia **doğru**. ✅
  - **`flutter#128305`** — **AÇIK**, başlık birebir *"Incorrect Caret Position When Using WidgetSpan in TextEditingController"*; `buildTextSpan` içinde WidgetSpan → caret başa atlıyor. ⇒ **WidgetSpan YASAĞI gerçek bir kısıta dayanıyor.** ✅
  - *Süreç notu: harici oturum kendi bulgularından birini kendisi çürütmüş (**"Superlist Apple Design Award aldı" iddiası YANLIŞ** — teslim materyalinde KULLANILMAZ). Üreten ≠ denetleyen orada da korunmuş; araştırma kalitesine itiraz yok.*
- **[YAKINSAMA — iki bağımsız oturum aynı mayını buldu] TÜRKÇE LOCALE KURALI BİRLEŞTİRİLDİ.** Harici oturum: *"Dart `toUpperCase()` Türkçe i→İ dönüşümünü YAPMAZ"* (frontend). Bu oturum: ADR 0003 **K3-A2** `ToLowerInvariant` + `COLLATE "C"` + `ToLower()`/`ToUpper()` **banned-API** (backend). **Aynı tuzağın iki ucu** ⇒ tek kural olarak yürütülür: *kültüre-duyarlı büyük/küçük harf dönüşümü hiçbir katmanda kimlik/eşleştirme yolunda kullanılmaz; gösterim için locale-duyarlı yardımcı AYRI yazılır.*
- **[KARAR — Onur, 25 Tem 2026] K10-d: AYRI `ADR-0004` YAZILMAZ → `DESIGN.md` + spec'e gömülü.** Token'lar (renk/tip/boşluk/radius/hareket/ses) + ban-list **`DESIGN.md`** olarak repoda durur ve **`GOREV-slice-3b`** spec'ine *"tasarım anayasası"* bölümü olarak girer. *Gerekçe (Cowork önerisi):* ADR 0001/0002/0003 **geri alınamaz mimari kararlardı** (katman sınırı, senkron mekaniği, kimlik şeması); bir renk token'ı ya da yay sabiti **geri alınabilir** — onu tam ADR kapı döngüsüne sokmak, kapı bütçesini mimari olmayan bir şeye harcamaktır (~1-2 gün). *Reddedilen:* harici notun istediği tam ADR-0004 + tam kapı döngüsü. **PAZARLIKSIZ İSTİSNA: a11y kapısı verify zincirine GİRER** — kontrast 4.5:1 · 48dp tap target · `MediaQuery.disableAnimations`'ta hareket token'ları sıfırlanır. O gerçekten ısıran bir kapıdır ve ODEV §2'nin "çalışan uygulama" ölçütüne dokunur.
- **[KARAR — Onur, 25 Tem 2026] K10-e: İMZA ÖĞELER MUST/NICE BÖLÜNDÜ — KESME SIRASI ŞİMDİDEN ADLANDIRILDI.**
  - **MUST:** (1) **Canlı-ayrıştırmalı NLP quick-add** — ODEV §4.1'in zaten kilitli tek farklılaştırıcısı. Özel `TextEditingController.buildTextSpan`; **WidgetSpan YASAK** (`flutter#128305`, doğrulandı); IME composing aralığına dokunulmaz; altta silinebilir chip önizleme sırası + `SemanticsService.announce`; **Türkçe tarih ayrıştırıcı deterministik ~150 satır kural** (`chrono_dart` İngilizce-only) ⇒ **property + mutant kapısı kurulabilir — KAPI DOKTRİNİ FRONTEND'E TAŞINIR**; tıkla-iptal yalnız tarih token'ında. (2) **Mürekkep-dolumu checkbox ritüeli** — `CustomPainter` + motor spring + haptik + `Semantics(checked)` + 48dp. *Tek "vay be" anı, ucuz.*
  - **NICE (yalnız TAMPON AÇILIRSA):** editoryal Fraunces gün başlığı · karanlık modda mesh aurora · ödül gibi boş-durumlar · ses tasarımı (`flutter_soloud`) · confetti · partikül süpürme · parallax kart · gooey geçiş.
- **[COWORK İTİRAZI — KAYDA GEÇTİ, gizlenmiyor] Harici not, tasarım yönü kılığında bir KAPSAM GENİŞLEMESİ taşıyordu.** ODEV §4.1 farklılaştırıcıyı *"YALNIZ NLP"* diye kilitlemiş ve **Kanban+Takvim'i kalite değil TAKVİM gerekçesiyle** elemişti. Notun imza-öğe listesi (checkbox ritüeli + gün başlığı + aurora + ödül boş-durumlar + ses + haptik + confetti + partikül + gooey + parallax) **aynı ağırlıkta bir yüktü ama eleme süzgecinden hiç geçmeden geliyordu.** Ölçüm: bugün **25 Tem**, plan 3a'ya **21-23 Tem** diyordu ve 3a **hâlâ kilitli değil** (9 bloker) ⇒ **zaten 2+ gün geride**; tampon **toplam 3 gün**; notun istediği ADR-0004 kapı döngüsü **1-2 gün**; **~11 yeni Flutter paketi** her biri **lisans+CVE kapısı** (kırmızı çizgi 3) + `dependencies.md` satırı — **not bunu hiç bütçelememişti**. Kaba toplam **3-5 gün** = tamponun tamamı ve fazlası. **K10-d + K10-e bu itirazın karşılığıdır.** *Karşı argüman da kayda geçti (zayıf değil): ODEV §2 "önce uygulamaya bakılacak" diyor ⇒ görsel etki birincil ölçüte doğrudan hizmet ediyor; soru "yapılsın mı" değil "ne kadarı ve ne zaman"dı.*
- **[COWORK İTİRAZI — teknik, küçük] `google_fonts` GEREKSİZ.** K10-c fontları subset edip **asset olarak gömüyor**; bu durumda `google_fonts` paketine hiç ihtiyaç yok (`TextStyle(fontFamily:)` yeter). Harici not ikisini birden istiyordu. **Paket listesinden ÇIKARILDI** ⇒ bir bağımlılık, bir lisans satırı, bir CVE kalemi eksildi.
- **[SIRA — PAZARLIKSIZ] `slice-3a-auth` KİLİTLENMEDEN tasarım işine BAŞLANMAZ.** Güzel bir checkbox animasyonu, ödenmemiş bir kimlik borcunu ödemez. Ayrıca **web demo notları** (spec'e taşınacak): `--wasm` build (skwasm + CanvasKit fallback) + `FragmentProgram` precache · **UI'da emoji YOK** (web'de Noto Color Emoji geç-indirme tuzağı) · M3 Expressive Flutter'da **YOK** (`flutter#168813` açık, ekip "stratejik duraklama"da) ⇒ özel token sistemi mid-2026'da **gerçek farklılaştırıcı**.
- **Sıradaki ilk iş:** 3a kilitlendikten SONRA → `DESIGN.md` (token + ban-list + a11y kuralları) → `design:design-critique` + `design:accessibility-review` kapıları (red-team EN SON) → `GOREV-slice-3b` spec'ine gömülür. **Onur "başla" DEMEDEN başlanmaz.**

## ⏭ CHECKPOINT (25 Tem 2026 — oturum 13: **ARGON2ID PAKET KAPISI KOŞULDU → GEÇTİ (Konscious)**; K9 kilitlendi)
- **Protokol koşuldu:** `docs/ODEV.md` (tam) + `CLAUDE.md` (tam) + bu dosyanın oturum 10-12 checkpoint'leri + KİLİTLENEN KARARLAR + KARAR GÜNLÜĞÜ + AÇIK İŞLER okundu. **`git status` Desktop Commander ile kaynaktan ölçüldü: ağaç TEMİZ · HEAD `060a37a` · origin/main `56362ed` · 15 ileri · push yok** — devir notuyla birebir uyuştu.
- **[HAFIZA DÜZELTMESİ — Onur onayladı] KİLİTLENEN KARARLAR bölümü GEÇERSİZ KILINMIŞ üç karar taşıyordu** (satır ~226/228/229): "farklılaştırıcılar **dördü de**" (K7-a ile YALNIZ NLP'ye indi) · "tempo **~4-6 hafta**" (K7-b ile ~15 Ağu oldu) · "AI için **Semantic Kernel**" (K7-a ile AI asistan ELENDİ ⇒ SK projede YOK). Üstleri çizilip yürürlükteki karar + pini yazıldı; **eski metin SİLİNMEDİ** (karar izi korunur). *Kusur sınıfı: tepedeki checkpoint ile gövdedeki kilit bloğu arasında çelişki — dosyayı yukarıdan okuyan sonraki oturum yanlış kapsam alırdı.*
- **[KAPI SONUCU — K8-c'nin ŞARTI, KAYNAKTAN ÖLÇÜLDÜ] Argon2id lisans+CVE+bakım+ÇALIŞIRLIK kapısı KOŞULDU. HÜKÜM: GEÇTİ.** Seçilen: **`Konscious.Security.Cryptography.Argon2` 1.3.1**. PBKDF2 geri dönüşü (K8-c) **TETİKLENMEDİ**.
  - **Konscious 1.3.1 — GEÇTİ:** lisans **MIT** (nuspec SPDX + GitHub `license.spdx_id`; geçişli `Konscious...Blake2` 1.1.1 de MIT, `System.Memory` 4.5.4 net9'da facade) · **CVE 0** (`dotnet list package --vulnerable --include-transitive`, Onur'un makinesinde, NuGet zafiyet akışı **2026.07.24** tarihli) · net9.0 restore+build **0 uyarı 0 hata** · **FİİLEN ÇALIŞTI: OWASP parametreleriyle (m=19456 KiB, t=2, p=1) 32 baytlık hash, 270 ms** (gerçek `dotnet run` çıktısı).
  - **⚠ ADLANDIRILMIŞ RİSK — GİZLENMİYOR: Konscious ~25 AY HAREKETSİZ.** `pushed_at = 2024-06-18`, son NuGet sürümü 1.3.1 (19 Haz 2024), GitHub'da hiç release yok, **20 açık issue + 3 açık PR**, arşivlenmemiş, 6.9M indirme. Bir CVE düşerse yamayı gönderecek bakımcı olmayabilir.
  - **`Isopoh.Cryptography.Argon2` 2.0.0 — RET (LİSANS).** Üç kaynak üç farklı şey diyor: nuspec **CC BY 4.0** · GitHub SPDX **`NOASSERTION`** · repoda "Unknown LICENSE" + `CC0-1.0 COPYING.txt`. Hiçbiri OSI onaylı bir **yazılım** lisansı değil (CC, kendi SSS'sinde CC lisanslarını yazılım için önermez). Kırmızı çizgi 3 + `dependencies.md`'nin "izinli OSI ailesi" kaydı ⇒ parola hash'i için kabul edilemez. Son sürüm Ağu 2023.
  - **`NSec.Cryptography` 26.4.0 (libsodium) — RET (İŞLEVSEL, lisans/bakım DEĞİL).** MIT ✓, CVE 0 ✓, aktif bakım ✓ (`pushed_at = 2026-04-30`) — **ama hedef platformda OWASP parametreleriyle KOŞMUYOR.** Onur'un makinesinde ölçülen: `p=1 m=19456KiB t=2` → `CryptographicException` · `m=65536` → FAIL · `m=262144` → FAIL · **yalnız `m=8KiB` (11 ms) ve `m=1024KiB` (686 ms) çalıştı** = kriptografik olarak savunulamaz seviye. Ayrıca **`p=2` ve `p=4` daha nesne kurulurken `ArgumentException: parameters out of range`** ⇒ libsodium Argon2id **yalnız p=1** kabul ediyor. *Sebep HİPOTEZ olarak işaretlendi (KANITLANMADI): libsodium `sodium_malloc`/`VirtualLock` kilitli bellek istiyor, Windows'un varsayılan işlem-başına kilitli bellek kotası ~1-19 MiB arasında ısırıyor; eşiği daraltan koşu cihaz koptuğu için yarım kaldı.* **ODEV §2 ("değerlendiricinin makinesinde kesinlikle çalışacak") karşısında belirleyici.**
  - **[SÜREÇ NOTU] "Aktif bakımlı olan aday koşmadı, dormant olan koştu."** Bakım metriği tek başına seçim ölçütü olsaydı yanlış paketi seçerdik. Kapının çalışırlık bacağı (fiilen koştur) bu turda **belirleyici olan bacaktı** — kör kapı doktrininin paket seçimine uygulanmış hâli.
- **[KARAR — Onur, 25 Tem 2026] K9: HASH = Konscious Argon2id + MİMARİ İZOLASYON.** Dormant-bakım riski **kapatılmıyor, izole ediliyor**: (1) `IPasswordHasher` portu (Application/Domain'de arayüz, Infrastructure'da impl) ⇒ paket tipi katmanlarda sızmaz · (2) hash string'ine **algoritma kimliği + parametreler gömülür** (`$argon2id$v=19$m=19456,t=2,p=1$<salt>$<hash>`) ⇒ PBKDF2'ye veya yeni parametreye geçiş **tek sınıf değişikliği + girişte sessiz rehash** olur, migration değil · (3) **ISIRAN KAPI:** eski formüllü/eski parametreli bir hash başarılı girişte **rehash EDİLMEZSE test KIRILMALI**. *Gerekçe (Onur ölçütü, K8-d ile aynı): değerlendiriciyi etkileyecek olan "Argon2id kullandım" değil, "bağımlılığı ölçtüm, dormant olduğunu gördüm, arkasına port koydum ve yarın değiştirebilirim"dir.*
- **Sıradaki ilk iş:** (1) **ADR 0003 — kimlik & yetkilendirme** yaz (K8-a..d + K9 + üç adayın kapı tablosu + `ICurrentUser` sözleşmesi + owner query-filter'ın yeri + push/pull-authz aktivasyonu + "kripto primitifini yazmayız, akışı yazarız" ilkesi). (2) **Bağımsız kapı** (üreten ≠ denetleyen: ADR'yi yazan el onaylamaz) → Onur kilitler. (3) `GOREV-slice-3a-auth` spec'i → **en çok İKİ denetim turu** (K6 tavanı) → Claude Code build → **Cowork TEMİZ OTURUMDA bağımsız doğrular.**
- **NOT (ortam, tanı):** NSec'in yüksek-bellekli probe'u koşarken **cihaz köprüsü İKİ KEZ düştü** (aynı işlemde). Üçüncü kez denenmedi. Kapı sonucu bundan etkilenmedi (belirleyici ölçümler kopmadan önce alındı), ama libsodium'un kilitli-bellek davranışı bu makinede sorunlu — **NSec'e ileride dönülürse bu not hatırlanmalı.**
- **[ADR 0003 YAZILDI — TASLAK v1] `docs/ADR/0003-kimlik-ve-yetkilendirme.md`** (24.674 bayt). Onur iki çatalı kilitledi: **owner filtresi = EF global query filter + `IgnoreQueryFilters` yasağı** (reddedilen: yalnız açık depo filtresi · ikisi birden ⇒ ölü tuzak) · **kaba-kuvvet savunması KAPSAM İÇİ** = .NET yerleşik RateLimiter (yeni NuGet yok; hesap KİLİTLEME değil). Belge K3-A..K3-K + 12 mutantlık ısıran-kapı tablosu içeriyor.
- **[BAĞIMSIZ KAPI — BİR TUR KOŞTU] HÜKÜM: KİLİTLENEMEZ (iki denetçi de bağımsız olarak).** Üreten ≠ denetleyen uygulandı: ADR'yi yazan el onaylamadı; iki ayrı bağımsız denetçi (mimari + red-team) koşturuldu. **Toplam: 9 BLOKER · 22 MAJÖR · 9 MİNÖR · 9 EKSİK MUTANT.**
  - **[COWORK DOĞRULAMASI — alt-ajan beyanı AKTARILMADI, kaynaktan yeniden ölçüldü]:**
    - **B1 DOĞRULANDI (ve düzeltildi):** ADR 0003'te `SignalR|hub` için `Select-String` **2 eşleşme** verdi — ama **ikisi de "GitHub" kelimesinin içindeki 'hub'**; SignalR/hub **konu olarak SIFIR kez geçiyor**. Denetçinin "grep = 0" iddiası özde DOĞRU. `PROJE_HAFIZA:191` (slice-2b2 kilidi) *"hub deny-by-default (gerçek auth hâlâ auth diliminde)"* diyor ⇒ **hub kimliği bu dilimin borcudur ve ADR onu hiç ele almıyor.** Gerçek zamanlı işbirliği iki taç mücevherden biri; kimliksiz gösterilemez.
    - **B4 DOĞRULANDI ve BEKLENENDEN AĞIR:** ADR 0003'te `outbox_messages` **0 kez** geçiyor. `PROJE_HAFIZA:145` **AÇIK BULGU C** birebir: *"`outbox_messages.owner_id` doğrulanmamıştır, bu dilim düzeltmez (**auth dilimi birleştirmeli**)"*. ⇒ **Auth dilimine atanmış BEŞİNCİ bir borç varmış; K8-a'nın "dört borç" sayımı EKSİKMİŞ.** Pull kapsam filtresi ve K2-F2'nin `user:{owner_id}` sinyal grubu hâlâ istemci-beyanlı `WireOp.ActorId`'den türeyen bir kolona dayanıyor ⇒ **K3-F2'nin "ActorId yetki kararına hiçbir yolla girmez" iddiası arka kapıdan çürüyor** ve M2 mutantı bunu göremiyor. ADR §7 yine de dört borcu "✓" ilan ediyor — **fazla iddialı beyan.**
    - **B5/R6 DOĞRULANDI — İKİ DENETÇİ BAĞIMSIZ OLARAK BULDU (yakınsama sinyali):** ADR §1 dilimin varlık gerekçesini dört istemci sorusuna dayandırıyor (*"bu satır kimin" · "token nerede duruyor" · "401'de kuyruk" · "çıkışta yerel DB"*), §2'de **yalnız birincisi** karara bağlanmış. ⇒ Dilim, **kendini haklı çıkaran işi yapmadan** kapanıyor; 3b builder'ı tam da önlenmek istenen yerde kararsız kalır. **Güvenlik sonucu somut:** Web'de yenileme token'ı `localStorage`'a konursa tek XSS onu sızdırır; çıkışta yerel Drift DB'sinin silinmesi red line #4'ün istemci-tarafı ihlalidir.
  - **DİĞER BLOKERLER (kendi ADR metnimden doğrulanabilir, hepsi TUTUYOR):** **R1** zarafet penceresinin yüklemi tanımsız ⇒ saldırgan 5 sn'de bir `/refresh` çağırarak **sonsuz zarafet zinciri** kurar, reuse-detection **yapısal olarak erişilemez** olur · **R2** hız sınırlama anahtarı (IP+e-posta) partition'ı **saldırgana seçtiriyor** ⇒ rastgele e-postalarla sayaç hiç dolmaz, her istek 270 ms + 19 MiB dummy Argon2 yakar ⇒ **Risk #5'in "hız sınırlama DoS'u kaldırır" telafi iddiası YANLIŞ**; ayrıca parola-püskürtme hiç tetiklenmez · **R3** `/register` açık + unique index ⇒ **sayım oracle'ı bitişik uçta ücretsiz duruyor** ve K3-J2'nin kapsamında bile değil ⇒ K3-B5'in "PAZARLIKSIZ" iddiası **kendi belgesi içinde yanlışlanıyor** · **R4** `TokenValidationParameters` hiç kararlaştırılmamış (`ClockSkew` varsayılanı 5 dk ⇒ beyan edilen "≤15 dk" fiilen **≤20 dk**; `alg` pinlenmemiş; `MapInboundClaims=false` yok ⇒ `sub` claim'i sessizce kaybolur) · **R5** `family_id`'nin ne zaman doğduğu tanımsız ⇒ K3-C2'nin "tüm oturumlar düşer" cümlesi ile logout semantiği çelişiyor · **B2** `User`'ın senkronlanabilir kök olup olmadığı yazılmamış ⇒ global filtre `User`'a uygulanırsa **anonim `/login` `UnauthenticatedException` alır, giriş fiziksel olarak kilitlenir** · **B3** global filtre `/sync` pull'una **fiziksel olarak erişemez** (ham SQL, `commit_xid`/`server_seq` EF'te map edilmemiş) ⇒ K3-E1'in *"tek sorgu bile sızdıramaz"* gerekçesi taç mücevher yolunda geçersiz, **ve pull-authz mutantı YOK** · **R7** "kör kapı YOK" diyen belge en tehlikeli mekanizmasını (zarafet penceresi), en geniş kontrolünü (deny-by-default) ve kendi *"en sinsi ihlal"* ilan ettiği davranışı (`Guid.Empty`) **mutantsız** bırakmış.
  - **MUTANT TABLOSU HÜKMÜ:** 12 mutantın **M1 ÖLÜ TUZAK** (zarafet penceresi baseline'da da iptali engeller ⇒ test kırmızı doğar, "düzeltilir", mutant sessizce ölür), **M7 pratikte ölü tuzak** (istatistiksel eşik ⇒ flaky ⇒ gevşetilir; yapısal ölçüte çevrilmeli), **M3 yarım ölü tuzak** (K3-G1 pull filtresi ayrı katmanda ⇒ EF filtresi kaldırılınca `/sync` hâlâ filtreli), **M2/M9 yarım** (create yolu / yaşam döngüsü kapsanmıyor), **M4/M6 sinyali yanlış yazılmış** (banned-API ⇒ "test FAIL" değil "derleme kırılır"). **9 eksik mutant adlandırıldı** (M13-M21: zarafet sınırı · deny-by-default · `Guid.Empty` · `ClockSkew`/`alg` · mutlak ömrün döndürmeyle uzaması · logout aile kapsamı · dispatcher `ICurrentUser` · sahiplik TOCTOU · e-posta `Trim`/NFC).
  - **DENETÇİLERİN TEMİZ ÇIKARDIKLARI (dürüst kalibrasyon — pile-on yapmadılar):** UUIDv7 ile **hedefli entity-id işgali UYGULANABİLİR DEĞİL** (RFC 9562: 48 bit zaman + **74 bit CSPRNG** ⇒ 2⁷⁴) · hız sınırlama anahtarının DB'ye bakmadan istekten türemesi sayım oracle'ı **üretmiyor** · katman yönü **DOĞRU** (`ICurrentUser` Application ← `DbContext` Infrastructure = DIP'e uygun, gizli ters bağımlılık yok, hiçbir NetArchTest kuralı gevşemek zorunda değil) · `ToLowerInvariant`+`COLLATE "C"` `I/ı` çatalını **doğru** kapatıyor · Argon2id/RateLimiting/25-ay aritmetiği **doğru** · slice-1 KANIT'ı BannedApiAnalyzers'ın **canlı ısırdığını** zaten kanıtlamış ⇒ M6/M10'un "derlemeyi kırar" iddiası mekanik olarak sağlam.
  - **YENİ BULGU (denetçinin ADR'nin kendi gerekçesini çürüttüğü yer):** K3-G2 imleç yan-kanalı *"kapatılamaz"* diye beyan edilmişti — **YANLIŞ**: `server_seq` bir `IDENTITY` kolonu olduğu için sızan şey "hacim çıkarımı" değil **tam sayaçtır** (ve `commit_xid` üzerinden **PostgreSQL instance'ının toplam txn hızıdır**), ve **imlecin istemciye opak/HMAC'li döndürülmesi** semantiği bozmadan bunu tam olarak kapatır. Ayrıca boş-sayfa `nextCursor` çatalı hiç kurulmamış (küresel ufuk ⇒ sızıntı; eski imleç ⇒ her boş ankette O(toplam yazma) tarama = DoS çarpanı).
- **[SÜREÇ] K6 TAVANI: bu ADR için BİR tur denetim koştu, BİR tur hakkı kaldı.** Bulgu hacmi (9 bloker) tavanın *"ikincisi de temiz çıkmazsa mekanizma tartışılır"* şartını ciddi kılıyor: v2 tek turda kapanmazsa ADR'nin **kapsamı bölünmeli** (kimlik çekirdeği vs senkron-yetki aktivasyonu), çünkü tek ADR'ye dört borç + hub + outbox yüklemek K6'nın *"tek devasa spec"* dersinin ADR düzeyindeki tekrarı olur.
- **Sıradaki ilk iş (TEMİZ OTURUM — dolu bağlamda ADR/spec yazma yasağı yürürlükte):** (1) Onur bloker çözümlerini işaretlenebilir şıklarla **kilitler** (özellikle: hub kimliği bu dilimde mi 3b'de mi · `outbox.owner_id` birleştirme · zarafet penceresi mi istemci-mutex mi · `/register` sayımı kabul mü edilecek · `family_id` = giriş başına mı). (2) **ADR 0003 v2** yazılır. (3) İkinci ve SON denetim turu. (4) Kilit → `GOREV-slice-3a-auth` spec'i.

## ⏭ CHECKPOINT (20 Tem 2026 — oturum 12 devamı: **K8 — slice-3a-auth TASARIMI KİLİTLENDİ**, build BEKLEMEDE)
- **[COWORK BULGUSU — ADR taraması, kaynaktan ölçüldü] "İnce kimlik dilimi" göründüğünden büyük.** ADR'ler auth dilimine **dört ADLANDIRILMIŞ ertelenmiş gereksinim** biriktirmiş: **K-D5** (`0001:64` — `ICurrentUser` portu slice-1'de arayüz, *"implementasyonu + owner query-filter kimlik dilimiyle kodlanır"*) · **M-G push-authz** (`0002:145` — *"ingest, her op için 'actor bu entity'yi yazabilir mi' kontrolü yapmalı. Mekanizma auth diliminde"*) · **K2-E3 pull-authz** (`0002:217,253`) · **M-C `clientId→principal`** (`0002:244`). **TAHMİN DÜZELTİLDİ: 1,5-2 gün → 2,5-3 gün.** (21-23 Tem penceresi tutuyor, ama tampon yemiyor demek yanlış olurdu.)
- **[KARAR — Onur] K8-a: AUTH KAPSAMI = TAM AKTİVASYON.** Dört ertelenmiş gereksinimin **dördü de** bu dilimde açılır: `ICurrentUser` impl + owner query-filter + push-authz (owner eşitliği düzeyinde) + pull-authz + `clientId→principal`. *Gerekçe:* 3b'nin tüm depo/senkron katmanı bunların üstüne kurulacak; sonradan açmak yeniden yazımdır. *Reddedilenler:* yalnız kimlik+owner filter (M-G/E3 açık kalırdı) · yalnız token doğrulama (veri izolasyonu olmadan çok-kullanıcılı demo ⇒ işbirliği vitrini gösterilemez).
- **[KARAR — COWORK] K8-b: TOKEN = kısa ömürlü JWT (~15 dk) + OPAK yenileme token'ı (DB'de, DÖNDÜRMELİ + YENİDEN-KULLANIM TESPİTİ).** *Gerekçe:* yenileme çevrimdışı mimaride zaten zorunluydu (K7-c); opak+DB seçildi çünkü **gerçek çıkış ancak iptal edilebilir token'la olur**, ve döndürme+reuse-detection **ısıran kapı kurulabilir bir davranıştır** (mutant: tüketilmiş yenileme token'ı kabul edilirse test KIRILMALI). *Reddedilen:* stateless JWT-yenileme (iptal edilemez, kapı kuracak davranış yok) · tek uzun ömürlü JWT (24 saat sonra kuyruk gönderilemez ⇒ çevrimdışı vitrinini zedeler).
- **[KARAR — Cowork önerisi, Onur onayladı] K8-c: PAROLA HASH = Argon2id — ŞARTLI.** OWASP birincil önerisi; .NET BCL'de yok ⇒ üçüncü parti paket. **ŞART [PAZARLIKSIZ]: spec'ten ÖNCE lisans + CVE + bakım-durumu kapısı koşulur** (kırmızı çizgi 3). **Paketin bakım geçmişi DOĞRULANMADI — iddia edilmedi.** Kapı düşerse **PBKDF2'ye (BCL, sıfır bağımlılık) dönülür**; bu geri dönüş şimdiden onaylıdır, yeni karar gerektirmez.
- **[KARAR — COWORK] K8-d: ASP.NET Core Identity KULLANILMAYACAK — ince elle implementasyon.** Onur ölçütü verdi: *"mimari ve kod açısından görecek kişiyi en çok ne etkileyecekse o."* *Gerekçe:* Identity `DbContext`'i `IdentityDbContext`'e çevirir, Infrastructure tiplerini yukarı iter, 7 tablonun 5'i kullanılmaz ⇒ **mevcut NetArchTest kapıları gevşetilir veya istisna alır. Kendi kurduğun kapıyı üçüncü parti için gevşetmek, kod kalitesi ölçen bir ödevde en kötü sinyaldir.** Kapsam darken (parola sıfırlama/2FA/kilitleme YOK) elle implementasyon ~200-300 satır ve **kriptografi yazılmıyor** — Argon2id üçüncü partiden gelir, yalnız akış yazılır. **ADR 0003'e aynen girecek ilke: kripto primitifini yazmayız, akışı yazarız.** *Reddedilen:* Identity (katman baskısı) · melez `PasswordHasher<T>` (hash kararını fiilen PBKDF2'ye kilitlerdi).
- **[ONUR TALİMATI] Haftalık kullanım limiti yaklaşıyor. "İşlere başla kararları al, ben BAŞLA diyinceye kadar bekle."** ⇒ Kararlar alındı ve yazıldı; **ADR 0003 ve spec YAZILMADI, Claude Code'a görev VERİLMEDİ.** Onur'un açık "başla" komutu beklenir.
- **Sıradaki ilk iş (Onur "başla" dediğinde):** (1) **Argon2id lisans+CVE+bakım kapısı** koş (düşerse PBKDF2, K8-c). (2) **ADR 0003 — kimlik & yetkilendirme** yaz (K8-a..d + Identity'yi eleme gerekçesi + `ICurrentUser` sözleşmesi + owner filter yeri + push/pull-authz aktivasyonu). (3) Bağımsız kapı → kilit. (4) `GOREV-slice-3a-auth` spec'i → **en çok İKİ denetim turu** (K6 tavanı) → Claude Code build → **Cowork TEMİZ oturumda bağımsız doğrular.** Isıran kapı adayları şimdiden: tüketilmiş yenileme token'ı kabulü · `WireOp.ActorId`'nin token `sub`'ı yerine kullanılması (F5) · owner filter'ın atlanması.

## ⏭ CHECKPOINT (20 Tem 2026 — oturum 12: **İKİ AÇIK KARAR KAPANDI + K7** — kod/spec HENÜZ YAZILMADI)
- **Protokol koşuldu:** `docs/ODEV.md` (tam) + `CLAUDE.md` (tam) + bu dosya (oturum 6-11 checkpoint'leri, KİLİTLENEN KARARLAR, KARAR GÜNLÜĞÜ, AÇIK İŞLER) okundu. **`git status` Desktop Commander ile kaynaktan ölçüldü: ağaç TEMİZ · HEAD `d77fcb9` · origin/main `56362ed` · 13 commit ileri · push yok** — devir notuyla birebir uyuşuyor.
- **[COWORK ÖLÇÜMÜ — TAHMİN, devir notunda yoktu] Takvim aritmetiği tutmuyordu.** 1-8 Ağu penceresi **12-19 gün**; iş kırılımı (Flutter iskelet+Drift+kuyruk 3-4g · çekirdek parite ekranları 3-4g · RRULE+hatırlatıcı 1-2g · çakışma demosu 1g · gerçek zamanlı istemci 2g · auth 1-2g · CI/CD+paketleme 1g · NLP 1-2g) ham **13-18 gün**, bu projenin kapı yüküyle (~%30, geçmiş dilimlerden) **17-23 gün**. **Sığmıyordu.** Bu ölçüm Onur'a karar öncesi sunuldu.
- **[KARAR — Onur, 20 Tem 2026] K7-a: FARKLILAŞTIRICI = YALNIZ NLP HIZLI EKLEME.** Havuzdan bir tanesi. *Gerekçe:* §5'e göre Todoist'in **en çok övülen etkileşimi**; ayrıştırıcı **saf/deterministik** ⇒ property + mutant kapısı kurulabilir (kapı doktrini geçerli kalır); **backend değişikliği SIFIR** (tarih/öncelik/etiket/RRULE alanları 3a'da materyalize). *Elenenler adlandırıldı:* **AI asistan** (LLM deterministik değil ⇒ **mutantla ısırtılamaz**, kapısız kalır veya kapı tiyatrosu doğurur; API anahtarı + çevrimdışı vitriniyle çelişki) · **Google Takvim/time-blocking** (OAuth + dış API + **ikinci bir senkron/çakışma problemi**; değerlendiricinin makinesinde ağ/hesap olmadan çalışmaz ⇒ "kesinlikle çalışan uygulama" ölçütüne tehdit) · **Kanban+Takvim** (kalite değil **takvim** gerekçesiyle; `boardPos` backend'de hazır, drag-drop ~2-3 gün + kırılgan widget testi + a11y yükü ⇒ **tampon açılırsa İLK geri çağrılacak aday**). `docs/ODEV.md` §4.1'e yazıldı.
- **[KARAR — Onur, 20 Tem 2026] K7-b: KAPSAM SABİT, TAKVİM ESNEDİ → hedef ~15 Ağu 2026.** *Reddedilenler:* kesme sırasını şimdi kilitleyip alttan düşürmek · kapsam+takvim ikisini de sabitleyip kapı yükünü düşürmek. **UYARI kayda geçti:** esnetilen tarih şirketin verdiği tarih değil (ODEV.md §8(2) **hâlâ açık** — ödevin ne zaman/hangi biçimde beklendiği bilinmiyor); yani esnetme maliyeti dışsal değil, **ölçülemiyor**. Plan [TAHMİN]: 21-23 Tem `slice-3a-auth` · 24 Tem-5 Ağu `slice-3b` · 6-8 Ağu gerçek zamanlı istemci · 9-10 Ağu NLP · 11-12 Ağu CI/CD+paketleme · 13-15 Ağu tampon+teslim. **Tampon yalnız 3 gün.**
- **[KARAR — COWORK, 20 Tem 2026] K7-c: AUTH = İNCE KİMLİK DİLİMİ (`slice-3a-auth`), slice-3b'den ÖNCE.** Onur kararı açıkça Cowork'e bıraktı ("en uygun yol ne ise sen belirle") ⇒ **bu bir Onur kilidi değil, Cowork kararıdır; gerekçesi denetlenebilir olsun diye tam yazıldı.** *Gerekçe:* (1) kimlik burada özellik değil **ŞEMA kararı** — yerel satırın sahibi, token deposu, 401'de kuyruğun kaderi, çıkışta yerel DB Drift şemasını belirler; sonraya bırakmak migration + depo katmanı yeniden yazımı demektir. (2) **Gerçek zamanlı işbirliği vitrini iki gerçek kullanıcı ister.** (3) **F5 kilidi ancak böyle fiilen devreye girer** — bugün `WireOp.ActorId` istemci-beyanlı ve satır enjeksiyonu yüzeyi açık. *"Tam auth" bilinçle seçilmedi:* parola sıfırlama/e-posta doğrulama demoya sıfır katkı + SMTP/sır/dış bağımlılık ⇒ **§4.1'de AI ve Google Takvim'i eleyen risk sınıfının aynısı**. *Ama yenileme token'ı DÂHİL* — çevrimdışı istemcide erişim token'ı dolduğunda kuyruktaki yazımların kaybolmaması **mimari zorunluluk**, konfor değil. *Auth kapsam dışı [adlandırılmış]:* parola sıfırlama · e-posta doğrulama · OAuth/sosyal giriş · 2FA · RBAC.
- **DEĞİŞEN DOSYALAR:** `docs/ODEV.md` §3.1 (takvim) · §4.1 (farklılaştırıcı seçimi + eleme tablosu) · §6.1 (auth kararı) · §8 (madde 3 KAPANDI, madde 2 vurgulandı, madde 5 eklendi). **Kod/spec YAZILMADI.**
- **Sıradaki ilk iş:** `slice-3a-auth` **tasarımı** işaretlenebilir şıklarla Onur'a sunulacak (protokol adım 5: token modeli · parola hash'i · kullanıcı↔veri ilişkisi · ısıran kapı tasarımı) → kilit → spec → **en çok İKİ denetim turu** (K6 tavanı) → Claude Code build → Cowork temiz oturumda bağımsız doğrular. Sonra `slice-3b`. **UYARI: 3b'nin tek devasa spec olarak yazılması K6'nın dersine aykırıdır — alt dilimlere bölünmesi 3b tasarımında ayrıca kararlaştırılmalı.**

## ⏭ CHECKPOINT (20 Tem 2026 — oturum 11 devamı: **ÖDEV KAPSAMI YAZIYA GEÇTİ → ÖNCELİK DEĞİŞTİ**)
- **`docs/ODEV.md` OLUŞTURULDU — kapsam otoritesi artık O dosyadır**, hafızadaki türev cümleler değil. Ödev **sözlü** verilmiş, **yazılı kapsam gelmeyecek** (karşı taraf adayın hayal gücünü/kapsam kurma yeteneğini de ölçüyor) ⇒ **kapsamı biz tanımlıyoruz ve tanımın kendisi değerlendirmeye tabi.**
- **[YENİ BİLGİ — 1] DEĞERLENDİRME: "kesinlikle çalışan uygulama olacak; ÖNCE UYGULAMAYA bakılacak, SONRA mimari ve kod."** (Onur teyit etti.)
- **[YENİ BİLGİ — 2] SÜRE: 2-3 hafta** (Onur'un kendi tahmini, şirket tarihi değil). Başlangıç 18 Tem ⇒ hedef **1-8 Ağu 2026**.
- **[DÜZELTME — kayda geçti] ÇEVRİMDIŞI ONUR'UN İSTEĞİYDİ.** Sözlü brief'te açıkça var: *"TickTick gibi çevrimdışı çalışsın, sonra senkron olsun."* Bu oturumda önce "Cowork önerisi" diye yazmıştım — **YANLIŞTI**, düzeltildi. Cowork'ten gelen şey **özellik değil, mekanizma derinliğidir** (HLC + katmanlı CRDT + oracle + property + 16 mutant); brief o derinliği istemedi, mimari-kalite ölçütüne cevap olarak seçildi. Köken tablosu `docs/ODEV.md` §7'de.
- **[KAPSAM KİLİDİ — Onur, 20 Tem 2026] ÇEKİRDEK PARİTE + 2 VİTRİN.** Çekirdek (görev/liste/proje/etiket/öncelik/tarih/RRULE/hatırlatıcı/arama/karanlık mod) + iki taç mücevher (çevrimdışı senkron + gerçek zamanlı işbirliği) + farklılaştırıcılardan **yalnız 1-2'si** (önceki "dördü de" kararı **DARALTILDI**; hangileri olduğu henüz seçilmedi).
- **[REFERANS UYGULAMALAR — Cowork araştırdı]** Onur örnek uygulamanın adını hatırlamadı, *"en çok indirme/beğeni alan hangisiyse o"* dedi. **Birincil: Todoist** (50M+ kullanıcı; en çok övülen etkileşim = doğal dille hızlı ekleme). **İkincil: TickTick** (Onur'un çevrimdışı referansı; iOS ~4.8 / Play ~4.5). *Rakamlar ikincil karşılaştırma kaynaklarından; oturum 1'deki uygulamanın bu ikisinden biri olduğu DOĞRULANMADI.*
- **⚠ ÖNCELİK SONUCU — K5'i (tek turda hepsi) YENİ BİLGİ GEÇERSİZ KILIYOR.** K3/K4/K5 kilitlendiğinde "önce uygulama" ve "2-3 hafta" **bilinmiyordu**. Bugünkü tablo: backend %100 kanıtlı, **istemci %0**, kritik yol **slice-3b**. Bir test kapısının (D5-a) onarım spec'i 3 oturum yedi ve yakınsamadı. Gerekçe doktrinin kendisi: *beyan edilmiş sınır kabul edilebilir, gizlenmiş sınır edilemez* — zayıf bir kapı, olmayan bir istemciden az zarar verir.
- **[KARAR — Onur, 20 Tem 2026] K6: DÜZELTME TURU ASKIYA ALINDI.** Süren oturum durduruldu. **`D5-a` ADLANDIRILMIŞ SAPMA olarak DONDURULDU** — kapı bugünkü (zayıf ama KANIT'ta açıkça beyanlı) hâliyle kalır. **K3/K4/K5 İPTAL DEĞİL, ERTELENDİ**: DÜZELTME-1 [v6] + DÜZELTME-2 [v1] ve bu oturumun 12 bulgusu yürürlükte duruyor; teslimden önce vakit kalırsa aynı kararlarla koşulur. **Kritik yol artık slice-3b.**
- **[SÜREÇ DERSİ — K6'nın gerekçesi, yazıya geçirildi] Denetim turlarına TAVAN yok idi ve maliyet kontrolsüz büyüdü.** Bir test kapısının onarım spec'i 6 tur / 3 oturum yedi, bloker dizisi (3→1→2→2→5→2) yakınsamadı ve üründe sıfır değişiklik üretti. **Bundan sonra her spec için tavan: en çok İKİ bağımsız denetim turu; ikincisi de temiz çıkmazsa mekanizma tartışılır, üçüncü tur yazılmaz.** Bu, "üreten ≠ denetleyen" doktrinini kaldırmaz — turların **sayısını** sınırlar.
- **[TAVANIN SINIRI — PAZARLIKSIZ, K6 ile birlikte kilitlendi] Tavan YALNIZ bir spec'in kaç kez yeniden YAZILACAĞINA uygulanır.** Şunlar tavana **TABİ DEĞİLDİR** ve hiçbir gerekçeyle atlanmaz: (1) bir kapının **ısırdığını mutantla kanıtlaması** (kör kapı yok) · (2) **Cowork'ün build'i bağımsız doğrulaması** (builder beyanına güvenilmez) · (3) mutantların **gerçekten koşulması** (akıl yürütmeyle "beklenir" yazılmaz — 2b1 BULGU-1) · (4) her iddianın **kaynaktan ölçülmesi** (alt-ajan/builder beyanı aktarılmaz) · (5) **kırmızı çizgiler** (sır yok · PII minimum · lisans+CVE · kalıcı silme Onur onayı · build artefaktı git-ignore). **"Artık tavanımız var" cümlesi doğrulamayı atlamanın gerekçesi OLAMAZ.**
- **[STANDARDIN DOĞRU OKUNUŞU] Kırmızı çizgi "kusursuz olmak" değil, "kusurunu GİZLEMEMEK"tir.** D5-a bugün olmasını istediğimizden zayıf bir kapıdır — bu gerçek bir kapsama kaybıdır, "sorun değil" denmez; telafisi **açıkça beyan edilmiş ve ertelenmiş** olmasıdır. Ayrıca son üç oturumda peşinde koşulan şey kusursuz **iş** değil kusursuz **belgeydi**: ürün kodu her kapıdan ilk doğrulanmış build'de geçti, yakınsamayan şey bir testi düzeltme talimatının yazımıydı. **Durma kuralı olmayan mükemmeliyetçilik yüksek standart değil, kalibrasyonsuz standarttır.**
- **[ÖNGÖRÜLEN EROZYON NOKTASI — slice-3b] Kalite kaybı bu kararda değil, 3b'de olur.** Şimdiye kadarki en büyük dilim, ilk kez zaman baskısı altında ve ilk kez Flutter tarafında. **3b'nin de kendi ısıran kapıları ve kendi KANIT'ı olacaktır** — "hızlı olsun" diye kapı atlanmaz. Tavan tur sayısına uygulanır, **kapıya değil.**
- **Sıradaki ilk iş:** **slice-3b spec'i** — Flutter istemci (Drift/SQLite + senkron kuyruğu + çevrimdışı anahtarı + çakışma demosu), platform **Web + Android**; artı çekirdek parite ekranları. Ardından **auth** (gerçek zamanlı işbirliği kimlik olmadan gösterilemez) ve **CI/CD**. Spec yazımından ÖNCE Onur'a işaretlenebilir şıklarla sunulacak iki açık karar var: **(1) hangi 1-2 farklılaştırıcı** (`docs/ODEV.md` §4c), **(2) auth'un 3b'den önce mi sonra mı geleceği.**

## ⏭ CHECKPOINT (20 Tem 2026 — oturum 11: BÖLÜNMÜŞ İKİ SPEC'E BİR TUR BAĞIMSIZ DENETİM — **TEMİZ DEĞİL, 2 BLOKER + 4 MAJÖR**)
- **Kapsam: yalnız çapraz-tutarlılık + bölünme deliği taraması, yeniden tasarım YOK.** Temiz oturum (spec'leri yazan oturum değil). Ağaç temiz, HEAD `6edfd13`, origin/main `56362ed`, 8 commit ileri, push yok. Hiçbir spec dosyası değiştirilmedi (yalnız bu checkpoint).
- **HÜKÜM: İKİ SPEC DE KİLİTLENEMEZ.** Bloker dizisi 3→1→2→2→5→2. **Altıncı tur da gerçek bloker buldu; sıfıra yakınsamıyor.**
- **B1 [BLOKER — A] `SONUC:` bloğu ve KANIT başlığı hiçbir kapsamda değil; `mutant-14`'ünki YANLIŞLANIYOR ama düzeltmek DUR sebebi. AYNI DİKİŞİN YEDİNCİ KIRILMASI.** Ölçtüm: `mutant-14-…txt:5` *"Isirmasi ZORUNLU: D5-b adim 1 FAIL."* · `:121-122` *"SONUC: mutant-14 ISIRDI (**SADECE** D5-b adim 1 oldu, spec'in zorunlu kildigi tam olarak buydu)."* — A §2:156 bu mutantın **2 test** kıracağını öngörüyor ve §6:415 `TOPLAM`'ı 2'ye çektiriyor ⇒ iki satır da yalan olur. A §6:381-382 kapsamı **(b)/(c)/ÖZET** ile sınırlıyor; kriter 5c:362 izin listesini *"**tam olarak** şunlardır, başkası **DUR** sebebidir"* diye kapatıyor. `grep -c 'SONUC'` **A'da 0, B'de 0**; `'Isirmasi ZORUNLU'` **A'da 0, B'de 0** ⇒ blok iki spec'te de hiç anılmıyor. Builder çıkışsız: ya repoda **fiilen yanlış bir KANIT** bırakır (görevin varlık sebebinin ta kendisi, A:141-143) ya da 5c'yi çiğneyip DUR verir. **Delik bölünmenin ürünüdür:** tüketici izin listesi (5c) v5'te YOKTU, v6'da iki görevin sınırını polislemek için eklendi ve `SONUC` sayılmadı. Diğer 7 mutantın SONUC'u ölçümle uyumlu kalıyor — kusur yalnız `mutant-14`'te.
- **B2 [BLOKER — B] ERRATA-A/ERRATA-B: emir ilan edilmiş, uygulatan KRİTER + RAPOR KALEMİ + YETKİ PİNİ YOK; ERRATA-B ayrıca kriter 6 ile çelişiyor.** B §3b iki yürürlükteki hükmü değiştiriyor (ERRATA-A → `ana spec:366` KANIT v2.1 (a); ERRATA-B → `cowork-bagimsiz-dogrulama.txt:133,139`). Ölçtüm: B'nin kriter (127-146) + teslim (148-153) bölgesinde `errata|ana spec|materyalizasyon|cowork-bagimsiz` = **sıfır eşleşme**. Sonuç: B bitince `ana spec:366` (a) kuralı ve `ana spec:416` kriter 4 (*"16 mutant, KANIT KURALI v2.1'e birebir uygun"*) **16/16 için yanlış** kalır ve hiçbir adım onarmaz. ERRATA-B'nin hedefi `KANIT/` altında ve **git'te izlenen** bir dosya (`git ls-files` ile teyit) ⇒ B kriter 6 (*"değişen satırlar yalnız (a) bölümünde"*) onu yazmayı yasaklıyor; yazmamak ölü ilan bırakıyor. **Bu, A'nın v6'da kriter 10 + YETKİ PİNİ ile kapattığı deliğin B v1'de birebir tekrarıdır** (asimetrik bölünme).
- **M1 [MAJÖR — B] Kriter 4 `mutant-13`'te sağlanamaz.** Kriter 4:132 *"**ÖZET'te adı geçen TÜM testler** adım 3'te FAIL verdi"*. Ölçtüm: `mutant-13-…txt` ÖZET'i **üç** test adı taşıyor ve ikisinin (`Task_field_keys_are_fully_covered_by_…`, `TaskList_…`) **YEŞİL kaldığını kendi cümlesiyle beyan ediyor** (`:137-139`). B §3 adım 3 doğru daraltmayı yapıyor (*"`HEDEF` başlığındakiler ve varsa beyan edilmiş yan ısırma"*), kriter 4 yapmıyor — ve dayatan kapı kriter 4. Sayısal tablo (**"kalan 13 mutant 1'er"**) ÖZET'lerle uyumlu ⇒ tek başına çelişki değil; kusur cümlenin genişliğinde. **Onur isterse BLOKER'a yükseltilebilir** (emsal: M1/oturum 10 majör sayılmıştı, "garantili yanlış DUR"lar bloker).
- **M2 [MAJÖR — B] `DÜZELTME-1:427` pini YANLIŞ, iki kez kullanılmış** (B:112 ve B:117). Ölçtüm: `"(a) OLDUĞU GİBİ KALIR"` A'da **tek eşleşme, satır 456**; A:427 alâkasız bir prose satırı. Aynı cümledeki `ana spec:366` ve `ana spec kriter 4` (=416) pinleri **DOĞRU**.
- **M3 [MAJÖR — B] §1'in "üçüncü kusur" sayımı 17 ediyor.** B:41-43 *"16 dosyanın index satır sayımı: **15 dosyada 1**, mutant-16'da 2, mutant-6'da 0"* ⇒ 15+1+1=17. Ölçtüm: **index=0 → 1 dosya · index=1 → 14 dosya · index=2 → 1 dosya = 16.** Doğrusu **14**. Sonuç (mutant-6'da 0) doğru; yanlış olan sayı — ve bu sayım hem "üçüncü kusur"un tek kanıtı hem kriter 5'in `index` yerine `diff --git` seçme gerekçesi.
- **M4 [MAJÖR — A] §6'nın `mutant-10`/`11` prose pini hiçbir kriterin kapsamında değil.** §6:417-432 gerekçe cümlelerini PAZARLIKSIZ değiştirtiyor **ve** §6:430-432 *"'yeni ham çıktıyla değiştir' bu iki cümle için **uygulanamaz**"* diyor ⇒ kriter 5b'nin *"ÖZET … **yeni ölçümle** güncellendi"* niteleyicisi onu tutmuyor; kriter 10 ise *"§6'nın **DÖRT** metinsel emri"* deyip SAPMA-4 + ERRATA-1/2/3'ü sayıyor — bu **beşinci** emir listede yok. (Kriter 10 tam da bu boşluk için v6'da yazılmıştı.)
- **M5 [MAJÖR — B, bölünme deliğinin cevabı] Adım 3'ün beyanı DÜRÜST ama ENSTRÜMANTASYONU EKSİK.** Handoff'un sorusu: yama ile (b)/(c) ölçümü arasındaki bayt-bağı zayıfladı, "öldürme listesini yeniden üretir" beyanı yeterli mi? **Cevap: kapı kör DEĞİL** (`dotnet build` → `dotnet test --no-build --filter`, sıra doğru; mutasyon uygulanmazsa yeşil geçer ⇒ DUR; yanlış dosya sayısı adım 2'nin PİN'inde DUR) **ama** kriter 4 yalnız *"test adı + sonuç"* istiyor ⇒ Cowork, B'nin adım-3 çıktısını A'nın kaydettiği **(b) ham kırmızısıyla karşılaştıramaz**. Bedelsiz sıkılaştırma: adım 3'ün **ham** koşucu çıktısı (assert mesajları dâhil) rapora girsin, Cowork onu (b) ile karşılaştırsın.
- **MİNÖRLER:** A §2'nin iki ana-spec hücre erratası (v5 §5:383/385) yetki pini + rapor (h) listesinin dışında (5b jenerik olarak kapsıyor) · B §2 *"(b)/(c)/ÖZET … onlar DÜZELTME-1'in ürünüdür"* — 8 mutant için doğru, kalan 8 için değil · A §7(2) `docs(kanit)` commit mesajı ana spec errata'sını tarif etmiyor · B'de adım 3'ün `dotnet build`'inin **0 hata** vermesi pinsiz (patlarsa `--no-build` eski ikililere koşar; yön güvenli — yanlış DUR — ama pinsiz) · naif `grep -c '\[Fact\]'` **55** verir (2 doc-yorumu eşleşmesi), kriter 3'ün "53"ü doğru ama ölçüm yöntemi pinsiz.
- **TEMİZ ÇIKAN — kaynaktan yeniden ölçüldü:** **Sayı dikişi bu turda TUTTU:** A §6 "4 ADIM"=4 · B §3 "6 ADIM"=6 · "on bir adım 0..10"=11 tablo satırı · kriter 10 "DÖRT"=4 · §2 8+8=16 · "kalan 13"/"kalan 15"/"kalan 7" hepsi doğru. **Beklenen kırılan-test sayıları ↔ KANIT `TOPLAM: Failed:`** birebir (1=2 · 16=3 · 2,7,9,10,11,13,14=1'er; 14→2 öngörülen). **110 aritmetiği doğru** (gerçek `[Fact]`: Arch 5 + Api 12 + SyncCore 40 + Persistence 53). **v6'da YENİ eklenen kaynak pinlerinin hepsi doğru** — `EntityMaterializer.cs:38` (`TaskProjection.From(entityId, state)`) · `MaterializationRoundTripTests.cs:15-18` (BEYAN yorumu) · `:92-95` ve `LiteralOracleD5bTests.cs:190-193` (`OrderOp`) · `OrSetField.cs:229-236` (`MaxStamp` tüm `Adds`'i tarar ⇒ ölü doğan add'ın damgası sayılır). `ana spec:366` ✓ · kriter 4=416 ✓ · `cowork:130/133/139` ✓. **16 mutasyonun tamamı yalnız `src/**`'te** ⇒ B §2'nin gerekçesi ve adım 4'ün `-- src` kontrolü yeterli. **`diff --git`/`@@`/`+++ b/` satırlarının hepsi 1) bölümünün içinde** ⇒ B kriter 5 ile kriter 6 birbirini dışlamıyor. `verify-run-raw/full` **git'te izleniyor** ⇒ A kriter 5c(ii) doğru. `mutant-10`/`11`'in yanlışlanan gerekçe cümleleri **ÖZET'in `YAN ISIRMA:` satırında** yaşıyor ⇒ 5c(i) onları kapsıyor (bu aday temiz çıktı).
- **[HAFIZA DÜZELTMESİ] `mutant-16` index↔dosya eşlemesi:** ölçtüm — `TaskProjection.cs` ↔ `455ebe5..4ea65b8`, `TaskListProjection.cs` ↔ `a8e52a5..75f98a7`. **A §6:398-400'ün sırası DOĞRU**; yukarıdaki oturum-10 B5 maddesinin konumsal okunuşu TERSTİR. Revert için önemsiz (ikisi de reverte edilir), kayıt için düzeltildi.
- **YÖNTEM NOTU:** bulguların bir kısmını bağımsız bir alt-ajan buldu; **hiçbirini beyanla kabul etmedim** — hepsini kaynak dosyadan kendim yeniden ölçtüm (`grep -c`, `sed -n`, `git ls-files`). Alt-ajanın bir "bloker"ını (kriter 4/`mutant-13`) **MAJÖR'e indirdim** (§3'ün daraltması var, sayısal tablo doğru); bir "bloker"ını (SONUC) ve bir "bloker"ını (ERRATA yetkisi) doğruladım. Ayrıca kendi ilk şüphemi (**Persistence `[Fact]`=55 ⇒ kriter 3 yanlış**) kaynaktan çürüttüm: iki eşleşme doc-yorumunda, gerçek sayı **53**, kriter 3 DOĞRU — SAPMA-3 dersi tuttu.
- **[KARAR — Onur, 20 Tem 2026] ÜÇ ŞIK DA KİLİTLENDİ (düzeltme HENÜZ YAZILMADI):**
  - **K3 — B1 çözümü: kriter 5c NEGATİFE ÇEVRİLİR + İÇ-TUTARLILIK KRİTERİ.** Tüketici beyaz liste kaldırılır; **TEK yasak = `1) MUTASYON DIFF` bloğu** (o DÜZELTME-2'nindir). Geri kalan her satır **ölçümle tutarlı olmak ZORUNDADIR**. Yeni kabul kriteri: *"hiçbir KANIT dosyası kendi içinde çelişmez — başlıktaki `Isirmasi ZORUNLU:`, `4) OZET`'in `TOPLAM:` satırı ve `SONUC:` cümlesi AYNI test kümesini anlatır"*. Ek olarak `mutant-14`'ün iki satırı (başlık + SONUC) için **birebir pinli metin** verilir — muğlaklık bırakılmaz. Telafi: rapor (g)'de KANIT'ta değişen **her** satırın gerekçesi. *Reddedilenler:* beyaz listeye üçüncü kalem eklemek ("ölçümle yanlışlanmışsa" builder yargısına açıktı) · yalnız `mutant-14`'ü pinlemek (sınıfı açık bırakırdı — bu ailenin altı turluk kusuru).
  - **K4 — B2 çözümü: (a) ERRATA-A için A'nın kalıbı B'ye KOPYALANIR** — yeni kabul kriteri + **YETKİ PİNİ** (ana spec `GOREV-slice-3a-materyalizasyon.md`'nin satır 366 (a) kalemi ve kriter 4 = satır 416 güncellenir; **BUILDER yapar**, `docs(kanit)` commit'ine dâhil; `PROJE_HAFIZA`/`docs/ADR` yasağı kapsam dışı) + rapor kalemi. **(b) ERRATA-B için dosya DONDURULUR:** `KANIT/slice-3a/cowork-bagimsiz-dogrulama.txt` **Cowork'ün TARİHSEL denetim kaydıdır, builder DOKUNMAZ**; B kriter 6 (*"değişen satırlar yalnız (a)'da"*) **saf kalır**, çelişki buharlaşır; ERRATA-B'nin kaydı DÜZELTME-2 metni + bu hafızadır. *Gerekçe:* builder'a Cowork'ün denetim kaydını düzelttirmek "üreten ≠ denetleyen"i **tersinden** deler. *Reddedilen:* kriter 6'ya istisna açmak.
  - **K5 — KAPSAM: tek turda hepsi.** B1 + B2 + M1-M5 + 5 minör aynı düzeltmede kapanır. *Gerekçe:* bu ailede "majörü sonraya bırakmak" iki kez blokere döndü (`mutant-16`, `mutant-13` — ölçülüp taşınmayan sayılar).
- **Sıradaki ilk iş:** **TEMİZ OTURUMDA** K3/K4/K5'e göre iki spec'i düzelt (A → v7, B → v2; dolu bağlamda spec yazma yasağı yürürlükte) → **AYRI bir temiz oturumda** bir tur daha bağımsız denetim → kilit → Claude Code DÜZELTME-1 build → Cowork temiz oturumda doğrular → DÜZELTME-2 → slice-3b (Flutter), collab-auth.

## ⏭ CHECKPOINT (20 Tem 2026 — oturum 10: DÜZELTME-1 [v5] ÇAPRAZ-TUTARLILIK TARAMASI — **TEMİZ DEĞİL, 5 BLOKER**)
- **Kapsam: yalnız çapraz-tutarlılık taraması, yeniden tasarım YOK.** Ağaç temiz, HEAD `2c7cbcd`, origin/main `56362ed`, 6 commit ileri, push yok. Hiçbir dosya değiştirilmedi (bu hafıza checkpoint'i hariç).
- **HÜKÜM: v5 KİLİTLENEMEZ.** 5 bloker + 1 majör + 6 minör. **Beşinci tur da gerçek bloker buldu; dizi 3→1→2→2→5, sıfıra yakınsamıyor — üstelik SAYI ARTTI.**
- **B1 [BLOKER] §7(3)(g) ↔ §6:328-330 — AYNI DİKİŞİN BEŞİNCİ KIRILMASI, EN ESKİ KALINTI.** §7:406 raporun *"mutant-16'ya **dokunulmadığı**"*nı teyit etmesini istiyor; §6 *"bu dosya §2 gereği **YENİDEN KOŞULUR** … (b) ve (c) bölümleri değiştirilir, diff bloğu taze üretilir"* diyor. `git show` ile kanıtlandı: bu satır **v3'ten (96c2f5b) beri karakteri karakterine değişmedi** — v3'ün BLOKER-1'i §6'da onarıldı, §7'de bırakıldı; v4 ve v5 da göremedi.
- **B2 [BLOKER] §6:330 "6 adımlı" ↔ sıra fiilen 7 adım.** `git diff 5f8a6fa..2c7cbcd` ile kanıtlandı: v5, v4 kusuru #7'yi (ara dosya silme) **7. adım olarak ekledi** ve sayıyı yalnız 4→6 güncelledi. **v4'ün kusurunun birebir tekrarı** (v5 kendi başlığında bu tekrarı tarif ediyor, sonra aynısını yapıyor). §6:366 zaten "(5), (6) ve **(7)**" diyor ⇒ sıranın 7 olduğu belgenin kendi içinde sabit.
- **B3 [BLOKER] Adım 5, adım 1'in PAZARLIKSIZ yasağını çiğniyor.** Adım 1: *"`>` YÖNLENDİRMESİ KULLANMA. PowerShell 5.1'in `>` operatörü UTF-16LE + CRLF yazar; **adım 5 her satırı farklı gösterir, adım 6 'does not apply' verir**"*. Adım 5 (satır 358) tam da o adımın girdisini `… | sed '1d;$d' **>** <kanit>.extracted` ile üretiyor. `--ignore-cr-at-eol` UTF-16LE'yi kurtarmaz. *(Ek risk, DOĞRULANMADI: `sed` PowerShell 5.1 PATH'inde olmayabilir.)*
- **B4 [BLOKER] "BEDAVA KONTROL" garantili YANLIŞ DUR üretir.** §6:331-332 taze `git diff`in mevcut blokla **bayt özdeş** olmasını, ayrışırsa DUR'u emrediyor. Kaynaktan `cat -A` ile ölçtüm: mutant-16 bloğunda **tek-boşluk bağlam satırı = 0**, tam-boş satır = 5; mutant-1'de 0 / 3. `git diff` boş bağlam satırını `" "` yazar ve iki dosya bloğu arasına boş satır **koymaz** ⇒ bayt özdeşlik **fiziksel olarak imkânsız**. (v3/v4'ün "garantili yanlış DUR" sınıfının üçüncü örneği.)
- **B5 [BLOKER] TAŞINMAMIŞ ÖLÇÜM — `mutant-16` İKİ dosyalıdır, §6'nın sırası tekil `<dosya>`.** KANIT:130 *"'+++ b/' = **2 satır**"* ölçülmüş; mutant-16 KANIT'ının `git checkout` satırı (172) **iki** dosya reverte ediyor (`TaskProjection.cs` + `TaskListProjection.cs`, index çiftleri `a8e52a5..75f98a7` **ve** `455ebe5..4ea65b8`). §6 adım 1/4 tekil `<dosya>` yazıyor ve §6:327 hash çiftlerinden **yalnız birini** alıntılıyor ⇒ yama mutasyonun yarısını kaydeder, adım 4'ün revert'i diğer dosyayı mutasyonlu bırakır ⇒ (c) yeşil koşumu kırmızı çıkar. **`mutant-16`/`mutant-13` ile aynı "ölçüldü ama taşınmadı" sınıfının ÜÇÜNCÜ vakası.**
- **M1 [MAJÖR] Kriter 5b ↔ §6:366.** Kriter 5b *"§6'nın **(5)/(6)** doğrulama çıktıları"* istiyor; §6:366 *"**(5), (6) ve (7)**'nin çıktısını her KANIT'a yaz"* diyor. Kriteri geçen teslim §6'yı ihlal eder (tek yönlü tuzak). Ayrıca adım 7 çıktısının yeri §6 içinde de çelişik ("rapora yazılır" ↔ "her KANIT'a yaz").
- **Minörler:** §7:403 commit mesajı 8 KANIT onarımını "mutant-1 diff block" diye tarif ediyor · §2:89 VAR listesi yalnız mutant-1'i sayıyor (12 satır sonra kendi düzeltiyor) · §3:141 "kolonların **yarısı**" ↔ §1:82 "15 kolonun **6**'sı" · satır 18/162 "kriter 4"/"kriter 6(b)" **ana spec'in** kriterleri ama niteleyici düşmüş (satır 365 doğru yapıyor) · §2:113 muhafızı §E4'e atfediyor, kural §3:152-153'te · §1:82 "altısını" ama KANIT:102-104 **yedi** kalem sayıyor (savunulabilir: adım 7 kısmen eksikti — ŞÜPHELİ bıraktım).
- **TEMİZ ÇIKAN — ve bu turun asıl kazancı: MUHAFIZ SAYILARI DOĞRU.** Beklenen kırılan-test sayılarını (`mutant-1`=2 · `14`=2 · `16`=3 · `2,7,9,10,11`=1'er) birleşik senaryo altında **kaynaktan kendim yeniden türettim** (`LiteralOracleD5bTests.cs` assert'leri + `MaterializationRoundTripTests.cs` + `TaskProjection.cs` + 8 mutant KANIT'ının diff hedefi). **Mimari gerekçe doğrulandı:** D5-a her iki tarafta da **aynı** `…Projection.From`'u çağırır ⇒ projeksiyon mutasyonları (7,9,10,11,14,16) simetrik kayar, D5-a onları **göremez**; yalnız `EntityMaterializer.cs` mutasyonları asimetriktir (1 ve 2), 2 ise `owner_id`'yi vurur ve `TaskProjection` **record'unda owner alanı YOK** ⇒ D5-a kördür. `malformed_fields`'ı assert eden **yalnız** Adim1/5/9'dur ⇒ mutant-7 ve 14'ün yayılması tam da spec'in dediği kadardır; Adim9'un `ShouldNotContain` biçimi (ASSERT BİÇİMİ PİNİ) sayıyı 2'de tutuyor. **Üç kova = 12+1+7 = 20; Task tarafı 15 ⇒ SAPMA-1'in "15 kolon"uyla ve `TaskProjection`'ın 16 alanı eksi `EntityId` ile birebir uyuşuyor.** v5 atıfları (§D5:311, §5:370/383/385/400, §D2b:223) ve kaynak satır pinleri (`EntityState.cs:63-65,77-79`, `OrSetField.cs:51-52,89-93`, `LiteralOracleD5bTests.cs:106,186,190-193`, `TestSupport.cs:211,228-231`) tek tek açıldı, **hepsi doğru**.
- **YÖNTEM NOTU (SAPMA-3 kuralı korundu):** blokerlerin 4'ünü bağımsız bir alt-ajan buldu; **hiçbirini beyanla kabul etmedim** — beşini de kaynak dosyadan kendim yeniden ölçtüm (`git show`, `git diff`, `cat -A`, `grep -c '^ $'`). Alt-ajanın 12 iddiasından 11'i doğrulandı, 1'i (bulgu 12) ŞÜPHELİ'ye indirildi.
- **[KARAR — Onur, 20 Tem 2026] GÖREV BÖLÜNDÜ + MEKANİZMA DEĞİŞTİ.** Onur iki şıkkı seçti: (1) **böl + basitleştir**, (2) **bu oturumda yaz** (temiz oturum önerimi bilinçli olarak reddetti). Sonuç: **DÜZELTME-1 [v6]** = senaryo birleştirme (§E1-E4, beş turdur temiz) + 8 mutantın yeniden ölçülüp KANIT **(b)/(c)/ÖZET** güncellemesi. **DÜZELTME-2 [v1] YENİ DOSYA** = `GOREV-slice-3a-DUZELTME-2-KANIT-diff-butunlugu.md`, diff bütünlüğü; diff artık **metne yapıştırılmaz**, `git diff --output=` ile `KANIT/slice-3a/patch/*.patch` **dosyası** olarak bırakılır ⇒ doğrulama tek satır `git apply --check`. v5'in 7 adımlı ritüeli (sınır işaretleri, `sed` çıkarma, bayt-özdeşlik, `<temp>.patch`) **tamamen KALDIRILDI** ⇒ B2/B3/B4/M1 buharlaştı, B5 çözüldü, B1 §7(g)'de düzeltildi.
- **[COWORK HATASI — KAYDA GEÇTİ] Dolu bağlamda yazdım ve TAM OLARAK teşhis ettiğim kusuru yeniden ürettim.** Yazdığım iki spec'i bağımsız denetçiye verdim; **2 yeni BLOKER + 7 majör** çıktı, hepsini kaynaktan kendim teyit ettim: (a) **DÜZELTME-2 adım 3'te `dotnet test --no-build` vardı ama `dotnet build` YOKTU** ⇒ mutasyon derlenmez, hedef testler yeşil geçer, kendi kırmızı çizgim 16/16'da tetiklenirdi (`verify.ps1:50,53` doğru sırayı gösteriyor); (b) **DÜZELTME-1 kriter 5c** *"değişen satırlar yalnız (b)/(c)/ÖZET'te"* diyordu ama aynı §6'nın SAPMA-4'ü **git'te izlenen** iki dosyayı sildiriyor (`git ls-files` ile doğrulandı) ⇒ **aynı §5↔§6 dikişinin ALTINCI kırılması, bu kez benim elimden.** Majörler: `mutant-6`'nın bloğunda `index` satırı **hiç yok** (üçüncü kusur sınıfı; kriter `index` üzerinden ölçülemez, `diff --git`e çevrildi) · `mutant-8` **ortam-bağımlı** ("UTC CI'da ISIRMAZDI"), koşulsuz kapıya çevrilmişti · `VAR=deger komut` **POSIX**'tir, PowerShell'de çalışmaz · KANIT v2.1 (a)'yı fiilen değiştiriyordum, ERRATA ilan etmemiştim · §6'nın SAPMA-4 + ERRATA-1/2/3'ünün ne kriteri ne rapor kalemi vardı, üstelik **"errata edilir" kimin işi belirsizdi** · `mutant-10`/`11`'in yanlışlanan gerekçesi için "yeni ham çıktıyla değiştir" **uygulanamaz** (ham çıktı gerekçe içermez). **HEPSİ DÜZELTİLDİ** (yetki pini + kriter 10 + ERRATA-A/B + `$env:` biçimi + `mutant-8` muafiyeti + doğru gerekçe metni pinlendi).
- **DERS [öncekine ek]:** bağımsız denetim kapısı **çalışıyor** — beş turdur her turda gerçek bloker buluyor ve bu turda **üretenin Cowork'ün kendisi olması fark etmedi**. Ama "dolu bağlamda yazma" dersi de **ampirik olarak doğrulandı**: aynı oturumda hem denetleyip hem yazmak yeni bloker doğurdu.
- **DURUM: İKİ SPEC DE KİLİDE HAZIR DEĞİL.** Düzeltmeler uygulandı ve sayı/dikiş taraması geçti (A: 10 kriter · §6 = 4 madde ↔ "4 ADIM" ✓ · B: 6 madde ↔ "6 ADIM" ✓ · 8 kriter · POSIX öneki 0 · `dotnet build` 3 yerde), **ama düzeltilmiş hâlleri HENÜZ BAĞIMSIZ BİR TUR GÖRMEDİ.**
- **Sıradaki ilk iş:** **TEMİZ OTURUMDA** her iki spec'e **bir tur bağımsız denetim** (yeniden tasarım yok, yalnız çapraz-tutarlılık + "bölünmenin açtığı delik" ekseni). Temizse Onur kilitler → Claude Code **DÜZELTME-1**'i build eder → Cowork temiz oturumda doğrular → sonra **DÜZELTME-2** → sonra slice-3b (Flutter), collab-auth.

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 9: slice-3a BAĞIMSIZ DOĞRULANDI — **DÜZELT (dar kapsam: yalnız D5-a)**)
- **HÜKÜM: üretim kodu ve 15 kapı UYGUN; D5-a yeniden inşa edilmeli.** Temiz oturum, spec'i yazan oturum değil. KANIT: `KANIT/slice-3a/cowork-bagimsiz-dogrulama.txt`. Doğrulanan ağaç 56362ed; `git clean -xdf` sonrası ScratchProbeTests.cs gitti. **Hiçbir şey push edilmedi, ağaç temiz.**
- **verify.ps1 DEĞİŞMEDEN kendi koşumumda geçti:** diff BOŞ · build **0 uyarı / 0 hata** · **110/110** (arch 5 + Api 12 + SyncCore 40 + Persistence 53; taban 85 → +25) · CVE **0** · ayrı koşumla sayısal **exit 0**. Kriter 1/2/7/8 sağlandı.
- **16 MUTANTIN 16'SI KENDİ KOŞUMUMDA ISIRDI**, her biri spec'in adreslediği kapıdan; her mutant sonrası `git checkout -- src tests` + `git status` ile ağaç temizliği doğrulandı (hepsi TEMİZ), kapanışta tam suite **110/110 yeşil, git status boş**.
  - **AYIRT EDİCİLİK KANITI:** mutant-3/4/5/15 aynı test metodunu (`D0b_...`) kırıyor ⇒ metot adı YETMEZ. Assert mesajları ham çıktıdan okundu: dördü **dört farklı assert**'i düşürüyor (satır 94 enjeksiyon / 77 B-görmemeli / 86 by-id 404 / 103 TaskList ayağı). D0-b kör tek-assert değil.
  - **BULGU-B AMPİRİK OLARAK KAPANDI:** mutant-6 yalnız D3b'nin **şema beyanı** testini kırdı, davranışsal test kırılmadı ⇒ musl'da `COLLATE "C"` savunma-derinliğidir, davranışsal kapı değildir. Builder'ın dürüstlük beyanı doğru çıktı.
  - **mutant-16** birincil assert'lerin İKİSİNİ birden (adım 7 + adım 8, ayrı test metotları) düşürdü; D0-c yan hasarı EK'tir, yerine geçen değil ⇒ **Cowork okuma bulgusu 3 KAPANDI**. **mutant-1** spec'in istediği çifti düşürdü (D5-b Adım2 + D5-a round-trip, ikisi de `completed_at`) ⇒ **bulgu 2 KAPANDI**.
  - **mutant-13** yalnız **canlılık assert'ini** kırdı (spec üç-kova assert'inin de kırılmasını öngörüyordu — öngörü isabetsiz, yön daha sıkı). v5'in "muafiyet kaldırıldı, canlılık assert'i öldürür" iddiası DOĞRULANDI.
- **[SAPMA-1 — MAJÖR, RET] D5'in PAZARLIKSIZ "ORTAK SENARYO — DOKUZ adım" pini İNŞA EDİLMEDİ.** İnşa edilen: iki ayrı dosya, **11 bağımsız test**, her biri kendi kurulumuyla. **D5-a'nın Task senaryosu dokuz adımdan ALTISINI içermiyor** (meşru null, tag remove, isDeleted+çakışma, ayrıştırılamayan tarih, ikinci kimlikli actor, boardPos, "True") ⇒ karşılaştırdığı **15 kolonun 6'sı varsayılan değerinde** (false/`{}`/NULL) karşılaştırılıyor; NULL'ı NULL'a eşitlemek kalıcılık zinciri hakkında hiçbir şey kanıtlamaz. TaskList ayağı `name`'i hiç değiştirmiyor ⇒ bayat-`name` hatası D5-a'ya **görünmez**. **Kök neden bu:** mutant-1 ısırmayınca sorun "testin zayıflığı" değil **spec-uyum sapmasıydı**; builder §5'in "DUR ve bildir, istisna YOKTUR" kuralı yerine eksik adımlardan **yalnız birini** ekleyip devam etti (6c2774c). Yön doğru ve dürüstçe raporlandı, ama D5-a'nın varlık sebebi **enumere edilmemiş** mutasyonları yakalamaktır; bilinen tek mutant ısırana dek elle ayarlanmış senaryonun bu yeteneği hakkında **hiçbir kanıt yok**. (2b1 BULGU-2 kör koruma + v1'in D5 totolojisi = aynı aile.) **GEREKEN:** D5-a dokuz adımlı ORTAK senaryo üzerine (D5-b ile paylaşılan tek kurulum) yeniden kurulsun, sonra **mutant-1 yeniden ölçülsün**. Diğer 15 mutant etkilenmez.
- **[KRİTER 9 — Cowork'ün spec kusuru KAPANDI] Backfill GERÇEK kalıcı volume'de ölçüldü** (`momentum_momentum-pgdata`): `sync_scalar_meta=0`, `sync_orset_tags=0`. Ayrıca kalıcı DB **hâlâ slice-3a öncesi 7 tablolu şemada** (tasks/task_lists/task_tags YOK) ⇒ yeni migration oraya hiç uygulanmamış; backfill'i gerektirecek ne veri ne şema var. Builder'ın SONUCU doğruydu, **YÖNTEMİ geçersizdi** (efemeral Testcontainers DB'sinde ölçmüştü).
- **[SAPMA-2 — KABUL] `ProjectionFields.cs`** kriter 6(b)'de yok ama **5(a)'nın `Sync/Projection/*` jokeri gerçekten kapsıyor**; saf/IO-suz/InvariantCulture doğrulandı, tek-implementasyon kuralını bozmuyor, **güçlendiriyor** (kuralları iki projeksiyona kopyalamak kırmızı çizgi ihlali olurdu). Kusur kriter 6(b)'nin eksik sayımında.
- **[SAPMA-3 — RET, düzeltilsin] `mutant-1` ve `mutant-16`'nın KANIT diff'leri GEÇERLİ DİFF DEĞİL** — eklenen satırlarda `+` öneki düşmüş (`+++ b/` → `++ b/`); diğer 14'ü temiz. Uygulanamayan diff izlenebilirliği kırar (bu iki mutantı elle yeniden kurmak zorunda kaldım). **2b2 BULGU-4'ün aynı ailesi.**
- **[SAPMA-4 — KABUL] `verify-run-raw.txt` + `verify-run-full.txt`** gereksiz artık; kanonik `verify-run.txt` tam ve `EXIT_CODE=0` (doğrulandı).
- **[SAPMA-3 — COWORK HATASI, KAYDA GEÇTİ] İlk teşhisim YANLIŞTI.** "mutant-1 ve mutant-16'nın diff'lerinde `+` öneki düşmüş" dedim; bağımsız denetim kırdı, **kaynak dosyadan kendim yeniden ölçtüm**: her iki dosyada da `^++ ` = **0 satır**, `+++ b/` yerinde. Gördüğüm bozukluk **kendi yama-çıkarma/görüntüleme hattımın artefaktıydı**; kaynağı doğrulamadan KANIT'a kusur diye yazdım ve düzeltme spec'inde **pazarlıksız emre** çevirdim. **`mutant-16`'nın KANIT diff'i TEMİZDİR, dokunulmayacak.** Gerçek kusur yalnız `mutant-1`'de ve başka: `index xxxxxxx..yyyyyyy` **yer tutucu** hash'ler (blok elle yazılmış) + hunk sayaçları gövdeyle uyuşmuyor ⇒ parse edilemiyor. **KURAL TEYİDİ: bir kusur iddiası KANIT'a girmeden önce KAYNAK DOSYADAN, kendi araç zincirimin dışında bir yolla teyit edilir.** (2b2 BULGU-4'te builder'ı eleştirdiğim kusurun aynısı; oturum 7'deki "alt-ajan beyanını doğrulamadan aktarma" hatasının tekrarı.)
- **DÜZELTME SPEC'İ YAZILDI: `GOREV_CLAUDE_CODE/GOREV-slice-3a-DUZELTME-1-D5-ortak-senaryo.md` [v4] — KİLİT ONUR'DA.** **ÜÇ BAĞIMSIZ DENETİM TURU GEÇTİ, üçü de gerçek bloker buldu** (**v1: 3 bloker + 6 majör · v2 delta: 1 bloker + 4 majör · v3 delta: 2 bloker + 3 majör**). Üreten ≠ denetleyen korundu: spec'i Cowork yazdı, üç turu da bağımsız alt-ajanlar koştu, **her bloker Cowork tarafından kaynaktan yeniden ölçüldü**.
  - **v3'ün blokerleri — İKİSİ DE v3'ün KENDİ DÜZELTMESİNİN YAN ETKİSİYDİ (ders):** (1) §2'ye yeniden-koşum kuralı eklenirken §6 güncellenmedi ⇒ `mutant-16` için "yeniden koşulur" ile "DOKUNMA" **çelişti** (v2'de kırılan §2↔§3 çelişkisinin §6'ya kaymış hâli); (2) **`mutant-16` bugün ZATEN 3 test kırıyor** (adım 7 + adım 8 + **D0-c yan hasarı**) ama v5 §5:385 beklenen olarak **2** yazıyor ⇒ "fazla kırılırsa DUR" muhafızı **garantili yanlış DUR** üretirdi. Cowork bu sayıyı bu oturumda kendi koşumuyla ölçmüştü ama spec'e taşımayı atlamıştı.
  - **DÖRDÜNCÜ TUR — hedefli soru işe yaradı.** Tur, "bu oturumda ölçülüp spec'e taşınmamış BAŞKA sayı var mı?" sorusuna odaklandı ve **cevabı EVET çıktı: `mutant-13`.** Cowork ölçmüştü (yalnız canlılık assert'i kırıldı, SyncCore 39/40 = **1 test**) ama v5 §5:400 hâlâ "üç-kova assert'i de kırılır" diyor — üstelik v5 kendi §D2b:223'üyle de çelişiyor. **`mutant-16`'nın AYNASI:** orada spec **eksik** sayıyordu (yanlış "FAZLA ⇒ DUR"), burada **fazla** söylüyor (yanlış "AZ ⇒ DUR"). ERRATA-2 ile kapatıldı (`mutant-13` yeniden koşulmaz, errata metinseldir).
  - **v4'ün blokerleri:** (1) §6'nın mutant-16 maddesi "4 adımlı" diyordu ama sıra 6 adımdı — **aynı §2↔§6 dikişinin ÜÇÜNCÜ kırılması**; (2) "KANIT bloğunu geri çıkar" adımı **deterministik değildi** (sınır işareti yok) ⇒ builder kendi ad-hoc çıkarıcısını yazacaktı — **SAPMA-3'ü doğuran araç-hattı sınıfının ta kendisi, üstelik "kanıt adımı" diye emredilmiş hâli**. `<<<BEGIN RAW DIFF` sınırları pinlendi. Majörler: `git diff … > dosya` PowerShell 5.1'de **UTF-16LE+CRLF** yazar (⇒ `--output=` pinlendi) · `--whitespace` `git apply`'ın seçeneğidir, `git diff`'in değil · `mutant-10`/`mutant-11` KANIT'larının **gerekçeleri** birleşik senaryoda yanlışlanıyor (sonuç 1 test kalıyor, gerekçe çöküyor) · ara dosyalar silinmiyordu (16 izlenmeyen dosya, kriter 4 düşerdi).
  - **DERS [yazıya geçirildi, iki maddelik]:** (a) bu spec **sıkı bağlı bir kısıt sistemidir**; her düzeltme turu yeni kusur doğurur (4 tur: **3 → 1 → 2 → 2** bloker, sıfıra yakınsamadı) — bir maddeyi değiştirirken ona atıf yapan HER bölüm birlikte güncellenmeli (§2↔§6 dikişi üç kez kırıldı). (b) **Bu oturumda ölçülen HER sayı spec'e taşınmalı**; ölçüp taşımamak ölçmemekle aynı sonucu verdi ve iki ayrı bloker doğurdu (`mutant-16`, `mutant-13`). **Bir sonraki doğrulamada ilk iş: KANIT'taki her ölçümü spec'e karşı çapraz kontrol et.**
  - **Onur'un kilitlediği iki karar:** (K1) **paylaşılan kurucu + `stopAfterStep`** — senaryo tek yerde, D5-b'nin 9 testi N'e kadar koşar, D5-a N=10 koşar; *reddedilen:* tek metot+checkpoint (mutant-16'nın adım 7/8 ayrımı kaybolurdu). (K2) **senaryo 0..10'a genişletildi** (v5 §D5'e errata): **adım 0** (en düşük damga: `priority`/`projectId`/`remindAt`/`recurrenceRule`) + **adım 10** (en yüksek damga: geçerli `dueAt` + bozuk `remindAt`).
  - **v1'in blokerleri:** (1) `is_deleted`/`has_delete_edit_conflict`'in "varsayılan değil" olacağı iddiası **fiziksel olarak üretilemezdi** — `EntityState.cs:63-65,77` Ordinal + kısa devre gereği adım 9'un `"True"`'su sonrası ikisi de zorunlu `false`; (2) v1 D5-a'yı **bugünkünden ZAYIF** yapıyordu (dokuz adım `priority`/`projectId`/`remindAt`/`recurrenceRule` hiç yazmaz, `dueAt` yalnız bozuk ⇒ **üç kolonda regresyon**, `ReadTimestampAsync` ölü koda dönerdi); (3) §6 var olmayan bir kusuru emrediyordu (yukarıdaki Cowork hatası).
  - **v2'nin blokeri (delta):** **`mutant-14`'ün yayılma alanı GENİŞLİYOR.** Mutasyonu `notes` durum-2'deyken **her projeksiyonda** malformed ekler; birleşik senaryoda `notes` adım 1b'de null'lanıp bir daha yazılmaz ⇒ `Adim5`'in `ShouldBe(["dueAt"])` **tam dizi eşitliği** de kırılır ⇒ **mutant-14 artık 2 test kırar**. v2 "diğer 15 mutant yeniden koşulmaz" diyerek bunu **tespit edilemez** kılıyordu ⇒ repoda **fiilen yanlış KANIT** kalırdı ("YAN ISIRMA: YOK"). Cowork mutasyon diff'ini ve assert'i okuyarak doğruladı. **v3'ün düzeltmesi: D5'te kapısı olan 8 mutant (1,2,7,9,10,11,14,16) YENİDEN KOŞULUR**; diğer 8'i (3,4,5,6,8,12,13,15) D5'ten etkilenmez.
  - **v3'ün diğer pinleri:** adım 10 `completion` grubuna **DOKUNMAZ** (yoksa mutant-1'in tek ısırma yüzeyi `completed_at` ölür) · adım 4 **YENİ eleman** ekler (tombstone **TAG düzeyindedir**, eleman düzeyinde değil) · adım 3 remove'u `Observed=[Tag3]` taşır · `notes` kova (ii)'den (iii)'e taşındı (mutant-1 onu yazar, bayat değil) · mutant-1 **TAM 2 test** düşürür, TaskList round-trip **YEŞİL kalır** · `git apply --check` **revert sonrası** koşulur.
- **Sıradaki ilk iş:** Onur **DÜZELTME-1 [v3]**'ü kilitler → Claude Code build eder → **Cowork temiz oturumda bağımsız doğrular** (özellikle: mutant-1'in tam 2 testi düşürdüğü, mutant-14'ün 2 testi düşürdüğü, E3'ün üç kovalı kolon ölçümü, `git diff -- src` boşluğu). Sonra slice-3b (Flutter), sonra collab-auth.

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 8 kapanış: slice-3a BUILD GELDİ, DOĞRULAMA TEMİZ OTURUMA DEVREDİLDİ)
- **Claude Code slice-3a'yı build etti.** Commit zinciri: `43d7d26` (kod) → `6c2774c` (D5-a güçlendirme) → `c0388cb` + `f0c7167` (KANIT). Push YAPILMADI. Ağaçta yalnız 0-baytlık untracked `tests/Momentum.Persistence.Tests/ScratchProbeTests.cs` (builder'ın silme izni yoktu; `git clean -xdf` halleder). KANIT: 21 dosya (16 mutant + D0 kırmızı/yeşil + 3 verify).
- **Builder beyanı:** 110/110 test (85 + 25 yeni), verify exit 0, CVE 0, 16/16 mutant ısırdı, EF entity 10 (ölçüldü), `.FFFFFFF` kesirsiz girdiyi tek başına eşliyor (ölçüldü), `TimeZoneInfo.Local.Id = "Turkey Standard Time"`, musl collation ayrışmıyor (mutant-6 yalnız şema beyanını ısırıyor — BULGU-B'nin öngördüğü sonuç).
- **DOĞRULAMA BU OTURUMDA YAPILMADI — DOKTRİN GEREĞİ.** v5 spec'ini bu oturum yazdı; aynı oturumun o spec'e karşı build denetlemesi "ne yazdığımı biliyorum" yanlılığı üretir (oturum 6'da kilitlenen kural). Doğrulama **temiz oturuma** devredildi.
- **COWORK'ÜN OKUMA BULGULARI (doğrulayıcı bunlara özellikle baksın):**
  1. **[EN ÖNEMLİ] Builder mutant-1 ısırmayınca DURMADI, testi güçlendirip devam etti.** Spec §5: *"Bir mutant ısırmıyorsa testi ZAYIFLATMA — DUR ve bildir. İstisna YOKTUR."* Yön doğru (güçlendirme, zayıflatma değil; 2b2/D7 emsali var) ama **asıl soru şu:** D5-a neden kör kaldı? Spec **ORTAK SENARYO** diyor (D5-a ve D5-b **aynı** 9 adımlı senaryoyu okur) ve adım 2 zaten grup-küçültmedir. Builder "orijinal tasarımda olmayan bir grup-küçültme adımı ekledim" diyorsa **D5-a ile D5-b ayrı senaryolar üzerine kurulmuş** olabilir — bu bir spec-uyum sapmasıdır ve güçlendirme semptomu yamamış olur. **Doğrulayıcı: D5-a ve D5-b gerçekten ORTAK senaryoyu mu paylaşıyor?**
  2. **mutant-1 için tablo ↔ anlatı çelişkisi.** Spec "D5-a FAIL + D5-b adım 2 FAIL" istiyor; raporun anlatısı ikisini de diyor ama özet tablosu yalnız "D5-b Adım2" yazıyor. Güçlendirmeden sonra D5-a gerçekten düştü mü, KANIT'tan doğrula.
  3. **mutant-16'nın yan hasarı (D0-c) spec'te öngörülmemişti.** Açıklanabilir (list_pos hiç doğru yazılmıyor → sıralama testi de düşer) ama izolasyon iddiasını zayıflatır. **Birincil** assert'lerin (adım 7 ve 8) düştüğü ayrıca görülmeli.
  4. **`Domain/Sync/Projection/ProjectionFields.cs`** kriter 6(b)'nin literal listesinde yok; builder 5(a)'nın joker izniyle adjudike etti. Gerekçe makul (ayrıştırma kuralı iki projeksiyonda kopyalanmasın) ama **saf/IO-suz olduğu ve tek-implementasyon kuralını bozmadığı doğrulanmalı**.
  5. **[SPEC KUSURU — BENİM] Kriter 9'un backfill ölçümü YANLIŞ VERİTABANINDA yapıldı.** Builder efemeral Testcontainers DB'sinde ölçmüş (doğal olarak 0/0, hiçbir şey kanıtlamaz). Kastedilen, slice-2c'nin kriter 8'inde ölçülen **kalıcı compose volume'üydü** (`momentum_momentum-pgdata`). Spec hangi DB olduğunu yazmamış — **muğlaklık benim kusurum**. Doğrulayıcı gerçek volume'de ölçmeli.
  6. Küçük: `verify-run-raw.txt` + `verify-run-full.txt` gereksiz artık dosyalar (ilk yakalama Write-Host akışını kaçırmış); kanonik olan `verify-run.txt` tam ve exit 0 mı, teyit et. `SHOW lc_collate` PG17'de hata verdi, builder `pg_database.datcollate`'e döndü (dürüst pivot). Yeni uçlar için Api.Tests seviyesinde ayrı test yok (adlandırılmış sapma).
- **[COWORK HATASI — kayda geçti] Build commit'leri DOĞRULANMADAN push edildi.** Cowork hafıza commit'ini push ederken builder'ın dört commit'i de beraberinde gitti (`a4293e7..7ff3fd1`). Önceki dilimlerde **push = "doğrulandı" sinyaliydi**; bu dilimde değildir. Zarar yok (private repo, deploy yok, geçmiş bozulmadı) ama kalıp kırıldı. **Kural teyidi:** bundan sonra doğrulama bitmeden push edilecekse ya ayrı dal kullanılır ya da push bilinçli olarak ertelenir.
- **Sıradaki ilk iş:** TEMİZ OTURUM → slice-3a'yı bağımsız doğrula (aşağıdaki devir bloğu).

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 8: slice-3a SPEC [v5] HAZIR, BEŞ DENETİM TURU GEÇTİ — KİLİT ONUR'DA)
- **BEŞİNCİ TUR (delta denetimi) 2 bloker daha buldu ve ikisi de Cowork'ün kendi kusuruydu:** (1) **`mutant-16` `KeyNotFoundException` fırlatırdı** — `state.Fields["pos"]` indeksleyicisi eksik anahtarda fırlatır; `_fields`'e yalnız `ApplyField`/`LoadField` yazar ve Fractional alanlar oraya **hiç girmez** ⇒ istisna `ProcessOpAsync`'e sızar (try/catch yok), txn geri alınır, `/v1/sync` 500 döner, satır hiç yazılmaz ⇒ kapı "sessiz NULL" yüzünden değil **istisna** yüzünden kırılırdı (iddia edilen ısırma yüzeyi DEĞİL, üstelik cascade). Diff **savunmacı okuma** (`TryGetValue`) olarak pinlendi. (2) **"YAPISAL SINIR — ADLANDIRILMIŞ MUAFİYET" İDDİASI YANLIŞTI** — v2/v3/v4 `mutant-13(b)`'yi "öldürülemez" ilan edip **"kör kapı yok" doktrinine muafiyet açmıştı**; denetim `_entities` ↔ `DescribeFieldKeys` **canlılık assert'inin** (reflection) onu öldürdüğünü gösterdi. Teşhis de yanlıştı: (b) "D2b tek yönlü olduğu için" değil, **bugünkü registry üzerinde davranışsal olarak EŞDEĞER bir mutant** olduğu için hayatta kalıyordu — ve eşdeğer mutant **muafiyetle değil, kapsam dışı bırakılarak** ele alınır. **Muafiyet kaldırıldı; doktrinde delik yok.** Ayrıca adım 7/8 asimetrisi keyfiydi (`listPos`/`boardPos` ile `pos` birebir aynı fizik) ⇒ `mutant-16` ikisini birden kapsar, sayı değişmedi.
- **DERS (yazıya geçirildi):** kendi açtığın muafiyeti "yapısal sınır" diye gerekçelendirmek, doktrin deliğinin en tehlikeli biçimidir — iddiayı kırmayı bir başkasına denet.
- **Yapılan:** `GOREV_CLAUDE_CODE/GOREV-slice-3a-materyalizasyon.md` **[v5]** yazıldı, **Onur kilitledi**. Commit **ffdc926** (spec + hafıza, 486 ekleme), Desktop Commander ile `origin/main`'e push edildi (`fdb98f2..ffdc926`); ağaç temiz. Kod/test **HENÜZ YAZILMADI** — build Claude Code'da.
- **ONUR'UN KİLİTLEDİĞİ 7 FORK (F1–F7):** materyalizasyon **aynı op-txn'ında** (asenkron projektör REDDEDİLDİ = E-1 hata sınıfı) · `owner_id` satırda, **ilk yazan sahiplenir** (geçici politika) · **tipli kolon + hoşgörülü ayrıştırma** (malformed işareti; meta otorite kalır) · OR-Set kapsamı **yalnız `tags`** · **`owner_id` kimliği doğrulanmış actor'dan** (F5, aşağıdaki bulgunun düzeltmesi) · **Task↔TaskList bağı İCAT EDİLMEZ**, 3b proje bazında gruplar (F6) · **dilim bölünmez** (F7).
- **DENETİM ZİNCİRİ:** v1 → (red-team **5 bloker + 11 majör + 7 minör** · teknik doğruluk **4 bloker + 6 majör + 9 minör**) → v2 → (**hedefli yeniden-doğrulama: 2 kalan bloker + 7 majör + 11 minör**) → v3 → (**taze kapanış denetimi: 1 kalan bloker + 4 majör + 2 minör**) → v4 → (**delta denetimi: 2 kalan bloker + 5 çelişki**) → **v5**. **Toplam 14 bloker.** Beş turun beşi de gerçek bloker buldu. Üreten ≠ denetleyen korundu: spec'i Cowork yazdı, dört denetim turunun hepsi bağımsız alt-ajanlarca koşuldu.
- **DENETİMİN YAKALADIĞI EN ÖNEMLİ ŞEYLER (hepsi bu projenin kendi derslerinin tekrarıydı):**
  1. **v1'in "taç mücevher" kapısı TOTOLOJİYDİ** — satırı `TaskProjection.From(state)` yazıyor, kapı `From(hydrate(...))` ile karşılaştırıyordu; `From` içindeki her mutasyon **iki tarafı birden** bozduğu için 3 mutant (gevşek ayrıştırma, sabit-false bayrak, iptal-edilmiş tag) **hiç ısırmıyordu**. **slice-2b1 BULGU-2'nin (kör koruma) birebir tekrarı.** Düzeltme: kapı ikiye ayrıldı — **D5-a** (round-trip, `From`↔satır) + **D5-b LİTERAL ORACLE** (elle yazılmış beklentilere karşı ham SQL; `From` çağrılmaz).
  2. **`isDeleted` için İKİNCİ İMPLEMENTASYON yazdırılıyordu** — `EntityState.cs:11,72` Ordinal `"true"` kullanıyor, v1 `bool.TryParse` pinlemişti. `isDeleted="True"` ⇒ satır silinmiş görünür **ama çakışma bayrağı hiç yanmaz** (K2-C4 ihlali). **ERRATA E-1'in hata sınıfı.** Düzeltme: **`EntityState.IsDeleted`** (additive, aynı sabitler) + `HasDeleteEditConflict` onu çağırır; `deleteKey` erişimi birebir pinlendi.
  3. **`owner_id` İSTEMCİ KONTROLÜNDEYDİ** — `WireOp.ActorId` tel alanıdır; kimliği doğrulanmış `command.ActorId` push yoluna **hiç girmiyor** (`SyncCommandHandler.cs:109`). Kimliği doğrulanmış Mallory `actorId=<kurban>` göndererek **kurbanın listesine satır enjekte edebilirdi**. Bugün sömürülemez (auth yok ⇒ 401) ama okuma API'si bunun üzerine kuruluyordu. F5 ile kapatıldı (**outbox/SyncPuller'a DOKUNULMADI**; sapma D7 çıpasıyla belgeleniyor).
  4. **`mutant-13`'ün YÖNÜ TERSTİ** (v2 ve v3'ün ilk hâli) — "sabit liste döndür" mutasyonu bugünkü doğru listeyle özdeş olduğu için kapıyı **aynen yeşil** bırakıyordu ⇒ **hiç ham kırmızı üretilemezdi**, KANIT kuralı (b) sağlanamazdı. Düzeltme: **iki parçalı** tanım (a: `_entities`'e geçici alan ⇒ FAIL · a+b: sabit liste ⇒ PASS) + **yapısal sınırın adlandırılması**: (b) parçası D2b'nin tek-yönlülüğü gereği **öldürülemez, hayatta kalan mutanttır** ve §5'in "ısırmayan mutantta DUR" kuralına **adlandırılmış muafiyettir**.
  5. **`mutant-1`'in kapı atfı FİZİKSEL OLARAK YANLIŞTI** (v2) — op `listPos`'a dokunuyorsa delta yazımı da onu yazar ⇒ o assert geçer. Adım 7 mutant kapısı değil, kanal-karışıklığı regresyon önlemi diye yeniden adlandırıldı.
  6. **`task_lists` uçtan uca KAPISIZDI** (v2/v3) — şemada vardı ama hiçbir teslimat bir satır yazıp okumuyordu; `pos → Orders` kanal hatası korumasızdı. `mutant-16` + D5-b adım 8 + D0-b adım 5 ile kapatıldı.
  7. Diğer blokerler: port `op` almadığı için `mutant-1` **inşa edilemiyordu** · `SchemaTests.cs:20` `ShouldBe(7)` kabul kriteriyle **çelişiyordu** · endpoint'e ham SQL **arch Rule 2'yi kırıyordu** (okuma portu zinciri eklendi) · **`DateTimeStyles.RoundtripKind` `DateTimeOffset`'te ETKİSİZDİR** ve offset'siz girdi **makinenin yerel saat dilimini** alıyordu (`AssumeUniversal|AdjustToUniversal` pinlendi).
- **SPEC'İN ŞEKLİ:** 9 teslimat (D0 kırmızı-önce · D1 şema · D2 saf projeksiyon + D2a/D2b/D2c/D2d · D3 materyalizasyon yazıcısı + D3b collation · D4 okuma portu+API · D5-a/D5-b · D6/D7/D8 çıpalar) · **16 mutant** · 12 kabul kriteri · 4 açık bulgu.
- **DÜRÜSTLÜK BEYANLARI (mutantsız olduğu AÇIKÇA yazılanlar):** D2c (kültür — tr-TR'de ısırmaz, gerekçesi doğrulandı) · D2d (`mutant-8` **yalnız yerel TZ ≠ UTC** makinede ısırır) · D3b (`mutant-6` muhtemelen **yalnız şema beyanını** ısırır; musl `strcoll`≈`strcmp`) · D5-b adım 7 · D6/D7/D8 çıpaları · D5-a'nın "projeksiyon fonksiyonunu ÖLÇMEZ" beyanı · `mutant-13`(b)'nin hayatta-kalma muafiyeti.
- **AÇIK BULGULAR:** **A** — registry'de `Task→TaskList` bağı YOK (`taskListId` yok); Onur kilitledi: **icat edilmez**, 3b proje bazında gruplar; bağ istenirse ADR errata. **B** — `mutant-6`'nın davranışsal ısırma yüzeyi **ölçülmedi** (musl). **C** — `outbox_messages.owner_id` doğrulanmamıştır, bu dilim düzeltmez (auth dilimi birleştirmeli). **D** — `task_lists` bu dilimde **bağlantısız** bir tablodur (A'ya bağlı).
- **Sıradaki ilk iş:** Onur v4'ü **kilitler** → Claude Code'a verilir → build → **Cowork bağımsız doğrular** (D0'ın üç kırmızı-önce çıktısı, D5-b'nin 9 literal assert'i, **16 mutantı KENDİ koşarak**; özellikle `mutant-13`'ün iki koşumu, `mutant-9`'un iki uçtan birden ısırması, `mutant-16`'nın adım 8'i kırması). Sonra slice-3b (Flutter), sonra collab-auth.
- **Uyarı:** spec **kilitli değildir**; v4'ten sonra hiçbir tam denetim turu koşulmadı (yalnız 6 hedefli düzeltme + mekanik tutarlılık taraması: 16 mutant boşluksuz, 9 adım ↔ 9 assert, sayılar hizalı).

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 7 devamı: slice-2c BAĞIMSIZ DOĞRULANDI — **SÜRÜM UYGUN (koşulsuz)**)
- **BULGU-3 KAPANDI.** Claude Code slice-2c'yi build etti → **9a5fc7b** (kod+test) + **971e17d** (KANIT). **Cowork bağımsız doğruladı:** `git clean -xdf` sonrası verify.ps1 değişmeden **exit 0** · build **0/0** · **85/85** kendi koşumla (arch 5 + Api 12 DB'siz + SyncCore 33 + Persistence 35; 75 → 85, +10 yeni) · CVE 0 · **Domain diff YALNIZ `OrSetField.cs`** · ADR/HAFIZA dokunulmamış. KANIT: `KANIT/slice-2c/cowork-bagimsiz-dogrulama.txt`.
- **7 MUTANTIN 7'Sİ KENDİ KOŞUMDA ISIRDI** (14:50→14:56, tam çözüm testi + `--blame-hang-timeout 120s` + `DOTNET_CLI_UI_LANGUAGE=en`, her biri sonrası ağaç temizliği doğrulanarak). **İki kritik iddia ampirik olarak kanıtlandı:** (a) **mutant-5 D0'ı YEŞİL bıraktı, D2b'yi kırdı** → "MaxStamp'ten add'leri çıkar" biçimindeki semantik gerilemeyi (düzeltmenin option-1c'ye çürümesi) yakalayan TEK kapı D2b'dir; (b) **mutant-7 (yarım-max, yalnız `WallMs`) D0/D0b/D2/D3/D4'ün HEPSİNİ yeşil bıraktı, yalnız D2d(P8) kırdı** → spec'in ÜÇÜNCÜ denetim turunun kazandırdığı şey tam olarak budur; v2'de D2d'nin üreteci pinli değildi ve mutant-7 yoktu, bu kusur sınıfı sessizce geçerdi.
- **Düzeltme (ADR E-1/E-1b) üç noktada birden uygulanmış ve doğrulanmış:** `ApplyAdd` + `LoadTag` ortak `MergeStamp` (TAM `Hlc.CompareTo`, yarım-max değil; `LoadTag` `ApplyAdd`'i çağırmıyor → mutant-3 ayırt edici kaldı, yalnız D2'yi kırdı) · `SyncStore.UpsertTagAsync` **GREATEST** (mutant-4 yalnız D3'ü kırdı → E-1b kör koruma DEĞİL, ısırdığı kanıtlı).
- **KANIT KURALI v2.1 TUTTU — 2b2'nin BULGU-4'ü KAPANDI:** 7/7 dosyada ham İngilizce koşucu özet satırı (her birinde 8 adet), diakritik katlama/yeniden yazım YOK, revert-sonrası koşumlar TAM SUITE (2b2'de filtreliydi), koşulmamış "beklenir" iddiası YOK.
- **Sapma adjudikasyonu: 5 KABUL / 0 RET.** Yeni test yardımcısı (spec zaten şart koşuyordu) · **spec'in 6-vs-7 mutant tutarsızlığı — KUSUR BENİMDİ**, v3'e geçerken §2'de "6 mutant" kaldı; builder tabloyu otoriter alıp 7'yi koşturdu, doğru karar, spec düzeltildi · oturum-içi kaza (commit öncesi `git checkout --` ile D1/D2/D6 silinmesi, elle yeniden yazım) — nihai kodu satır satır denetledim, doğru, risk materyalize olmadı · "P3 iter=1000'de kırılmadı, 30000'de ayrıştı" dürüstlüğü — spec'in "ZORUNLU kırmızı" ifadesi fazla güçlüymüş, ama D1b yine doğru karardı (benim koşumumda mutant-5 ve mutant-6 P3'ü kırdı) · KANIT ayrı commit.
- **Builder'ın kendi işaret ettiği çelişki çözüldü:** mutant-1'de Persistence 35/35 GEÇTİ → **D4 kırılmadı**. Rapor yazım hatasıydı, sonuç doğru.
- **STRATEJİK DÖNÜŞ (Onur kilitledi, 19 Tem 2026): sıradaki dilim collab-auth DEĞİL, VİTRİN.** Tespit: DB'de 8 tablo var, **hiçbiri entity tablosu değil**; Flutter istemci hiç başlamadı. Dört dilimdir inşa edilen senkron motoru güçlü (property + oracle + mutant + ADR/errata izi) ama **çalışırken kimse göremiyor**. İşe-alım ödevinde çoğu değerlendirici önce çalıştırır. Zaman baskısı yok ama "durduk yere uzamasın".
- **ADR 0002'ye K2-I1/I2/I3 eklendi (kilitli):**
  - **K2-I1 — İstemci SUNUCU-OTORİTER uzlaştırır, TEK resolver [PAZARLIKSIZ].** Flutter kendi HLC/CRDT motorunu TAŞIMAZ; çevrimdışı yazım iyimser uygulanır + kuyruğa girer, çakışma çözümü yalnız sunucuda, pull'da sunucu durumu otoriter. Gerekçe bu projenin kendi ölçümü: ERRATA E-1 (aynı değişmezliğin üç çelişen kopyası) ve 2c'nin D1b'si (bağımsız Oracle üretimle ayrıştı — ve o sadece bir *test* motoruydu). Ödünleşim adlandırıldı: rollback/flicker olabilir, UI gizlemez. İleride ikinci resolver istenirse Dart tarafına da oracle+property+mutant zinciri kurulmadan YAZILAMAZ.
  - **K2-I2 — Materyalize entity satırı inşa edilir** (ADR §2'nin açık kalan yarısı): `tasks`/`task_lists` + okuma API'si. Kapsam pini: **Task + TaskList**; `Project`/`Tag` ertelendi (Project üyeliği auth diliminde zaten yeniden ele alınacak → şimdi yapmak iki kez yapmak olur).
  - **K2-I3 — Dilim bölünmesi:** **slice-3a** = sunucu materyalizasyonu + okuma API'si · **slice-3b** = Flutter istemci (Drift + senkron kuyruğu + çevrimdışı anahtarı + çakışma demosu), platform **Web + Android**. Her biri kendi KANIT'ıyla kapanır.
- **Sıradaki ilk iş:** **slice-3a spec'ini yaz** (GOREV_CLAUDE_CODE) → bağımsız denetim turları → kilit → Claude Code build → Cowork doğrular. Sonra slice-3b, sonra collab-auth.
- **Ertelenenler (adlandırılmış, açık):** K2-E3 push-authz/gerçek kimlik · D5 salt-okur üye boşluğu · K2-G2 grup-düşürme yarısı · K2-C7 pull ayağı (Y1 muafiyeti) · Redis backplane/yatay ölçek · compaction↔remove sıra bağımlılığı (tasarlanmış GC ödünleşimi) · `Cancelled` canonical↔observed sapması + `Remap`'in kalıcı olmaması (yalnız compaction ile tetiklenir, üretim çağıranı yok) · `Db.cs:63` UtcNow yedeği · SyncPuller snapshot damgası · `Project`/`Tag` materyalizasyonu.

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 7: slice-2b2 BAĞIMSIZ DOĞRULANDI — SÜRÜM UYGUN (koşullu))
- **Son yapılan:** Claude Code slice-2b2'yi build etti → **commit 9911556** (46 dosya, +2326/−21; PROJE_HAFIZA/ADR dokunulmamış, push YAPILMADI). **Cowork bağımsız doğruladı** (temiz oturum; spec'i yazan oturum değil): `git clean -xdf` sonrası **verify.ps1 DEĞİŞMEDEN exit 0** — build `-warnaserror` **0/0** · **75/75 test** kendi koşumla (arch 5 + **Api 12 DB'siz** + SyncCore 26 **boş diff** + Persistence 32) · CVE **0** · Domain **BOŞ diff** · sır yok. KANIT: `KANIT/slice-2b2/cowork-bagimsiz-dogrulama.txt`.
- **11 MUTANTIN 11'İ KENDİ KOŞUMDA ISIRDI** (11:48→12:02, tam çözüm testi + `--blame-hang-timeout 120s`, her biri `git checkout -- .` + `git status` ile temizlik doğrulanarak). **mutant-8 (amiral kapı): `FOR UPDATE SKIP LOCKED` → `FOR UPDATE` ⇒ Npgsql `55P03 lock timeout`, 6 sn'de FIRLAYAN İSTİSNA, HANG DEĞİL** — hang-timeout hiç devreye girmedi. **BULGU-2 KAPANDI:** D0 adım-3 artık kilitsiz yolda GREATEST'i koruyan gerçek bir kapı (mutant-1 ısırdı); D0-b'nin `pg_locks WHERE NOT granted` interleaving pini kurulu ve mutant-9 ile ölçülüyor.
- **BULGU-3 [ASIL — SLICE DIŞI, CİDDİ, AÇIK]: P1 convergence property GERÇEKTEN İHLAL EDİLİYOR.** `ConvergenceAndMonotonicityProperties.P1_permutations_of_an_op_set_converge` **CsCheck seed `fcGWfMJW_dB2`** ile **temiz ağaçta birebir yeniden üretildi** (flake değil, determinist). Fark tek satır: bir Project entity'sinde `C|1` ↔ `C|0`, yani **`EntityState.HasDeleteEditConflict` (ADR K2-C4, TÜRETİLMİŞ) ingest sırasına bağlı**. Aynı entity'nin tüm `F|`/`O|`/`S|` satırları iki sırada da aynı → saklanan durum yakınsıyor, yakınsamayan şey türetilmiş C4 bayrağı. **Kök neden — güçlü hipotez (KANITLANMADI):** `OrSetField.ApplyAdd` → `Adds.TryAdd(tag, hlc)` ("ilk damga kalır") **değişmeli değil**; aynı tag farklı Hlc ile iki kez gelirse ileri/ters sırada farklı damga kalır → `MaxStamp()` değişir → üyelik (`PresentElements`) etkilenmediği için `S|` aynı kalırken C4 bayrağı 1↔0 döner. **İKİNCİ DERECE BULGU (süreç):** P1 **CsCheck tohumunu pinlemiyor** → 2a'nın "39/39" ve 2b1'in "60/60" hükümleri kısmen şansa dayanıyordu. Sıklık: rastgele tohumla 20 ardışık koşumda 0 FAIL (nadir). **Sorumluluk 2a/Domain'dir; 2b2 Domain'e dokunmadı (boş diff) → 2b2'nin hükmünü düşürmez, ama "taç mücevher kapısı TAM" denemez.**
- **BULGU-4 [KANIT kalite kusuru — builder'a bildirildi]:** D9 KANIT KURALI iki noktada ihlal: (1) 11 mutant dosyasının hiçbirinde **kırmızı koşumun ham koşucu özet satırı yok** — sayımlar düzyazıyla yeniden yazılmış; (2) **mutant-3'ün kırılan-test listesi EKSİK** — builder 2 test yazmış, benim tam koşumumda **3** kırıldı (eksik olan: `DispatcherTests.Scope_transition_dispatches_to_both_old_and_new_scope_groups`); (3) 1/3/11'de assertion satır numaraları yanlış (kopyala-yapıştır izi). **[DÜZELTME:** (3)'ün ilk hâli "`ModelValidationTests` sınıfı repoda yok" diyordu — **YANLIŞTI**, sınıf `SchemaTests.cs:9`'da var; bir alt-ajanın beyanını kendim doğrulamadan aktardım, hedefli denetim yakaladı. Aynı kusurdan builder'ı eleştirirken işledim. **(1) ve (2) aynen ayakta**; (2)'yi kendi koşumumla doğrulamıştım.**]** **Bu 2b1'in BULGU-1'i DEĞİL** (mutantlar gerçekten koşulmuş — 11/11'ini ben doğruladım); kusur **izlenebilirlikte**. Kural: bundan sonra KANIT'a **ham koşucu çıktısı YAPIŞTIRILIR**, yeniden yazılmaz.
- **mutant-6 / D7 onarımı:** builder ilk D7 testinin ısırmadığını dürüstçe raporlamış ve testi **zayıflatmadan güçlendirmiş** (row1'in kendi yeni-scope yayını ayrı pump'la önceden tüketiliyor → `scope:A` yalnız eski-scope yolundan gelebiliyor). **Onarılmış testi ben mutant-6 ile kırdım: gerçekten ısırıyor → onarım geçerli.** Spec'in D7 tarifi eksikti (SPEC kusuru, kod kusuru değil).
- **Sapma adjudikasyonu: 7 KABUL / 0 RET.** FakeCurrentUser=Compile-link · migration `--startup-project Infrastructure` · G3'te 2 sn sınırlı bekleme (kabul kriteri 5'in "sleep YOK"u **H9'a** bağlıdır; H9'da sleep gerçekten yok) · G3 arka-plan dispatcher yarışı (adlandırılmış risk; 13 tam koşumda kendiliğinden flake vermedi) · Scoped lifetime (D2-e pini birebir) · Options POCO · commit amend. **D5 salt-okur-üye boşluğu TEYİT EDİLDİ, kapanmadı** (auth dilimi).
- **Küçük bulgu:** `Db.cs:63` `at ?? DateTimeOffset.UtcNow` yedeği, D6-b'nin test-seed duvar saati yasağında açık kapı bırakıyor (`at` vermeyi unutan gelecek test KB-1 tuzağını sessizce geri getirir; kabul kriteri 5b yakalamaz). Öneri: yedeği kaldır, `at`'i zorunlu yap.
- **BULGU-3 TEŞHİS EDİLDİ VE KÖK NEDEN KANITLANDI (oturum 7 devamı).** Klonda (`%TEMP%\cowork2b2\diag`, gerçek repo el değmedi) enstrümante edildi: `(el1, tag 00000004-0002-…)` çifti **iki farklı damgayla** geliyor — `A = 1700000300000.00000005.…0001` (op#4, clamp tavanına kırpılmış) ve `B = 1700000046192.00000000.…0000` (op#6); `deleteKey = 1700000300000.00000004.…0003`; sıralama **A > deleteKey > B**. `ApplyAdd`'in `Adds.TryAdd`'i **ilk geleni** sakladığı için ileri sırada A, ters sırada B kalıyor → `MaxStamp()` deleteKey'in iki yanına düşüyor → C4 bayrağı `1↔0`. **H1 DOĞRULANDI; H2 (RemoveStamps), H3 (farklı ingest sonucu), H4 (compaction), H5 (deleteKey farkı) ÇÜRÜTÜLDÜ.** Kusurun yeri: `OrSetField.cs:59` — birleştirme yarı-kafes **join** değil; "ilk yazan kazanır" idempotent ama **komütatif değil**.
- **ADR 0002 ERRATA E-1 + E-1b KİLİTLENDİ (Onur).** E-1: add damgasında **"en büyük damga kalır"**; kuralın **ÜÇ uygulama noktası** var ve üçü bugün birbiriyle çelişiyor — `ApplyAdd` (ilk-yazan) · `LoadTag` (son-yazan) · `SyncStore.UpsertTagAsync` SQL'i (koşulsuz son-yazan). Şema DEĞİŞMEZ. E-1b: `UpsertTagAsync` `GREATEST`'e geçer — **"SQL'de ikinci LWW yasak"** kuralına adlandırılmış istisna, K2-A4 kalıbının aynısı; **ısırdığı kanıtlanmadan kapı sayılmaz**. E-1'in "yan kazanç" fıkrası denetimde **düzeltildi**: compaction↔remove sıra bağımlılığı KALIR (K2-C2/Y2'nin tasarlanmış GC ödünleşimi), E-1 onu komütatif yapmaz.
- **slice-2c SPEC [v3] HAZIR:** `GOREV_CLAUDE_CODE/GOREV-slice-2c-yakinsama-duzeltmesi.md`. **ÜÇ TUR bağımsız denetim:** v1 → (red-team **5 bloker+5 majör+7 minör** · teknik doğruluk **2 bloker+5 majör+6 minör**) → v2 → (hedefli yeniden-doğrulama **1 kalan bloker+2 majör+1 minör**) → **v3**. **Denetimin yakaladıkları:** (1) `mutant-3`'ün kapısı **kördü** — `LoadTag` üretimde tag başına yalnız BİR kez çağrılıyor (PK gereği), max-join ile son-yazan **üretim yolunda ayırt edilemez** → D2 "API sözleşmesi kapısı" diye yeniden çerçevelendi; (2) **bağımsız Oracle da aynı kusuru taşıyor** (`OracleEngine.cs:291`) → D1 sonrası P3 zorunlu kırmızı; kabul kriteri 2'ye adlandırılmış istisna eklendi (D1b); (3) `mutant-6`'nın ısırma yüzeyi **bugün hiç yok** → D2c yeni teslimat; (4) izolasyon pini yalnız set kanalını kapatıyordu, C4'ün **dört kanalı** var → pin genişletildi; (5) **v2, v1'in "yarım-max" blokerini kapattığını BEYAN etmiş ama kapatmamıştı** — D2d'nin üreteci pinlenmemişti ve `OrSetProperties`'in mevcut `H(wall)` deyimi körlüğü garanti ediyordu → üreteç pini + **`mutant-7-applyadd-wallms-only-max`** eklendi; (6) D0'ın ön-koşul assert'i **yanlış katmandaydı** (clamp öncesi) → clamp sonrasına taşındı; (7) KANIT v2.1'in dayattığı özet satırı Türkçe yerelde hiç basılmıyordu → `DOTNET_CLI_UI_LANGUAGE=en` pinlendi. **Teknik denetim olumlu hükümleri:** metin `GREATEST` bu kodlama+collation ile **GÜVENLİ** (`{WallMs:D13}.{Counter:x8}.{ClientId:N}` sabit genişlik; her iki kolon da zaten `.UseCollation("C")`); `CompactBelow` `MaxStamp()`'i **bozmuyor** (canonical = `belowActive`'in arg-max'ı) ama bu bağ yazısızdı → D6 ile pinlendi.
- **KENDİ HATAM (düzeltildi):** slice-2b2 KANIT'ımın BULGU-4(3) maddesi "`ModelValidationTests` sınıfı repoda yok" diyordu — **YANLIŞ**, `SchemaTests.cs:9`'da var (sınıf adı dosya adından farklı olduğu için dosya-adıyla arayan ilk tarama bulamadı). Bir alt-ajanın beyanını **kendim doğrulamadan aktardım**; builder'ı tam bu kusurdan eleştirirken aynısını yaptım. KANIT ve bu hafıza düzeltildi. **BULGU-4'ün (1) ve (2) maddeleri aynen ayakta** — (2)'yi kendi koşumumla doğrulamıştım.
- **Sıradaki ilk iş:** slice-2c spec v3'ü **Claude Code'a ver** → build → **Cowork bağımsız doğrular** (D0/D0b, D2b/D2c, D2d, D3'ün ayırt edici testi ve **7 mutantı KENDİ koşarak**; özellikle mutant-5'in D0'ı yeşil bırakıp D2b'yi, mutant-7'nin D0/D0b'yi yeşil bırakıp D2d'yi kırdığını). Sonra 2b3/collab-auth (ertelenenler: K2-G2 grup-düşürme yarısı, K2-C7 pull ayağı/Y1 muafiyeti, push-authz E3, Redis backplane).
- **KABUL KRİTERİ 8 KAPANDI — ölçümle, varsayımla değil (Onur onayıyla, 19 Tem 2026).** Compose volume'ü (`momentum_momentum-pgdata`, PG 17, 46,4 MB) gerçekten var ve **migration'lar oraya UYGULANMIŞ** — 8 tablonun tamamı mevcut. **AMA tablolar BOŞ:** `sync_orset_tags = 0`, `outbox_messages = 0`, yinelenen `(entity,set,element,tag)` = 0. **Backfill GEREKMİYOR.** Yöntem: throwaway `postgres:17-alpine` konteyneri volume'e bağlandı, salt-okur sorgu koşuldu, konteyner silindi; volume'e dokunulmadı. *Not: ilk denemede volume adını öneksiz yazdım, Docker `momentum-pgdata` adında boş bir volume yarattı; Onur `docker volume rm` ile temizledi. Asıl ad proje önekli: `momentum_momentum-pgdata`.*
- **Uyarı:** BULGU-3 kapanmadan senkron çekirdeği "kanıtlanmış yakınsak" sayma.

## ⏭ DEVİR NOTU (19 Tem 2026 — oturum 6: slice-2b1 BAĞIMSIZ DOĞRULANDI — SÜRÜM UYGUN (koşullu))
- **Son yapılan:** Claude Code slice-2b1'i build etti → **commit 4a721b1** (64 dosya; PROJE_HAFIZA/ADR dokunulmamış, sır yok, push YAPILMADI). **Cowork bağımsız doğruladı** (builder beyanına güvenmeden, her ölçüm yeniden koşuldu): `git clean -xdf` sonrası temiz rebuild **0/0** (-warnaserror) · **60/60 test** kendi koşumla (arch 4 + api 9 + SyncCore 26 **değişmeden** + Persistence 21, Testcontainers) · **5/5 mutant CANLI ısırdı** (m1→H7b, m2→H8, m3→H12, m4→H10, m5→rollback-atomiklik; hepsi `git checkout` ile birebir geri alındı — dosyalar artık tracked olduğundan 2a'daki Edit-revert zayıflığı ortadan kalktı) · şema `\d` ↔ ADR K2-F1 **birebir** (kendi compose+migration+psql koşumla; `signaled_at`/`attempts`/`available_at` builder raporunda sayılmamıştı ama **şemada var**; `server_seq` dizisi seqstart=1 → "≥1" değişmezi tutuyor; commit_xid/server_seq EF'te map **edilmemiş**) · **Domain ADDITIVE-ONLY: 201 ekleme / 0 silme**, SyncCore testleri + verify.ps1 boş diff · **CVE 0** (8 proje) · sır yok (yalnız açık placeholder'lar). KANIT: `KANIT/slice-2b1/cowork-bagimsiz-dogrulama.txt`.
- **BULGU-1 (süreç ihlali, RET edilen sapma):** Builder mutant-3'ün **ikinci varyantını KOŞMADI**, KANIT'a akıl yürütmeyle "beklenir" diye yazdı — projenin **"kör kapı yok"** ilkesinin doğrudan ihlali (üretici kendi kapısını akıl yürütmeyle onaylamış). Cowork koştu.
- **BULGU-2 (ASIL BULGU — kör koruma) [AÇIK]:** Cowork'un tasarladığı **ayırt edici mutant** (GREATEST → düz yazım, **client advisory-lock YERİNDE**) → **21/21 YEŞİL, hiç ısırmadı**. Yani **ADR K2-A4'ün "GREATEST kalıcı" pini bugün hiçbir testin ısırdığı bir yüzey değil.** Spec'in D10-3 ikinci-varyant reçetesi de **ayırt edici değil** (varyant A ve B *aynı* satırda `ConcurrencyTests.cs:33` kırılıyor; GREATEST'e hiç ulaşılmıyor). **Kod kusuru DEĞİL** (GREATEST doğru yerde, ADR'ye uygun) — **kapı boşluğu**. Kilit tutulurken aynı client'ın saat yazımları zaten serileşiyor → GREATEST fiilen savunma-derinliği.
- **KARAR (Onur, 19 Tem 2026) → (a) AYIRT EDİCİ TEST EKLENECEK.** 2b2 spec'inin **ilk maddesi (D0)**. Kilitli reçete:
  - **Test:** `Client_clock_never_goes_backwards_when_a_writer_skips_the_lock` (Persistence.Tests).
  - **Kurgu:** (1) client C normal `/sync` yolundan yüksek HLC yazar → `sync_client_clock.hlc = H_high`. (2) **Kilidi TUTMADAN**, port seviyesinde doğrudan `IClientClock.UpsertGreatestAsync(C, H_low)` çağrılır (`H_low < H_high`) — geleceğin "kilidi unutan" kod yolunu taklit eder. (3) Assert: saat **hâlâ `H_high`** (geri atlamadı). (4) **Ters yön:** `UpsertGreatestAsync(C, H_higher)` → saat `H_higher` olur (test tek-yönlü no-op mutasyonla tatmin edilemesin).
  - **Neden port seviyesinde:** tam `/sync` yolu kilidi alır **ve** zaten monoton etkin-HLC üretir; bu yüzden GREATEST oraya hiç ulaşmaz. Test edilen şey **depolama-seviyesi değişmezi**: "saat, çağıranın disiplininden BAĞIMSIZ olarak monotondur".
  - **Isırması zorunlu 2 mutant (KANIT):** m-A `GREATEST(...)` → `excluded.hlc` (düz yazım) ⇒ adım-3 FAIL · m-B `GREATEST(...)` → `sync_client_clock.hlc` (hiç güncelleme) ⇒ adım-4 FAIL. **İkisi de ısırmazsa test kör demektir.**
  - **Doktrin:** testi **Claude Code yazar**, Cowork iki mutantı kendi ısırtır (üreten ≠ denetleyen korunur).
- **Sapma adjudikasyonu: 11 KABUL / 1 RET.** KABUL: EF **9.0.1**'e hizalama (Npgsql 9.0.4 → Relational 9.0.1 kısıtı gerçek; spec'in 9.0.18 önerisi geçersizdi — **SPEC-ERRATA**) · Mediator lifetime `[assembly: MediatorOptions(Scoped)]` (kaynak-üreteç derleme-zamanı kısıtı) · koşullu Npgsql health (503 testiyle kapılı) · Health/design-time-factory yerleşimi (arch yeşil) · flag-only resync (spec D3 refinement) · mutant geri-alma=Edit (zayıf ama Cowork git checkout'la yeniden koştu) · payload clamp Domain'i yeniden kullanır · snapshot owner-filtresi outbox'tan (adlandırılmış erteleme) · Testcontainers image ctor · paralelsizlik (xid/xmin cluster-global) · PATH. **RET: mutant-3 ikinci varyantının koşulmaması (BULGU-1).**
- **slice-2b2 SPEC HAZIR [v4] (oturum 6 devamı ✓):** `GOREV_CLAUDE_CODE/GOREV-slice-2b2-gercek-zaman.md`. Onur 3 forku kilitledi: **`ISignalPublisher` portu** (dispatcher SignalR'ı bilmez; H9 kaydeden sahte publisher'la determinist ölçülür) · **hub deny-by-default** (gerçek auth hâlâ auth diliminde) · **`IScopeMembershipSource` portu** (C7 old_scope çift-yayını bu dilimde gerçekten test edilir).
  **DÖRT TUR bağımsız denetim:** v1 → (mimari + red-team: **8 bloker + 12 majör**) → v2 → (hedefli doğrulama: **6 kalan bloker + 10 bulgu**) → v3 → (ikinci hedefli doğrulama: **4 kalan bloker + 6 bulgu**) → **v4**.
  **Denetim zincirinin yakaladığı en önemli şeyler:** (1) v1 kendi amiral kapısını (H9/`SKIP LOCKED`) **2b1'in kör-kapı kusuruyla birebir aynı şekilde** yeniden üretiyordu (sıralı pump + N<LIMIT + mutant yok); (2) v2'de **9 mutantın 3'ü ısırmıyordu** — biri v2'nin *kendi* lease düzeltmesinin yan etkisiyle öldü; (3) v2'nin hub-reddi assert yüzeyi **SignalR kaynağıyla çürütüldü** (`Context.Abort()` handshake'ten SONRA çalışır → `StartAsync` patlamaz) → v4'te **negotiate-401 middleware**; (4) v3'ün saat pini **test-seed yolundan** (`Db.cs`) deliniyordu ve "düzeltmenin doğal hâli" kusuru geri getiriyordu; (5) D8-v'in ikinci fazı **append-only outbox'ta kurulamıyordu**; (6) mutant-9'un interleaving'i pinlenmediği için **flake** olacaktı.
  **v4 kilit pinleri:** lease'li talep (gerçek at-least-once; ADR K2-F2'nin adlandırılmış refinement'i) · **tek saat kaynağı = `TimeProvider` parametresi**, SQL `now()` + **kolon DEFAULT'u + test seed'leri** dâhil yasak · `PumpOnceAsync` istisnayı **sızdırır** (mutant-8'in tek öldürme yolu) · `lock_timeout` bağlantı dizesinde · satır, gruplarından **herhangi biri** başarısızsa kapatılmaz · **kalıcı iskarta ve devre kesici YOK** (ikincisi mutantsız olduğu için kendisi kör kapıydı) · **11 mutant**, her biri hedef assert'i açıkça tanımlı · **D9-b "mutantsız kapılar" dürüstlük beyanı** (kapı diye sunulan her şey ısırdığını kanıtlar; kanıtlayamayan savunma-derinliği diye adlandırılır).
- **OTURUM 6 KAPANIŞI:** Spec Claude Code'a verildi, **2b2 build'i ŞU AN SÜRÜYOR**. Repo son commit `03c5fb1`, `origin/main` ile eşit, ağaç temizdi. Cowork doğrulaması **temiz oturuma** devredildi — gerekçe yalnız bağlam değil **doktrin**: v4 spec'ini yazan Cowork oturumu, aynı oturumda o spec'e karşı build denetlerse "ne yazdığımı biliyorum" yanlılığıyla eksik uygulamayı tamamlanmış sayabilir (üreten ≠ denetleyen).
- **Sıradaki ilk iş (temiz oturum):** build dönünce **Cowork bağımsız doğrular** — spec'i diskten yeniden okuyarak: temiz rebuild · 60 + yeni testler · **D0/D0-b** · **H9 (kilitli-küme kesişimi ∅)** · **11 mutantı KENDİ ısırt** (özellikle `mutant-8-no-skip-locked` → fırlayan istisna, HANG değil) · `\d outbox_messages`'ta **`available_at` Default'u BOŞ mu** · Domain BOŞ diff · CVE · sapma adjudikasyonu → hüküm + push + hafıza checkpoint.
- **Uyarı:** BULGU-2 kapanmadan "taç mücevher kapısı TAM" denemez — kapanışı 2b2'nin **D0**'ıdır.

## ⏭ DEVİR NOTU (18 Tem 2026 — oturum 5: slice-2a SPEC HAZIR + spec-QA GEÇTİ)
- **Son yapılan:** Onur kilitledi: **yol = slice-2a önce** (2b sonraki; DB hazır) + **CsCheck** (lisans NuGet'ten teyit: **Apache-2.0** — ADR risk #9 kapandı). **`GOREV_CLAUDE_CODE/GOREV-slice-2a-senkron-cekirdek.md` [v2] yazıldı ve bağımsız spec-QA'dan geçti:** QA **5 bloker + 9 majör** buldu (Duplicate semantiği · P1↔cap çelişkisi · P7 remap↔add-wins↔eşdeğerlik · görülmemiş-tag tombstone · mutant-5 kör kapı; EntityType tipi, EffectiveOpHlc gözlemlenebilirliği, eşzamanlılık sözleşmesi, oracle sıra-pini, set-damga önek kuralı, cap formülü, üreteç alanları, RejectedInvalid kapalı-liste, remap-compose) → hepsi işlendi → **hedefli yeniden-doğrulama: 5 bloker-düzeltmesi TUTTU**, tek çarpışma (Y-1 EffectiveOpHlc D5↔P2) kapatıldı. Üreten ≠ denetleyen korundu (2 bağımsız denetçi turu).
- **Spec'in kilit pinleri:** clamp HER HLC'ye / monoton-etkin YALNIZ op-HLC'ye (ADR H1'in Cowork-kilitli yorumu; fork #2 + P1 gerekçesi) · registry-ihlali = op-bütünü REJECT · kısmi grup-yazımı legal + REPLACE · OR-Set görülmemiş-tag iptali kalıcı (tombstone) + compose-remap + cap=100 kesin formül · dedup clamp'ten ÖNCE (replay determinizmi) · Applied-kökenli Duplicate orijinal EffectiveOpHlc taşır · 2a eşzamanlılık = global ingest kilidi (per-client advisory-lock 2b'ye).
- **BUILD + BAĞIMSIZ DOĞRULAMA (oturum 5 sonu ✓ — HÜKÜM: SÜRÜM UYGUN):** Claude Code slice-2a'yı build etti → **commit 7e588a4** (51 dosya; PROJE_HAFIZA/ADR dokunulmamış, bin/obj/sır yok — commit-stat'la denetlendi). **Cowork bağımsız doğruladı (builder beyanına güvenmeden):** rebuild `-warnaserror` **0/0** · **39/39 test** (arch 4 + api 9 + SyncCore 26) kendi koşumla · **verify.ps1 exit 0** + **CVE 0 zafiyet** · **mutant-4 ve mutant-2 CANLI yeniden ısırtıldı** (m4→P1/P9/P6 FAIL; m2→P7×3/P3/P1 FAIL; revert→yeşil; kör kapı yok) · kalan 3 mutantın KANIT'ı denetlendi (5/5 ısırdı) · **oracle-izolasyon kendi taramamla** (Oracle/ altında tek using: `System.Text`) · **registry ADR B2 ile birebir** (Task 8+3+2+1; Project/TaskList/Tag tam) · CsCheck 4.7.0 Apache-2.0 (nuspec SPDX). **Sapma adjudikasyonu: 9/9 KABUL**; #1 **SPEC-ERRATA**: `RejectedInvalid` ProcessedOps'a KAYDEDİLMEZ (adım-1 zaman-bağımsız → replay'de determinist yeniden-red; oracle birebir; "reddedilenler kaydedilir" kuralı receive-time-bağımlı redler [absürt/registry/cap] için geçerli — 2b spec'ine bu haliyle taşınacak). Diğerleri: grup-üye doğrulaması (registry'nin doğal uzantısı), op-içi add-önce (üyelik-nötr), Edit-ile-revert (dosyalar untracked'dı), P3↔P10 kapsam bölüşümü, üreteç havuzları, PATH, compaction-testte, RemoveStamps (spec-izinli).
- **slice-2b1 SPEC HAZIR (oturum 5 devamı ✓):** Onur kilitledi: **2b → 2b1 (kalıcılık+/sync) + 2b2 (F2 dispatcher+SignalR+H9)**; **EF Core Migrations**. `GOREV_CLAUDE_CODE/GOREV-slice-2b1-kalicilik-sync.md` [v2] yazıldı; **bağımsız spec-QA: 5 bloker + 9 majör + 10 minör** (kritikler: farklı-client aynı-entity lost-update → **client→entity çift advisory-lock protokolü**; SyncIngest-reuse reçetesi; H8-ii holdback düzeltmesi; tombstone `hlc NULL` temsili; grup REPLACE marker-satırı) → hepsi işlendi → **hedefli yeniden-doğrulama: 5/5 TUTTU** + 2 kalıntı-majör (H12 farklı-entity pini, boş-grup marker) kapatıldı. Spec-kilit pinleri: karar YALNIZ Domain'de (SQL-LWW yasak; tek istisna sync_client_clock GREATEST) · commit_xid/server_seq EF'te map edilmez (GENERATED ALWAYS + migrationBuilder.Sql) · xid8 parametre text/`NpgsqlDbType.Xid8` · H10 restore = hlc substring(24,32) per-client lazy · flag-only resync (adlandırılmış refinement) · PostgreSQL License erratası (Npgsql).
- **Sıradaki ilk iş:** Onur 2b1 spec'ini **Claude Code'a verir** → build → **Cowork bağımsız doğrula** (rebuild, tüm testler, 5 mutantı kendi ısırt, şemayı `\d` ile ADR F1'e karşı, Domain-additive git-diff, CVE) + KANIT + hafıza. Sonra **2b2 spec** (dispatcher F2 + SignalR G/G2 + H9).
- **Açık:** PATH hijyeni opsiyonel (DC kabuklarında dotnet/flutter full-path veya registry-PATH tazele). Yeni commit'ler DC ile push (gh keyring kalıcı).

## ⏭ DEVİR NOTU (18 Tem 2026 — oturum 4: ADR 0002 KİLİTLENDİ)
- **Son yapılan:** **ADR 0002 (Senkron Protokol Mekaniği / taç mücevher)** yazıldı ve **3 bağımsız kapıdan** geçirilip **KİLİTLENDİ** (`docs/ADR/0002-senkron-mekanigi.md`, v3). Akış: Onur 4 forku kilitledi (katmanlı çakışma · istemci-HLC korunur · sinyal+pull · yoklamalı Outbox) → **engineering ×2** (architecture+system-design) **4 örtüşen bloker** buldu (relayHighWater boşluksuz değil · F2↔F3 çelişki · nodeId çakışması · GC-resync yok) → v2 → **red-team** **2 yeni bloker** (resync-imleci atlama · eşzamanlı clamp yarışı) buldu + v2'nin 9 düzeltmesini doğruladı → v3 → **hedefli yeniden-doğrulama: iki bloker-düzeltmesi TUTTU**, 4 netleştirme (Y1/Y2/COLLATE/seq≥1) kapatıldı → Onur onayı → KİLİT.
- **Kilitlenen çekirdek:** HLC `(wallMs,counter,clientId,operationId)` **koşulsuz tam-sıra** (COLLATE "C"/Ordinal) · **iki-saat ayrımı** (çakışma=edit-HLC; imleç=`(commit_xid,server_seq)`+`pg_snapshot_xmin` ufku; **resync devam-imleci=`(horizon,0)`**) · **katmanlı çakışma** (per-alan LWW + eş-çözülen grup + OR-Set add-wins+compaction + kesirli-index + isDeleted-LWW + çakışma-yüzeyleme) · idempotency (operationId dedup, ON CONFLICT) · `/sync` batch (op-başına txn, UoW opt-out, entity-arası soft-ref) · Outbox (owner/scope yönlendirme, `SKIP LOCKED` **sinyal ≠ imleç**) · SignalR **payload'suz dürtü** · **GC-ufku→zorunlu resync** · taç-mücevher kapısı (bağımsız oracle + CRDT-yasa property + mutant + Testcontainers görünürlük/eşzamanlılık).
- **Ertelenen/sınırlanan (Onur onayladı):** RRULE *occurrence* kapsam dışı (tekrar-dilimi; RRULE-string skaler-LWW) · entity-arası *soft-ref + reconciliation* · OR-Set *cap* · push-authz + `clientId→principal` *auth dilimine* · tek-primary varsayımı (read-replica monotonluğu ileride).
- **Sıradaki ilk iş:** **slice-2a spec** (`GOREV_CLAUDE_CODE`) — **saf senkron çekirdeği + taç mücevher kapısı, DB'siz** (HLC A1–A4 saf + çözümleyici + sunucu-zorlamalı registry + zarf tipleri + oracle + property + mutant, **saf Domain**; PostgreSQL/reboot gerekmez). Akış: spec sun → spec-QA → Claude Code build → Cowork bağımsız doğrula. Sonra **DB/Docker dilimi (WSL2+reboot)** → **slice-2b** (Outbox/`/sync`/SignalR kalıcılığı + Testcontainers hazard testleri H7b/H8/H9/H11/H12 + HLC restart H10).
- **GitHub (oturum 4 sonu ✓):** gh CLI 2.96 kuruldu; **private repo YAYINDA → https://github.com/tuzakavcisi1-cloud/Momentum** (remote `origin`, `main`→`origin/main`, 9 commit push edildi; sır taraması temiz — red line #1 ✓). gh auth hesabı: `tuzakavcisi1-cloud` (keyring). Commit yazarı repo-yerel Onur Kesim / onurkesimbjk@gmail.com.
- **ORTAM ARTIK TAM ✓ (oturum 4 sonu):** `flutter doctor` → **• No issues found!** — Flutter 3.44.6 · Android SDK 36.1.0 · **VS C++ Build Tools 2022 17.14.36** (masaüstü hazır) · Chrome/Web · Windows · 3 cihaz · ağ hepsi ✓. **Docker 29.6.1** + **WSL2** (Ubuntu v2 + docker-desktop backend Running) kurulu ve çalışıyor. Runbook: `araclar/KURULUM-kalan-adimlar.md`. **Ortam bulgusu (kalıcı):** bazı DC kabukları SDK'sız x86 `dotnet`'i öne alıyor; 64-bit SDK 9.0.316 kurulu, Claude Code doğru kullanıyor (PATH hijyeni opsiyonel). flutter doctor VS kontrolü yalnız `%PROGRAMFILES(X86)%` env'i olmayan kabukta çöker (Onur'un terminalinde sorun yok).
- **Açık:** Ortam TAM — DB için env engeli **kalktı** (Docker+WSL2 hazır); PostgreSQL slice-2b'de Docker Compose ile gelir (ek kurulum/reboot YOK). Oturum 4 **SARI** → sıradaki iş (slice-2a spec; DB hazır olduğundan istenirse slice-2b de açık) temiz oturumda ("başla").

## ⏭ DEVİR NOTU (18 Tem 2026 — oturum 3: slice-1 BUILD + BAĞIMSIZ DOĞRULANDI)
- **Son yapılan:** Claude Code slice-1'i (backend omurga) build etti → **commit 6a614aa**. **Cowork bağımsız doğruladı** (Desktop Commander / gerçek FS, builder beyanına güvenmeden): temiz rebuild **0 uyarı / 0 hata** (-warnaserror); **13/13 test** (arch 4 + api 9); **CVE 0 zafiyet** (6 proje); commit **temiz** (PROJE_HAFIZA/docs/ADR'ye dokunulmamış, bin/obj/sır yok, tree clean, 8 KANIT commit'te). **Kör kapı yok — CANLI kanıt:** üretime `DateTime.UtcNow` enjekte → **RS0030 build FAILED**; `git checkout` → **0/0** geri döndü. (health-ready→503 kapısı geçen 13 test içinde.) **HÜKÜM: SÜRÜM UYGUN.**
- **Errata/karar:** Shouldly lisansı **BSD-3-Clause** (ADR K-H2 etiketi errata ile düzeltildi) — permissive & red-line-safe → **RATİFİYE**; izinli lisanslar artık MIT/Apache/**BSD-3-Clause**. (dependencies.md zaten doğru yazmış.) Kalıntı: Claude Code test host'u (PID 13640) açık kalıp DLL kilitlemişti → Cowork kapattı (kural: host'u kapat).
- **Sıradaki ilk iş:** **ADR 0002** — senkron protokol mekaniği (delta tel-format + HLC tick + alan-düzeyi çakışma + idempotency + Outbox tablo şeması + taç-mücevher doğrulama kapısı): şıklarla sun → engineering/red-team kapıları → kilit → spec. Alternatif (Onur seçerse): önce DB/Docker dilimi (reboot) ya da ilk entity dikey dilimi.
- **Açık:** DB ertelendi (Docker/WSL2, reboot). GitHub ilk push'ta authorize.

## ⏭ DEVİR NOTU (18 Tem 2026 — oturum 3: ADR 0001 KİLİTLENDİ)
- **Son yapılan:** **ADR 0001 (Genel Mimari / Backend Omurga)** yazıldı ve **3 bağımsız kapıdan** geçti: engineering:architecture + engineering:system-design + red-team. v1→v3 acımasız denetimle olgunlaştı — taşıyıcı senkron/işbirliği kontratları (entity-base, `operationId` zarfı, integration-event zarfı, **Outbox** atomikliği, sunucu **HLC** otoritesi, **soft-delete/tombstone**) 0001'e *yön* olarak alındı; **"şimdi-kodla vs yön-notu"** çizgisi açıkça çizildi (over-engineering'e karşı). İki lisans tuzağı yakalandı: **FluentAssertions 8 + AutoMapper ticari** → **Shouldly + Mapster (MIT)**. `DateTime.UtcNow` yasağı NetArchTest→**BannedApiAnalyzers**'a taşındı; **CVE kapısı** eklendi (red-line #3 tamamlandı). **K-B1a mediator** Onur tarafından Cowork'e devredildi → araştırmayla **`martinothamar/Mediator` (MIT, stabil pin)** seçildi. ADR **KİLİTLİ**. Dosya: `docs/ADR/0001-genel-mimari.md`.
- **Sıradaki ilk iş:** **Claude Code slice-1 build** — spec HAZIR + denetlendi: `GOREV_CLAUDE_CODE/GOREV-slice-1-backend-omurga.md` (v2; bağımsız spec-QA'dan geçti — 11 muğlaklık + 2 kör kapı [CVE exit-0, arch Kural-2 minimal-API] + 2 kırılgan test [health-503, ProblemDetails ortam tuzağı] kapatıldı). Onur Claude Code'a verecek → build → **Cowork Desktop Commander ile BAĞIMSIZ doğrula** (build/test/arch/banned/CVE'yi kendi koş) + KANIT + hafıza checkpoint. Sonra **ADR 0002** (senkron mekaniği).
- **Açık:** DB ertelendi (Docker/WSL2). GitHub ilk push'ta authorize. **Bakım notu:** global `CLAUDE.md` `engineering`/`design`/`operations`'ı "kaldırıldı" gösteriyor ama bu oturumda kuruluydu ve kapılar fiilen çalıştı → bakım kuralına göre listeden silinmeli (3 denetçi de işaretledi).

## ⏭ DEVİR NOTU (18 Tem 2026 — oturum 2 kapanış)
- **Son yapılan:** Ortam kuruldu+doğrulandı: **.NET 9 SDK 9.0.316 (64-bit, makine PATH'inde öne alındı)**, **Flutter 3.44.6 + Dart 3.12.2** (`C:\src\flutter`, kullanıcı PATH). **`git init` (branch `main`) + kapsamlı `.gitignore`/`.gitattributes` + ilk commit `a12b3a2`** (14 dosya, iskelet .gitkeep'lerle korundu). Repo kimliği (repo-yerel): Onur Kesim / onurkesimbjk@gmail.com.
- **PostgreSQL — ERTELENDİ (Onur kararı):** Native `initdb` bu makinede çalışmıyor: **sistem locale tr-TR / ANSI cp1254**; initdb "Türkiye"deki non-ASCII `ü`'yü reddediyor. Üç varyasyon (`--no-locale`, açık `--lc-*=C`, ICU) + `LC_ALL=C` env + `Set-Culture` — hiçbiri çözmüyor (sistem locale'i değiştirip reboot gerek). Docker da WSL2 istiyor (kurulu değil → reboot). Karar: **DB ertelendi**, kalıcılık diliminde **Docker** ile kurulacak. Atıl yarım kurulum `C:\Program Files\PostgreSQL\16` Onur onayıyla **tamamen silindi** (klasör + ARP + Başlat menüsü; doğrulandı).
- **Sıradaki ilk iş:** Backend **omurga** dikey dilimi → **ADR 0001 (genel mimari: Clean Architecture + senkron stratejisi)** tasarımını işaretlenebilir şıklarla sun → onay → engineering/red-team kapıları → KİLİT → ilk GOREV_CLAUDE_CODE spec (solution + katmanlar + health ucu) → Claude Code build → verify. DB gerektiğinde ayrı adım: Docker (WSL2+reboot).
- **Açık karar:** DB kurulumu kalıcılık diliminde netleşecek (Docker varsayılan). **Dosyalar:** bu dosya + CLAUDE.md.

## PROJE (tek cümle)
Çok platformlu (Flutter: Android/iOS/Web/Windows) + N-katmanlı .NET 9 backend + PostgreSQL ile; çevrimdışı-öncelikli senkron ve gerçek zamanlı işbirliği vitrinli bir görev yönetimi (to-do) uygulaması. Bağlam: önemli bir yazılım şirketinden gelen işe-alım/portfolyo ödevi; **ODAK = mimari & kod kalitesi.**

## KİLİTLENEN KARARLAR
- **Olgunluk:** işlevsel prototip ama deploy-edilebilir KALİTEDE; portfolyo + "yayına götürme sürecini biliyoruz" (CI/CD, store paketleme, imzalama artefaktları — fiilen yayınlanmayacak).
- **Ana vitrin (taç mücevher):** (1) çevrimdışı-öncelikli senkron + çakışma çözümü, (2) gerçek zamanlı işbirliği. **İKİSİ BİRDEN.**
- **Farklılaştırıcılar:** ~~(dördü de): NLP hızlı ekleme, Kanban+Takvim görünümü, Time-blocking + Google Takvim, AI asistan (görev bölme/öneri).~~ → **GEÇERSİZ (K7-a, 20 Tem 2026).** Yürürlükteki karar: **YALNIZ NLP HIZLI EKLEME.** Diğer üçü adlandırılarak elendi (`docs/ODEV.md` §4.1 eleme tablosu). *Kanban+Takvim = tampon açılırsa İLK geri çağrılacak aday.*
- **Çekirdek:** görev/proje/liste/etiket/öncelik/tarih-sontarih/tekrar(RRULE)/hatırlatıcı/arama/karanlık mod.
- **Tempo:** ~~dengeli ~4-6 hafta;~~ → **GEÇERSİZ (K7-b, 20 Tem 2026).** Yürürlükteki hedef: **~15 Ağu 2026** (başlangıç 18 Tem; Onur'un 2-3 haftalık tahmini + kapı yükü ölçümüyle esnetildi; şirketin verdiği tarih DEĞİL — `docs/ODEV.md` §3.1, §8(2) hâlâ açık). Strateji değişmedi: **OMURGA-ÖNCE / DİKEY-DİLİM** (önce mimari+senkron+kimlik, sonra özellikler dikey dilim).
- **Teknoloji:** Flutter istemci (local-first: Drift/SQLite + senkron kuyruğu) + .NET 9/ASP.NET Core (Clean Architecture/DDD/CQRS) + PostgreSQL; real-time **SignalR**; ~~AI için **Semantic Kernel**~~ → **GEÇERSİZ (K7-a, 20 Tem 2026):** AI asistan elendi (LLM deterministik değil ⇒ mutantla ısırtılamaz + çevrimdışı vitriniyle çelişir) ⇒ **Semantic Kernel PROJEDE YOK, bağımlılık olarak eklenmeyecek.**
- **Platform gerçeği:** Mac YOK. iOS yalnız CI'da (macOS runner) DERLENİR (fiili cihaz/simülatör testi YOK, kod-hazır). Android + Web CANLI. Windows masaüstü bu PC'de derlenebilir (VS C++ toolchain gerekir — sonraki dilimde kurulacak).
- **Figma:** BAĞLANMADI (Flutter widget + design-system dokümanıyla). **GitHub:** bağlanacak — ilk push anında authorize (Claude in Chrome ile).

## DENETÇİ MİMARİSİ (üreten ≠ denetleyen)
- **Katman 1 — Otomatik kapılar (verify zinciri):** backend `dotnet build`+test+analyzer+güvenlik taraması; frontend `flutter analyze`+test+a11y smoke; OpenAPI kontrat testi; **TAÇ MÜCEVHER KAPISI** (senkron/çakışma mantığını uygulama kodunu çağırmadan bağımsız 2. implementasyonla yeniden hesapla + idempotency); her kapı MUTANT testiyle "ısırdığını" kanıtlar (kör kapı yok).
- **Katman 2 — Uzman denetçi ajanlar (her kilit, bağımsız alt-ajan):** mimari (`engineering:architecture`/`system-design`), kod&güvenlik (`engineering:code-review`), test (`engineering:testing-strategy`), UX/a11y (`design:accessibility-review`/`design-critique`), süreç/risk (`operations:risk-assessment` + `engineering:deploy-checklist`), **RED-TEAM EN SON.**
- **İş akışı:** ADR/tasarım kilidi → engineering skill doğrulama → red-team → KİLİT → GOREV_CLAUDE_CODE spec → Claude Code build → verify zinciri → uzman denetçiler → red-team → düzeltme → kapanış + KANIT.
- **Rol bölümü:** Cowork = tasarım/ADR/spec/orkestrasyon/hafıza/kapı; Claude Code = build. Cowork, Code'un beyanına güvenmez; artefaktı bağımsız doğrular.

## KIRMIZI ÇİZGİLER [PAZARLIKSIZ]
1. Sırlar (connection string, API key, JWT secret) repoya GİRMEZ — kullanıcı-sırrı yönetimi/.env + `.gitignore`.
2. Kişisel veri (PII) minimumda; gizlilik-öncelikli tasarım.
3. Bağımlılık eklerken lisans + CVE kontrolü (kapı).
4. Kalıcı silme, para/hesap işlemi, güvenlik ayarı değişimi → Onur onayı olmadan YOK.
5. Build artefaktları (`bin/`,`obj/`,`build/`,`node_modules/`) git-ignore.

## ORTAM DURUMU (18 Tem 2026, Windows PC — oturum 2 tanısı)
- **KURULU:** git, Node/npm, Claude Code (`claude.exe`), Android Studio + Android SDK, winget. Mimari AMD64. Desktop OneDrive'da DEĞİL.
- **.NET (çözüldü ✓):** 64-bit .NET 9 SDK **9.0.316** kuruldu (`C:\Program Files\dotnet`). Eski x86 (`C:\Program Files (x86)\dotnet`, SDK'sız) duruyor ama makine PATH'inde 64-bit **öne alındı** (indeks 0 vs 6). Doğrulandı: `dotnet` → 9.0.316, `--list-sdks` → 9.0.316.
- **Flutter (✓):** Flutter **3.44.6** stable + Dart **3.12.2** (`C:\src\flutter`); `bin` kullanıcı PATH'ine eklendi (yeni terminal görüyor, doğrulandı). Doctor GAP'leri (sonraki dilimlerde çözülecek, omurgayı engellemez): Android cmdline-tools + lisanslar eksik; Visual Studio C++ (Windows masaüstü) yok — doctor VS kontrolü `%PROGRAMFILES(X86)%` env'i olmadığı için çöktü. Chrome(web)+cihazlar OK.
- **git (✓):** Repo başlatıldı — branch `main`, ilk commit `a12b3a2`, `.gitignore`+`.gitattributes` (sırlar/`bin`/`obj`/`build`/`.env` hariç). Kimlik repo-yerel: Onur Kesim / onurkesimbjk@gmail.com.
- **PostgreSQL (ERTELENDİ):** Native `initdb` tr-TR/cp1254 sistem locale'i yüzünden çalışmıyor (non-ASCII `ü`; bayrak/env/culture çözmedi). Atıl yarım kurulum `C:\Program Files\PostgreSQL\16` Onur onayıyla **silindi** (temiz). Karar: kalıcılık diliminde **Docker** (WSL2+reboot gerekli).
- **EKSİK (sonraki dilim):** DB (Docker+WSL2, reboot), VS C++ Build Tools (Windows masaüstü dilimi), Android cmdline-tools+lisans (Android dilimi).

## KARAR GÜNLÜĞÜ
- **18 Tem 2026:** Proje başladı. Pazar+disiplin araştırıldı (2 alt-ajan). Teknoloji .NET+Flutter+Postgres kilitlendi. Denetçi mimarisi onaylandı (`engineering`+`design`+`operations` plugin'leri kuruldu). Kapsam: 2 taç mücevher + 4 farklılaştırıcı, omurga-önce. İsim: **Momentum**. Platform: Mac yok → iOS CI-only. Kurulumlar Onur onayıyla winget'le başladı.
- **18 Tem 2026 (oturum 2 — planlama):** Ortam tanısı: .NET 9 SDK'nın kurulmadığı tespit edildi (devir notu düzeltildi). Onur onayıyla kilitlenen kurulum kararları: bu oturumda tam kurulum, Native PostgreSQL 16 (Docker ertelendi), Flutter git clone stable → `C:\src\flutter`. 64-bit .NET 9 SDK PATH'te öne alınacak; eski x86'ya dokunulmayacak.
- **18 Tem 2026 (oturum 2 — yürütme):** .NET 9 SDK 9.0.316 (64-bit) kuruldu, makine PATH'inde x86'nın önüne alındı (doğrulandı). Flutter 3.44.6 stable kuruldu (`flutter doctor` çalıştı; Android cmdline-tools/lisans + VS C++ eksikleri sonraki dilimlere). `git init` (main) + `.gitignore`/`.gitattributes` + ilk commit `a12b3a2`. **PostgreSQL kararı değişti → ERTELENDİ:** tr-TR/cp1254 sistem locale'i initdb'yi kırıyor (bayrak/env/culture çözmedi); WSL2 yok → native ve Docker ikisi de reboot ister. Onur: DB'yi ertele, kalıcılık diliminde Docker. Atıl `C:\Program Files\PostgreSQL\16` Onur onayıyla silindi (klasör+ARP+Başlat menüsü).

- **18 Tem 2026 (oturum 3 — ADR 0001 KİLİT):** ADR 0001 (Clean Arch 4 katman + CQRS/feature-folder; senkron YÖNÜ delta+HLC+alan-düzeyi [mekanik 0002]; UUIDv7; Outbox; soft-delete/tombstone; ProblemDetails RFC 9457; ısıran kapılar NetArchTest+BannedApiAnalyzers+CVE+ready-503) **3 bağımsız kapıdan** (architecture / system-design / red-team) geçirilip kilitlendi. Lisans-temiz yığın: martinothamar/Mediator + Shouldly + Mapster + Serilog + Asp.Versioning + Scalar (MIT/Apache). K-B1a Onur devri → martinothamar/Mediator (MIT) araştırmayla seçildi. FluentAssertions 8 / AutoMapper (ticari) ELENDİ.

- **18 Tem 2026 (oturum 4 — ADR 0002 KİLİT):** Senkron mekaniği ADR'si **3 bağımsız kapıdan** (engineering architecture+system-design → red-team EN SON → hedefli doğrulama) geçirilip kilitlendi (v1→v3). engineering **4 bloker** (relayHighWater boşluksuz değil, F2↔F3 çelişki, nodeId çakışması, GC-resync yok) + red-team **2 bloker** (resync-imleci atlama, eşzamanlı clamp yarışı) buldu; hepsi işlendi. Çözüm çekirdeği: imleç **`pg_snapshot_xmin` commit-ufku** (sinyalden ayrık) + **`operationId` koşulsuz eşitlik-bozucu** + **`(horizon,0)` resync-imleci**. Katman: çözümleyici=Domain, yayıncı=Infrastructure (NetArchTest temiz). Üreten ≠ denetleyen doktrini süreç boyunca uygulandı: hiçbir sürüm kendi üreticisince onaylanmadı.

- **18 Tem 2026 (oturum 5 — slice-2b açılış):** Onur kilitledi: **2b iki alt-dilime bölündü** — **2b1 = kalıcılık+/sync** (EF şeması + Compose + /v1/sync + xmin-imleç + resync + soft-ref + UoW opt-out + H7b/H8/H12/H10/H11-kısmi + UUIDv7-sıralama + ready→Npgsql-503) → **2b2 = gerçek-zaman** (F2 dispatcher + SignalR G/G2 + H9). **Şema yönetimi = EF Core Migrations** (Postgres-özel kısımlar migrationBuilder.Sql ile). ADR 0001 2b-yükümlülükleri not edildi: UUIDv7 sıralama testi (K-E1), ModelBuilder doğrulama (K-F1), UoW yalnız komutlar + /sync opt-out (K-G1/K2-E5), sırlar user-secrets/env.
- **18 Tem 2026 (oturum 5 — slice-2a KAPANIŞ):** Claude Code build (7e588a4) Cowork tarafından bağımsız doğrulandı → **SÜRÜM UYGUN** (39/39; 2 mutant Cowork elinde CANLI ısırdı; verify+CVE temiz; oracle izole; registry=ADR). 9 builder-sapması adjudike edildi (9/9 kabul; RejectedInvalid-kaydedilmez = spec-errata, 2b'ye taşınacak). Üreten ≠ denetleyen tam uygulandı: spec-QA (2 tur bağımsız denetçi) + build (Claude Code) + doğrulama (Cowork) ayrı ellerde.
- **19 Tem 2026 (oturum 6 — slice-2b1 KAPANIŞ, koşullu):** Claude Code build (**4a721b1**) Cowork tarafından bağımsız doğrulandı → **SÜRÜM UYGUN (koşullu)**: 60/60 test, 5/5 mutant Cowork elinde ısırdı (git checkout ile birebir revert), şema=ADR K2-F1 birebir, Domain 201/0 additive, CVE 0, sır yok. **İki bulgu:** (1) builder mutant-3'ün ikinci varyantını koşmadan "beklenir" diye KANIT'a yazdı → **kör kapı ihlali, RET**; (2) Cowork'un ayırt edici mutantı (**GREATEST düz yazım + kilit yerinde**) **hiç ısırmadı** → **ADR K2-A4 GREATEST pini kör bir koruma**; spec D10-3 reçetesi de ayırt edici değil (her iki varyant aynı assertion'da kırılıyor). Kod kusuru değil, kapı boşluğu; (a) ayırt edici test / (b) ADR'de savunma-derinliği olarak yeniden sınıflandırma kararı Onur'a bırakıldı. **SPEC-ERRATA:** EF 9.0.18 önerisi geçersiz (Npgsql 9.0.4 → Relational 9.0.1); 9.0.1'e hizalandı. Üreten ≠ denetleyen doktrini bu dilimde **fiilen iş gördü**: denetçi, üreticinin atladığı kapıyı koşup üreticinin kaçırdığı boşluğu buldu.
- **18 Tem 2026 (oturum 5 — slice-2a açılış):** Protokol koşuldu (hafıza+CLAUDE tam okundu; git temiz, HEAD=3235e8d=origin/main). Onur kilitledi: **yol = slice-2a önce** (ADR §3 sırası; 2b sonraki adım, DB hazır) · **property kütüphanesi = CsCheck**. **ADR risk #9 KAPANDI:** CsCheck lisansı NuGet'ten teyit edildi → **Apache-2.0** (MIT değil; izinli ailede, aktif bakım May 2026, 0 bağımlılık, net8.0→net9 uyumlu). FsCheck=BSD-3 (alternatifti). Spec-level kilitler: registry-ihlali = **REJECT** (remap değil; ADR H11 "kanal-atlayan alan reddedilir" ile tutarlı) · OR-Set add-tag soft-cap = **100**/(entity,set,eleman).

- **19 Tem 2026 (oturum 8 — slice-3a SPEC v4):** Onur 7 fork kilitledi (§3/F1–F7); en önemlisi **F5**: materyalize satırın `owner_id`'si **kimliği doğrulanmış** `command.ActorId`'den gelir — denetim, `WireOp.ActorId`'nin **istemci-beyanlı** olduğunu ve kimliği doğrulanmış actor'ın push yoluna hiç girmediğini kanıtladı (satır enjeksiyonu yüzeyi; bugün auth olmadığı için sömürülemez). **DÖRT denetim turu**, toplam **12 bloker**: en ağır ikisi Cowork'ün kendi ürettiği kusurlardı — (1) "taç mücevher" kapısının **totoloji** olması (2b1 BULGU-2'nin tekrarı), (2) `isDeleted` için **ikinci implementasyon** yazdırılması (E-1'in tekrarı). Ayrıca `mutant-13`'ün **yönü tersti** ve hiç ham kırmızı üretemiyordu; `mutant-1`'in kapı atfı fiziksel olarak yanlıştı; `task_lists` uçtan uca kapısızdı. **Ders (yazıya geçirildi):** bir projeksiyon kapısının iki tarafı da aynı fonksiyonu çağırıyorsa o kapı hiçbir şey ölçmez — **literal oracle** şarttır. Kapsam kararları: Task↔TaskList bağı **icat edilmez** (ADR K2-B2 kilitli), OR-Set yalnız `tags`, arama ertelendi, backfill yok (D8 çıpası).

## AÇIK İŞLER / SONRAKİ ADIMLAR
1. ✓ .NET 9 + Flutter kuruldu; `flutter doctor` çalıştı (Android cmdline-tools/lisans + VS C++ sonraki dilimlerde).
2. ✓ `git init` (main) + `.gitignore`/`.gitattributes` + ilk commit `a12b3a2`.
3. ✓ **ADR 0001 KİLİTLİ (v3)** (`docs/ADR/0001-genel-mimari.md`) — Clean Arch + CQRS/martinothamar-Mediator; 3 bağımsız kapı (architecture + system-design + red-team) geçti.
4. `verify` iskeleti (`tests/`) + ilk mutant/ısırma testi kurgusu.
5. ✓ **slice-1 spec** (v2, spec-QA geçti) → ✓ **Claude Code build (6a614aa)** → ✓ **Cowork BAĞIMSIZ DOĞRULADI** (rebuild 0/0, 13/13 test, CVE temiz, banned-gate canlı ısırdı, commit temiz; **SÜRÜM UYGUN**; Shouldly=BSD-3 ratifiye). **SIRADA:** ✓ ADR 0002 (senkron mekaniği) **KİLİTLENDİ** (oturum 4, v3, 3 kapı) → madde 8.
6. DB: kalıcılık diliminde Docker Desktop (WSL2+reboot) → PostgreSQL (compose). (Atıl native kurulum silindi ✓)
7. ✓ GitHub bağlandı (private repo + push; gh keyring).
8. ✓ **slice-2a TAMAM:** spec (v2, spec-QA) → ✓ Claude Code build (**7e588a4**) → ✓ **Cowork BAĞIMSIZ DOĞRULADI: SÜRÜM UYGUN** (rebuild 0/0, 39/39, verify exit 0, CVE 0, 2 mutant canlı + 5/5 KANIT, oracle izole, registry=ADR B2). **SIRADA:** slice-2b spec (DB/kalıcılık; Testcontainers hazard kapıları).
9. **ADR 0003 — KİMLİK ÇEKİRDEĞİ (`slice-3c-auth`, 1/2): KANONİK SÜRÜM = v6, KİLİTLİ DEĞİL.** *(Bu madde 25 Tem 2026 oturum 23'te güncellendi; eski metni **TASLAK v1 / ikinci ve SON tur / K6 tavanı** diyordu ve **BAYATTI** — K6 tavanı **K13-a ile kaldırıldı**, ADR **K11-h ile bölündü**, beş yazım turu ve **beş kapı turu** koştu.)* **Bugünkü durum:** v6 `docs/ADR/0003-kimlik-cekirdegi.md` (sha256 `6f6be71a…38cb08`); kapı-5'in **12 bloker + 7 majörü** ve kapı-4'ten devreden iki majör **kapatıldı**; Onur **K28** ve **K30** ile sekiz çatal kilitledi. Argon2id paket kapısı GEÇTİ (Konscious 1.3.1, K9).
   **⏱ GÜNCELLEME [26 Tem 2026, oturum 25 — K34]: 6. TUR KOŞTU, ARTIK "SIRADA" DEĞİLDİR.** Kapı-6 (oturum 24, 17 bağımsız denetçi) hem v6'yı hem **onarılmış aracı** hem **kapı-5 raporunu** denetledi ⇒ **HÜKÜM: v6 KİLİTLENEMEZ — 9 bloker · 31 majör · 14 minör · 8 çürütülen/daraltılan iddia.** Rapor: `KANIT/adr-0003/kapi-6-denetim-raporu.md` (34.106 bayt) · ham alt-ajan çıktıları `KANIT/adr-0003/kapi-6-ham-ajan-ciktilari/` (**11 dosya · 457.046 bayt**, ölçüldü — devir notunun *"12 dosya"*sı yanlıştı). **Turun kazanımı:** altın kümenin 17 kontrolünün 17'si mutasyonla zorlandı, 17'si de ısırdı ⇒ kapı-5'in *"altın küme süs olabilir"* endişesi KAPANDI. **Turun iki yapısal boşluğu:** ADR 0002 altı turdur hiç açılmadı · `docs/ODEV.md` hiçbir turda okunmamıştı.
   **Bugün (K34, 26 Tem 2026): kapı-6'nın YEDİ çatalının YEDİSİ de kilitlendi** (K34-a sonlanma ispatı → K13-a üstün · K34-b M56 probu · K34-c K3-L8 4. dalı [ŞEMA] · K34-d §3.2(9) izolasyonu · K34-e ADR 0002 v7'den önce açılır · K34-f araç onarımı FARKLI EL · K34-g M-L 5 mutantı beyanlı sınır + 3b borç listesi).
   **SIRADA: (1) ADR 0002'nin 9 atıfının ölçümü → (2) v7 YAZIMI (bu oturum; araca DOKUNULMAZ) → (3) ARACIN 2. ONARIMI — AYRI EL → (4) 7. TUR BAĞIMSIZ KAPI — AYRI OTURUM, `ADR 0002` + `src/` + `tests/` BAĞLI olarak ve KAPI-6 RAPORUNU DA denetleyerek.** Temiz çıkarsa Onur kilitler → **ADR 0004 (senkron-yetki aktivasyonu)** → `GOREV-slice-3c-auth` spec'i → Claude Code build → Cowork TEMİZ OTURUMDA doğrular.
10. **`DESIGN.md` — Tasarım Sistemi (K10 yönünde):** renk/tip/boşluk/radius/hareket/ses token'ları + ban-list + a11y kuralları. **Bu iş için ayrı bir ADR YAZILMAZ (K10-d);** *(⚠ 25 Tem 2026 oturum 23 düzeltmesi: eski metin *"Ayrı **ADR-0004** YAZILMAZ"* diyordu ve **NUMARA ÇAKIŞMASI ÜRETİYORDU** — **`ADR 0004` ARTIK VARDIR ve konusu SENKRON-YETKİ AKTİVASYONU'dur** (K11-h; ayrıca bu dosyanın K11 checkpoint'indeki *"NUMARA UYARISI"* satırı). K10-d **frontend tasarım ADR'sini** kastediyordu ve o karar geçerlidir: tasarım `DESIGN.md`'ye gömülür.)* `GOREV-slice-3b` spec'ine *"tasarım anayasası"* olarak gömülür. Kapılar: `design:design-critique` + `design:accessibility-review` → red-team EN SON. **a11y kapısı verify zincirine girer (PAZARLIKSIZ).** İmza öğeler MUST/NICE bölünmüş (K10-e). **~~`slice-3a-auth`~~ → `slice-3c-auth` KİLİTLENMEDEN ve Onur "başla" DEMEDEN BAŞLANMAZ.** *(⏱ 26 Tem 2026, oturum 25 — K34: bu maddedeki dilim adı da **BAYATTI**; K14-h adı `slice-3c-auth` olarak değiştirmişti. `slice-3a` = 0002 K2-I3'ün sunucu materyalizasyonu, `slice-3b` = Flutter istemci. Aynı bayat ad `docs/ODEV.md` satır 50 ve 121'de de ölçüldü ve orada da düzeltildi.)*
