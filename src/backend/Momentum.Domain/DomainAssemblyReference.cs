namespace Momentum.Domain;

/// <summary>
/// Marker type anchoring the <c>Momentum.Domain</c> assembly for architecture tests
/// (ADR 0001 K-H1). Entities/value objects are coded in later slices (ADR 0001 section 3).
/// </summary>
public sealed class DomainAssemblyReference;
