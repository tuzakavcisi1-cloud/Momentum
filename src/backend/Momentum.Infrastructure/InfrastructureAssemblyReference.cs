namespace Momentum.Infrastructure;

/// <summary>
/// Marker type anchoring the <c>Momentum.Infrastructure</c> assembly for architecture tests
/// (ADR 0001 K-H1). This layer is intentionally minimal in slice-1.
/// </summary>
public sealed class InfrastructureAssemblyReference;
