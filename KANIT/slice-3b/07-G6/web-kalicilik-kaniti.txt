G6 -- KALICILIK KAPISI -- WEB (Chrome, COOP/COEP başlıkları VAR)

Koşum: flutter run -d chrome --web-header=Cross-Origin-Opener-Policy=same-origin
--web-header=Cross-Origin-Embedder-Policy=require-corp --dart-define=ENABLE_FLUTTER_DRIVER=true
--print-dtd (gerçek GorevListesiEkrani, DURUM_VITRINI YOK)

Console log (Chrome DevTools, http://localhost:65500 üzerinden Claude Browser ile okundu):
  MOMENTUM-G6-KANIT chosenImplementation=WasmStorageImplementation.opfsLocks
  missingFeatures={MissingBrowserFeature.dedicatedWorkersInSharedWorkers}

HÜKÜM: `opfsLocks`, spec'in beklenen kümesinde (`opfsShared` VEYA `opfsLocks`).
GEÇTİ. `dedicatedWorkersInSharedWorkers` eksikliği opfsShared'dan opfsLocks'a
düşüşe neden oluyor (kalıcılık kaybı DEĞİL, hala OPFS tabanlı).
