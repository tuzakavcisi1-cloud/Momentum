MUTANT M4 -- flutter run -d chrome COOP/COEP --web-header bayraklari OLMADAN calistirildi
(--dart-define=ENABLE_FLUTTER_DRIVER=true --print-dtd, gercek ekran).

Console log:
  MOMENTUM-G6-KANIT chosenImplementation=WasmStorageImplementation.sharedIndexedDb
  missingFeatures={MissingBrowserFeature.dedicatedWorkersInSharedWorkers, MissingBrowserFeature.sharedArrayBuffers}

Karsilastirma (bayraklar VARKEN, ayni oturum):
  chosenImplementation=WasmStorageImplementation.opfsLocks
  missingFeatures={MissingBrowserFeature.dedicatedWorkersInSharedWorkers}

HUKUM: ISIRDI (beklenen kirmizi: secilen implementasyon opfs* DEGIL). COEP basligi
kaldirilinca sharedArrayBuffers ozelligi de kayboluyor (nedensel zincir dogrulandi) ve
WasmDatabase secimi opfsLocks'tan sharedIndexedDb'ye (kalici ama COOP/COEP kaybi) duser.
