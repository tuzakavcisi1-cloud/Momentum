# KANIT/CI/05 -- ERRATUM (is emri v3 / K176, bolum 3a) -- SATIR SONU OLCULDU
# Yazan: Cowork (o69). Kilitli is emrinin TASARIMI degismez; DUZELEN bir OLGUDUR.

## YANLIS OLAN
v3 bolum 3a birebir soyle diyor:
    ".gitattributes:7 => dosya CRLF'tir. ORTAM.md 38'in IKILI yordami zorunlu:
     bayt yamasi (desendeki \n -> \r\n)"
Bu cikarim .gitattributes'tan TURETILMISTI, dosya OLCULMEMISTI.

## OLCUM (10 Agu 2026, cihaz mount'u)
    git --no-optional-locks ls-files --eol -- araclar/verify.ps1
    i/lf    w/lf    attr/text eol=crlf      araclar/verify.ps1

    ham sayim: bayt=3747  CR=0  LF=94  CRLF=0

=> araclar/verify.ps1 SU ANDA hem index'te hem calisma agacinda **LF**'dir.
   Oznitelik CRLF ISTIYOR ama dosya HENUZ oyle degil.

## KOK NEDEN (olculdu)
    git --no-optional-locks log --oneline -1 -- .gitattributes  =>  e407f55
.gitattributes bu depoya BU GECE (e407f55, 10 Agu 2026 23:25) commit'lendi. verify.ps1
ondan ONCE checkout edilmisti; git dosyalari kendiliginden yeniden yazmaz, yalnizca
CHECKOUT aninda normalize eder.

## SONUC -- IKI FARKLI GERCEK, IKISI DE DOGRU
  * BU MAKINEDE, SU ANDA : verify.ps1 LF'tir. Bayt yamasi \n ile yapilir.
  * TEMIZ BIR KLONDA / SONRAKI CHECKOUT'TA : CRLF olarak materyallesir.

## KANONIK YORDAM (v3 3a'nin yerine gecen)
1. Yamalamadan ONCE OLC:  git --no-optional-locks ls-files --eol -- araclar/verify.ps1
2. 'w/' ne diyorsa desen ONA gore kurulur (w/lf => \n, w/crlf => \r\n).
   VARSAYIM YASAK: 'ORTAM.md 38' zaten bunu soyluyordu -- "dosya CRLF ise ...", KOSULLU.
   v3 kosulu SABIT SANDI.
3. Yamadan SONRA yine 'ls-files --eol' ile olculur ve sha256 kaydedilir.

## LATENT TEHLIKE (beyan)
Bu dosya LF olarak commit'lendikten sonra, baska bir makinede checkout edildiginde CRLF
olarak inecektir. Dolayisiyla verify.ps1'in sha256'si MAKINEDEN MAKINEYE DEGISIR.
=> verify.ps1 icin calisma-kopyasi <-> HEAD blob bayt karsilastirmasi TEK BASINA KORDUR
   (ORTAM.md 31'in tam olarak uyardigi sinif). Kimlik olcen her ayak ya LF'e normalize
   etmeli ya da 'ls-files --eol' ciktisini kanita yazmalidir.

## BU ERRATUM'UN KENDI DERSI
Cowork bu turda ALTINCI kez ayni sinifi uretti: bir OLGU, olculebilecekken bir BEYANDAN
(burada .gitattributes'tan) turetildi. Kural yaziliydi ("Olc ya da [DOGRULANMADI] yaz"),
uygulanmadi.
