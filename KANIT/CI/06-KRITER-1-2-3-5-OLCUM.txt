# KANIT/CI/06 -- KRITER 1, 2, 3, 5 OLCUMU (IS-EMRI-o69-backend-CI-v3 / K176)
# Olcen: Claude Code (builder). Cowork bunlari BAGIMSIZ yeniden kosacak (K26,
# is emri §5.2: "1, 3, 5'i yeniden koşar, statik, ortam istemez").

## KRITER 1 -- TABAN (uc dizin AYRI AYRI, ls-files --cached --others --exclude-standard)
    git --no-optional-locks ls-files --cached --others --exclude-standard GOREV_CLAUDE_CODE | wc -l  => 32
    git --no-optional-locks ls-files --cached --others --exclude-standard docs/ADR          | wc -l  =>  6
    git --no-optional-locks ls-files --cached --others --exclude-standard araclar           | wc -l  => 41
=> BEKLENEN 32 * 6 * 41 ile BIREBIR AYNI. Dosya sayisi degismedi (yalniz mevcut
   dosyalar ICERIK olarak degisti); yeni dosya ACILMADI (K175/2).

## KRITER 2 -- FARK OLCUMU (KANIT/CI/00-taban-olcum.txt'teki ANLIK GORUNTUYE karsi)
Dizin dizin (tam-agac KOSULMADI, ORTAM.md 44):
    git --no-optional-locks status --porcelain --untracked-files=normal -- .github/workflows/ci.yml
     M .github/workflows/ci.yml
    git --no-optional-locks status --porcelain --untracked-files=normal -- araclar/verify.ps1
     M araclar/verify.ps1
    git --no-optional-locks status --porcelain --untracked-files=normal -- araclar/ci-kapisi.py
     M araclar/ci-kapisi.py
    git --no-optional-locks status --porcelain --untracked-files=normal -- GOREV_CLAUDE_CODE
     M GOREV_CLAUDE_CODE/GOREV-A13-ios-iskeleti-ci.md
     M GOREV_CLAUDE_CODE/GOREV-W3b-web-yayina-alma.md
    git --no-optional-locks status --porcelain --untracked-files=normal -- docs/ADR
    (BOS -- ADR 0004 dokunulmadi, K175(1) parki korundu)
    git --no-optional-locks status --porcelain --untracked-files=normal -- DURUM.md
     M DURUM.md
    git --no-optional-locks status --porcelain --untracked-files=normal -- KANIT/CI
    ?? KANIT/CI/00-taban-olcum.txt (+ Cowork'un 04/05 dosyalari, kapsam disi degil)

=> FARK TAM OLARAK is emrinin izin verdigi ALTI yol: ci.yml * verify.ps1 *
   ci-kapisi.py * GOREV-A13-ios-iskeleti-ci.md * GOREV-W3b-web-yayina-alma.md *
   DURUM.md (SS6 satiri) -- arti KANIT/CI/** (yasagin DISINDA). BASKA HICBIR
   YOL DEGISMEDI (docs/ADR dahil).

## KRITER 3 -- CIKARMA TESTI (yanlislanabilir, is emri §4/3)
    python -c "... .github/workflows/ci.yml'den 'flutter build ios --no-codesign'
    satirindan SONRAKI HER SEYI (backend blogu, TEK PARCA, dosyanin SONUNDA) kes,
    KALAN kismin sha256'sini olc ..."

    yeni dosya toplam bayt        : 957
    cikarma sonrasi KALAN bayt    : 580
    cikarilan BLOK bayt           : 377
    KALAN kismin sha256           : 283d785e9fc3ee91d65ba2f0eee0764bb111c6c7e8e1ad0604a8563269ec22d1
    HEDEF (HEAD:ci.yml sha256)    : 283d785e9fc3ee91d65ba2f0eee0764bb111c6c7e8e1ad0604a8563269ec22d1
    OZDES                         : True

    Cikarilan blok (tek parca, dosyanin EN SONUNA eklendi -- istemci/ios
    govdelerinin ICINE hicbir satir DUSMEDI):
      backend:
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: .
        steps:
          - uses: actions/checkout@v4
          - run: pwsh --version
            shell: pwsh
          - run: docker info
            shell: pwsh
          - uses: actions/setup-dotnet@v4
            with:
              global-json-file: global.json
          - run: ./araclar/verify.ps1
            shell: pwsh

=> v2'nin Y2 karsi-orneginin (global 'shell: pwsh' + 'if: false' istemci/ios
   govdesine SESSIZCE ekleme) burada MUMKUN OLMADIGI dogrudan gorulur: bu iki
   is HICBIR SEKILDE degismedi (istemci/ios blok metinleri HEAD ile bayt-ozdes
   kalan 580 baytin ICINDE).

## KRITER 5 -- ci-kapisi.py ALTIN KUMESI (buyudukten sonra)
    python araclar/ci-kapisi.py --altin-kume
    => 22/22 GECTI, EXIT 0 (13 eski + 9 yeni: 8 ayak x1 + 1 coklu-ayak vakasi)
       Yeni ayaklarin HEPSI en az bir ISIRAN vaka kazandi (K40): 14=G31a *
       15=G31b * 16=G31c * 17=G31d * 18=G31e (tur-2 karsi ornegi) * 19=G31f
       (tur-2 IKINCI karsi ornegi) * 20=G31g * 22=G31h (bagimsiz denetimde
       bulunan akis-stili kor kapi, bkz KANIT/CI/09) * 21=G31c+G31g (coklu, K40).
       🔴 SAYI 21/21'DEN 22/22'YE CIKTI -- bagimsiz denetim G31/h'i (akis-stili
       YAML) eksik buldu, KANIT/CI/09'da DUZELTILDI. Bu satir o duzeltmeden
       SONRAKI HALI yansitir.

    python araclar/ci-kapisi.py .
    => BULGU YOK: G28/a,b * G29/a * G30/a,b,c * G31/a-h hepsi gecti. EXIT 0.
       (gercek, DEGISTIRILMIS ci.yml + pbxproj + gitignore uzerinde kosuldu)

## BEYAN
_CI_TEMIZ fikstürü bu turda GERCEK ci.yml'e HIZALANDI (kuresel 'defaults:'
eklendi, adim-duzeyi 'working-directory:' kaldirildi, 'backend:' isi eklendi)
-- Y5'in uyardigi "ikinci ci.yml kopyasi zaten sapmis" kusuru bu ISLEMLE
kapatildi ve yeni ayaklar bu YUZDEN kor DOGMADI.
