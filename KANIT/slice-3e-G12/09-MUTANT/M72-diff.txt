M72 -- signalr_json_sinyal.dart -- "_tekMesajiIsle"ye mesaj['arguments'] okuyan bir satir ekle
Hedef ayak: Y2 (K77/6 -- arguments/cursorHint yalniz yorumda gecebilir)

--- once ---
        if (mesaj['target'] == 'Changed') {
          gunlukYaz('Changed alindi -- SinyalDegisiklik yayinlaniyor');

--- sonra ---
        if (mesaj['target'] == 'Changed') {
          final mutantArgs = mesaj['arguments'];
          gunlukYaz('Changed alindi -- SinyalDegisiklik yayinlaniyor ($mutantArgs)');
