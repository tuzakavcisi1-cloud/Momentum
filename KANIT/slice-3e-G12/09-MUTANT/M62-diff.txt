M62 -- signalr_json_sinyal.dart -- "_cerceveyiAyir" yalniz ilk parcayi dondursun
Hedef ayak: A6 (tek cercevede 0x1E ile ayrilmis UC mesaj -- ucu de islenir, sira korunur)

--- once ---
    if (parcalar.isNotEmpty && parcalar.last.isEmpty) {
      parcalar.removeLast();
    }
    return parcalar;
  }

--- sonra ---
    if (parcalar.isNotEmpty && parcalar.last.isEmpty) {
      parcalar.removeLast();
    }
    return parcalar.take(1).toList();
  }
