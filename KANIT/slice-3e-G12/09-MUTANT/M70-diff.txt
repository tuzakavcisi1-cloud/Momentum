M70 -- signalr_json_sinyal.dart -- durdur()'e eklenen "_denetleyici.close()" cagrisini sil
Hedef ayak: A13 (durdur() sonrasi olaylar akisi KAPANMALI)

--- once ---
    await _kanaliKapat();
    await _denetleyici.close();
  }

--- sonra ---
    await _kanaliKapat();
  }
