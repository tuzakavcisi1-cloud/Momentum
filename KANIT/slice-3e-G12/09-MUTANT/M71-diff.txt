M71 -- gorev_deposu.dart -- Timer.periodic(... cekmeTuruCalistir ...) ekle
Hedef ayak: Y1 (K68 yoklama yasagi -- beyaz liste disi dosyada Timer + govdede cekmeTuruCalistir)

--- once (dosya basi) ---
import 'dart:convert';
import 'dart:math';

--- sonra ---
import 'dart:async';
import 'dart:convert';
import 'dart:math';

// GOREV-slice-3e-G12 M71 MUTANT -- gecici, kapinin KIRMIZI'sini kanitlar,
// hemen geri alinir. src/client/lib altinda signalr_json_sinyal.dart DISINDA
// bir Timer.periodic + govdesinde cekmeTuruCalistir -- K68 ihlali.
void mutantYoklamaSuphesi() {
  Timer.periodic(const Duration(seconds: 5), (_) {
    cekmeTuruCalistir();
  });
}

NOT: bu mutant `flutter analyze`/`flutter test` icin DERLENMEZ (cekmeTuruCalistir
bu kapsamda TANIMSIZ) -- kasitli, cunku M71 YALNIZ statik kapiyi
(`yoklama-yasagi-kapisi.py`) hedefler, flutter araclarini DEGIL.
