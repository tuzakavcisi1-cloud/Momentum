M65 -- signalr_json_sinyal.dart -- "_negotiate"te statusCode != 200 kontrolunu kaldir
Hedef ayak: A9 (negotiate 401 -- SinyalBaglandi yok, WS HIC acilmaz, kanalAcici cagrilmadi)

--- once ---
    if (yanit.statusCode != 200) {
      throw StateError('negotiate basarisiz: HTTP ${yanit.statusCode}');
    }
    final govde = jsonDecode(yanit.body) as Map<String, Object?>;

--- sonra ---
    final govde = jsonDecode(yanit.body) as Map<String, Object?>;

NOT (test kalitesi bulgusu -- mutant matrisi calisirken OLCULDU): A9'un
ILK fixture'i (bos govde, `http.Response('', 401)`) bu mutantı YAKALAMAZDI
-- status kontrolu olmadan da `jsonDecode('')` PATLAR, ayni "baglanti
koptu" sonucuna varilir, mutant SESSIZCE gecerdi (olu tuzak). Test
GUCLENDIRILDI: govde artik GECERLI bir connectionToken tasiyor
(`{"connectionToken":"gecerli-ama-401-govdesi"}`, 401) -- boylece status
kontrolu SILINDIGINDE govde basariyla parse edilip "basarili" sayilir ve
kanalAcici cagrilir, ayak GERCEKTEN kirilir.
