GOREV-W3b -- ORTAM OLCUMU (K80, el: Claude Code, Cowork Desktop Commander olmadan olcemedi)
===========================================================================================
Tarih: cihazdan olculdu (bkz. asagida `Get-Date` cikisi degil -- islem loglarindaki saat damgalari)

--- docker ps (ONCE, T1-T5'ten once) ---
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
(bos -- hicbir konteyner CALISMIYORDU)

--- docker ps -a ---
CONTAINER ID   IMAGE                COMMAND                  CREATED       STATUS                    PORTS     NAMES
e76618f6546c   postgres:17-alpine   "docker-entrypoint.s…"   13 days once  Exited (0) 21 hours once            momentum-postgres
(konteyner VARDI ama DURMUSTU)

--- netstat -ano | findstr :5298 (T1-T5'ten once) ---
(kesin olcum: Get-NetTCPConnection -LocalPort 5298 -> BOS. `findstr :5298` naif deseni
 52989 gibi ALAKASIZ bir portu YANLIS-POZITIF yakaladi -- ORTAM.md'nin findstr uyarisi
 bu turda BIREBIR dogrulandi; kesin olcum icin Get-NetTCPConnection kullanildi.)
PORT SERBESTTI.

--- adb.exe devices (tam yolla) ---
C:\Users\gulci\AppData\Local\Android\Sdk\platform-tools\adb.exe devices
* daemon not running; starting now at tcp:5037
* daemon started successfully
List of devices attached
(cihaz/emulator YOK -- bu gorev icin gerekmiyor, K80 disiplini geregi yine de olculdu)

--- docker start momentum-postgres + yoklama (K80: sabit sleep degil, YOKLANDI) ---
deneme 1: starting
deneme 2: starting
deneme 3: healthy   <- ~6 saniyede HAZIR oldu

--- flutter --version (K86: tam yol, PATHEXT cozulmez) ---
C:\src\flutter\bin\flutter.bat --version
Flutter 3.44.6 - channel stable - https://github.com/flutter/flutter.git
Framework - revision ee80f08bbf (4 hafta once) - 2026-07-08 15:02:06 -0700
Engine - hash d3a3293399556a85388faf8c6f0723a7a5597aa8 (revision 83675ed276) (1 ay once) - 2026-06-30 16:59:03.000Z
Tools - Dart 3.12.2 - DevTools 2.57.0
DURUM.md'nin belirttigi 3.44.6 ile BIREBIR AYNI -- YAMA FARKI YOK (spec §4c'nin
"yama farki olculur ve _BUILD.json'a yazilir" sarti: fark SIFIR, _BUILD.json
flutterSurum="3.44.6" yazdi).

--- verify.ps1 ONCESI hazirlik sirasi (ORTAM.md, oturum 50 dersi) ---
1) docker start momentum-postgres -- healthy yoklandi (yukarida)
2) netstat/Get-NetTCPConnection ile :5298 BOS oldugu dogrulandi
3) verify.ps1 kosuldu (bkz. 01-verify-ps1-ham.txt)

--- Backend/emulator/docker son durum (bu KANIT yazilirken) ---
- momentum-postgres: CALISIYOR (bu gorev sirasinda baslatildi, birakildi -- baska
  oturumlar/testler icin faydali, kapatmak icin ozel bir talimat YOKTU)
- Momentum.Api sureci: KAPALI (port 5298 serbest -- hicbir talimat acik birakmayi
  istemedi, verify.ps1'in bir sonraki kosumunu KILITLEMEMESI icin kapatildi)
- adb/emulator: bu gorev icin hic kullanilmadi, acilan bir sey yok
