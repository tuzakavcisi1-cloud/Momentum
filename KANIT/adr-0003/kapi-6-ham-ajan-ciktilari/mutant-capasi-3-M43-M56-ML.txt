================================================================================
ADR 0003 v6 — 6. TUR BAĞIMSIZ KAPI / MUTANT PAYI-3
KAPSAM: M43…M56 (14 satır) + M-L5…M-L9 (5 satır) = 19 satır
GÖREV: Onur'un K32-b kilidi — ÖLÇÜM ARACININ BEYAN ETTİĞİ EN BÜYÜK SINIRI
       ELLE KAPATMAK. Araç çıpa hücresinde jeton görür; jeton, mutantın kararı
       GERÇEKTEN ısırdığını KANITLAMAZ. Bu rapor her mutantı üç soruyla zorlar.
BELGE: /mnt/user-data/uploads/TO DO LİST/Momentum/docs/ADR/0003-kimlik-cekirdegi.md
       (1123 satır; mutant tablosu 843-920)
DENETÇİ NOTU: Bu oturum belgeyi YAZMADI. Hiçbir kod koşturulmadı (repo bu
       ortamda yok) — hükümler BELGENİN KENDİ METNİ + BELGENİN KENDİ
       ÖLÇÜMLERİ üzerinden kurulmuştur. Koşturulması gereken her kalem
       "ÖLÇÜM KOMUTU" başlığında ADIYLA yazılıdır. Emin olunmayan her kalem
       [ŞÜPHELİ] işaretlidir.
TARİH: 25 Tem 2026
================================================================================

--------------------------------------------------------------------------------
0. HÜKÜM ÖZETİ (ayrıntı aşağıda, mutant bloklarında)
--------------------------------------------------------------------------------

BLOKER (kör kapı — mutasyon uygulansa bile sinyal YEŞİL kalır):
  B6-1  M56 — kill sinyalindeki parola `123456` (6 karakter) `[KS-17]`=15
        uzunluk kuralıyla ZATEN `400` alır ⇒ kara liste kaldırılsa da assert
        yeşil kalır. Belge bunu KENDİ SATIRINDA (satır 216) yazıyor.
  B6-2  M43 ayak (3) — kapı-4'ün ADIYLA "ölmüyor" diye ölçtüğü ayak, v6'da
        hâlâ kill sinyalinin içinde ve kendi mutasyonu YOK. §3.1 satır 939 bu
        ayağı K3-L10'un "başlık yoksa 400" kuralının TEK kapısı ilan ediyor
        ⇒ o kural fiilen kapısız-ve-beyansız (Ma-6/B7 hata sınıfı).
  B6-3  M51 — mutasyonun ikinci şıkkı ("AAD yalnız `family_id` ile kurulur"),
        satır A ile satır B FARKLI ailelerdeyse ISIRMAZ; satırın kendi
        gerekçesi ("başka bir ailenin halefi") testi tam da o kuruluma itiyor.
        "A ve B AYNI ailenin iki satırıdır" önkoşulu PİNLİ DEĞİL.

MAJÖR:
  M6-1  M45 — mutasyon biçimi pinli değil: "INSERT'ü öne al" varyantı AYNI
        transaction + 0-satırda geri alma ile sahipsiz satır BIRAKMAZ ⇒ o
        şıkkın altında sinyal yeşil kalır.
  M6-2  M45 — ayırt edici değil: kill assert'i (paralel iki /refresh, "aile
        satır sayısı tam olarak 1 artmıştır") v6'da M39'un DA değişmezidir.
  M6-3  M46 — ayırt edici değil: M11 (kontrol 1 tamamen kaldırılır) birebir
        aynı gözlemi üretir ("31. /login 429 ALMAZ"). Pozitif kontrol yok.
  M6-4  M47 — ölmeyen ayak: "silme başlıkları çerezin kendi öznitelikleriyle
        birebir aynıdır" ayağı, YALNIZ `__Host-mrt` silme başlığı kaldırılınca
        ölmez (`__Host-mct`'nin öznitelikleri sağlam kalır). M36→M36b emsali
        bu satıra uygulanmamış.
  M6-5  M48 — ayrık (disjonktif) mutasyon: "`ValidateIssuer` **veya**
        `ValidateAudience`". Her ayak yalnız kendi yarısında ölür ⇒ satır
        M4/M36b gibi "İKİ AYRI KOŞUM" diye pinlenmeli ya da bölünmeli.
  M6-6  M49 — ayırt edicilik SIFIR: kill sinyali M32b'nin testinin ta kendisi;
        M32b'nin kendi mutasyonu da aynı testi kırar. Bağımsız gözlem yüzeyi
        eklemiyor.
  M6-7  M52 ayak (1) — "tam `[KS-19]` ⇒ `201`" ayağı, e-posta politikası
        KALDIRILDIĞINDA da yeşil kalır ⇒ kill ayağı değil POZİTİF KONTROL.
        Doğru muamelenin emsali belgede var (M35: "Pozitif kontrol zorunlu").
  M6-8  M52 — [ŞÜPHELİ] ölçülmemiş olgusal iddia: 255 karakterlik bir e-posta
        PostgreSQL btree anahtar sınırını (≈2704 bayt) AŞAMAZ ⇒ "`500` DEĞİL"
        ayağının yazılı gerekçesi karşılıksız.
  M6-9  M53 — mutasyon biçimi pinli değil: CSRF'i AYNI transaction içinde
        sonraya alıp `403`'te geri alan varyant `T1`'i tüketmez ⇒ ikinci istek
        `200` alır ⇒ sinyal yeşil kalır = kör kapı riski.
  M6-10 M53 ayak (1) — "`403` döner" ayağı kendi mutasyonu altında ÖLMEZ
        (doğrulama hâlâ koşuyor, yalnız sonra) ⇒ önkoşuldur, kill ayağı değil.
        Ayrıca M25 sinyalin TAMAMINI kırar ⇒ ayırt edicilik zayıf.
  M6-11 M54 ayak (2) — "salt alanı `[KS-23]`'ün salt uzunluğundadır" ayağı,
        satırın KENDİ adlandırdığı mutasyon (`new byte[16]`) altında ölmez.
  M6-12 M56 ayak (2) — pozitif kontrol, kill ayağı gibi yazılmış (ölmez).
  M6-13 M-L7 ayak (2) — "A yeniden girince kuyruğu duruyor" ayağı, kullanıcı-
        başına-dosya → tek-dosya mutasyonu altında ÖLMEZ; ölçtüğü şey K3-L7'nin
        DİĞER özelliği ("çıkışta silme yoktur").
  M6-14 M-L9 / K3-L8(3) — `[KS-4]` ikinci ve İLGİSİZ bir anlamda kullanılıyor
        (istemci geri çekilme TAVANI). 1-K tablosunda KS-4'ün "kapısı" sütunu
        M1·M29·M30; M-L9 yazılı değil. K28-a KS-4'ü değiştirirse istemcinin
        geri çekilme tavanı SESSİZCE değişir.
  M6-15 M-L devir — beş kalem §7'de ADIYLA yazılı (BUHARLAŞMAMIŞ), ama ADR
        0004 borçlarının aksine DURUM/kabul ölçütü tablosu YOK ve hiçbir cümle
        `slice-3b` spec'ini bu beş mutantı taşımaya BAĞLAMIYOR ⇒ satır 922'nin
        kapattığı asimetrinin kardeşi.

MİNÖR (bloker/majör değil, kayda geçiyor):
  m-1  M44 — gerekçe abartısı: "çıkış fiilen `[KS-4]` gecikir" ve "çalışan bir
       halef döndürür" ifadeleri belgenin kendi dal kurallarıyla yanlışlanır
       (halef de `revoked_at` almıştır). KAPI SAĞLAM, gerekçe abartılı.
  m-2  M47 — önkoşul yazılmamış: testin `X-Client-Kind: web` ile koşması
       gerekir (aksi hâlde çerez hiç doğmaz); ayrıca K3-L3 `Max-Age=0` YANINDA
       "geçmiş `Expires`" de istiyor, kill sinyali onu ölçmüyor.
  m-3  M50 — seviye `B` fizibilitesi: "iki ayrı şifreleme çağrısı" doğrudan
       çağrılabilir bir şifreleme dikişi ister; §2-M yalnız
       `IRefreshTokenService`'i adlandırıyor, metot şekli pinli değil.
  m-4  M55 — "üç etiket tek bir `HkdfLabels` içinde `const string`, başka
       hiçbir yerde literal geçmez" pini ÖLÇÜLMÜYOR (kill sinyali yalnız üç
       anahtarın farklılığını ölçer). Kural doğru, kapısı yok — K3-I4 zaten
       kapılı göründüğü için araç bunu göremez.
  m-5  K3-B6(2) — kara liste "kayıt ile **parola değiştirme** yollarında"
       kontrol edilir diyor; parola değiştirme K3-K3 ile KAPSAM DIŞI (satır
       647) ⇒ var olmayan bir yola kural yazılmış.
  m-6  M-L5 — "eşzamanlı **N** adet 401" — N pinli değil (kanonik sayı da
       değil). K30-b'nin ruhu gereği en azından "N ≥ 2" yazılmalı.
  m-7  M-L9 — "üstel geri çekilme" ve tavanın KENDİSİ ayrı bir gözlem
       yüzeyine sahip değil: dalları birleştiren mutasyon bu ayağı da
       trivial olarak düşürür, ama YANLIŞ TAVANLI doğru bir implementasyon
       M-L9'u GEÇER ⇒ tavan fiilen kapısız.
  m-8  KAPSAM DIŞI AMA KAYDA GEÇİYOR: §6 Risk #17 birebir "rotasyon uç
       noktası … **§7'nin devir listesine yazılmıştır**" diyor; §7'nin
       D-1…D-7 listesinde ROTASYON YOKTUR (`grep "rotasyon"` ⇒ 4 eşleşme,
       hiçbiri §7'de). Bu, kapı-5'in B5-5'te kapattığı kusurun (beyan
       edildiği söylenen ama yazılmamış sınır) BİREBİR TEKRARIDIR — ve bu
       kez ters yönde: kalem §6'ya yazıldı, devir listesine yazılmadı.

TEMİZ ÇIKANLAR: M44 (m-1 notuyla) · M50 (m-3 notuyla) · M55 (m-4 notuyla)

SAYIM: 19 satır → 3 BLOKER · 15 MAJÖR · 3 TEMİZ (majörler 12 satıra dağılıyor;
M45·M52·M53·M56 ikişer majör taşıyor).

================================================================================
1. YÖNTEM — ÜÇ SORU VE "SÜS AYAK" TESTİ
================================================================================

Her satır için:
  S1 ÖLÜYOR MU?    Mutasyon uygulandığında kill sinyali GERÇEKTEN FAIL eder mi?
                   Hayırsa: KÖR KAPI = BLOKER.
  S2 BASELINE?     Mutasyon yokken sinyal GEÇER mi? Hayırsa: ÖLÜ TUZAK = MAJÖR.
  S3 AYIRT EDİCİ?  Başka bir mutasyon da aynı testi kırıyorsa: MAJÖR.

ÇOK AYAKLI SATIRLARDA belgenin KENDİ PAZARLIKSIZ KURALI uygulanır (satır 829-838,
birebir):
   "çok assert'li bir mutant satırında HER ayak, o satırın mutasyonu altında
    ÖLMEK ZORUNDADIR. Ölmeyen ayak iki seçenekten birine gider: (a) kendi
    mutasyonuyla ayrı bir mutant olur; ya da (b) kill sinyalinden çıkarılıp
    ÖNKOŞUL ya da DEĞİŞMEZ diye yeniden adlandırılır. ÜÇÜNCÜ SEÇENEK YOKTUR."
Bu kural bu turda ÖLÇÜT olarak kullanılmıştır: kuralı yazan belge, kuralı kendi
satırlarının hepsinde uygulamamıştır.

================================================================================
2. MUTANT BLOKLARI
================================================================================

--------------------------------------------------------------------------------
M43 — girdi kanalı seçimi (K3-L10)                       [BLOKER + MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 893, birebir):
| **M43** | Sunucu girdi kanalını `X-Client-Kind`'dan değil **kanalın
  varlığından** seçer ("çerez varsa çerezden oku") | **ÜÇ AYAK:**
  (1) "`X-Client-Kind: native` + geçerli `__Host-mrt` çerezi + gövdede token
      **YOK** ⇒ `400`; çerez **kullanılmaz**" **FAIL**;
  (2) "`X-Client-Kind: web` + gövdede geçerli token + çerez **YOK** ⇒ istek
      **başarısızdır**" **FAIL**;
  (3) "`X-Client-Kind` **başlıksız** `/refresh` `400` alır" **FAIL** | TS |

S1 ÖLÜYOR MU?
  Ayak (1): EVET. Baseline: native ⇒ yalnız gövde okunur, gövde boş ⇒ `400`.
    Mutasyon: çerez var ⇒ çerezden okunur; CSRF başlığa (native) koşullu
    olduğu için atlanır ⇒ `200` ⇒ assert (`400`) kırılır. ÖLÜR. ✔
  Ayak (2): HAYIR / EN İYİ İHTİMALLE BELİRSİZ. Mutasyon YALNIZ KANAL SEÇİMİNİ
    değiştiriyor; CSRF zorunluluğu §2-L10(3) gereği `X-Client-Kind == web`'e
    koşullu KALIR (belgenin kendi anlatımı satır 758: mutasyon altında
    "`X-Client-Kind: native` gönderen bir istek CSRF katmanını tümüyle atlar"
    — yani CSRF hâlâ BAŞLIĞA bakıyor). Test "çerez YOK" diyor ⇒ `__Host-mct`
    de yok ⇒ web modunda CSRF doğrulaması BAŞARISIZ ⇒ `403` ⇒ istek
    "başarısızdır" ⇒ assert MUTASYON ALTINDA DA YEŞİL. ÖLMEZ.
    Ek kusur: assert bir durum kodu PİNLEMİYOR ("başarısızdır"); ayak (1)
    `400` pinlerken (2) pinlemiyor — belgenin kendi M14/M24 standardıyla
    (uç adı/boru hattı pinlenir) asimetri.
  Ayak (3): HAYIR. Mutasyon kanal SEÇİMİNİ değiştirir; "başlık yoksa `400`"
    kuralı (K3-L10'un birinci maddesi) mutasyondan ETKİLENMEZ ⇒ başlıksız
    istek mutasyon altında da `400` alır ⇒ assert YEŞİL. ÖLMEZ.
    ⚠ BU AYAK ZATEN ÖLÇÜLMÜŞTÜ: satır 830, birebir — "Kapı-4, dört mutantın
    (`M41`(2) · **`M43`(3)** · `M31`(4) · `M36`'nın 7 assert'inden 5'i) kendi
    mutasyonu altında ÖLMEDİĞİNİ ölçtü". Dördünden ÜÇÜ onarıldı (M41→M41b,
    M31→M52, M36→M36b). **M43(3) onarılmadı ve satırda duruyor.**
    Üstelik §3.1 satır 939 birebir: "K3-L10'un *başlık yoksa `400`* kuralı ve
    native-çerez-yok ayağı | Kapısızdı (Ma-6) | **M43**'ün üçüncü ayağı"
    ⇒ belge o kuralın kapısını ÖLMEYEN bir ayağa bağlamış durumda.

S2 BASELINE YEŞİL Mİ? Üç ayak da baseline'da geçer (native+boş gövde ⇒ 400;
  web+çerezsiz ⇒ CSRF yok ⇒ 403; başlıksız ⇒ 400). ✔ Ölü tuzak YOK.

S3 AYIRT EDİCİ Mİ? Ayak (1) ayırt edicidir (M25/M28/M53 bu gözlemi üretmez).
  Ayak (3) hiçbir şeyi ayırt etmez — ölmediği için.

HÜKÜM: **BLOKER (B6-2)** — ayak (3), belgenin KENDİ pazarlıksız kuralının
  ("üçüncü seçenek yoktur") ihlalidir ve K3-L10'un bir kuralını fiilen
  kapısız-ve-beyansız bırakır. **MAJÖR** — ayak (2) pinli mutasyon biçimi
  altında ölmez ve durum kodu pinlemez.
ÖNERİLEN ONARIM (kilit gerektirir):
  (a) Ayak (3) → **M43b** olur; mutasyonu: "`X-Client-Kind` yoksa `native`
      varsayılır (varsayılana düşme yolu açılır)"; kill: "başlıksız `/refresh`
      `400` alır" FAIL. VEYA ayak (3) kill sinyalinden çıkarılıp "önkoşul"
      diye yeniden adlandırılır.
  (b) Ayak (2)'ye durum kodu pinlenir ve önkoşulu yazılır: "`__Host-mct`
      **geçerli** gönderilir (CSRF ayağı ısırmasın), yalnız `__Host-mrt`
      yoktur ⇒ mutasyonda gövdeden okunur ⇒ `200`; baseline `400`."
      Böylece ayak GERÇEKTEN mutasyonu ölçer.
ÖLÇÜM KOMUTU (belge düzeyi — bu turda KOŞULDU):
  grep -n "M43" 0003-kimlik-cekirdegi.md
  sed -n '829,838p;893p;939p' 0003-kimlik-cekirdegi.md
ÖLÇÜM KOMUTU (kod düzeyi — builder KANIT'a yazacak):
  dotnet test --filter FullyQualifiedName~Refresh_channel_selection  # baseline
  git apply mutants/M43.patch && dotnet test --filter ...            # her ayak AYRI

--------------------------------------------------------------------------------
M44 — dal önceliği (c) ↔ (b) (K3-C6(2))                             [TEMİZ]
--------------------------------------------------------------------------------
ALINTI (satır 894, birebir):
| **M44** | K3-C6(2)'nin dal önceliği bozulur: (c) replay dalı (b)'den **önce**
  değerlendirilir | "`/logout`'tan **sonra**, `T1.consumed_at + [KS-4]`
  **içinde** sunulan tüketilmiş token **`401`** alır (`200` DEĞİL)" **FAIL** | TC |

S1 ÖLÜYOR MU? EVET. Senaryo: /login → /refresh (T1 tüketilir, T2 doğar,
  `successor_secret_enc` dolar) → /logout (aile `revoked_at` alır) → T1 sunulur.
  Baseline: UPDATE 0 satır (consumed_at dolu) ⇒ dal SELECT'i ⇒ (a) hayır ⇒
  (b) `revoked_at IS NOT NULL` ⇒ `401`. Mutasyon: (c) önce bakılır ⇒ pencere
  içinde ✔, halef tüketilmemiş ✔, `successor_secret_enc` dolu ✔ ⇒ `200` ⇒
  assert kırılır. ÖLÜR. ✔
S2 BASELINE YEŞİL Mİ? EVET (`401`). Süpürücü/`FakeTimeProvider` ilerletilmediği
  için (c) koşulları zaten sağlanır, ama (b) önce kazanır. ✔
S3 AYIRT EDİCİ Mİ? EVET. M26 (yüklemden `revoked_at IS NULL` çıkarılır) bu
  testi KIRMAZ: T1 zaten `consumed_at` dolu olduğu için UPDATE yine 0 satır
  döner ve dal SELECT'i (b)'yi bulur ⇒ `401` ⇒ yeşil. M29/M30 (pencere
  koşulları) da (b)'nin önceliğini değiştirmez ⇒ yeşil. M18 tek aileyi
  etkilemez. Belgenin "M26 bunu görmez" iddiası DOĞRULANDI. ✔

HÜKÜM: **TEMİZ.**
MİNÖR (m-1): satır 315 birebir "builder (c)'yi önce ayrıştırırsa `/logout`'tan
  sonraki `[KS-4]` boyunca `T1` hâlâ `200` ve **çalışan bir halef** döndürür ⇒
  çıkış fiilen `[KS-4]` gecikir." Bu YANLIŞTIR: `/logout` AİLENİN TAMAMINI
  iptal eder ⇒ döndürülen halef `T2` de `revoked_at` taşır ⇒ `T2` ile yapılacak
  `/refresh` dal (b)'den `401` alır. Gerçek maruziyet "çıkışın gecikmesi" değil,
  "`200` gövdesiyle ÖLÜ bir token teslim edilmesi"dir (bilgi sızıntısı +
  istemci durum makinesinin bozulması). KAPI SAĞLAM; GEREKÇE ABARTILI ve
  belgenin kendi dal kurallarıyla yanlışlanıyor.
ÖLÇÜM KOMUTU:
  sed -n '305,323p;894p' 0003-kimlik-cekirdegi.md
  # kod: TC testi, FakeTimeProvider ileri ALINMAZ; psql ile
  #   SELECT token_hash, consumed_at, revoked_at FROM refresh_tokens WHERE family_id=…;

--------------------------------------------------------------------------------
M45 — halef INSERT'ünün işlem sınırı (K3-C6(1))              [MAJÖR ×2]
--------------------------------------------------------------------------------
ALINTI (satır 895, birebir):
| **M45** | Halef `INSERT`'ü tüketim `UPDATE`'inden **önceye** alınır (veya ayrı
  transaction'a çıkarılır) | **gerçek Postgres'te paralel** iki `/refresh` aynı
  `T1` ile: "ailenin satır sayısı **tam olarak 1** artmıştır" **FAIL**
  *(kaybeden istek sahipsiz bir satır bırakırsa 2 artar)* | TC |

S1 ÖLÜYOR MU? ŞARTA BAĞLI — **mutasyon biçimi pinli değil.**
  Şık A ("ayrı transaction'a çıkarılır"): kaybeden isteğin INSERT'ü commit
    edilir, UPDATE 0 satır döner ⇒ sahipsiz satır kalır ⇒ +2 ⇒ ÖLÜR. ✔
  Şık B ("`INSERT` `UPDATE`'ten ÖNCEYE alınır", AYNI transaction): §2-C6
    kararı zaten "TEK transaction" diyor (madde 1). Sıra bozulur ama UPDATE 0
    satır dönünce en doğal kod yolu işlemi geri alır/boş kapatır (kararın
    kendi 2. maddesi bunu tarif ediyor) ⇒ INSERT edilen satır GERİ ALINIR ⇒
    aile +1 ⇒ **assert YEŞİL ⇒ MUTANT HAYATTA KALIR.**
  ⇒ Satırın adlandırdığı iki şıktan BİRİ ısırmıyor. Belge bu hata sınıfını
  başka yerde ADIYLA kapatmıştı: M40 "MUTASYON BİÇİMİ PİNLİ", M32 "yalnız
  `using` eklemek yetmez". Aynı disiplin buraya uygulanmamış.
S2 BASELINE YEŞİL Mİ? EVET (başarılı /refresh aileyi +1 yapar). ✔
S3 AYIRT EDİCİ Mİ? **HAYIR.** M39'un v6 kill sinyali (satır 882) birebir:
  "**her iki durumda da** *ailenin satır sayısı **tam olarak 1** artmıştır*
  **FAIL**" — AYNI TEST (gerçek Postgres'te paralel iki /refresh, aynı T1),
  AYNI DEĞİŞMEZ. M39'un mutasyonu (check-then-act) altında iki istek de
  UPDATE+INSERT yapar ⇒ aile +2 ⇒ M45'in assert'i de kırılır.
  ⇒ M39 ile M45 bu testle AYIRT EDİLEMEZ. (İroni: v6, M39'un ayırt edici
  olmayan "tam olarak biri 200" sinyalini KALDIRIP yerine tam da M45'in
  değişmezini koydu ⇒ bir kör kapı kapatılırken iki mutant çakıştırıldı.)

HÜKÜM: **MAJÖR ×2** (M6-1 mutasyon biçimi · M6-2 ayırt edicilik).
ÖNERİLEN ONARIM: (a) mutasyon biçimi pinlenir: "halef `INSERT`'ü **ayrı bir
  transaction'da** koşar ve **commit edilir**" (tek gözlemlenebilir biçim);
  (b) M45'e M39'un üretemeyeceği bir ayak eklenir — ör. "kaybeden istek `401`
  alır **ve** `replaced_by_id` ile işaret edilen satır **VARDIR**" (M39'da
  kaybeden istek `200`/`reuse` alır, M45'te `401` alır ve ORTADA SAHİPSİZ BİR
  SATIR bırakır) — yani ayrım "sahipsiz satırın `replaced_by_id`'sinin hiçbir
  satırdan işaret EDİLMEMESİ"dir.
ÖLÇÜM KOMUTU:
  sed -n '294,303p;882p;895p' 0003-kimlik-cekirdegi.md
  # kod: TC, Testcontainers; iki paralel HttpClient; sonra:
  #   SELECT count(*) FROM refresh_tokens WHERE family_id = @fid;
  #   SELECT id FROM refresh_tokens r WHERE NOT EXISTS
  #     (SELECT 1 FROM refresh_tokens p WHERE p.replaced_by_id = r.id) AND …;

--------------------------------------------------------------------------------
M46 — UseRateLimiter, UseRouting'den ÖNCEYE alınır (K3-L4/K3-J1)   [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 896, birebir):
| **M46** | **`UseRateLimiter`, `UseRouting`'den ÖNCEYE alınır** | **ÖNKOŞUL:**
  her istek FARKLI rastgele e-posta. "`[KS-10]`'un tavanını aşan `/login`
  isteği `429` **ALIR**" **FAIL** — çünkü uç-bazlı politikalar
  (`RequireRateLimiting`) **çözülemez** ⇒ **hiçbir sınır uygulanmaz** | TS |

S1 ÖLÜYOR MU? EVET. Belgenin birincil-kaynak ölçümü (satır 516, aspnetcore
  `release/9.0`) birebir: "If this endpoint has no EnableRateLimitingAttribute
  & there's no global limiter, don't apply any rate limits." +
  `if (enableRateLimitingAttribute is null && _globalLimiter is null)
  { return _next(context); }`. `UseRouting`'den önce endpoint `null`'dır ⇒
  metadata yok ⇒ hiçbir sınır uygulanmaz ⇒ `429` hiç doğmaz ⇒ assert kırılır.
  ÖLÇÜLMÜŞ ve GÖZLEMLENEBİLİR. ✔ (v4'ün kör mutasyonu gerçekten onarılmış.)
S2 BASELINE YEŞİL Mİ? EVET, ama önkoşul yükü ağır: `[KS-10]`=30/5dk ⇒ 31 istek;
  her istek sahte Argon2 (`[KS-24]`≈270 ms) yakar ⇒ ≈8-9 sn; §3.2(9.1) gereği
  sınıf KENDİ `WebApplicationFactory`'sini kurar. Kontrol 2 (farklı e-posta) ve
  kontrol 3 (ardışık istek) tetiklenmez. ✔
S3 AYIRT EDİCİ Mİ? **HAYIR.** M11'in mutasyonu ("Hız sınırlayıcı (kontrol 1)
  tamamen kaldırılır") ile M46'nın mutasyonu AYNI gözlemi üretir: `[KS-10]`
  tavanını aşan istek `429` ALMAZ. M46'nın kill sinyalinde M11'i eleyecek
  HİÇBİR ayak yok (M11'inkinde en azından `limit == "ip"` ve `Retry-After`
  var; M46'nınkinde ikisi de yok). Test kırıldığında "kontrol 1 mi silindi,
  sıra mı bozuldu" sorusunun cevabı YOKTUR.
  Ek: M23 de (IP partition → e-posta) rastgele e-postalarla aynı assert'i
  kırar ⇒ ÜÇ mutant tek gözleme yığılmış.

HÜKÜM: **MAJÖR (M6-3)** — ayırt edicilik yok.
ÖNERİLEN ONARIM: M46'ya **pozitif kontrol** eklenir; M11 altında YEŞİL kalıp
  M46 altında ÖLEN bir ayak gerekir. Doğal aday: sıra bozulunca **ÜÇ politika
  birden** kaybolur, M11 ise yalnız 1a'yı siler ⇒ ayak:
  "`[KS-12]`'nin tavanını aşan `/logout` isteği `429` **ALIR**" **FAIL**
  (M11 altında bu ayak yeşil kalır ⇒ ayrım doğar). İkinci aday: mutasyonda
  `RequireRateLimiting` metadata'sı çözülemediği için `limit` uzantısı HİÇ
  doğmaz; baseline'da `problem.Extensions["limit"] == "ip"` gelir.
ÖLÇÜM KOMUTU:
  sed -n '504,520p;896p' 0003-kimlik-cekirdegi.md
  # kod: TS; 31 istek; assert 429 + limit=="ip" + Retry-After;
  #      AYRICA /logout politikasının ayrı ısırdığı ölçülür.

--------------------------------------------------------------------------------
M47 — /logout'ta __Host-mrt silme başlığı (K3-L3 / Ma-12)          [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 897, birebir):
| **M47** | `/logout` yanıtından **`__Host-mrt` silme başlığı** kaldırılır |
  "`/logout` yanıtı **`__Host-mrt` ve `__Host-mct`'nin İKİSİNİ birden**
  `Max-Age=0` ile siler ve silme başlıkları çerezin **kendi öznitelikleriyle
  birebir aynıdır**" **FAIL** | TS |

S1 ÖLÜYOR MU? AYAK AYRIK OKUNMALIDIR:
  Ayak A ("İKİSİNİ birden siler"): EVET, ölür — `__Host-mrt` silme başlığı yok
    ⇒ assert kırılır. ✔
  Ayak B ("silme başlıkları çerezin kendi öznitelikleriyle birebir aynıdır"):
    **ÖLMEZ.** Mutasyon yalnız `__Host-mrt`'nin silme başlığını KALDIRIR;
    `__Host-mct`'nin silme başlığı ve öznitelikleri (Path=/, Secure,
    SameSite=Strict, `__Host-` öneki) DEĞİŞMEZ ⇒ B assert'i mutasyon altında
    da YEŞİL. Süs ayak.
    ⇒ Bu, kapı-4 majör #9'un M36 için ölçtüğü hata sınıfının BİREBİR
    tekrarıdır (M36'nın beş özniteliği ölmediği için M36b'ye taşınmıştı).
    Emsal belgenin İÇİNDE ve bu satıra uygulanmamış.
S2 BASELINE YEŞİL Mİ? EVET (K3-L3'ün "ÇIKIŞTA HER İKİ ÇEREZ DE SİLİNİR"
  kararı), ANCAK önkoşul yazılmamış: test `X-Client-Kind: web` ile koşmalıdır
  (native'de çerez hiç doğmaz — K3-L10 kural 2). Ayrıca K3-L3 satır 690
  `Max-Age=0` **ve geçmiş tarihli `Expires`** istiyor; kill sinyali `Expires`
  ayağını hiç ölçmüyor ⇒ o alt-özellik kapısız (m-2).
S3 AYIRT EDİCİ Mİ? Ayak A için EVET (M18/M26/M25 bu başlığı üretmez/silmez).

HÜKÜM: **MAJÖR (M6-4)** — ölmeyen öznitelik ayağı.
ÖNERİLEN ONARIM: Ayak B, satırdan çıkarılıp ya **M47b** olur (mutasyon: silme
  başlığı `Path`/`Secure`/`SameSite` olmadan yazılır — tarayıcı silmeyi farklı
  çerez sanar) ya da "**değişmez**" diye yeniden adlandırılır. Ayrıca `Expires`
  ayağı ya kill sinyaline girer ya §3.1'e beyan edilir.
ÖLÇÜM KOMUTU:
  sed -n '689,691p;897p' 0003-kimlik-cekirdegi.md
  # kod: TS; response.Headers.GetValues("Set-Cookie") tam metin karşılaştırması

--------------------------------------------------------------------------------
M48 — ValidateIssuer / ValidateAudience (K3-C7 / Ma-7)             [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 898, birebir):
| **M48** | `ValidateIssuer` **veya** `ValidateAudience` `false` yapılır |
  "**yanlış `iss`** taşıyan (aksi hâlde geçerli, doğru anahtarla imzalı) token
  korumalı uçta **`401`** alır" **FAIL** · "**yanlış `aud`** için aynı" **FAIL**
  | TS |

S1 ÖLÜYOR MU? HER AYAK YALNIZ KENDİ YARISINDA ÖLÜR:
  `ValidateIssuer=false` ⇒ `iss` ayağı ölür ✔, `aud` ayağı YEŞİL kalır ✘.
  `ValidateAudience=false` ⇒ tam tersi.
  ⇒ Satır tek bir mutasyon gibi yazılmış ama fiilen İKİ mutasyondur; çoklu-
  assert kuralı ("HER ayak, O SATIRIN mutasyonu altında ölmek zorundadır")
  ihlal ediliyor.
  ⚠ Belge bu biçimi başka yerde DOĞRU yazıyor: M4 birebir "**İKİ AYRI
  COMMIT'TE koşulur [Ma-16]**", M36b birebir "**her biri AYRI commit**".
  M48'de o cümle YOK ⇒ belgenin kendi standardıyla asimetri.
S2 BASELINE YEŞİL Mİ? EVET, ama testin yanlış `iss`/`aud` taşıyan **doğru
  imzalı** token üretebilmesi gerekir; bu ancak §3.2(10)'un `TEST_MASTER_KEY`
  sabitinden K3-I4'ün HKDF'iyle `K_jwt` türetilerek yapılabilir. Satır bunu
  yazmıyor (M24'te "gerçek `AddJwtBearer` boru hattı" pinlenmişken burada
  imzalama anahtarının nereden geleceği pinsiz) ⇒ inşa edilebilir ama PİNSİZ.
S3 AYIRT EDİCİ Mİ? EVET. M16 (ClockSkew/HS512) bu assert'leri kırmaz; M42b
  (info etiketi) altında token HİÇ doğrulanmaz ⇒ yine `401` gelir ⇒ assert
  YEŞİL kalır (yanlış-pozitif üretmez). ✔

HÜKÜM: **MAJÖR (M6-5)** — ayrık mutasyon, ölmeyen ayak.
ÖNERİLEN ONARIM: satıra M4'ün cümlesi eklenir — "**İKİ AYRI KOŞUM:**
  (1) `ValidateIssuer=false` ⇒ `iss` ayağı FAIL; (2) `ValidateAudience=false`
  ⇒ `aud` ayağı FAIL" — ya da M48/M48b olarak bölünür. Ayrıca test imzalama
  anahtarının `TEST_MASTER_KEY`→HKDF yolundan geldiği §3.2'ye yazılır.
ÖLÇÜM KOMUTU:
  sed -n '846p;879p;898p;934p' 0003-kimlik-cekirdegi.md   # M4 · M36b · M48 · §3.1
  # kod: TS; JwtSecurityTokenHandler ile iss/aud değiştirilmiş token üretilir

--------------------------------------------------------------------------------
M49 — ProblemDetails çeviriminin katmanı (K18-a / §2-M)            [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 901, birebir):
| **M49** | `ProblemDetails` çevirimi **Api'den Application'a taşınır**
  (Application handler'ı `RateLimitedException` yerine doğrudan
  `ProblemDetails` döndürür) | "`M32b`'nin NetArchTest kuralı
  (`Microsoft.AspNetCore.*` ⊥ `Momentum.Application`) **YEŞİL kalır**" **FAIL**
  — kural kırmızıya döner | NA |

S1 ÖLÜYOR MU? EVET. `ProblemDetails` birebir `namespace Microsoft.AspNetCore.Mvc`
  (belgenin satır 790'daki ölçümü) ⇒ Application'da gerçek tip kullanımı doğar
  ⇒ NetArchTest kuralı kırmızıya döner ⇒ assert kırılır. ✔
S2 BASELINE YEŞİL Mİ? EVET (kural bugün yeşil). ✔
S3 AYIRT EDİCİ Mİ? **HAYIR — SIFIR.** M49'un kill sinyali M32b'nin TESTİDİR.
  M32b'nin kendi mutasyonu ("Application'da `HttpContext` alanı") AYNI testi
  kırar. İki mutant arasındaki tek fark, kuralı ihlal eden TİPİN adıdır
  (`HttpContext` ↔ `ProblemDetails`) — ikisi de AYNI namespace ailesinden.
  Bir kuralı iki kez ihlal etmek, ikinci bir kapı ÜRETMEZ.
  Belge bunu erdem gibi yazıyor ("Bu mutant başka bir kapının ısırdığını
  ısırtır") ama bu, S3 ölçütünün TANIMI gereği ayırt edicilik yokluğudur.
  Kararın (K18-a: "kontrol 2/3 Application'da KALIR, çevirim Api'de") GERÇEK
  gözlemlenebilir sonucu — `RateLimitedException`'ın Application'da
  fırlatılması ve `429` gövdesinin YALNIZ Api'deki `RateLimitProblemFactory`
  tarafından üretilmesi — hiçbir mutantla ölçülmüyor.

HÜKÜM: **MAJÖR (M6-6)**.
ÖNERİLEN ONARIM: M49 ya (a) M32b'nin altına "**emsal/değişmez**" notu olarak
  indirilir (kapı sayılmaz), ya da (b) kendi gözlem yüzeyine kavuşturulur:
  seviye `TS` bir ayak — "kontrol 2 reddinde fırlatılan istisna tipi
  `Momentum.Application.RateLimitedException`'dır **ve** `429` gövdesi
  middleware ayağının gövdesiyle **şekilce aynıdır** (`limit` uzantısı hariç)"
  ⇒ bu ayak M32b altında YEŞİL kalır, M49 altında ölür ⇒ ayrım doğar.
ÖLÇÜM KOMUTU:
  sed -n '786,802p;886p;901p' 0003-kimlik-cekirdegi.md
  # kod: NA (NetArchTest) + TS (istisna tipi ve gövde şekli)

--------------------------------------------------------------------------------
M50 — AES-256-GCM nonce sabitlenir (K3-I5(1))                      [TEMİZ]
--------------------------------------------------------------------------------
ALINTI (satır 902, birebir):
| **M50** | AES-256-GCM nonce'u **sabitlenir** (sıfır dizisi ya da `family_id`
  türevi) | "**aynı** düz metin ve **aynı** `family_id`/`token_hash` için iki
  ayrı şifreleme çağrısı **FARKLI** `successor_secret_enc` üretir" **FAIL** —
  sabit nonce'ta çıktılar **birebir aynı** olur | B |

S1 ÖLÜYOR MU? EVET. Aynı anahtar + aynı nonce + aynı AAD + aynı düz metin ⇒
  AES-GCM deterministiktir ⇒ ciphertext ve tag birebir aynı ⇒ "FARKLI" assert'i
  kırılır. Mutasyonun her iki şıkkı da (sıfır dizisi VE `family_id` türevi)
  aynı gözlemi üretir — çünkü test AYNI `family_id`'yi kullanıyor. ✔
  (Not: mutasyon disjonktif ama İKİ ŞIK DA ISIRIYOR ⇒ M45/M48'in kusuru
  burada YOK. Bu, satırın doğru yazıldığının kanıtı.)
S2 BASELINE YEŞİL Mİ? EVET (taze CSPRNG nonce ⇒ 2^-96 dışında farklı). ✔
S3 AYIRT EDİCİ Mİ? EVET. M51 (AAD kaldırılır) altında nonce hâlâ tazedir ⇒
  çıktılar farklı ⇒ M50'nin assert'i YEŞİL kalır. M42b/M55 (anahtar değişir)
  altında da çıktılar farklıdır ⇒ yeşil. ✔

HÜKÜM: **TEMİZ.**
MİNÖR (m-3): seviye `B` fizibilitesi bir dikişe bağlı — "iki ayrı şifreleme
  çağrısı" ancak doğrudan çağrılabilir bir şifreleme metodu varsa yazılabilir.
  §2-M yalnız "`IRefreshTokenService` — AES-256-GCM" diyor; metot şekli
  (ör. `byte[] SealSuccessor(byte[] raw, Guid familyId, byte[] tokenHash)`)
  PİNLİ DEĞİL. M40'ın "MUTASYON BİÇİMİ PİNLİ" disiplini burada gözlem yüzeyi
  için de gerekli — aksi hâlde builder şifrelemeyi private bir yardımcıya
  gömer ve `B` seviyesi `TC`'ye kayar.
ÖLÇÜM KOMUTU:
  sed -n '444,458p;902p' 0003-kimlik-cekirdegi.md
  # kod: B; aynı girdiyle iki çağrı, Assert.NotEqual(blob1, blob2)

--------------------------------------------------------------------------------
M51 — AAD kaldırılır / yalnız family_id (K3-I5(2))                [BLOKER]
--------------------------------------------------------------------------------
ALINTI (satır 903, birebir):
| **M51** | AAD kaldırılır (`associatedData` hiç geçilmez) **veya** yalnız
  `family_id` ile kurulur | "**satır A**'nın `successor_secret_enc` değeri
  **satır B**'ye kopyalandığında çözme **BAŞARISIZ** olur
  (`AuthenticationTagMismatchException`) ve istek K3-C6'nın **yeniden-kullanım
  dalına** düşer" **FAIL** | TC |

S1 ÖLÜYOR MU? ŞIK ŞIK AYRILMALIDIR — ve İKİNCİ ŞIK ISIRMIYOR:
  Şık A (AAD hiç geçilmez): A'nın bloğu B'de çözülür ⇒ istek dal (c)'den `200`
    alır ⇒ "çözme BAŞARISIZ" assert'i kırılır ⇒ ÖLÜR. ✔
  Şık B (AAD **yalnız `family_id`**):
    - Test A ve B'yi **FARKLI ailelerden** seçerse: `family_id` farklı ⇒ AAD
      farklı ⇒ çözme YİNE BAŞARISIZ ⇒ assert **YEŞİL** ⇒ **MUTANT HAYATTA
      KALIR = KÖR KAPI.**
    - Test A ve B'yi **AYNI aileden** seçerse: `family_id` aynı ⇒ AAD aynı ⇒
      çözme başarılı ⇒ ÖLÜR. ✔
  ⇒ Hangi kurulumun geçerli olduğu satırda YAZILI DEĞİL. Üstelik satırın KENDİ
  GEREKÇESİ testi YANLIŞ kuruluma itiyor; birebir: "AAD olmadan şifreli blok
  **satırdan satıra taşınabilir** ⇒ K15-a'nın replay-idempotency'si **başka
  bir ailenin halefini** döndürebilirdi." (Aynı cümle §2-I5 satır 453'te de
  var.) "Başka bir aile" okuyan builder testi FARKLI ailelerle kurar ⇒ şık B
  ölçülemez.
  ⚠ Bu, M30'un satırındaki pinlemenin ("PİN: pencere dışına çıkılırsa M29 ile
  ayırt edilemez hâle gelir ⇒ *pencere İÇİNDE* koşulu testin önkoşuludur")
  birebir kardeşidir: orada önkoşul yazılmış, burada YAZILMAMIŞ.
S2 BASELINE YEŞİL Mİ? EVET — baseline'da AAD = `family_id ‖ token_hash`;
  aynı ailedeki iki satırın `token_hash`'i farklı olduğu için kopyalanan blok
  yine çözülemez ⇒ `AuthenticationTagMismatchException` ⇒ K3-I5(3) gereği
  fail-closed ⇒ reuse dalı ⇒ `401`. ✔ (AYNI aile önkoşulu baseline'ı BOZMAZ —
  yani onarımın bedeli sıfırdır.)
S3 AYIRT EDİCİ Mİ? EVET (aynı aile kurulumunda): M50 altında AAD sağlamdır ⇒
  kopyalanan blok çözülmez ⇒ M51'in assert'i yeşil kalır. M40/M40b altında
  `successor_secret_enc` `NULL`'dır ⇒ farklı dal. ✔

HÜKÜM: **BLOKER (B6-3)** — mutasyonun adlandırılmış ikinci şıkkı, satırın kendi
  gerekçesinin dayattığı kurulumda ISIRMIYOR.
ÖNERİLEN ONARIM (tek cümle, bedeli sıfır): kill sinyaline önkoşul pinlenir —
  "**ÖNKOŞUL:** satır A ve satır B **AYNI ailenin** iki ayrı satırıdır
  (`family_id` aynı, `token_hash` farklı)". Bu kurulumda İKİ ŞIK DA ölür ve
  baseline yeşil kalır. İstenirse ikinci ayak: "**FARKLI ailenin** satırına
  kopyalandığında da çözme başarısızdır" — ama bu ayak şık B altında ölmez ⇒
  "**değişmez**" diye etiketlenmelidir (çoklu-assert kuralı).
ÖLÇÜM KOMUTU:
  sed -n '444,460p;871p;903p' 0003-kimlik-cekirdegi.md
  # kod: TC; psql ile blok kopyalama:
  #   UPDATE refresh_tokens SET successor_secret_enc =
  #     (SELECT successor_secret_enc FROM refresh_tokens WHERE id=@A)
  #   WHERE id=@B;   -- @A ve @B AYNI family_id'de olmalı

--------------------------------------------------------------------------------
M52 — e-posta girdi politikası (K3-B6 / B-6)                    [MAJÖR ×2]
--------------------------------------------------------------------------------
ALINTI (satır 904, birebir):
| **M52** | **E-posta** girdi politikası kaldırılır (uzunluk **ve** format
  doğrulaması) | **ÜÇ AYAK:** "tam `[KS-19]` uzunluğunda geçerli e-posta
  **`201`**" · "`[KS-19]`'dan **bir fazla** karakterlik e-posta **`400`**
  (`500` DEĞİL)" · "gösterim-adlı e-posta (`Ad <a@x.com>` biçimi) **`400`**"
  — herhangi biri **FAIL** | TS |

S1 ÖLÜYOR MU?
  Ayak (1) "tam `[KS-19]` ⇒ `201`": **ÖLMEZ.** Politika KALDIRILDIĞINDA da
    254 karakterlik geçerli e-posta kabul edilir ⇒ `201` ⇒ assert YEŞİL.
    Bu bir kill ayağı değil, sınırın ALT tarafını pinleyen POZİTİF KONTROLDÜR.
    Belgede doğru muamelenin emsali VAR — M35 birebir: "**Pozitif kontrol
    zorunlu:** doğru imzalı + doğru aileli değer **kabul edilir**" — orada
    pozitif kontrol kill ayaklarından AYRI yazılmış. M52'de yazılmamış.
  Ayak (2) "`[KS-19]`+1 ⇒ `400`": EVET ölür (mutasyonda `201` ya da `500`). ✔
  Ayak (3) "gösterim-adlı ⇒ `400`": EVET ölür (format doğrulaması yok ⇒
    `MailAddress` gösterim-adlı biçimi kabul eder ⇒ `201`). ✔
  (Mutasyon "uzunluk **ve** format" birlikte kaldırıldığı için M48'deki ayrık-
  mutasyon kusuru burada YOK.)
S2 BASELINE YEŞİL Mİ? EVET, üç ayak da. ✔
S3 AYIRT EDİCİ Mİ? EVET — B-6 ayrımından sonra M31 (parola) bu ayakları
  kırmaz; M21 (Trim/NFC) gösterim-adlı e-postayı yine reddeder. ✔

HÜKÜM: **MAJÖR (M6-7)** — ölmeyen ayak kill sinyaline yazılmış.
İKİNCİ BULGU — **MAJÖR (M6-8) [ŞÜPHELİ — birincil kaynaktan ölçmedim]:**
  Ayak (2)'nin "(`500` DEĞİL)" parantezinin gerekçesi satır 904 ve 221'de
  birebir: "`email_normalized` btree index'inin anahtar boyutu aşılırsa
  Postgres hata verir". **Bu olgusal olarak tutmuyor:** PostgreSQL'de btree
  giriş boyutu sınırı sayfa boyutunun ≈1/3'üdür (8 KB sayfada ≈2704 bayt);
  255 karakterlik bir e-posta (≈255-1020 bayt, UTF-8) bu sınırı AŞAMAZ.
  ⇒ Ayağın SAVUNMACI DEĞERİ durur (`500` gelmemeli), ama YAZILI GEREKÇESİ
  ölçülmemiş ve büyük olasılıkla yanlıştır. Belgenin kendi §4 doktrini
  ("ölçülmemişi ölçülmemiş diye yaz") gereği ya ÖLÇÜLMELİ ya `[DOĞRULANMADI]`
  işaretlenmelidir. **Bunu bu turda ÖLÇMEDİM** — Postgres yok; hüküm
  literatür bilgisine dayanıyor, o yüzden [ŞÜPHELİ].
ÖNERİLEN ONARIM: (a) ayak (1) "**pozitif kontrol**" diye yeniden adlandırılır;
  (b) `500` gerekçesi ölçülür (`SELECT` ile 255 karakterlik e-posta INSERT
  denemesi) ya da `[DOĞRULANMADI]` yazılır.
ÖLÇÜM KOMUTU:
  sed -n '214,231p;877p;904p' 0003-kimlik-cekirdegi.md
  # gerekçe ölçümü (Postgres 17):
  #   CREATE TABLE t(e text); CREATE INDEX ON t(e);
  #   INSERT INTO t VALUES (repeat('a',243)||'@example.com');   -- 255 karakter
  #   -- hata bekleniyorsa GÖSTER; gelmiyorsa gerekçe DÜŞER

--------------------------------------------------------------------------------
M53 — /refresh'te CSRF'in sırası (K3-J3)                        [MAJÖR ×2]
--------------------------------------------------------------------------------
ALINTI (satır 905, birebir):
| **M53** | `/refresh`'te CSRF doğrulaması tüketim `UPDATE`'inden **sonraya**
  alınır | "geçersiz CSRF token'ı ile yapılan `/refresh` `403` döner **ve AYNI
  yenileme token'ı ile, `FakeTimeProvider` `[KS-4]`'ü AŞACAK KADAR
  ilerletildikten sonra yapılan geçerli-CSRF'li `/refresh` `200` döner**"
  **FAIL** — mutasyonda ikinci istek `401` + `reuse_detected` alır ve **aile
  düşer** | TC |

S1 ÖLÜYOR MU?
  Ayak (1) "`403` döner": **ÖLMEZ.** Mutasyon doğrulamayı KALDIRMIYOR, yalnız
    SONRAYA alıyor ⇒ geçersiz CSRF yine `403` üretir ⇒ assert YEŞİL. Bu bir
    ÖNKOŞUL/kurulum adımıdır, kill ayağı değil. (Çoklu-assert kuralı ihlali.)
  Ayak (2) "ikinci istek `200`": ŞARTA BAĞLI — **mutasyon biçimi pinli değil.**
    Şık A (tüketim COMMIT edilir, sonra CSRF bakılır, `403` döner): `T1`
      tüketilmiştir ⇒ `[KS-4]` aşıldıktan sonra ikinci istek dal (d)'ye düşer
      ⇒ `401` + `reuse_detected` ⇒ assert kırılır ⇒ ÖLÜR. ✔
    Şık B (CSRF, tüketim `UPDATE`'iyle **AYNI transaction** içinde sonraya
      alınır ve `403` yolunda işlem GERİ ALINIR): `T1` tüketilmemiş kalır ⇒
      ikinci istek `200` ⇒ assert **YEŞİL** ⇒ **MUTANT HAYATTA KALIR.**
    §2-C6(1) "tüketim ve halef doğumu TEK transaction içindedir" diye zaten
    pinliyor ⇒ şık B, mutasyonun EN DOĞAL biçimidir. Belge M40'ta mutasyon
    biçimini tam da bu sebeple pinlemişti; burada pinlememiş.
S2 BASELINE YEŞİL Mİ? EVET. Birinci istek `403` (yazma yok) ⇒ `T1` bakir;
  saat `[KS-4]` aşacak kadar ileri alınır (bu `T1`'i etkilemez: `expires_at`
  `[KS-2]`=30 gün); ikinci istek `200`. CSRF çerezinin `Max-Age`'i `[KS-29]`
  (ailenin kalan ömrü) ⇒ 10 dk sonra hâlâ geçerli. ✔
  (v5'in kör kapısı — "ikinci istek `[KS-4]` İÇİNDE" — gerçekten onarılmış;
  bu onarım DOĞRUDUR ve teyit ediliyor.)
S3 AYIRT EDİCİ Mİ? **ZAYIF.** M25 (CSRF doğrulaması TAMAMEN kaldırılır)
  altında: birinci istek `403` ALMAZ (`200` alır, `T1` tüketilir) ⇒ ayak (1)
  kırılır; ikinci istek `[KS-4]` dışında `401`+`reuse` alır ⇒ ayak (2) da
  kırılır ⇒ **M25 sinyalin TAMAMINI kırar.** Testin hangi mutasyonu yakaladığı,
  ancak "hangi assert düştü" bilgisi KANIT'a yazılırsa ayrılabilir — ki ayak
  (1) M53 altında AYAKTA kalıp M25 altında düştüğü için ayrım fiilen VARDIR,
  ama satır bunu ayırt edici bir ÖLÇÜT olarak yazmıyor.

HÜKÜM: **MAJÖR ×2** (M6-9 mutasyon biçimi · M6-10 ölmeyen ayak + zayıf ayrım).
ÖNERİLEN ONARIM: (a) mutasyon biçimi pinlenir — "tüketim `UPDATE`'i **COMMIT
  EDİLİR**, CSRF doğrulaması commit'ten SONRA koşar" (tek gözlemlenebilir
  biçim); (b) ayak (1) "**önkoşul**" diye yeniden adlandırılır; (c) M25'ten
  ayrımı satıra ÖLÇÜT olarak yazılır: "M53 altında önkoşul ayağı YEŞİL kalır,
  M25 altında KIRILIR — KANIT ayak-ayak yazılır."
ÖLÇÜM KOMUTU:
  sed -n '594,601p;866p;905p' 0003-kimlik-cekirdegi.md
  # kod: TC; iki istek arasında FakeTimeProvider += [KS-4]+1dk;
  #   psql: SELECT consumed_at, revoked_at, revoked_reason FROM refresh_tokens …

--------------------------------------------------------------------------------
M54 — parola salt'ı sabitlenir (K3-B1(2) / K28-d(a))               [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 906, birebir):
| **M54** | Parola hash'inde salt **SABİTLENİR** (global sabit / `new byte[16]`)
  | "AYNI parola iki kez hash'lendiğinde üretilen iki PHC dizesi **FARKLIDIR**
  ve salt alanı `[KS-23]`'ün salt uzunluğundadır" **FAIL** | B |

S1 ÖLÜYOR MU?
  Ayak (1) "iki PHC dizesi FARKLIDIR": EVET ölür (sabit salt ⇒ aynı parola ⇒
    aynı PHC dizesi). ✔
  Ayak (2) "salt alanı `[KS-23]`'ün salt uzunluğundadır": **ÖLMEZ** — ve bunu
    satırın KENDİ adlandırdığı mutasyon biçimi kanıtlıyor: `new byte[16]`
    **16 BAYTLIKTIR** ⇒ `[KS-23]`'ün salt uzunluğu (16 bayt) korunur ⇒ assert
    mutasyon altında YEŞİL. Süs ayak.
    (Yalnız "global sabit" şıkkı 16 bayt DIŞINDA bir uzunluk seçerse ölür —
    ama o da pinli değil; satır 16 baytlık bir sabiti ADIYLA yazıyor.)
S2 BASELINE YEŞİL Mİ? EVET (taze CSPRNG salt ⇒ farklı PHC; salt alanı 16
  bayt). ✔  Seviye `B` fizibil: `IPasswordHasher.Hash` iki kez çağrılır.
S3 AYIRT EDİCİ Mİ? EVET. M5 (rehash-on-login), M6/M6b (karşılaştırma),
  M34 (PHC ayrıştırma), M32 (NetArchTest) bu assert'i kırmaz. K28-d(a)'nın
  tespiti ("sabit salt M5·M6·M6b·M7·M31·M32·M34'ün hepsini geçer") DOĞRULANDI
  — M54 gerçekten yeni bir yüzey kapatıyor. ✔

HÜKÜM: **MAJÖR (M6-11)** — ölmeyen ikinci ayak, üstelik satırın kendi
  mutasyon örneğiyle kanıtlanabilir hâlde.
ÖNERİLEN ONARIM: ayak (2) "**değişmez**" diye yeniden adlandırılır (PHC
  biçiminin kapısı zaten M34'tür), ya da kendi mutasyonuna kavuşturulur
  (mutasyon: salt uzunluğu 16→8 bayt; kill: "salt alanı `[KS-23]`'ün salt
  uzunluğundadır" FAIL) — bu ikincisi K3-B1'in gerçek bir alt-özelliğidir.
ÖLÇÜM KOMUTU:
  sed -n '184,188p;906p' 0003-kimlik-cekirdegi.md
  # kod: B; var h1=Hash(p); var h2=Hash(p); Assert.NotEqual(h1,h2);
  #      + PHC salt alanı Base64 çözülüp uzunluğu ölçülür

--------------------------------------------------------------------------------
M55 — iki alt anahtarın info etiketi AYNI yapılır (K3-I4/K28-d(b))  [TEMİZ]
--------------------------------------------------------------------------------
ALINTI (satır 907, birebir):
| **M55** | İki alt anahtarın `info` etiketi **AYNI** yapılır (kopyala-yapıştır:
  `K_csrf`'in etiketi `K_jwt`'ninki olur) | "`K_jwt` · `K_csrf` · `K_rt`
  **ikişer ikişer FARKLIDIR**" (`FixedTimeEquals` ile üç çift) **FAIL** | B |

S1 ÖLÜYOR MU? EVET. HKDF deterministiktir; aynı prk + aynı info + aynı uzunluk
  ⇒ aynı çıktı ⇒ `K_csrf == K_jwt` ⇒ üç çiftten biri eşit ⇒ assert kırılır. ✔
S2 BASELINE YEŞİL Mİ? EVET ve DETERMİNİSTİK: üç etiket farklı ⇒ üç çıktı
  farklı. Gözlem yüzeyi PİNLİ — §2-M satır 781 `IKeyRing` portunu üç özellikle
  (`JwtSigningKey` · `CsrfHmacKey` · `RefreshSecretKey`) adıyla yazıyor ⇒
  seviye `B` gerçekten kurulabilir. (M50'de eksik olan dikiş burada VAR.) ✔
S3 AYIRT EDİCİ Mİ? EVET. M42b (etiket `v1`→`v2`) altında üç etiket hâlâ
  ikişer ikişer farklıdır ⇒ M55'in assert'i YEŞİL ⇒ ayrım net. M42c (efemer
  üretim) altında da anahtarlar rastgele ve farklıdır ⇒ yeşil. M42'nin
  "türetme kaldırılır + efemer" bileşiği, efemer ayağı yüzünden üç anahtarı
  yine FARKLI bırakır ⇒ yeşil. ✔
  (Tek teorik çakışma: "türetme kaldırılır ve kök anahtar ÜÇÜNDE DE doğrudan
  kullanılır" biçimindeki bir mutasyon M55'i de kırardı; ama M42'nin yazılı
  biçimi bu değildir.)

HÜKÜM: **TEMİZ.** Kapı-5'in 6. aday blokerinin kapatılışı SAĞLAM — v6'daki
  en iyi yazılmış üç satırdan biri.
MİNÖR (m-4): çıpa hücresindeki "**Ek pin:** üç etiket tek bir `static class
  HkdfLabels` içinde `const string` olarak yazılır, başka hiçbir yerde string
  literal olarak geçmez" kuralı HİÇBİR ŞEYLE ÖLÇÜLMÜYOR. Kill sinyali yalnız
  anahtarların farklılığını ölçer; ikinci bir tanım noktası doğsa da anahtarlar
  farklı kalabilir ⇒ pin kapısız. Ölçüm aracı K3-I4'ü "kapılı" gördüğü için bu
  boşluğu göremez (aracın kendi beyan ettiği 2. sınır — K32-b'nin konusu).
  Öneri: kural ya `D` seviyesinde bir analizör/`grep` kapısına bağlanır
  (`grep -c "momentum:v1:" src/ ⇒ 3, hepsi HkdfLabels.cs'de`) ya §3.1'e
  beyan edilir.
ÖLÇÜM KOMUTU:
  sed -n '429,442p;781p;907p' 0003-kimlik-cekirdegi.md
  # kod: B; FixedTimeEquals(K_jwt,K_csrf) == false (üç çift)
  # pin ölçümü (D): grep -rn "momentum:v1:" src/ | wc -l  ⇒ 3 beklenir

--------------------------------------------------------------------------------
M56 — sızmış-parola kara listesi (K3-B6(2) / K28-c)         [BLOKER + MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 908, birebir):
| **M56** | Sızmış-parola kara listesi kontrolü kaldırılır | "kara listedeki
  bir parola (`123456`) ile `/register` **`400`** alır **ve** listede olmayan,
  `[KS-17]` uzunluğunu sağlayan bir parola **`201`** alır" **FAIL** | B |
(Aynı sinyal §2-B6(2) satır 236'da birebir tekrarlanıyor.)

S1 ÖLÜYOR MU? **HAYIR — KÖR KAPI.**
  `123456` **6 karakterdir**. `[KS-17]` = **15 karakter** (satır 132, NIST SP
  800-63B-4 `SHALL`). Kara liste kontrolü KALDIRILSA BİLE, `123456` asgari
  uzunluk kuralına takılır ve `/register` **`400`** döner ⇒ kill sinyalinin
  birinci ayağı **MUTASYON ALTINDA DA YEŞİL KALIR.**
  ⚠ Belge bunu KENDİ SATIRINDA yazıyor — satır 216, birebir:
     "`123456` parolasına karşı Argon2id'nin `m=19456,t=2` yatırımı hiçbir şey
      satın almaz — **uzunluk, KDF'den önce gelir.**"
     Yani belge, `123456`'yı UZUNLUK kuralının hallettiği kanonik örnek olarak
     kullanıyor; sonra AYNI parolayı kara listenin kill sinyali yapıyor.
  ⚠ K3-J3'ün bağlayıcı sırası da bunu pekiştiriyor: kara liste "girdi doğrulama
     katmanı"ndadır (satır 234) — uzunluk doğrulaması da aynı katmandadır ve
     hangisinin önce koştuğu YAZILI DEĞİL; ama `400` HER İKİ SIRADA DA gelir.
  ⇒ M56 bugünkü yazımıyla ÖLDÜRÜLEMEZ.
  ⚠ İKİNCİ KATMAN KUSUR: sinyali onaracak parolanın **hem `[KS-31]` listesinde
    hem ≥ `[KS-17]`=15 karakter** olması gerekir. "En yaygın 10.000 parola"
    listeleri ezici çoğunlukla 6-12 karakterdir ⇒ böyle bir kesişim ÜRETİLEBİLİR
    OLMAYABİLİR [ŞÜPHELİ — liste kaynağı henüz seçilmemiş, K3-B6(2) "belirli
    bir kaynak PİNLEMEZ" diyor]. Bu, mutantı düzeltmenin `[KS-31]`'in
    KENDİSİNİ (liste seçimini) bağladığı anlamına gelir: liste, ≥15 karakterlik
    en az bir kayıt İÇERMEK ZORUNDADIR, aksi hâlde kapı hiç kurulamaz.
  Ayak (2) "listede olmayan, `[KS-17]`'yi sağlayan parola `201` alır":
    **ÖLMEZ** — kontrol kaldırıldığında da `201` gelir. Pozitif kontroldür;
    satır bunu "İkinci ayak zorunludur: tek ayakla *her şeyi reddet* mutasyonu
    da testi geçerdi" diye gerekçelendiriyor — gerekçe DOĞRU, ama ayağın YERİ
    yanlış (M35'te olduğu gibi "Pozitif kontrol" başlığıyla kill sinyalinin
    DIŞINA yazılmalı). Çoklu-assert kuralı ihlali.
S2 BASELINE YEŞİL Mİ? EVET — ama TRİVİAL olarak: baseline'da `123456` yine
  `400` alır (uzunluktan). Yani sinyal baseline'da da mutasyonda da AYNI
  sonucu veriyor ⇒ M46'nın v4'teki kusurunun ("test asla kırılamazdı") birebir
  aynısı, bu kez M56'da.
S3 AYIRT EDİCİ Mİ? HAYIR — ayırt edecek bir gözlem üretmiyor; M31'in birinci
  ayağı (`[KS-17]`'den bir eksik ⇒ `400`) aynı gözlemi zaten üretiyor.

HÜKÜM: **BLOKER (B6-1)** + **MAJÖR (M6-12)**.
ÖNERİLEN ONARIM (kilit gerektirir, çünkü `[KS-31]`'e şart ekliyor):
  (a) Kill sinyalindeki örnek parola, **hem kara listede hem ≥ `[KS-17]`**
      olan bir kayıtla değiştirilir. Örn. `qwertyuiopasdfgh` (16),
      `password123456789` (17), `123456789012345` (15) gibi adaylar; hangisinin
      seçileceği liste kaynağı seçilince ÖLÇÜLEREK belirlenir — belgeye
      LİTERAL parola değil, kural yazılır:
      "kara listenin **`[KS-17]` uzunluğunu sağlayan** bir kaydı `400` alır".
  (b) `[KS-31]` satırına şart eklenir: "liste **en az bir** `≥ [KS-17]`
      uzunluğunda kayıt İÇERMELİDİR; içermiyorsa M56 kurulamaz ve K3-B6(2)
      §3.1'e beyan edilir." (Bu, K28-c'nin geri dönüş yolunun — "liste
      küçültülür/alt küme üretilir" — yeni bir kısıtıdır.)
  (c) Ayak (2) "**Pozitif kontrol**" diye kill sinyalinin dışına alınır.
ÖLÇÜM KOMUTU (belge — bu turda KOŞULDU):
  grep -n "123456" 0003-kimlik-cekirdegi.md          # ⇒ satır 216, 236, 908
  sed -n '132p;216p;232,237p;908p' 0003-kimlik-cekirdegi.md
ÖLÇÜM KOMUTU (liste — spec'te koşacak):
  awk 'length($0)>=15' blocklist.txt | wc -l    # 0 ise M56 İNŞA EDİLEMEZ

--------------------------------------------------------------------------------
M-L TABLOSU — ORTAK DEĞERLENDİRME: "[devir] GERÇEK BİR DEVİR MİDİR?"
--------------------------------------------------------------------------------
SORU (Onur): `slice-3b` spec'i HENÜZ YOK. Devir borcu §7'de ADIYLA yazılı mı,
yoksa kapı "devredildi" denilerek buharlaştırılmış mı?

ÖLÇÜM (bu turda koşuldu):
  grep -n "slice-3b" 0003-kimlik-cekirdegi.md      # 12 eşleşme
  grep -n "\[devir\]" 0003-kimlik-cekirdegi.md     # 8 satır: 882, 916-920, 963, 989
  sed -n '912,922p;982p;989p;1108,1119p' 0003-kimlik-cekirdegi.md

CEVAP — BUHARLAŞMAMIŞ. Üç ayrı yerde, üç ayrı kimlikle yazılı:
  (1) §3 M-L tablosu (satır 914-920): beş satır, mutasyon + kill sinyali +
      seviye + "[devir] K3-Lx — kapısı `slice-3b` spec'inde koşar".
  (2) §3.1 (satır 982 ve 989): hem MUTANT kimliğiyle ("M-L5…M-L9") hem KARAR
      kimliğiyle ("K3-L5 · K3-L6 · K3-L7 · K3-L8 · K3-L9") kapısız beyan
      edilmiş. Satır 989 kapı-5'in B5-3 düzeltmesini de anlatıyor: v5 yalnız
      mutant kimliğiyle yazıyordu ⇒ araç beşini "kapısız-ve-beyansız" görüyordu
      ⇒ karar kimliği eklendi ve M-L satırlarındaki `[çıpa+]` etiketleri
      `[devir]` yapıldı (çünkü `[çıpa+]` onları AYNI ANDA kapılı gösteriyordu
      = çelişki). **Bu düzeltme DOĞRUDUR ve teyit ediliyor.**
  (3) §7 (satır 1117): beşi de ADIYLA ve konusuyla yazılı — "M-L5
      (tek-uçuşluluk + web ayağı: Web Locks, K3-L9 — seviye DART + DART-WEB) ·
      M-L6 (401'de kuyruk) · M-L7 (kullanıcı-başına DB) · M-L8 (ağ hatası ≠
      401, K3-L8) · M-L9 (429/5xx = geçici, K3-L8/3 — B3'ün istemci ayağı)".
  ⇒ "Kapı devredildi" denip kaybolan HİÇBİR KALEM YOK. Satır 922'nin ölçütü
     ("0004'e giden her mekanizma adlandırılmıştı; 3b'ye giden hiçbiri
     adlandırılmamıştı ⇒ asimetri ⇒ ihlal ⇒ kapatıldı") KARŞILANMIŞ.

ANCAK — KALAN BOŞLUK, **MAJÖR (M6-15)**:
  §7'de ADR 0004 borçları için bir **BORÇ TABLOSU** var (satır 1100-1106):
  sütunlar "borç | bu belgede | ADR 0004'te | **durum**" ve durumlar 🔴/🟡.
  `slice-3b` borçları için **böyle bir tablo YOK** — beş kalem tek bir düz
  madde işaretinde. Sonuçları:
   (a) **Kabul ölçütü yok:** borcun ne zaman kapandığı yazılı değil.
   (b) **Bağlayıcılık yok:** hiçbir cümle `slice-3b` spec'ini bu beş mutantı
       taşımaya ZORLAMIYOR. §7 satır 1118'in "Sıradaki" zinciri
       (0004 → GOREV-slice-3c → build → doğrulama) `slice-3b`'yi ancak satır
       1119'da "Sonra:" diye anıyor ve orada mutantlardan söz edilmiyor.
   (c) ⇒ Asimetri, satır 922'nin kapattığı asimetrinin İKİNCİ TÜREVİDİR:
       kalemler adlandırıldı ama borç MUHASEBELEŞTİRİLMEDİ.
  ÖNERİ: §7'ye ikinci bir tablo — "borç | bu belgede | `slice-3b`'de | durum" —
  ve tek cümlelik bağlayıcılık: "`slice-3b` spec'i M-L5…M-L9'u mutant tablosu
  olarak TAŞIMADAN kilitlenemez."

BİR DE ADI GEÇEN AMA GİTMEYEN BİR BORÇ VAR — **MİNÖR (m-8), KAPSAM DIŞI:**
  §6 Risk #17 (satır 1093) birebir: "rotasyon uç noktası **ADR 0004'ün ya da
  işletim diliminin işidir** ve **§7'nin devir listesine yazılmıştır**."
  `grep -n "rotasyon"` ⇒ satır 11, 436, 439, 1093. **§7'de (1095-1119) SIFIR.**
  ⇒ "Beyan edildiği söylenen ama yazılmamış sınır" kusuru (kapı-5 B5-5) bu kez
  TERS YÖNDE tekrarlanmış: kalem §6'ya yazıldı, devir listesine yazılmadı.
  (Bu kalem M42/M42b ailesine ait; benim mutant payımın dışında ama K32-b'nin
  ruhu gereği kayda geçiriliyor.)

--------------------------------------------------------------------------------
M-L5 — istemcide tek-uçuşlu kilit (K3-L5 + K3-L9)      [TEMİZ, m-6 notuyla]
--------------------------------------------------------------------------------
ALINTI (satır 916, birebir):
| **M-L5** | İstemcideki tek-uçuşlu kilit kaldırılır | "eşzamanlı N adet 401
  karşısında `/refresh` **TAM OLARAK 1 kez** çağrılır" **FAIL** · **web ayağı:**
  "iki sekmede eşzamanlı yenilemede `/refresh` tam olarak 1 kez çağrılır"
  **FAIL** (Web Locks, K3-L9) | DART + **DART-WEB** | **[devir]** K3-L5 |

S1 ÖLÜYOR MU? EVET (her iki ayak). Kilit kaldırılırsa N eşzamanlı 401, N adet
  `/refresh` doğurur ⇒ "TAM OLARAK 1" kırılır. Web ayağı: sekmeler arası kilit
  yoksa iki sekme iki çağrı yapar ⇒ kırılır. Mutasyon "kilit kaldırılır" TEK
  mekanizmayı siliyor ve İKİ ayağı da düşürüyor ⇒ çoklu-assert kuralına
  UYGUN. ✔
  (İnce nokta: yalnız `Completer` mutex'i olan, Web Locks'suz bir
  implementasyon ayak 1'i GEÇER ayak 2'de DÜŞER — bu bir kusur değil, K3-L9'un
  ayrı bir gözlem yüzeyine sahip olduğunun kanıtıdır ve seviye sütunu
  `DART + DART-WEB` diye zaten ikisini ayırıyor. DOĞRU YAZILMIŞ.)
S2 BASELINE YEŞİL Mİ? EVET — kilit varken 1 çağrı. `slice-3b`'de koşacak. ✔
S3 AYIRT EDİCİ Mİ? EVET. M-L6/L7/L8/L9 bu sayacı etkilemez. ✔
DEVİR AYAĞI: `[devir]` etiketi + §3.1(989) + §7(1117) ⇒ GERÇEK DEVİR. ✔

HÜKÜM: **TEMİZ** (devir ayağı sağlam).
MİNÖR (m-6): "eşzamanlı **N** adet 401" — `N` pinli değil ve kanonik bir sayı
  da değil. K30-b'nin kuralı ("bundan sonra yazılacak her kill sinyalini
  bağlar") gereği en azından "N ≥ 2 (test `N = 5` kullanır)" gibi bir pin
  gerekir; aksi hâlde `N = 1` yazan bir builder mutantı ISIRTAMAZ.

--------------------------------------------------------------------------------
M-L6 — 401'de kuyruğun temizlenmesi (K3-L6)                        [TEMİZ]
--------------------------------------------------------------------------------
ALINTI (satır 917, birebir):
| **M-L6** | 401'de gönderilmemiş kuyruk temizlenir | "gönderilmemiş op'lar
  diskte kalır ve yeniden girişte gönderilir" **FAIL** | DART |
  **[devir]** K3-L6 |

S1 ÖLÜYOR MU? EVET. Mutasyon kuyruğu siler ⇒ "diskte kalır" assert'i kırılır;
  "yeniden girişte gönderilir" ayağı da kırılır (gönderilecek şey kalmadı).
  İki ayak da AYNI mutasyondan düşer ⇒ kural ihlali YOK. ✔
S2 BASELINE YEŞİL Mİ? EVET (K3-L6: kuyruk bekler, düşürülmez). ✔
S3 AYIRT EDİCİ Mİ? EVET. M-L7 (tek DB dosyası) altında kuyruk yine durur ⇒
  M-L6'nın assert'i yeşil kalır ⇒ ayrım net. ✔
DEVİR AYAĞI: ✔ (§3.1/989 + §7/1117).

HÜKÜM: **TEMİZ.**

--------------------------------------------------------------------------------
M-L7 — kullanıcı-başına DB dosyası (K3-L7)                        [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 918, birebir):
| **M-L7** | Kullanıcı-başına DB dosyasından tek DB dosyasına dönülür |
  "A çıkıp B girince A'nın görevleri okunamaz **VE** A yeniden girince kuyruğu
  duruyor" **FAIL** | DART | **[devir]** K3-L7 |

S1 ÖLÜYOR MU?
  Ayak (1) "A çıkıp B girince A'nın görevleri **okunamaz**": EVET ölür — tek
    dosyada A'nın satırları B'nin oturumundan görülebilir/okunabilir hâle
    gelir ⇒ assert kırılır. ✔ (K3-L7'nin "izolasyon DOSYA DÜZEYİNDE" ayağının
    gerçek kapısı budur.)
  Ayak (2) "A yeniden girince **kuyruğu duruyor**": **ÖLMEZ.** Kuyruğun
    durmasını sağlayan mekanizma dosya AYRIMI değil, K3-L7'nin **"Çıkışta
    SİLME YOKTUR"** kuralıdır (satır 721). Tek dosyaya dönülse bile hiçbir şey
    silinmediği için A'nın gönderilmemiş op'ları yerinde kalır ⇒ assert
    mutasyon altında da YEŞİL.
  ⇒ Çoklu-assert kuralı ihlali: iki ayak İKİ FARKLI özelliği ölçüyor, ikincisi
    satırın mutasyonuna DUYARSIZ. Bu, B-6'nın (M31'e yığılmış e-posta ayağı)
    ve kapı-4 majör #9'un (M36'nın beş özniteliği) BİREBİR aynı hata sınıfıdır.
S2 BASELINE YEŞİL Mİ? EVET (iki ayak da). ✔
S3 AYIRT EDİCİ Mİ? Ayak (1) için EVET (M-L6 bu izolasyonu üretmez).

HÜKÜM: **MAJÖR (M6-13)** — ölmeyen ikinci ayak.
ÖNERİLEN ONARIM: ayak (2) ya "**değişmez**" diye yeniden adlandırılır (kapısı
  zaten M-L6'dır: "401'de kuyruk temizlenmez"), ya da K3-L7'nin silme-yasağı
  için AYRI bir mutant açılır: **M-L7b** — mutasyon: "çıkışta yerel DB dosyası
  SİLİNİR"; kill: "A yeniden girince kuyruğu **duruyor**" FAIL. (Bu ikincisi
  kırmızı çizgi 4'ü — kalıcı silme — doğrudan koruyan bir kapı olur; bugün o
  çizgi hiçbir mutantla ölçülmüyor.)

--------------------------------------------------------------------------------
M-L8 — ağ hatası dalı ≠ 401 dalı (K3-L8/2)                         [TEMİZ]
--------------------------------------------------------------------------------
ALINTI (satır 919, birebir):
| **M-L8** | Ağ hatası dalı `401` dalıyla birleştirilir | "ağ hatasında istemci
  **çevrimdışı-yetkili** kalır; 'oturum gerekli'ye GEÇMEZ" **FAIL** | DART |
  **[devir]** K3-L8 |

S1 ÖLÜYOR MU? EVET. Dallar birleşince ağ hatası "oturum gerekli"ye götürür ⇒
  assert kırılır. Tek mekanizma, tek ayak, temiz. ✔
S2 BASELINE YEŞİL Mİ? EVET (K3-L8/2 pazarlıksız ayrım). ✔
S3 AYIRT EDİCİ Mİ? EVET — M-L9 (429/5xx dalı) altında AĞ HATASI dalı hâlâ
  ayrıdır ⇒ M-L8'in assert'i yeşil kalır; tersi de doğru. İki mutant AYNI
  karara (K3-L8) çıpalı ama İKİ FARKLI özelliği ölçüyor ⇒ bu, doğru bir
  ayrımdır (M36/M36b emsalinin doğru uygulanmış hâli). ✔

HÜKÜM: **TEMİZ.**

--------------------------------------------------------------------------------
M-L9 — 429/5xx dalı ≠ 401 dalı (K3-L8/3)                          [MAJÖR]
--------------------------------------------------------------------------------
ALINTI (satır 920, birebir):
| **M-L9** | `429`/`5xx` dalı `401` dalıyla birleştirilir | "`/refresh` **`429`**
  döndüğünde istemci **çevrimdışı-yetkili kalır**, 'oturum gerekli'ye **GEÇMEZ**
  ve `Retry-After` süresi kadar (yoksa üstel geri çekilme, tavan `[KS-4]`)
  bekler" **FAIL** | DART | **[devir]** K3-L8 |

S1 ÖLÜYOR MU?
  Ayak (1) "çevrimdışı-yetkili kalır" + Ayak (2) "'oturum gerekli'ye GEÇMEZ":
    EVET, ikisi de ölür. ✔
  Ayak (3) "`Retry-After` süresi kadar (yoksa üstel geri çekilme, **tavan
    `[KS-4]`**) bekler": TRİVİAL ölür (dallar birleşince yeniden deneme hiç
    olmaz) ama **AYIRT EDİCİ DEĞİL**: tavanı YANLIŞ ayarlanmış (ör. 60 sn)
    ama dalı DOĞRU ayrılmış bir implementasyon M-L9'u **GEÇER** ⇒ tavan
    fiilen kapısız (m-7).
S2 BASELINE YEŞİL Mİ? EVET. ✔
S3 AYIRT EDİCİ Mİ? Ayak 1-2 için EVET (M-L8'den ayrı — yukarıda). ✔

HÜKÜM: **MAJÖR (M6-14)** — ama gerekçe ayaklarda değil, KANONİK SAYIDA:
  **`[KS-4]` burada İKİNCİ ve İLGİSİZ bir anlamda kullanılıyor.**
  §1-K.1 satır 99 birebir:
     | **KS-4** | Replay-idempotency penceresi | **10 dk** | **K14-a** | M1 · M29 · M30 |
  KS-4'ün tanımı SUNUCU TARAFLI bir penceredir (tüketilmiş token'ın halefinin
  aynen döndürülebileceği süre; sahibi K14-a, Onur kilidi K28-a). K3-L8/3
  (satır 734) ve M-L9 aynı `[KS-4]`'ü **İSTEMCİNİN ÜSTEL GERİ ÇEKİLME TAVANI**
  olarak kullanıyor — bambaşka bir büyüklük, farklı bir katman, farklı bir
  sahip. Sonuçları:
   (a) KS-4'ün "kapısı" sütununda M-L9 **YAZILI DEĞİL** ⇒ 1-K tablosu bu
       kullanımı bilmiyor.
   (b) K28-a yarın `[KS-4]`'ü değiştirirse (v6'da zaten 60 sn → 10 dk
       değişti — satır 365'in `[KS-LITERAL]` notu bunu anlatıyor) istemcinin
       geri çekilme tavanı **SESSİZCE** değişir. Bu, §1-K'nın kurulma
       gerekçesinin ("kardeş kalem güncellendi, kendisi unutuldu") TERSTEN
       gerçekleşmiş hâlidir: tekilleştirme, ilgisiz iki büyüklüğü BİRLEŞTİRDİ.
   (c) 10 dakikalık bir geri çekilme tavanı, `[KS-11]`=120 istek/5 dk olan bir
       `/refresh` politikası için AŞIRI YÜKSEKTİR (pencere 5 dk) — yani sayı
       türetilmiş değil, ÖDÜNÇ ALINMIŞTIR.
ÖNERİLEN ONARIM: 1-K.1'e **yeni bir satır** — ör. `KS-32 | İstemci geri
  çekilme tavanı | <değer> | K3-L8(3) | M-L9` — ve K3-L8/3 ile M-L9 ona atıf
  yapar. Değer `[KS-11]`'in penceresiyle (5 dk) tutarlı seçilmelidir. Ayrıca
  "tavan" için AYRI bir ayak/mutant gerekir (m-7) ya da §3.1'e beyan edilir.
ÖLÇÜM KOMUTU:
  sed -n '99p;732,736p;920p' 0003-kimlik-cekirdegi.md
  grep -n "KS-4" 0003-kimlik-cekirdegi.md | wc -l    # kullanım yerleri sayılır

================================================================================
3. ARACIN BEYAN ETTİĞİ SINIRA DAİR — K32-b'NİN CEVABI
================================================================================
§3.1 satır 956-958 birebir: "Araç **etiketin varlığını** ölçer, mutantın o
kararı **gerçekten ısırdığını** ölçemez — çıpa sütununa etiket eklemek, kapıyı
bir **tiyatroya** çevirebilir."

BU TURDA O SINIR ELLE KAPATILDI ve **ARACIN GÖREMEYECEĞİ 3 BLOKER + 15 MAJÖR**
bulundu. Aracın koşumu bu 19 satırda **`K1` = 0** verir (hepsinin çıpası dolu),
ama:
  · M56'nın çıpası `K3-B6(2)` dolu — kapı ÖLDÜRÜLEMEZ.
  · M43'ün çıpası `K3-L10` dolu — üçüncü ayak ölmüyor ve §3.1 onu tek kapı
    ilan ediyor.
  · M51'in çıpası `K3-I5(3)` dolu — mutasyonun yarısı ısırmıyor.
  · M49'un çıpası `K3-J4 · K3-J3 · §2-M` dolu — testi M32b'nin testiyle AYNI.
⇒ **Jeton, ısırmanın kanıtı değildir.** Bu turun bulduğu 18 kalemin
**HİÇBİRİ** `adr-kapi-taramasi.py` ile bulunamazdı; hepsi kill sinyalinin
SEMANTİĞİNİ okumayı gerektiriyor.

ARACA ÖNERİLEN İKİ MEKANİK KONTROL (aracın sınırını DARALTIR, kaldırmaz):
  K7 (yeni): çok ayaklı bir kill sinyalinde **pozitif assert** (`201`/`200`/
     "FARKLIDIR"/"eşittir") tespit edilirse ve satır "Pozitif kontrol" ya da
     "önkoşul"/"değişmez" etiketi TAŞIMIYORSA ⇒ bulgu üret.
     (Bu tek kural M52(1) · M54(2) · M56(2) · M47(B) · M53(1) · M-L7(2)'yi
      MEKANİK olarak yakalardı — bu turun majörlerinin ALTISI.)
  K8 (yeni): mutasyon hücresinde "**veya**" geçen ve satırda "İKİ AYRI
     KOŞUM/COMMIT" ifadesi BULUNMAYAN her satır ⇒ bulgu üret.
     (M45 · M48 · M51'i yakalardı — üç kalem, biri BLOKER.)

================================================================================
4. KAPANIŞ — BU TURUN HÜKMÜ (payıma düşen 19 satır için)
================================================================================
BU 19 SATIR BUGÜNKÜ HÂLİYLE KİLİTLENEMEZ.
  · 3 BLOKER: M56 · M43(3) · M51 — ikisi ölçüm hatası, biri (M43) kapı-4'ün
    ADIYLA yazdığı ve v6'da ONARILMAMIŞ bir borç.
  · 15 MAJÖR: 12 satıra dağılıyor; ALTISI tek bir kök nedenden geliyor —
    "pozitif kontrol / önkoşul / değişmez, kill ayağı gibi yazılmış".
  · 3 TEMİZ: M44 · M50 · M55.
EN AĞIR BULGU: **M56.** Belge, `123456`'yı bir yerde "uzunluk kuralının
hallettiği örnek" diye yazıp AYNI parolayı başka bir yerde kara listenin kill
sinyali yapıyor. Bu, kapı-6'nın bulması gereken türden bir kusur değil —
belgenin KENDİ İKİ SATIRINI yan yana koyunca görünen bir kusur; ve v5'in en
yeni kalemi (K28-c) tam olarak burada kör doğmuş.
EN SİSTEMİK BULGU: kapı-4'ün "**ÜÇÜNCÜ SEÇENEK YOKTUR**" kuralı, kuralı yazan
belgenin kendi satırlarının **en az altısında** uygulanmamış. Kural doğru;
uygulaması eksik.
================================================================================
