================================================================================
ADR 0003 v6 — 6. TUR BAĞIMSIZ KAPI / MUTANT PAYI 2
Kapsam: M24 → M42c (tabloda mevcut 27 satır)
Belge: /mnt/user-data/uploads/TO DO LİST/Momentum/docs/ADR/0003-kimlik-cekirdegi.md
Yöntem: K32-b — "jeton görmek kapı görmek değildir". Her satır için ÜÇ SORU
        (1) ÖLÜYOR MU? (2) BASELINE YEŞİL Mİ? (3) AYIRT EDİCİ Mİ?
        + çıpa hücresindeki karar ID'sinin gerçekten test edilen karar olup
        olmadığı + satır 833'ün PAZARLIKSIZ çok-ayak kuralı.
================================================================================

KAPSAM TEYİDİ
-------------
Görevde sayılan mutantlardan tabloda BULUNANLAR (27):
M24 M25 M26 M27 M28 M29 M30 M31 M32 M32b M32c M33a M33b M34 M35 M36 M36b
M37 M38 M39 M40 M40b M41 M41b M42 M42b M42c
Tabloda BULUNMAYAN: "M33" (tekil) — v3'te vardı, B6 ile M33a/M33b'ye bölündü
(satır 522-535). Payımdaki 27 satırın 27'si incelendi.
Satır 843-882 birinci tablo bloğu; satır 884-908 ikinci tablo bloğu
(M32b·M32c·M42b·M42c ikinci blokta yaşıyor).

ÖLÇÜM ORTAMI NOTU [DÜRÜSTLÜK]
-----------------------------
Bu kapı bir BELGE kapısıdır: repoda `slice-3c-auth` kodu henüz YOKTUR
(belgenin kendi ölçümü, §3.2(7) satır 1007: `Dockerfile` yok; §3.2(5) satır
1002: `AddJwtBearer` hiç çağrılmıyor). ⇒ Aşağıdaki hükümler mutantı FİİLEN
KOŞARAK değil, belgenin KENDİ PİNLERİ + belgenin kendi birincil-kaynak
ölçümleri üzerinden türetilmiştir. Her hükmün yanına, builder kodu yazdığında
hükmü ÇÜRÜTECEK/DOĞRULAYACAK somut ölçüm komutu yazıldı. Türetilemeyen
hiçbir yere hüküm yazılmadı; belirsiz kalanlar [ŞÜPHELİ] işaretlidir.


################################################################################
BÖLÜM A — BLOKER BULGULAR (KÖR KAPI)
################################################################################

================================================================================
BLOKER-1 · M42 (satır 892) — KİLL SİNYALİ TÜRETME YARISI İÇİN KÖR;
                             MUTASYON HÂLÂ BİLEŞİK; M42c İLE AYIRT EDİLEMEZ
================================================================================
SATIRIN BİREBİR ALINTISI (892):
| **M42** | Alt anahtar türetme kaldırılır ve CSRF HMAC anahtarı **her açılışta
yeniden üretilir** (efemer) | *"konteyner `docker compose restart` edildikten
**sonra**, restart öncesi alınmış `__Host-mct` + `__Host-mrt` çiftiyle yapılan
`/refresh` **`200`** döner"* **FAIL** | **KON** | **[B8 / K16-c]** K3-I3'ün açık
vaadi … M25/M35 hangi anahtar seçilirse seçilsin **yeşil kalır** ⇒ bu kapı
olmadan hiçbir test bunu yakalayamaz |

ÇAPRAZ ALINTI — §2-I satır 442:
"**Kapı: M42** (yalnız **türetmenin varlığı**: alt anahtar yerine kök anahtar
doğrudan kullanılırsa ısırır) **+ M42b** …"

ÇAPRAZ ALINTI — M42c satırı (900):
"**[kapı-4 majör #10]** v4'te bu ayak **M42'nin içine bileşik olarak**
gömülüydü (türetme + efemer üretim **tek** mutasyonda) ⇒ türetme yarısı hiçbir
testle gözlemlenemiyordu. **Ayrıldı.**"

(1) ÖLÜYOR MU? — HAYIR (türetme yarısı için). KÖR KAPI.
    §2-I satır 442 M42'yi "YALNIZ türetmenin varlığı" diye tanımlar. O
    mutasyonu tek başına uygula: `K_csrf = HKDF.Expand(masterKey, "…csrf-hmac")`
    yerine `K_csrf = masterKey`. Kök anahtar K3-I3 (satır 466) gereği
    `./.secrets/momentum-master.key` dosyasında KALICIDIR ve "Sonraki açılışlar
    **aynı** anahtarı okur". ⇒ restart sonrası aynı `K_csrf` (= masterKey)
    türetilir ⇒ `__Host-mct`'nin HMAC'i doğrulanır ⇒ `/refresh` **200 DÖNER**
    ⇒ assert YEŞİL KALIR ⇒ MUTANT HAYATTA KALIR.
    M42'nin sinyalinin ısırmasının TEK sebebi mutasyonun İKİNCİ yarısıdır
    (efemer üretim) — ve o yarı, satır 900'ün kendi beyanıyla M42c'dir.
    ⇒ kapı-4 majör #10'un "AYRILDI" iddiası YARIM UYGULANMIŞTIR: M42c satırı
    YAZILMIŞ ama M42'nin mutasyon hücresi ve kill sinyali HİÇ DEĞİŞMEMİŞTİR.

(2) BASELINE YEŞİL Mİ? — EVET. Baseline'da anahtar kalıcı, /refresh 200. Sorun
    baseline değil, mutasyon altındaki ölümsüzlüktür.

(3) AYIRT EDİCİ Mİ? — HAYIR. M42c (satır 900) ile kill sinyali BİREBİR aynı
    cümledir ("docker compose restart edildikten sonra, restart ÖNCESİ alınmış
    __Host-mct + __Host-mrt çiftiyle yapılan /refresh 200 döner" FAIL), kurulum
    aynı, seviye aynı (KON). M42c ek olarak dosya-sistemi ayağını (mtime)
    taşır ⇒ M42c, M42'nin STRİKT ÜST KÜMESİDİR. M42 hiçbir şey eklemez.

    Ayrıca: türetmenin kaldırılması BAŞKA bir kapı tarafından zaten ısırılır —
    M55 (satır 907) "K_jwt · K_csrf · K_rt ikişer ikişer FARKLIDIR" ölçer;
    türetme kaldırılıp üçü de kök anahtara eşitlenirse M55 FAIL eder. ⇒ M42'nin
    kendi sinyali kör, ısırdığı iddia edilen karar başka mutantta kapılı.

ÇIPA HÜCRESİ DOĞRU MU? — HAYIR. Çıpa "[B8 / K16-c] K3-I3'ün açık vaadi" diyor;
    K3-I3 = geliştirme bootstrap'ı / anahtar kalıcılığı. Ama satır 442'ye göre
    M42'nin ısırtması gereken karar **K3-I4** (HKDF türetmesi). Çıpa hücresinde
    K3-I4 GEÇMİYOR. §3.1'in tarama aracı (satır 949) çıpa sütununa bakar ⇒ araç
    bu satırı "kapılı" sayar, oysa ısırdığı karar yazılı değildir. Bu, K32-b'nin
    tarif ettiği "jeton var, ısırma yok" durumunun birebir örneğidir.

HÜKÜM: **BLOKER.** İki seçenek: (a) M42'nin mutasyon hücresi "YALNIZ türetme
kaldırılır; kök anahtar doğrudan üç yerde kullanılır" diye daraltılır VE kill
sinyali `KON`'dan `B` seviyesine indirilip ALTIN VEKTÖRE çıpalanır (M42b'nin
kardeşi: "pinli kök anahtardan türetilen K_jwt ≠ masterKey"); ya da (b) M42
VOID edilir ve türetme kapısı M55'e devredildiği açıkça yazılır. Üçüncü seçenek
(bugünkü hâl) belgenin kendi "KÖR KAPI YOK" doktrinini ihlal eder.

ÖLÇÜM KOMUTU (builder kodu yazdığında hükmü çürütmek/doğrulamak için):
  # 1. mutasyonu YALNIZ türetme yarısına uygula:
  #    Infrastructure/Crypto/KeyDerivation.cs → K_csrf = masterKey (HKDF atla)
  git checkout -b mut/M42-yalniz-turetme
  docker compose up -d --build
  RESP=$(curl -s -c jar.txt -X POST localhost:8080/v1/auth/login \
         -H 'X-Client-Kind: web' -d '{"email":"a@x.com","password":"<15+>"}')
  docker compose restart api && sleep 5
  curl -s -o /dev/null -w '%{http_code}\n' -b jar.txt -X POST \
       localhost:8080/v1/auth/refresh -H 'X-Client-Kind: web' \
       -H "X-CSRF-Token: $(grep __Host-mct jar.txt | awk '{print $7}')"
  # BEKLENEN (bulgunun doğrulanması): 200  ⇒ mutant HAYATTA ⇒ kör kapı
  # Belgenin iddiası:                 401  ⇒ ancak efemer yarı uygulanırsa
  # Ek: M42 ve M42c'nin sinyal metinlerinin aynı olduğunu göster:
  sed -n '892p;900p' docs/ADR/0003-kimlik-cekirdegi.md | grep -o 'restart[^"]*200[^"]*'


================================================================================
BLOKER-2 · M29 (satır 870) — ÖNKOŞUL PİNLENMEMİŞ; K3-C6(3) KOŞUL 3 VE
                             TEMBEL SİLME, MUTASYONU MASKELİYOR
================================================================================
SATIRIN BİREBİR ALINTISI (870):
| **M29** | Replay-idempotency penceresi **sınırsızlaştırılır** (`consumed_at +
[KS-4]` koşulu kaldırılır) | `FakeTimeProvider` **`[KS-4]`'ü AŞACAK KADAR
ileri alınır**: *"tüketilmiş token yeniden sunulunca **aile iptal olur**"*
**FAIL** | TC | **[K14-a]** … · **[çıpa+]** **K3-C5** … · **K3-C6(3)** |

DAYANAK — K3-C6(3), satır 328-331: replay-idempotency ÜÇ koşulun ÜÇÜNÜ birden
ister:
  1. `now ≤ T1.consumed_at + [KS-4]`
  2. halefin `consumed_at`'i hâlâ `NULL`
  3. **`T1.successor_secret_enc IS NOT NULL`** — "Değilse dal **(d)**'ye düşülür."
DAYANAK — K3-C6(5)(1), satır 362: "**Fırsatçı (tembel) silme:** her `/refresh`
işleminde, aynı transaction içinde, **o ailenin** `consumed_at < now - [KS-4]`
olan satırlarının `successor_secret_enc`'i `NULL`'lanır."

(1) ÖLÜYOR MU? — [HAYIR / ŞARTA BAĞLI] ⇒ KÖR KAPI RİSKİ.
    Mutasyon YALNIZ koşul 1'i kaldırır. Test kurgusu saati `[KS-4]`'ü aşacak
    kadar ileri alır. O anda T1'in `consumed_at`'i `now - [KS-4]`'ten eskidir.
    Replay isteği bir `/refresh` isteğidir ⇒ K3-C6(5)(1)'in tembel silmesi
    "her /refresh işleminde" koşar ⇒ **T1.successor_secret_enc NULL'lanır** ⇒
    **koşul 3 düşer** ⇒ dal (d) ⇒ aile İPTAL OLUR ⇒ assert "aile iptal olur"
    **YEŞİL KALIR** ⇒ MUTANT HAYATTA KALIR.
    Aynı sonuç ikinci bir yoldan da doğar: süpürücü (`RefreshSecretSweeper`)
    fixture'a kayıtlıysa ve saat `[KS-4]`+`[KS-6]` ilerlemişse sır zaten
    NULL'dır.
    Mutant ancak (a) süpürücü kayıtlı DEĞİLSE **ve** (b) tembel silme, dal
    değerlendirmesinden SONRA koşuyorsa ölür. Belge bu ikisinin HİÇBİRİNİ
    yazmıyor.

    ⚠ EKSİK PİN ADLANDIRILIYOR: **tembel silme ile dal (a)/(b)/(c)/(d)
    değerlendirmesinin SIRASI belgede hiçbir yerde yazılı değildir.** §2-C'nin
    "DAL ÖNCELİĞİ PAZARLIKSIZDIR" kuralı (satır 314-321) yalnız dalların kendi
    aralarındaki sırayı pinler; K3-C6(5)(1)'in aynı transaction içindeki
    NULL'lamasının bu zincirin NERESİNDE olduğu boştur. Bu, M44'ün kapattığı
    "dal sırası yazılmazsa sessiz açık doğar" kusurunun BİREBİR KARDEŞİDİR —
    ve bu kez M29'u kör yapar.

(2) BASELINE YEŞİL Mİ? — EVET. Baseline'da koşul 1 zaten düşer ⇒ dal (d) ⇒
    aile iptal olur ⇒ assert geçer. (Baseline mutasyon altındakiyle AYNI
    sonucu verir — kör kapının tanımı budur.)

(3) AYIRT EDİCİ Mİ? — Kâğıt üzerinde M30'dan ayrılır (M30 pencere İÇİNDE kalır;
    satır 871'in kendi PİN'i bunu doğru kuruyor). Ama (1) gereği M29 hiçbir
    şeyi ısırmadığı için ayırt edicilik tartışması konusuzdur.

EMSAL — BELGE BU TEHLİKEYİ BİLİYOR AMA YALNIZ M40b'DE UYGULAMIŞ:
M40b satırı (889) birebir: "**ÖNKOŞUL [kapı-5 aday bloker 4 — ZAMANA DEĞİL
MEKANİZMAYA ÇIPALI]:** süpürücü fixture'a **KAYDEDİLMEZ** (`SweepAsync` hiç
çağrılmaz)". Aynı önkoşul M29'da YOKTUR.

HÜKÜM: **BLOKER.** Gerekli onarım (üçü birden):
  (a) M29'un satırına önkoşul yazılır: "süpürücü fixture'a KAYDEDİLMEZ" **ve**
      "tembel silme, dal değerlendirmesinden SONRA koşar" (ya da test, T1'in
      `successor_secret_enc`'ini elle doldurulmuş tutar).
  (b) K3-C6'ya, tembel silmenin dal zincirindeki YERİ pazarlıksız olarak
      yazılır (M44'ün emsali).
  (c) Alternatif ve daha temiz: M29'un mutasyonu "koşul 1 kaldırılır"dan
      "koşul 1 kaldırılır **ve** tembel silme/süpürme devre dışı" bileşiğine
      ÇEVRİLMEZ — bileşik mutasyon M42'nin düştüğü tuzaktır. Doğrusu (a).

ÖLÇÜM KOMUTU:
  # baseline + mutasyon aynı testle koşulur, İKİ sonuç da kaydedilir
  dotnet test tests/Momentum.Api.Tests \
    --filter FullyQualifiedName~ReplayWindow_expired_replay_revokes_family \
    -l "console;verbosity=detailed"
  # sonra mutasyonu uygula (koşul 1'i kaldır) ve AYNI komutu koş:
  #   BEKLENEN (belge iddiası): FAIL
  #   BEKLENEN (bu bulgu):      PASS  ⇒ kör kapı
  # ek gözlem — testin içinde replay'den HEMEN ÖNCE ve SONRA:
  psql -c "select token_hash, consumed_at, successor_secret_enc is null as sec_null,
           revoked_reason from refresh_tokens where family_id='<fid>' order by created_at;"
  # sec_null = true ise koşul 3 zaten düşmüştür ⇒ mutasyon maskelenmiştir


################################################################################
BÖLÜM B — MAJÖR BULGULAR
################################################################################

================================================================================
MAJÖR-1 · M40 İKİNCİ AYAK (satır 888) — ÖLÜ TUZAK (BASELINE KIRMIZI DOĞAR)
================================================================================
SATIRIN BİREBİR ALINTISI (888, ikinci ayak):
"(2) *"süpürme turları arasındaki süre `[KS-6]`'dir"* — `FakeTimeProvider`
`[KS-6]`'nin **bir eksiği** kadar ilerletildiğinde satır **hâlâ doludur**,
`[KS-6]` kadar ilerletildiğinde **`NULL`**'dır **FAIL**"

KANONİK DEĞERLER (1-K.1, satır 99-101):
  [KS-4] = **10 dk** (600 sn)   ← K28-a bunu 60 sn'den 10 dk'ya ÇIKARDI
  [KS-6] = **60 sn**
SÜPÜRÜCÜNÜN YÜKLEMİ (K3-C6(5)(2), satır 363):
  `… WHERE successor_secret_enc IS NOT NULL AND consumed_at < @now - interval
  '10 minutes'` ve yorumda birebir: "değeri **[KS-4] ile AYNI olmak ZORUNDADIR**
  — bu SQL'in **YAŞ EŞİĞİDİR**, süpürme PERİYODU değil".

(1) ÖLÜYOR MU? — Evet (mutasyon `SweepAsync` no-op olduğu için satır asla
    NULL olmaz). Ama bu soru konusuz kalır, çünkü:

(2) BASELINE YEŞİL Mİ? — **HAYIR. ÖLÜ TUZAK.**
    Saat `t0`'dan (tüketim anı) yalnız `[KS-6]` = 60 sn ilerletildiğinde satırın
    yaşı 60 sn'dir. Süpürücünün YAŞ EŞİĞİ `[KS-4]` = 600 sn'dir. 60 < 600 ⇒
    `WHERE` yüklemi **hiçbir satırla eşleşmez** ⇒ satır **DOLU KALIR**.
    Ayağın ikinci yarısı ("[KS-6] kadar ilerletildiğinde NULL'dır") **BASELINE'DA
    KIRILIR.** Bu, §3.2(8)'in ("her mutant gerçekten koşulur ve mutasyondan ÖNCE
    testin YEŞİL olduğu kaydedilir") doğrudan ihlalidir.
    KÖK NEDEN: bu ayak, `[KS-4]` = 60 sn olduğu dönemde yazılmıştı; o dönemde
    60 sn ilerletmek yaş eşiğini TAM olarak aşıyordu. K28-a `[KS-4]`'ü 10 dk'ya
    çıkarınca ayak SESSİZCE ÖLÜ TUZAĞA döndü. Belge bu sessiz yanlışlanmayı
    satır 365'te BAŞKA bir literal için adlandırmış ("Sayı düştü, atıf kaldı"),
    ama M40'ın ayağına uygulamamıştır.

(2b) İKİNCİ KUSUR — MEKANİZMA PİNLENMEMİŞ:
    Ayak "süpürme turları ARASINDAKİ SÜRE"yi ölçer ⇒ ölçülen şey
    `BackgroundService`'in ZAMANLAYICISIDIR. Bu ancak zamanlayıcı
    `FakeTimeProvider`'dan besleniyorsa (ör. `new PeriodicTimer(period,
    timeProvider)`) ölçülebilir. Belge bunu HİÇBİR YERDE pinlemez; tersine
    K3-C6(5)(2) satır 363 birebir "süpürme işi **doğrudan çağrılabilir bir
    metoda** (`SweepAsync`) ayrılır ⇒ test `BackgroundService`'in
    zamanlayıcısını **beklemek zorunda kalmaz**" der. `SweepAsync` doğrudan
    çağrılırsa PERİYOT ölçülemez; zamanlayıcı beklenirse §3.2(9)(3)'ün
    "pencerenin dolmasını bekleyen test YAZILMAZ" yasağının kardeşi bir gerçek
    bekleme doğar. ⇒ ayak, belgenin kendi koşum sözleşmesiyle inşa edilemez
    durumdadır. [Bu ikinci kusur, birinci kusur onarılsa bile durur.]

(3) AYIRT EDİCİ Mİ? — Birinci ayak M40b'den doğru biçimde ayrılır (M40'ın
    mutasyon biçimi PİNLİ: "servis kaydı DURUR"; M40b'de süpürücü hiç
    kaydedilmez). Ayırt edicilik SORUN DEĞİL.

HÜKÜM: **MAJÖR (ÖLÜ TUZAK).** Onarım: ayak, yaş eşiğini AŞMIŞ bir satır
üzerinde periyodu ölçecek biçimde yeniden yazılır — ör. saat `[KS-4]`
ilerletilir (satır artık süpürülebilir ama HENÜZ SÜPÜRÜLMEMİŞ olmalıdır ⇒ ilk
tur `[KS-4]`'ten sonraki ilk `[KS-6]` sınırında gelir), sonra `[KS-6]`'nin bir
eksiği → DOLU, `[KS-6]` → `NULL`. VE zamanlayıcının `TimeProvider`'dan
beslendiği §3.2'ye madde olarak PİNLENİR. Aksi hâlde ayak `[KS-6]`'yi
ölçmekten çıkarılıp `[KS-4]`+`[KS-6]` tek ayağına (birinci ayak) indirgenir ve
`[KS-6]`'nin kapısız olduğu §3.1'de BEYAN EDİLİR.

ÖLÇÜM KOMUTU:
  # 1) sayıları belgeden çöz:
  grep -n 'KS-4\*\*.*|.*10 dk\|KS-6\*\*.*|.*60 sn' docs/ADR/0003-kimlik-cekirdegi.md
  # 2) baseline'ı MUTASYONSUZ koş — ayak 2'nin kırmızı doğduğunu göster:
  dotnet test tests/Momentum.Api.Tests \
    --filter FullyQualifiedName~Sweeper_period_is_KS6 -l "console;verbosity=detailed"
  # BEKLENEN (bu bulgu): baseline'da FAIL ("satır hâlâ dolu") ⇒ ölü tuzak
  # 3) yaş eşiğinin SQL'de [KS-4] olduğunu doğrula:
  grep -rn "interval '10 minutes'" src/backend/


================================================================================
MAJÖR-2 · SATIR 106-107 — "[KS-4] + [KS-6] = 120 sn" ARİTMETİĞİ YANLIŞ
                          (M40/M40b'nin ÖLÇÜM KURGUSUNUN DAYANAĞI)
================================================================================
BİREBİR ALINTI (satır 106-108):
"> **KS-4 × KS-6 aritmetiği (§6 Risk #13'ün dayanağı):** şifreli halef satırı
en kötü
>  **KS-4 + KS-6 = 120 sn** yaşar (pencere dolar, süpürücü bir sonraki turda
siler)."

ÖLÇÜM: [KS-4] = 10 dk = 600 sn (satır 99) · [KS-6] = 60 sn (satır 101).
        600 + 60 = **660 sn = 11 dk ≠ 120 sn.**

BELGENİN KENDİSİ BU KUSURU BAŞKA BİR YERDE ADLANDIRMIŞ AMA BU KOPYAYI
ONARMAMIŞ — satır 365 birebir:
"<!-- [KS-LITERAL: GERİ ÇEKİLEN literalin tarihsel kaydı] v6: burada *"en kötü
durumda 120 sn"* yazıyordu; K28-a `[KS-4]`'ü 60 sn'den 10 dk'ya çıkarınca bu
literal SESSİZCE YANLIŞLANDI. Sayı düştü, atıf kaldı. -->"

⇒ Aynı yanlış sayı, §2-C'de GERİ ÇEKİLMİŞ, ama §1-K.1'in kanonik-sayı
tablosunun hemen altındaki kutuda **YÜRÜRLÜKTE DURUYOR** — üstelik orası
sayıların KANONİK olduğu bölümdür ve builder önce oraya bakar. Bu, belgenin
kendi §4 kuralının ("bir sayı, onu üreten ölçüm değiştiğinde YENİDEN ÖLÇÜLÜR",
satır 952) ihlalidir ve M40'ın ikinci ayağının ölü tuzağıyla AYNI KÖK NEDENE
sahiptir (MAJÖR-1).

HÜKÜM: **MAJÖR.** Satır 107 ya `[KS-4] + [KS-6] = 660 sn (11 dk)` diye
düzeltilir ya da (tercih edilen, çünkü sayı atıfla yazılmalıdır) literal
tümüyle kaldırılıp "en kötü **[KS-4] + [KS-6]**" diye bırakılır. §6 Risk #13'ün
metni de aynı taramadan geçirilmelidir.

ÖLÇÜM KOMUTU:
  grep -n '120 sn' docs/ADR/0003-kimlik-cekirdegi.md
  # BEKLENEN onarım sonrası: yalnız satır 365'in [KS-LITERAL] tarihsel kaydı
  # kalır; satır 107'deki YÜRÜRLÜKTEKİ iddia kalmaz.
  python3 -c "print(600+60)"   # 660


================================================================================
MAJÖR-3 · M34 İKİNCİ AYAK (satır 876) — ÖLÜ TUZAK (traceId MUAFİYETİ YOK)
================================================================================
SATIRIN BİREBİR ALINTISI (876):
| **M34** | PHC ayrıştırıcısının savunmacı dalı kaldırılır (bozuk format istisna
atar) | DB'ye elle bozuk `password_hash` yazılır: *"`/login` **`401`** döner
(`500` DEĞİL) ve **gövde bilinen/bilinmeyen e-posta ile aynıdır**"* **FAIL** |
TC | **[Ma-4]** K3-B5'in tek-tip yanıt garantisinin kapısı · **[çıpa+]**
**K3-B7** |

KARDEŞ SATIRIN DOĞRU HÂLİ — M37 (satır 880):
"*"… yanıtları, **`extensions.traceId` alanı düşürüldükten sonra**, bayt bayt
aynıdır (durum kodu dâhil)"*"

BELGENİN KENDİ BİRİNCİL-KAYNAK ÖLÇÜMÜ (satır 204):
"**ÖLÇÜM (dotnet/aspnetcore `release/9.0`, `DefaultProblemDetailsWriter.cs`):**
`var traceId = Activity.Current?.Id ?? httpContext.TraceIdentifier;
context.ProblemDetails.Extensions["traceId"] = traceId;` — **koşulsuz.** ⇒ iki
ayrı isteğin gövdesi **hiçbir zaman** bayt bayt aynı olamaz; M37 baseline'da
**kırmızı doğardı = ölü tuzak.**"

(1) ÖLÜYOR MU? — Birinci ayak ("401 döner, 500 DEĞİL") EVET ölür: mutasyon
    altında bozuk PHC `FormatException` → `500` ⇒ FAIL. ✔

(2) BASELINE YEŞİL Mİ? — **HAYIR (ikinci ayak). ÖLÜ TUZAK.**
    "gövde bilinen/bilinmeyen e-posta ile aynıdır" ayağı, iki AYRI isteğin
    ProblemDetails gövdelerini karşılaştırır. `traceId` koşulsuz yazıldığı için
    iki gövde **hiçbir zaman** aynı olamaz ⇒ ayak baseline'da kırmızı doğar.
    Bu, satır 204'ün M37 için ÖLÇEREK kapattığı kusurun birebir aynısıdır;
    düzeltme M34'e TAŞINMAMIŞTIR. §3.2(3) `DefaultProblemDetailsWriter`'ı
    PAZARLIKSIZ kıldığı için kaçış yolu da yoktur.

(3) AYIRT EDİCİ Mİ? — Birinci ayak ayırt edicidir: M37'nin mutasyonu
    (bilinmeyen-e-posta dalının farklı gövde/kod döndürmesi) bozuk PHC senaryosunu
    hiç kurmaz ⇒ M34'ün testi M37 altında yeşil kalır; M34'ün mutasyonu da
    M37'nin testini kırmaz (M37 geçerli hash'lerle koşar). ✔

HÜKÜM: **MAJÖR (ÖLÜ TUZAK).** Onarım tek satırlıktır: M34'ün ikinci ayağı
M37'nin ifadesiyle hizalanır — "gövde, **`extensions.traceId` alanı
düşürüldükten sonra** bilinen/bilinmeyen e-posta ile aynıdır".

ÖLÇÜM KOMUTU:
  # baseline'da (mutasyonsuz) iki yanıtı yan yana koy:
  curl -s -X POST localhost:8080/v1/auth/login -H 'Content-Type: application/json' \
       -d '{"email":"bilinmeyen@x.com","password":"<15+>"}' > a.json
  curl -s -X POST localhost:8080/v1/auth/login -H 'Content-Type: application/json' \
       -d '{"email":"bozuk-hash@x.com","password":"<15+>"}' > b.json
  diff <(jq -S . a.json) <(jq -S . b.json)                 # traceId FARKI çıkar ⇒ ölü tuzak
  diff <(jq -S 'del(.extensions.traceId)' a.json) \
       <(jq -S 'del(.extensions.traceId)' b.json)          # boş ⇒ onarılmış ayak yeşil


================================================================================
MAJÖR-4 · M31 İKİNCİ AYAK (satır 872) — SÜS AYAK; SATIR 833'ÜN PAZARLIKSIZ
                                        KURALININ DOĞRUDAN İHLALİ
================================================================================
SATIRIN BİREBİR ALINTISI (872):
| **M31** | Parola asgari/azami uzunluk doğrulaması kaldırılır | **SINIR DEĞER,
ÜÇ AYAK (hepsi PAROLA — mutasyon parola doğrulamasıdır):** *"`[KS-17]`'den
**bir eksik** karakterlik parola `400`"* · *"**tam `[KS-17]`** karakterlik
parola **`201`**"* · *"`[KS-18]`'den **bir fazla** karakterlik parola `400`
**ve Argon2 KOŞMAZ**"* — **herhangi biri FAIL** | TS | … |

UYGULANAN KURAL — satır 833 (PAZARLIKSIZ):
"*çok assert'li bir mutant satırında **HER ayak, o satırın mutasyonu altında
ÖLMEK ZORUNDADIR.*** Ölmeyen ayak **iki seçenekten birine** gider: (a) kendi
mutasyonuyla **ayrı bir mutant** olur; ya da (b) kill sinyalinden çıkarılıp
**`önkoşul`** ya da **`değişmez`** diye yeniden adlandırılır … **Üçüncü seçenek
yoktur.**"

AYAK AYAK ÖLÇÜM (mutasyon: uzunluk doğrulaması KALDIRILIR):
  Ayak 1 — 14 karakterlik parola:
      baseline 400 ✔ / mutasyon altında **201** ⇒ assert FAIL ⇒ **ÖLÜR** ✔
  Ayak 2 — tam 15 karakterlik parola:
      baseline 201 ✔ / mutasyon altında **yine 201** (uzunluk doğrulaması
      kalkınca 15 karakter zaten kabul edilir) ⇒ assert **GEÇER** ⇒
      **ÖLMEZ** ✘✘✘
  Ayak 3 — 129 karakterlik parola:
      baseline 400 + Argon2 koşmaz ✔ / mutasyon altında 201 + Argon2 KOŞAR
      ⇒ assert FAIL ⇒ **ÖLÜR** ✔

⇒ Ayak 2 bir KAPI DEĞİL, bir SÜSTÜR — kapı-4 majör #9'un M36/M41/M43/M31'de
adlandırdığı sınıfın birebir aynısı, aynı satırda, HÂLÂ AÇIK.

BELGENİN KENDİ SAVUNMASI VE NEDEN YETMEDİĞİ:
Satır 872 birebir: "**15/14 çifti pazarlıksızdır:** yalnız *"`a` reddedilir"*
testi, çizgi 10'a da 15'e de düşse **yeşil kalır** = ölü ölçüt". Bu gerekçe
DOĞRUDUR — ayak 2 gerçekten çizgiyi TAM 15'e pinler ve gereklidir. Ama satır
833 bu gerekçeyi zaten öngörmüş ve karşılığını yazmıştır: böyle bir ayak
**kill sinyalinden çıkarılıp `önkoşul`/`değişmez` diye ADLANDIRILIR**. Emsal
belgenin kendi içindedir — M35 satırı (877) aynı işi doğru yapar:
"**Pozitif kontrol zorunlu:** doğru imzalı + doğru aileli değer **kabul
edilir** (aksi hâlde *"her şeyi reddet"* mutantı da testi geçer)" — ayrı
etiketle, kill ayağı olarak DEĞİL. M31 ise üç ayağı "herhangi biri FAIL" diye
tek kill sinyaline yığar.

(2) BASELINE YEŞİL Mİ? — Üç ayak da baseline'da yeşildir (ayak 2'nin parolası
    kara listede olmamak kaydıyla — bkz. MİNÖR-6). ✔
(3) AYIRT EDİCİ Mİ? — Ayak 1 ve 3 ayırt edicidir; M52 (e-posta) M31'in testini
    kırmaz, M56 (kara liste) de kırmaz. ✔

HÜKÜM: **MAJÖR.** Onarım: ayak 2, kill sinyalinden çıkarılır ve satırın içinde
"**DEĞİŞMEZ (kill ayağı DEĞİL): tam `[KS-17]` karakterlik parola `201` alır —
çizgiyi 15'e pinler; M31'in mutasyonu altında ÖLMEZ ve bu BEYANLIDIR**" diye
yeniden adlandırılır. Kill sinyali "**iki** ayak: 14 → 400 · 129 → 400 ve
Argon2 KOŞMAZ — herhangi biri FAIL" olur.

ÖLÇÜM KOMUTU:
  # ayak-ayak baseline+mutasyon koşumu (satır 838'in KANIT şartı):
  for L in 14 15 129; do
    P=$(python3 -c "print('x'*$L)")
    curl -s -o /dev/null -w "len=$L http=%{http_code}\n" -X POST \
      localhost:8080/v1/auth/register -H 'Content-Type: application/json' \
      -d "{\"email\":\"u$L@x.com\",\"password\":\"$P\"}"
  done
  # BASELINE  bekleniyor: 14→400  15→201  129→400
  # MUTASYON  bekleniyor: 14→201  15→201  129→201
  #                              ^^^^^^^  ayak 2 DEĞİŞMEZ ⇒ süs kanıtlanır


================================================================================
MAJÖR-5 · M33a (satır 874) — "GET /" AYAĞI KENDİ MUTASYONU ALTINDA ÖLMEZ;
                             TABLO SATIRI §2-J'NİN KENDİ TANIMINA TERS
================================================================================
TABLO SATIRININ BİREBİR ALINTISI (874):
| **M33a** | Fallback ucundan **`AllowAnonymous` kaldırılır** | *"kimliksiz
**`GET /` ve `GET /tasks`** **`200`** döner ve gövdesi `index.html`'dir"*
**FAIL** | TS | **[bloker #3 / K14-e / B6]** … |

§2-J'NİN KENDİ M33a TANIMI (satır 533) — TEK YOL:
"- **M33a** — mutasyon: fallback ucundan **`AllowAnonymous` kaldırılır**.
Kill: *"kimliksiz **`GET /tasks`** **`200`** döner ve gövdesi `index.html`'dir"*
**FAIL**."
⇒ Tablo satırı §2-J'de OLMAYAN bir ayak ("`GET /`") EKLEMİŞTİR.

PAZARLIKSIZ MIDDLEWARE SIRASI (satır 507-512, birebir):
    UseDefaultFiles      →  UseStaticFiles        ← auth'tan ÖNCE
    UseRouting
    UseRateLimiter
    UseAuthentication    →  UseAuthorization
    MapControllers / MapGroup("/v1")
    MapFallbackToFile("index.html").AllowAnonymous()   ← AÇIKÇA anonim

(1) ÖLÜYOR MU? — "GET /tasks" ayağı EVET (fallback ucuna düşer; `AllowAnonymous`
    kalkınca `FallbackPolicy` ısırır ⇒ 401 ⇒ FAIL ✔).
    **"GET /" ayağı HAYIR.** `UseDefaultFiles` isteği `/` → `/index.html` diye
    YENİDEN YAZAR ve hemen ardından gelen **auth'tan ÖNCEKİ** `UseStaticFiles`
    dosyayı servis eder ⇒ istek `UseAuthorization`'a **hiç ulaşmaz**, fallback
    ucu **hiç çalışmaz** ⇒ mutasyon (fallback'ten `AllowAnonymous` kaldırılması)
    bu yolu **hiç etkilemez** ⇒ ayak **YEŞİL KALIR** ⇒ SÜS AYAK.
    (Belgenin kendi ölçümü bu mekanizmayı zaten kuruyor: satır 527, dosya-benzeri
    yolların `{*path:nonfile}` kısıtına DÜŞMEDİĞİ ve `UseStaticFiles`'ın YERİNİN
    onları belirlediği ölçülmüş; `/index.html` tam olarak dosya-benzeri yoldur.)

(2) BASELINE YEŞİL Mİ? — EVET, iki ayak da (M33b'nin satırındaki `wwwroot`
    yer tutucu önkoşulu sağlanırsa; bkz. MİNÖR-5). ✔

(3) AYIRT EDİCİ Mİ? — "GET /" ayağı yalnız ölmemekle kalmaz, **M33b'nin
    mutasyonu altında ÖLÜR** (`UseStaticFiles` auth'tan sonraya alınırsa
    `/index.html` endpoint'siz kalır ⇒ `FallbackPolicy` ⇒ 401) ⇒ ayak, ait
    olduğu satırın değil KOMŞU satırın kapısıdır ⇒ ayırt ediciliği de bozar.

HÜKÜM: **MAJÖR.** Onarım: tablo satırı §2-J satır 533 ile hizalanır — kill
sinyalinden "`GET /` ve" ifadesi ÇIKARILIR. (İstenirse `GET /` ayrı bir
`önkoşul` satırı olarak yazılabilir; kill ayağı olamaz.)

ÖLÇÜM KOMUTU:
  # mutasyon: MapFallbackToFile("index.html")  ← .AllowAnonymous() SİLİNİR
  for P in / /tasks /main.dart.js; do
    curl -s -o /tmp/b -w "$P -> %{http_code} " localhost:8080$P
    head -c 40 /tmp/b; echo
  done
  # BASELINE : /→200(index)  /tasks→200(index)  /main.dart.js→200(js)
  # MUTASYON : /→200(index)  /tasks→401         /main.dart.js→200(js)
  #             ^^^^^^^^^^^  "GET /" ayağı DEĞİŞMEZ ⇒ süs kanıtlanır


================================================================================
MAJÖR-6 · M36 (satır 878) — "VEYA"LI MUTASYON + İKİ KILL AYAĞI:
                            HANGİ DAL UYGULANIRSA DİĞER AYAK ÖLMEZ
================================================================================
SATIRIN BİREBİR ALINTISI (878):
| **M36** | Çerezlerden **`__Host-`** öneki kaldırılır (**veya** `Domain`
özniteliği eklenir) | **BU MUTASYONUN ISIRDIĞI İKİ AYAK:** *"`Set-Cookie`
başlıkları **`__Host-`** ile başlar"* · *"**`Domain=` içermez**"* — **herhangi
biri FAIL** | TS | **[bloker #4]** … **[kapı-4 majör #9]** v4 bu satıra **yedi**
assert yığmıştı; beşi … **`__Host-` mutasyonu altında ÖLMÜYORDU** ⇒ süs. Onlar
**M36b**'ye taşındı. Çıpa: **K3-L2** |

AYAK AYAK ÖLÇÜM:
  DAL A — mutasyon: `__Host-` öneki kaldırılır (çerez adı `mrt`/`mct` olur)
      Ayak 1 ("`__Host-` ile başlar")  : FAIL ⇒ ÖLÜR ✔
      Ayak 2 ("`Domain=` içermez")     : önek kalkması `Domain` EKLEMEZ ⇒
                                          başlık hâlâ `Domain` taşımaz ⇒
                                          assert GEÇER ⇒ **ÖLMEZ** ✘
  DAL B — mutasyon: `Domain=momentum.app` eklenir (ad `__Host-mrt` KALIR)
      Ayak 1 : sunucunun yazdığı başlık hâlâ `__Host-` ile başlar (ASP.NET Core
               `__Host-` semantiğini ZORLAMAZ, başlığı olduğu gibi yazar) ⇒
               assert GEÇER ⇒ **ÖLMEZ** ✘
      Ayak 2 : FAIL ⇒ ÖLÜR ✔

⇒ Tek bir commit'te hangi dal uygulanırsa uygulansın **ayaklardan biri
YEŞİL KALIR** ⇒ satır 833 ihlali. Bu, satırın KENDİ çıpa hücresinde
"kapı-4 majör #9" diye adlandırılan kusurun aynısıdır: beş süs ayak M36b'ye
taşınmış, ama kalan iki ayağın **iki farklı mutasyona** ait olduğu gözden
kaçmıştır.

DOĞRU EMSAL BELGENİN İÇİNDE — M36b (satır 879), birebir:
"**DÖRT AYRI KOŞUM, her biri kendi mutasyonu altında** … — **her biri AYRI
commit**". M36 aynı biçimi almamıştır.

(2) BASELINE YEŞİL Mİ? — EVET, iki ayak da. ✔
(3) AYIRT EDİCİ Mİ? — M36b'den ayrılır (M36b öznitelikleri ölçer, adı/`Domain`'i
    değil) ✔; ama satırın KENDİ iki dalı birbirinden ayrılmamıştır.

HÜKÜM: **MAJÖR.** Onarım (M36b'nin biçimiyle birebir): satır
"**İKİ AYRI KOŞUM, her biri kendi mutasyonu altında:** (a) önek kaldırılır ⇒
*"`Set-Cookie` başlıkları `__Host-` ile başlar"* FAIL; (b) `Domain=` eklenir ⇒
*"`Domain=` içermez"* FAIL — **her biri AYRI commit**" diye yeniden yazılır.

ÖLÇÜM KOMUTU:
  # her dal AYRI commit'te, aynı gözlemle:
  curl -s -D - -o /dev/null -X POST localhost:8080/v1/auth/login \
    -H 'X-Client-Kind: web' -H 'Content-Type: application/json' \
    -d '{"email":"a@x.com","password":"<15+>"}' | grep -i '^set-cookie:'
  # BASELINE : set-cookie: __Host-mrt=…; path=/; secure; httponly; samesite=strict; max-age=…
  #            set-cookie: __Host-mct=…; path=/; secure; samesite=strict; max-age=…
  # DAL A     : "__Host-" YOK   → ayak1 FAIL, ayak2 hâlâ PASS
  # DAL B     : "domain=" VAR   → ayak2 FAIL, ayak1 hâlâ PASS


================================================================================
MAJÖR-7 · M39 (satır 882) — AYIRT EDİCİ DEĞİL: TAŞIYICI DEĞİŞMEZ M45'İN
                            KILL SİNYALİYLE BİREBİR AYNI
================================================================================
M39'UN BİREBİR ALINTISI (882):
| **M39** | Atomik `UPDATE` yerine check-then-act (`SELECT` sonra `UPDATE`)
yazılır | **gerçek Postgres'te paralel** iki `/refresh` aynı `T1` ile: *"**tam
olarak biri** `200`, diğeri `401`+`reuse_detected` **ALIR; VEYA** (K14-a'nın
replay penceresi devreye girdiyse) **ikisi de `200` alır ve dönen token'lar
ÖZDEŞTİR**"* — **her iki durumda da** *"ailenin satır sayısı **tam olarak 1**
artmıştır"* **FAIL** | TC | … **Değişmez artık yanıt kodu değil, AİLENİN SATIR
SAYISIDIR** (K3-C6/1) |

M45'İN BİREBİR ALINTISI (895):
| **M45** | Halef `INSERT`'ü tüketim `UPDATE`'inden **önceye** alınır (veya ayrı
transaction'a çıkarılır) | **gerçek Postgres'te paralel** iki `/refresh` aynı
`T1` ile: *"ailenin satır sayısı **tam olarak 1** artmıştır"* **FAIL**
*(kaybeden istek sahipsiz bir satır bırakırsa 2 artar)* | TC | … |

(1) ÖLÜYOR MU? — EVET. Check-then-act altında iki istek de T1'i tüketilmemiş
    görür, ikisi de halef üretir ⇒ aile +2 ⇒ FAIL ✔
(2) BASELINE YEŞİL Mİ? — EVET (koşullu `UPDATE`'in satır kilidi ⇒ +1). ✔
(3) AYIRT EDİCİ Mİ? — **HAYIR.**
    Kurulum AYNI ("gerçek Postgres'te paralel iki `/refresh` aynı `T1` ile"),
    seviye AYNI (TC), ve M39'un **kendi satırının "artık DEĞİŞMEZ budur" diye
    ilan ettiği** ölçüt ("ailenin satır sayısı tam olarak 1 artmıştır") M45'in
    kill sinyalinin **TAMAMIDIR**. ⇒ **M45'in mutasyonu M39'un testini de
    kırar.** Kapıyı hangi mutasyonun ısırdığı ayırt edilemez; "kaç test kırıldı"
    muhasebesi (M19 satırının açıkça talep ettiği disiplin) bozulur.

    KISMİ SAVUNMA VE SINIRI: M39'un yanıt-kodu yan cümlesi ("ikisi de 200 alır
    **ve dönen token'lar ÖZDEŞTİR**") M45 altında YEŞİL kalır — çünkü M45'te
    kaybeden istek 401+`reuse_detected` alır ve disjonksiyonun birinci kolu
    sağlanır. ⇒ ayırt ediciliği taşıyan şey satır sayısı DEĞİL, **token
    özdeşliği/yanıt kodu disjonksiyonudur**. Ama satır, tam tersini ilan eder:
    "Değişmez **artık yanıt kodu değil**, AİLENİN SATIR SAYISIDIR". Belge
    ayırt ediciliği taşıyan ayağı geri çekmiş, taşımayanı öne almıştır.

HÜKÜM: **MAJÖR.** Onarım (iş bölümü açıkça yazılır):
  • **M45** satır sayısını sahiplenir (sahipsiz satır ölçütü tam olarak odur).
  • **M39** ayırt ediciliğini yanıt/token ekseninden alır: *"iki istek de `200`
    alırsa dönen yenileme token'ları **ÖZDEŞ** olmak ZORUNDADIR"* — check-then-act
    altında iki FARKLI halef döner ⇒ FAIL; M45 altında bu ayak yeşil kalır.
  • Satır sayısı ölçütü M39'da **`değişmez`** etiketiyle (kill ayağı DEĞİL)
    tutulabilir — satır 833'ün (b) şıkkı.

ÖLÇÜM KOMUTU:
  # aynı T1 ile paralel iki /refresh (TC, gerçek Postgres):
  T1=<token>; for i in 1 2; do (curl -s -X POST localhost:8080/v1/auth/refresh \
     -H 'X-Client-Kind: native' -d "{\"refreshToken\":\"$T1\"}" -o r$i.json \
     -w "req$i=%{http_code}\n") & done; wait
  psql -c "select count(*) from refresh_tokens where family_id='<fid>';"
  jq -r .refreshToken r1.json r2.json | sort -u | wc -l   # 1 ⇒ ÖZDEŞ, 2 ⇒ FARKLI
  # BASELINE      : count=+1 · (200/401) veya (200/200 + özdeş)
  # M39 mutasyonu : count=+2 · 200/200 + FARKLI  ⇒ token ayağı da ısırır
  # M45 mutasyonu : count=+2 · 200/401           ⇒ token ayağı ISIRMAZ (ayrım burada)


================================================================================
MAJÖR-8 · §2-I SATIR 442 — M42b İÇİN GERİ ÇEKİLMİŞ KILL SİNYALİ HÂLÂ
                           YÜRÜRLÜKTE YAZILI (BELGE İKİ SİNYAL TAŞIYOR)
================================================================================
SATIR 442'NİN BİREBİR ALINTISI:
"**+ M42b** (`info` etiketi değiştirilirse — ör. `v1` → `v2` — **mevcut
oturumların JWT'leri doğrulanamaz ⇒ `401`**)"

M42b TABLO SATIRININ BİREBİR ALINTISI (899):
"**[kapı-5 B5-12 — GERİ ÇEKİLEN KİLL SİNYALİ]** v5 bunu *"restart sonrası
`401`"* diye ölçüyordu; **ayırt edici DEĞİLDİ**: `info` mutasyonu statiktir,
mutasyona uğramış derlemede restart öncesi ve sonrası **aynı** anahtar türetilir
⇒ sinyal baselinede de mutasyonda da aynı sonucu verirdi. Altın vektör tek
derlemede, deterministik ve `B` seviyesinde ısırır"

⇒ §3'te GERİ ÇEKİLEN sinyal, §2-I'de HİÇBİR GERİ ÇEKME İŞARETİ OLMADAN
duruyor. Builder §2'yi okuyup "401" testini yazarsa, kapı-5'in ölçerek kapattığı
ayırt-edicilik kusuru AYNEN geri döner. Bu, belgenin kendi sınıflandırmasıyla
"sayı düştü, atıf kaldı" (satır 365) kusurunun kardeşidir — burada düşen bir
sayı değil, bir KILL SİNYALİDİR.

NOT — M42b'nin TABLO SATIRI SAĞLAMDIR:
  (1) ÖLÜYOR MU? EVET — altın vektör hex'leri `info` baytları değişince tutmaz;
      ASCII→UTF-16 kodlama değişimi de aynı vektörü kırar. ✔
  (2) BASELINE YEŞİL Mİ? EVET — pinli kök anahtar + pinli ASCII kodlama
      (satır 440) + KANIT'a yazılı vektör ⇒ deterministik, `B` seviyesi. ✔
  (3) AYIRT EDİCİ Mİ? EVET — M42c (efemer) altın vektörü kırmaz; M55 (etiket
      ÇAKIŞMASI) vektörün üçünü birden değiştirir ama M55'in kendi sinyali
      "ikişer ikişer FARKLI"dır; M42b tek tek DEĞERE bakar. ✔
  Kusur satırda değil, §2-I'deki bayat kopyadadır.

HÜKÜM: **MAJÖR.** Onarım: satır 442'nin M42b parantezi
"…(`info` etiketi ya da bayt kodlaması değiştirilirse **KANIT'taki ALTIN
VEKTÖR hex'leri tutmaz**; *"restart sonrası 401"* sinyali **kapı-5 B5-12 ile
GERİ ÇEKİLMİŞTİR — ayırt edici değildi*)" diye yeniden yazılır.

ÖLÇÜM KOMUTU:
  grep -n 'doğrulanamaz ⇒ `401`' docs/ADR/0003-kimlik-cekirdegi.md
  # Onarım sonrası: 0 eşleşme (ya da yalnız [KS-LITERAL] tarihsel kayıt içinde)
  grep -n 'ALTIN VEKTÖR' docs/ADR/0003-kimlik-cekirdegi.md
  # Onarım sonrası: §2-I ve §3 tablosu AYNI sinyali göstermeli


================================================================================
MAJÖR-9 · §2-J SATIR 567-570 — M41 İÇİN BAYAT İKİ-AYAKLI KILL SİNYALİ +
                               YANLIŞ ÖNKOŞUL; M41b §2'DE HİÇ GEÇMİYOR
================================================================================
SATIR 567-570'İN BİREBİR ALINTISI:
"> **Kapı: M41 — iki ayaklı.** Mutasyon: handler'daki e-posta penceresi
kaldırılır.
>  1. *"**aynı** normalize e-posta ile **6.** `/login` denemesi `429` alır **ve**
`problem.Extensions[\"limit\"] == \"email\"`"* **FAIL**.
>  2. *"**aynı IP'den farklı e-postalarla** gelen 6. istek `429` **ALMAZ**"*
**FAIL** — bu ikinci ayak **R2'yi de korur** … *(Kontrol 1'in tavanına
çarpmamak için test **6 istekle sınırlıdır**; `[KS-10]` tavanının altındadır.)*
>  Seviye **TS**. Önkoşul: kontrol 1'in tavanı bu testte tetiklenmemelidir
(**K16-b sayıları bunu zaten sağlar**)."

M41 TABLO SATIRININ BİREBİR ALINTISI (890):
"**[kapı-4 majör #9]** v4'ün ikinci ayağı (*"aynı IP'den farklı e-postalarla
gelen istek `429` ALMAZ"*) **bu mutasyon altında ÖLMÜYORDU** — kontrol 2
kaldırılınca da o istek `429` almazdı ⇒ süs. O ayak **M41b**'ye taşındı."

ÜÇ AYRI KUSUR, HEPSİ AYNI KUTUDA:
 (a) **Ölmediği ÖLÇÜLEN ayak, §2-J'de hâlâ M41'in kill ayağı olarak yazılı.**
     Kapı-4 majör #9'un kapatılması yalnız §3 tablosuna işlenmiş; §2-J'ye
     taşınmamış. Builder §2'yi okursa süs ayağı yeniden yazar.
 (b) **M41b, §2'nin HİÇBİR YERİNDE geçmiyor.** `grep 'M41b'` ⇒ yalnız satır 890
     ve 891 (§3 tablosu). Kontrol 2'nin ANAHTARININ e-posta olduğu kararı
     (K3-J2(2)) §2'de kapısız görünüyor.
 (c) **"Önkoşul: … K16-b sayıları bunu zaten sağlar" İFADESİ YANLIŞ VE
     TEHLİKELİ.** §3.2(9) satır 1012 birebir ölçüyor: "kontrol 1 **tek
     partition**'dır (`TestHost`'ta `RemoteIpAddress` **`null`**). ⇒ paylaşılan
     bir `WebApplicationFactory`'de M23'ün istekleri + M11'in istekleri +
     M52/M31'in `/register` çağrıları **aynı kovayı** doldurur ve **M41'in/
     M46'nın negatif assert'leri (`429` ALMAZ) SIRA BAĞIMLI olarak kırılır.**"
     ⇒ Kovayı boş tutan şey "K16-b sayıları" DEĞİL, §3.2(9)(1)'in
     "her sınıf KENDİ `WebApplicationFactory`'sini kurar" izolasyonudur.
     §2-J'nin cümlesi, M11 satırının (853) "**Sayı artık yazılmıyor … bu kusur
     bir daha DOĞAMAZ**" diye ilan ettiği ÖLÜ TUZAK sınıfını yeniden yazar.
 (d) Ek: satır 568-569 "**6.**" sayısını LİTERAL yazıyor (`[KS-13]`+1). M11
     satırı bu yazım biçimini açıkça yasakladı ("`[KS-n]`'e atıf yapılıyor").
     Satırda `[KS-LITERAL]` muafiyet yorumu da YOK.

NOT — M41'in TABLO SATIRI (890) SAĞLAMDIR:
  (1) ÖLÜR ✔ (kontrol 2 kalkınca aynı e-postayla tavan aşılsa da 429 gelmez;
      `limit=="email"` ve `Retry-After` ayakları da aynı assert içinde düşer —
      `FixedWindowLease`'in `MetadataName.RetryAfter` taşıdığı satır 936'da
      ÖLÇÜLMÜŞ.)
  (2) BASELINE YEŞİL ✔ (satırın kendi önkoşulu doğru: "`[KS-10]`'un kovası
      boştur (§3.2(9) izolasyonu)").
  (3) AYIRT EDİCİ ✔ (M11 kontrol 1'i kaldırır ⇒ M41'in testi yine kontrol 2'den
      429 alır ⇒ yeşil kalır; M22 eşzamanlılık ister, ardışık isteklerde
      tetiklenmez).

HÜKÜM: **MAJÖR.** Onarım: satır 567-570 tümüyle yeniden yazılır — M41 **TEK
ayaklı** ilan edilir, ikinci ayak **M41b**'ye atıfla anılır, önkoşul
"§3.2(9)(1)'in sınıf-başına taze fabrika izolasyonu" diye düzeltilir, "6."
literali `[KS-13]`'e çevrilir.

ÖLÇÜM KOMUTU:
  grep -n 'M41b' docs/ADR/0003-kimlik-cekirdegi.md      # bugün: yalnız 890,891
  grep -n 'K16-b sayıları bunu zaten sağlar' docs/ADR/0003-kimlik-cekirdegi.md
  grep -n '\*\*6\.\*\* `/login`' docs/ADR/0003-kimlik-cekirdegi.md
  # onarım sonrası üç grep de ya boş ya §3 ile TUTARLI çıkmalı


================================================================================
MAJÖR-10 · M41b (satır 891) — MUTASYON BİÇİMİ PİNLENMEMİŞ;
                              NULL `RemoteIpAddress` KÖR KAPI ÜRETEBİLİR
================================================================================
SATIRIN BİREBİR ALINTISI (891):
| **M41b** | Kontrol 2'nin **anahtarı** normalize e-postadan **IP'ye** çevrilir
| *"**aynı IP'den FARKLI e-postalarla** `[KS-13]`'ün tavanını aşan istekler
`429` **ALMAZ**"* **FAIL** — mutasyonda IP anahtarlı sayaç dolar ve `429` gelir
| TS | **[kapı-4 majör #9]** … Çıpa: **K3-J2(2)** |

ÇATIŞAN ÖLÇÜM — K15-b (satır 123-126 ve 1012), birebir:
"`RemoteIpAddress` bu dağıtımda köprü ağ geçididir; **`TestHost` altında
`null`'dır**."

(1) ÖLÜYOR MU? — **[ŞÜPHELİ / MUTASYON BİÇİMİNE BAĞLI].**
    Kill sinyali NEGATİF bir assert'tir ("`429` **ALMAZ**") ⇒ mutasyon altında
    `429` GELMEZSE mutant hayatta kalır. `TestHost`'ta IP `null` olduğu için
    mutasyonun somut yazımı belirleyicidir:
      • `partition = ctx.Connection.RemoteIpAddress?.ToString() ?? "unknown"`
        ⇒ tüm istekler TEK partition ⇒ sayaç dolar ⇒ `429` ⇒ assert FAIL ⇒
        **ÖLÜR** ✔
      • `partition = ctx.Connection.RemoteIpAddress.ToString()`
        ⇒ `NullReferenceException` ⇒ `500` ⇒ istek `429` **ALMAZ** ⇒ assert
        **GEÇER** ⇒ **MUTANT HAYATTA KALIR = KÖR KAPI** ✘
    Belge iki yazımdan hangisinin geçerli olduğunu **pinlemez**.

    ⚠ Ayrıca satır 891 "**aynı IP'den** FARKLI e-postalarla" der; `TestHost`'ta
    "aynı IP" diye bir şey **yoktur** (hepsi `null`). Sinyal, ölçülmüş olguyla
    çelişen bir dille yazılmıştır — M11 satırının (853) tam olarak bu yüzden
    *"aynı IP'den"* ifadesini **DÜŞÜRDÜĞÜ** kayıtlıdır: "*"aynı IP'den"*
    ifadesi de **düştü**: K15-b ölçümüyle `TestHost`'ta `RemoteIpAddress`
    **`null`**'dır." M41b bu düzeltmeyi ALMAMIŞTIR.

    EMSAL: M22 satırı (863) tam bu tehlike için pin yazar — birebir:
    "*"her istek farklı IP'den"* koşulu, `UseRateLimiter`'dan **ÖNCE** eklenen
    **tek satırlık test-only middleware** ile kurulur
    (`ctx.Connection.RemoteIpAddress = IPAddress.Parse($"10.0.0.{i}")`)".
    M41b'de bu pin YOKTUR — oysa M41b'nin sinyali IP kavramına M22'den DAHA
    doğrudan bağlıdır.

(2) BASELINE YEŞİL Mİ? — EVET, ama şartlı: `[KS-10]`'un kovası taze olmalıdır
    (§3.2(9)(1)). Satır 891'de bu önkoşul **YAZILI DEĞİL** (kardeşi M41'in
    satırında var). Paylaşılan fabrikada negatif assert satır 1012'nin ölçtüğü
    gibi **sıra bağımlı olarak kırılır** ⇒ ikinci bir ölü-tuzak riski.

(3) AYIRT EDİCİ Mİ? — M41'den ayrılır (M41 pozitif `429` bekler, M41b negatif)
    ✔; M23'ten ayrılır (M23 kontrol 1'in partition'ı) ✔ — koşulu (1) çözülürse.

HÜKÜM: **MAJÖR.** Onarım (üçü birden):
  (a) M22'nin test-only middleware pini M41b'ye de yazılır (istekler gerçek,
      birbirinden farklı IP taşısın) — böylece "aynı IP" ifadesi anlam kazanır
      ve `null` kaynaklı `500` yolu kapanır;
  (b) mutasyon biçimi PİNLENİR: "anahtar `ctx.Connection.RemoteIpAddress` **ile
      kurulur ve NULL-güvenlidir**" (M32/M40'ın "MUTASYON BİÇİMİ PİNLİ"
      emsali);
  (c) satır 891'e M41'in önkoşulu ("`[KS-10]`'un kovası boştur — §3.2(9)")
      eklenir; ayrıca assert "`429` almaz" yerine "**`200`/`401` alır** (yani
      `429` DEĞİL)" diye POZİTİFLEŞTİRİLİR ⇒ `500` yolu artık sinyali
      maskeleyemez.

ÖLÇÜM KOMUTU:
  # mutasyon: kontrol 2'nin anahtarı IP'ye çevrilir
  for i in $(seq 1 6); do
    curl -s -o /dev/null -w "%{http_code} " -X POST localhost:8080/v1/auth/login \
      -H 'Content-Type: application/json' \
      -d "{\"email\":\"rnd$i@x.com\",\"password\":\"<15+>\"}"
  done; echo
  # BASELINE          : 401 401 401 401 401 401     (429 YOK)  ⇒ assert PASS
  # MUTASYON (null-güvenli): … 429                  ⇒ assert FAIL ⇒ ölür ✔
  # MUTASYON (null-güvensiz): … 500                 ⇒ assert PASS ⇒ KÖR KAPI ✘
  # farkı ayırt etmek için:
  grep -rn 'RemoteIpAddress' src/backend/ tests/


################################################################################
BÖLÜM C — TEMİZ ÇIKAN MUTANTLAR (ÜÇ SORUNUN ÜÇÜ DE OLUMLU)
################################################################################
Aşağıdaki 18 satır için ÜÇ SORU da olumlu yanıtlandı. Her biri için mutasyonun
ısırdığı yol, baseline'ın neden yeşil doğduğu ve hangi kardeş mutasyondan
neyle ayrıldığı ayrı ayrı gösterildi. (Küçük gözlemler BÖLÜM D'de.)

--------------------------------------------------------------------------------
M24 (satır 865) — `ICurrentUser` `"sub"` yerine `ClaimTypes.NameIdentifier`
--------------------------------------------------------------------------------
KILL: *"`MapInboundClaims=false` altında geçerli token ile korumalı uç `200`
döner"* FAIL · TS · Çıpa: K3-C7'nin ölçülmüş yan etkisi · [çıpa+] **K3-D2**
(1) ÖLÜR ✔ — K3-C7 tablosu (satır 382) `MapInboundClaims=false` PİNLİ; satır
    392-394'ün kaynaktan ölçümü birebir: "`ClaimTypeMapping.InboundClaimTypeMap`
    … `false` yapıldığında **çeviri hiç koşmaz** ⇒ `ClaimTypes.NameIdentifier`
    **DOLMAZ**" ⇒ mutasyon altında **her istekte** `UnauthenticatedException`
    ⇒ yanıt `200` DEĞİL ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — §3.2(2) sahte `TestAuthHandler`'ı YASAKLAR ve satırın
    kendi [Ma-14] PİN'i bunu tekrar eder ("test **gerçek `AddJwtBearer` boru
    hattından** geçer"); §3.2(10) kök anahtarı test yapılandırmasından verir ⇒
    `Program` boot eder. §3.2(2)'nin ikinci fıkrası `FakeCurrentUser` ile DI
    ikamesini de yasakladığı için delik adıyla değil KAPALI kapanmıştır.
(3) AYIRT EDİCİ ✔ (kabul edilmiş tek istisnayla) — M16/M48 bozuk token'larla
    koşar, M24 geçerli token kullanır; M14/M33a daha GEVŞEK davranış üretir ⇒
    `200` bozulmaz; M15 (`Guid.Empty`) altında korumalı uç yine `200` döner.
    **Bilinen tek örtüşme M19'dur** ve satır 861 bunu açıkça adlandırır:
    "**YAN ISIRMA — ADLANDIRILIYOR:** host kurulamadığı için `TS` seviyesindeki
    **diğer tüm testler de düşer** … KANIT'a *"YAN ISIRMA: tüm `TS` süiti"*
    diye **YAZILMAK ZORUNDADIR**". Beyanlı olduğu için gizli ayırt-edicilik
    kaybı sayılmaz.
ÇIPA DOĞRU MU? ✔ — [çıpa+] K3-D2 birebir "`ClaimTypes.NameIdentifier`
**okunmaz**" der; test edilen karar tam olarak budur.
ÖLÇÜM: dotnet test --filter ~CurrentUser_reads_sub_claim ; ayrıca
  grep -rn 'MapInboundClaims' src/backend/   # false olmalı
  grep -rn 'TestAuthHandler\|FakeCurrentUser' tests/Momentum.Api.Tests/  # 0 olmalı

--------------------------------------------------------------------------------
M25 (satır 866) — Double-submit CSRF doğrulaması TAMAMEN kaldırılır
--------------------------------------------------------------------------------
KILL: *"geçerli yenileme çerezi + **EKSİK** `X-CSRF-Token` ile `/refresh`
reddedilir"* FAIL · TS · Çıpa: K3-L3(3)
(1) ÖLÜR ✔ — doğrulama tümüyle kalkınca başlıksız istek KABUL edilir ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — K3-L3(3) gereği çerez==başlık + HMAC beklenir; başlık
    yoksa reddedilir. CSRF yüzeyi satır 692 gereği yalnız `/refresh`'tir ⇒ uç
    seçimi doğru.
(3) AYIRT EDİCİ ✔ — M35'in mutasyonu (yalnız HMAC kalkar) altında EKSİK başlık
    hâlâ reddedilir (çerez ≠ yok) ⇒ M25'in assert'i yeşil kalır. M53'ün
    mutasyonu (CSRF sırası) altında da istek yine reddedilir (yalnız tüketim
    önce olur) ⇒ yeşil kalır. ⇒ M25'i yalnız kendi mutasyonu ısırır.
    M25 ⊂ M35 yönünde tek-yönlü kapsama vardır (M25'in mutasyonu M35'in
    ayaklarını da kırar) — bu KUSUR DEĞİLDİR: M25 strikt üst-küme mutasyondur,
    M35 onun rafine hâlidir ve satır 877 bu ilişkiyi zaten gerekçelendirir
    ("v2'nin M25'i naif implementasyonda da **yeşil geçerdi** = kör kapı").
ÖLÇÜM:
  curl -s -o /dev/null -w '%{http_code}\n' -b jar.txt -X POST \
    localhost:8080/v1/auth/refresh -H 'X-Client-Kind: web'   # başlıksız
  # BASELINE 403 · MUTASYON 200 ⇒ ölür

--------------------------------------------------------------------------------
M26 (satır 867) — `/refresh` yükleminden `revoked_at IS NULL` çıkarılır
--------------------------------------------------------------------------------
KILL: *"`/logout` sonrası aynı yenileme token'ı ile `/refresh` **`401`** alır"*
FAIL · TC · Çıpa: [bloker #2]
(1) ÖLÜR ✔ — K3-C6(1)'in `UPDATE`'inden `revoked_at IS NULL` çıkarılırsa,
    `/logout` ile iptal edilmiş ama TÜKETİLMEMİŞ ve süresi DOLMAMIŞ token
    yüklemi sağlar ⇒ 1 satır güncellenir ⇒ `200` ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — `UPDATE` 0 satır ⇒ dal (b) (`revoked_at IS NOT NULL`)
    ⇒ `401`, aile iptali YOK. Satır 273'ün "v2'de bu uç FİİLEN NO-OP'TU"
    kaydıyla birebir tutarlı.
(3) AYIRT EDİCİ ✔ — M44 (dal sırası) altında M26'nın testi: token tüketilmemiş
    olduğu için dal (c) hiç konu değildir ⇒ yine `401` ⇒ yeşil kalır. M18
    (logout kapsamı) altında da yeşil kalır. Ters yönde: M26'nın mutasyonu
    M44'ün testini kırmaz (M44 TÜKETİLMİŞ token sunar ⇒ `consumed_at IS NULL`
    yüklemi zaten düşer) ⇒ iki yönlü ayrım tamdır. Satır 867'nin kendi cümlesi
    de bunu doğrular: "bu ayak olmadan **`/logout` fiilen no-op'tur** ve M18
    yine de yeşil kalır".
ÖLÇÜM:
  # login → logout → aynı refresh token ile /refresh
  # BASELINE 401 · MUTASYON 200 ⇒ ölür; ayrıca
  psql -c "select revoked_at, revoked_reason from refresh_tokens where family_id='<fid>';"

--------------------------------------------------------------------------------
M27 (satır 868) — `/refresh` yükleminden `expires_at > @now` çıkarılır
--------------------------------------------------------------------------------
KILL: `FakeTimeProvider` `[KS-2]`'yi AŞACAK KADAR ileri alınır: *"süresi geçmiş
yenileme token'ı `401` alır **ve aile iptal edilmez**"* FAIL · TC · [bloker #2]
(1) ÖLÜR ✔ — yüklemden `expires_at > @now` çıkınca süresi dolmuş token
    güncellenir ⇒ `200` ⇒ assert FAIL.
(2) BASELINE YEŞİL ✔ — `UPDATE` 0 satır ⇒ dal (b) ⇒ `401`, satır 310'un
    "aile iptali **YOK** — zaten ölü" kuralı gereği `revoked_reason` yazılmaz.
    Saat ilerletmesi §3.2(5)'in PAZARLIKSIZ `FakeTimeProvider` ezmesiyle
    kurulur ve §3.2(9)'un hız-sınırlayıcı istisnası burada konu değildir.
(3) AYIRT EDİCİ ✔ — M26 (revoked_at) altında süresi dolmuş token yine `401`
    alır (expires_at yüklemi durur) ⇒ yeşil kalır; M29/M30 tüketilmiş token
    senaryosudur, burada token tüketilmemiştir.
KÜÇÜK GÖZLEM (kusur değil, BÖLÜM D-7'de): "ve aile iptal edilmez" konjonksiyonu
    kendi mutasyonu altında bağımsız olarak ÖLMEZ; ancak tek assert içinde
    olduğu ve `401→200` yarısı ısırdığı için test yine kırılır ⇒ satır 833'ün
    "ayak" tanımına girmez.
ÖLÇÜM:
  # FakeTimeProvider += 31 gün, sonra /refresh
  # BASELINE 401 + revoked_reason IS NULL · MUTASYON 200 ⇒ ölür

--------------------------------------------------------------------------------
M28 (satır 869) — Web modunda yenileme token'ı yanıt GÖVDESİNE de eklenir
--------------------------------------------------------------------------------
KILL: *"`X-Client-Kind: web` ile gelen `/login` ve `/refresh` yanıtlarının **ham
gövde metni** yenileme token'ının değerini **içermez**"* FAIL · TS · [bloker #6]
(1) ÖLÜR ✔ — mutasyon gövdeye token yazar ⇒ ham metin taraması eşleşir ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — ve bu satır, belgenin en iyi kurulmuş kapılarından
    biridir: K3-C2'nin `Base64Url` pini (satır 256-257) taramayı yapısal olarak
    güvenli kılar. Birebir ölçüm: standart Base64 kullanılsaydı
    `JsonSerializer` `+` karakterini `+`'ye çevirir ve tarama "**≈%49
    olasılıkla ıskalardı**"; `Base64Url` alfabesinde `+` ve `/` **yoktur** ⇒
    JSON kaçışı devre dışı kalır. Test alan adına değil HAM GÖVDEYE bakar ⇒
    builder'ın alan adlandırması sinyali etkilemez.
(3) AYIRT EDİCİ ✔ — M43 GİRDİ kanalını ölçer (istemcinin gönderdiği), M28
    ÇIKTI kanalını (sunucunun döndürdüğü); M12 DB'yi ölçer. Üçü ayrı yüzey.
ÖLÇÜM:
  RT=$(grep __Host-mrt jar.txt | awk '{print $7}')
  curl -s -X POST localhost:8080/v1/auth/login -H 'X-Client-Kind: web' \
       -d '{"email":"a@x.com","password":"<15+>"}' | grep -c -- "$RT"
  # BASELINE 0 · MUTASYON ≥1 ⇒ ölür

--------------------------------------------------------------------------------
M30 (satır 871) — Replay'den "halef tüketilmemiş olmalı" koşulu kaldırılır
--------------------------------------------------------------------------------
KILL: `[KS-4]` PENCERESİNİN İÇİNDE kalınır (saat İLERLETİLMEZ), halef
KULLANILIR, sonra `T1` yeniden sunulur: *"**aile iptal olur**"* FAIL · TC
(1) ÖLÜR ✔ — mutasyon K3-C6(3) koşul 2'yi kaldırır. Pencere İÇİNDE olunduğu
    için koşul 1 ✔ ve koşul 3 ✔ (sır henüz ne tembel silmeyle ne süpürücüyle
    NULL'lanmıştır — yaş eşiği `[KS-4]` aşılmamıştır) ⇒ dal (c) açılır ⇒ `200`,
    aile İPTAL EDİLMEZ ⇒ assert FAIL. **M29'un düştüğü maskelenme burada
    YAPISAL OLARAK doğmaz** — çünkü pencere içinde kalınır.
(2) BASELINE YEŞİL ✔ — koşul 2 düşer ⇒ dal (d) ⇒ `reuse_detected`, aile düşer.
(3) AYIRT EDİCİ ✔ — satırın kendi PİN'i doğru ve YETERLİ: "**PİN:** pencere
    dışına çıkılırsa M29 ile **ayırt edilemez** hâle gelir ⇒ *"pencere İÇİNDE"*
    koşulu testin önkoşuludur". Ters yön de tutar: M29'un mutasyonu (koşul 1)
    M30'un testini etkilemez, çünkü M30 pencere içindedir ve koşul 1 zaten
    sağlanır ⇒ M30'un assert'i M29 altında yeşil kalır.
ÖLÇÜM:
  # saat İLERLETİLMEZ; T2 kullanılır; sonra T1 sunulur
  psql -c "select revoked_reason from refresh_tokens where family_id='<fid>' limit 1;"
  # BASELINE reuse_detected · MUTASYON NULL (+200) ⇒ ölür

--------------------------------------------------------------------------------
M32 (satır 873) — `Application`'da `Konscious.Security.Cryptography` tipi
--------------------------------------------------------------------------------
KILL: *"`Konscious.*` tipleri Domain/Application/Api'de **görünmez**"*
NetArchTest kuralı FAIL · NA · [çıpa+] K3-B2 · K3-B1
(1) ÖLÜR ✔ — satırın MUTASYON BİÇİMİ PİNİ bu kapıyı kurtaran şeydir; birebir:
    "yalnız `using` eklemek **yetmez** (kullanılmayan `using` derlemeye referans
    yazmaz ⇒ NetArchTest görmez); mutasyon **gerçek bir tip kullanımıdır**
    (ör. `Application` içinde `typeof(Argon2id)` ya da bir alan bildirimi)".
    Gerçek tip kullanımı derleme referansı doğurur ⇒ kural kırmızıya döner.
(2) BASELINE YEŞİL ✔ — `IPasswordHasher` portu Application'da, implementasyon
    Infrastructure'da (§2-M).
(3) AYIRT EDİCİ ✔ — M32b (`Microsoft.AspNetCore.*`) ve M32c
    (`Microsoft.IdentityModel.*`) farklı namespace kurallarını ısırır; üç kural
    üç ayrı assert'tir.
KÜÇÜK GÖZLEM (D-2): mutasyonun `Application.csproj`'a bir `PackageReference`
    eklemeyi de gerektirdiği yazılı değil.
ÖLÇÜM: dotnet test tests/…ArchitectureTests --filter ~Konscious_not_visible

--------------------------------------------------------------------------------
M32b (satır 886) — `Application`'da `Microsoft.AspNetCore.Http` tipi
--------------------------------------------------------------------------------
KILL: *"`Microsoft.AspNetCore.*` tipleri Domain/**Application**'da görünmez"*
NetArchTest kuralı FAIL · NA · [Ma-8]
(1) ÖLÜR ✔ — mutasyon biçimi M32'ye atıfla pinli ("gerçek tip kullanımıdır").
(2) BASELINE YEŞİL ✔ — K18-a'nın katman ayrımı gereği `ProblemDetails` çevirimi
    Api'dedir (satır 587: "Api uç delegesi: `RateLimitedException` →
    `ProblemDetails`"), Application'da `HttpContext` yoktur.
(3) AYIRT EDİCİ — ✔ ama **BEYANLI ÖRTÜŞME VAR ve bu kusur değildir**: M49
    (satır 901) aynı kuralı bilerek ısırtır ve kendi satırında bunu adlandırır
    ("Bu mutant **başka bir kapının ısırdığını** ısırtır: katman kararı bir
    kural olarak yazıldı, bu mutant o kuralın **fiilen uygulandığını**
    kanıtlar"). Örtüşme gizli değil, tasarım gereğidir. Tek eksik ASİMETRİDİR:
    M49 M32b'yi anıyor, M32b M49'u anmıyor (bkz. D-3).
ÖLÇÜM: dotnet test --filter ~AspNetCore_not_visible_in_Application

--------------------------------------------------------------------------------
M32c (satır 887) — `Application`'da `Microsoft.IdentityModel.Tokens` tipi
--------------------------------------------------------------------------------
KILL: *"`Microsoft.IdentityModel.*` tipleri Domain/**Application**'da
görünmez"* NetArchTest kuralı FAIL · NA · [Ma-8]
(1) ÖLÜR ✔ (2) BASELINE YEŞİL ✔ — `IAccessTokenIssuer` portunun izolasyonu
    §2-M'de yazılı; token üretimi Infrastructure'dadır.
(3) AYIRT EDİCİ ✔ — kendi namespace'ini ısırtır; M32/M32b farklı kurallar.
    K-H1'in "**Her** kural commit'li negatif/mutant testle ısırdığını kanıtlar"
    şartı, satır 820'nin beyanıyla birlikte bu üçlüde SAĞLANMIŞTIR.
ÖLÇÜM: dotnet test --filter ~IdentityModel_not_visible_in_Application

--------------------------------------------------------------------------------
M33b (satır 875) — `UseStaticFiles` auth middleware'inden SONRAYA alınır
--------------------------------------------------------------------------------
KILL: *"kimliksiz **`GET /main.dart.js`** `200` döner **ve gövdesi `index.html`
DEĞİLDİR**"* FAIL · TS
(1) ÖLÜR ✔ — satır 527'nin birincil-kaynak ölçümü birebir: dosya-benzeri yol
    `{*path:nonfile}` kısıtına **düşmez** ⇒ endpoint `null` kalır ⇒ statik
    middleware auth'tan sonraysa `FallbackPolicy` devreye girer ve **`401`**
    verir ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — satırın kendi ÖNKOŞULU bunu kurtarır ve B6'nın ölü
    tuzağını kapatır: "test derlemesinde `wwwroot/index.html` **ve**
    `wwwroot/main.dart.js` yer tutucu olarak bulunur (K3-J1) — yoksa baseline
    **kırmızı doğar**".
(3) AYIRT EDİCİ ✔ — İKİ YÖNLÜ doğrulandı: M33a'nın mutasyonu altında
    `/main.dart.js` üst-seviye statik middleware'den (auth'tan ÖNCE) gelir ⇒
    M33b'nin assert'i yeşil kalır. M33b'nin mutasyonu altında `/tasks` yine
    `200`+`index.html` döner (satır 525-526: `MapFallbackToFile` **kendi
    `UseStaticFiles`'ını kurar**) ⇒ M33a'nın `/tasks` ayağı yeşil kalır.
    "gövdesi `index.html` DEĞİLDİR" ikinci yarısı da gereklidir ve doğrudur:
    onsuz fallback'in döndürdüğü `index.html` de `200` sayılırdı.
ÖLÇÜM: (MAJÖR-5'in komutuyla aynı; `/main.dart.js` satırına bakılır)
  # BASELINE 200 + js gövdesi · MUTASYON 401 ⇒ ölür

--------------------------------------------------------------------------------
M35 (satır 877) — CSRF'in HMAC doğrulaması kaldırılır (yalnız çerez==başlık)
--------------------------------------------------------------------------------
KILL: İKİ AYAK + POZİTİF KONTROL · TS · [bloker #4]
  (1) *"geçerli biçimli ama İMZASIZ/YANLIŞ İMZALI bir CSRF değeri hem çerezde
      hem başlıkta gönderilirse `/refresh` **reddedilir**"* FAIL
  (2) [Ma-6 — AİLE BAĞI] *"**BAŞKA bir ailenin `family_id`'siyle DOĞRU
      İMZALANMIŞ** bir CSRF değeri … **reddedilir**"* FAIL
  "**Pozitif kontrol zorunlu:** doğru imzalı + doğru aileli değer **kabul
  edilir** (aksi hâlde *"her şeyi reddet"* mutantı da testi geçer)"
(1) ÖLÜR — **İKİ AYAK DA** ✔ (satır 833'ün sınavını GEÇEN örnek satır):
    Ayak 1: HMAC kalkınca çerez==başlık eşleşir ⇒ KABUL ⇒ FAIL ✔
    Ayak 2: aile bağı **yalnız HMAC yeniden hesabıyla** kontrol edilebilir —
    `family_id` tele hiç çıkmaz (satır 682: `value = nonce + "." +
    Base64Url(HMAC(K_csrf, nonce + "|" + family_id))`), sunucu onu SUNULAN
    yenileme token'ının ailesinden alır (satır 685) ⇒ HMAC doğrulaması
    kalkarsa aile kontrolü de zorunlu olarak kalkar ⇒ KABUL ⇒ FAIL ✔
(2) BASELINE YEŞİL ✔ — `[KS-28]` entropisi ve `Base64Url` kodlaması pinli
    (satır 683) ⇒ "geçerli biçimli ama yanlış imzalı" değer inşa edilebilir.
(3) AYIRT EDİCİ ✔ — M25'ten ayrılır (M25 EKSİK başlık ölçer; M35'in mutasyonu
    altında eksik başlık hâlâ reddedilir). M53'ten ayrılır (M53 SIRA ölçer,
    reddin kendisini değil). Pozitif kontrol, "her şeyi reddet" dejenere
    mutantını da eler — satır 833'ün (b) şıkkının doğru kullanımı.
ÖLÇÜM:
  # ayak 1: nonce doğru, imza baytı bozulmuş; çerez=başlık aynı değer
  # ayak 2: BAŞKA family_id ile doğru imzalanmış değer
  # pozitif: doğru imza + doğru aile
  # BASELINE 403/403/200 · MUTASYON 200/200/200 ⇒ iki ayak da ölür

--------------------------------------------------------------------------------
M36b (satır 879) — `__Host-mrt`'nin öznitelikleri TEK TEK düşürülür
--------------------------------------------------------------------------------
KILL: **DÖRT AYRI KOŞUM, her biri kendi mutasyonu altında:** `Secure` ·
`HttpOnly` (yalnız `__Host-mrt`) · `SameSite=Strict` · `Max-Age` (ailenin kalan
`expires_at`'ine eşit, ±2 sn) — **her biri AYRI commit** · TS
(1) ÖLÜR — DÖRDÜ DE ✔. Satır 833'ün istediği biçimin BİREBİR uygulanmış hâli
    budur ve belgedeki en temiz çok-ayaklı satırdır: her ayağın KENDİ mutasyonu
    ve KENDİ commit'i vardır ⇒ "ölmeyen ayak" sınıfı yapısal olarak doğamaz.
(2) BASELINE YEŞİL ✔ — `Max-Age` ayağı `[KS-3]`/`[KS-29]`'a ("ailenin KALAN
    `expires_at`'i") çıpalı; §3.2(5) `FakeTimeProvider` ezmesiyle ±2 sn
    toleransı fazlasıyla karşılanır. `HttpOnly` ayağının kapsamı doğru
    daraltılmış: `__Host-mct` satır 687 gereği `HttpOnly` **DEĞİLDİR**
    ("istemci okumalı") ⇒ ayak yalnız `__Host-mrt` için yazılmış.
(3) AYIRT EDİCİ ✔ — M36 adı/`Domain`'i ölçer, M36b öznitelikleri; M47 SİLME
    başlıklarını ölçer. `Path=/` ayağının DÜŞÜRÜLMESİ de doğru gerekçelidir:
    "`__Host-` öneki `Path=/`'ı tarayıcı tarafından **zorunlu** kılar ⇒ ayrı
    bir kapı değil, önekin **sonucudur**" ⇒ süs ayak bilinçle elenmiş.
ÖLÇÜM: (MAJÖR-6'nın `set-cookie` grep'i; dört commit, dört koşum)

--------------------------------------------------------------------------------
M37 (satır 880) — `/login`'in bilinmeyen-e-posta dalı FARKLI gövde/kod döndürür
--------------------------------------------------------------------------------
KILL: *"bilinmeyen e-posta + yanlış parola ile mevcut e-posta + yanlış
parolanın yanıtları, **`extensions.traceId` alanı düşürüldükten sonra**, bayt
bayt aynıdır (durum kodu dâhil)"* FAIL · TS
(1) ÖLÜR ✔ (2) BASELINE YEŞİL ✔ — traceId muafiyeti satır 204'ün birincil
    kaynak ölçümüyle (`DefaultProblemDetailsWriter` traceId'yi KOŞULSUZ yazar)
    kurulmuş; §3.2(3) özel `IProblemDetailsWriter`'ı yasakladığı için sinyal
    builder tercihine kaymaz. **Bu satır, MAJÖR-3'te (M34) eksik olan
    düzeltmenin doğru uygulanmış hâlidir.**
(3) AYIRT EDİCİ ✔ — M7 çağrı SAYISINI ölçer (yapısal ölçüt), gövdeyi değil ⇒
    M7'nin mutasyonu M37'nin assert'ini kırmaz; M34 bozuk PHC senaryosu kurar,
    M37 geçerli hash'lerle koşar.
ÖLÇÜM: (MAJÖR-3'ün `jq 'del(.extensions.traceId)'` diff'i)

--------------------------------------------------------------------------------
M38 (satır 881) — `family_id` her `/login`'de yeniden üretilmez
--------------------------------------------------------------------------------
KILL: *"aynı kullanıcının iki ardışık `/login`'i **FARKLI** `family_id`
üretir"* FAIL · TC · [D3] K3-C3'ün doğum anı
(1) ÖLÜR ✔ — kullanıcı başına sabit `family_id`'de iki giriş aynı aileyi
    verir ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — iki `/login`, `[KS-13]`=5 ve `[KS-10]`=30 tavanlarının
    ikisinin de ALTINDA kalır ⇒ hız sınırı tetiklenmez.
(3) AYIRT EDİCİ ✔ — satırın kendi gerekçesi doğru: "K3-C3'ün doğum anı v2'de
    yalnız **tesadüfen** kapsanıyordu; M18 ayırt edici değildi". M18'in
    mutasyonu (logout tüm aileleri düşürür) iki `family_id`'nin FARKLI olmasını
    etkilemez ⇒ M38 yeşil kalır. M26/M27 yüklem mutantlarıdır, doğum anını
    değiştirmez.
ÖLÇÜM:
  psql -c "select count(distinct family_id) from refresh_tokens where user_id='<uid>';"
  # BASELINE 2 · MUTASYON 1 ⇒ ölür   (ya da JWT'nin `fid` talebi karşılaştırılır)

--------------------------------------------------------------------------------
M40b (satır 889) — K3-C6(5)'in FIRSATÇI (TEMBEL) SİLME ayağı kaldırılır
--------------------------------------------------------------------------------
KILL: *"aynı ailede yeni bir `/refresh` yapıldıktan sonra, `[KS-4]`'ü aşmış
ESKİ satırın `successor_secret_enc`'i **`NULL`**'dır"* FAIL · TC · Çıpa:
K3-C6(5)
ÖNKOŞUL (birebir): "süpürücü fixture'a **KAYDEDİLMEZ** (`SweepAsync` hiç
çağrılmaz); `FakeTimeProvider` **`[KS-4]`ü aşacak kadar** ilerletilir."
(1) ÖLÜR ✔ — tembel silme kalkınca eski satırın sırrı dolu kalır; süpürücü de
    kayıtlı olmadığı için MASKELEYEMEZ ⇒ FAIL.
(2) BASELINE YEŞİL ✔ — ve bu, kapı-5 aday bloker 4'ün DOĞRU kapatılmış hâlidir:
    önkoşul **ZAMANA DEĞİL MEKANİZMAYA** çıpalı olduğu için `[KS-4]`/`[KS-6]`
    sayıları değişse bile önkoşul geçerliliğini korur. Yeni `/refresh` mümkün:
    ailenin canlı token'ı tüketilmemiştir ve `[KS-4]` (10 dk) « `[KS-2]`
    (30 gün) ⇒ `expires_at` hâlâ gelecektedir.
(3) AYIRT EDİCİ ✔ — M40'ın mutasyonu (SweepAsync no-op, servis KAYITLI) M40b'nin
    testini kıramaz, çünkü M40b'nin fixture'ında süpürücü zaten kayıtlı
    değildir ve silmeyi yapan tembel yoldur. Ters yönde M40b'nin mutasyonu
    M40'ın süpürücü testini etkilemez.
KÜÇÜK GÖZLEM (D-1): satırın parantez içi gerekçesindeki "`[KS-4]`=`[KS-6]`
    olduğu için" ifadesi BUGÜN YANLIŞTIR (600 sn ≠ 60 sn) — ama bu bir GEREKÇE
    bayatlığıdır; mekanizmaya çıpalı önkoşulun kendisi doğru ve yeterlidir.
ÖLÇÜM:
  psql -c "select id, consumed_at, successor_secret_enc is null as sec_null
           from refresh_tokens where family_id='<fid>' order by created_at;"
  # BASELINE eski satır sec_null=true · MUTASYON false ⇒ ölür

--------------------------------------------------------------------------------
M41 (satır 890) — Kontrol 2 (e-posta penceresi, Application handler) kaldırılır
--------------------------------------------------------------------------------
KILL: **BU MUTASYONUN ISIRDIĞI TEK AYAK:** *"**aynı** normalize e-posta ile
`[KS-13]`'ün tavanını aşan `/login` denemesi `429` alır **ve** `limit ==
"email"` **ve** `Retry-After` taşır"* FAIL · TS · Çıpa: K3-J2(2)
(1) ÖLÜR ✔ — kontrol 2 kalkınca aynı e-postayla `[KS-13]`+1 deneme `429`
    ALMAZ ⇒ üç konjonksiyon birden düşer.
(2) BASELINE YEŞİL ✔ — satırın önkoşulu DOĞRU yazılmış: "`[KS-10]`'un kovası
    boştur (§3.2(9) izolasyonu)". `Retry-After` ayağı da havada değil, ÖLÇÜLMÜŞ:
    satır 936 birebir "`FixedWindowLease` `MetadataName.RetryAfter` **taşıyor**,
    `ConcurrencyLease` **taşımıyor** ⇒ koşulluluk kalktı".
(3) AYIRT EDİCİ ✔ — M11 (kontrol 1) altında M41'in testi yine kontrol 2'den
    `429` alır ⇒ yeşil kalır. M22 (kontrol 3) eşzamanlılık ister; M41 ardışık
    isteklerle koşar. M41b negatif assert'tir. M49 `ProblemDetails` çevirimini
    taşır ama `limit` uzantısını korur.
    **Satırın kendi tarihi de doğrudur:** v4'ün ölmeyen ikinci ayağı bu satırdan
    ÇIKARILMIŞ ve M41b'ye taşınmıştır ⇒ satır 833'ün sınavını GEÇER.
KUSUR SATIRDA DEĞİL §2-J'DEDİR — bkz. MAJÖR-9.
ÖLÇÜM:
  for i in $(seq 1 6); do curl -s -o /tmp/p -w "%{http_code} " -X POST \
    localhost:8080/v1/auth/login -d '{"email":"same@x.com","password":"<15+>"}'; done
  jq -r '.extensions.limit' /tmp/p ; grep -i '^retry-after' /tmp/headers
  # BASELINE …429 + limit=email + Retry-After · MUTASYON hiç 429 yok ⇒ ölür

--------------------------------------------------------------------------------
M42b (satır 899) — `info` etiketi/bayt kodlaması değiştirilir
--------------------------------------------------------------------------------
KILL: *"KANIT'a yazılı **pinli kök anahtardan** türetilen `K_jwt` · `K_csrf` ·
`K_rt`, KANIT'taki **ALTIN VEKTÖR hex'lerine BİREBİR eşittir**"* FAIL · B
(1) ÖLÜR ✔ — `v1`→`v2` ya da ASCII→UTF-16 kodlama değişimi HKDF girdisini
    değiştirir ⇒ üç alt anahtar da farklı çıkar ⇒ vektör tutmaz.
(2) BASELINE YEŞİL ✔ — üç pin birden bunu mümkün kılar: `info` etiketleri
    birebir yazılı (satır 431-433), bayt kodlaması pinli (satır 440: "US-ASCII
    … sondaki `NUL` yoktur, BOM yoktur"), `HKDF.Expand` çağrısı pinli
    (satır 435). `B` seviyesi ⇒ DB/konteyner gerekmez, deterministik.
(3) AYIRT EDİCİ ✔ — ve bu, kapı-5 B5-12'nin DOĞRU onardığı yerdir: eski
    "restart sonrası 401" sinyali ayırt edici DEĞİLDİ ("`info` mutasyonu
    statiktir … restart öncesi ve sonrası **aynı** anahtar türetilir"); altın
    vektör tek derlemede ısırır. M55'ten de ayrılır: M55 etiketlerin
    ÇAKIŞMASINI (ikişer ikişer FARKLI), M42b tek tek DEĞERİ ölçer.
KUSUR SATIRDA DEĞİL §2-I SATIR 442'DEDİR — bkz. MAJÖR-8.
ÖLÇÜM:
  dotnet test --filter ~Hkdf_golden_vectors
  # KANIT/adr-0003/hkdf-altin-vektor.txt içindeki üç hex ile birebir karşılaştırma

--------------------------------------------------------------------------------
M42c (satır 900) — Alt anahtarlar her açılışta yeniden üretilir (efemer)
--------------------------------------------------------------------------------
KILL: *"konteyner `docker compose restart` edildikten sonra, restart ÖNCESİ
alınmış `__Host-mct` + `__Host-mrt` çiftiyle yapılan `/refresh` **`200`**
döner"* FAIL · KON
GÖZLEM YÜZEYİ (birebir, beş adım): "(1) `docker compose up -d`; (2) `/login`
ile çerez çifti alınır ve **dosyaya yazılır**; (3) `docker compose restart api`;
(4) `docker compose exec api ls -l ./.secrets/momentum-master.key` ⇒ **dosya
VARDIR ve mtime restart'tan ÖNCEDİR**; (5) **aynı** çerez çiftiyle `/refresh`
⇒ **`200`**."
(1) ÖLÜR ✔ — efemer mutantta (4) dosyayı bulamaz ve (5) `401` döner.
(2) BASELINE YEŞİL ✔ — K22-a ile `Dockerfile` + giriş betiği + compose `api`
    servisi BU DİLİMİN İŞİ ilan edildi (satır 476) ⇒ `KON` artık var olan bir
    artefakta çıpalı. §3.2(10)'un test-anahtarı kararı bu satırı ETKİLEMEZ ve
    bu açıkça yazılmış (satır 1022: "M42c ise **`KON` seviyesindedir** — gerçek
    imajda koşar, fixture'ı hiç görmez").
(3) AYIRT EDİCİ ✔ — **ve ayırt ediciliği taşıyan şey doğru teşhis edilmiş**:
    "`KON` seviyesinde tek başına bir `401`, onlarca başka sebeple de gelebilir
    (ağ, migration, port çakışması, sağlıksız konteyner) ⇒ sinyali **ayırt
    edici kılan şey dosya sistemi ayağıdır**". M8b'den ayrılır (M8b `Production`
    yolunda dosyanın OLUŞMAMASINI ister; M42c `Development` yolunda dosyanın
    KALICILIĞINI).
    ⚠ M42 ile örtüşme VARDIR — ama kusur M42'dedir (BLOKER-1), M42c'de değil:
    M42c strikt üst-kümedir ve gözlem yüzeyi pinlidir.
ÖLÇÜM: (BLOKER-1'in komut bloğu + adım 4)
  docker compose exec api ls -l --time-style=full-iso ./.secrets/momentum-master.key
  # BASELINE: dosya VAR, mtime < restart anı · MUTASYON: dosya YOK ⇒ ölür


################################################################################
BÖLÜM D — MİNÖR GÖZLEMLER (BLOKER/MAJÖR DEĞİL; KAYDA GEÇİYOR)
################################################################################
D-1  M40b (889) — gerekçedeki "`[KS-4]`=`[KS-6]` olduğu için süpürme turu da
     hak ediliyor" ifadesi bugün YANLIŞ (600 sn ≠ 60 sn); K28-a sonrası
     bayatlamış. Mekanizmaya çıpalı önkoşul doğru olduğu için hüküm değişmez.
     Onarım: parantez, "`[KS-4]` aşıldığında süpürücü **birden çok tur** hak
     eder" diye güncellenir. (MAJÖR-2 ile aynı kök neden.)

D-2  M32 · M32b · M32c — mutasyon biçimi "gerçek tip kullanımı" diye pinli, ama
     mutasyonun `Momentum.Application.csproj`'a bir `PackageReference`
     (Konscious, Microsoft.IdentityModel.Tokens) ya da `FrameworkReference`
     (`Microsoft.AspNetCore.App`) eklemeyi de gerektirdiği YAZILI DEĞİL. Bu
     eklenmezse mutasyon **derlenmez** ve M4'ün "İKİ AYRI COMMİT" sınıfına
     düşer. Onarım: satırlara "mutasyon, gerekli proje referansını da ekler"
     cümlesi.

D-3  M32b (886) — M49 (901) aynı NetArchTest kuralını bilerek ısırtır ve bunu
     KENDİ satırında adlandırır; M32b'nin satırı M49'u ANMAZ. Asimetri, "kaç
     test kırıldı" muhasebesini M19'un talep ettiği disiplinden uzaklaştırır.
     Onarım: M32b'ye "**bu kuralı M49 da ısırtır — beyanlıdır**" notu.

D-4  M25 (866) — çıpa hücresi `K3-L3(3)` (İMZALI ikinci hat). Testin ölçtüğü
     şey ise double-submit'in VARLIĞIDIR (eksik başlık reddi). Doğru çıpa
     `K3-L3` (üç ayağın bütünü) ya da `K3-L3(1)/(3)`'ün ayrımı olurdu; L3(3)
     M35'in çıpasıdır. §3.1'in tarama aracı yalnız etiketin varlığını ölçtüğü
     için bu ayrımı göremez (satır 956'nın kendi beyan ettiği sınır).

D-5  M33a (874) — `wwwroot/index.html` yer tutucu önkoşulu YALNIZ M33b'nin
     satırında yazılı, oysa M33a'nın "gövdesi `index.html`'dir" ayağı da ona
     bağlı. Onarım: önkoşul iki satırda da anılır (ya da §3.2'ye madde olur).

D-6  M31 (872) — "tam `[KS-17]` karakterlik parola" ayağının seçilen parolasının
     `[KS-31]` kara listesinde (K3-B6(2)) OLMAMASI gerektiği pinli değil;
     listede bir parola seçilirse baseline `400` verir. M56 satırı bu pini
     kendi ayağı için yazmış ("listede olmayan, `[KS-17]` uzunluğunu sağlayan
     bir parola"), M31'e taşınmamış.

D-7  M27 (868) — "ve aile iptal edilmez" konjonksiyonu kendi mutasyonu altında
     bağımsız olarak ölmez; tek assert içinde olduğu için testi yine de kırar
     ⇒ satır 833'ün "ayak" tanımına girmediği kanaatindeyim. Yine de daha
     temizi: bu yarıyı `değişmez` etiketiyle ayırmak (dal (b)'nin "aile iptali
     YOK" kuralının başka kapısı yoktur).

D-8  ÇIPA HÜCRESİNDE K3-* KARAR KİMLİĞİ OLMAYAN SATIRLAR (payımda):
     M26 · M27 (yalnız "[bloker #2]") · M28 ("[bloker #6]") · M30 ("[K14-a +
     Ma-16]") · M33a ("[bloker #3 / K14-e / B6]") · M42 (K3-I3 var ama YANLIŞ
     karar). Bunlar §3.1'in aracının K3-* kapsamasını ölçme biçimiyle uyumsuz
     değildir (araç kararlardan satırlara bakar), ama ters yönde okunabilirlik
     kaybıdır: bir satırın hangi kararı ısırttığı bloker numarasından değil,
     karar kimliğinden okunmalıdır.

D-9  KAPSAM DIŞI AMA AYNI SINIF (payım dışında, kayda geçiriyorum — payı 3'ün
     denetçisi doğrulamalıdır): **M52** (904) birinci ayağı ("tam `[KS-19]`
     uzunluğunda geçerli e-posta `201`") ve **M56** (908) ikinci ayağı
     ("listede olmayan … parola `201`") kendi mutasyonları altında ÖLMEZ —
     MAJÖR-4'te (M31 ayak 2) teşhis edilen SÜS AYAK sınıfının birebir
     kardeşleri. M56 bunu kendi satırında GEREKÇELENDİRİR ("İkinci ayak
     zorunludur: tek ayakla *"her şeyi reddet"* mutasyonu da testi geçerdi")
     ama satır 833'ün istediği `önkoşul`/`değişmez` ADLANDIRMASINI yapmaz.
     ⇒ Satır 833 kuralının belge genelinde tutarlı uygulanmadığına dair üçüncü
     kanıt.


################################################################################
BÖLÜM E — HÜKÜM ÖZETİ
################################################################################
İNCELENEN: 27 satır (M24 → M42c)

BLOKER (2) — KÖR KAPI:
  B1 · M42   (satır 892) — türetme yarısı için kill sinyali kör (kök anahtar
                           kalıcı ⇒ restart testi mutasyon altında da 200);
                           mutasyon hâlâ bileşik; M42c ile ayırt edilemez;
                           çıpa K3-I3 yazıyor, ısırtılması gereken karar K3-I4.
  B2 · M29   (satır 870) — önkoşul pinlenmemiş; K3-C6(5)(1)'in tembel silmesi
                           (ve/veya süpürücü) koşul 3'ü düşürüp dal (d)'yi
                           zaten açar ⇒ mutasyon altında assert yeşil kalır.
                           Ek: tembel silme ile dal değerlendirmesinin SIRASI
                           belgede hiç yazılı değil.

MAJÖR (10):
  M1 · M40 ayak 2 (888)        — ÖLÜ TUZAK ([KS-6] ilerletmesi yaş eşiği
                                 [KS-4]'ü aşmaz) + periyot ölçümünün mekanizması
                                 pinlenmemiş (K3-C6(5) tersini söylüyor).
  M2 · satır 106-107           — "[KS-4]+[KS-6] = 120 sn" YANLIŞ (=660 sn);
                                 satır 365 aynı literali geri çekmiş, kopya
                                 kanonik tabloda duruyor.
  M3 · M34 ayak 2 (876)        — ÖLÜ TUZAK; traceId muafiyeti yok (M37'de var).
  M4 · M31 ayak 2 (872)        — SÜS AYAK; satır 833 ihlali.
  M5 · M33a "GET /" (874)      — SÜS AYAK (UseDefaultFiles+UseStaticFiles
                                 auth'tan önce); §2-J satır 533 ile çelişik.
  M6 · M36 (878)               — "veya"lı mutasyon + iki ayak: her dalda bir
                                 ayak ölmüyor; satır 833 ihlali (M36b doğru).
  M7 · M39 (882)               — AYIRT EDİCİ DEĞİL; taşıyıcı değişmez M45'in
                                 kill sinyaliyle birebir aynı.
  M8 · §2-I satır 442          — M42b'nin GERİ ÇEKİLMİŞ kill sinyali yürürlükte.
  M9 · §2-J satır 567-570      — M41'in bayat iki-ayaklı sinyali + yanlış
                                 önkoşul ("K16-b sayıları zaten sağlar");
                                 M41b §2'de hiç geçmiyor; "6." literali.
  M10· M41b (891)              — mutasyon biçimi pinlenmemiş; TestHost'ta
                                 `RemoteIpAddress` null ⇒ negatif assert
                                 500 yoluyla maskelenebilir (kör kapı riski);
                                 M22'nin test-only middleware pini yok.

TEMİZ (18): M24 · M25 · M26 · M27 · M28 · M30 · M32 · M32b · M32c · M33b ·
            M35 · M36b · M37 · M38 · M40b · M41 · M42b · M42c
            (M41 ve M42b'nin TABLO satırları temizdir; kusurları §2'deki bayat
             kopyalardadır — MAJÖR-9 ve MAJÖR-8.)

KESİŞEN KÖK NEDEN [EN ÖNEMLİ YAPISAL BULGU]:
  MAJÖR-1 · MAJÖR-2 · D-1 aynı tek olaydan doğuyor: **K28-a'nın `[KS-4]`'ü
  60 sn'den 10 dk'ya çıkarması, ona bağlı ÜÇ AYRI YERDE yeniden ölçülmedi.**
  Belge bu kusur sınıfını satır 365'te KENDİ ADLANDIRIYOR ("Sayı düştü, atıf
  kaldı") ve satır 952'de kural hâline getiriyor ("bir sayı, onu üreten ölçüm
  aracı değiştiğinde YENİDEN ÖLÇÜLÜR") — ama kuralı kendi `[KS-4]`
  değişikliğine uygulamamış. ⇒ ÖNERİ: kanonik bir sayı değiştiğinde o sayıya
  atıf yapan TÜM satırları listeleyen bir tarama, `araclar/adr-kapi-taramasi.py`
  ya da kardeş bir betik olarak KOŞULMALIDIR (bugünkü araç yalnız K3-* etiket
  varlığını ölçüyor — satır 956'nın beyan ettiği sınır tam olarak burada
  ısırıyor).

  MAJÖR-4 · MAJÖR-5 · MAJÖR-6 · D-9 ikinci bir kök nedeni paylaşıyor: **satır
  833'ün PAZARLIKSIZ kuralı §3 tablosunda TUTARSIZ uygulanmış.** Doğru
  uygulanmış örnekler belgenin içindedir (M36b'nin "DÖRT AYRI KOŞUM", M35'in
  ayrılmış "Pozitif kontrol") ⇒ onarım yeni bir doktrin gerektirmez, mevcut
  emsalin M31/M33a/M36'ya (ve payım dışında M52/M56'ya) TAŞINMASINI gerektirir.

  MAJÖR-8 · MAJÖR-9 üçüncü bir kök neden: **§3'te onarılan kill sinyalleri
  §2'ye taşınmıyor.** İki belgede iki farklı sinyal yaşıyor ve builder §2'yi
  okuyabilir. ⇒ ÖNERİ: her mutant numarası için §2 ve §3'teki sinyal metinlerini
  karşılaştıran bir tutarlılık kontrolü (grep tabanlı, ucuz).

KİLİT HÜKMÜ (bu payın kapsamı için): **KİLİTLENEMEZ.**
2 bloker kapanmadan ve satır 833'ün üç ihlali (M31 · M33a · M36) onarılmadan
tablo "KÖR KAPI YOK" başlığını taşıyamaz.

================================================================================
DÜRÜSTLÜK BEYANI
================================================================================
• Hiçbir mutant FİİLEN KOŞULMADI — `slice-3c-auth` kodu henüz yazılmadı
  (belgenin kendi ölçümü). Hükümler belgenin PİNLERİ + belgenin kendi
  birincil-kaynak ölçümleri üzerinden türetildi; her hükmün yanına onu
  çürütecek/doğrulayacak somut komut yazıldı.
• [ŞÜPHELİ] olarak işaretlenen tek kalem MAJÖR-10'un (1) sorusudur: M41b'nin
  ölüp ölmediği, pinlenmemiş mutasyon yazımına bağlıdır ve iki yazımdan biri
  kör kapı üretir. "Ölmüyor" diye kesin hüküm yazılmadı; "pinlenmediği için
  ölümü garanti değil" diye yazıldı.
• BLOKER-2'nin (M29) tembel-silme maskelemesi, K3-C6(5)(1)'in "her `/refresh`
  işleminde" ifadesinin lafzına dayanır. Eğer builder tembel silmeyi dal
  değerlendirmesinden SONRA koşarsa mutant ölür — ama belge bu sırayı HİÇ
  YAZMADIĞI için bugün bir yazı-turadır ve bir kapı yazı-turaya bırakılamaz.
  Bu, bulgunun BLOKER sayılma gerekçesidir.
• Payımın dışındaki mutantlara (M1–M23, M43–M56) hüküm YAZILMADI; yalnız
  ayırt-edicilik zorunlu kıldığında (M45 · M49 · M55 · M52 · M56) atıf yapıldı.
================================================================================
