--- /tmp/pre_m129.dart	2026-08-01 22:40:54.167426900 +0300
+++ src/client/lib/veri/ayarlari_hazirla.dart	2026-08-01 22:43:00.920335300 +0300
@@ -26,10 +26,8 @@
         'GUID olmalı -- backend Guid.TryParse kullanır, aksi halde her istek 401');
   }
   await db.transaction(() async {
-    // d [ONUR KİLİDİ]
+    // d [ONUR KİLİDİ] -- [MUTANT M134] gorevler+senkronKuyrugu silme satirlari cikarildi
     await deposu.devUserIdDegistir(ezme);
-    await db.delete(db.gorevler).go();
-    await db.delete(db.senkronKuyrugu).go();
   });
   return deposu.yukleVeyaOlustur(); // e
 }
