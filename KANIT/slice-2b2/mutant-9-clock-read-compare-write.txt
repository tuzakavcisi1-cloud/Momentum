==== MUTANT-9: mutant-9-clock-read-compare-write ====
Kural: GOREV-slice-2b2-gercek-zaman.md D9 madde 9 -- D0-b'yi kirmasi zorunlu (v3 duzeltmesi: yaris
sirasi conn1=H_high (commit YOK) / conn2=H_low, TERS sira degil -- v2'nin sirasi ısırmazdı).
Dosya: src/backend/Momentum.Infrastructure/Sync/ClientClock.cs (UpsertGreatestAsync)
Komut: dotnet build (once, 0/0) + dotnet test tests/Momentum.Persistence.Tests/Momentum.Persistence.Tests.csproj --nologo --blame-hang-timeout 120s

--- (a) UYGULANAN MUTASYONUN TAM DIFF'I ---
diff --git a/src/backend/Momentum.Infrastructure/Sync/ClientClock.cs b/src/backend/Momentum.Infrastructure/Sync/ClientClock.cs
index d6ba595..413afa8 100644
--- a/src/backend/Momentum.Infrastructure/Sync/ClientClock.cs
+++ b/src/backend/Momentum.Infrastructure/Sync/ClientClock.cs
@@ -35,12 +35,24 @@ public sealed class ClientClock(SyncDbContext db) : IClientClock

     public async Task UpsertGreatestAsync(Guid clientId, Hlc effectiveOpHlc, CancellationToken cancellationToken)
     {
-        await using var command = await db.CreateRawCommandAsync(
+        Hlc? current = null;
+        await using (var select = await db.CreateRawCommandAsync("SELECT hlc FROM sync_client_clock WHERE client_id = @c", cancellationToken))
+        {
+            select.Parameters.AddWithValue("c", clientId);
+            if (await select.ExecuteScalarAsync(cancellationToken) is string encoded && Hlc.TryParse(encoded, out var hlc))
+            {
+                current = hlc;
+            }
+        }
+
+        var winner = current is { } existing && existing > effectiveOpHlc ? existing : effectiveOpHlc;
+
+        await using var upsert = await db.CreateRawCommandAsync(
             "INSERT INTO sync_client_clock (client_id, hlc) VALUES (@c, @h) " +
-            "ON CONFLICT (client_id) DO UPDATE SET hlc = GREATEST(excluded.hlc, sync_client_clock.hlc)",
+            "ON CONFLICT (client_id) DO UPDATE SET hlc = @h",
             cancellationToken);
-        command.Parameters.AddWithValue("c", clientId);
-        command.Parameters.AddWithValue("h", effectiveOpHlc.Encode());
-        await command.ExecuteNonQueryAsync(cancellationToken);
+        upsert.Parameters.AddWithValue("c", clientId);
+        upsert.Parameters.AddWithValue("h", winner.Encode());
+        await upsert.ExecuteNonQueryAsync(cancellationToken);
     }
 }

(SQL-disi bir "oku, C#'ta karsilastir, sonra KOSULSUZ upsert et" kalibi; upsert'in kendisi hala
 ON CONFLICT DO UPDATE kullanir ama SET degeri artik SQL GREATEST degil, ONCEDEN C#'ta karara
 baglanmis "winner" degeridir -- boylece bos/bayat okuma sonucu dogrudan yazilabilir.)

--- (b) KIRILAN TUM TESTLER (gercek koşum ciktisi, 31/32 yesil / 1/32 FAIL) ---
1) Momentum.Persistence.Tests.ClientClockGateTests.Client_clock_greatest_upsert_is_atomic_under_concurrent_writers  [FAIL] -- HEDEF
   Assertion (satir 86): (await app.GetClientClockAsync(client)).ShouldBe(high);
   Shouldly.ShouldAssertException:
     should be   Hlc { WallMs = 1700000010000 (high), ... }
     but was     Hlc { WallMs = 1700000001000 (low), ... }
   Mekanizma (dogrulandi, spekulasyon degil): conn1 (H_high) once INSERT eder, COMMIT ETMEZ. conn2
   (H_low) SELECT'i READ COMMITTED altinda conn1'in commit'lenmemis satirini GORMEZ -> current=null okur
   -> winner=H_low'a karar verir -> INSERT ... ON CONFLICT DO UPDATE SET hlc=H_low denemesi conn1'in
   commit'lenmemis satirina CARPISIR ve conn1 commit edene kadar BLOKE OLUR (pg_locks WHERE NOT granted
   ile dogrulandi, v4 KB-D interleaving pini). conn1 commit edince conn2'nin bekleyen upsert'i cozulur ve
   -- CONFLICT VAR oldugundan -- DO UPDATE SET hlc=H_low KOSULSUZCA calisir: conn2'nin BAYAT kararindan
   dogan H_low, conn1'in H_high'ini EZER. Klasik kayip guncelleme.

Diger 31 test YESIL kaldi (bu mutasyon yalniz eszamanli/yaris senaryosunda gozlemlenir; D0'in
tek-thread'li adim-3/4'u -- ayni sirali okuma-yazma icinde calistigindan -- etkilenmez).

--- (c) GIT CHECKOUT SONRASI YESIL KOSUM ---
git checkout -- src/backend/Momentum.Infrastructure/Sync/ClientClock.cs
dotnet test tests/Momentum.Persistence.Tests/Momentum.Persistence.Tests.csproj --filter "FullyQualifiedName~Client_clock_greatest_upsert_is_atomic" --nologo --blame-hang-timeout 120s
=> Basarili! - Basarisiz: 0, Basarili: 1, Atlanan: 0, Toplam: 1

SONUC: ISIRDI (D0-b, hedef), dogru yaris sirasiyla (v2'nin ters sirasi degil). Kor kapi degil.
