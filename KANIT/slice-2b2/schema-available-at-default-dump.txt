==== SCHEMA KANITI — outbox_messages.available_at Default sutunu BOS (D6-1 / kabul-kriteri 5b) ====
Yontem: throwaway postgres:17-alpine container + `dotnet ef database update` (InitialSync +
DispatcherIndexes) + `psql \d outbox_messages` (Testcontainers'in kullandigi migration zinciriyle
BIREBIR ayni iki migration; Cowork kendi Testcontainers kosusuyla yeniden dogrulayabilir).

$ dotnet ef database update --project src/backend/Momentum.Infrastructure --startup-project src/backend/Momentum.Infrastructure
Applying migration '20260718202450_InitialSync'.
Applying migration '20260719072330_DispatcherIndexes'.
Done.

$ psql -U postgres -d momentum -c '\d outbox_messages'
                                 Table "public.outbox_messages"
     Column     |           Type           | Collation | Nullable |           Default
----------------+--------------------------+-----------+----------+------------------------------
 id             | uuid                     |           | not null |
 aggregate_type | text                     |           | not null |
 aggregate_id   | uuid                     |           | not null |
 operation_id   | uuid                     |           | not null |
 owner_id       | uuid                     |           | not null |
 scope_id       | uuid                     |           |          |
 old_scope_id   | uuid                     |           |          |
 actor_id       | uuid                     |           | not null |
 event_type     | text                     |           | not null |
 payload        | jsonb                    |           | not null |
 hlc            | text                     | C         | not null |
 occurred_at    | timestamp with time zone |           | not null |
 signaled_at    | timestamp with time zone |           |          |
 attempts       | integer                  |           | not null | 0
 available_at   | timestamp with time zone |           | not null |               <-- DEFAULT BOS (was `now()` before D6-1)
 commit_xid     | xid8                     |           | not null | pg_current_xact_id()
 server_seq     | bigint                   |           | not null | generated always as identity
Indexes:
    "PK_outbox_messages" PRIMARY KEY, btree (id)
    "ix_outbox_dispatch" btree (commit_xid, server_seq) WHERE signaled_at IS NULL          <-- D6-2 (yeni)
    "ix_outbox_messages_owner_cursor" btree (owner_id, commit_xid, server_seq)
    "ix_outbox_messages_owner_id" btree (owner_id)
    "ix_outbox_owner_scope" btree (owner_id, scope_id) WHERE scope_id IS NOT NULL          <-- D6-3 (yeni)
    "ux_outbox_messages_commit" UNIQUE CONSTRAINT, btree (commit_xid, server_seq)

DOGRULAMA: available_at satirinin Default sutunu BOS (2b1'deki `now()` kaldirildi, D6-1).
attempts (0), commit_xid (pg_current_xact_id()), server_seq (identity) DEGISMEDI -- yalniz
available_at'in DB-default'u kaldirildi, spec'in pinledigi TEK degisiklik.
