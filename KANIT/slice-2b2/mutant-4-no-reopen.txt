==== MUTANT-4: mutant-4-no-reopen ====
Kural: GOREV-slice-2b2-gercek-zaman.md D9 madde 4 -- D8-iii'u kirmasi zorunlu.
Dosya: src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs (PumpOnceAsync)
Komut: dotnet build (once, 0/0) + dotnet test tests/Momentum.Persistence.Tests/Momentum.Persistence.Tests.csproj --nologo --blame-hang-timeout 120s

--- (a) UYGULANAN MUTASYONUN TAM DIFF'I ---
diff --git a/src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs b/src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs
index af51259..922934a 100644
--- a/src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs
+++ b/src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs
@@ -58,22 +58,13 @@ public sealed class OutboxDispatcher : BackgroundService

         var publisher = scope.ServiceProvider.GetRequiredService<ISignalPublisher>();
         var envelopes = BuildEnvelopes(rows);
-        var failures = await publisher.PublishAsync(envelopes, cancellationToken);
-        var failedGroups = failures.Select(f => f.Group).ToHashSet(StringComparer.Ordinal);
+        await publisher.PublishAsync(envelopes, cancellationToken);

         var okIds = new List<Guid>();
         var reopenRows = new List<(Guid Id, int BackoffSeconds)>();
         foreach (var row in rows)
         {
-            // D1 YB-1: a row is closed only if EVERY group it belongs to succeeded.
-            if (GroupsFor(row).Any(failedGroups.Contains))
-            {
-                reopenRows.Add((row.Id, Backoff(row.Attempts)));
-            }
-            else
-            {
-                okIds.Add(row.Id);
-            }
+            okIds.Add(row.Id);
         }

         await claimStore.CloseAsync(okIds, reopenRows, now, cancellationToken);

(NOT: `failures`/`failedGroups`'un kaldirilmasi CS0219/unused-variable derleme hatasindan (warnaserror)
 kacinmak icindir -- ayni tek-amacli mutasyonun (basarisizligi yok say, hep kapat) dogal parcasi.)
(build: 0 Uyari / 0 Hata)

--- (b) KIRILAN TUM TESTLER (gercek koşum ciktisi, 31/32 yesil / 1/32 FAIL) ---
1) Momentum.Persistence.Tests.DispatcherTests.Publish_failure_reopens_the_row_and_the_next_pump_retries_it  [FAIL] -- HEDEF
   Assertion (satir 206): (await Db.ScalarAsync<bool>(...,"SELECT signaled_at IS NULL...")).ShouldBeTrue();
   Shouldly.ShouldAssertException: should be True but was False
   (basarisiz yayina ragmen satir KAPATILDI -- geri-acma/yeniden-yayin kalici olarak kayboldu)

Diger 31 test YESIL kaldi (bu mutasyon yalniz "kismi basarisizlik -> geri-acma" yolunu etkiler; H9'da
hicbir yayin hic basarisiz olmadigindan H9 etkilenmez).

--- (c) GIT CHECKOUT SONRASI YESIL KOSUM ---
git checkout -- src/backend/Momentum.Infrastructure/Sync/OutboxDispatcher.cs
dotnet test tests/Momentum.Persistence.Tests/Momentum.Persistence.Tests.csproj --filter "FullyQualifiedName~Publish_failure_reopens" --nologo --blame-hang-timeout 120s
=> Basarili! - Basarisiz: 0, Basarili: 1, Atlanan: 0, Toplam: 1

SONUC: ISIRDI (D8-iii, hedef). Kor kapi degil.
